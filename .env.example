# Firebase Web app config (App Check + frontend SDK)
# T1 v3.41 — Production values는 .env.local에 별도 보관 (gitignored)
# Phase A monitoring mode → Phase B enforce ON

VITE_APP_CHECK_SITE_KEY=<reCAPTCHA v3 site key>
VITE_FIREBASE_API_KEY=<Firebase project Web app apiKey>
VITE_FIREBASE_APP_ID=<Firebase Web app appId>
