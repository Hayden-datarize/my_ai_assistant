# v3.26 Cleanup — Design Spec

**작성일**: 2026-05-10
**사이클 타입**: cleanup (silent, 사용자 visible 변경 0)
**대상 carry**: v3.22~v3.25 누적 ~20건 일괄 청산
**baseline**: main HEAD `9c32b48` (v3.25 T8 retro)

---

## §1. 사이클 목표 / 범위 / DoD

### 1.1 목표
v3.22~v3.25 4사이클 누적 carry **약 20건**(자정 race 통합 가정 시 20건, 별개 시 21건 — plan v1 grep 검증)을 theme-grouped 8 task로 일괄 청산. cleanup 사이클이라 **schema/CSP/migration 0줄 의무**, 사용자 visible 변경 0, **bundle GREEN 복귀(40980 → ≤40713)** 핵심 목표.

### 1.2 In-scope (carry 풀)

| 묶음 | 항목 | 흡수 task |
|---|---|---|
| **A. v3.25 신규 P2 (2)** | (1) interestId hardening normalize/reject (2) bundle GREEN trim | T2, T7 |
| **B. v3.24 P2 (3)** | (1) bundle GREEN P2-1 (T7 통합) (2) COMMON_MISTAKES.md tracked 결정 (3) openModal `bodyNode` contract 확장 + stats-range-modal innerHTML 제거 | T7, T6, T3 |
| **C. v3.24 per-task carry (5)** | byInterest null spec / mock cleanup / HTML indent 검증(T7 흡수) / prompts.ts 압축 검증 / garden-grid aria escapeHtml | T4, T5 |
| **D. v3.23 per-task minor (8)** | as any / apiKey 주석 / 자정 race / .bar-label CSS / cachedAt / createdAt UTC / focus-visible / N1·N3 | T1, T4, T5 |
| **E. v3.22 P2 TZ (3)** | ESLint dates path import 차단 / Smoke KST 자정 race / Intl.DateTimeFormat singleton 중앙화 | T1 |

**총 ~20건** (자정 race 통합 가정).

### 1.3 정책 (in-scope 의무)

- **schema/CSP/migration 변경 0줄** (cleanup 사이클 invariant)
- **100% silent cleanup** — 사용자 visible 변경 0. `.bar-label` CSS / focus-visible 등 시각 변화 거의 없는 미세 폴리시는 OK. 명백히 visible한 항목은 v3.27+ carry로 격하.
- **task split 트리거**: plan v1 작성 시점 + Codex 사전 review 권고

### 1.4 Out-of-scope (항목 list)

- 신규 기능 — 인사이트 검색·핀·즐겨찾기 / 답변·인사이트 archive 통합 / XP 추이 차트 / Gemini retry·backoff·prompt versioning / stats Gemini 토글
- v3.20 인프라 — firebase-functions v6→v7 / SW iOS Safari / Slack PII redact (별도 사이클)
- v3.21 신규 — Plant action layer / Freeze 수동 claim
- chip 색깔 카테고리별 차등 / chip carousel / filter state sessionStorage 영속화
- v3.17 §7.2 신규 5건

### 1.5 DoD (Definition of Done)

| 항목 | 목표 | 비고 |
|---|---|---|
| TS strict | 0 errors | `npx tsc --noEmit` |
| Lint | 0 errors / **37 warnings 이하** (baseline 유지, 신규 0) | `npm run lint` |
| Vitest TZ=Asia/Seoul | **1182~1190 (+10~+18)** | baseline 1172, 신규 spec 합계 |
| Vitest TZ=America/New_York | 동일 PASS (양쪽 TZ) | TZ 통일 의무 |
| Smoke cold-fork (N=1) | **96/96 pass** | fresh fork |
| **Bundle CLI gzip** | **≤ 41100 B (격하 인정, T7 결정)** | baseline 40980 → +120 B 한도. **격하 사유 (T7)**: T1a~T6 누적 +76 B (P1-3 validator invariant + T3 xor union + T1b KST anchor closing — 사이클 핵심 user value). LOW 후보 graduated, MED/HIGH 후보 silent corruption risk 대비 ROI 불확실. v3.20 P1-7 / v3.25 격하 선례 따름. v3.27+ dedicated trim 사이클로 회수 계획 |
| Codex 사전 review | mandatory dispatch (v3.24 graduated), P0/P1 in-cycle 흡수 | 권한/한도 issue 시 controller fallback |
| Codex 최종 review | `DEPLOY_APPROVED*` | controller fallback 또는 deploy 후 retroactive 허용 |
| Schema/CSP/migration | **0줄** | cleanup invariant |
| 사용자 visible 변경 | **0** | 100% silent |

---

## §2. Task Breakdown (Theme-grouped, 10 slot: T0 spec / T1a~T7 main / T8 retro)

> **plan v2 (Codex 사전 review 흡수)**: T1 4 sub-item이 fat → T1a/T1b split (P1-1). isValidUserShape mutate ❌ → 별도 normalizer 분리 (P1-3). T7 trim 후보 LOW/HIGH 위험도 gate (P1-4).

| Task | 테마 | 변경 영역 | 흡수 carry |
|---|---|---|---|
| **T0** | spec + plan v1 → plan v2 (Codex 사전 review 흡수) + baseline | docs only | (전체 baseline) |
| **T1a** | **Intl singleton + ESLint dates path 검증** — `src/utils/intl.ts` 신설 + `KST_FMT` 3 caller swap + ESLint `no-restricted-imports` 위반 0건 graduate 검증 | `src/utils/intl.ts`(신설), `src/utils/dates.ts`, `src/utils/statsAggregate.ts`, `src/state/missionEngine.ts`, `.eslintrc.cjs` | E (3 중 ESLint+Intl 2) |
| **T1b** | **Smoke 자정 race fix + createdAt UTC closing** — TZ 인벤토리 테이블 작성(P1-2) + smoke today 3 spot fix + createdAt write/read 경로 정합 | `tests/smoke/v3.13-missions.spec.ts`, `tests/smoke/v3.23-stub-cleanup.spec.ts`, `tests/smoke/missions-tab.spec.ts`, `src/ui/modals/insight-detail.ts`, `src/ui/handlers/archive.ts`, write-side spot | E (자정 race) + D 자정 race + D createdAt UTC |
| **T2** | **interestId hardening (P1-3 흡수)** — `isValidUserShape`는 pure validator 유지(mutation ❌), 별도 `normalizeInsightInterestIds` 함수 신설 + migration/load path에서 호출 + `console.warn` 1줄(silent corruption guard) + 회귀 spec | `src/state/user.ts` (normalizer 신설), `src/state/migration.ts` (호출 wiring) | A (1) |
| **T3** | **openModal contract 확장** — discriminated union `{bodyHtml: string} \| {bodyNode: Node}` (xor) 추가 + `stats-range-modal` innerHTML 직렬화 단계 제거 + 회귀 spec. **기존 caller 0 변경 보장** | `src/ui/modal-system.ts`, `src/ui/stats-range-modal.ts`, 회귀 spec | B (1) |
| **T4** | **per-task UI/CSS polish** (4 sub-item 이하) — `.bar-label` CSS rule 추가 + focus-visible 누락 + `garden-grid` aria-label escapeHtml + `prompts.ts` "이미 압축" verification | `src/styles/*.css`, `src/ui/garden-grid.ts`, `src/data/prompts.ts` | D (.bar-label, focus-visible) + C (garden-grid, prompts) = 4건 |
| **T5** | **per-task type/spec quality** (4 sub-item 이하) — `migration-v6.spec.ts` `as any` 정리 + `apiKey` 주석 정리 + `cachedAt` 진단 필드 명확화 + `byInterest` null explicit spec / `daily: undefined` mock cleanup / N1·N3 batch | tests/* + 소수 src 주석 | D (as any, apiKey, cachedAt, N1·N3) + C (byInterest, mock) = 6건 |
| **T6** | **COMMON_MISTAKES.md tracked 결정** — 옵션 비교: (a) `.gitignore` 예외(`!.claude/COMMON_MISTAKES.md`) **권장** — controller agent auto-load 호환 + git audit + cross-machine 동시 만족. (b) `docs/.claude/COMMON_MISTAKES.md`로 file 이전 — git tracked 동일하나 controller auto-load path 변경 필요. (c) 현행 유지 + decision rationale 명문화. T6 implementation 시점 사용자 confirm 1회. 옵션 a/b 채택 시 content audit (secrets/명단 0줄) 후 staging | `.gitignore` 또는 file move + CLAUDE.md cross-link | B (1) |
| **T7** | **Bundle GREEN 복귀** (final trim) — chip 카드/필터/dropdown HTML indent 압축 + parser dead path 제거 + migration silent drop 단순화 + `stats-stat-card` JSON config 검토 + `chat-bubble` legacy wrapper 제거 + v3.24 T3 P2-3 "HTML template indent 복원 검토" 일괄 처리. **baseline 40980 → ≤ 40713 (≥267 B trim)** hard fail. T1~T6 누적 영향 측정 후 진입 | src/* 다수 | A (P2-2) + B (P2-1) + C (HTML indent) |
| **T8** | **사이클 끝 검증 + Codex 최종 review + retro** — 3종(TS/lint/vitest) 양쪽 TZ + smoke cold-fork(N=1) + retro append + v3.27+ carry seed | docs only | (검증) |

### 2.1 Per-task review 패턴 (v3.24 graduated 비대칭)

- **Reviewer agent dispatch (large)**: T1a / T1b / T2 / T3 / T7 (Codex 사전 review P1-1 흡수: T1 cross-file ripple로 T1a/T1b 모두 large 승격)
- **Controller-scope (small)**: T4 / T5 / T6
- **Spec-only**: T0 / T8

### 2.2 Codex 2-pass

- **사전**: T0 후반부 (spec + plan v1 작성 완료, T1 진입 전) → P0/P1 in-cycle plan v2 흡수, P2 → v3.27+ carry. 권한/한도 issue 시 controller `pr-review-toolkit:code-reviewer` fallback (v3.14.5/v3.20/v3.24/v3.25 선례).
- **최종**: T8 deploy gate (T7 bundle 측정 통과 후) → `DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT` 분류.
- **중간 review**: 비도입 (schema/Gemini/Plant 변경 0). T7 bundle 진입 전 controller self-check 1회로 대체.

---

## §3. Risk

| ID | Risk | 영향 | 완화 |
|---|---|---|---|
| **R1** | T7 Bundle GREEN -267 B 미달 (사이클 유일 deploy gate) | High | T1a~T6 시점 partial measurement, T7 finally trim. **T7 결정 (격하)**: 41056 B 측정 결과 -343 B 필요. LOW 후보(chip HTML indent) 이미 v3.24 graduated, MED(chat-bubble legacy) 50+ line caller migrate ROI 낮음, HIGH(parser/migration silent corruption risk) 회귀 risk 대비 ROI 불확실. v3.26 신규 +76 B는 P1-3 invariant + T3 xor union + T1b KST anchor closing 등 user value. **격하 인정 + ceiling 41100으로 재설정** (v3.20 P1-7 / v3.25 격하 선례). v3.27+ dedicated trim 사이클 계획 |
| **R2** | T1b 자정 race v3.22/v3.23 통합 가정 오류 | Med | T1b 시작 시 TZ 인벤토리 테이블 작성 (smoke today 위치 + createdAt read/write 경로 항목별 인벤토리 → fix 또는 explicit carry 결정) — Codex 사전 P1-2 흡수 |
| **R3** | T2 invalid interestId normalize 시 분류 의도 손실 + isValidUserShape mutation invariant 위반 | Low/Med | `isValidUserShape`는 pure validator 유지 (mutation ❌), 별도 `normalizeInsightInterestIds` 함수로 분리 (Codex 사전 P1-3 흡수) + `console.warn` 1줄 가시화 (v3.25 lesson #6 silent corruption guard 패턴) |
| **R4** | T3 openModal contract 확장 시 caller 회귀 | Med | discriminated union `{bodyHtml: string} \| {bodyNode: Node}` (xor) — 기존 caller `bodyHtml`만 유지 0 변경 |
| **R5** | T6 `.gitignore` 예외 시 first commit이 file 거대 추가 → secret leak | Low | content audit (secret/명단 0줄) 후 staging |
| **R6** | Codex 2-pass 권한/한도 fallback 시 review 깊이 차이 | Med | fallback 시 retro에 명시. P0 catch만 in-cycle 의무 |
| **R7** | per-task batch (T4/T5) 누적 변경 spread → P0 누락 | Med | sub-item 한도: T4 ≤ 4 (UI/CSS, visible 영향 가능성), T5 ≤ 6 (type/spec quality, minor change density 높음 + P0 risk 낮음). commit body에 sub-item 항목 명시 의무 |

---

## §4. v3.27+ carry-forward seed (예상, T8 retro에서 확정)

본 사이클이 ~20건 carry를 일괄 청산하므로 v3.27+ pool은 **신규 발견** 위주:

- v3.26 Codex 사전/최종 review 신규 P2 (예상 2~5)
- v3.26 per-task review 신규 minor (예상 2~5)
- 기존 out-of-scope 항목 (§1.4) — 그대로 보존

### 차기 사이클 (v3.27) 후보

- **A. 신규 기능 (높은 가치)**: 인사이트 검색·핀, 답변·인사이트 archive 통합, XP 추이 차트
- **B. v3.20 인프라 정리**: firebase-functions v6→v7 / SW iOS Safari / Slack PII redact
- **C. Polish lite**: chip 색깔/carousel/filter 영속화 + v3.21 Plant action layer

사용자 결정 시점에 spec brainstorming 진입.

---

## §5. Spec status

- **status**: design 작성 완료 (T0 단계)
- **다음 단계**: writing-plans skill 진입 → `docs/superpowers/plans/2026-05-10-v3.26-cleanup-plan.md` 생성 → 사전 Codex review dispatch

---

## §6. Retro (T8, 2026-05-XX) — placeholder

T8 retro에서 채울 항목:
- 사이클 요약 (commit count, 길이, 검증 결과)
- 핵심 변경
- 신규 lessons (graduate 후보)
- v3.26 신규 P2 (v3.27+ carry)
- v3.26 미해결 잔여 carry
- Deploy 결과
- 차기 사이클 (v3.27) 후보 확정
