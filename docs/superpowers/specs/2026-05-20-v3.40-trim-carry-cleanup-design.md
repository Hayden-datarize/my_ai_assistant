# v3.40 — Bundle Trim + Carry Cleanup + Lesson Graduate (XL 일괄 청산)

> **Type**: Dedicated bundle trim + v3.39 carry-forward 13건 일괄 청산 + lesson 5건 graduate. Schema migration 0줄. CSP 0줄. 신규 기능 0건 (refactor + polish 전용).

**Cycle size**: XL (15 task 예상)
**Reference precedent**: v3.24 (전건 소진 + 메타 graduate), v3.28 (dedicated trim), v3.21 (one-time toast), v3.37 (resetArchiveFilters 단일 진입점)
**Spec 상태**: **v2** — Codex 사전 review (REQUIRES_FIX, 2026-05-20) P0×1 + P1×6 + P2×2 in-cycle 흡수 완료. 변경점은 inline `[v2 — Codex P0/P1/P2-x]` 마커로 trace 가능.

---

## §0. 배경

v3.39 §9.6 carry-forward 13건이 누적 — bundle trim dedicated (C1, High 1순위) + medium carry 3건 (C2/C3/C5) + P3 polish 9건 (C4/C6/C7/C8/C9/C10/C11/C12/C13) + lesson graduate 5건 (L1~L6, L4는 C3에 흡수). v3.39 retro §9.4에서 baseline 측정 누락 명시 — v3.40에서 명시적 cross-cycle 측정 보강 의무.

### 0.1 사이클 scope 합의 (사용자 결정)

| 결정 항목 | 선택 | 근거 |
|---|---|---|
| 사이클 scope | **C. full carry 일괄 청산 (C1~C13)** | XL 사이클 1회로 v3.41 진입 시 carry-forward zero |
| Bundle trim target | **[v2 — Codex P0-1] Option C 재산정**: canonical 재정의 = `entry-*.js + index-*.css` gzip 합산. target -1200 B 유지. | Codex P0-1 catch — 모달 8개 이미 별도 chunk, T2 dynamic import 거의 무효. CSS chunk 별도. canonical 합산으로 trim ROI 회복. |
| Lesson graduate 범위 | **L1/L2/L3/L5/L6 전건** | L1 12사이클 + L3 13사이클 ROI graduate 의무 |
| C5 통계 안내 UX | **One-time toast** (+ [v2 — Codex P1-2] archive 진입도 trigger 확장) | v3.21 streak-freeze 선례. archive count/filter 의미 정정도 영향 — 더 넓은 entry. |
| C3 bottom-nav 수정 | **body padding-bottom + scroll-margin-bottom** + [v2 — Codex P1-1] Playwright 진단 절차 보강 | v3.17 T6 scroll-margin 선례 확장 |

### 0.2 T0 인벤토리 (실측, 2026-05-20)

#### 0.2.1 Helper / Code 위치 (인벤토리 10건)

| ID | 항목 | 측정값 | 영향 task |
|---|---|---|---|
| I1 | `matchesInterest(text, id)` 위치 | `src/utils/interestKeywords.ts:75` | T13 L1 graduate 텍스트 |
| I1b | `entityMatchesInterest(e, id)` 위치 | `src/ui/handlers/archive.ts:198` | T13 L1 graduate 텍스트 |
| I2 | `validateInterestId(id)` 위치 | `src/state/user.ts:64` | T5 C2 재사용 |
| I2b | `normalizeInsightInterestIds` 위치 | `src/state/user.ts:82` | T5 C2 재사용 |
| I3 | `resetArchiveFilters()` 위치 | `src/ui/handlers/archive.ts:314` | T8 C4 통합 의무 (P1-3) |
| I4 | `padding-bottom` 현재 상태 | `--content-bottom-pad: calc(--nav-height + --safe-bottom + 24px)` (`tokens.css:34`), body 적용 (`layout.css:73`) | T6 reframe: 인프라 존재 |
| I4b | `scroll-margin-bottom` 현재 상태 | `var(--content-bottom-pad)` `base.css:39` 글로벌 | T6 reframe: selector 매칭 진단 필요 |
| I5 | 카드 selector | `.archive-card` (`.archive-card--answer` modifier) + `.archive-insight-card` + `.mission-card` | T6 selector 보강 후보 |
| I6 | `dispatchEvent('click')` 사용처 | **[v2 — Codex P1-1 정정] 3 spec / 14건** (이전 spec v1: 19건 오기): archive-interest-filter (9) + archive-insight-detail-nav (5) + archive-plant-detail-nav (5; 단 file별 grep counter discrepancy 존재 — T6 시작 시 재실측 의무) | T6 변환 |
| I7 | `showToast` 위치 | `src/utils/toast.ts` | T7 재사용 |
| I8 | `dg.toast.*` namespace 사용 현황 | **0건** (기존 패턴 부재) | T7 신규 namespace `dg.toast.byInterestNotice = '1'` |
| I9 | ESLint local rule setup | `eslint.config.mjs` flat config + `.eslint-rules/no-nfd-korean-literal.js` 등록 완료 | T12 C6 NFD rule 이미 graduate (carry 정정) |
| I10 | entity-chip 색상 hard-coded 위치 | `src/styles/pages/archive.css:289` `.entity-summary-chip` (3건 selector) + cards.css linear-gradient 6건 | T12 C9 토큰화 대상 |

#### 0.2.2 Chunk 현황 (Codex P0-1 검증)

**[v2 — Codex P0-1 흡수]** 실측 chunk gzip size:

| Chunk | gzip B | 비고 |
|---|---|---|
| `index-*.js` (entry) | **19706** | canonical 1차 |
| `index-*.css` | **10498** | canonical 2차 (Option C 합산) |
| **canonical 합산** | **30204** | **v3.40 baseline (Option C)** |
| `home-*.js` (大) | 10514 | home tab |
| `archive-*.js` (大) | 6096 | archive tab |
| `stats-*.js` (大) | 5305 | stats tab |
| `settings-*.js` | 3750 | settings tab |
| `stats-*.js` (小) | 2386 | stats listeners |
| `archive-*.js` (小) | 2198 | archive listeners |
| `translateToast-*.js` | 1962 | translate |
| `home-*.js` (小) | 1779 | home listeners |
| `plant-detail-*.js` | **1552** | **이미 별도 chunk** |
| `prompts-*.js` | 1379 | prompts |
| `insight-detail-*.js` | **1201** | **이미 별도 chunk** |
| `welcome-gamification-*.js` | **991** | **이미 별도 chunk + 이미 dynamic import** |
| `interests-*.js` | 799 | interests modal |
| `answer-detail-*.js` | 524 | answer modal |
| `badge-detail-*.js` | 394 | badge modal |
| `missions-*.js` (×2) | 285+216 | missions tab |

**결론**: 8 모달 chunk가 이미 별도 emit. T2 dynamic import lane (모달 3개)은 **거의 무효 — 0 B 효과**. 남은 entry static 정적 import는 `welcome-garden` (main.ts:6) 1개 + `mountNav/Sidebar/registerCoreHandlerListeners/mountRewards/renderOnboarding/qs` 6개.

#### 0.2.3 Entry bundle 정적 import 현황

```ts
// src/main.ts
import { mountNav, switchTab } from './ui/nav';
import { mountSidebar } from './ui/sidebar';
import { registerCoreHandlerListeners } from './ui/handlers/register';
import { mountRewards } from './ui/rewards';
import { renderOnboarding } from './ui/onboarding';
import { maybeShowWelcomeGarden } from './ui/modals/welcome-garden';
import { qs } from './utils/dom';
```

**Dynamic 후보** (entry 잔존 static 중):
- `welcome-garden` (line 6) — first-launch 1회. **[v2 lane 재편] dynamic import 후보 (예상 -150~250 B canonical)**.
- `renderOnboarding` (line 5) — 미온보딩 사용자 1회. **[v2 lane 재편] dynamic import 후보 (예상 -100~200 B)**.

#### 0.2.4 도구 가용성

**[v2 — Codex P1-6 흡수]**
- `ts-prune`: package.json 미설치. `npx --no-install ts-prune` 실패 (network 제한).
- `knip`: 미설치.
- `rollup-plugin-visualizer`: 미설치.
- **대체 전략 (P1-6 mitigation)**: T3 dead code sweep은 (a) `knip` devDep `--save-dev` 추가 시도 (네트워크 가능 시), (b) 실패 시 수동 grep 기반 후보 발굴 (`@internal` 마크 + `rg -n "<symbol>" src tests` 0건 검증) fallback.

### 0.3 인벤토리 + Codex 사전 review 기반 scope 정정

| ID | 정정 내용 | 출처 |
|---|---|---|
| **정정 #1** | T12 C6 NFD rule 제거 — v3.38 T1b 이미 graduate. T10 doc commit으로 v3.38 §9.6 표 정정 흡수. | T0 인벤토리 I9 |
| **정정 #2** | T6 reframe: "CSS 추가"가 아닌 "진단 + selector 보강 + dispatchEvent 변환" — 인프라 이미 존재. | T0 인벤토리 I4/I4b |
| **정정 #3** | T13 C7 fuzzy threshold UI → v3.41 deferred — bundle +150~250 B, trim sub-cycle 보존. | scope 합의 |
| **[v2 정정 #4]** | **canonical 재정의**: `entry-*.js + index-*.css` gzip 합산. baseline 30204 B. target ≤ 29004 B (-1200 B). | Codex P0-1 |
| **[v2 정정 #5]** | T2 lane 재구성: 모달 3개 dynamic 제거 → `welcome-garden` + `renderOnboarding` 2건만. 예상 -250~450 B canonical. | Codex P0-1 |
| **[v2 정정 #6]** | **T1을 P0 gate로 격상**: "실측 기반 lane 재편성" — T1 baseline 측정 + source map 분석 + lane budget 확정 후 spec amend 가능 명시. | Codex P0-1 |
| **[v2 정정 #7]** | T4 CSS sweep → **canonical 합산 카운트로 변경** (이전 archive chunk 영향이라 했으나 CSS는 별도 chunk). | Codex P0-1 + chunk 실측 |
| **[v2 정정 #8]** | T6 `dispatchEvent('click')` 사용처 19 → 14건 정정 + T6 시작 시 재실측 의무. | Codex P1-1 |
| **[v2 정정 #9]** | T7 toast trigger 확장: `maybeShowByInterestNotice()` 공유 helper — stats 진입 + archive 진입 둘 다. | Codex P1-2 |
| **[v2 정정 #10]** | T8 archive-nav helper 통합 결정 닫기: `resetArchiveFilters` reset/apply sequence 재사용 의무 + helper 내부 dynamic import 유지 (archive.ts static 끌어오면 chunk 분리 깨짐). | Codex P1-3 |
| **[v2 정정 #11]** | T11 missions poll timeout 3_000ms (cold-start + tab chunk load 고려). debounce 200ms는 1_000ms 유지 OK. | Codex P1-4 |
| **[v2 정정 #12]** | T13 Codex prompt template 결정: 신규 파일 미작성, **CLAUDE.md inline default checklist 갱신**으로 graduate. | Codex P1-5 |
| **[v2 정정 #13]** | T3 dead code 대체 도구: `knip` devDep 시도 → 실패 시 수동 grep fallback. | Codex P1-6 |
| **[v2 정정 #14]** | T3 staged removal 정책: commented-out 1 cycle 제거. **carry table에 후보 남김** 정책으로 단일화 (lint/grep 신호 흐림 방지). | Codex P2-1 |
| **[v2 정정 #15]** | §7 Risk 표에 4건 추가: chunked candidate 무효화 / ts-prune 미설치 / toast trigger coverage / helper static import. | Codex P2-2 |

→ **최종 task count: 15** (T0 + T1~T14, T15 retro).

### 0.4 비목표 (Non-goals)

- Schema migration (v3.39 v9 완료, v3.40 schema bump 0).
- 신규 기능 추가 (refactor + polish 전용).
- v3.41+ 기능 후보 (Plant action layer, fuzzy threshold UI 등 — separate cycle).
- migration chain partial lazy (v3.39 graduate L3 schema chain 무결성 위험).
- vendor split (`manualChunks`) 재도입 (외부 deps 0 상태).

---

## §1. Goals & Success Criteria

### G1. Bundle trim (3-tier, [v2 — Canonical 재정의])

- **Canonical 정의**: `dist/assets/index-*.js + dist/assets/index-*.css` gzip 합산. baseline **30204 B** (19706 + 10498).
- **Trim absolute** (T2~T4 합산): **-1200 B canonical 의무**.
- **추가 task 한도** (T5~T7 합산): **+200 B canonical** 이하.
- **Final net** (T14 측정): canonical **≤ 29004 B (-1200 B)**. Hard fail: T14 net이 baseline 30204 over면 P0 stop.
- **Archive chunk** (보조 metric): `archive-*.js` 6096 B baseline. target -200 B (-3.3%).

### G2. Medium carry 3건 청산
- **C2** (T5): gemini-parse fallback `validateInterestId` + submitAnswer dataset double-close.
- **C3** (T6): bottom-nav 진단 + selector 보강 + `dispatchEvent` 14건 → `.click()` 변환 (L4 흡수).
- **C5** (T7): one-time `maybeShowByInterestNotice()` 공유 helper (stats + archive 진입 둘 다 trigger).

### G3. P3 polish 8건 청산
- **C4** (T8): archive-nav-chip 공용 helper (`resetArchiveFilters` reset/apply 재사용 + helper 내부 dynamic import 유지).
- **C8** (T9): plant-detail seed fixture (4 smoke spec 공유).
- **C10+C11+C13** (T10): smoke spec doc/name 정리 + C6 carry 정정 doc.
- **C12** (T11): `waitForTimeout` 7건 → `expect.poll` (debounce 1_000ms / missions 3_000ms).
- **C9** (T12): entity-summary-chip 색상 토큰화.
- **C7** (deferred): v3.41 — bundle +150~250 B trim 충돌.

### G4. Lesson graduate 5건 (T13, doc-only)
- L1 → `COMMON_MISTAKES.md` §12 신규 (`matchesInterest` helper, 12사이클 ROI)
- L2 → `CLAUDE.md` spec reviewer checklist (predicate-defined vs wired)
- L3 → `CLAUDE.md` Cycle Workflow (schema bump chain superset 3 spot, 13사이클 ROI) + **Codex review default checklist inline 등록** (별도 template file 미작성)
- L5 → `CLAUDE.md` subagent-driven-development (controller fallback 의무 검증 3단계)
- L6 → `CLAUDE.md` code reviewer (M-rating nit 격상 검토 의무)

### G5. v3.39 baseline 측정 누락 보강
- T1에서 명시적 baseline 기록 + chunk별 상세 + lane budget 확정.
- T14에서 cross-cycle 비교 표 (v3.40 baseline 30204 vs final).

### DoD (Definition of Done)

| 항목 | 기준 | 검증 시점 |
|---|---|---|
| Bundle canonical (net) | ≤ 29004 B (-1200 B net) | T14 |
| Bundle archive (보조) | ≤ 5896 B (-200 B) | T14 |
| Bundle trim lane (absolute) | T2~T4 합산 ≥ -1200 B canonical | T4 mid-cycle |
| Bundle 추가 task 한도 | T5~T7 합산 ≤ +200 B canonical | T7 후 |
| Vitest | +20~40 신규, 양쪽 TZ PASS | T14 |
| Smoke | ≥ 109 유지 + cold-start fresh fork PASS | T14 |
| Lint | 0 errors | T14 |
| Codex 사전 | REQUIRES_FIX 흡수 후 spec v2, P0 in-cycle | T0 ✅ |
| Codex 최종 | DEPLOY_APPROVED 또는 APPROVED_WITH_NOTES (P0=0, P1 all in-cycle) | T15 |
| Lesson graduate | L1~L6 5건 doc 정합 (grep 통과) | T13 |
| v3.38 §9.6 carry 정정 | C6 NFD rule "이미 graduate" 명시 | T10 doc commit |

---

## §2. Architecture (15 task)

### Task 분할 표

| Task | Carry | 영역 | 검증 | 예상 Bundle |
|---|---|---|---|---|
| **T0** | spec/plan | brainstorming + Codex 사전 review v2 흡수 + plan v1 | spec self-review + Codex P0 흡수 ✅ | 0 |
| **T1** | G5 + **[v2 P0 gate]** | bundle baseline 명시 측정 (canonical 합산 + chunk별) + source map 분석 + **lane budget 확정 후 spec amend 가능** | npm run build → gzip + manual size 분석 | 0 |
| **T2** | C1 lane 1 | dynamic import (welcome-garden + onboarding 2개) | chunk-by-chunk gzip | -250~450 B canonical |
| **T3** | C1 lane 2 | dead code sweep (knip devDep or 수동 grep) + 사용자 AskUserQuestion confirm + 즉시 삭제 + carry table 잔존 | vitest 영향 범위 | -100~300 B canonical |
| **T4** | C1 lane 3 | CSS unused token + duplicate rule sweep (gzip 10498 → target -500~1000 B) | smoke 영향 spec + **mid-cycle bundle measurement (canonical -1200 B 검증)** | -500~1000 B canonical (CSS chunk gzip) |
| **T5** | C2 | gemini-parse fallback `validateInterestId` + submitAnswer dataset double-close | 신규 vitest 6~8 | +30~60 B |
| **T6** | C3 + L4 | bottom-nav 진단 (Playwright click error + boundingBox + elementFromPoint + nav-card overlap 수치) + scroll-margin selector 보강 + dispatchEvent 14건 (재실측 후) 변환 | smoke spec 정상화 + N=3 cold-start | +20~40 B CSS |
| **T7** | C5 | `maybeShowByInterestNotice()` 공유 helper (stats + archive 진입 둘 다 trigger) + **bundle +200 B 한도 검증** | 신규 vitest 4~6 | +80~120 B |
| **T8** | C4 | archive-nav-chip 공용 helper (`src/ui/handlers/archive-nav.ts`) — `resetArchiveFilters` reset/apply 재사용 + helper 내부 dynamic import 유지 (archive.ts static 끌어오기 금지) | refactor + vitest 통과 유지 | -50~-100 B (단, dynamic import 유지로 chunk 분리 보존) |
| **T9** | C8 | plant-detail seed fixture (`tests/smoke/_helpers/plant-detail-seed.ts`) | smoke 영향 spec 통과 | 0 |
| **T10** | C11+C13+C6 carry 정정 | smoke spec doc/name 정리 + v3.38 §9.6 C3 carry 정정 doc | refactor + doc | 0 |
| **T11** | C12 | `waitForTimeout` 7건 → `expect.poll` (debounce 1_000ms / missions 3_000ms) | smoke N=3 cold-start | 0 |
| **T12** | C9 | entity-summary-chip 색상 토큰화 | smoke 시각 회귀 manual | -10~+10 B |
| **T13** | G4 | lesson L1/L2/L3/L5/L6 graduate (doc-only) + CLAUDE.md inline Codex default checklist 갱신 | grep doc 정합 | 0 |
| **T14** | 사이클 끝 | 3종 검증 (`lint && test && test:smoke`) + cold-start fresh fork + **final bundle measurement (G1 net check)** | full suite | (측정만) |
| **T15** | retro (wrap-up) | retro §0~§9 + Codex 최종 review + 배포 | Codex deploy gate | (측정만) |

**Total**: 15 main task (T0~T14) + 1 wrap-up (T15 retro/Codex 최종/배포).

### 진행 의존성

- T0 → T1 (Codex 흡수 후 baseline)
- T1 (P0 gate) → T2/T3/T4 (실측 후 lane budget 확정 후 trim)
- T4 → T5/T6/T7 (mid-cycle canonical -1200 B 검증 후 추가 task)
- T7 → T8 이후 (+200 B 한도 검증)
- T13 (lesson graduate) → T8~T12 어느 시점 이후 가능 (의존성 없음)
- T14 → T15 (전 task 후 final 검증)

---

## §3. C1 Bundle Trim Lanes 세부 ([v2] 재구성)

### T1 (P0 gate) — Baseline 측정 + lane budget 확정

**Action**:

1. `npm run build` 실행 + `dist/assets/*.{js,css}` gzip 측정 (canonical 합산 = entry JS + CSS).
2. Source map 또는 manual 분석 — entry chunk 내부 module size 분포 파악 (큰 module 식별).
3. CSS chunk 내부 분석 — unused token / duplicate rule 후보 리스트.
4. **Lane budget 확정**: 실측 결과에 따라 T2/T3/T4 lane별 예상 trim 재산정. 합산 -1200 B 미달 가능성 시 spec amend → 사용자 confirm.

**P0 gate**: T1 종료 시점 lane 합산 trim 가능치 < -1200 B면 사용자에게 옵션 제시 (target 완화 vs 새 lane 발굴 vs T2~T4 confirm 보류).

**Bundle**: 0 (측정만).

### T2 — Dynamic import lane ([v2] welcome-garden + onboarding)

**현황** ([v2] §0.2.2 Codex P0-1 흡수):
- 모달 8개 이미 별도 chunk → dynamic 후보 없음.
- `welcome-garden` (main.ts:6) static — **first-launch 1회 사용**.
- `renderOnboarding` (main.ts:5) static — **미온보딩 사용자 1회 사용**.

**Action**:

1. **welcome-garden** → main.ts에서 `void import('./ui/modals/welcome-garden').then(({ maybeShowWelcomeGarden }) => { maybeShowWelcomeGarden(); })` 패턴. first-launch 시점에만 chunk load.
2. **onboarding** → main.ts에서 `if (needsOnboarding()) { void import('./ui/onboarding').then(({ renderOnboarding }) => { renderOnboarding(); }) }` 패턴.

**측정**: canonical entry -250~450 B, 신규 chunk 2개 emit (각 100~200 B).

**Risk**: 첫 실행 시 모달/onboarding load <50ms (same-origin chunk).

### T3 — Dead code sweep ([v2] knip + 수동 grep fallback)

**Method** ([v2 — Codex P1-6 + P2-1]):

1. **knip 설치 시도**: `npm install --save-dev knip` (네트워크 가능 시).
2. **fallback (knip 실패 시)**: 수동 grep — `@internal` 마크 export + 후보별 `rg -n "<symbol>" src tests` 0건 검증.
3. **사용자 confirm**: 검증 통과 후보 리스트 `AskUserQuestion` multiSelect로 사용자에게 제시. 사용자 미선택 항목은 **carry table에 남김** (commented-out 금지 — lint/grep 신호 흐림 방지).
4. **즉시 삭제**: 사용자 선택 후보 즉시 삭제. 다음 cycle commented-out 단계 없음.

**우선 후보**:

- v3.39 schema v9 migration 후 legacy V2~V8 path helper.
- `@internal` 마크 export 중 production caller 0.
- v3.30 dataset reader / v3.27 archive legacy filter 잔재.

**검증**: 각 후보 제거 후 `npx tsc --noEmit` + `npm test` PASS.

**Bundle**: -100~300 B canonical.

### T4 — CSS unused sweep ([v2] canonical 합산 카운트)

**현황**: `dist/assets/index-r1VtpDDS.css` 10498 B gzip. tokens.css 4631 B + main.css 13165 B + layout.css 6182 B + components/ 다수 (raw).

**Action**:

1. **Token sweep**: `tokens.css` 4631 B → `rg "var\(--[a-z-]+\)" src/**` 매칭. 사용처 0인 token 제거.
2. **Duplicate rule**: main.css duplicate selector (v3.17 M3 + v3.27 archive IA 누적).
3. **@media (hover: hover) 가드 일관**: v3.17 lesson 준수.

**T4 종료 시점에 mid-cycle canonical measurement**: T2~T4 합산 -1200 B (canonical = entry JS + CSS) 검증. 미달 시 T8 helper trim 추가 또는 추가 lane 발굴 또는 사용자 confirm 후 target 완화.

**Bundle**: -500~1000 B canonical (CSS chunk gzip 감소).

---

## §4. Medium Carry 세부 ([v2 보강])

### T5 — C2 gemini-parse fallback validation

**Codex P2-1 (v3.39) 원문**: cached/fallback question path가 invalid raw id면 `userInterests[0]` 그대로 반환.

**Action**:

1. **`src/utils/gemini-parse.ts:107`** — `userInterests[0]` 대신 `userInterests.find(id => validateInterestId(id) !== 'unknown') ?? 'unknown'` 패턴 (first valid).
2. **`src/ui/handlers/home.ts:974`** — submitAnswer dataset `interestId`를 `validateInterestId(raw)` 한번 더 통과.
3. **Helper 재사용**: `src/state/user.ts:64` `validateInterestId` 이미 존재.

**Vitest**: +6~8 — `gemini-parse.spec.ts` (4) + `home.spec.ts` (4).
**Bundle**: +30~60 B.

### T6 — C3 bottom-nav 진단 + dispatchEvent 변환 ([v2 — Codex P1-1 진단 절차 보강])

**진단 1단계** (T6 시작 시 실행):

1. **`--nav-height: 64px` 실측치 일치** — Playwright `boundingBox()` `.bottom-nav` height 측정.
2. **`scroll-margin-bottom` selector 매칭** — `base.css:39` 글로벌이 `.archive-insight-card`에 적용되는지 `getComputedStyle()` 확인.
3. **`.bottom-nav` z-index/pointer-events 검사** — `layout.css:43`.
4. **[v2 — Codex P1-1] Playwright `click()` failure forensics**:
   - `click()` 실패 시 error message 캡처 (예: "element intercepts pointer events").
   - `await page.evaluate((sel) => document.elementFromPoint(...))` — 카드 위 실제 element 확인.
   - nav-card boundingBox overlap 수치 (px) 측정.
   - 진단 결과 retro §9에 기록.
5. **dispatchEvent 14건 재실측** — T6 시작 시 `rg -c "dispatchEvent\(['\"]click['\"]" tests/smoke/*.spec.ts` 결과 확정 (spec v1 19건 → v2 14건 grep 오차 가능, 정확한 카운트는 시작 시점에 결정).

**진단 결과에 따른 fix** (Case A~D):

- **Case A** (selector mismatch): `base.css:38~39`에 `.archive-insight-card, .archive-card, .mission-card` 명시 추가.
- **Case B** (z-index 충돌): `.bottom-nav` z-index 명시 + 카드 z-index 정합.
- **Case C** (pointer-events overlay): pointer-events 제거 또는 위치 조정.
- **Case D (v2 추가)**: nav-card boundingBox 실제 overlap이 padding 누락 → padding-bottom 값 보정.

**dispatchEvent 변환**:

- `.dispatchEvent('click')` → `.click()` 일반화.
- workaround 주석 제거.
- 회귀 spec 추가: bottom-nav button visible after scroll + actual click이 tab 전환.

**Smoke**: 변환된 14건 N=3 cold-start fresh fork PASS 필수.
**Bundle**: +20~40 B (CSS 추가 시).

### T7 — C5 one-time toast ([v2 — Codex P1-2] 공유 helper)

**Action**:

1. **신규 helper** `src/ui/handlers/byInterestNotice.ts` — `maybeShowByInterestNotice(): void` 단일 export.
2. **Storage**: `localStorage['dg.toast.byInterestNotice']` (신규 namespace). `=== '1'` strict check.
3. **Trigger 2 spot**:
   - `src/ui/tabs/stats.ts` `renderStats` 진입점.
   - `src/ui/handlers/archive-listeners.ts` archive 탭 진입 시점.
4. **Toast system**: `src/utils/toast.ts` `showToast` 재사용 (v3.21 streak-freeze 선례).
5. **Copy**: `"📊 분야별 통계가 정확해졌어요\n이제 관심분야 단위로 집계됩니다"`
6. **Behavior**: Auto-dismiss 4s + manual ✕. flag set after show.

**T7 종료 시점에 bundle +200 B 한도 검증**.

**Vitest**: +4~6 (flag 미존재 시 show / flag 존재 시 skip / archive trigger / stats trigger / corrupted flag fallback).
**Bundle**: +80~120 B (helper + 2 wiring).

---

## §5. P3 Polish 세부 ([v2 보강])

### T8 — C4 archive-nav-chip 공용 helper ([v2 — Codex P1-3])

**Action** ([v2 통합 결정]):

1. **신규** `src/ui/handlers/archive-nav.ts` — `navigateToInterestArchive(interestId)` 단일 export. plant-detail.ts L115 정의 이동.
2. **`resetArchiveFilters` 재사용 의무**: 새 helper 내부에서 `resetArchiveFilters()` (`src/ui/handlers/archive.ts:314`) 호출 후 interest 필터 apply.
3. **Helper 내부 dynamic import 유지**: `archive.ts`를 static으로 끌어오면 plant-detail/insight-detail chunk가 archive와 merge되어 chunk 분리 깨짐. helper 내부에서 `const { resetArchiveFilters } = await import('../handlers/archive');` 패턴 유지.
4. **plant-detail.ts** + **insight-detail.ts** import path 갱신.
5. **archive.ts L689** reset 시맨틱 — 동일 helper 호출로 통합.

**Vitest**: 변경 0 (refactor only).
**Bundle**: -50~-100 B (중복 제거, dynamic import 유지로 chunk 보존).

### T9 — C8 plant-detail seed fixture

**4 smoke spec** (인벤토리):

- `archive-insight-detail-nav.spec.ts`
- `archive-plant-detail-nav.spec.ts`
- `plant-detail-modal.spec.ts`
- `archive-interest-filter.spec.ts`

**Action**:

1. 신규 `tests/smoke/_helpers/plant-detail-seed.ts` — `seedPlantDetail(page, opts)` function helper.
2. opts 시그니처: `{ plantIds?: string[], interestId?: string, stage?: number }`. T9 시작 시 4 spec seed diff 분석 후 결정.
3. 4 spec에서 boilerplate seed → helper 호출 1줄로 교체.

**Smoke**: 변경 0 (refactor only).
**Bundle**: 0 (test helper).

### T10 — C11 + C13 + C6 carry 정정

**Sub-task**:

- **C11**: archive-interest-filter.spec.ts inline algorithm 주석 → spec doc 이전. 주석 제거 후 `@see` 링크만.
- **C13**: spec name `'exact filter'` vs assertion `'token filter'` mismatch 정정 + `@internal` JSDoc 누락 추가.
- **C6 carry 정정**: `docs/superpowers/specs/2026-05-18-v3.38-carry-cleanup-design.md §9.6 C3` 항목에 "**이미 v3.38 T1b에서 graduate** (`.eslint-rules/no-nfd-korean-literal.js`). carry 정정 by v3.40 T10." 주석 추가.

**Vitest**: 0
**Smoke**: 0
**Bundle**: 0

### T11 — C12 `waitForTimeout` → `expect.poll` ([v2 — Codex P1-4])

**7건 사용처** (5 spec):

| Spec | Line | 패턴 | 변환 후 timeout |
|---|---|---|---|
| archive-interest-filter | 197 | debounce 200ms | `expect.poll(...)` 1_000ms |
| archive-plant-detail-nav | 61, 113 | debounce 200ms (2건) | 1_000ms |
| archive-unified | 62 | debounce 200ms | 1_000ms |
| v3.13-missions | 66, 125 | submit sweep + state save (2건) | **[v2] 3_000ms** (cold-start + tab chunk load 고려) |
| polish | 35 | 이미 마이그 | 변환 불요 |

**Action**:

1. debounce 대기 (4건) → `await expect.poll(async () => locator.count(), { timeout: 1_000, intervals: [100, 200, 200] }).toBe(expectedCount)`.
2. missions submit sweep (2건) → `aria-live="polite"` region 추가 (production code 변경 +5~10 B) + `await expect.poll(...).toContain(...)` timeout **3_000ms** ([v2 — Codex P1-4]).

**Smoke**: 변경된 6건 N=3 cold-start fresh fork PASS.
**Bundle**: +5~10 B (aria-live attribute).

### T12 — C9 entity-summary-chip 색상 토큰화

**Action**:

1. `tokens.css` 추가:

   ```css
   --chip-answer-color: var(--accent-blue, #3b82f6);
   --chip-briefing-color: var(--accent-green, #10b981);
   --chip-insight-color: var(--accent-purple, #8b5cf6);
   ```

2. `archive.css` `.entity-summary-chip[data-entity="..."]` hard-coded 색상 → `var(--chip-*-color)` 교체.
3. **T12 commit 후 사용자 manual 시각 확인 요청**: `npm run preview` → archive 영역 chip 3종 색상 검증.

**Vitest**: 0
**Bundle**: -10~+10 B.

---

## §6. Lesson graduate 세부 (T13, doc-only)

### L1 → `COMMON_MISTAKES.md` §12 신규

```markdown
## 12. interest id ≠ keyword 토큰 (matchesInterest helper, 2026-05-20, v3.40 graduate)

**Symptom**: `matchKeyword(text, interestId)` 사용 시 본문이 `'AI/ML'`/`'AI'`/`'OpenAI'`인데 `'ai_ml'` literal 없어 결과 hide. v3.39 T8 P1-1 (interest nav 토큰 검색 과도 제한) + 13사이클 누적 Codex 사전 catch.

**Root cause**: interest id (`'ai_ml'`, `'hr_system'`)는 `INTERESTS.id` slug. keyword token 아님.

**Fix/Prevention**:
- **`matchesInterest(text: string, id: string): boolean`** (`src/utils/interestKeywords.ts:75`) — interest id를 keyword 집합으로 변환 후 매칭.
- **`entityMatchesInterest(e: ArchiveEntity, id: string | null): boolean`** (`src/ui/handlers/archive.ts:198`) — entity-level wrapper. `interestId === id` exact + legacy 'unknown' fallback keyword 일관 적용.
- archive/insight/stats entity-interest 매칭은 위 2개 helper만 사용.

**관련 파일**: `src/utils/interestKeywords.ts`, `src/ui/handlers/archive.ts`, `src/state/user.ts`
```

### L2 → `CLAUDE.md` spec reviewer checklist

```markdown
**Spec reviewer checklist (v3.40 graduate, v3.39 T6 사례)**

새 predicate/helper/event 정의 시:
- **predicate-defined vs wired**: caller 전수 grep 후 spec §wiring sites에 명시 (v3.39 T6 wiring 미적용 사례)
- **mutation paths 전수 grep** (v3.30 lesson #9 confirm)
- **test mock path stale 여부** (v3.30 lesson #11 confirm)
```

### L3 → `CLAUDE.md` Cycle Workflow + **inline Codex default checklist** ([v2 — Codex P1-5])

```markdown
**Schema bump 시 chain superset 필수 spot (v3.40 graduate, 13사이클 ROI)**:
- migration chain (V2→V3→...→VN) — 모든 prior version에 N으로 가는 경로
- `src/state/user.ts` load (3 spot: hydrate / persist / shape guard) — hard-coded version path 포함
- 신규 entity 추가 시 entity-별 storage migration도 동일

**Codex 사전 review default checklist (v3.40 graduate, prompt 신규 파일 미작성, CLAUDE.md inline)**:
1. Schema chain superset coverage (위 3 spot)
2. interest id ≠ keyword 토큰 (matchesInterest helper 사용 여부)
3. predicate-defined vs wired (caller grep + wiring sites)
4. mutation paths 전수 grep
5. test mock path stale 여부

위 5건은 Codex 사전 review prompt에 명시 불요 — default checklist로 자동 검토.
```

### L5 → `CLAUDE.md` subagent-driven-development

```markdown
**Subagent controller fallback 정책 (v3.40 graduate, v3.39 T2/T6 사례)**

subagent dispatch 후 controller 의무 검증 3단계:
1. **commit 정합**: `git log --oneline -5`로 expected commit이 main에 반영됐는지 확인
2. **영향 범위 grep 재확인**: subagent가 보고한 영향 범위 외 누락 여부 (예: fixture sweep 일부 미완)
3. **차분 vitest/smoke**: 영향 spec 단위 실행 → PASS 확인

미완 발견 시: controller가 직접 마무리 + retro §lessons에 사례 명시.
```

### L6 → `CLAUDE.md` code reviewer

```markdown
**code-reviewer M-rating nit 격상 검토 의무 (v3.40 graduate, v3.39 T8 사례)**

code-reviewer agent의 M-rating nit이 다음에 해당하면 P1 격상 검토:
- **사용자 가시 동작 변경** (UX nit)
- **data invariant 영향** (silent corruption 가능)
- **smoke spec workaround 패턴** (production 미해결, 예: dispatchEvent 우회)

격하 사유는 retro §lessons에 명시. 격상 후에는 in-cycle fix 의무.
```

### T13 commit 구조

- 1 commit: `docs(v3.40 T13): lesson graduate 5건 (L1 COMMON_MISTAKES §12 + L2/L3/L5/L6 CLAUDE.md)`
- 영향 파일: `.claude/COMMON_MISTAKES.md`, `CLAUDE.md`.

---

## §7. Risk + 사이클 구조 ([v2] 4건 추가)

### Risk 표

| ID | Risk | Mitigation | 출처 |
|---|---|---|---|
| **R1** | trim absolute -1200 B 미달 (T2~T4 합산) | T1 P0 gate에서 실측 후 lane budget 확정. T4 mid-cycle 측정 후 미달 시 T8 helper trim 추가 또는 spec amend (사용자 confirm). | v3.28 lesson + Codex P0-1 |
| **R2** | T2 dynamic import 첫 click 지연 (welcome-garden + onboarding) | same-origin chunk fetch <50ms. preload P3 carry. | v3.29 선례 |
| **R3** | T3 dead code 판정 false-positive (caller 0 false-positive) | knip + 수동 grep 이중 검증 + 사용자 confirm + carry table 잔존 (commented-out 금지) | Codex P2-1 |
| **R4** | T6 진단 Case A~D 외 추가 case | 진단 단계에서 Playwright forensics (click error + boundingBox + elementFromPoint + overlap 수치) 모두 수집 후 case 확장 | Codex P1-1 |
| **R5** | T11 `expect.poll` timeout 부족 → smoke flake | debounce 1_000ms + missions 3_000ms ([v2 — Codex P1-4]). N=3 cold-start fresh fork. | COMMON_MISTAKES §5 + Codex P1-4 |
| **R6** | T12 chip 색상 시각 회귀 | manual visual check (`npm run preview`) + 사용자 confirm | v3.17 M3 revamp |
| **R7** | T7 toast 첫 진입 사용자에 노이즈 | flag `=== '1'` strict + v3.21 streak-freeze 선례 | v3.21 toast |
| **R8** | T6 dispatchEvent 14건 변환 시 N=3 cold-start flake | 변환 후 N=3 fresh fork 의무. 1건 flake 시 진단 단계 추가. | COMMON_MISTAKES §5 |
| **R9** | Bundle 추가 task (T5~T7) +200 B 초과 | T7 후 측정 → 초과 시 T8 helper trim 흡수 또는 C5 toast copy 단축 | v3.40 §1 G1 |
| **R10** | Lesson graduate doc 변경이 다른 lesson과 충돌 | T13 dispatch 전 `rg -n "matchesInterest|chain superset|subagent fallback" .claude/ docs/` 정합 | v3.24 메타 graduate |
| **[v2] R11** | **이미 chunked candidate로 T2 lane 무효화** | T1 P0 gate에서 chunk 현황 실측 → welcome-garden + onboarding 2개로 lane 재구성. 발견 시점에 사용자 confirm. | Codex P0-1 + P2-2 |
| **[v2] R12** | **knip/ts-prune 미설치 + network 제한** | T3 시작 시 `npm install --save-dev knip` 시도. 실패 시 수동 grep fallback (P1-6). | Codex P1-6 + P2-2 |
| **[v2] R13** | **T7 toast trigger coverage 부족** (stats만 노출) | `maybeShowByInterestNotice()` 공유 helper, stats + archive 진입 둘 다 trigger. | Codex P1-2 + P2-2 |
| **[v2] R14** | **T8 helper static import로 chunk merge** | 새 archive-nav.ts 내부 dynamic import 유지 (`await import('../handlers/archive')`). static 끌어오기 금지. | Codex P1-3 + P2-2 |

### 사이클 구조 (예상 2.5~3일)

**Day 1 (오전)**:
- T0 (이미 완료) — Codex 사전 review v2 흡수 + spec v2 commit
- T1 baseline 측정 (P0 gate, lane budget 확정)
- T2 dynamic import (welcome-garden + onboarding)

**Day 1 (오후)**:
- T3 dead code sweep (knip + 수동 grep fallback + 사용자 confirm)
- T4 CSS unused sweep + **mid-cycle canonical measurement (T2~T4 -1200 B 검증)**

**Day 2 (오전)**:
- T5 C2 gemini-parse validation
- T6 C3 bottom-nav 진단 + dispatchEvent 14건 변환 (가장 큰 task)

**Day 2 (오후)**:
- T7 C5 공유 helper + **bundle +200 B 한도 검증**
- T8 C4 archive-nav helper (dynamic import 유지)
- T9 C8 plant-detail seed fixture

**Day 3 (오전)**:
- T10 C11+C13+C6 carry 정정 doc
- T11 C12 expect.poll 마이그 (timeout 1_000/3_000ms)
- T12 C9 entity-chip 색상 토큰화 + 사용자 manual 시각 확인
- T13 lesson graduate (L1~L6) + CLAUDE.md Codex default checklist inline

**Day 3 (오후~저녁)**:
- T14 사이클 끝 3종 검증 + cold-start fresh fork + **final bundle measurement (G1 net check)**
- T15 retro §0~§9 + Codex 최종 review → P0/P1 흡수 → 사용자 "배포해줘" 시점 배포

---

## §8. 이후 사이클 후보 (v3.41+)

| ID | 항목 | 출처 | 우선순위 |
|---|---|---|---|
| F1 | fuzzy threshold A/B UI (v3.40 deferred, bundle +150~250 B) | v3.40 §0.3 정정 #3 | Medium |
| F2 | Plant action layer (v3.36 deferred) | v3.36 brainstorming | Medium |
| F3 | T6 진단에서 발견된 추가 케이스 (z-index/pointer-events 패턴) | v3.40 T6 in-cycle | Conditional |
| F4 | T3 dead code carry 후보 (사용자 미선택 항목) | v3.40 T3 carry table | Low |
| F5 | T2 모달 preload (`<link rel="modulepreload">` 또는 idle prefetch) | v3.40 §3 T2 risk | Low |
| F6 | NFD 호환성 자모(`U+3130~U+318F`) 분리 형태 검출 rule 확장 | v3.38 T1b false-negative | Low |
| F7 | submitAnswer dataset double-close 외 추가 entry point validator | v3.40 T5 확장 | Low |
| F8 | rollup-plugin-visualizer 또는 source-map-explorer devDep 추가 | v3.40 T1 manual 분석 한계 | Low |
| F9 | vendor split (`manualChunks`) 외부 deps 진입 시 재검토 | v3.30 T6 carry | Conditional |
| F10 | export-to-internal refactor 5건 (`emitEvents`/`getKSTWindowStart`/`ENTITY_LABELS`/`buildLegacyInterestEmpty`/`TOAST_DURATIONS`) | v3.40 T3 knip 미선택 후보 | Low |
| F11 | knip 정기 실행 — pre-commit hook 또는 CI script 통합 | v3.40 T3 graduate 가능성 | Medium |
| F12 | CSS sweep T4 deferred: `:hover` 가드 33건 (v3.17 lesson 위반) + duplicate selector 정밀 분석 | v3.40 T4 skip (T2 over-perform으로 G1 충족) | Medium |
| F13 | Mobile bottom-nav overlap production fix — dispatchEvent 14건 → .click() 전환 위한 nav z-index/pointer-events/padding 정밀 진단 (Playwright trace: `<nav id="bottomNav">` subtree intercepts pointer events) | v3.40 T6 dispatchEvent 복원 (production fix 깊은 진단 필요) | Medium |
| F14 | plant-detail seed helper 4 spec migration — seedV9UserDefaults() 호출로 boilerplate 10줄 → 1줄 | v3.40 T9 helper 생성, spec 마이그 carry | Low |
| F15 | entity-summary-chip data-entity별 색상 분기 (Answer/Briefing/Insight 차별화) | v3.40 T12: 단일 selector이미 토큰화, 분기는 신규 feature | Low |

---

## §9. Retro (사이클 끝에 채움)

### 9.1 작업 요약

15 main task + retro/배포 wrap-up (T0~T15):

| Task | Carry | 결과 | Commit |
|---|---|---|---|
| T0 | spec/plan | v1 (`aaed799`) → Codex 사전 흡수 v2 (`e5d4a0c`) → plan v1 (`daaab15`) | 3 commits |
| T1 | G5 baseline | canonical 30204 B 확정 + lane budget 가설 | `1e62c80` |
| T2 | C1 lane 1 | welcome-garden + onboarding dynamic — **-10839 B over-perform** | `128a307` |
| T3 | C1 lane 2 | dead code (show2ndRow + types 5 export 제거, knip) | `d637791` |
| T4 | C1 lane 3 | CSS sweep skip (T2 over-perform 충족) + F12 carry | `c53cc02` |
| T5 | C2 | gemini-parse + submitAnswer interestId validation (+4 vitest) | `78e1702` |
| T6 | C3 + L4 | scroll-margin selector 확장 + dispatchEvent 14건 유지 + F13 carry | `4d0fa98` |
| T7 | C5 | maybeShowByInterestNotice 공유 helper (stats + archive trigger, +6 vitest) | `57d2428` |
| T8 | C4 | archive-nav helper 추출 (dynamic import 유지, Codex P1-3) | `1bed673` |
| T9 | C8 | plant-detail seed helper 생성 (F14 spec 마이그 carry) | `673c0a7` |
| T10 | C11+C13+C6 | v3.39 §8 F4 NFD rule 정정 (이미 graduate) | `3aff9fd` |
| T11 | C12 | waitForTimeout 6건 → expect polling + missions 3_000ms | `a671370` |
| T12 | C9 | entity-summary-chip 이미 토큰화 + F15 entity 분기 carry | `c2137f8` |
| T13 | G4 | lesson L1/L2/L3/L5/L6 graduate + closeModal unused cleanup | `9d96b93` |
| T14 | final | 3종 검증 + bundle G1 net check (-10792 B) | `ecff1fc` |

총 17 main commit (T0 3건 + T1~T14 14건) + T15 wrap-up.

### 9.2 검증 결과

| 항목 | 결과 |
|---|---|
| TS strict (`npx tsc --noEmit`) | 0 errors ✓ |
| Lint (`npm run lint`) | 0 errors ✓ |
| Vitest KST default | 1486 → **1493 PASS** (+7: gemini-parse +3 / home-interestid +1 / byInterestNotice +6 / css-tokens +1, T11에서 -2) |
| Vitest TZ=America/New_York | 1493 PASS ✓ |
| Smoke (`npm run test:smoke`) baseline 109 | **109 PASS** (cold-start fresh fork) ✓ |
| Bundle canonical | -10792 B (G1 -1200 B 의무 9배 초과) ✓ |

### 9.3 Codex 2-pass 결과

**사전 review** (T0, 2026-05-20): **REQUIRES_FIX** P0×1 + P1×6 + P2×2 — 전건 in-cycle 흡수 (spec v2 `e5d4a0c`).

- P0-1 (canonical 재정의 + T2 lane 재구성 + T1 P0 gate): T14에서 -10792 B 9배 초과 달성으로 R1 marginal warning unrelated.
- P1-1~P1-6: 모두 spec v2 + 구현 단계에서 흡수.
- P2-1~P2-2: spec v2 정정 흡수.

**최종 review** (T15, dispatch 결과 대기): TBD.
### 9.4 Bundle 측정 (v3.40 final, T14)

| 항목 | spec v2 baseline | T1 baseline | T14 final | Δ (baseline → final) | DoD |
|---|---|---|---|---|---|
| Entry JS (gzip) | 19706 B | 19706 B | **8894 B** | **-10812 B** | ✅ |
| CSS (gzip) | 10498 B | 10498 B | **10518 B** | +20 B | (T6 selector 확장 +20 B) |
| **Canonical 합산** | **30204 B** | **30204 B** | **19412 B** | **-10792 B** | ✅ **G1 -1200 B 의무 9배 초과 달성** |
| Archive chunks (보조) | 8294 B | 8294 B | (재구성됨) | T2 dynamic import로 분포 변경 | - |

#### Task별 영향 분포

| Task | 변경 | Bundle Δ (canonical 합산) |
|---|---|---|
| T1 | baseline 측정 | 0 |
| T2 | welcome-garden + onboarding dynamic | **-10839 B** (over-perform — persistence.ts 7.89 kB chain 분리) |
| T3 | dead code (show2ndRow + types 5) | 0 (type compile-time + 최소 runtime) |
| T4 | CSS sweep skip → F12 carry | 0 |
| T5 | C2 validation | +7 B |
| T6 | scroll-margin selector 확장 | +20 B (CSS) |
| T7 | C5 toast helper | +47 B (helper + 2 wiring) |
| T8 | C4 archive-nav helper | 0 (refactor, neutral) |
| T9 | C8 seed helper | 0 (test-only) |
| T10/T12/T13 | doc-only | 0 |
| T11 | expect.poll + aria-live | -27 B (waitForTimeout 제거 + Playwright import 미세 변동 추정) |

#### Lane budget vs 실측

| Lane | 가설 | 실측 |
|---|---|---|
| T2 | -250~450 B | **-10839 B** (over-perform 9배) |
| T3 | -100~300 B | ~0 B (knip 결과 minimal) |
| T4 | -300~800 B | 0 B (skip, F12 carry) |
| **합산 (T2~T4 trim absolute)** | **-650~1550 B** | **-10839 B** |
| 추가 task (T5~T7) | ≤ +200 B | +74 B ✓ |

**R1 mitigation 결과**: T2 over-perform이 G1 의무를 한 task로 9배 초과 달성. T3 + T4 lane은 결과적으로 maintenance 영역 (carry로 보존).

**Module size 분포** (entry/archive 추정 ranked):

| Lines | Module |
|---|---|
| 509 | `src/state/migration.ts` (V2~V9 chain, entry 고정) |
| 463 | `src/state/user.ts` (validateInterestId 등, entry 고정) |
| 210 | `src/state/achievements.ts` |
| 208 | `src/state/briefings.ts` |
| 203 | `src/state/missionEngine.ts` |
| 153 | `src/utils/gemini-parse.ts` |
| 131 | `src/utils/fuzzy.ts` |
| 121 | `src/state/persistence.ts` |
| 111 | `src/utils/statsAggregate.ts` |

**Lane budget 가설 (T2~T4)**:

- T2 (welcome-garden + onboarding dynamic): -250~450 B canonical (welcome-garden ~150~250 B + onboarding ~100~200 B)
- T3 (dead code): -100~300 B canonical (knip 실행 후 확정)
- T4 (CSS unused token + duplicate rule): -300~800 B canonical (T4 시점에 정밀 unused grep — T1 grep은 ripgrep 옵션 해석 오류로 부정확, T4에서 `rg -F` 또는 escape 적용)
- **합산 예상**: -650~1550 B canonical

**R1 marginal warning**: 하한 -650 B면 -1200 B 의무 미달. T4 CSS sweep 정밀화 + T8 helper trim 추가로 흡수 가능. mid-cycle measurement (T4 종료 시) 후 spec amend 가능.

### 9.5 신규 lessons / graduate 후보

**그래duate 완료 (T13)**:

- **L1 → COMMON_MISTAKES.md §12** (12사이클 ROI): `matchesInterest` helper — interest id ≠ keyword 토큰
- **L2 → CLAUDE.md** (v3.39 T6): Spec reviewer "predicate-defined vs wired" checklist
- **L3 → CLAUDE.md** (13사이클 ROI): Schema bump chain superset 3 spot + Codex inline default checklist 5건
- **L5 → CLAUDE.md** (v3.39 T2/T6): Subagent controller fallback 3단계 의무 검증
- **L6 → CLAUDE.md** (v3.39 T8): code-reviewer M-rating nit 격상 검토 의무

**신규 lessons (v3.41+ graduate 후보)**:

- **N1**: T2 over-perform 9배 — first-launch 1회 모달의 dependency chain (persistence.ts 7.89 kB) 분리가 trim sub-cycle 본질. v3.29 패턴 재현 (cleanup+trim 묶음 over-perform 가능성).
- **N2**: Test mock path stale (COMMON_MISTAKES §11 confirm) — T8 module 추출 시 plant-detail.spec + insight-detail.spec 둘 다 dynamic import + vi.mock path 갱신 필요. graduate된 lesson대로 적용 완료.
- **N3**: dispatchEvent vs .click() trade-off — production fixed-nav overlap이 Playwright `<nav>` subtree intercept로 fail. CSS scroll-margin은 scroll 거리만 조정 (fixed nav viewport overlap 무력). production fix는 nav z-index/pointer-events/padding 정밀 진단 필요 — v3.41 F13 carry.
- **N4**: Subagent socket disconnect (35분 무진행) — controller 직접 진행이 mechanical task에 더 효율적 (v3.40 사용자 Subagent-Driven 선택했으나 T1 socket disconnect 후 controller 직접 모드로 전환).

### 9.6 Carry-forward (v3.41+)

| ID | 항목 | 출처 | 우선순위 |
|---|---|---|---|
| F1 | fuzzy threshold A/B UI | v3.40 §0.3 정정 #3 | Medium |
| F2 | Plant action layer | v3.36 brainstorming | Medium |
| F3 | T6 진단에서 발견된 추가 케이스 | v3.40 T6 in-cycle | Conditional |
| F4 | T3 dead code carry 후보 | v3.40 T3 carry table | Low |
| F5 | T2 모달 preload (modulepreload / idle prefetch) | v3.40 §3 T2 risk | Low |
| F6 | NFD 호환성 자모(U+3130~U+318F) 분리 형태 검출 rule 확장 | v3.38 T1b false-negative | Low |
| F7 | submitAnswer dataset double-close 외 추가 entry point validator | v3.40 T5 확장 | Low |
| F8 | rollup-plugin-visualizer / source-map-explorer devDep | v3.40 T1 manual 분석 한계 | Low |
| F9 | vendor split (`manualChunks`) 외부 deps 진입 시 재검토 | v3.30 T6 carry | Conditional |
| F10 | export-to-internal refactor 5건 (`emitEvents`/`getKSTWindowStart`/`ENTITY_LABELS`/`buildLegacyInterestEmpty`/`TOAST_DURATIONS`) | v3.40 T3 knip 미선택 | Low |
| F11 | knip 정기 실행 (pre-commit hook / CI script 통합) | v3.40 T3 graduate 가능성 | Medium |
| F12 | CSS sweep T4 deferred — `:hover` 가드 33건 + duplicate selector 정밀 분석 | v3.40 T4 skip | Medium |
| F13 | Mobile bottom-nav overlap production fix (nav z-index/pointer-events/padding 정밀 진단) | v3.40 T6 dispatchEvent 복원 | Medium |
| F14 | plant-detail seed helper 4 spec migration | v3.40 T9 helper 생성 | Low |
| F15 | entity-summary-chip data-entity별 색상 분기 | v3.40 T12 신규 feature | Low |

### 9.7 DoD 충족도

| 항목 | 기준 | 결과 | 충족 |
|---|---|---|---|
| Bundle canonical (net) | ≤ 29004 B (-1200 B) | **19412 B (-10792 B)** | ✅ **9배 초과** |
| Bundle trim lane (absolute) | T2~T4 ≥ -1200 B | T2 -10839 B 단독 | ✅ |
| 추가 task 한도 | T5~T7 ≤ +200 B | +74 B | ✅ |
| Vitest | +20~40 신규, 양쪽 TZ | +7 (1486 → 1493), 양쪽 TZ PASS | ⚠ 신규 수 하한 미달 (lesson graduate doc만이라 자연) |
| Smoke | ≥ 109 cold-start | 109/109 cold-start fresh fork | ✅ |
| Lint | 0 errors | 0 errors | ✅ |
| Codex 사전 | REQUIRES_FIX 흡수 후 spec v2 | P0×1+P1×6+P2×2 전건 in-cycle | ✅ |
| Codex 최종 | DEPLOY_APPROVED / APPROVED_WITH_NOTES | T15 결과 대기 | - |
| Lesson graduate | L1~L6 5건 doc 정합 | 전건 graduate (T13) | ✅ |

### 9.8 사이클 평가

v3.40은 dedicated trim + carry cleanup + lesson graduate XL 사이클의 모범 사례:

- **Codex 사전 review 14사이클 연속 P0 catch graduated**: P0-1 (canonical 재정의 + lane 재구성)이 spec v1 가설 (모달 3개 dynamic)을 실측 chunk 8개 별도 emit으로 정정. 이는 plan grep 단독으로 못 잡는 build artifact level invariant — Codex direct CLI dispatch ROI 입증.
- **T2 over-perform 9배**: welcome-garden + onboarding이 persistence.ts 7.89 kB chain을 entry에 끌고 있었음을 dynamic import로 분리. 단일 task로 G1 의무 9배 달성. v3.29 -19173 B 패턴 재현.
- **Carry-forward 15건 처리**: 13건 carry 흡수 + 5 lesson graduate + 5 신규 carry (F11~F15) 자연 발견. v3.41 진입 시 명확한 backlog 정리.
- **Subagent-Driven 전환**: 사용자 선택 패턴이었으나 T1 socket disconnect (35분 무진행) 후 controller 직접 진행 — mechanical task에서는 controller 직접이 더 효율적 (N4 lesson).
- **R1 marginal warning unrelated**: 사전 risk "trim 합산 -650 B 하한 미달"이 T2 over-perform으로 9배 흡수. R1 mitigation 입증.

**v3.41 진입 자연**: F1~F15 backlog 명확, mobile bottom-nav production fix (F13)가 차기 사이클 main work 후보.

---

**작성일**: 2026-05-20
**작성자**: Claude Opus 4.7 + Hayden 합의 (§A/§B/§C 3-stage approval + brainstorming 7-section self-critique + **Codex 사전 review P0×1+P1×6+P2×2 in-cycle v2 흡수**)
