# v3.40 — Bundle Trim + Carry Cleanup + Lesson Graduate (XL 일괄 청산)

> **Type**: Dedicated bundle trim + v3.39 carry-forward 13건 일괄 청산 + lesson 5건 graduate. Schema migration 0줄. CSP 0줄. 신규 기능 0건 (refactor + polish 전용).

**Cycle size**: XL (15 task 예상)
**Reference precedent**: v3.24 (전건 소진 + 메타 graduate 묶음), v3.28 (dedicated trim 패턴), v3.21 (one-time toast 패턴), v3.37 (resetArchiveFilters 단일 진입점 패턴)
**Spec 상태**: v1 — Codex 사전 review 대기

---

## §0. 배경

v3.39 §9.6 carry-forward 13건이 누적 — bundle trim dedicated (C1, High 1순위) + medium carry 3건 (C2/C3/C5) + P3 polish 9건 (C4/C6/C7/C8/C9/C10/C11/C12/C13) + lesson graduate 5건 (L1~L6, L4는 C3에 흡수). v3.39 retro §9.4에서 baseline 측정 누락 명시 — v3.40에서 명시적 cross-cycle 측정 보강 의무.

### 0.1 사이클 scope 합의 (사용자 결정)

| 결정 항목 | 선택 | 근거 |
|---|---|---|
| 사이클 scope | **C. full carry 일괄 청산 (C1~C13)** | XL 사이클 1회로 v3.41 진입 시 carry-forward zero |
| Bundle trim target | **표준 -1000 B canonical / -300 B archive** (net) | v3.29 over-perform 패턴 제외, dedicated trim 자연 target |
| Lesson graduate 범위 | **L1/L2/L3/L5/L6 전건** | L1 12사이클 + L3 13사이클 ROI graduate 의무, 나머지는 doc-only 1 task |
| C5 통계 안내 UX | **One-time toast** | v3.21 streak-freeze 선례, +80~120 B 허용 |
| C3 bottom-nav 수정 | **body padding-bottom + scroll-margin-bottom** | v3.17 T6 scroll-margin 선례 확장 |

### 0.2 T0 인벤토리 (실측, 2026-05-20)

스펙 v1 작성 전 10건 인벤토리 grep 결과:

| ID | 항목 | 측정값 | 영향 task |
|---|---|---|---|
| I1 | `matchesInterest(text, id)` 위치 | `src/utils/interestKeywords.ts:75` | T13 L1 graduate 텍스트 |
| I1b | `entityMatchesInterest(e, id)` 위치 | `src/ui/handlers/archive.ts:198` | T13 L1 graduate 텍스트 |
| I2 | `validateInterestId(id)` 위치 | `src/state/user.ts:64` | T5 C2 재사용 |
| I2b | `normalizeInsightInterestIds` 위치 | `src/state/user.ts:82` | T5 C2 재사용 |
| I3 | `resetArchiveFilters()` 위치 | `src/ui/handlers/archive.ts:314` | T8 C4 통합 결정 |
| I4 | `padding-bottom` 현재 상태 | `--content-bottom-pad: calc(var(--nav-height) + var(--safe-bottom) + 24px)` (`tokens.css:34`), `layout.css:73` body 적용 | **T6 reframe: 이미 인프라 존재** |
| I4b | `scroll-margin-bottom` 현재 상태 | `var(--content-bottom-pad)` `base.css:39` 글로벌 | **T6 reframe: archive 카드 selector 매칭 진단 필요** |
| I5 | 카드 selector | `.archive-card` (`.archive-card--answer` modifier) + `.archive-insight-card` + `.mission-card` | T6 selector 보강 후보 |
| I6 | `dispatchEvent('click')` 사용처 | **3 spec / 19 사용처**: archive-interest-filter (9) + archive-insight-detail-nav (5) + archive-plant-detail-nav (5) | T6 19건 변환 |
| I7 | `showToast` 위치 | `src/utils/toast.ts` | T7 재사용 |
| I8 | `dg.toast.*` namespace 사용 현황 | **0건** (기존 패턴 부재) | T7 신규 namespace `dg.toast.byInterestNotice = '1'` 정의 |
| I9 | ESLint local rule setup | `eslint.config.mjs` flat config + `.eslint-rules/no-nfd-korean-literal.js` 등록 완료 | **T12 C6 NFD rule 이미 graduate (carry 정정 필요)** |
| I10 | entity-chip 색상 hard-coded 위치 | `src/styles/pages/archive.css:289` `.entity-summary-chip` (3건 selector) + cards.css linear-gradient 6건 | T12 C9 토큰화 대상 명확 |
| I-NFD | `no-nfd-korean-literal.js` 실제 내용 | 음절 블록(`U+AC00~U+D7A3`) + 자모 영역(`U+1100~U+11FF`) 모두 catch + NFC normalize 검증 | C6 carry 정정 흡수 (T10 doc commit) |
| I-Nav | `.bottom-nav` element + `#bottomNav` | `src/ui/nav.ts:26` qs로 select + `layout.css:43` `.bottom-nav` z-index/position | T6 진단 후보 (z-index/pointer-events 충돌 가능성) |

### 0.3 인벤토리 기반 scope 정정

**정정 #1**: **T12 C6 NFD rule 제거** — v3.38 T1b 이미 graduate. carry는 docs/code 미동기화로 잘못 들어감. T10 doc commit으로 v3.38 §9.6 표 정정 흡수.

**정정 #2**: **T6 reframe** — "CSS 추가"가 아닌 "진단 + selector 보강 + dispatchEvent 19건 변환". 인프라 이미 존재(`--content-bottom-pad`, `scroll-margin-bottom` 글로벌). dispatchEvent 우회가 필요했던 진짜 원인 진단 (z-index/pointer-events/selector mismatch 중 하나).

**정정 #3**: **T13 C7 fuzzy threshold UI → v3.41 deferred** — bundle +150~250 B 영향, trim sub-cycle 보존.

→ **최종 task count: 15** (T0 + T1~T14, T15 retro).

### 0.4 비목표 (Non-goals)

- Schema migration (v3.39 v9 완료, v3.40 schema bump 0).
- 신규 기능 추가 (refactor + polish 전용).
- v3.41+ 기능 후보 (Plant action layer, fuzzy threshold UI 등 — separate cycle).
- CSP/migration storage rewrite (0줄).

---

## §1. Goals & Success Criteria

### G1. Bundle trim (3-tier)
- **Trim absolute** (T2~T4 합산): **-1200 B canonical** 의무.
- **추가 task 한도** (T5~T7 합산): **+200 B canonical** 이하 (C2 validation +30~60 B + C3 CSS +20~40 B + C5 toast +80~120 B = 약 +130~220 B 가산).
- **Final net** (T14 측정): canonical **≤ 18706 B (-1000 B)**, archive **≤ 7994 B (-300 B)**.
- Hard fail: T14 net이 baseline 19706/8294 over면 P0 stop.

### G2. Medium carry 3건 청산
- **C2** (T5): gemini-parse fallback `userInterests[0]` → `userInterests.find(isValid) ?? 'unknown'` + submitAnswer dataset `validateInterestId` double-close.
- **C3** (T6): bottom-nav 진단 + selector 보강 + dispatchEvent 19건 → `.click()` 변환 (L4 흡수).
- **C5** (T7): one-time `byInterestNotice` toast (stats 진입 시 1회).

### G3. P3 polish 8건 청산 (9건 중 C6 제거)
- **C4** (T8): archive-nav-chip 공용 helper 추출 (3 callsite 도달).
- **C8** (T9): plant-detail seed fixture 추출 (4 smoke spec 공유).
- **C10+C11+C13** (T10): smoke spec doc/name 정리 + C6 carry 정정 doc.
- **C12** (T11): waitForTimeout 7건 → expect.poll/aria-live.
- **C9** (T12): entity-summary-chip 색상 토큰화.
- **C7**: v3.41 deferred (bundle +150~250 B trim 충돌).

### G4. Lesson graduate 5건 (T13, doc-only)
- L1 → COMMON_MISTAKES.md §12 신규 (`matchesInterest` helper, 12사이클 ROI)
- L2 → CLAUDE.md spec reviewer checklist (predicate-defined vs wired)
- L3 → CLAUDE.md Cycle Workflow (schema bump chain superset 3 spot, 13사이클 ROI)
- L5 → CLAUDE.md subagent-driven-development (controller fallback 의무 검증 3단계)
- L6 → CLAUDE.md code reviewer (M-rating nit 격상 검토 의무)

### G5. v3.39 baseline 측정 누락 보강
- T1에서 명시적 baseline (canonical + archive + chunk별 source map) 기록.
- T14에서 cross-cycle 비교 표 (v3.39 baseline vs v3.40 final).

### DoD (Definition of Done)

| 항목 | 기준 | 검증 시점 |
|---|---|---|
| Bundle canonical (net) | ≤ 18706 B (-1000 B) | T14 |
| Bundle archive (net) | ≤ 7994 B (-300 B) | T14 |
| Bundle trim lane (absolute) | T2~T4 합산 ≥ -1200 B | T4 mid-cycle |
| Bundle 추가 task 한도 | T5~T7 합산 ≤ +200 B | T7 후 |
| Vitest | +20~40 신규, 양쪽 TZ PASS | T14 |
| Smoke | ≥ 109 유지 + cold-start fresh fork PASS | T14 |
| Lint | 0 errors | T14 |
| Codex 사전 | REQUIRES_FIX 흡수 후 plan v2, P0 in-cycle | T0 |
| Codex 최종 | DEPLOY_APPROVED 또는 APPROVED_WITH_NOTES (P0=0, P1 all in-cycle) | T15 |
| Lesson graduate | L1~L6 5건 doc 정합 (grep 통과) | T13 |
| v3.38 §9.6 carry 정정 | C6 NFD rule "이미 graduate" 명시 | T10 doc commit |

---

## §2. Architecture (15 task)

### Task 분할 표

| Task | Carry | 영역 | 검증 | 예상 Bundle |
|---|---|---|---|---|
| **T0** | spec/plan | brainstorming + plan v1 + Codex 사전 review | spec self-review + Codex P0 흡수 | 0 |
| **T1** | G5 | bundle baseline 명시 측정 (canonical + archive + chunk source map) | npm run build → gzip + rollup-plugin-visualizer | 0 |
| **T2** | C1 lane 1 | dynamic import (plant-detail / insight-detail / welcome-gamification) | chunk-by-chunk gzip | -400~600 B canonical / +200~300 B archive |
| **T3** | C1 lane 2 | dead code sweep (ts-prune 후보 리스트 → 사용자 confirm → staged removal) | vitest 영향 범위 | -200~400 B canonical |
| **T4** | C1 lane 3 | CSS unused token + duplicate rule sweep | smoke 영향 spec + **mid-cycle bundle measurement** | -300~500 B archive + -50~100 B canonical |
| **T5** | C2 | gemini-parse fallback validation (`isValidInterestId`) + submitAnswer dataset double-close | 신규 vitest 6~8 | +30~60 B |
| **T6** | C3 + L4 | bottom-nav 진단 (z-index / pointer-events / selector) + scroll-margin selector 보강 + dispatchEvent 19건 변환 | smoke spec 정상화 + N=3 cold-start | +20~40 B CSS |
| **T7** | C5 | one-time `dg.toast.byInterestNotice` toast (stats 진입 시 1회) | 신규 vitest 4~6 + **bundle +200 B 한도 검증** | +80~120 B |
| **T8** | C4 | archive-nav-chip 공용 helper (`src/ui/handlers/archive-nav.ts`) | refactor + vitest 통과 유지 | -50~-100 B |
| **T9** | C8 | plant-detail seed fixture (`tests/smoke/_helpers/plant-detail-seed.ts`) | smoke 영향 spec 통과 | 0 |
| **T10** | C11+C13+C6 carry 정정 | smoke spec doc/name 정리 + v3.38 §9.6 C3 carry 정정 doc | refactor + doc | 0 |
| **T11** | C12 | waitForTimeout 7건 → expect.poll/aria-live | smoke N=3 cold-start | 0 |
| **T12** | C9 | entity-summary-chip 색상 토큰화 (`--chip-answer/briefing/insight-color`) | smoke 시각 회귀 manual | -10~+10 B |
| **T13** | G4 | lesson L1/L2/L3/L5/L6 graduate (doc-only) + Codex prompt template 갱신 | grep doc 정합 | 0 |
| **T14** | 사이클 끝 | 3종 검증 (`lint && test && test:smoke`) + cold-start fresh fork + **final bundle measurement (G1 net check)** | full suite | (측정만) |
| **T15** | retro (wrap-up) | retro §0~§9 + Codex 최종 review + 배포 | Codex deploy gate | (측정만) |

**Total**: 15 main task (T0~T14) + 1 wrap-up (T15 retro/Codex 최종/배포). 위 표 16 row 중 T15는 사이클 검증 외 마무리 단계.

### 진행 의존성

- T1 → T2/T3/T4 (baseline 후 trim)
- T4 → T5/T6/T7 (mid-cycle bundle 검증 후 추가 task)
- T7 → T8 이후 (+200 B 한도 검증 후 polish)
- T13 (lesson graduate)는 T8~T12 어느 시점 이후 가능 (의존성 없음)
- T14 → T15 (전 task 후 final 검증)

---

## §3. C1 Bundle Trim Lanes 세부 (T2 / T3 / T4)

### T2 — Dynamic import lane

**현황** (I-Inv): `src/main.ts:6` `welcome-garden` 1개만 static. `src/ui/nav.ts:18~22` 5탭 lazy 완료. plant-detail/insight-detail/welcome-gamification 모달은 archive/stats/missions에서 정적 import.

**Action**:
1. **plant-detail** (`src/ui/modals/plant-detail.ts`) — `archive.ts` 진입점에서 `() => import('../modals/plant-detail').then(m => m.openPlantDetail)` 패턴.
2. **insight-detail** (`src/ui/modals/insight-detail.ts`) — 동일 (insight chip 클릭 시점).
3. **welcome-gamification** (`src/ui/modals/welcome-gamification.ts`) — first-launch 1회만 호출, `void import('./ui/modals/welcome-gamification').then(...)` 패턴 in main.ts.

**측정**: 모달 chunk가 entry에서 archive chunk로 이동. canonical -400~600 B, archive +200~300 B.

**Risk**: 첫 클릭 시 모달 load <50ms (same-origin chunk). preload는 P3 carry.

### T3 — Dead code sweep

**Method**:

1. `npx ts-prune` (TypeScript dead code finder) 실행 → 후보 리스트 생성.
2. 후보별 `rg -n "<symbol>" src tests` 0건 검증 (dynamic call / CustomEvent listener 누락 방지).
3. **사용자 confirm**: 검증 통과한 후보 리스트를 `AskUserQuestion`으로 사용자에게 제시 (multiSelect로 제거할 항목 선택). 사용자 미선택 항목은 v3.41 carry.
4. **Staged removal** (옵션): 첫 cycle commented-out 단계 거치고 다음 cycle 실제 삭제 — false-positive risk 큰 후보(public API surface 등)에만 적용. 단순 internal helper는 즉시 삭제.

**우선 후보**:
- v3.39 schema v9 migration 후 legacy V2~V8 path helper.
- `@internal` 마크 export 중 production caller 0.
- v3.30 dataset reader / v3.27 archive legacy filter 잔재 (`a.type` 의미 정정 후 unused).

**검증**: 각 후보 제거 후 `npx tsc --noEmit` + `npm test` PASS.

### T4 — CSS unused sweep + mid-cycle bundle measurement

**현황** (I-Inv): tokens.css 4631 B, main.css 13165 B unminified, layout.css 6182 B, components/ 다수.

**Action**:
1. **Token sweep**: `tokens.css` 4631 B → `rg "var\(--[a-z-]+\)" src/**` 매칭. 사용처 0인 token 제거.
2. **Duplicate rule**: main.css duplicate selector (v3.17 M3 revamp + v3.27 archive IA 재편 누적).
3. **`@media (hover: hover)` 가드 일관**: v3.17 lesson 준수.

**T4 종료 시점에 mid-cycle bundle measurement 실행**: T2~T4 합산 -1200 B 의무 확인. 미달 시 T8 (archive-nav helper) trim 추가 또는 lane 추가 trim.

---

## §4. Medium Carry 세부 (T5 / T6 / T7)

### T5 — C2 gemini-parse fallback validation

**Codex P2-1 원문**: cached/fallback question path가 invalid raw id면 `userInterests[0]`를 그대로 반환. load-boundary normalize가 나중에 정정하지만 write-time invariant 약함.

**Action**:
1. **`src/utils/gemini-parse.ts:107`** — `userInterests[0]` 대신 `userInterests.find(id => validateInterestId(id) !== 'unknown') ?? 'unknown'` 패턴 (first valid).
2. **`src/ui/handlers/home.ts:974`** — submitAnswer dataset `interestId`를 `validateInterestId(raw)` 한번 더 통과 (write-time double-close).
3. **Helper 재사용**: `src/state/user.ts:64` `validateInterestId(id: string): string` 이미 존재 (invalid → `'unknown'` 반환). 추가 helper 정의 불요.

**Vitest**: +6~8 — `gemini-parse.spec.ts` (4: empty / invalid / valid first / valid 2nd) + `home.spec.ts` (4: dataset 정상 / corrupt / cached / fallback).

**Bundle**: +30~60 B (validator 재사용).

### T6 — C3 bottom-nav 진단 + dispatchEvent 19건 변환 + L4 흡수

**진단 1단계** (T6 시작 시 실행):
1. **`--nav-height: 64px` 실측치 일치 확인** — playwright snapshot으로 `.bottom-nav` height 측정.
2. **`scroll-margin-bottom` 글로벌 selector 매칭 확인** — `base.css:39`이 `.archive-insight-card`에 적용되는지 확인.
3. **`.bottom-nav` z-index/pointer-events 검사** — `layout.css:43`. archive 카드와 z-index 충돌 또는 invisible overlay 가능성.

**진단 결과에 따른 fix**:
- Case A (selector mismatch): `base.css:38~39`에 `.archive-insight-card, .archive-card, .mission-card` 명시적 추가.
- Case B (z-index 충돌): `.bottom-nav` z-index 명시 + 카드 z-index 정합.
- Case C (pointer-events overlay): pointer-events 제거 또는 위치 조정.

**dispatchEvent 19건 변환**:
- `archive-interest-filter.spec.ts` (9건), `archive-insight-detail-nav.spec.ts` (5건), `archive-plant-detail-nav.spec.ts` (5건)
- `.dispatchEvent('click')` → `.click()` 일반화
- workaround 주석 제거
- 회귀 spec 추가: bottom-nav button visible after scroll, nav button actual click이 tab 전환

**Vitest**: 0 (smoke 영역).
**Smoke**: 변경된 19건 모두 N=3 cold-start fresh fork PASS 필수.
**Bundle**: +20~40 B (CSS 추가 시).

### T7 — C5 one-time `byInterestNotice` toast

**Action**:
1. **Trigger**: `src/ui/tabs/stats.ts` `renderStats` 진입점에서 flag check.
2. **Storage**: `localStorage['dg.toast.byInterestNotice']` (신규 namespace). `=== '1'` strict check.
3. **Toast system**: `src/utils/toast.ts` `showToast` 재사용 (v3.21 streak-freeze 선례). API 시그니처는 T7 작업 시 정확히 확인.
4. **Copy**: `"📊 분야별 통계가 정확해졌어요\n이제 관심분야 단위로 집계됩니다"`
5. **Behavior**: Auto-dismiss 4s + manual ✕. flag set after show.

**T7 종료 시점에 bundle +200 B 한도 검증**: T5+T6+T7 합산 측정.

**Vitest**: +4~6 (flag 미존재 시 show / flag 존재 시 skip / show 후 flag set / corrupted flag fallback).

**Bundle**: +80~120 B.

---

## §5. P3 Polish 세부 (T8 / T9 / T10 / T11 / T12)

### T8 — C4 archive-nav-chip 공용 helper

**Callsite 3** (인벤토리 확인):
- `src/ui/modals/plant-detail.ts:83` — `navigateToInterestArchive(interestId)` (정의: L115)
- `src/ui/modals/insight-detail.ts:137` — 동일
- `src/ui/handlers/archive.ts:689` — 동일 reset 시맨틱 공유

**Action**:
1. **신규** `src/ui/handlers/archive-nav.ts` — `navigateToInterestArchive(interestId)` 단일 export. plant-detail.ts L115 정의 이동.
2. **plant-detail.ts** + **insight-detail.ts** import path 갱신.
3. **archive.ts L689** reset 시맨틱 — `resetArchiveFilters()` (L314)와 통합 검토. 단일 진입점 의도 명시 (v3.31 L5 / v3.37 T5 precedent).

**Vitest**: 변경 0 (refactor only).
**Bundle**: -50~-100 B (중복 제거).

### T9 — C8 plant-detail seed fixture

**4 smoke spec** (인벤토리 확인):
- `archive-insight-detail-nav.spec.ts`
- `archive-plant-detail-nav.spec.ts`
- `plant-detail-modal.spec.ts`
- `archive-interest-filter.spec.ts`

**Action**:
1. **신규** `tests/smoke/_helpers/plant-detail-seed.ts` — `seedPlantDetail(page, opts)` function helper.
2. **opts 시그니처**: `{ plantIds?: string[], interestId?: string, stage?: number }` — 4 spec seed diff 분석 후 결정 (T9 시작 시).
3. **4 spec에서 boilerplate seed** → helper 호출 1줄로 교체.

**Smoke**: 변경 0 (refactor only, 통과 유지).
**Bundle**: 0 (test helper).

### T10 — C11 + C13 + C6 carry 정정 (smoke spec doc/name + docs sync)

**Sub-task**:
- **C11** (T7 smoke inline algo 주석): `tests/smoke/archive-interest-filter.spec.ts` inline algorithm 주석 → spec doc 이전. 주석 제거 후 `@see docs/superpowers/specs/2026-05-19-v3.39-...md` 링크만.
- **C13** (spec name/JSDoc): spec name `'exact filter'` vs assertion `'token filter'` mismatch 정정 + `@internal` JSDoc 누락 추가.
- **C6 carry 정정** (도큐먼테이션 sync): `docs/superpowers/specs/2026-05-18-v3.38-carry-cleanup-design.md §9.6 C3 "NFD 자모 분리 검출 rule"` 항목에 "**이미 v3.38 T1b에서 graduate** (`/.eslint-rules/no-nfd-korean-literal.js`). carry 정정 by v3.40 T10." 주석 추가.

**Vitest**: 0
**Smoke**: 0
**Bundle**: 0

### T11 — C12 `waitForTimeout` → `expect.poll`

**7건 사용처** (5 spec):
| Spec | Line | 패턴 | 변환 후 |
|---|---|---|---|
| archive-interest-filter | 197 | debounce 200ms | `expect.poll(() => results.count()).toBe(N)` |
| archive-plant-detail-nav | 61, 113 | debounce 200ms (2건) | 동일 |
| archive-unified | 62 | debounce 200ms | 동일 |
| v3.13-missions | 66, 125 | submit sweep + state save (2건) | `aria-live` region 또는 `data-testid` observe |
| polish | 35 | 이미 마이그 | 변환 불요 |

**Action**:
1. debounce 대기 (4건) → `await expect.poll(async () => locator.count(), { timeout: 1_000, intervals: [100, 200, 200] }).toBe(expectedCount)`.
2. submit sweep 대기 (2건) → mission state element `aria-live="polite"` 추가 (production code 변경 가능) + `expect.poll`.

**Smoke**: 변경된 6건 N=3 cold-start fresh fork PASS.
**Bundle**: 0 (aria-live attribute 추가 +5~10 B 가능).

**Risk**: `expect.poll` timeout 부족 → flake. 보수적 1_000ms 시작, 미달 시 1_500ms 확장.

### T12 — C9 entity-summary-chip 색상 토큰화

**현황** (I10 인벤토리):
- `src/styles/pages/archive.css:289~303` `.entity-summary-chip` 정의 (color/background hard-coded 가능)
- `src/styles/components/cards.css:112~127` 6 linear-gradient (카테고리별)
- `src/styles/components/modal.css:41~64` `.plant-action-chip, .insight-archive-nav-chip` 통합 토큰 사용 중

**Action**:
1. **`tokens.css` 추가**:
   ```css
   --chip-answer-color: var(--accent-blue, #3b82f6);
   --chip-briefing-color: var(--accent-green, #10b981);
   --chip-insight-color: var(--accent-purple, #8b5cf6);
   ```
2. **`archive.css`** `.entity-summary-chip[data-entity="answer/briefing/insight"]` hard-coded 색상 → `var(--chip-*-color)` 교체.
3. **T12 commit 후 사용자 manual 시각 확인 요청** (R7 mitigation): `npm run preview` → archive 영역 chip 3종 색상 검증.

**Vitest**: 0
**Bundle**: -10~+10 B (token 재사용 효과 / token 추가 byte).

---

## §6. Lesson graduate 세부 (T13, doc-only)

### L1 → `COMMON_MISTAKES.md` §12 신규

```markdown
## 12. interest id ≠ keyword 토큰 (matchesInterest helper, 2026-05-20, v3.40 graduate)

**Symptom**: `matchKeyword(text, interestId)` 사용 시 본문이 `'AI/ML'`/`'AI'`/`'OpenAI'`인데 `'ai_ml'` literal 없어 결과가 hide됨. v3.39 T8 P1-1 (interest nav 토큰 검색 과도 제한) + 13사이클 누적 Codex 사전 catch.

**Root cause**: interest id (`'ai_ml'`, `'hr_system'`)는 `INTERESTS.id` slug. keyword token 아님. `matchKeyword`는 keyword 비교 전용.

**Fix/Prevention**:
- **`matchesInterest(text: string, id: string): boolean`** (`src/utils/interestKeywords.ts:75`) — interest id를 keyword 집합으로 변환 후 매칭.
- **`entityMatchesInterest(e: ArchiveEntity, id: string | null): boolean`** (`src/ui/handlers/archive.ts:198`) — entity-level wrapper. `interestId === id` exact + legacy 'unknown' fallback keyword 일관 적용.
- archive/insight/stats entity-interest 매칭은 위 2개 helper만 사용.

**관련 파일**: `src/utils/interestKeywords.ts`, `src/ui/handlers/archive.ts`, `src/state/user.ts`
```

### L2 → `CLAUDE.md` spec reviewer checklist (Cycle Workflow 섹션 내)

```markdown
**Spec reviewer checklist (v3.40 graduate, v3.39 T6 사례)**

새 predicate/helper/event 정의 시:
- **predicate-defined vs wired**: caller 전수 grep 후 spec §wiring sites에 명시 (v3.39 T6 wiring 미적용 사례)
- **mutation paths 전수 grep** (v3.30 lesson #9 confirm)
- **test mock path stale 여부** (v3.30 lesson #11 confirm)
```

### L3 → `CLAUDE.md` Cycle Workflow (chain superset 명문화)

```markdown
**Schema bump 시 chain superset 필수 spot (v3.40 graduate, 13사이클 ROI)**:
- migration chain (V2→V3→...→VN) — 모든 prior version에 N으로 가는 경로
- `src/state/user.ts` load (3 spot: hydrate / persist / shape guard) — hard-coded version path 포함
- 신규 entity 추가 시 entity-별 storage migration도 동일
- Codex 사전 review prompt template (`docs/superpowers/templates/codex-review-prompt.md` 또는 controller logic)에 chain superset coverage를 default checklist로 등록 — 별도 명시 불요.
```

### L5 → `CLAUDE.md` subagent-driven-development (controller fallback 의무)

```markdown
**Subagent controller fallback 정책 (v3.40 graduate, v3.39 T2/T6 사례)**

subagent dispatch 후 controller 의무 검증 3단계:
1. **commit 정합**: `git log --oneline -5`로 expected commit이 main에 반영됐는지 확인
2. **영향 범위 grep 재확인**: subagent가 보고한 영향 범위 외 누락 여부 (예: fixture sweep 일부 미완)
3. **차분 vitest/smoke**: 영향 spec 단위 실행 → PASS 확인

미완 발견 시: controller가 직접 마무리 + retro §lessons에 사례 명시.
```

### L6 → `CLAUDE.md` code reviewer (M-rating nit 격상 검토)

```markdown
**code-reviewer M-rating nit 격상 검토 의무 (v3.40 graduate, v3.39 T8 사례)**

code-reviewer agent의 M-rating nit이 다음에 해당하면 P1 격상 검토:
- **사용자 가시 동작 변경** (UX nit)
- **data invariant 영향** (silent corruption 가능)
- **smoke spec workaround 패턴** (production 미해결, 예: dispatchEvent 우회)

격하 사유는 retro §lessons에 명시. 격상 후에는 in-cycle fix 의무.
```

### T13 commit 구조

- **1 commit**: `docs(v3.40 T13): lesson graduate 5건 (L1 COMMON_MISTAKES §12 + L2/L3/L5/L6 CLAUDE.md)`
- 영향 파일: `.claude/COMMON_MISTAKES.md`, `CLAUDE.md`, (선택) `docs/superpowers/templates/codex-review-prompt.md`

---

## §7. Risk + 사이클 구조

### Risk 표

| ID | Risk | Mitigation | 출처 |
|---|---|---|---|
| **R1** | trim absolute -1200 B 미달 (T2~T4 합산) | T1 source map 분석 → lane별 budget 확정 (T2 -500 B / T3 -350 B / T4 -350 B 가설). T4 mid-cycle 측정 후 미달 시 T8 helper trim 추가 또는 carry. | v3.28 lesson |
| **R2** | T2 dynamic import 첫 클릭 지연 | same-origin chunk fetch <50ms. preload P3 carry. | v3.29 선례 |
| **R3** | T3 dead code 판정 오류 (caller 0 false-positive) | `ts-prune` + 사용자 confirm + staged removal (1 cycle commented-out → 다음 cycle 제거) | 일반 dead-code 패턴 |
| **R4** | T6 진단 결과 mitigation 무력 (Case A~C 외) | 진단 단계에서 추가 Case 발견 시 spec amend → Codex 사전 review 재요청 | v3.18 H5 padding cascade 선례 |
| **R5** | T11 `expect.poll` timeout 부족 → smoke flake | 1_000ms 보수 시작 + N=3 cold-start fresh fork. 미달 시 1_500ms 확장. | COMMON_MISTAKES §5 cold-start |
| **R6** | T12 chip 색상 시각 회귀 | manual visual check (`npm run preview`) + 사용자 confirm | v3.17 M3 revamp 패턴 |
| **R7** | T7 toast 첫 진입 사용자에 노이즈 | flag `=== '1'` strict + v3.21 streak-freeze 선례 | v3.21 toast |
| **R8** | T6 dispatchEvent 19건 변환 시 N=3 cold-start flake | 변환 후 N=3 fresh fork 의무. 1건이라도 flake 시 진단 단계 추가 (selector / timing 문제 분리) | COMMON_MISTAKES §5 |
| **R9** | Bundle 추가 task (T5~T7) 합산 +200 B 초과 | T7 후 측정 → 초과 시 T8 helper trim으로 흡수 또는 C5 toast copy 단축 | v3.40 §1 G1 |
| **R10** | Lesson graduate doc 변경이 다른 lesson과 충돌 | T13 dispatch 전 `rg -n "matchesInterest|chain superset|subagent fallback" .claude/ docs/` 정합 | v3.24 메타 graduate 선례 |

### 사이클 구조 (예상 2.5~3일)

**Day 1 (오전)**:
- T0 spec v1 작성 → Codex 사전 review dispatch → P0/P1 흡수 → plan v1 → spec v2
- T1 baseline 측정 (canonical + archive + chunk source map + lane budget 확정)
- T2 dynamic import (plant-detail + insight-detail + welcome-gamification)

**Day 1 (오후)**:
- T3 dead code sweep (ts-prune → 사용자 confirm → staged removal)
- T4 CSS unused sweep + **mid-cycle bundle measurement (T2~T4 -1200 B 검증)**

**Day 2 (오전)**:
- T5 C2 gemini-parse validation
- T6 C3 bottom-nav 진단 + dispatchEvent 19건 변환 (가장 큰 task — 진단 + 수정 + smoke N=3)

**Day 2 (오후)**:
- T7 C5 one-time toast + **bundle +200 B 한도 검증**
- T8 C4 archive-nav helper
- T9 C8 plant-detail seed fixture

**Day 3 (오전)**:
- T10 C11+C13+C6 carry 정정 doc
- T11 C12 waitForTimeout → expect.poll
- T12 C9 entity-chip 색상 토큰화 + 사용자 manual 시각 확인
- T13 lesson graduate (L1~L6)

**Day 3 (오후~저녁)**:
- T14 사이클 끝 3종 검증 + cold-start fresh fork + **final bundle measurement (G1 net check)**
- T15 retro §0~§9 + Codex 최종 review → P0/P1 흡수 → 사용자 "배포해줘" 시점 배포

---

## §8. 이후 사이클 후보 (v3.41+)

| ID | 항목 | 출처 | 우선순위 |
|---|---|---|---|
| F1 | fuzzy threshold A/B UI (v3.40 deferred, bundle +150~250 B) | v3.40 §0.3 정정 #3 | Medium |
| F2 | Plant action layer (v3.36 deferred) | v3.36 brainstorming | Medium |
| F3 | T6 진단에서 발견된 추가 케이스 (z-index/pointer-events 패턴) | v3.40 T6 in-cycle | Conditional |
| F4 | T3 dead code staged removal (commented-out 1 cycle 후 제거) | v3.40 T3 | Low |
| F5 | T2 모달 preload (`<link rel="modulepreload">` 또는 idle prefetch) | v3.40 §3 T2 risk | Low |
| F6 | NFD 호환성 자모(`U+3130~U+318F`) 분리 형태 검출 rule 확장 | v3.38 T1b false-negative | Low |
| F7 | submitAnswer dataset double-close 외 추가 entry point validator | v3.40 T5 확장 | Low |

---

## §9. Retro (사이클 끝에 채움)

### 9.1 작업 요약 (TBD)
### 9.2 검증 결과 (TBD)
### 9.3 Codex 2-pass 결과 (TBD)
### 9.4 Bundle 측정 (TBD — canonical/archive baseline vs final + chunk별)
### 9.5 신규 lessons / graduate 후보 (TBD)
### 9.6 Carry-forward (v3.41+) (TBD)
### 9.7 DoD 충족도 (TBD)
### 9.8 사이클 평가 (TBD)

---

**작성일**: 2026-05-20
**작성자**: Claude Opus 4.7 + Hayden 합의 (§A/§B/§C 3-stage approval + brainstorming 7-section self-critique)
