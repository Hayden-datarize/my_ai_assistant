# v3.41 — Security & Stability Hardening (P0 Slack auth + P1×5 cluster)

> **Type**: Security/stability hardening cycle. Codex pre-cycle audit (2026-05-20, `/tmp/v3.41-codex-audit.txt`) 기반 P0×1 + P1×5 일괄 흡수. 신규 기능 0건. Schema migration 0줄. P2 3건은 v3.42 carry.

**Cycle size**: M (8 task — T0 + T1~T6 + T7 verify + T8 retro)
**Reference precedent**: v3.20 (hotfix-first + cleanup 통합), v3.18.1 (security 다발 흡수), v3.31 L5 (Single Fix Point)
**Spec 상태**: **v2** — Codex 사전 review (PROCEED_AFTER_FIXES, 2026-05-20, `/tmp/v3.41-codex-pre.txt`) P0×0 + P1×8 + P2×3 in-cycle 흡수. 변경점은 inline `[v2 — Codex P1-N]` 마커로 trace 가능.

---

## §0. 배경

v3.40 배포 직후 사용자 요청으로 Codex 전수 audit (security / performance / arch-debt) 실행. 9 finding 도출: **P0×1 (Slack DM relay 인증 부재) + P1×5 + P2×3**. P0는 production abuse surface (외부에서 `curl`로 직접 호출하여 `@datarize.ai` 직원 누구에게나 Slack DM 발송 시도 + Slack 멤버 enumeration). P1 5건은 XSS / SW stale / URL scheme / silent fail / dead control 등 카테고리 응집 — 모두 S effort.

### 0.1 사이클 scope 합의 (사용자 결정 2026-05-20)

| 결정 항목 | 선택 | 근거 |
|---|---|---|
| hotfix 분리 여부 | **통합 (v3.41 T1로)** | P0 active exploit 아님 + datarize 화이트리스트가 부분 mitigation. App Check은 backend 단독이라 P1 5건과 conflict-free. 분리 사이클 overhead 회피. v3.20 선례. |
| Scope (조심형/표준/야심) | **표준** | P1 5건 모두 S effort + 카테고리 응집 (보안 4 + 안정성 1 + dead control 1). 메시지 명확 = "Security & Stability Hardening". |
| Archive 기간 필터 (구현 vs 숨김) | **숨김** | v3.41 메시지 dilution 회피. v3.2 deferred → 일부 v3.23 (주간 리포트) 흡수. 차후 search/pin과 통합 검토. |
| T1 App Check 깊이 | **Minimal (App Check only)** | site key 발급 + frontend `getToken()` + backend `request.app` 검증. user-level Auth는 v3.42+ deferred. |
| **[v2 — Codex P1-1]** T1 rollout 순서 | **2-phase** — Phase A: site key 등록 + frontend deploy + 7일 monitor (enforce OFF), Phase B: enforce ON | frontend token 배포 전 enforce ON이면 정상 사용자도 403 race. v3.40 hotfix와 동일한 production-first 안전 |
| **[v2 — Codex P1-1]** reCAPTCHA provider | **v3 (default)** | 무료. GCP billing 활성 확인되면 Enterprise 가능. |
| **[v2 — Codex P1-8]** `user_not_found` UX | **generic 202 Accepted** + settings 테스트 버튼 copy "전송 요청을 접수했어요. DM이 오지 않으면 봇 설치/이메일을 확인해주세요" | enumeration 차단 vs 사용자 오타 UX reconcile |

### 0.2 T0 인벤토리 (실측, 2026-05-20)

#### 0.2.1 Helper / 기존 자산 위치

| ID | 항목 | 측정값 | 영향 task |
|---|---|---|---|
| I1 | `isHttpsUrl(value)` 위치 | `src/utils/url.ts:33` | T4 재사용 |
| I2 | `escapeHtml(text)` 위치 | `src/utils/escapeHtml.ts` | T2 fallback (numeric guard 우선) |
| I3 | `parseJsonText<T>` 위치 | `src/utils/gemini-parse.ts:4` (runtime type check 없음) | T2 신규 `parseEvaluationResponse` 신설 |
| I4 | `sendAnswerDm` 본체 | `functions/src/sendAnswerDm.ts` (CORS + `@datarize.ai` + rate limit 有, App Check/Auth 無) | T1 enforceAppCheck 추가 |
| I5 | SW `CACHE_NAME` | `public/sw.js:2` = `daily-growth-v9` | T3 v10 bump |
| I6 | SW fetch handler | `public/sw.js:87` (method 분기 없음) | T3 GET-only intercept |
| I7 | briefings url 검증 현황 | `src/state/briefings.ts:35-57` (`imageUrl`만 isHttpsUrl, `url`/`title`/`summary` 무방어 — comment에 명시) | T4 url field 강제 |
| I8 | boot dynamic import | `src/main.ts:35` (welcome-garden + onboarding + bootMainApp 모두 `void` fire-and-forget) | T5 reportAsync 신설 |
| I9 | archive period filter | `src/ui/tabs/archive.ts:28` (`<select id="archivePeriod" class="archive-period">`), `tabs/archive.ts:120-122` dispatcher, `handlers/archive-listeners.ts:19` listener, `ui/events.ts:29,80,100,102` EventMap+EVENT_NAMES+V32_DEFERRED_EVENTS 3곳, `tests/unit/events.spec.ts:43,50` expect 28/2 | T6 DOM/handler/listener/events.ts/test 일괄 제거 |
| I10 | Firebase App Check 설치 여부 | **미설치** (`firebase` package + `firebase/app-check` import 없음) | T1 npm install 필요 |
| **[v2 — Codex P1-2]** I11 | Bundle baseline 실측 | **`index-*.js` gzip 9.11 KB**, `archive-*.js` gzip 9.12 KB (Codex T0 build, 2026-05-20) | T7 dual record DoD 정합 |
| **[v2 — Codex P1-2]** I12 | `slack.ts` 정적 import 경로 | `src/ui/handlers/home.ts:20` → home chunk에 emit. AppCheck를 slack.ts 정적 import 시 **home chunk에 들어감** (index 아님) | T1 chunk lane 결정 |
| **[v2 — Codex P1-5]** I13 | RSS feed source URL 분포 | `src/ui/handlers/home.ts:749` `interestToFeeds` 매핑 — **모두 `https://`** (실측). http feed 0건 | T4 isSafeUrl http 허용 결정 → 결과적으로 `isHttpsUrl` 충분, 그러나 import/restore 경계 대비로 `isSafeUrl` 유지 |
| **[v2 — Codex P2-2]** I14 | `src/utils/toast.ts` import 분석 | `import` 0건 (의존성 없음). module-level DOM side-effect 0건 (`showToast()` 호출 시점에만 DOM 조작) | T5 circular dep false alarm 확정 |
| **[v2 — Codex P1-4]** I15 | 기존 SW spec | `tests/unit/sw.spec.ts:15`, `tests/lint/sw-passthrough.spec.ts:24` (v3.20 image/font pass-through regression guard) | T3 spec 보존 + 갱신 의무 |
| **[v2 — Codex P2-1]** I16 | `loadAnswers` migrateAnswer timing | `src/state/persistence.ts:26` — legacy key write-back 1회 후 Phase B key는 **read-time normalize only (write-back 없음)** | T2 spec wording 정정 |

#### 0.2.2 Codex audit 9 finding 분류 (sprint scope)

| ID | Severity | 영역 | Task | 비고 |
|---|---|---|---|---|
| F1 | **P0** | Security A.5 | **T1** | Slack DM relay App Check |
| F2 | **P1** | Security A.1 / Arch C.6 | **T2** | AI evaluation score XSS |
| F3 | **P1** | Security A.3 / Perf B.5 | **T3** | SW stale shell + POST cache |
| F4 | **P1** | Security A.6 | **T4** | Briefing URL scheme |
| F5 | **P1** | Perf B.5 | **T5** | Boot dynamic import silent fail |
| F6 | **P1** | Arch C.5 / C.10 | **T6** | Archive period filter dead control |
| F7 | P2 | Perf B.4 | — | first briefing storage thrash → **v3.42 carry** |
| F8 | P2 | Arch C.1 / C.9 | — | state↔ui layer cycle (L effort) → **v3.42 carry** |
| F9 | P2 | Arch C.7 | — | Knip config → **v3.42 carry** |

### 0.3 비목표 (Non-goals)

- 신규 기능 추가 (UI / state / service 모두 0)
- Schema migration (v8 유지)
- User-level Firebase Auth (App Check만, v3.42+ deferred)
- Layer split refactor (P2 F8, L effort)
- Knip config (P2 F9, v3.42 cleanup batch)
- v3.22~v3.39 누적 carry-forward (이미 v3.40에서 청산 완료)
- Bundle trim 단독 목적 (security/stability 부산물로만 측정)

---

## §1. T1 — Slack DM Relay App Check Enforcement (P0 F1)

### 1.1 Finding 요약 (Codex)

- **File**: `functions/src/sendAnswerDm.ts:28`
- **Symptom**: 누구나 `curl -X POST https://us-central1-my-ai-assistant-904f3.cloudfunctions.net/sendAnswerDm` 로 body 조작 → `@datarize.ai` 도메인 이메일 대상 Slack DM 발송 시도. `user_not_found` vs success 응답 차이로 직원 enumeration 가능.
- **Root cause**: CORS는 브라우저 제약만, server-side 호출은 통과. App Check/Auth/HMAC/timestamp replay guard 부재.

### 1.2 결정

**Minimal App Check only** (user-level Firebase Auth는 v3.42+ deferred):

1. Firebase Console: App Check site key 발급 (**[v2 — Codex P1-1] reCAPTCHA v3 default** — 무료. Enterprise는 GCP billing 활성 시만)
2. Frontend (신규 `src/services/appCheck.ts`): `initializeAppCheck` + `getToken()` → `X-Firebase-AppCheck` header
3. Backend (`functions/src/sendAnswerDm.ts`): `onRequest` config에 `enforceAppCheck: true` 추가
4. **[v2 — Codex P1-8] Error response generic화 + UX copy reconcile**:
   - `user_not_found` → `202 Accepted` + `{ok: true, accepted: true}` (enumeration 차단)
   - `src/ui/tabs/settings.ts` 테스트 버튼 success copy 수정: "전송 요청을 접수했어요. DM이 오지 않으면 봇 설치/이메일을 확인해주세요"
   - `@gmail.com` 같은 domain invalid는 400 유지 (기존 동작)
5. Regression test 추가: App Check token 부재 시 401, `nonexistent@datarize.ai`는 202 generic (enumeration 차단 확인), 정상 → 200
6. **[v2 — Codex P1-1] 2-phase rollout (production race 차단)**:
   - **Phase A** (T1 deploy): site key 등록 (Console enforce OFF/monitor), frontend `getAppCheckToken()` deploy, DevTools에서 `X-Firebase-AppCheck` header 발급 확인, 최소 24시간 monitor (token 발급률 ≥ 99%)
   - **Phase B** (T8 deploy 후 별도): backend `enforceAppCheck: true` deploy + Console enforce ON
   - Phase A/B 사이 frontend monitor 결과는 retro §9.4에 기록

### 1.3 변경 범위

**신규 파일**:
- `src/services/appCheck.ts` — Firebase App Check init + token getter

**수정 대상**:
- `src/services/slack.ts` — `fetch('/api/sendAnswerDm', ...)` header에 App Check token 추가
- `src/ui/tabs/settings.ts` — **[v2 — Codex P1-8]** 테스트 버튼 success copy 갱신 (`user_not_found` 202 generic 대응)
- `functions/src/sendAnswerDm.ts` — **Phase B에서** `enforceAppCheck: true` + error response generic화 + `user_not_found` 202 처리
- `functions/src/__tests__/sendAnswerDm.spec.ts` (신설) — App Check + enumeration 차단 regression
- `package.json` — `firebase` (v10+) frontend dep 추가 (이미 있으면 skip)
- `index.html` — App Check debug token meta (개발 환경 only)
- `firebase.json` — App Check 활성화 확인

### 1.4 Risk

- App Check site key 발급은 Firebase Console (외부) 작업 → 사용자 1회 필요 (실행 가능한 콘솔 권한 확인)
- Frontend `firebase` SDK 추가로 bundle 영향 — **[v2 — Codex P1-2] `slack.ts`가 home.ts:20에서 정적 import이므로 AppCheck 정적 import 시 home chunk에 emit (index 아님)**. T7 dual record를 index/home/settings/firebase chunk 4-record로 확장. canonical 합산 Δ ≤ +15 KB gzip. over 시 **in-cycle lazy load 의무** (`await import('./appCheck')` 또는 `slack.ts` 내부 fetch 직전 dynamic import).
- 기존 Playwright smoke의 Slack 테스트 spec 1건 App Check 우회 필요 (test mode token / Phase A는 enforce OFF라 영향 없음)
- App Check token 만료/재발급 race 가능 — `getToken({forceRefresh:false})` default 사용
- **[v2 — Codex P1-1] 2-phase rollout**: Phase A 단독 deploy로 race 차단. Phase B는 monitor 24시간 후
- **rollback** 경로: backend `enforceAppCheck: false` 단일 commit으로 revert 가능 (Phase B만)

---

## §2. T2 — AI Evaluation Score Guard (P1 F2)

### 2.1 Finding 요약 (Codex)

- **File**: `src/ui/handlers/archive.ts:1162`, `src/ui/handlers/stats.ts:450`, `src/services/gemini.ts:88`
- **Symptom**: Gemini가 `score`를 string으로 반환하거나 localStorage 손상 시 archive/stats modal `bodyHtml` raw interpolation → XSS 가능.
- **Root cause**: `parseJsonText<{ score: number; feedback: string }>(text)` 은 generic type assertion만 — runtime check 없음. `answer-detail.ts`는 type guard 있는데 archive/stats path 누락.

### 2.2 결정 — Single Fix Point (v3.31 L5)

`src/services/gemini.ts`에 **`parseEvaluationResponse(text): { score: 1|2|3|4|5; feedback: string }`** 신설:

1. **[v2 — Codex P1-3] `parseJsonText` throw 흡수 의무**: 내부에서 try/catch — malformed/plain text/garbage JSON 시 `{score:3, feedback:''}` + `console.warn('[parseEvaluationResponse] parse failed', err)` 확정. caller에 throw 전파 X.
2. `score`: `Number(raw.score)` → `Number.isInteger` + clamp 1~5 → 미충족 시 `3` fallback + `console.warn`
3. `feedback`: `typeof === 'string'` + length ≤ 500 → 미충족 시 empty string

또한 **[v2 — Codex P2-1]** `src/state/migration.ts`의 `migrateAnswer`는 **read-time normalize only (write-back 없음)** — `loadAnswers` 매 호출 시 normalize 적용, idempotent. 기존 legacy key 1회 write-back과 분리.

렌더링 path는 `escapeHtml(String(score))` + `escapeHtml(feedback)` 적용 (이미 feedback은 escape됨, score만 추가).

### 2.3 변경 범위

- `src/services/gemini.ts` — `parseEvaluationResponse` export
- `src/state/migration.ts` — `migrateAnswer` evaluation normalize 추가
- `src/ui/handlers/archive.ts:1162` — score 렌더 `escapeHtml(String(...))` 적용
- `src/ui/handlers/stats.ts:450` — 동일
- `tests/unit/gemini-parse.spec.ts` — `parseEvaluationResponse` table-driven **[v2 — Codex P1-3] 케이스 11**: (1) valid `{score:3, feedback:"good"}` (2) string score `"5"` coerce (3) out-of-range `6` fallback (4) negative `-1` fallback (5) non-integer `2.5` fallback (6) missing `feedback: null` (7) **fenced ` ```json {...}``` ` (Gemini 흔한 응답)** (8) **garbage prefix + valid JSON** (9) **malformed JSON throw → fallback `{3, ''}`** (10) **plain text "score 5점"** → fallback (11) empty `{}` → fallback
- `tests/unit/migration.spec.ts` — evaluation normalize 회귀

### 2.4 Risk

- Gemini 응답 schema change 시 fallback `3`이 silent corruption처럼 보일 수 있음 → console.warn 출력 (v3.40 L1 silent fail 회피)
- **[v2 — Codex P2-1]** Migration normalize timing — **read-time normalize only, write-back 없음**. `loadAnswers` 매 호출마다 적용. idempotent 보장.

---

## §3. T3 — Service Worker v9→v10 Bump + GET-only Intercept (P1 F3)

### 3.1 Finding 요약 (Codex)

- **File**: `public/sw.js:2,87`
- **Symptom**: deploy 직후 cached `/index.html`이 먼저 반환 → old hashed chunk 요청 → 404 또는 stale code. `/api/sendAnswerDm` POST도 generic cache branch 통과 → `cache.put()` reject promise 누적.
- **Root cause**: stale SW 사고는 이미 v3.2a/v3.11.1 hotfix로 graduated, 그러나 stale-while-revalidate 패턴이 navigation에도 적용된 게 root.

### 3.2 결정

1. `CACHE_NAME = 'daily-growth-v10'` (bump)
2. `fetch` handler 진입에서 `event.request.method !== 'GET'` → `return` (cache 우회)
3. `/api/`로 시작하는 path도 `return` (functions 호출 우회)
4. Navigation request (`event.request.mode === 'navigate'`) → **network-first** with cached `/index.html` fallback (offline 시에만 cache)
5. 정적 asset (`/assets/index-*.js` 등 hashed)만 cache-first 유지

### 3.3 변경 범위

- `public/sw.js` — CACHE_NAME bump + method/path 가드 + navigation strategy 분기
- **[v2 — Codex P1-4] 기존 spec 보존 + 갱신 의무**:
  - `tests/unit/sw.spec.ts` (image/font pass-through regression — v3.20 hotfix 보호)
  - `tests/lint/sw-passthrough.spec.ts` (정적 분석 lint)
  - 신규 케이스 추가: POST `/api/sendAnswerDm` pass-through, navigation network-first with `/index.html` fallback
- (선택) `tests/smoke/sw.spec.ts` 신설 — Playwright SW 동작 확인

### 3.4 Risk

- offline mode 동작 변경 — navigation 시 network 실패 → cached `/index.html` fallback OK, 하지만 hashed chunk fetch 실패는 graceful degradation 없음 (이전과 동일)
- 기존 Playwright spec의 `serviceWorkers: 'block'` 가정 영향 없음 (이건 SW 자체를 끄는 옵션)
- **[v2 — Codex P1-6] "v9-controlled 첫 reload" 잔여 위험 명시**: v10 network-first는 v10 SW가 control 잡은 뒤부터 유효. deploy 직후 첫 navigation은 아직 old v9 SW 처리 가능 → stale `/index.html` 가능성 남음. **v10 activated 이후 reload부터 보장**. retro §9.4에 production 첫 reload 사용자 1명 manual confirm 의무 (사용자 본인).

---

## §4. T4 — Briefing URL Scheme Validation (P1 F4)

### 4.1 Finding 요약 (Codex)

- **File**: `src/state/briefings.ts:34`, `src/ui/handlers/home.ts:489`, `src/ui/handlers/archive.ts:1167`
- **Symptom**: RSS의 `url`이 `javascript:`/`data:`/malformed scheme이면 카드/archive anchor href에 그대로. `<a href="javascript:alert(1)">` 클릭 시 XSS 실행.
- **Root cause**: `isHttpsUrl()` 이미 있는데 `imageUrl`에만 적용. briefings.ts:35 comment에 명시적으로 acknowledged.

### 4.2 결정 — Single Fix Point + Boundary

**[v2 — Codex P1-7] write + read + render 3-layer guard** (boundary만으로는 손상된 localStorage / import 데이터의 render sink가 남음):

1. **Write boundary**: `src/state/briefings.ts`의 `saveBriefings` entry — 각 item의 `url` field에 `isSafeUrl(url)` 검증 → 실패 시 item drop + `console.warn`
2. **Read boundary**: `loadBriefings()` — 동일 normalizer 적용 (read-time). saveBriefings와 같은 helper 호출하여 idempotency.
3. **Render boundary (defense-in-depth)**: `src/ui/handlers/home.ts:489` `main.href = isSafeUrl(b.url) ? b.url : '#'` / `src/ui/handlers/archive.ts:1167` 동일 패턴.
4. `fetchFeed` (`src/services/rss.ts`) boundary에도 동일 검증 추가 (RSS → state 진입 경계).
5. `title` / `summary` / `sourceTitle` 등 텍스트 field는 escapeHtml 렌더링 path에서 이미 안전 → 추가 처리 없음.

### 4.3 변경 범위

- `src/utils/url.ts` — `isSafeUrl` 신설 (http+https only)
- `src/state/briefings.ts` — **[v2 — Codex P1-7]** `saveBriefings` + `loadBriefings` 양쪽 entry url 검증 + `normalizeBriefingItem` validator helper export
- `src/services/rss.ts` — `fetchFeed` 응답 normalize에서 invalid url item drop
- `src/state/briefings.ts:35-37` comment 갱신 (write + read + render 3-layer)
- **[v2 — Codex P1-7]** `src/ui/handlers/home.ts:489` render path safeHref re-check (`main.href = isSafeUrl(b.url) ? b.url : '#'`)
- **[v2 — Codex P1-7]** `src/ui/handlers/archive.ts:1167` render path safeHref re-check (`escapeHtml(safeUrl)` 변환 전 isSafeUrl gate)
- `tests/unit/briefings.spec.ts` — `javascript:` / `data:` / empty / valid mix table + read-time normalize 회귀
- `tests/unit/rss.spec.ts` — invalid url item drop 회귀

### 4.4 Risk

- 기존 RSS feed에 http (non-https) link 존재 시 drop → 사용자 가시 감소. **결정**: http도 허용 (isHttpsUrl이 아닌 `isSafeUrl`로 분기 — `^https?:` 허용, `javascript:`/`data:`/`vbscript:`/`file:` 차단). 신규 helper `src/utils/url.ts:isSafeUrl` 추가.

---

## §5. T5 — reportAsync Helper + Boot Dynamic Import Wrap (P1 F5)

### 5.1 Finding 요약 (Codex)

- **File**: `src/main.ts:35`
- **Symptom**: `void import('./ui/modals/welcome-garden').then(...)` 패턴이 reject 시 unhandled rejection만 남고 사용자 빈 화면. v3.40 L1 silent-fail lesson 재발 surface.
- **Root cause**: `void` prefix가 promise 추적 차단 + .catch 부재.

### 5.2 결정

`src/utils/reportAsync.ts` 신설 — 단일 helper:

```ts
export function reportAsync<T>(label: string, p: Promise<T>): Promise<T | undefined> {
  return p.catch(err => {
    console.warn(`[reportAsync:${label}]`, err);
    // 토스트는 옵셔널 — 환경 mount 안된 상태 가능
    try { showToast(`${label} 로드 실패 — 새로고침 해주세요`); } catch { /* noop */ }
    return undefined;
  });
}
```

`src/main.ts`의 3개 dynamic import 모두 `reportAsync('welcome-garden', import(...).then(...))` 패턴으로 wrap.

### 5.3 변경 범위

- `src/utils/reportAsync.ts` (신설)
- `src/main.ts` — 3개 site `void import(...)` → `reportAsync(label, import(...).then(...))`
- `tests/unit/reportAsync.spec.ts` — reject → warn + 동일 resolve 흐름 + toast mount 실패 catch

### 5.4 Risk

- `showToast` import 순환의존 위험 — `reportAsync`가 utils → utils 내부 호출 OK, 단 main.ts 시점에서 toast mount 전이면 try/catch로 흡수
- console.warn은 production console noise 증가 — sentry-style telemetry 없으니 warn 유지

---

## §6. T6 — Archive Period Filter Dead Control 숨김 (P1 F6)

### 6.1 Finding 요약 (Codex)

- **File**: `src/ui/handlers/archive.ts:115`, `src/ui/tabs/archive.ts:28`
- **Symptom**: 사용자 "이번 주/이번 달" select 보임 → 선택 시 "v3.2 준비 중" toast. 기능 미완 가시 UX.
- **Root cause**: v3.2 deferred로 placeholder 남음. CustomEvent는 wired지만 behavior가 dummy.

### 6.2 결정 — 숨김 (구현 X) — **[v2 — Codex P1-6] exact deletion list**

실측 인벤토리 (I9 참조):
1. `src/ui/tabs/archive.ts:28` — `<select class="archive-period" id="archivePeriod">` block (label + 3 option) 제거
2. `src/ui/tabs/archive.ts:120-122` — dispatcher `archivePeriod.addEventListener('change', ...)` 제거
3. `src/ui/handlers/archive-listeners.ts:19-22` — `on('dg:archive:period-change', ...)` listener 제거
4. `src/ui/events.ts:29` — `EventMap` entry `'dg:archive:period-change': undefined` 제거
5. `src/ui/events.ts:80` — `EVENT_NAMES` 배열에서 `'dg:archive:period-change'` 제거 (28 → 27)
6. `src/ui/events.ts:100-103` — `V32_DEFERRED_EVENTS` 배열에서 `'dg:archive:period-change'` 제거 (2 → 1, `dismiss-backup`만 남음)
7. `tests/unit/events.spec.ts:43,44` — expect 28 → 27, set size 28 → 27
8. `tests/unit/events.spec.ts:50` — `V32_DEFERRED_EVENTS` length 2 → 1
9. `tests/unit/handlers-register.spec.ts` — period 관련 mock 있다면 정리
10. `src/styles/pages/archive.css` — `.archive-period` selector 있다면 제거

### 6.3 변경 범위

- `src/ui/tabs/archive.ts` — DOM 마크업 + dispatcher 제거
- `src/ui/handlers/archive-listeners.ts` — listener 제거
- `src/ui/events.ts` — EventMap + EVENT_NAMES + V32_DEFERRED_EVENTS 3곳 제거
- `src/styles/pages/archive.css` — `.archive-period` rule 제거
- `tests/unit/events.spec.ts` — count assertion 갱신 28→27, 2→1
- `tests/unit/handlers-register.spec.ts` — mock 정리

### 6.4 Risk

- 구현 의도가 v3.42+에 다시 필요해질 가능성 — 그 때 다시 만들면 됨 (현재 dead control이 더 손해). retro에 carry note 명시.
- 사용자 인터뷰: 이 기능을 기다리고 있었던 사용자가 있다면 숨김에 대한 reaction 가능. 비개발자 PM 동의 받음.

---

## §7. T7 — 3종 검증 + Bundle Dual Record + N=5

### 7.1 검증 명령

```bash
# 1) TS strict
npx tsc --noEmit                                              # → 0 errors

# 2) Lint
npm run lint                                                  # → 0 errors

# 3) Vitest 양쪽 TZ
TZ=Asia/Seoul npm test                                        # → PASS
TZ=America/New_York npm test                                  # → PASS

# 4) Functions tests
npm --prefix functions test                                   # → PASS (App Check regression 포함)

# 5) Smoke
npm run test:smoke                                            # → 98/98 (또는 sw spec +1, sendAnswerDm spec +1)

# 6) [v2 — Codex P1-2] Bundle quad record (T1 chunk lane 정합)
gzip -c dist/assets/index-*.js | wc -c                        # index canonical
gzip -c dist/assets/home-*.js | wc -c                         # home chunk (slack.ts 정적 import → AppCheck 영향)
gzip -c dist/assets/settings-*.js | wc -c                     # settings chunk
gzip -c dist/assets/archive-*.js | wc -c                      # archive chunk
ls -la dist/assets/firebase*.js 2>/dev/null || true           # firebase shared chunk (있을 시)
```

### 7.2 Bundle threshold — [v2 — Codex P1-2] 정정

**Baseline (Codex T0 실측 2026-05-20)**:
- `index-*.js` gzip = **9.11 KB** (9,034 B)
- `archive-*.js` gzip = **9.12 KB** (9,119 B, 2 chunk 합산)

**Threshold (T1 SDK 추가 고려)**:
- **index canonical**: GREEN ≤ +300 B / AMBER ≤ +500 B (T1 영향 최소화 의무)
- **home chunk**: 변동 추적 (T1 AppCheck → slack.ts 정적 import 시 여기로 emit)
- **settings chunk**: 변동 추적
- **archive chunk**: 변경 없음 예상 (T6은 trim)
- **canonical 합산 (index + home + settings + firebase chunk)**: Δ ≤ **+15 KB gzip** (DoD)
- over 시: **in-cycle lazy load 의무** — `slack.ts` 내부 `fetch` 직전 `await import('./appCheck')` 동적 import. spec amendment X (T1 in-cycle fix).

### 7.3 N=5 (T1 + T2 + T3 신규 spec 5/5)

```bash
for i in 1 2 3 4 5; do TZ=Asia/Seoul npm test -- gemini-parse reportAsync briefings sw sendAnswerDm; done
```

---

## §8. T8 — Codex 최종 Review + Retro + Obsidian Sync

### 8.1 Codex 최종 review dispatch

```bash
codex exec --skip-git-repo-check -o /tmp/v3.41-codex-final.txt "<prompt — DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT 분류>"
```

분류:
- **DEPLOY_APPROVED** — 즉시 배포
- **APPROVED_WITH_NOTES** — P1×N + P2×M, in-cycle 흡수 가능한 P1만 추가 commit
- **REJECT** — P0 발견 시 사이클 미종료

### 8.2 Retro 작성

`docs/superpowers/specs/2026-05-20-v3.41-security-stability-hardening-design.md` 의 §9 (retro) 섹션 작성:
- §9.1 commit summary
- §9.2 task별 산출물 + verification
- §9.3 bundle 측정 (dual record)
- §9.4 사전 review 흡수 + 최종 review 결과
- §9.5 신규 lessons (있다면)
- §9.6 v3.42 carry-forward (P2 F7/F8/F9 + 신규 발견)
- §9.7 사용자 결정 사항
- §9.8 다음 사이클 권고

### 8.3 Obsidian sync

`/Users/hayden/Hayden/20_Projects/Dev_Project/SJ_Daily Growth(Ai assistant)/` vault에:
- `02-Cycles/` 본 spec 복사
- `03-Lessons/` 신규 memory file (`project_daily_growth_v341.md`)
- `00-Index.md` 사이클 타임라인 표 한 줄 추가

---

## §9. Risk Heatmap (요약) — [v2 — Codex P1 8건 흡수 반영]

| 영역 | Risk | Mitigation |
|---|---|---|
| T1 App Check | Firebase Console 외부 작업 필요 | 사용자 1회 site key 발급. fallback `enforceAppCheck:false` |
| T1 Bundle | [v2 P1-2] slack.ts 정적 import로 home chunk 영향 가능 | quad record 측정 + over 시 in-cycle lazy load |
| T1 Rollout | [v2 P1-1] frontend token 배포 전 enforce ON 시 정상 사용자 403 race | **2-phase**: Phase A (frontend deploy + monitor 24h), Phase B (enforce ON) |
| T1 UX copy | [v2 P1-8] `user_not_found` 202 generic 시 settings 테스트 버튼 "성공" 표시 → 사용자 혼란 | success copy 수정 ("접수했어요. DM이 없으면 봇/이메일 확인") |
| T2 score guard | Gemini schema change silent + parseJsonText throw | [v2 P1-3] 내부 try/catch + fallback `{3, ''}` + console.warn |
| T2 normalize | [v2 P2-1] migrate timing 부정확 | "read-time normalize, write-back 없음"으로 wording 정정 |
| T3 SW | offline UX 미세 변경 + [v2 P1-6] v9-controlled 첫 reload 잔여 위험 | 기존 cache hashed asset 유지. v10 activated 이후부터 보장 명시. 기존 sw.spec.ts + sw-passthrough.spec.ts 보존 |
| T4 URL | [v2 P1-7] write boundary만으로는 render sink 남음 | write + read + render 3-layer guard. 실측 RSS feed 모두 https (I13). |
| T5 reportAsync | [v2 P2-2] toast.ts circular dep false alarm — grep 의무 부재 | T5 Step 0에 `rg "reportAsync|from .*toast"` grep 의무 추가 |
| T6 dead control | [v2 P1-6] id mismatch (archivePeriod vs archivePeriodFilter) | exact deletion list 10건 (DOM/dispatcher/listener/events.ts 3곳/test 2건/CSS) |
| Cross | App Check rollout 후 production 사용자 token 발급 race | 2-phase (Phase A enforce OFF deploy + monitor 24h, Phase B enforce ON) |

---

## §10. Verification Matrix (DoD)

| Task | TS | Lint | Vitest 양쪽 TZ | Functions test | Smoke | Bundle | N=5 |
|---|---|---|---|---|---|---|---|
| T1 | ✅ | ✅ | ✅ | ✅ (신규 spec) | ✅ (Slack happy path) | △ (quad record, ≤ +15 KB) | ✅ |
| T2 | ✅ | ✅ | ✅ (신규 spec +5) | — | — | ✅ | ✅ |
| T3 | — | — | — | — | ✅ (sw spec +1) | — | ✅ |
| T4 | ✅ | ✅ | ✅ (신규 spec +5) | — | — | ✅ | ✅ |
| T5 | ✅ | ✅ | ✅ (신규 spec +3) | — | — | ✅ | ✅ |
| T6 | ✅ | ✅ | ✅ | — | ✅ | ✅ (trim 미세) | — |
| T7 | ✅ 통합 | ✅ | ✅ 통합 | ✅ | ✅ | ✅ dual record | ✅ |

**전체 신규 vitest spec 예상**: +~13 (T2 5 + T4 5 + T5 3)
**전체 신규 smoke**: +1~2 (T3 sw + T1 sendAnswerDm)
**Functions test**: +5~8 (App Check + enumeration)
