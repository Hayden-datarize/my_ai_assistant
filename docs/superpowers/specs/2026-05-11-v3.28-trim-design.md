# v3.28 Bundle Trim Cycle (Dedicated)

> **Status**: Pre-reserved (v3.27 T10 commit, plan §6.2 R5 mitigation — skip 불가).
> **Trigger**: v3.27 배포 직후. main work 사이클 없이 trim 단독 진입.

---

## §1. 사이클 목표 / 범위

### 1.1 목표

v3.27까지 누적된 bundle drift 회수 — **41056 baseline → 41200대 GREEN 복귀** (현재 42731, Δ −1500 B 목표).

### 1.2 범위 (확정)

| 출처 | 항목 |
|---|---|
| v3.27 Codex 최종 P2-1 | `#insightsTab` CSS orphan 4곳 — `src/styles/layout.css:71/:90/:104` 외. T2a tab 제거 후 dead selector. |
| v3.27 Codex 최종 P2-2 | `Answer.pinned`/`Briefing.pinned` write-side normalize — load/save 경계에서 `?? false` 명시화 (schema drift 방지) |
| v3.24 carry | bundle GREEN 복귀 |
| v3.26 carry | bundle GREEN 복귀 |

### 1.3 범위 (후보 grep — T0에서 확정)

| 후보 | Why | T0 grep 결과 (2026-05-11) |
|---|---|---|
| ~~chat-bubble legacy wrapper~~ | v3.9 toast unify 잔재 가설 | **active code 확인 — 제외** (home.ts:42/839/848/1060/1122, rewards.ts:105 사용 중. `src/ui/chat-bubble.ts:68-83` 호출 활성) |
| parser dead path | v3.25 insight parser 폴백 dead branch | `src/utils/gemini-parse.ts:51-95` fallback branch — `tests/unit/parse-insight-response.spec.ts:11-124`로 coverage 있음. **branch-level proof 필요** (테스트 가린 dead만 제거) |
| migration chain simplify | v3.27 T1 v7→v8 lazy migrate 후 chain 6 위치 (`user.ts:137/202/217/227/234/251`) v8 우선 prepend / range check 압축 | dead drop 아니라 chain re-order. typed schema-version guard 의무 (P1-1) |
| innerHTML indent 압축 | settings.ts / home.ts 일부 indent 잔존 가설 | **표면 작음 — `src/ui/tabs/home.ts:8`, `src/ui/tabs/settings.ts:37` 이미 one-line**. T0 grep mandatory로 정확한 후보 측정 |
| dead import sweep | archive.ts (`closeModal`) precedent — 전체 src grep | TS strict TS6133 + side-effect-only import 수동 점검 |
| LOW utility const | 단일 사용 const inline화 | T0 grep으로 surface 측정 필요 |

### 1.4 Out of scope

- 신규 기능 X (main work 묶음 금지)
- API 변경 X
- schema 변경 X

---

## §2. Task Breakdown (Codex 사전 review 흡수 후 — plan v2 동기화)

| Task | 내용 | 예상 Δ |
|---|---|---|
| T0 | spec amend + plan v1/v2 + Codex 사전 review (mandatory, fallback 명시) | docs |
| T1 | CSS orphan sweep — `#insightsTab` 3 selector (layout.css:71/:90/:104) + comment ref 2곳 | −60 B |
| T2 | pinned write-side normalize (Option A: type narrow + 3-layer backfill) — schema/briefings/user `pinned?: boolean → pinned: boolean` + `makeAnswer` default false + `migrateAnswer`/`loadBriefings` copy-based backfill + archive.ts read-side `?? false` 9곳 제거 | −200 B |
| T3 | migration chain re-order — typed schema-version guard (`typeof v === 'number'`) + v8 우선 prepend 또는 range check 압축 (6 위치) | −80 B |
| T4 | dead import sweep — TS6133 unused + side-effect import 수동 점검 (`closeModal` precedent 확산) | −50 B |
| T5 | LOW utility const inline + innerHTML indent (T0 grep으로 surface 측정 mandatory) | 표면 실측 후 확정 |
| T6 | 3종 검증 + 양쪽 TZ vitest + smoke 97/97 + bundle 최종 측정 + Codex 최종 review | docs |
| T7 | retro + memory + vault sync + (사용자 명시 시) firebase deploy | docs |

**예상 누적 Δ: −390 B (확정) + T5 표면 실측분** (DoD −1231 B 달성 여부는 T5 측정 결과에 의존 — 미달 시 잔여 v3.29 carry)

---

## §3. DoD

- bundle CLI gzip ≤ **41500 B** (현재 42731, 목표 Δ ≥ −1200 B)
- TS strict 0 / lint baseline 유지
- vitest 양쪽 TZ regression 0 (1217 baseline)
- smoke 97/97
- Codex 사전 review + 최종 review 둘 다 dispatch

---

## §4. Risk

| # | Risk | Mitigation |
|---|---|---|
| R1 | dead import 제거가 dynamic import 회귀 유발 | grep + spec coverage 양쪽 검증 |
| R2 | CSS orphan 제거가 historic browser cache 회귀 | hashing 의존 — Vite asset hash가 자동 처리 |
| R3 | trim 목표 미달 (LOW surface 좁음) | candidate grep T0에서 정확한 측정, 부족하면 v3.29로 잔여 격하 |

---

## §7. Retro (2026-05-11 사이클 완료)

### 7.1 사이클 요약

| 항목 | 값 |
|---|---|
| Task 수 | 8 (T0~T7) — T5 R3 발동 격하 + T4 commit 0 |
| Commit 수 | 6 (T0×3 docs + T1/T2/T3) — T4/T5 commit 0 |
| 최종 commit | `04f7855` (T3) — T6/T7 검증·retro만 |
| vitest KST | **1224/1224 PASS** (baseline 1217 + T2 +7 = 1224) |
| vitest NY | **1224/1224 PASS** (양쪽 TZ same count) |
| Smoke | **97/97 PASS** (16.6s) |
| TS strict | 0 errors |
| Lint | 30 (2 errors + 28 warnings) — baseline 유지 (v3.27 T2b 잔존, T2~T4 무관) |
| Bundle CLI gzip | **42778 B** (baseline 42731 → Δ **+47 B over**, DoD 41500 미달 Δ +1278 B) |
| Codex 사전 review | **REQUIRES_FIX** P0=5 + P1=4 → plan v2 1:1 흡수 (`db3536f` → `cbd4de6` + `e235d0c`) |
| Codex 최종 review | **APPROVED_WITH_NOTES** P0=0 / P1=1 (retro 흡수) / P2=1 (v3.29 carry) |

### 7.2 DoD 평가

- bundle ≤ 41500 B: ❌ **MISS** (R3 발동 — plan v2 §Risk R3 명시 — `APPROVED_WITH_NOTES` + 잔여 v3.29 carry 허용)
- TS strict 0 / lint baseline / vitest 양쪽 TZ regression 0 / smoke 97/97 / Codex 2-pass: ✅

### 7.3 R3 발동 정당화

T0 Step 4 LOW surface 측정 결과 (controller grep):
- `innerHTML = \`` multi-line: **0 hits** (이미 one-line)
- Single-use const: storage keys / endpoint URLs — readability 손실
- Dead import: 0 (TS strict + ESLint 자연 catch, v3.27 T8 `closeModal` 이후 유입 0)
- Side-effect import: 0

T1 +3 B / T3 −2 B / T4 0 = **trim cycle 본연 net +1 B**.
T2 +46 B는 v3.27 carry P2-2 (write-side normalize) **invariant 강화 task의 side cost** — Codex 사전 review P0 4건 (P0-1~P0-4) 흡수 비용. 정당.

### 7.4 핵심 변경

- **T1 (`cfc127b`)**: `#insightsTab` CSS orphan 3 selector (`layout.css:68/88-93/101`) + comment ref 2곳 (stats.css:9 / archive.css:9). v3.27 T2a tab 폐기 잔재.
- **T2 (`1c65ccd`)**: pinned write-side normalize Option A — Codex P0=4 흡수.
  - `Answer.pinned?: boolean` → `pinned: boolean` (schema/briefings/user 3 type)
  - `makeAnswer({pinned: false, ...input})` spread 앞 default (input override 허용)
  - `migrateAnswer` pass-through + legacy 양 분기 copy-based backfill (P0-3 idempotency)
  - `loadBriefings` map copy-based normalize
  - `migrateUserToV7` insights forward-compat (TS strict ripple)
  - archive.ts read-side `?? false` 9곳 제거 (TS strict 자연 통과)
  - home.ts 신규 briefing append + Insight literal `pinned: false`
  - tests/state/pinned-normalize.spec.ts 신설 7 spec + 24 fixture sweep
- **T3 (`04f7855`)**: migration chain typed-guard — Codex P1-1 흡수.
  - `user.ts` 5 위치 (`:137/:202/:217/:227/:234`) `typeof === 'number'` guard + range check (`sv >= N && sv <= 8`)
  - `:251` 단일 비교는 ROI 0 격하

### 7.5 신규 lessons (graduate 후보)

- **L1: 사전 Codex P0 ROI 10사이클 연속** (v3.18~v3.28). 본 사이클 P0=5 catch (makeBriefing 미존재 / backfill 위치 / in-place mutation / TS strict fail / DoD budget unsupported) → 모두 plan v2 1:1 흡수. CLAUDE.md graduated 명문화 ROI 확정 강화. 차기 사이클부터 graduated lesson으로 carry.
- **L2: dedicated trim cycle에 invariant 강화 task 묶지 말 것**. v3.28 spec §1.2가 P2-1 (CSS orphan) + P2-2 (pinned write-side normalize)를 묶었지만, P2-2는 type/runtime invariant 강화 task — Codex P0=4 catch 후 3-layer backfill 추가로 +46 B side cost. trim cycle DoD를 invariant 강화 task가 압도. 차기 trim cycle은 **trim-only** 또는 invariant 강화는 별도 budget 명시.
- **L3: T0 Step 4 LOW surface 측정 mandatory ROI**. plan v1에서 T5 −300 B 가정 → Codex P0-5 catch → T0 Step 4 mandatory grep으로 surface 0 사전 확인 → R3 사이클-끝이 아닌 T5 진입 직전에 격하 결정 (effort 절감). T0 측정 mandatory가 cycle 중간 격하의 trigger로 graduated 권고.
- **L4: type narrow + 3-layer backfill 패턴 — Option A graduated**. v3.27 P2-2 (write-side normalize) 같은 케이스는 (a) type narrow optional→required, (b) write-side helper default, (c) 모든 load/migrate boundary copy-based backfill, (d) read-side fallback 제거 — 4 step이 표준. P0-3 idempotency 강제 (in-place mutation 금지).

### 7.6 v3.29 Carry-forward (P2 신규 + P1 잔여 격하)

| # | 항목 | 출처 | 우선순위 |
|---|---|---|---|
| C1 | bundle DoD 41500 도달 — 미달 잔여 Δ +1278 B | T5 R3 격하 (surface 0) | P1 (소비/추적용) |
| C2 | already-v8 Insight records `pinned` backfill — `migrateUserToV8` early-return 분기 amend | Codex 최종 P2-1 | P2 (runtime falsy-safe라 격하) |
| C3 | `makeAnswer` `pinned: undefined` 명시 corner case | T2 review minor | P3 (caller 0이라 risk 0) |
| C4 | `migrateUserToV6` insights backfill 일관성 | T2 review minor | P3 (chain end V8 보장으로 production-safe) |

### 7.7 차기 사이클 후보

- **v3.29 (cleanup + 일부 trim)**: C1~C4 + 검색 하이라이트 + fuzzy / v3.20 인프라 / v3.21 Plant action / v3.17 §7.2. cleanup + trim 묶음으로 net negative Δ 목표 (L2 lesson 적용 — invariant 강화 task 분리).
- 또는 **trim-only cycle 재시도** — L2 lesson 따라 P2-2 같은 invariant 강화 묶음 없이 pure trim. surface는 cumulative drift watchpoint.

---

**작성**: 2026-05-11 (v3.27 T10 사전 예약 → v3.28 사이클 완료 시점)
**baseline commit**: v3.27 deploy 직후 (`38c73a5` T10 retro + `9a11f93` T9 sweep)
**최종 commit**: `04f7855` (T3) + 본 retro append
**Codex thread**: 단일 thread (사전 → 최종, --resume) `019e173b-55c6-7ef1-98d8-0e71ab6b5d77`
**디스크립터**: dedicated bundle trim — 누적 drift 회수 / R3 발동 / 사전 P0=5 catch ROI 10사이클 연속
