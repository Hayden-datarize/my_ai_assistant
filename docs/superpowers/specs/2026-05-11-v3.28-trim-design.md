# v3.28 Bundle Trim Cycle (Dedicated)

> **Status**: Pre-reserved (v3.27 T10 commit, plan §6.2 R5 mitigation — skip 불가).
> **Trigger**: v3.27 배포 직후. main work 사이클 없이 trim 단독 진입.

---

## §1. 사이클 목표 / 범위

### 1.1 목표

v3.27까지 누적된 bundle drift 회수 — **41056 baseline → 41200대 GREEN 복귀** (현재 42731, Δ −1500 B 목표).

### 1.2 범위 (확정)

| 출처 | 항목 |
|---|---|
| v3.27 Codex 최종 P2-1 | `#insightsTab` CSS orphan 4곳 — `src/styles/layout.css:71/:90/:104` 외. T2a tab 제거 후 dead selector. |
| v3.27 Codex 최종 P2-2 | `Answer.pinned`/`Briefing.pinned` write-side normalize — load/save 경계에서 `?? false` 명시화 (schema drift 방지) |
| v3.24 carry | bundle GREEN 복귀 |
| v3.26 carry | bundle GREEN 복귀 |

### 1.3 범위 (후보 grep — T0에서 확정)

| 후보 | Why |
|---|---|
| chat-bubble legacy wrapper | v3.9 toast unify 잔재 |
| parser dead path | v3.25 insight parser 폴백 dead branch |
| migration silent drop | v3.27 T1 v7→v8 lazy migrate 후 v6 이하 silent drop 분기 dead |
| innerHTML indent 압축 | settings.ts / home.ts 일부 indent 잔존 |
| dead import sweep | archive.ts (`closeModal`) precedent — 전체 src grep |
| LOW utility const | 단일 사용 const inline화 |

### 1.4 Out of scope

- 신규 기능 X (main work 묶음 금지)
- API 변경 X
- schema 변경 X

---

## §2. Task Breakdown (skeleton — T0에서 확정)

| Task | 내용 | 예상 Δ |
|---|---|---|
| T0 | spec amend + plan + 사전 Codex review | docs |
| T1 | CSS orphan sweep (`#insightsTab` 4곳) | −60 B |
| T2 | pinned write-side normalize | −20 B |
| T3 | chat-bubble legacy / parser dead | −300 B (목표) |
| T4 | migration silent drop simplify | −200 B (목표) |
| T5 | innerHTML indent 추가 | −150 B (목표) |
| T6 | dead import sweep | −100 B (목표) |
| T7 | 3종 + 양쪽 TZ + smoke + bundle 최종 + Codex 최종 + retro | docs |

**예상 누적 Δ: −830 B** (실측은 T0에서 grep 후 update)

---

## §3. DoD

- bundle CLI gzip ≤ **41500 B** (현재 42731, 목표 Δ ≥ −1200 B)
- TS strict 0 / lint baseline 유지
- vitest 양쪽 TZ regression 0 (1217 baseline)
- smoke 97/97
- Codex 사전 review + 최종 review 둘 다 dispatch

---

## §4. Risk

| # | Risk | Mitigation |
|---|---|---|
| R1 | dead import 제거가 dynamic import 회귀 유발 | grep + spec coverage 양쪽 검증 |
| R2 | CSS orphan 제거가 historic browser cache 회귀 | hashing 의존 — Vite asset hash가 자동 처리 |
| R3 | trim 목표 미달 (LOW surface 좁음) | candidate grep T0에서 정확한 측정, 부족하면 v3.29로 잔여 격하 |

---

**작성**: 2026-05-11 (v3.27 T10 사전 예약 commit)
**baseline commit**: pending v3.27 retro commit
**디스크립터**: dedicated bundle trim — 누적 drift 회수
