# v3.48 — Streak Freeze 현황 패널 + 활동 내역 (수동 claim reframe)

**Date**: 2026-05-22
**Type**: 신규 user-facing 기능 (v3.21 §8.6 carry) + schema v9→v10
**규모**: Medium (~1.5h) — schema bump 포함 (되돌리기 영향)

---

## §1. 배경 + 목표

v3.21이 Streak Freeze를 **자동 전용**으로 출시: 7일마다 ❄️ 1개 regen(cap 2), 결석일 1:1 자동 consume, 홈 배너 `🔥 N일 · ❄️ N`. v3.21 §8.6은 "수동 claim / settings UI / 재충전 timer / 소비 history"를 v3.22+로 deferred.

**"수동 claim" reframe (사용자 결정 2026-05-22)**: freeze가 이미 자동 regen + 자동 consume이라 "수동 claim 버튼"은 자동 시스템과 중복되고 실익이 모호. 대신 §8.6의 실질 가치인 **가시성**(보유량 · 다음 충전 · 활동 내역)을 settings에 추가한다. 수동 claim 버튼은 비목표.

**확정 결정**:
- ① 내역은 **충전(earned) + 사용(consumed) 통합 타임라인** (소비만 기록 시 결석 없는 사용자는 늘 빈 내역).
- ② 다음 충전 표시는 **"예상"** — regen은 답변 시점에만 실행되므로 "약 D일 (답변 시 충전)"으로 정직 표기.

---

## §2. Existing Landscape (변경 경계)

| 파일 | 현 상태 | v3.48 변경 |
|---|---|---|
| `src/state/user.ts` | `XpHistoryEntry` + `streakFreeze:{count,lastEarnedAt}` + `schemaVersion:9` + getXpHistory | `FreezeHistoryEntry` 타입 + `freezeHistory?:[]` 필드 + sv 10 + 3 spot(hydrate/persist/shape guard) + freezeHistory write in `recordDailyAnswer` + `getFreezeHistory` |
| `src/state/migration.ts` | migrateUserToV9 (최신) | `migrateUserToV10` chain superset |
| `src/state/freezeEngine.ts` | `regenerateFreeze`(void) / `consumeFreezeForGap` | **변경 없음** (regen 전후 count 차이로 earned 산출 — 시그니처 무변경) |
| `src/ui/tabs/settings.ts` | `.settings-group` 섹션 패턴 | "❄️ Streak Freeze" 섹션 추가 (render + bindHandlers read) |
| `tests/state/*` | freezeEngine/migration/user spec | migrateUserToV10 + write + getFreezeHistory + 다음충전 계산 spec |
| `tests/smoke/*` | settings smoke | settings "❄️ Streak Freeze" 섹션 노출 1 시나리오 |

**기존 상수** (`freezeEngine.ts`): `FREEZE_CAP=2`, `SEVEN_DAYS_MS=7*86400_000`. **재사용** (다음 충전 계산에 동일 7d window).

---

## §3. Schema v9→v10 (xpHistory v8 선례 1:1)

### FreezeHistoryEntry

```ts
interface FreezeHistoryEntry {
  date: string;                    // KST 'YYYY-MM-DD' (getKstDateStr)
  kind: 'earned' | 'consumed';     // 충전 / 사용
  amount: number;                  // >0 정수 (regen grant 또는 consumed days)
}
```

User 타입: `freezeHistory?: FreezeHistoryEntry[];`

### migrateUserToV10 (chain superset)

```ts
export function migrateUserToV10(u: unknown): User {
  const r = u as Record<string, unknown> & { schemaVersion?: number; freezeHistory?: unknown };
  if (r.schemaVersion === 10) return r as unknown as User;   // idempotent / downgrade barrier
  const v9 = migrateUserToV9(u);
  const v9r = v9 as unknown as Record<string, unknown>;
  const freezeHistory = Array.isArray(v9r.freezeHistory) ? v9r.freezeHistory : [];
  return { ...v9, schemaVersion: 10 as const, freezeHistory } as unknown as User;
}
```

### user.ts 4 spot (Explore 실측)

1. **User 타입** `schemaVersion: 9` → `10`.
2. **getCachedUser hydrate**: range `sv0 >= 2 && sv0 <= 9` → `<= 10`; chain에 `user = migrateUserToV10(user);` 추가 (v9 호출 다음).
3. **getCachedUser persist**: `parsed?.schemaVersion !== 9` → `!== 10`.
4. **isValidUserShape**:
   - 기존 `sv >= 4 && sv <= 9` (plant) → `<= 10`; `sv >= 8 && sv <= 9` (xpHistory) → `<= 10` (v10은 v9 superset).
   - 신규 freezeHistory guard:
     ```ts
     // v3.48: v10 신규 freezeHistory 배열 guard (silent NaN 차단, v3.12 lesson).
     if (sv >= 10 && sv <= 10) {
       if (!Array.isArray(r.freezeHistory)) return false;
       for (const h of r.freezeHistory as unknown[]) {
         if (!h || typeof h !== 'object') return false;
         const he = h as Record<string, unknown>;
         if (typeof he.date !== 'string' || he.date.length === 0) return false;
         if (he.kind !== 'earned' && he.kind !== 'consumed') return false;
         if (typeof he.amount !== 'number' || !Number.isInteger(he.amount) || he.amount <= 0) return false;  // 사전 review P1-3: 양의 정수
       }
     }
     ```

**§16 (migrate fast-path normalize) 정합**: v10 fast-path early-return은 `schemaVersion===10`만 — 손상 freezeHistory는 isValidUserShape guard가 read-time에 차단(false → re-migrate). xpHistory와 동일 contract.

---

## §4. 기록 지점 — recordDailyAnswer (xpHistory push 선례)

regen/consume 자리(user.ts ~line 367~392)에서 earned/consumed 산출 후 push. **freezeEngine 시그니처 무변경** — count before/after 캡처로 earned 산출:

```ts
// v3.48: freeze 활동 내역 기록 (충전 + 사용 통합). engine 시그니처 무변경 — count 차이로 earned 산출.
const freezeBefore = u.streakFreeze.count;
regenerateFreeze(u, now);
const earned = u.streakFreeze.count - freezeBefore;   // regen grant (0~2)

// ... consume (기존) ...
// const result = consumeFreezeForGap(u, gap); freezeConsumed = result.consumed;

if (!Array.isArray(u.freezeHistory)) u.freezeHistory = [];   // optional init guard (xpHistory 선례)
if (earned > 0) u.freezeHistory.push({ date: today, kind: 'earned', amount: earned });
if (freezeConsumed > 0) u.freezeHistory.push({ date: today, kind: 'consumed', amount: freezeConsumed });
```

**순서 주의**: earned는 regen 직후 캡처, consumed는 기존 `consumeFreezeForGap` 결과(`freezeConsumed`) 사용. push는 둘 다 saveUser 이전 같은 atomic write에 포함 (xpHistory T4 핀 패턴 정합).

**브랜드 신규 user**: v5 migration default `{count:2}`는 recordDailyAnswer 이벤트가 아니므로 history entry 없음 — 빈 내역에서 시작, 이후 실제 regen부터 누적. 정상.

---

## §5. 읽기 헬퍼 — getFreezeHistory (getXpHistory 선례)

```ts
export interface FreezeHistoryView {
  date: string;
  kind: 'earned' | 'consumed';
  amount: number;
}

export function getFreezeHistory(limit = 10): FreezeHistoryView[] {
  const user = getCachedUser();
  const raw = user?.freezeHistory ?? [];
  if (raw.length === 0) return [];
  // 최근 entry desc (배열 끝이 최신 — push 순서). slice 후 reverse.
  return raw.slice(-limit).reverse().map((h) => ({ date: h.date, kind: h.kind, amount: h.amount }));
}
```

### 다음 충전 계산 (순수 helper, schema 무관)

```ts
// count >= cap → null (가득 참). 아니면 lastEarnedAt + 7d - now (ms, 음수 가능 → 0 clamp).
export function getNextFreezeEtaDays(u: { streakFreeze: { count: number; lastEarnedAt: string } }, now = new Date()): number | null {
  if (u.streakFreeze.count >= 2) return null;   // FREEZE_CAP
  const lastMs = Date.parse(`${u.streakFreeze.lastEarnedAt}T00:00:00+09:00`);
  if (!Number.isFinite(lastMs)) return 0;
  const remainingMs = lastMs + 7 * 86400_000 - now.getTime();
  return Math.max(0, Math.ceil(remainingMs / 86400_000));
}
```

(위치: `freezeEngine.ts` — FREEZE_CAP/7d 상수 동일 모듈 재사용. count≥cap이면 timer 무의미하므로 null sentinel.)

---

## §6. Settings UI — "❄️ Streak Freeze" 섹션

settings.ts `renderSettings`의 `.settings-group` 패턴 추가:

```html
<section class="settings-group">
  <div class="settings-group-title">❄️ Streak Freeze</div>
  <div id="freezeStatus" class="settings-freeze-status"><!-- 보유 + 다음충전 read-render --></div>
  <div id="freezeHistory" class="settings-freeze-history"><!-- 활동 내역 list --></div>
  <details><summary>Streak Freeze란?</summary><p>...자동 충전/소비 설명...</p></details>
</section>
```

`bindHandlers`(또는 render 직후)에서 read-render:
- **보유**: `❄️ {count} / 2`.
- **다음 충전**: `getNextFreezeEtaDays(u)` → `null`이면 "보유 가득 (2/2)", 숫자 N이면 "다음 ❄️까지 약 {N}일 (답변 시 충전)" (N=0이면 "곧 충전돼요 (답변 시)").
- **활동 내역**: `getFreezeHistory(10)` → 각 entry `{formatKoreanDate(date)} ❄️ {+amount 충전 | -amount 사용 (연속 유지)}`. 빈 배열이면 "아직 내역이 없어요".
- 모든 텍스트 escapeHtml (date/amount는 안전하나 일관).

신규 claim 버튼 없음. 이벤트 신규 0 (settings 탭 전환마다 재렌더 → read-render로 충분).

CSS: `.settings-freeze-status` / `.settings-freeze-history` 최소 rule (기존 settings 토큰 재사용, list는 줄간격).

---

## §7. 검증 (DoD)

### 단위

- **migration** (`tests/state/migration*.spec.ts`): migrateUserToV10 — (a) v9 user → v10 + freezeHistory `[]`, (b) idempotent(v10 그대로), (c) downgrade barrier(v11 가정 무손상), (d) 손상 freezeHistory(non-array) → `[]`, (e) chain superset(v2~v9 prior 모두 v10 도달 — 대표 v5/v8 spot).
- **validation** (`isValidUserShape`): v10 freezeHistory entry valid/invalid(kind 오타 / amount NaN / amount≤0 / date 빈문자).
- **write** (`tests/state/user-record-with-freeze.spec.ts` 확장): recordDailyAnswer 시 (a) regen 발생 → earned entry push, (b) consume 발생 → consumed entry push, (c) 둘 다 0 → push 없음.
- **getFreezeHistory**: 빈 → `[]`, N개 → 최근 desc limit.
- **getNextFreezeEtaDays**: count<2 일반 / count=2 → null / 시계역행(remaining<0) → 0 / lastEarnedAt 손상 → 0.

### Smoke (`tests/smoke/`)

- settings 진입 → "❄️ Streak Freeze" 섹션 + 보유량(`❄️`) 텍스트 노출. seed freezeHistory 1 entry → 내역 1줄 표시.
- 외부 API mock 없으면 `serviceWorkers:'block'` 불요(설정만), 단 RSS auto-refresh 억제는 기존 settings smoke 패턴 따름.

### 3종 게이트 + 양쪽 TZ

각 task: tsc 0 / lint 0 / `npm test -- freeze|migration|user` PASS, 신규 N=5. 사이클 끝: `npm run lint && npm test && npm run test:smoke` + `TZ=America/New_York npm test`.

### Bundle

index canonical(stats/settings는 lazy chunk라 first-paint 영향 0 예상) + archive dual-record. settings chunk Δ 기록.

---

## §8. Codex 2-pass (schema bump → 강화)

- **사전 review** (T0, spec+plan 직후): **schema chain superset coverage가 본 사이클 핵심 checklist**.
  - chain superset(v2~v9 → v10 모든 경로) / user.ts 3 spot / fast-path normalize 정합(§16) / freezeHistory write 순서(regen earned 캡처 vs consume) / validation guard.
  - matchesInterest N/A.
  - **실행 결과 (2026-05-22, CLI direct 성공)**: **P0×2 + P1×4 전건 plan v2 흡수**.
    - **P0-1 (CRITICAL)**: migrator V2~V9에 `=== 10` pass-through barrier 누락 → v10 user가 hydrate chain에서 downgrade cascade (missions reset / 손상 freezeHistory를 corrupt 아닌 `[]`로 무마). v3.39 T1 `=== 9` 추가 패턴대로 8개 migrator에 `=== 10` 추가 (plan Task 1 Step 3b) + V2~V9 same-reference 테스트 8건(Step 3c).
    - **P0-2**: chain superset 테스트 강화 (persisted v10 + 비어있지 않은 missions 보존 + 손상 freezeHistory).
    - **P1-1**: userFixture 경로 `tests/unit/state/userFixture.ts` 정정 + `schemaVersion:9` 리터럴 sweep.
    - **P1-2**: 테스트 날짜 `getKstDateStr` anchor (UTC slice → NY TZ flake 차단).
    - **P1-3**: validation `Number.isInteger` (분수 차단) + 분수 테스트.
    - **P1-4**: history consumed 라벨 "연속 유지" 제거 (consumeFreezeForGap preserved=false 가능).
- **최종 review** (배포 직전): schema/migration은 되돌리기 영향 → **심층** 관점. `DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT`.

---

## §9. 비목표 (v3.49+)

- **수동 claim 버튼 / pending 풀** — 자동 시스템과 중복, UX 친화성 의문 (사용자 결정).
- **freeze 소비 시 push 알림 / 배너 live-update** — settings read-render로 충분.
- **history chart 시각화** — 텍스트 list만 (xpHistory chart와 달리 freeze 이벤트 희소).
- 로드맵상 다음: v3.49 M3 missions·뱃지 grid.

---

## §10. Risk

| Risk | 대응 |
|---|---|
| schema bump 누락 spot → silent corruption | user.ts 4 spot + migration chain — Codex 사전 review chain superset 필수 검토 |
| earned 산출이 regen 외 경로 영향 | regen 직후 즉시 before/after 캡처 (다른 mutation 사이 끼임 없음) |
| 다음 충전 timer가 cap에서 stale | count≥2 → null sentinel → "가득 참" 분기 |
| freezeHistory 무한 성장 | freeze 이벤트 희소(주1회 regen) — getFreezeHistory read-time slice. xpHistory와 동일 정책(무한 저장 OK) |
| KST anchor (date) 비-KST 환경 drift | entry date `getKstDateStr` + ETA 계산 `+09:00` anchor (Pattern A/D). 양쪽 TZ 테스트 |

---

## §11. Retro (2026-05-22)

### 11.1 결과 요약

| 항목 | 값 |
|---|---|
| Commits | 8 (T0 spec/plan/v2 3 + T1~T5 5) |
| vitest | 1588 / 1588 (양쪽 TZ), v3.47 baseline 1558 → **+30** (migration 12 + validation 6 + write 4 + eta 4 + settings 4) |
| smoke | 111 / 111 (v3.47 110 → +1), N=3 cold-fork 0 flake (1회 transient flake 재실행 PASS) |
| 3종 게이트 | tsc 0 / lint 0 / vitest PASS |
| Bundle (index canonical) | 9385 → **9384 B (-1, flat)** — schema/settings는 settings(4498)/persistence(7914) lazy chunk, first-paint 영향 0 |
| schema | **v9 → v10** (freezeHistory) |

### 11.2 Codex 2-pass

- **사전 review** (CLI direct 성공): **P0×2 + P1×4 전건 in-cycle 흡수**.
  - **P0-1 (CRITICAL)**: migrator V2~V9에 `=== 10` pass-through barrier 누락 → v10 user hydrate 시 downgrade cascade. v3.39 T1 `=== 9` 패턴대로 8개 barrier 추가 + V2~V9 same-reference 테스트 8건. **schema bump의 chain superset 규율을 사전 review가 정확히 catch** — 구현 전이 아니었으면 production에서 매 load마다 missions reset/데이터 손상.
  - P0-2 chain superset 테스트 강화 / P1-1 userFixture 경로+sweep / P1-2 KST anchor 테스트 / P1-3 Number.isInteger / P1-4 "연속 유지" 라벨 제거.
- **최종 review**: 배포 직전 dispatch (schema/migration → 심층 관점).

### 11.3 신규 lessons

1. **schema bump chain superset = 명시적 `=== N` barrier sweep** — 각 migrator가 `>= N`이 아니라 명시적 `=== N` 나열을 쓰므로, 신규 버전 N 추가 시 **V2~V(N-1) 전부**에 `=== N` 추가 필수. v3.39(v9)·v3.48(v10) 연속 사례. CLAUDE.md "Schema bump chain superset 필수 spot"에 "migrator barrier sweep" 명문화 후보.
2. **schemaVersion 리터럴 sweep 2종** — (a) 타입 `schemaVersion: N` User 리터럴(tsc가 catch) (b) 런타임 `.toBe(N)` 단정(tsc 통과, 전체 suite로만 catch). bump 시 둘 다 grep. mkUser 중앙 fixture는 `freezeHistory: []` 동반.
3. **incomplete sv4 seed + 강화된 validation** — primeOnboardedUser(sv4)는 plantStateByInterest 미포함이라 신규 v10 plant guard에서 null. addInitScript 보강 필요 (plant-detail smoke 선례).

### 11.4 v3.49+ carry

- 신규 carry 0 (전건 in-cycle).
- 비목표 유지: 수동 claim 버튼/pending 풀, history chart, 배너 live-update.
- 로드맵상 다음: v3.49 M3 missions·뱃지 grid 게임화.

---

**End of spec.**
