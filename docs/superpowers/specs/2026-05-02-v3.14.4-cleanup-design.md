# DG v3.14.4 — Cleanup (B+ scope, lessons graduation 포함)

**Date**: 2026-05-02
**Series**: v3.14.x (cleanup)
**Baseline (claimed)**: v3.14.3 HEAD `e185a14` — 704 vitest pass / lint 0 errors / bundle gzip JS **32.10 kB** (v3.14.3 retro 보고값)
**Baseline (실측 dist, 2026-05-02 spec 작성 시점)**: `dist/assets/index-C6loBrBg.js` gzip **31.22 kB** — 보고값과 **Δ −0.88 kB drift** 관찰됨. T0에서 reconcile 필요 (Lesson #4의 bundle 버전).
**Playwright baseline**: smoke 93/93 (warm) / cold-start fresh fork (T0 N=2 측정 결과) consistent fail **4건** (Plan T3 scope 정정 — v3.14.3 retro 보고는 부정확):
- `tests/smoke/desktop-layout.spec.ts:84` — viewport 1440×900 + 1920×1080 (`firstCard.waitFor`, 5000ms)
- `tests/smoke/tier-downgrade.spec.ts:61` — 404 case (`toHaveAttribute('data-tier', '2')`, default 5s)
- `tests/smoke/tier-downgrade.spec.ts:72` — 500 case (동일)
- 합산 4 fail (desktop 2 + tier-downgrade 2)
- 그 외 cold-2 cascade (heatmap/v3.12-game-feedback/card-translation/interests-edit/layout/missions-tab)는 inconsistent — N=2에 1회만 fail, T3 scope 외 (v3.14.5+ open)
**Spec author**: Claude (controller)
**Status**: APPROVED for plan (사용자 합의 2026-05-02 — 옵션 B+)

---

## 1. 사이클 정의

**v3.14.4**는 v3.14.3에서 carry-forward된 backlog를 처리하면서, **lessons #1/#2/#4/#5를 영구 가드레일로 graduation**하는 cleanup 사이클이다. v3.14 시리즈를 깔끔히 닫고 v3.15 Game World로 진입하기 위한 마지막 무빚 단계.

배포 단위는 **단일 commit pack → main 직접 commit (직배포 패턴, origin remote 미등록)**. 사용자 "배포해줘" 명시 시점에만 `npm run build` → `firebase deploy --only hosting`.

### 1.1 옵션 B+를 채택한 이유 (사용자 합의)

- **옵션 A (numeric guards만)**: P3 잔품 16건이 v3.15에 또 누적됨. 사이클 entropy 차단 실패.
- **옵션 B (표준)**: 잔품 흡수 + flake timeout bump으로 균형. 단, **lessons graduation 누락**.
- **옵션 C (full)**: smoke flake centralize 정책 설계 시 scope creep 위험. 가치 대비 비용 과대.
- **옵션 B+ (채택)**: B + (가)Codex 사전·최종 review 표준화 + (나)baseline 측정 Task 0 우선화 + (다)lessons graduation. 셋 다 **future cycle 생산성 영구 향상** ROI가 높음.

### 1.2 명시 제외 항목

- **v3.15 Game World (캐릭터/정원)** — 게임화 시리즈 3/3, 별도 사이클 design 단계.
- **smoke flake centralize 정책** — 본 사이클은 simple timeout bump (5→10s)만. helper/centralize는 cold-start 2번째 등장 시점에 mechanical refactor (옵션 C 거부 사유).
- **새 기능 추가** — cleanup 한정.
- **escapeHtml 추가 스윕** — v3.12.1 P2-NEW-3에서 graduate 완료. 추가 발견 시 v3.15 진입 task로 격하.

---

## 2. 범위 (T0~T17 = 18 task)

### 2.1 ⚙️ 운영 / 측정 (T0, T13, T15, T16, T17 = 5건)

| ID | 항목 | 출처 | Acceptance |
|---|---|---|---|
| **T0** | 사이클 baseline 측정 — `npm test` count + `npm run build` bundle gzip + smoke `npx playwright test` cold-start 결과 캡처. v3.14.3 retro 32.10 kB 주장 vs 실측 31.22 kB drift 원인 확인. v3.14.2 "2 known flake" 정체와 v3.14.3 4 cold-start flake 동일 집합인지 reconcile (Lesson #4의 bundle 버전 + 4번째 lesson 검증). | (나)+(라) | T0 결과를 plan 파일 `§Baseline` 섹션에 명기 |
| **T13** | Codex review template 표준화 — plan template (writing-plans 산출 plan 파일)에 "spec phase Codex 사전 review" + "최종 Codex review" 두 step 명시. v3.14.3 lesson #1: NaN 직렬화 한계는 plan controller가 grep으로 못 잡음 → 사전 review 필수. | (가) Lesson #1 graduation | `docs/superpowers/plans/` 작성 시 사용할 template 1곳 수정. 미존재 시 본 cycle plan에서 best-practice 명시 후 v3.15 사이클부터 적용. |
| **T15** | Codex 사전 review (spec phase) — spec + draft plan을 Codex가 1-pass review. P0/P1/P2 분류, in-cycle 반영. | OPS | Codex 출력 첨부, in-cycle 반영 task 명세 |
| **T16** | Codex 최종 review — 모든 main work commit 후 1-pass. DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT 분류. | OPS | Codex 출력 첨부, deploy 게이트 |
| **T17** | 사이클 retro + memory 갱신 — 본 spec §8 retro 섹션 추가, MEMORY.md 갱신, `project_daily_growth_v3144.md` 작성 commit. | OPS | retro pointer commit |

### 2.2 🔴 데이터 가드 (T1, T2 = 2건, codex 사전 P2)

| ID | 항목 | 위치 | Acceptance |
|---|---|---|---|
| **T1** | `usage.count` numeric guard — `Number.isFinite` check 후 NaN/Infinity 시 0으로 normalize | `src/state/usage.ts:33-35` (실측 시 line 정확화) | v3.14.3 T3 firstSeenAt 패턴 동일 적용. 신규 spec 2~3 it-blocks (NaN / Infinity / 정상값 round-trip) |
| **T2** | `missions.active.{windowStart, progress}` numeric guard — active 내부 필드. cumulative count는 isFinite 있으나 active 미적용 (codex 사전 P2). | `src/state/missionTypes.ts:25-26` (실측 시 line 정확화) | NaN/Infinity 가드 + 기본값 fallback. 신규 spec 2~3 it-blocks |

### 2.3 🟡 테스트 안정성 (T3 = 1건, codex P1-1/P1-2 반영)

> **사전 확인**: `playwright.config.ts:5` 글로벌 `timeout: 30_000` (test 단위 30s). 따라서 Codex P1-1 지적대로 `test.timeout` bump으로는 cold-start race를 해결 못 함 — 진짜 부족분은 Playwright의 default `expect` (5s) / action (5s) 대기. v3.14.3 T5의 실제 효과 mitigation은 **DOM hydrate signal `await expect(...).toBeVisible()`** 추가였음 (timeout 변경 아님).

| ID | 항목 | 출처 | Acceptance |
|---|---|---|---|
| **T3** | smoke spec cold-start race 안정화 — **per-spec DOM hydrate signal 우선** (v3.14.3 T5와 동일 패턴), timeout config 변경 금지(global 30s 충분). T0 N=2 측정 결과 실제 fail spec **4건** (v3.14.3 retro 보고 정정):<br>(a) `tests/smoke/desktop-layout.spec.ts:84` — viewport 1440×900 (`firstCard.waitFor`, 5000ms)<br>(b) `tests/smoke/desktop-layout.spec.ts:84` — viewport 1920×1080 (동일)<br>(c) `tests/smoke/tier-downgrade.spec.ts:61` — 404 case (`toHaveAttribute('data-tier', '2')`, default 5s)<br>(d) `tests/smoke/tier-downgrade.spec.ts:72` — 500 case (동일) | v3.14.3 Lesson #2 / codex P1-1, P1-2 | (1) 각 spec의 race 원인을 plan 단계에서 식별 (desktop briefing card render race / tier downgrade img.onerror race).<br>(2) `await expect(<readiness-anchor>).toBeVisible({ timeout: 10_000 })` 또는 기존 expect/waitFor에 `{ timeout: 10_000 }` 추가. static element는 readiness 신호 아님 (v3.14.3 T5 codex P1-2 lesson).<br>(3) cold-start fresh `vite preview` fork N=5 모두 PASS.<br>(4) helper 추출 안 함 (Q1-B′, "한 번만 쓰는 유틸 금지"). 4건 동일 패턴이라도 spec별로 inline.<br>(5) global `expect`/action timeout config 변경 안 함. centralize 정책은 §6 v3.14.5+ backlog.<br>(6) cold-2-only cascade fail (heatmap/v3.12-game-feedback/card-translation/interests-edit/layout/missions-tab)은 N=2에 1회만 발생하는 inconsistent flake — T3 scope 외, v3.14.5+ open. |

### 2.4 🟡 프로세스 / 계측 (T4, T5 = 2건)

| ID | 항목 | 출처 | Acceptance |
|---|---|---|---|
| **T4** | vitest count baseline reconcile — **두 단계 drift 모두 명시적 추적** (codex P1-3 반영):<br>(i) v3.14.2 retro 보고 **693** vs v3.14.3 사이클 시작 실측 **697** = Δ +4 미해명 (v3.14.3 lesson #4 원본).<br>(ii) v3.14.3 retro 보고 **704** vs T0 신규 실측 = Δ ?? (본 사이클).<br>plan template `§Test Count Baseline` 섹션에 (i)+(ii) 추적 포맷 표준화 + 신규 task별 +N 분담 명시. | Lesson #4 graduation / codex P1-3 | spec § + plan §에 둘 다 (i)+(ii) 명기. 가능 원인 가설(it.each / nested describe / dynamic count) 검토 후 미해명 시 "v3.15+ open"으로 명시. |
| **T5** | plan template `npm run lint` step 추가 — 각 task implementation step에 lint 검증 step 포함. | Lesson #5 graduation | template 수정. 본 cycle plan부터 적용 |

### 2.5 🟡 Per-task Minor P3 일괄 cleanup (T6~T11 = 6 task, 총 16건 흡수)

v3.14.3 retro carry-forward "Per-task Minor backlog 누적 P3급 ~16건". 영역별로 묶어 6 task로 처리.

| Task | 묶음 항목 | 개수 |
|---|---|---|
| **T6 spec-naming** | T3 M-1 (test name 일관성) + T6 M2 (assert 메시지) + T6 M4 (describe 그룹화) | 3 |
| **T7 wrap-scope-cleanup** | T7 M1 (wrap.querySelector 잔존 callsite 추가 발견 시) + T7 M2 (memo wrap 누락 가드) | 2 |
| **T8 mission-engine-comment** | T8 M1 (KST cross-midnight 주석 정리) + T8 M2 (mission rotation deterministic 주석) + T8 M3 (test seed 명시) | 3 |
| **T9 toast-anchor-jsdoc** | T11 M1 (`removeToast` JSDoc anchor 잔존) + T11 M2 (TOAST_DURATIONS 주석 보강) | 2 |
| **T10 hydrate-badges-spec** | T13 M1 (heading filter spec 주석) + T13 M2 (legacy ID list 주석) + T13 M3 (catalog filter test 추가 case) | 3 |
| **T11 chain-detach-followup** | T14 M1 (3rd it-block 후 4th case "외부 click closeAll" 추가 시점 결정) | 1 |
| **소계** | | **14** |

> 누적 16건 중 14건만 명세 — 2건은 P4 격하: (a) T3 cast 패턴 local-alias 변환 (pure rename, ROI 낮음), (b) T10 welcome-gamification mini-grid `renderBadgeButton` export (conditional, 등장 시).

### 2.6 🟡 메타 정정 (T12 = 1건)

| ID | 항목 | 출처 | Acceptance |
|---|---|---|---|
| **T12** | spec retro commit count + plan trailing whitespace 정정 — codex P2-1 (commit count 19+로 retro 반영됨, 사후 검증) + codex P2-4 (trailing whitespace docs only) | v3.14.3 codex 최종 P2-1, P2-4 | docs only, code 영향 0 |

### 2.7 📚 Lessons graduation (T14 = 1건, codex P1-5 반영)

> **포맷 일관성 (codex P1-5)**: `COMMON_MISTAKES.md` §1~§4는 모두 **Symptom / Root cause / Fix·Prevention** 3-step 형식 (lesson 90줄 분량). (a)(b)(c) 세 lesson은 동일 형식으로 graduate 가능하나, **(d) origin remote 미등록 / main 직배포는 "실수"가 아닌 "프로젝트 컨벤션"** — mistake 형식과 의미적 불일치. 따라서 **(d)는 분리하여 `CLAUDE.md` (project 루트)에 1줄 추가**, COMMON_MISTAKES.md는 mistake-form lesson 3건만 추가.

| ID | 항목 | 출처 | Acceptance |
|---|---|---|---|
| **T14** | **(a)(b)(c)는 `COMMON_MISTAKES.md` §5/§6/§7로 추가** — Symptom / Root cause / Fix·Prevention 3-step 형식, 기존 §1~§4 컨벤션 유지:<br>• §5 Playwright cold-start vs warm (v3.14.3 Lesson #2)<br>• §6 vitest count baseline drift (v3.14.3 Lesson #4)<br>• §7 사이클-끝 lint check (v3.14.3 Lesson #5)<br>**(d)는 `CLAUDE.md` (프로젝트 루트, "Project Overview" 섹션 끝 또는 신규 "Deploy 패턴" 1줄 섹션)에 컨벤션 1~2줄로 명시**: "origin remote 미등록 — main 직배포 패턴 (build → `firebase deploy --only hosting`). PR 워크플로 N/A." | (다)+(마) graduation, codex P1-5 | (a)(b)(c) ~70~80줄 추가, (d) 1~2줄 추가. 본 cycle 후 모든 신규 세션이 자동 로딩(CLAUDE.md auto-inject + COMMON_MISTAKES.md mandatory read). |

---

## 3. 검증 정책

### 3.1 vitest

- **baseline**: 704 (v3.14.3 retro 보고). T0에서 실측 reconcile (T4 (i)+(ii) drift 추적 포함).
- **target Δ 분담** (codex P1-4 산식 정렬): T1 +2~3 + T2 +2~3 + T6~T11 ~5~8 = **+9~14**.
- **target 절대값**: 704 baseline + 9~14 = **713~718**.
- **DoD**: lint 0 errors / TS strict 0 errors / 모든 신규 spec N=5 5/5 PASS (per-task review 표준). target lower bound 미달 시 plan 단계에서 task별 +N 재산정 후 spec §3.1·§4 동기화.

### 3.2 Playwright smoke

- **baseline**: 93/93 (warm) / cold-start 시 4 first-run flake.
- **target**: 93/93 (cold-start 포함, T3 timeout bump 후 N=5 5/5).
- **신규 spec 0건** — Per-task review에서 smoke 추가 정당화 시점 등장하면 T6~T11에 흡수. 그렇지 않으면 0.

### 3.3 Bundle size

- **baseline (vite reporter, chunked gzip estimation)**: 32.10 kB (v3.14.3 retro 보고값과 동일).
- **baseline (CLI `gzip -c dist/assets/index-*.js | wc -c`, single-stream)**: 31.22 kB.
- **drift 원인 (T0 reconcile)**: 두 값은 동일 build에 대한 **측정 도구 차이**. vite reporter는 청크별 추정, CLI는 단일 스트림 실측. 코드 변동 0.
- **target Δ**: ±0.15 kB (numeric guard 2건 + Per-task Minor — 코드 변경 적음). Lessons graduation은 docs only로 bundle 영향 0.
- **DoD**: T16/T17에서 두 도구 모두 측정값 spec §8 retro에 명기. **단일 표준 채택은 v3.15+ open** (§6 backlog) — 본 사이클은 dual-record.

### 3.4 Codex 2-pass

- **사전 (T15)**: spec + draft plan 작성 직후. P1/P2 분류 in-cycle 반영.
- **최종 (T16)**: 모든 main work commit 후. DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT.

### 3.5 사이클-끝 검증 (T17 직전)

- `npm run lint && npm test && npm run test:smoke` 3종 PASS 확인.
- `npm run build` 후 bundle 실측.
- N=5 신규 spec 5/5 reconfirm.

---

## 4. DoD (Definition of Done)

1. T1~T14 main work commit 완료 (T0 baseline + T13/T15/T16/T17 운영).
2. vitest count = baseline 704 + 9~14 = **713~718** (실제 +N은 T17 retro에 명기). 산식 §3.1.
3. Playwright 93/93 cold-start + warm 모두 PASS (N=5).
4. Bundle Δ ±0.15 kB (실측 기준).
5. Lint 0 / TS 0.
6. Codex 최종 review = DEPLOY_APPROVED 또는 APPROVED_WITH_NOTES (P0=0).
7. `.claude/COMMON_MISTAKES.md`에 lessons graduation 4건 반영 (T14).
8. plan template에 lint step + Codex 2-pass + baseline 측정 step 명시 (T5/T13).

---

## 5. Risk & Open

| Risk | 완화 |
|---|---|
| **T0 baseline drift 원인 미확인** — 실측 31.22 vs 보고 32.10 (Δ −0.88 kB) | T0에서 git log + dist mtime 대조. 이전 deploy build인지 확인. 미해결 시 T17 retro에 "drift 원인 미해결 — v3.15 진입 시 재측정" 명기. |
| **T3 cold-start 4 spec timeout bump으로 부족** | N=5 fail 시 centralize policy 검토 (옵션 C로 격상) — 단, 본 사이클 scope 초과 시 v3.14.5 hotfix로 이월. 기준: N=5 중 1회라도 fail = 격상. |
| **T13 plan template 미존재** | 첫 발견 시 본 cycle plan을 template으로 마련. v3.15부터 강제. |
| **T15 Codex 사전 review에서 P0 발견** | in-cycle 반영. P0 다수 시 spec 재작성 (사용자 재합의 필요). |
| **누적 P3 16건 중 14건만 처리, 2건 P4 격하** | 격하 2건은 §2.5 명시. v3.15 진입 시 자연 해소 또는 트리거 조건 도래 시 hotfix. |

---

## 6. v3.14.5+ Backlog (v3.14.4에서도 처리 안 하는 항목)

- **smoke flake centralize 정책 (helper/`smokeTimeout` const)** — cold-start 2번째 등장 시 mechanical refactor 트리거.
- **smoke cold-2-only inconsistent cascade** (T0 N=2 측정) — heatmap / v3.12-game-feedback / card-translation / interests-edit / layout / missions-tab. N=2에 1회만 발생. N=3+로 재현 일관성 확인 후 root cause 진단 필요.
- **bundle 측정 도구 표준화** — vite reporter (chunked gzip estimation, 32.10 kB) vs `gzip -c | wc -c` CLI (single-stream, 31.22 kB). 0.88 kB drift는 도구 차이. v3.15+에서 단일 표준 채택.
- **datetime mock helper 추출** — KST flake 2번째 등장 시 트리거.
- **격하된 P4 2건** — T3 cast pattern local-alias / T10 welcome-gamification mini-grid renderBadgeButton export.
- **신규 P3 (본 cycle per-task review에서 발생)** — v3.15 진입 시 자연 해소 또는 hotfix.

---

## 7. v3.15 진입 조건

- 본 cycle T17 완료.
- 사용자 "v3.15 Game World brainstorm" 트리거 명시.
- 다음 사이클 첫 task는 T0 baseline 측정 (본 cycle Lesson #4 graduation 적용 첫 사례).

---

## 8. 사이클 retro (T17 최종 — 2026-05-03)

### 8.0 핵심 결과

- **Tasks**: T0~T17 (18 task) + Hotfix-A1 + B1/C2 정정 + T3 review fix → **18 commits + 1 hotfix = 19 commits**
- **Vitest**: 704 (baseline) → **711 pass / 711 total** (Δ +7)
  - T1 +3 (usage-numeric-guard) / T2 +3 (migration-mission-numeric-guard) / T10 +1 (badge drift mixed case)
  - T3/T6/T8/T9: pure refactor / docs (+0). T11: no-op (+0). T7: T0 0 hit no-op (+0).
  - **DoD lower bound 9 미달 (+7 < 9)** — spec §3.1 작성 시 T6~T11 +5~8 추정이 과대평가. 실제 T6~T11 합산 +1만 (T10), 나머지는 refactor라 영향 0. P3 polish 흡수 본질이 spec 일관화·주석 정리이므로 신규 it 비대 발생은 부적절. **scope 충실 vs DoD 정렬 trade-off** — Codex 최종 review 수용 (커버리지 갭 없음).
- **Playwright**: 93/93 PASS cold-start fresh fork (54.4s, T3 TZ fix 후 안정)
- **Bundle (CLI gzip)**: 31.22 → **31.28 kB** (Δ +0.06, DoD ±0.15 충족 ✅)
- **Bundle (vite reporter)**: 32.10 → **32.16 kB** (Δ +0.06, dual-record per §3.3)
- **TS strict / Lint**: 0 / 0 errors (8 pre-existing warnings — `any` types in achievements.spec.ts / user-migration.spec.ts, 본 사이클 무관)

### 8.1 vitest count drift 추적 결과 (T4 graduation, codex P1-3)

본 cycle Lesson #4 graduation 작업으로 두 단계 drift 모두 **완전 해명**됨.

| 단계 | 보고값 | 실측값 (T4 검증) | Δ | 원인 |
|---|---|---|---|---|
| (i) v3.14.2 retro → v3.14.3 시작 | 693 | 697 | +4 | (i-a) v3.14.2 retro 보고 693 vs `5f82a59` 실측 **694** (=692 pass + 2 fail) = +1 (retro 보고 시 인터미턴트 fail 1건 mask). (i-b) `5f82a59` (694) → v3.14.3 T3 첫 commit `609740b` (697) = +3: T3 implementer 신규 추가 `tests/unit/state/seen-firstseenat-numeric-guard.spec.ts` 3 it-blocks. |
| (ii) v3.14.3 retro → 본 사이클 T0 | 704 | 704 (실은 703 pass + 1 fail mask) → hotfix `9c9c7d2` 후 704 pass clean | 0 net (mask cleared) | v3.14.3 retro 704 unit count 정확하나 1 fail (sweep-wiring KST date-RNG dependent, v3.13 T1 spec drift since) mask. T0 implementer 발견 → hotfix 결정론화. |
| (iii) 본 사이클 T0 → 끝 | 704 | 711 | +7 | T1 (+3) / T2 (+3) / T10 (+1). 산식 §3.1 기록. |

### 8.2 Codex review 결과

| 시점 | 결과 | 분류 | 처리 |
|---|---|---|---|
| 사전 (T15, spec phase) | 5 P1 발견 | P0=0/P1=5/P2=0 | 5건 모두 in-cycle 반영 (spec v2 commit `f823e4f`) |
| 최종 (T16, deploy gate) | DEPLOY_APPROVED | P0=0/P1=0/P2=2 | IR-1 + IR-2 v3.14.5+ backlog로 이월 |

**최종 review 분석 가치**:
- Q1 데이터 가드 정합성: T1/T2 패턴 일관, C6 invariant 보존 (sanitize 후 caller가 받은 객체 사용 — `migrateUserToV3` 외부 진입점 0)
- Q2 T3 TZ 견고성: cardnews.spec.ts 선례 1:1 매칭 + Playwright typed args 호환
- Q3 Lessons 깊이: 미래 세션 자동 가드레일 작동 가능 수준
- Q4 사후 정정 정확성: 3개 부정확 모두 커버 (704-pass mask, 4-flake 오진, bundle 측정 도구 차이)
- Q5 No-op 정당화: T11 single-active-modal 모델 + T7 0 hit 증거 충분
- Q6 Bundle 측정: 도구 차이 분리 명확
- Q7 DoD 미달: scope 충실 trade-off 수용 (커버리지 갭 없음)

### 8.3 신규 lessons (사이클 중 발견)

1. **사이클 시작 baseline 실측의 가치 (Lesson #4 강화)** — Hotfix-A1 패턴: retro 보고 vitest count가 정확해 보여도 intermittent fail mask 가능. T0 baseline 측정이 단순 숫자 확인 이상으로 spec drift 발견의 핵심 안전망.

2. **per-task implementer/reviewer 깊이의 가치 (Lesson #1 강화)** — T3 implementer가 "cold-start expect timeout race" 가정에서 시작했으나 실측 trace로 진짜 root cause인 TZ mismatch 발견. plan-prescribed timeout bump만으로는 안 풀렸을 것. v3.4 cardnews lesson이 "v3.5+ sweep value" 예측한 것이 v3.14.4에서 실제 confirmation 받음 — **lesson 자체가 미래 사이클 예측 device로 작동**.

3. **DoD 산식 작성 시 scope vs count trade-off 명시 필요 (Lesson #5 보완)** — spec §3.1 작성 시 P3 polish 흡수가 신규 it-block 추가로만 카운트 증가한다고 가정했으나 refactor 위주라 +0. **DoD 산식에 "최소 +N is hard target vs scope-faithful no-op acceptable"을 명시**하면 차기 사이클 산정 정확도 향상.

### 8.4 v3.14.5+ Backlog (carry-forward)

기존 §6 backlog 외:

- **IR-1 (Codex 최종 P2)**: `tests/smoke/heatmap.spec.ts:29-32,85-87` UTC ISO seed false-green 위험. v3.14.5에서 Node-side `getDateStr()` args + cell 일치 assertion 추가.
- **IR-2 (Codex 최종 P2)**: 본 §8 lesson count + T11 absorbed/relegated 정합성 — 본 retro에서 정리 완료 (16건 중 흡수 12, 격하 5: T6/T8/T9/T10/T11 = 12, T7 M1+M2 = 2, P4 격하 2, T11 M1 격하 1).
- **TZ guard sweep (T3 reviewer 발견)**: 6 smoke spec + 3 unit spec — heatmap / v3.12-game-feedback / card-translation / chat-bubble-action / modal-overhaul / disabled-button / interests-edit / modals-interests / settings-interests-section / triggers-mission. 잠재 dormant TZ bomb. v3.14.5 일괄 sweep.
- **T1 P2 (review backlog)**: negative count slips through (`{ count: -1000 }` LS 편집 시 cap bypass). 현재 NaN/Infinity guard만. `parsed.count >= 0` 추가.
- **T1 P2 (review backlog)**: fractional count (e.g., 5.5) passes — cosmetic.
- **T2 P2 (review backlog)**: `progressDates: [null, 42, "..."]` 같은 array 내부 corruption guard 미적용. v3.14.3 firstSeenAt graduation pattern 동일하게 `progressDates` 처리.
- **T2 P2 (review backlog)**: shape guard for malformed `MissionInstance` element (missing defId 등).
- **사이클-끝 build 도구 표준화**: vite reporter vs CLI gzip 측정 도구 차이 0.88 kB. v3.15+ 단일 표준 채택.
- **bundle 절대값 이전 사이클 reconcile**: v3.14.3 retro 32.10 등 보고가 vite reporter 기준이었음. 차기 사이클 보고는 CLI gzip 단일 표준 명시.
- **사이클 시작 시 hotfix triage 절차**: T0 baseline 측정 시 known intermittent fail 발견하면 즉시 hotfix triage (본 사이클 A1 패턴). plan template에 "T0 + hotfix slot" 추가 검토.
- **CLAUDE.md / `.claude/` git tracking 결정**: T14 graduation 시 CLAUDE.md만 commit, `.claude/COMMON_MISTAKES.md`는 `.gitignore` 패턴 존중으로 untracked 유지. functional은 정상이지만 cross-developer 가시성 부재. v3.15+에서 `.claudeignore`/`.gitignore` 정책 재검토.

### 8.5 차기 사이클 후보

- **v3.14.5 hotfix** (필요 시) — IR-1 + TZ sweep + numeric guard P2 묶음. 또는 v3.15 시작 시 자연 흡수.
- **v3.15 Game World** (게임화 시리즈 3/3, 캐릭터/정원). 트리거: `"v3.15 Game World brainstorm"`. `cumulative.{period}Count`을 진화 anchor로 활용.

### 8.1 vitest count drift 추적 결과 (T4 graduation, codex P1-3)

본 cycle Lesson #4 graduation 작업으로 두 단계 drift 모두 **완전 해명**됨.

| 단계 | 보고값 | 실측값 (T4 검증) | Δ | 원인 |
|---|---|---|---|---|
| (i) v3.14.2 retro → v3.14.3 시작 | 693 | 697 | +4 | (i-a) v3.14.2 retro 보고 693 vs `5f82a59` 실측 **694** (=692 pass + 2 fail) = +1 (retro 보고 시 인터미턴트 fail 1건 mask). <br>(i-b) `5f82a59` (694) → v3.14.3 T3 첫 commit `609740b` (697) = +3: T3 implementer 신규 추가 `tests/unit/state/seen-firstseenat-numeric-guard.spec.ts` 3 it-blocks (P1-1 fix 일부). git diff 확인. |
| (ii) v3.14.3 retro → 본 사이클 T0 | 704 | 704 (실은 703 pass + 1 fail mask) → hotfix `9c9c7d2` 후 704 pass clean | 0 net (mask cleared) | v3.14.3 retro 704 unit count 정확하나 1 fail (sweep-wiring KST date-RNG dependent) mask. T0 implementer 발견 → hotfix로 결정론화. |

**T4 측정 trail (직접 git checkout + `npm test` 실행)**:

```text
5f82a59 (v3.14.2 끝):     692 pass + 2 fail / 694 total
609740b (v3.14.3 T3):     695 pass + 2 fail / 697 total  ← lesson #4 "697" 정합
e185a14 (v3.14.3 retro):  703 pass + 1 fail / 704 total  ← retro 704 mask
9c9c7d2 (T0 hotfix):      704 pass / 704 total           ← clean baseline
a61d55e (T3 review fix):  710 pass / 710 total           ← T1+T2 누적 정확
```

**graduation 결론**: v3.14.3 lesson #4 "+4 미해명"은 (a) retro 측정 mask 1 + (b) 신규 spec 3개로 100% 해명. 사이클 시작 전 baseline은 **commit checkout + `npm test` 직접 측정**을 표준 절차로 graduate (가설 it.each / nested describe / dynamic count 모두 적용 안 됨, 단순 측정 mask + 신규 spec 추가). 본 표 포맷이 v3.15+ 신규 cycle plan template `§Test Count Baseline` 표준.

---

## 9. Spec 변경 이력

| 시점 | 변경 | 사유 |
|---|---|---|
| 2026-05-02 작성 | 초안 | 사용자 옵션 B+ 합의 후 |
| 2026-05-02 v2 | Codex 사전 review 5건 P1 in-cycle 반영 | (P1-1) §2.3 T3 timeout bump → DOM hydrate signal 패턴 (global 30s 충분, expect/action 5s가 진짜 부족) / (P1-2) §2.3 T3 settings spec 파일 후보 2개 명시 (translation / delete-all), T0에서 확정 / (P1-3) §2.4 T4 v3.14.2 retro 693 vs v3.14.3 시작 697 drift도 명시적 추적 / (P1-4) §3.1 + §4 산식 9~14 = 713~718 정렬 (lower bound 9 vs 10 inconsistency 해소) / (P1-5) §2.7 T14 (d) origin pattern은 mistake-form 부적합 → CLAUDE.md로 분리, COMMON_MISTAKES.md는 (a)(b)(c) lesson 3건만 |
| 2026-05-03 v3 | T0 측정 결과 반영 + B1/C2 정정 | (B1) §header Playwright baseline + §2.3 T3 4-spec list: heatmap/modal/polish/settings(추정) → desktop-layout(1440+1920) + tier-downgrade(404+500) (실측 정정). v3.14.3 retro 4-flake 보고는 부정확. cold-2-only inconsistent cascade는 §6 v3.14.5+ open. (C2) §3.3 bundle drift는 측정 도구 차이(vite reporter vs `gzip -c \| wc -c`) — 본 사이클은 두 값 다 기록, v3.15+에서 표준화. (Hotfix-A1) v3.14.3 retro 보고 704 pass는 실제 703 pass + 1 KST date-RNG dependent fail (sweep-wiring spec drift since v3.13 T1). hotfix `9c9c7d2`로 결정론화 — 본 사이클 baseline 진입 전 cleared. |
