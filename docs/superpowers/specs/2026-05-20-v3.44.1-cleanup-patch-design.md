# v3.44.1 — 설거지 대청소 Patch (Lightweight)

**Date**: 2026-05-20 (오늘 5번째 cycle, v3.41~v3.44 + patch)
**Type**: Patch cycle — lesson graduate (A) + working tree cleanup (B) + knip config tuning (C) 묶음. 신규 기능 0, schema 0, production 코드 변경 0.
**Reference precedent**: v3.14.1 (P2 cleanup batch), v3.20.1 (production hotfix), v3.18.1 (light fix bundle).

---

## §0. 배경 + scope

### 0.1 사용자 결정 (2026-05-20)

v3.44 deploy 직후 사용자 "더 할거는?" → controller 4 후보 ROI 비교 → 사용자 "A + B + C 묶음" 명시. 설거지 대청소 마무리.

| 결정 항목 | 결정 | 근거 |
|---|---|---|
| A — v3.44 L1~L4 graduate | ✅ | Dynamic namespace import knip 한계 + JSDoc @public + DoD audit 주기 |
| B — Working tree cleanup | ✅ | untracked 5건 정리 (gitignore 추가 또는 add) |
| C — Knip configuration hints 정리 | ✅ | 3건 redundant entry / functions/lib ignore |
| 명명 | v3.44.1 patch | precedent: v3.14.1 / v3.18.1 / v3.20.1 |

### 0.2 T0 인벤토리 (실측, 2026-05-20)

#### 0.2.1 A — v3.44 L1~L4 graduate 대상

| ID | Lesson | Target 위치 | Effort |
|---|---|---|---|
| L1 | Dynamic namespace import knip 추적 한계 | COMMON_MISTAKES §19 신규 (paired with §13~§18 패턴) | XS |
| L2 | Codex CLI direct + `< /dev/null` 첫 성공 (graduated reaffirmation) | retro에만 명시 (이미 CLAUDE.md graduate됨) | 0 |
| L3 | JSDoc `@public`/`@internal` semantic tag로 dead-code 도구 false-positive 차단 | COMMON_MISTAKES §19에 묶음 (L1과 같은 항목) | XS |
| L4 | `ignoreExportsUsedInFile:true` DoD audit 주기 | CLAUDE.md "Bundle 측정 표준" 섹션 후 "Dead-code audit 주기" 신규 mini-section | XS |

→ §19 1건 + CLAUDE.md mini-section 1건 (audit 주기). 효과적 묶음.

#### 0.2.2 B — Working tree untracked 항목

```
?? .claudeignore
?? docs/INDEX.md
?? docs/QUICK_REFERENCE.md
?? docs/archive/
?? docs/superpowers/plans/2026-05-01-v3.14.1-p2-cleanup-plan.md
```

각 항목별 분석 + 결정:

- `.claudeignore`: project-level Claude Code ignore file (controller가 자동 생성한 듯). git add 또는 gitignore에 추가
- `docs/INDEX.md`: token optimizer가 생성한 docs index (CLAUDE.md에 reference됨)
- `docs/QUICK_REFERENCE.md`: 동일 (token optimizer)
- `docs/archive/`: 옛 docs archive (token optimizer가 옛 파일 이동)
- `docs/superpowers/plans/2026-05-01-v3.14.1-p2-cleanup-plan.md`: 옛 plan 파일 (v3.14.1, 5/1 작성)

판단:
- `.claudeignore`: gitignore에 추가 (machine별 다를 수 있음)
- `docs/INDEX.md` + `docs/QUICK_REFERENCE.md`: tracked로 추가 (controller auto-load reference + cross-machine consistency)
- `docs/archive/`: tracked로 추가 (history 보존)
- `docs/superpowers/plans/2026-05-01-v3.14.1-p2-cleanup-plan.md`: tracked로 추가 (other v3.x plan과 정합)

#### 0.2.3 C — Knip configuration hints 정리

현재 3건 hints:

1. `functions/lib/**` ignore — workspace가 자동 ignore default라 redundant
2. `functions src/index.ts` entry — workspace에서 자동 인식
3. `functions vitest.config.ts` entry — 동일

v3.43 시도 (config minimal로 functions workspace 없이) 시 5건 unused files 잡힘 → workspace 명시 필요. 단 functions/lib ignore는 redundant. functions entry는 명시 안 해도 자동 인식 검증 필요.

판단: functions/lib ignore 제거 + functions entry 제거 후 재실행 → finding 0건 유지 시 채택, 실패 시 revert.

### 0.3 비목표

- C1 Phase B / C7 (내일 v3.45+)
- v3.17 §7.2 신규 기능 (별도 cycle)
- 신규 기능 / schema migration / production 코드 변경

---

## §1. T1 — A: v3.44 L1+L3 + L4 Graduate

### 1.1 변경 범위

**COMMON_MISTAKES.md 신규 §19**:

```markdown
## 19. Dynamic namespace import knip 한계 + JSDoc semantic tag (2026-05-20, v3.44.1 graduate from v3.44 L1+L3)

**Symptom**: Knip dead-code 분석에서 `import * as ns from './module'` 형태의 dynamic namespace import 후 `ns.exportName()` 호출은 unused로 잡힘.

**Root cause**: knip은 namespace import의 dotted access를 export reference로 추적 못 함 (정적 분석 한계). 코드는 정상 작동하지만 도구는 false-positive.

**Fix/Prevention**:

1. **JSDoc `@public` tag로 명시**:
   ```ts
   /** @public — main.ts:23 dynamic namespace import (`home.mountHomeHandlers()`)로 호출. */
   export function mountHomeHandlers(): void { ... }
   ```
   knip 6+ JSDoc tag 지원 — `@public` mark 시 unused 안 잡힘.
2. **`@internal` tag**: test에만 export하고 production caller 없는 경우 marker. 단 knip은 `@internal`을 unused로 잡지 않음 (test usage 인식).
3. **단순 import로 변경 (대안)**: `import { exportName } from './module'`. 단 code-splitting / lazy load 의도 시 namespace 유지 권장.

**§18 (vi.mock factory export pattern)과 paired** — test에서 mock 함수 외부 export 패턴과 동일하게 semantic tag 우선.

**관련 파일**: `src/ui/handlers/home.ts:249` (v3.44 사례), knip 6.x JSDoc tag docs.
```

**CLAUDE.md "Bundle 측정 표준" 섹션 뒤에 신규 mini-section** (L4):

```markdown
**Dead-code audit 주기 (v3.44.1 graduate, v3.44 L4 DoD note)**:

knip config의 `ignoreExportsUsedInFile: true` 옵션은 "같은 파일에서만 쓰이는 불필요한 export modifier"를 가린다 (진짜 dead code는 별도 catch). 단 public export surface가 실제로 audit되도록 주기적으로 옵션 없는 run 실행:

- 매 3~5 사이클 또는 schema bump 시점 한 번:
  ```bash
  npx knip --no-progress --include-entry-exports
  ```
- 또는 `knip.json` 임시 `ignoreExportsUsedInFile: false` toggle 후 재실행
- 잡힌 finding 중 진짜 public-only export는 `@public` JSDoc tag로 mark (§COMMON_MISTAKES §19 paired)
```

### 1.2 Risk

| Risk | Severity | Mitigation |
|---|---|---|
| §19 wording이 §18과 너무 비슷 | L | "paired with §18" 명시 + 차별 (namespace import vs vi.mock factory) |
| CLAUDE.md audit 주기 wording이 강제로 해석 | L | "권장" wording, "매 3~5 사이클 또는 schema bump 시점" 명확화 |

---

## §2. T2 — B: Working Tree Cleanup

### 2.1 변경 범위

**`.gitignore` 수정**:
- `.claudeignore` 한 줄 추가 (machine-specific)

**Tracked로 추가**:
- `docs/INDEX.md`
- `docs/QUICK_REFERENCE.md`
- `docs/archive/` (디렉토리 전체)
- `docs/superpowers/plans/2026-05-01-v3.14.1-p2-cleanup-plan.md`

각 파일은 token optimizer / 옛 plan 등 history 보존 + cross-machine consistency 가치.

### 2.2 Risk

| Risk | Severity | Mitigation |
|---|---|---|
| docs/archive/ 크기 큼 (repo bloat) | L | du -sh로 사전 측정, 100MB↑이면 별도 처리 |
| 옛 plan 파일 내용이 stale (v3.14.1 시점) | L | history reference 가치 — 그대로 tracked OK |

---

## §3. T3 — C: Knip Configuration Hints 정리

### 3.1 변경 범위

**knip.json 시도**:

1. `functions/lib/**` ignore 제거
2. `functions` workspace entry 제거 (자동 인식 의존)

재실행 후 finding 0 유지 시 채택. 실패 시 revert.

### 3.2 Risk + Abort

| 시나리오 | 처리 |
|---|---|
| Finding 0 유지 | 채택 — knip.json 더 minimal |
| Finding 1~3건 (functions/lib unused 등) | functions/lib ignore만 revert, functions entry는 제거 유지 |
| Finding 5건↑ | 전체 revert, hints 3건 그대로 유지 |

---

## §4. T4 — Verify + Codex 최종 + Bundle + 배포 + Sync

### 4.1 검증

- TS strict: 0 errors
- Lint: 0 errors
- Vitest 양쪽 TZ: 1546/1546
- Functions: 24/24
- Smoke: 109/109
- Knip: finding 0 유지

### 4.2 Bundle Quad Record

v3.44 → v3.44.1: docs/config only 변경이므로 Δ 모두 0 예상.

### 4.3 Codex 최종 review

- direct CLI + `< /dev/null`
- 분류: DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT

### 4.4 사전 review

small cycle exception 적용 — direct CLI 1차 시도, 한도 시 controller self-review fallback.

### 4.5 Retro + 배포 + Obsidian sync

- memory `project_daily_growth_v3441.md` 신규
- MEMORY.md entry append
- vault sync (02-Cycles + 03-Lessons + 01-Workflow)
- 사용자 "배포해줘" 명시 후 deploy

---

## §5. Risk Heatmap

| 영역 | Severity | Likelihood | Mitigation |
|---|---|---|---|
| T1 docs churn | L | High | docs only, 코드 영향 0 |
| T2 working tree add → 큰 commit | L | Low | du -sh 사전 측정 |
| T3 Knip config tuning 실패 | L | Mid | abort 조건 명확 |
| 4사이클 누적 후 사용자 fatigue | M | Low | lightweight (1.5h 예상) 명시 |

---

## §6. DoD

- [ ] §19 신규 + CLAUDE.md Dead-code audit 주기 mini-section (T1)
- [ ] .gitignore 갱신 + 4 파일/디렉토리 tracked (T2)
- [ ] Knip finding 0 유지 + hints 0건 또는 1~2건 (T3)
- [ ] 3종 검증 모두 PASS (T4)
- [ ] Bundle Δ 모두 0 (T4)
- [ ] Codex 최종 APPROVED (T4)
- [ ] retro + sync (T4)

---

## §7. v3.45 Carry-Forward

| ID | 항목 | 출처 | 우선순위 |
|---|---|---|---|
| C1 | T1 Phase B backend enforce + 202 + UX copy | v3.41 carry | **HIGH** (2026-05-21+ 24h monitor 후) |
| C7 | Firebase API key GCP referrer | v3.43 v2 강등 | M (C1 묶음) |
| C4 | first briefing storage thrash | v3.41 carry | L |
| C5 | state↔ui layer cycle 정리 | v3.41 carry | L (별도 사이클) |
| v3.17 §7.2 | 식물 detail modal / Streak Freeze 등 | v3.17 retro | M (신규 기능 cycle) |

## §8. 사용자 결정 사항

- ✅ A + B + C 묶음 채택 (사용자 명시)
- ✅ 명명 v3.44.1 patch (controller 결정, precedent v3.14.1 / v3.18.1 / v3.20.1)
- ✅ 비목표 명시: C1 Phase B (내일) / v3.17 §7.2 (별도 cycle)
