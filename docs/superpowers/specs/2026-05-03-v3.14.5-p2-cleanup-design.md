# DG v3.14.5 — P2 Cleanup (TZ sweep + numeric guard + IR-1)

**Date**: 2026-05-03
**Series**: v3.14.x (cleanup, 시리즈 종료 직전)
**Baseline (claimed from v3.14.4 retro `7d51a9a`)**: 711 vitest pass / lint 0 errors / Playwright 93/93 cold-start fresh fork (54.4s) / Bundle CLI gzip **31.28 kB**
**Baseline (실측)**: T0에서 reconcile (v3.14.4 graduated 표준 절차)
**Spec author**: Claude (controller)
**Status**: APPROVED for plan (사용자 합의 2026-05-03 — 옵션 B+ / TZ sweep A=단일 task)

---

## 1. 사이클 정의

**v3.14.5**는 v3.14.4에서 carry-forward된 P2 5건과 TZ guard sweep 9 spec을 일괄 정리하는 cleanup 사이클이다. v3.14 시리즈를 완전히 닫고 v3.15 Game World 진입을 위한 마지막 무빚 단계.

배포 단위는 **단일 commit pack → main 직접 commit (origin remote 미등록 직배포 패턴, CLAUDE.md graduated 컨벤션)**. 사용자 "배포해줘" 명시 시점에만 `npm run build` → `firebase deploy --only hosting`.

### 1.1 옵션 B+ (TZ sweep A=단일) 채택 사유

- **옵션 A (Lean, P2 only ~5 task)**: dormant TZ bomb 9 spec 잔존 → KST-shifted CI/host 등장 시 silent broken. v3.4 + v3.14.4 두 번 confirmation받은 패턴, 3번째 미연 방지 가치 명확.
- **옵션 B (채택, ~10 task)**: P2 5건 + TZ sweep 일괄 + bundle 측정 단일 표준 declaration. v3.4 cardnews + v3.14.4 T3 lesson을 graduate.
- **옵션 C (Full+, ~15+ task)**: smoke flake centralize helper + `.claude/` git tracking까지 — scope creep 위험. v3.14.4 옵션 C 거부 사유 동일 (helper centralize는 cold-start 2번째 등장 시 mechanical refactor 트리거가 맞음, T0 측정 결과로 판단).

**TZ sweep 깊이 = A (단일 task)**: 9 spec mechanical pattern (UTC ISO → `getDateStr()` import). v3.4 cardnews + v3.14.4 T3 review fix 둘 다 단일 import 패턴이고, per-task review가 9건 한 번에 충분. 일부 패턴 다를 시 plan 단계 dry-run 후 spillover (T4-2 격상).

### 1.2 명시 제외 항목

- **smoke flake centralize 정책 (helper / `smokeTimeout` const)** — cold-start 2번째 등장 시 mechanical refactor 트리거. **트리거 도래 기준 (codex 사전 P2-4 명시화)**: T0 cold-2 cascade N=3 측정 결과 동일 spec이 3회 모두 fail로 재현되면 도래. N=3 중 1~2회 fail은 inconsistent flake로 분류, status update만. 트리거 도래 시 **v3.15에서 처리** (본 사이클은 측정만, fix 없음).
- **`.claude/` git tracking 정책 결정** — `.gitignore` / `.claudeignore` 정책 논의 별도 mini-cycle.
- **격하된 P4 2건** (v3.14.4) — T3 cast pattern local-alias / T10 welcome-gamification mini-grid `renderBadgeButton` export. 트리거 미도래.
- **새 기능 추가** — cleanup 한정.
- **v3.15 Game World** — 별도 cycle design.

---

## 2. 범위 (T0~T9 = 10 task)

### 2.1 ⚙️ 운영 / 측정 (T0, T5, T6, T7, T8, T9 = 6건)

| ID | 항목 | 출처 | Acceptance |
|---|---|---|---|
| **T0** | 사이클 baseline 측정 — `npm test` count + `npm run build` bundle CLI gzip + smoke `npx playwright test` cold-start **N=3** + cold-2 cascade 발생률 데이터 수집. v3.14.4 graduated 표준 절차. | Lesson #4 graduated | T0 결과를 plan §Baseline 명기. cold-2 N=3 측정 결과는 §6 v3.15+ backlog status update |
| **T5** | Bundle 측정 단일 표준 채택 — CLI `gzip -c \| wc -c` canonical 선언, vite reporter dual-record 폐기. | v3.14.4 §3.3 + §6 backlog graduate | spec §3.3 + retro §8.x에 1단락 명시. 본 사이클부터 single-record |
| **T6** | Codex 사전 review (spec phase) — spec + draft plan 1-pass. P0/P1/P2 분류, in-cycle 반영. | CLAUDE.md graduated | Codex 출력 첨부, in-cycle 반영 task 명세 |
| **T7** | Codex 최종 review (deploy gate) — 모든 main work commit 후 1-pass. DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT. | CLAUDE.md graduated | Codex 출력 첨부, deploy gate |
| **T8** | 사이클 retro — 본 spec §8 retro 추가, lessons 신규 발견 시 §8.3 기록. | OPS | retro pointer commit |
| **T9** | memory 갱신 — MEMORY.md 포인터 + `project_daily_growth_v3145.md` 작성. | OPS | memory 파일 + 포인터 commit |

### 2.2 🟡 Codex P2 carry (T1 = 1 task)

| ID | 항목 | 위치 | Acceptance |
|---|---|---|---|
| **T1** | IR-1 heatmap KST seed false-green fix — 현재 `new Date().toISOString().slice(0,10)` 시드는 KST-shifted host에서 UTC 자정 이후 "내일" date로 오인 가능. Node-side `getDateStr()` import + assertion에서 cell `[data-date]` 일치 확인. v3.4 cardnews / v3.14.4 T3 review fix 패턴 동일. | `tests/smoke/heatmap.spec.ts:29-32, 85-87` | seed→cell 일치 assertion 추가. cold-start fresh fork N=5 PASS. 신규 it-block 0 (기존 it-block 내 assertion 강화) |

### 2.3 🔴 데이터 가드 (T2, T3 = 2 task, 4 항목 흡수)

| ID | 항목 | 위치 | Acceptance |
|---|---|---|---|
| **T2** | `usage.count` numeric guard 강화 — 현재 `Number.isFinite(parsed.count)` guard만 (v3.14.4 T1). 추가:<br>(a) negative count (`{ count: -1000 }` LS 편집 시 cap bypass) 차단 — `parsed.count >= 0`<br>(b) fractional count (`5.5`) → `Math.floor()` 정수화. | `src/state/usage.ts:37-39` | 신규 spec **3 it-blocks** (negative / fractional / 0 boundary). v3.14.3 firstSeenAt 패턴 동일 |
| **T3** | `migration.ts` `missions.active` array element guard:<br>(a) `progressDates: [null, 42, "..."]` array 내부 corruption guard (v3.14.3 firstSeenAt graduation pattern)<br>(b) `MissionInstance` shape guard for malformed element (missing `defId` 등) — sanitize 후 caller 객체 사용 invariant 보존 (v3.14.4 C6 invariant). | `src/state/migration.ts:107-127` (실측 시 line 정확화) | 신규 spec **4~6 it-blocks**. progressDates 3 case (null / non-string / mixed) + shape guard 2~3 case (missing defId / wrong type / partial) |

### 2.4 🟡 TZ guard sweep (T4 = 1 task, 9 spec 일괄)

| ID | 항목 | 출처 | Acceptance |
|---|---|---|---|
| **T4** | UTC ISO seed → `getDateStr()` import 패턴으로 일괄 sweep. **plan 단계 dry-run 필수** — 9 spec 중 패턴 다를 시 spillover task T4-2 격상.<br>**대상 (smoke 6 + unit 3, 실측 path는 plan에서 정확화)**:<br>(a) `tests/smoke/heatmap.spec.ts` (T1 IR-1 외 추가 잔여 site 발견 시)<br>(b) `tests/smoke/v3.12-game-feedback.spec.ts`<br>(c) `tests/smoke/card-translation.spec.ts`<br>(d) `tests/smoke/chat-bubble-action.spec.ts`<br>(e) `tests/smoke/modal-overhaul.spec.ts`<br>(f) `tests/smoke/disabled-button.spec.ts`<br>(g) unit `interests-edit.spec.ts`<br>(h) unit `modals-interests.spec.ts`<br>(i) unit `settings-interests-section.spec.ts` 또는 `triggers-mission.spec.ts` | v3.14.4 T3 reviewer P1 deferred | (1) 9 spec 모두 `getDateStr()` import 적용 (또는 spec별 inline KST helper if needed)<br>(2) cold-start fresh fork **N=5 모두 PASS**<br>(3) per-task review 1회로 9건 한 번에 검토<br>(4) global timeout config 변경 안 함<br>(5) 패턴 다른 spec 발견 시 plan T4-2 격상 (사용자 재합의)<br>(6) 신규 it-block 0 (기존 패턴 변경뿐) |

---

## 3. 검증 정책

### 3.1 vitest

- **baseline**: 711 (v3.14.4 retro `7d51a9a`). T0에서 실측 reconcile (Lesson #4 graduated 절차).
- **target Δ 분담**: T1 (+0, smoke assertion 강화) + T2 (+3 확정 — negative / fractional / 0 boundary) + T3 (+5 expected, +4~6 range — progressDates 3 case + shape 2~3 case) + T4 (+0, 패턴 변경뿐) = **+7~9** (lower bound 7 = T3 +4 case의 보수적 추정).
- **target 절대값**: 711 baseline + 7~9 = **718~720**.
- **DoD**: lint 0 errors / TS strict 0 errors / 모든 신규 spec **N=5 5/5 PASS**. target lower bound 미달 시 plan 단계에서 task별 +N 재산정 후 spec §3.1·§4 동기화.
- **scope-faithful no-op acceptable** (v3.14.4 graduated lesson): refactor/docs 위주 task가 +0이어도 DoD 미달 사유 아님 — 신규 it-block은 hard target이 아닌 soft estimate.

### 3.2 Playwright smoke

- **baseline**: 93/93 cold-start fresh fork (v3.14.4 retro 안정).
- **target**: 93/93 (cold-start + warm), N=5 PASS.
- **신규 spec 0건** — T1 IR-1은 기존 spec 내 assertion 강화. T4 sweep은 패턴 변경뿐.

### 3.3 Bundle size (단일 표준 채택, T5 graduated)

- **baseline (CLI `gzip -c dist/assets/index-*.js | wc -c`, single-stream)**: 31.28 kB (v3.14.4 retro `7d51a9a`).
- **canonical 선언**: 본 사이클부터 CLI gzip을 v3.14.5 retro single-record. vite reporter dual-record는 v3.14.4가 마지막 사례.
- **target Δ**: ±0.10 kB (코드 변경 적음, 대부분 docs + test). T2/T3 numeric guard 추가 ~0.05 kB, T1 + T4 = test only.
- **DoD**: T8 retro에 CLI gzip 단일값 명기.

### 3.4 Codex 2-pass

- **사전 (T6)**: spec + draft plan 작성 직후. P1/P2 분류 in-cycle 반영.
- **최종 (T7)**: 모든 main work commit 후. DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT.
- **권한 issue 시 (v3.14.2 carry-forward 패턴)**: 사전은 self-review로 대체. 최종은 cycle wrap-up에서 별도 시점 재시도.

### 3.5 사이클-끝 검증 (T8 직전)

- `npm run lint && npm test && npm run test:smoke` 3종 PASS 확인.
- `npm run build` 후 bundle CLI gzip 실측.
- N=5 신규 spec 5/5 reconfirm.

---

## 4. DoD (Definition of Done)

1. T0~T9 main work commit 완료.
2. vitest count = 711 baseline + 6~9 = **717~720** (실제 +N은 T8 retro에 명기). 산식 §3.1.
3. Playwright 93/93 cold-start + warm 모두 PASS (N=5).
4. Bundle Δ ±0.10 kB CLI gzip (실측 기준).
5. Lint 0 / TS strict 0.
6. Codex 최종 review = DEPLOY_APPROVED 또는 APPROVED_WITH_NOTES (P0=0).
7. T5 bundle 단일 표준 declaration 반영 (spec §3.3 + retro §8.x).

---

## 5. Risk & Open

| Risk | 완화 |
|---|---|
| **T4 9 spec 중 패턴 다를 시 spillover** | plan 단계 dry-run grep으로 9 spec 실제 패턴 확인. 다른 site 발견 시 T4-2 격상 (사용자 재합의 후 plan 갱신). |
| **T0 cold-2 cascade N=3 측정 결과 일관 등장** | smoke flake centralize 트리거 도래로 판단, **본 사이클 fix 안 함**. §6 v3.15 carry-forward로 명기. |
| **T6 Codex 사전 review에서 P0 발견** | in-cycle 반영. P0 다수 시 spec 재작성 (사용자 재합의 필요). |
| **Codex 권한 issue (v3.14.2 carry-forward 패턴)** | 사전은 self-review로 대체. 최종은 cycle wrap-up 별도 시점 재시도. |
| **bundle Δ ±0.10 초과** | T2/T3 numeric guard 코드량 예상 초과 시 재추정 후 spec §3.3 동기화. |
| **T2/T3 신규 it-block 산식 lower bound 미달** | scope-faithful no-op acceptable (v3.14.4 graduated lesson). DoD §4-2 trade-off 명시. |

---

## 6. v3.14.5+ Backlog (본 사이클에서 처리하지 않는 항목)

- **smoke flake centralize 정책 (helper / `smokeTimeout` const)** — cold-start 2번째 등장 시 mechanical refactor 트리거. T0 N=3 측정 결과로 트리거 도래 여부 판단.
- **`.claude/` git tracking 정책 결정** — `.gitignore` / `.claudeignore` 정책 별도 mini-cycle.
- **격하된 P4 2건** (v3.14.4) — T3 cast pattern local-alias / T10 welcome-gamification mini-grid `renderBadgeButton` export.
- **신규 P3** (본 cycle per-task review에서 발생 시) — v3.15 진입 시 자연 해소 또는 별도 hotfix.
- **bundle 절대값 이전 사이클 reconcile** — v3.14.3 32.10 / v3.14.4 31.28 등 보고가 vite reporter vs CLI gzip 혼재. v3.15 진입 baseline은 **CLI gzip 단일 표준**.

---

## 7. v3.15 진입 조건

- 본 cycle T8 + T9 완료 + 사용자 "배포해줘" 명시 후 deploy 완료.
- 사용자 "v3.15 Game World brainstorm" 트리거 명시.
- 다음 사이클 첫 task = T0 baseline 측정 (CLI gzip 단일 표준 첫 적용 사례).

---

## 8. 사이클 retro (T8 최종 — TBD)

> 사이클 종료 시 채워짐.

### 8.0 핵심 결과
### 8.1 vitest count drift
### 8.2 Codex review 결과
### 8.3 신규 lessons (사이클 중 발견)
### 8.4 v3.14.5+ Backlog (carry-forward)
### 8.5 차기 사이클 후보

---

## 9. Spec 변경 이력

| 시점 | 변경 | 사유 |
|---|---|---|
| 2026-05-03 작성 | 초안 | 사용자 옵션 B+ A-단일 TZ sweep 합의 후 |
| 2026-05-03 v2 | Codex 사전 review 반영 (P0=0/P1=2/P2=4) | (P1-1) §2.3 T3 progressDates readonly 충돌 → plan에서 object literal 안에서 처리 (코드 영향 plan만, spec 의미 동일). (P1-2) §2.4 T4 + §2.3 T1 모든 smoke spec이 `addInitScript` 브라우저 context 내부 → Node-side `getDateStr()` 호출 + `args` typed parameter 전달 패턴 (v3.14.4 T3 review fix `a61d55e` precedent). plan T1/T4 코드 전면 재작성. (P2-1) §2.3 T2 "2~3" → "3" 동기화. (P2-2) §3.1 산식 +6~9 → +7~9, lower bound 근거 명시. (P2-4) §1.2 / §6 cold-2 cascade trigger threshold "N=3 3회 모두 동일 spec fail" 명시화. P2-3은 P1-2에 흡수. |
