# v3.18.1 Hotfix — Design (Spec)

**Status**: DEPLOYED (2026-05-07, main HEAD `8874b57` + retro pending, Firebase 사용자 명시 시점 대기)
**Date**: 2026-05-05 ~ 2026-05-07
**Cycle type**: Production hotfix (v3.18 배포 직후 사용자 발견 issue 9건 중 즉시 fix 가능 6건)
**Predecessor**: v3.18 Cleanup (main HEAD `48b2cc4`, Firebase live, 2026-05-05 deployed)

---

## §1. 배경 + scope

v3.18 production 배포 직후 사용자가 9건 issue 보고 (2026-05-05). 진단 후 6건은 root cause 명확 + 즉시 fix 가능, 3건은 정책/UX 결정 필요로 v3.19로 carry.

### 1.1 본 사이클 in-scope (6건)

| H | 이슈 | Severity | Root cause | Fix 위치 |
|---|---|---|---|---|
| **H1** | welcome-garden 모달이 좌측 하단 inline 표시 | **P0** (visual broken) | `welcome-garden.ts:47` `class="modal-backdrop modal-card"` — CSS는 `.dg-modal-backdrop/.dg-modal-card` (`dg-` prefix). 0 적용 | `welcome-garden.ts` → `openModal()` helper로 refactor (welcome-gamification.ts 패턴) |
| **H2** | 첫 로그인 RSS 429 + 브리핑 빈 상태 | **P0** (boot UX broken) | `rss.ts:67` 429 시 빈 배열 반환, retry 없음. 8 feed 동시 fetch | `rss.ts` retry+exponential backoff (translate.ts isRetryableStatus 패턴) |
| **H3** | 이름 'Hayden' 자동 사용 | **P1** (개인정보 leak 위험 + UX) | `onboarding.ts:191` `state.name.trim() \|\| 'Hayden'` hardcoded fallback | onboarding 시 빈 이름 차단 (validate) — 빈 이름이면 '사용자' generic |
| **H4** | 정원 이름 2자 truncate (자기계발 → 자기) | **P1** (UX 의미 손실) | `garden-grid.ts:19` `mini ? label.slice(0, 2) : label` | `slice(0, 4)`로 완화 + 4자 초과 시 ellipsis |
| **H5** | 설정 좌우 여백 부족 | **P1** (UX) | `layout.css:68-75` `#settingsTab` 모바일 horizontal padding 없음 | `layout.css` 모바일 `#settingsTab { padding-left/right: 20px }` 추가 |
| **H6** | Gemini API 키 발급 안내 부재 | **P2** (onboarding UX) | `settings.ts:44-49` Gemini 섹션에 link/안내 없음 | settings.ts `<details>` 추가 (slack 패턴 차용) + `https://aistudio.google.com/app/apikey` 링크 |

### 1.2 본 사이클 out-of-scope (v3.19로 carry)

- **#3 첫 로그인 파란 배경** — 진단 더 필요. 재현 시 화면 캡처 + DOM state 필요.
- **#7 슬랙 웹훅 회사 HR 봇 고정** — 정책 결정 필요 (build env / UI 비활성 / 사용자 입력).
- **#8 슬랙 개인 DM/봇 분기** — UX + schema 변경 (slack webhook 2개 저장).

### 1.3 정책 (v3.18.1 hotfix 사이클)

- **신규 기능 0** — 6건 모두 production bug fix 또는 UX hotfix (P0/P1/P2). 새 화면/모달/탭 0.
- **Schema 변경 0** — user v4 그대로.
- **CSP 변경 0** — Gemini link는 `target="_blank"`, 외부 링크 직접 호출 X.
- **REJECT 시 in-cycle hotfix만 허용** — 신규 기능 추가 금지 (v3.18 §1.4 패턴).
- **사용자 명시 시점에만 배포** — main 직배포, push 금지.

---

## §2. Definition of Done (DoD)

- **H1~H6 모두 PASS** (per-task review P1 in-cycle 반영 후).
- **TS strict / lint 0 errors** (`npx tsc --noEmit && npm run lint`).
- **vitest +N (±2)** — baseline 883 pass / 138 files. 분포 추정:
  - H1 +2 (welcome-garden modal class assertion + openModal 호출 검증)
  - H2 +3 (429 retry + exponential backoff + max retries)
  - H3 +1 (onboarding empty-name fallback)
  - H4 +2 (truncate threshold + ellipsis)
  - H5 +1 (settings padding assertion)
  - H6 +1 (gemini link assertion)
  - **합계 +10 ±2** → 893 ±2.
- **신규 vitest spec N=5 5/5** (H1 modal / H2 RSS retry).
- **Playwright smoke 97/97 유지** (serialized `--workers=1`).
- **번들 측정**: net **+50~+200 B** 예상 (retry 로직 + Gemini link UI).
  - hard fail: +500 B 초과.
- **사전 + 최종 review 모두 P0=0** (P1 in-cycle, P2 → v3.19 backlog).
- **수동 verification**:
  - 첫 로그인 (localStorage 비움) → onboarding 흐름 → name 입력 / API key 입력 / interest 선택 → 홈 진입 → welcome-garden 모달 정상 표시 + 브리핑 정상 로드.
  - Settings 탭 좌우 여백 자연스러움.
  - Stats 탭 정원 이름 4자까지 표시 + 4자 초과는 ellipsis.

---

## §3. 위험 + 완화

| 위험 | 영향 | 완화 |
|---|---|---|
| **R1 H1 modal refactor에서 다른 케이스 회귀** (Escape / outside click 등) | 모달 close path 깨짐 | welcome-gamification.ts 패턴 그대로 차용 + open/close E2E |
| **R2 H2 retry가 무한 loop / 너무 긴 timeout** | 빈 페이지 시간 ↑ | max retries 2 + cap timeout 6s. caller `home.ts:582` `timeoutMs` 5000 → 6000으로 lift (T0 review P1-1 in-cycle, retry budget 1차 + 200ms backoff + 2차 + 800ms backoff + 3차 = ~5s 초과 가능). |
| **R3 H3 빈 이름 onboarding 막을 시 기존 사용자 'Hayden' 잔존** | 기존 'Hayden' 사용자가 prompt 받음 | 기존 사용자는 그대로 (재 onboarding 안 함). 신규 사용자만 적용 |
| **R4 H4 truncate 4자 (e.g., '자기계발') CSS overflow 가능** | 기존 mini cell width (50px)에 안 들어갈 수 있음 | font-size + ellipsis fallback |
| **R5 H5 layout.css 변경이 데스크톱 padding-left:80px와 충돌** | 데스크톱에 양쪽 padding 추가됨 | 모바일만 (`@media (max-width: 767px)`) 또는 일반 selector + 데스크톱 override 확인 |
| **R6 H6 외부 링크 (aistudio.google.com) CSP 위반 가능** | 링크 클릭 차단 | `target="_blank" rel="noopener noreferrer"` (이미 slack 안내 패턴 동일) |

---

## §4. 리뷰 게이트

CLAUDE.md "배포 전 독립 코드 리뷰" — hotfix 사이클이지만 6건 묶음이라 표준 리뷰 적용.

- **사전 review**: T0 직후. `pr-review-toolkit:code-reviewer` (Codex CLI 한도 fallback). 분류 P0/P1/P2.
- **최종 review**: H7 verification 후. `DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT`.
- **per-task review**: 6 main task 각각 dispatch (소형 fix가 많아 격하 가능, P1 in-cycle fix만).

---

## §5. 사이클 사이즈

- T0: spec + plan + 사전 review (1~3 commits).
- H1~H6: 6 main task / 6~9 commits (per-task fix 포함).
- H7: bundle + verification (1 commit).
- H8: retro + deploy (1 commit + firebase deploy).
- **총 9~14 commits**.

v3.11.1 / v3.12.2 / v3.3.2.1 (1 commit hotfix)보다 크지만 v3.14.5 P2 Cleanup (7 commits)와 비슷 — 6건 hotfix scope.

---

## §6. v3.19 carry-forward

- **Production issues 3건** (정책 결정 후 spec): #3 파란 배경 / #7 HR 봇 고정 / #8 DM/봇 분기.
- **v3.18 in-cycle 발견 P2 7건**: T1 describe label / `.garden-mini-cell` `@media (hover)` / T3 text-tertiary 2.87:1 AA / T4 일반 cell `:focus-visible:hover` combo / T7 G4-2 spy 확장 + heatmap-polish G4-3 / T8 parallel suite-flake + bundle additional trim.
- **v3.17 §7.2 신규 기능 5건**: 식물 detail modal / Streak Freeze / max stage ambient / M3 missions·뱃지 grid 게임화 / M4 glassmorphism welcome 모달.

---

## §7. Retro (사이클 종료)

### 7.1 핵심 결과
- **사이클 완료**: 2026-05-07, main HEAD `8874b57`, Firebase 사용자 명시 시점 대기.
- **Commits**: T0 spec/plan(`e8404d9`) + T0 review(`31a9c6c`) + H1~H6 main(6) + H1.1 follow-up(`8874b57`) + retro 1 = **10 commits** (v3.14.5 P2 Cleanup 7 commits 대비 살짝 큼).
- **vitest**: 883 → **896 pass / 141 files** (+13, T0 review 정정 분포 H1+1 / H2+3 / H3+3 / H4+2 / H5+1 / H6+3).
- **smoke**: parallel **97/97 PASS** (47s) + cold-fork N=10 layout **10/10 PASS** (DoD §4 충족).
- **bundle**: 34916 → **35122 B (+206 B, soft target +200 B over 6 B, hard fail +500 well under)** — AMBER zone retro 기록.
- **TS strict / lint**: 0 errors (11 pre-existing warnings baseline 유지).

### 7.2 6건 매핑 (전건 in-cycle 처리, 0 누락)
- **H1 #4 welcome-garden modal openModal() refactor** (P0, `81bda90`): `class="modal-backdrop modal-card"` (CSS 0 매칭) → `openModal()` helper. `bodyHtml` 안에 `.welcome-garden-modal` wrapper + button IDs 보존하여 기존 11 case 호환.
- **H1.1 smoke specs gardenIntroduced=true 일괄 seed** (P0 follow-up, `8874b57`): H1 modal 정상 작동 후 23 smoke spec click intercept 회귀 (43→14→0). Root cause는 시드 정합 문제 (production code 변경 0).
- **H2 #9 RSS 429 retry exponential backoff** (P0, `dc9446b`): translate.ts isRetryableStatus 패턴 차용. max 2 retry / 200ms·800ms backoff / 429+503만 retry. caller home.ts:582 timeoutMs 5000 → 6000 (T0 P1-1).
- **H3 #2 onboarding 'Hayden' fallback** (P1, `cbdc1f1`): `deriveOnboardingName(raw)` `@internal` helper 추출 + '사용자' generic. T0 P1-2 (TDD red gate inline 산식 → helper extraction).
- **H4 #5 정원 이름 truncate 완화** (P1, `a8f82e2`): mini label 2자 → 4자 + ellipsis. CSS `display: block` 추가 (T0 P1-3, ellipsis가 inline span에 미작동).
- **H5 #6 settings mobile padding** (P1, `99c97d9`): `@media (max-width: 767px)` wrap 안에 `#settingsTab { padding: 20px }` (T0 P1-4 데스크톱 회귀 차단).
- **H6 #1 Gemini API key 발급 안내** (P2, `e644d56`): Slack `<details>` 패턴 차용 + Google AI Studio link + `target="_blank" rel="noopener noreferrer"`.

### 7.3 Codex 2-pass 결과
- **사전 review** (`pr-review-toolkit:code-reviewer` fallback): **APPROVED_WITH_FIXES** (P0=2 + P1=4 + P2=2). 8건 모두 in-cycle 반영 (`31a9c6c`). H1 spec 회귀 catch + baseline 정합성 + H2 timeout + H3 helper extraction + H4 CSS display block + H5 mobile-only @media — 사전 게이트 가치 재입증.
- **최종 review** (deploy gate): **DEPLOY_APPROVED_WITH_NOTES** (P0=0 / P1=0 / P2=5 → v3.19 carry).

### 7.4 신규 lessons
1. **H1.1 smoke 시드 정합 회귀** — modal CSS 매칭이 정상화되면 idempotent flag 미설정 시 smoke 회귀 즉시 표면화. **TDD red gate로 잡을 수 없는 systemic gap** (production code 변경 0). v3.19에서 `tests/helpers/seed.ts` helper 통일로 근본 fix 권고.
2. **사전 review의 P0 적발 ROI 재입증** — H1 modal refactor가 기존 11 spec을 깨뜨릴 risk를 P0-1로 사전 catch. controller가 implementer에게 inline JSON 보존 패턴 명시한 결과 회귀 0.
3. **Bundle drift +6 B over soft target acceptable** — Cleanup 사이클이라도 retry logic + UI 추가 시 자연 증가. v3.16 T5 trim 패턴 (per-trim AMBER -212 B) 차기 사이클 적용 권고.
4. **TDD red gate "mechanical bulk fix"는 helper 통일이 진짜 fix** — H1.1 23 spec inline 시드는 single source of truth가 없으면 차후 재발. 정책 §1.4 정체성 측면에서도 helper 마이그레이션 = 새 기능 아닌 "기존 패턴 통일".

### 7.5 v3.19 carry-forward (final review에서 추가된 5건 + 기존)

**v3.18.1 final review N1~N5 (P2)**:
- N1 Smoke fixture helper 통일 (`tests/helpers/seed.ts:primeOnboardedUser`) — 23 spec 마이그레이션, H1.1 재발 차단.
- N2 Bundle additional trim — +206 B 회수 (v3.16 T5 trim 패턴).
- N3 settings.ts:83 Slack link `rel="noopener"` → `rel="noopener noreferrer"` (Gemini link와 일관, referrer leak 차단).
- N4 H2 retry telemetry — production 429 분포 observability.
- N5 `cap-reached` / `layout` spec에도 `gardenIntroduced=true` 보수적 시드 (방어적 일관성).

**기존 carry (spec §6 명시)**:
- v3.18 production issues 3건 (정책 결정): #3 파란 배경 진단 / #7 슬랙 HR 봇 고정 / #8 DM/봇 분기 schema.
- v3.18 in-cycle 발견 P2 7건.
- v3.17 §7.2 신규 기능 5건 (식물 detail modal / Streak Freeze / max stage ambient / M3 missions·뱃지 grid 게임화 / M4 glassmorphism).

### 7.6 차기 사이클 권고
- **v3.19** 혼합 패턴: N1 smoke fixture helper (P0 재발 차단) + N2 bundle trim + 신규 기능 1~2건 (식물 detail modal 또는 Streak Freeze).
- **모든 사이클 사전 review 1회 dispatch 의무화** — v3.18 + v3.18.1 두 사이클 연속 ROI 입증.

---

**End of spec.**
