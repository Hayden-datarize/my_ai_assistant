# DG v3.14.2 — Hotfix + P2 Legacy Cleanup (Design)

**Date**: 2026-05-02
**Series**: v3.14.x (cleanup), 게임화 시리즈 2/3 후속
**Baseline**: v3.14.1 HEAD `72e8a69` — 672 vitest pass / lint 0 errors / bundle gzip JS 31.88 kB
**Playwright baseline (실측 정정 2026-05-02)**: smoke 7/7 pass on `desktop-layout` + `tier-downgrade` (fresh build + preview, 10.9s). v3.14.1 메모 "89/93 = 4 pre-existing fail" 기록과 불일치 — 사이클 시작 시점에 4건은 이미 회복 상태. 본 사이클에서는 N=3 안정성 검증으로 closed 확정 후 retro 메모/스펙 정정.
**Spec author**: Claude (controller)
**Status**: APPROVED (for plan), with retro update 2026-05-02 reflecting Playwright 실측

---

## 1. 사이클 정의

**v3.14.2**는 두 가지를 한 사이클에 묶는다:

1. **P0 hotfix** — v3.14에서 발견되어 v3.14.1 기간 동안 carry된 Playwright **4건 pre-existing fail** 진단/수정.
2. **P2 legacy cleanup** — v3.13.2 후보 4건 + v3.12 잔존 6건 + v3.12.1 P2-NEW 3건 = **13건**의 누적 P2 정리.

배포 단위는 **단일 commit pack → main 직접 commit (직배포 패턴)**. 사용자 "배포해줘" 명시 시점에만 push/배포.

### 1.1 사이클을 한 번에 묶는 이유

- v3.13.1·v3.14.1과 동일한 cleanup 패턴 — 검증된 흐름.
- v3.12 잔존 P2 6건은 v3.13.1 메모에서 "v3.14 일괄 검토"로 명시 deferred → graduation 시점.
- v3.12.1 P2-NEW 3건도 사용자 정책 "오픈된 서비스 아니니 다 반영" 정합.
- P0와 P2를 분리하면 main 배포가 두 번 발생 → 사용자 부담 증가.

### 1.2 명시 제외 항목

- **v3.14 잔존 IntersectionObserver 변형** — design 변경 동반, 사용자 결정 대기.
- **v3.14 잔존 6탭 mobile 375px 라벨 wrap (P3)** — 측정값 부재, 다음 사이클에서 측정+결정 묶음.
- **v3.13.1에 closed된 Playwright `v3.12-game-feedback` 2건** — v3.14에서 fixture sync로 회복 완료.
- **v3.7+v3.13.1 silent-fail 정책** — 이미 통일 완료, 재처리 불필요. (P2-NEW-4는 손상 v2 silent path만 추가)

---

## 2. 범위 (워크아이템 14 = P0 회복검증 1 + P2 13)

### 2.1 P0 — Playwright 회복 검증 (1 트랙, ~1~2 task)

| ID | Spec | v3.14.1 메모 | 2026-05-02 실측 |
|---|---|---|---|
| P0-1 | `tests/smoke/desktop-layout.spec.ts` (assertion 1) | fail | **pass** |
| P0-2 | `tests/smoke/desktop-layout.spec.ts` (assertion 2) | fail | **pass** |
| P0-3 | `tests/smoke/tier-downgrade.spec.ts` (assertion 1) | fail | **pass** |
| P0-4 | `tests/smoke/tier-downgrade.spec.ts` (assertion 2) | fail | **pass** |

**상태**: 사이클 시작 시점(2026-05-02, main HEAD `72e8a69`)에 fresh build + preview 환경에서 7/7 pass로 검증됨. v3.14.1 메모 기록과 불일치 — 사이클 중 incidentally 회복(lint cleanup·fixture sync 부수효과 가능) 또는 v3.14.1 측정 시점 환경 노이즈로 판단.

**사이클 처리**:
- **N=3 안정성 검증** (T1) — env-flake 가능성 차단. 3회 모두 pass = closed 확정.
- **메모리/스펙 retro 정정** (T1 후속) — v3.14.1 메모 `Playwright 89/93 = 4 fail` → `89/89 (89 측정, 4건 회복 검증 완료)` 갱신.
- 안정성 검증 중 fail 발생 시 → wave 1(P0 fix) 자연 전환, plan 동적 갱신 + 사용자 컨펌.

### 2.2 P2 v3.13.2 후보 (4건)

| ID | 항목 | 출처 |
|---|---|---|
| P2-13.2-1 | `getActiveMissions` cross-midnight paired-call 테스트 (`prev=before` + `curr=after`) | v3.13.1 T8 reviewer |
| P2-13.2-2 | `migrateUserToV3` numeric guards `Number.isFinite` 통일 (NaN 누출 차단) | v3.13.1 T3 reviewer |
| P2-13.2-3 | `MissionInstance.progressDates` `readonly string[]` 타입 강화 (`Readonly<Omit<...>> & { readonly progressDates?: readonly string[] }`) | v3.13.1 T5 reviewer |
| P2-13.2-4 | v3.13 spec §1.4 retro에 v3.13.1 baseline 31.47 kB anchor sync (mechanical) | v3.13.1 메모 |

### 2.3 P2 v3.12 잔존 (6건)

| ID | 항목 | 출처 |
|---|---|---|
| P2-12-5 | `labor_law` VS16 cosmetic | v3.12 P2 #5 |
| P2-12-6 | 영문 짧은 ID substring over-credit → catalog keyword normalization (`matchKeyword` 적용 검토) | v3.12 P2 #6 |
| P2-12-7 | toast timer dedup | v3.12 P2 #7 |
| P2-12-8 | `__resetForTest` `clearAllTimers` 확장 | v3.12 P2 #8 |
| P2-12-9 | `hydrateBadges` 함수 size 분리 | v3.12 P2 #9 (잔존) |
| P2-12-10 | backfill saveUser race (theoretical) — guard 또는 spec annotation | v3.12 P2 #10 |

### 2.4 P2 v3.12.1 P2-NEW (3건)

| ID | 항목 | 출처 |
|---|---|---|
| P2-NEW-4 | 손상 v2 silent 정리 정책 — `getSaveErrorMessage` toast 통일 | v3.12.1 P2-NEW-4 |
| P2-NEW-5 | `as unknown as User` 이중 cast → `isValidUserShape` type guard helper로 격리. **대표 1~2 callsite만 마이그레이션, 나머지 follow-up backlog.** | v3.12.1 P2-NEW-5 |
| P2-NEW-6 | `.dg-modal #goStatsBtn` selector modal-scoped 좁히기 (defensive) | v3.12.1 P2-NEW-6 |

### 2.5 사이클 wrap-up

- **W-1** docs sync — memory + spec/plan retro
- **W-2** Codex 최종 독립 리뷰 (백그라운드 dispatch, fresh thread)
- **W-3** 사이클 retro doc commit (배포 대기)

---

## 3. 사이클 구조 (~18~21 task)

### 3.1 task ↔ 워크아이템 매핑 (초안 — plan에서 확정)

**총 task ~17~18** (P0 트랙이 4 task → 1 task로 축소).

| Task | 워크아이템 | 비고 |
|---|---|---|
| **T0** | plan commit + Codex **사전** review 백그라운드 dispatch | 관례 |
| **T1** | **P0 회복 검증** — full smoke run × 1 + N=3 안정성 (`desktop-layout` + `tier-downgrade` 3회 반복) + retro 정정 (v3.14.1 메모 갱신) | fail 1회 이상 시 wave 1 분기 |
| T2 | P2-13.2-1 cross-midnight paired-call test | |
| T3 | P2-13.2-2 `Number.isFinite` 통일 + regression spec | |
| T4 | P2-13.2-3 `progressDates` immutability | |
| T5 | P2-13.2-4 spec §1.4 baseline anchor (mechanical) | |
| T6 | P2-12-5 labor_law VS16 cosmetic | |
| T7 | P2-12-6 short-token over-credit **검증** task (v3.14.1 T2 closeout 확인) + 잔여 발견 시 fix | |
| T8 | P2-12-7 toast timer dedup | |
| T9 | P2-12-8 `__resetForTest` clearAllTimers | |
| T10 | P2-12-9 hydrateBadges 함수 size | |
| T11 | P2-12-10 backfill race (guard or annotation) | |
| T12 | P2-NEW-4 손상 v2 silent → toast | |
| T13 | P2-NEW-5 type guard helper + 대표 callsite 1~2 | callsite count Plan에서 확정 (현재 grep `as unknown as User` = 1) |
| T14 | P2-NEW-6 nested modal 대응 (`openModal` 반환값 expose 또는 modal-scoped guard) | |
| T15 | W-1 docs sync (memory + spec/plan retro) | |
| T16 | W-2 Codex 최종 독립 리뷰 (백그라운드 dispatch) | P0/P1 in-cycle fix |
| T17 | W-3 사이클 retro + 배포 대기 commit | 사용자 "배포해줘" 시점에만 push |

### 3.2 분기 처리

- **기본 path (현재 추정)**: 2026-05-02 실측에서 P0 4건 모두 pass — T1 N=3 안정성 통과 시 그대로 P2 트랙 진행, R1/R5 자연 해소.
- **분기 path (저확률, 안전장치)**: T1 N=3 중 1회라도 fail 발생 시 → wave 1(P0 fix) → wave 2(P2) 자연 전환 (구조 옵션 C). 사용자 컨펌 게이트 후 plan 갱신 commit.

---

## 4. DoD (Definition of Done)

| Metric | 목표 | 비고 |
|---|---|---|
| Vitest | **+15 ~ +25** (672 → ~687~697) | paired-call, Number.isFinite regression, immutability, short-token regression, P2-NEW-5 type guard, T11 race guard. P2-NEW-5 scope에 따라 폭 조정. |
| Playwright | **89 → 89 (변동 없음)** + N=3 안정성 검증 (`desktop-layout` + `tier-downgrade`) | 2026-05-02 실측 7/7 pass — N=3 모두 pass = closed. 신규 추가 0~2 가능. |
| Bundle gzip JS | **31.88 ± 0.3 kB** | P2 cleanup 위주, 큰 변동 없음. |
| Lint | **0 errors** (현재 0 유지) / warnings 8 pre-existing. | 신규 errors 0. |
| Schema migration | **0줄** | v2/v3 lazy migrate 변경 없음. |
| LocalStorage 키 | **0건 변경** | |
| CSP | **0건 변경** | |
| TS strict | **0 errors** | |
| N=5 stability | 신규 spec 5/5 | T6/T7/T11/T17 등 신규 it-block. |
| Per-task review | **18+ dispatch** (subagent-driven 표준) | spec + quality 2단계. |
| Codex review | **2-pass** (사전 + 최종) | v3.14 lesson #4 default. 백그라운드 OK. |

### 4.1 DoD 검증 시점

- T1 직후: N=3 안정성 결과 메모 + retro 정정 commit
- T14 직후: 전체 vitest + Playwright + lint + bundle 측정
- T16 (Codex 최종): P0/P1 발견 시 in-cycle fix → DoD 재측정

---

## 5. 리스크 / 블로커

| ID | 위험 | 완화 |
|---|---|---|
| ~~R1~~ | ~~Playwright fail 분기~~ | **자연 해소** (2026-05-02 실측 7/7 pass). T1 N=3 안정성에서 1회 fail 발생 시 R5 분기 발동. |
| **R2** | v3.12 #6 catalog over-credit. 신규 user 행동 변화 가능 (badge unlock 시점). | T7은 v3.14.1 T2 closeout **검증 task**로 처리. 잔여 발견 시 regression spec(`achievements-cross-interest.spec.ts` 패턴) + matchKeyword 확장. schema/migration 0줄 유지. |
| **R3** | P2-NEW-5 callsite 마이그레이션 폭주. helper 도입 후 모든 callsite를 한 번에 바꾸려는 유혹. | 사전 grep 결과 `as unknown as User` callsite = **1개** (`src/state/migration.ts:78`). 단일 callsite로 폭주 위험 자체 낮음 — Plan에서 1 callsite로 lock. |
| **R4** | 사이클 크기(~17~18 task). controller context 부담. | subagent-driven 표준 (per-task spec+quality review). controller 직접 코드 작성 0회 목표 (T0/T15/T17 docs만 직접). |
| **R5** | T1 N=3 안정성 중 1회라도 fail 시 wave 분기. | wave 1(P0 fix) → wave 2(P2) 분리 자연 전환 (구조 옵션 C). 사용자 컨펌 게이트. plan 갱신 commit으로 record. |
| **R6** | v3.14.1 lesson "Plan fixture는 codebase 실제 schema와 사전 매칭" 재발 위험. | Plan 작성 시 모든 fixture (Briefing/Mission/User schema)에 대해 한 번 더 grep + 컴파일 가능 여부 사전 검증. **사전 grep 결과는 Plan controller가 워크아이템별로 인용**. |
| **R7** | 메모리 vs 실측 불일치(2026-05-02 P0 사례) — 사이클 시작 시점에 baseline 검증 누락 시 동일 패턴 재발. | 모든 사이클 시작 시점에 **baseline 실측 task**(N=1 이상) 우선 — 메모리만 신뢰하지 않음. v3.14 lesson에 추가. |

---

## 6. Codex 정책

- **사전 1회 (T0 직후)** — `codex:rescue` 또는 Bash run_in_background로 codex CLI dispatch. plan critique (race·순환·invariant·fixture).
- **최종 1회 (T20)** — fresh thread 독립 리뷰. P0/P1 in-cycle fix policy. P2는 v3.14.3+ backlog.
- **시점**: 두 dispatch 모두 백그라운드 (사용자 권한 부여 받음). controller는 다른 task 진행하면서 결과 회신 시 적용.
- **2-pass 비용**: v3.13.1·v3.14 검증된 ROI — substantial cleanup cycle에 default 권고.

---

## 7. 사용자 정책 정합

- ✅ "오픈된 서비스 아니니 다 반영" — P2 in-cycle 처리 default. P2-NEW-5만 callsite 폭주 방지로 부분 반영.
- ✅ "git push는 사용자 '배포해줘' 시점에만" — T21 commit까지만 진행, push/배포는 사용자 명시 시점.
- ✅ "git add -A 지양" — 모든 commit은 명시적 staging.
- ✅ "되돌리기 어려운 동작은 반드시 확인" — schema migration 0줄, LocalStorage 키 변경 0, CSP 변경 0.
- ✅ "심층 리뷰 분류 P0/P1/P2" — Codex 최종 review에서 구분 명시.
- ✅ "skill 선택 — superpowers" — brainstorming → writing-plans → executing-plans 흐름 준수.

---

## 8. 다음 단계

- spec self-review (placeholder / 일관성 / 모호성 / scope) → 인라인 fix
- 사용자 spec review → OK 시 `superpowers:writing-plans` 진입 → `docs/superpowers/plans/2026-05-02-v3.14.2-hotfix-and-p2-cleanup-plan.md` 작성

### 8.1 사이클 종료 후 차기 후보

- **v3.14.3** — IntersectionObserver 변형 (`daily-briefing-5` view trigger 대안) 사용자 결정 + 6탭 mobile 라벨 wrap 측정/수정.
- **v3.15 Game World** (게임화 시리즈 3/3) — 캐릭터 진화. `cumulative.{period}Count` anchor 활용.

---

## 9. 사이클 lesson 사전 anchor (Plan controller용)

v3.14·v3.14.1 메모리에서 정착된 controller lesson을 Plan 작성 시 체크리스트화:

1. **Plan fixture는 codebase 실제 schema와 사전 grep** — Briefing/Mission/User type, LocalStorage KEY, function signature.
2. **Per-task review의 spec+quality 2단계** 유지 — quality에서 잡히는 P2가 spec review로는 안 잡힘.
3. **Codex 2-pass default** — substantial cleanup cycle.
4. **State→ui 역방향 의존 회피는 사이클 시작 시점에 결정** — T1 prerequisite로 분리.
5. **JSDoc invariant ≠ enforcement** — invariant 추가 시 실제 callers에 적용 verify 필수, test도 paired-call 형태가 베스트.
6. **Pre-existing 정책 violation 처리** — 같은 도메인 cleanup 시 묶어서 처리 (scope creep ≠ policy uniformity).
7. **Plan accounting per-file count 합산 검증** — v3.14.1 lesson #1 재발 방지.
8. **메모리 vs 실측 baseline 검증** (2026-05-02 v3.14.2 사례) — 사이클 시작 시점에 메모리만 신뢰하지 말고 실측 1회 우선. v3.14.1 메모 "4 pre-existing fail" 기록이 main 실측 7/7 pass와 불일치한 사례. 메모리 기록은 측정 시점 환경 노이즈 포함 가능 + 후속 commit이 incidentally fix 했을 가능성 둘 다 존재.

---

## 10. 사이클 retro (2026-05-02)

### 10.1 측정 결과 (T15)

| Metric | Forecast (DoD) | Actual | 결과 |
|---|---|---|---|
| Tasks | 17 | 17 | ✅ |
| Commits | ~17 | 17 (15 task + 2 spec) | ✅ |
| Vitest | +15~25 (672→~697) | **+21 (672→693)** | ✅ |
| Playwright baseline | 89/89 + N=3 | **93/93** (실측 retro: v3.14.1 메모 부정확 확정) | ✅ |
| Bundle gzip JS | 31.88 ± 0.3 kB | **31.94 (Δ +0.06)** | ✅ |
| Lint | 0 errors | 0 ✅ | ✅ |
| TS strict | 0 errors | 0 ✅ | ✅ |
| Schema migration | 0줄 | 0 ✅ | ✅ |
| LocalStorage 키 | 0 변경 | 0 ✅ | ✅ |
| CSP | 0 변경 | 0 ✅ | ✅ |
| N=5 stability 신규 spec | 5/5 | 신규 9 spec 모두 1차 PASS | ✅ |
| Per-task review | 18+ | 13쌍 + 1 combined = ~24 dispatch | ✅ |
| Codex 2-pass | 사전 + 최종 | **0 (settings 권한 issue, v3.14.3 재시도)** | ⚠️ |

### 10.2 14 워크아이템 1:1 처리 결과

| ID | Task | 결과 | 비고 |
|---|---|---|---|
| P0 | T1 | **closed** (실측 7/7 pass × N=3) | v3.14.1 메모 retro 정정 |
| P2-13.2-1 | T2 | DONE | 신규 spec 3 it-blocks |
| P2-13.2-2 | T3 | DONE | NaN/Infinity 차단 + 3 it-blocks |
| P2-13.2-3 | T4 | DONE_WITH_CONCERNS | plan typo 자체 수정 + 추가 mutation site fix |
| P2-13.2-4 | T5 | DONE | mechanical 1-line append |
| P2-12-5 | T6 | DONE | branch b 채택 (실측 stray 발견) |
| P2-12-6 | T7 | closed | v3.14.1 T2(592f8fd)에서 본질 처리, empty commit |
| P2-12-7 | T8 | DONE_WITH_CONCERNS | branch a (closeout) — `removeToast` guard 충분 |
| P2-12-8 | T9 (REPURPOSED) | DONE | spec + JSDoc 추가 (T8 P2-T8-1 in-cycle 흡수) |
| P2-12-9 | T10 | DONE | 70 lines → 22+18+44 split |
| P2-12-10 | T11 | DONE | JSDoc only |
| P2-NEW-4 | T12 | DONE + P1 fix | T12 reviewer P1 (isValidUserShape 비대칭) in-cycle fix `53c649d` |
| P2-NEW-5 | T13 | DONE | mechanical cast 격리 |
| P2-NEW-6 | T14 | DONE | openModal sig 변경 + welcome-gamification 마이그레이션 |
| W-1 | T15 | DONE | 본 retro |
| W-2 | T16 | **deferred** | Codex 권한 issue, v3.14.3에서 재시도 |
| W-3 | T17 | (다음 step) | retro commit |

### 10.3 lesson #8 검증

본 사이클이 lesson #8 (메모리 vs 실측 baseline 검증)을 정착시킨 첫 사례. T1 단계에서 v3.14.1 메모 부정확 발견 → spec retro `759c813`로 사이클 축소 (21 → 17 task). 메모 부정확이 후속 사이클에 재발하지 않도록 lesson anchor 추가됨.

### 10.4 신규 P2 (v3.14.3 backlog)

per-task review에서 누적된 P2 17건 + pre-existing P2-NEW (`seen.ts:19` `firstSeenAt` Number.isFinite) + 2 known flake (user-mission-wiring, v3.12-game-feedback) + Codex review 재시도. 메모 `project_daily_growth_v3142.md`의 "v3.14.3+ Backlog" 섹션 참조.

### 10.5 Codex 권한 issue

settings.json codex companion Bash 패턴이 잘못 등록되어 있어 (closing quote 없음 + `codex/:*` matching 부족), Agent dispatch 시 권한 거부. 본 사이클에서 광범위 패턴 추가 (`Bash(node /Users/.../codex/**)`) 했으나 **현재 세션에 적용 안 됨** (Claude Code는 세션 시작 시 settings 로드). Codex 사전·최종 review 모두 미실행. v3.14.3 사이클 시작 시 사용자 세션 재시작 → 권한 검증 prerequisite로.
