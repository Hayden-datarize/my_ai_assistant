# v3.29 Cleanup + Trim Cycle (C1 회수 + 묶음 carry)

> **Status**: T0 spec 작성 (Codex 사전 review 흡수 후 amend 완료)
> **Trigger**: v3.28 retro R3 발동 + C1(bundle 1278 B trim) + C2(Insight pinned backfill) carry. v3.28 §7.7 차기 후보 "cleanup + trim 묶음으로 net negative Δ 목표 (L2 lesson 적용)".

---

## §1. 사이클 목표 / 범위

### 1.1 목표

v3.28 carry C1 회수 + cleanup 묶음 — **bundle 42778 → 절대 ≤ 41500 B** (GREEN strict 복귀, 트림 집계 Δ ≤ −1448 B — F1+F2 +170 B 흡수 후 41500 도달) + C2 invariant 보강 + 검색 keyword 하이라이트 UX 마무리.

### 1.2 범위 (확정)

| # | 항목 | 출처 | 분류 | bundle 집계 |
|---|---|---|---|---|
| F1 | already-v8 Insight `pinned` backfill (`migrateUserToV8` early-return amend) | v3.28 Codex 최종 P2-1 | cleanup invariant | **trim 집계 외** (L2 mitigation) |
| F2 | archive 검색 keyword `<mark>` 하이라이트 (helper 신설 + boundary-safe + escape) | v3.28 §7.7 | cleanup UX | trim 집계 외 (+150 B 예상) |
| F3 | tab-level **진짜 dynamic import** — stats / archive / missions | v3.28 C1 / 차기 trim 전략 | trim main | 집계 (−800~−1500 B) |
| F4 | CSS purge / unused token 2nd-pass | safety net | trim | 집계 (−150~−400 B) |
| F5 | dead import + side-effect import 2nd-pass sweep | safety net | trim | 집계 (−50~−200 B) |

### 1.3 Out of scope

- 검색 fuzzy (1-char tolerance) — bundle +200~400 B 예상 → trim 목표 충돌, v3.30+ carry
- Plant action layer (v3.21 carry) — 별도 design 필요한 user-facing 신규 기능
- Schema 변경 — v8 stay
- F6-TZ (`src/ui/onboarding.ts:228` UTC slice) — Codex P2-2, onboardedAt UX 무영향이라 backlog

---

## §2. Task Breakdown (Codex 사전 review 흡수 후 v1)

| Task | 내용 | 예상 Δ bundle |
|---|---|---|
| T0 | spec amend (Codex 사전 review 흡수) + plan v1/v2 + Codex 사전 review (mandatory, fallback 명시) | docs |
| T1 | F1 — `migrateUserToV8` already-v8 branch copy-based backfill (`insights[].pinned` default false, `true` 보존) + `migration-v8.spec.ts:43` same-reference 단언 변경 + spec 4~6 신규 | +20 B (집계 외) |
| T2 | F2 — `src/utils/highlight.ts` helper 신설 (escapeHtml + regex escape + `<mark>` span only + boundary-safe + NFC) + archive 검색 결과 카드 적용 + spec 3~5 신규 | +150 B (집계 외) |
| T3 | F3 — `main.ts` / `nav.ts`에서 `stats/archive/missions` 렌더러를 `await import()` 동적 import로 전환 + first-mount hydrate + `vite.config.ts` `manualChunks` 보강 | −800~−1500 B (trim 집계) |
| T4 | F4 — CSS unused token / vendor reset 2nd-pass (`@layer base` grep + production CSS 분석) | −150~−400 B (집계) |
| T5 | F5 — dead import 2nd-pass + side-effect import 수동 점검 (`closeModal` precedent 확산) | −50~−200 B (집계) |
| T6 | 3종 검증 + 양쪽 TZ vitest (KST + TZ=America/New_York) + smoke 97/97 + N=3 cold-start + bundle 최종 측정 + Codex 최종 review | docs |
| T7 | retro + memory file + Obsidian vault sync (`02-Cycles/` + `03-Lessons/`) + (사용자 "배포해줘" 명시 시) `npm run build && firebase deploy --only hosting` | docs |

**예상 trim 집계 net Δ: −1000~−2100 B** (target 트림 집계 Δ ≤ **−1448 B** — 절대 41500 B 도달 위해 F1+F2 +170 B 흡수 필요. 하한 −1000 B는 41948 B로 GREEN 미달 → R4 발동 trigger)

### 2.1 Task별 Codex 사전 review 흡수 detail

- **T1 (F1, P1-1)**: Codex 확인 — `src/state/migration.ts:413` already-v8 early return, `src/state/user.ts:245` `getCachedUser` always-migrate. **idempotency 강제** (copy-based, in-place mutation 금지). `pinned: true` 명시값 보존 mandatory. 회귀 테스트 갱신 의무.
- **T2 (F2, P1-2)**: Codex 확인 — 현재 archive 검색은 filter-only (`archive.ts:379` NFC + `:640/:708` filter). 카드 body `textContent` 사용 (`:535/:561`). highlight 경로 자체가 없어 helper **신규 정의** + 자체 spec (escapeHtml + regex escape + `<mark>` only + boundary) 의무.
- **T3 (F3, P1-3 + R4 격상)**: Codex 확인 — `vite.config.ts:3` manualChunks 없음, `main.ts:3` / `nav.ts:2`에서 탭 렌더러 정적 import. **진짜 `await import()` 전환** 필수 (manualChunks-only는 초기 전송 미감소). first-mount hydrate + preload hint로 R1(FOUC) mitigation.
- **L2 mitigation (P2-1)**: F1을 **bundle Δ 집계 외**로 분류 (T1+T2는 trim DoD 산정 제외). 본 사이클 trim DoD는 T3+T4+T5 ≤ 41500 strict.
- **F6-TZ (P2-2)**: 본 사이클 out of scope, backlog.

---

## §3. DoD

- **bundle CLI gzip 절대 ≤ 41500 B** (current 42778, 트림 집계 target Δ ≤ **−1448 B** — F1+F2 +170 B 흡수 후 41500 도달) — **GREEN strict**
- TS strict 0 errors / lint baseline 30 유지 (v3.27 T2b 잔존)
- vitest 양쪽 TZ regression 0 (1224 baseline + F1/F2 신규 spec 7~11 → **1231~1235** 양쪽 TZ 동일 count)
- smoke 97/97 + N=3 cold-start (T3 dynamic import 회귀 safety)
- Codex 2-pass review 둘 다 dispatch (사전 ✅ 완료, 최종 T6에서 dispatch / CLI 한도 시 controller self-review fallback — v3.14.5/v3.20/v3.24 precedent)
- F1+F2 신규 spec 7~11개 명시
- 3종 검증 (`npx tsc --noEmit && npm run lint && npm test && npm run test:smoke`) 일괄 pass

---

## §4. Risk

| # | Risk | Mitigation | Priority |
|---|---|---|---|
| R1 | T3 tab-lazy 분리가 첫 navigate FOUC / 인터액션 지연 | preload hint (`<link rel="modulepreload">`) + first-mount eager hydrate / smoke 첫-navigate 회귀 spec |
| R2 | T3 chunk graph 변경 → cache invalidation 광범위 | vite asset hash 자동 처리 — deploy 1회 stale-cache 수용 (서비스 워커 image pass-through로 user-side 영향 최소) |
| R3 | F2 검색 하이라이트 regex → XSS | helper 단일 entry point — escapeHtml(text) → split by escaped-regex → `<mark>` wrap escaped span only. NFC normalize 재사용 (`archive.ts:379` precedent). spec 강제 (XSS 시도 5종) |
| R4 | **T3 분리 후 DoD 41500 미달** (Codex 격상 P1) — 하한 −1000 B는 41948 B로 GREEN 미달, 트림 집계 −1448 B 미달 시 발동 | T4+T5 safety net + T3 직후 중간 bundle 측정(예상 ≥ −800 B 미달 시 T4/T5 ROI 재투자 결정) + 미달 시 retro 명시 + v3.30 carry — v3.28 R3 재발 위험 명시적 수용 |
| R5 | Codex 사전 review CLI 한도 | controller self-review fallback (v3.14.5/v3.20/v3.24 precedent) — 본 사이클 사전 review는 **dispatch 성공**, 최종 review는 T6 dispatch 시 한도 점검 |
| R6 | F1 backfill이 prod user.insights에 in-place mutation 회귀 | copy-based pattern mandatory (`{...record, pinned: record.pinned ?? false}`) + idempotency spec |

---

## §5. Codex 사전 Review 결과 (2026-05-12, design phase)

**결과**: `PROCEED_AFTER_FIXES` (P0=0 / P1=3 / P2=2)

| # | Priority | Finding | 흡수 위치 |
|---|---|---|---|
| 1 | P1 | F1 already-v8 backfill 갭 (`migration.ts:413` early return) | T1 idempotency + copy-based + `migration-v8.spec.ts:43` 단언 변경 |
| 2 | P1 | F2 highlight 경로 자체 미존재 → helper 정의+테스트 의무 | T2 `src/utils/highlight.ts` 신설 + spec 3~5 |
| 3 | P1 | F3 manualChunks-only 미흡 → **진짜 dynamic import** 필요, R4 P1 격상 | T3 `await import()` 전환 + nav.ts/main.ts 재구성 + R4 P1 등재 |
| 4 | P2 | L2 위반 (F1 trim gate 내 invariant) | F1을 trim Δ 집계 외 분류 (§1.2 / §2.1 명시) |
| 5 | P2 | F6-TZ `onboarding.ts:228` UTC slice | Out of scope, backlog |

**디스크립터**: cleanup+trim 묶음 사이클, L2 lesson graduate 직후 첫 묶음. F1을 trim 집계 외로 분류해 lesson 정신 유지.

### 5.1 Self-review 보정 (2026-05-12, spec 작성 직후)

Codex 사전 review와 spec 작성 모두 동일 수식 갭 — F1+F2 +170 B 흡수 미반영 — 을 가졌음. controller self-review에서 발견·수정:

- 트림 집계 target: `−1278 B` → `−1448 B` (절대 41500 도달 위해 +170 B 흡수)
- 하한 −1000 B는 41948 B로 GREEN 미달 (R4 발동 trigger 명문화)
- §1.1 / §2 / §3 DoD / §4 R4 / §6 row 1 — 5곳 정정 완료

**신규 lesson 후보**: L2.2 — invariant 강화 task를 trim 집계 외 분류 시 절대 DoD에는 그 cost를 흡수해야 함 (집계 외 ≠ bundle 영향 0). retro에서 graduate 검토.

---

## §6. L2 lesson 적용 명시 (v3.28 graduate 직후)

L2: "**dedicated trim cycle에 invariant 강화 task 묶지 말 것**."

본 사이클은 dedicated trim이 아닌 cleanup+trim 명시 묶음. 그래도 L2 정신 따라:

1. **bundle Δ 집계 분리**: F1/F2(cleanup)는 trim DoD 산정 외 (§1.2 표 우측 컬럼). T3+T4+T5만 ≤ −1448 B target에 산입 (절대 41500 도달 위해 F1+F2 +170 B 흡수 후 산정).
2. **commit 분리**: T1(F1), T2(F2)는 trim task(T3~T5)와 commit 분리. 측정도 별도 step.
3. **review 우선순위**: Codex 사전 review에서 F1 P1 흡수 — already-v8 branch 갭은 v3.28에서도 발견 못 한 latent invariant. trim 묶음에 흡수하지 않았다면 production에서 발견됐을 risk.

L2 mitigation 후속 lesson 후보 (retro 시 확정):
- **L2.1**: cleanup+trim 묶음 시 bundle Δ 집계는 **trim task만** — invariant/UX 추가는 별도 소계.

---

## §7. Retro (2026-05-12 사이클 완료)

### 7.1 사이클 요약

| 항목 | 값 |
|---|---|
| Task 수 | 8 (T0~T7) — T5는 0-changes safety-net 확인 |
| Commit 수 | 9 (T0 spec + T0 plan + T1 + T1 review fix + T2 + T3 + T3 review fix + T4 + T6 verification) — T5 commit 없음 |
| 최종 main work commit | `9ee39d0` (T4) |
| vitest KST | **1239/1239 PASS** (195 files, 1224 baseline + 15 신규) |
| vitest NY | **1239/1239 PASS** (양쪽 TZ same count) |
| Smoke | **98/98 PASS** (N=1 12.0s + N=3 cold-start 11.7/12.2/13.7s, flake 0) |
| TS strict | 0 errors |
| Lint | 30 baseline 유지 (2E + 28W, v3.27 T2b 잔존) |
| **Bundle CLI gzip 절대값** | **23605 B** (baseline 42778 → Δ **−19173 B**, GREEN strict 41500 ≪ 17895 B 여유) |
| **트림 집계 net** | T3(−19479) + T4(−6) + T5(0) = **−19485 B** vs target −1448 B → **13.5× over target** |
| Cleanup 집계 외 | T1(+27/+1) + T2(+319) + T3 review fix(+99) ≈ +418 B (예산 +170 over 248 B, DoD impact 0) |
| Codex 사전 review | **PROCEED_AFTER_FIXES** P0=0 / P1=3 / P2=2 → 모두 in-cycle 흡수 (thread `a1b1baad858237076`) |
| Codex 최종 review | **APPROVED_WITH_NOTES** P0=0 / P1=0 / P2=2 (둘 다 v3.30 carry) |

### 7.2 DoD 평가

- bundle 절대 ≤ 41500 B GREEN strict: ✅ **실측 23605 B** (target 17895 B 여유 도달)
- TS strict 0 / lint baseline 30 / vitest 양쪽 TZ regression 0 / smoke 98/98 N=3 0 flake / Codex 2-pass dispatch ✅
- 트림 집계 target Δ ≤ −1448 B: ✅ **실측 −19485 B (13.5× over)**

### 7.3 핵심 변경

| Commit | Task | 내용 |
|---|---|---|
| `dd95541` | T0 | spec — Codex 사전 P1=3 흡수 + self-review 수식 정합 정정 5곳 |
| `7a40a08` | T0 | plan v1 — 8 task / TDD steps / L2 mitigation commit 분리 |
| `fc858a6` | T1 | migrateUserToV8 already-v8 backfill — copy-based + same-ref fast-path + spec 3 |
| `7ae95e2` | T1 fix | per-task review: insights 비배열 정규화 (asymmetric backfill catch) + spec 1 |
| `ecd1feb` | T2 | highlight.ts 신설 (escapeHtml 재사용 DRY) + archive 2 callsite + spec 10 |
| `b8afd93` | T3 | tab 렌더러 진짜 dynamic import — nav.ts/main.ts async + 5 async cascade + 4 unit spec + smoke 1 |
| `012e20e` | T3 fix | per-task review: I1 race (pendingSwitchToken) + I2 silent fail (.catch × 5 callsite) + spec 1 |
| `9ee39d0` | T4 | CSS unused token 12개 제거 (BEM modifier 14건 정확히 keep) |
| `bfde27e` | T6 | 3종 + 양쪽 TZ + smoke N=3 + bundle 23605 + Codex 최종 APPROVED_WITH_NOTES |

### 7.4 신규 lessons (graduate 후보)

- **L1 (graduate 후보)**: **사전 Codex P0/P1 ROI 11사이클 연속** (v3.18~v3.29). 본 사이클은 P0=0였지만 P1=3 catch (already-v8 backfill / highlight 미존재 / manualChunks-only 미흡 + R4 격상) 모두 in-cycle 흡수. v3.28에서 L1 graduate 결정 — v3.29에서 ROI 재확인.
- **L2.1 (graduate)**: **cleanup+trim 묶음 시 bundle Δ 집계는 trim task만**. F1/F2(cleanup invariant/UX)를 트림 DoD 산정 외로 분류해 L2 lesson 정신 유지. T3 over-perform으로 결과적으로 큰 이슈는 아니었지만, 작은 trim cycle이었다면 mitigation 효과 컸을 것. CLAUDE.md "Cycle Workflow" 섹션에 추가 명시 권고.
- **L2.2 (graduate 후보)**: **수식 정합 self-review mandatory** — spec 작성 시 "집계 외 ≠ bundle 영향 0" 갭. Codex 사전 review도 빠뜨림. controller self-review가 catch (−1278 → −1448 B 재산정 5곳). 향후 spec writer는 절대 metric과 상대 metric 간 수식 일관성 명시적 검증.
- **L3 (신규)**: **per-task code-reviewer agent ROI 확인**. T1/T2/T3 모두 pr-review-toolkit:code-reviewer dispatch — T1에서 asymmetric backfill (`insights` 비배열) catch, T3에서 race(I1) + silent fail(I2) catch. Codex 최종 review가 latent였다면 production 도달했을 risk. dispatch 정책 graduated 권고: 1+ file 신설 또는 async/concurrency 변경은 reviewer agent 의무.
- **L4 (신규)**: **trim task에서 dynamic import의 over-perform 가능성**. T3 estimate −800~−1500 B, 실측 **−19479 B** (13× over). 이유: 정적 import tree가 main bundle에 모두 inline됐었고, dynamic chunk split이 그 tree 전체를 외부화. 향후 dynamic import 후보가 발견되면 estimate 보수성 ↑. C5: vite reporter chunked estimation 신뢰성 재검토.

### 7.5 v3.30 carry-forward

| # | 항목 | 출처 | 우선순위 |
|---|---|---|---|
| C1 | `src/ui/handlers/stats.ts` 가 `home.ts:32` + `welcome-garden.ts:5`에서 static import (scrollToGardenSection) — dynamic split 부분 무효화. helper 분리 module로 추출 권고 | T3 code review I3 | P1 |
| C2 | `vite.config.ts` 빈 `manualChunks: {}` placeholder — drop 또는 TODO 명시 | T3 code review M1 | P3 |
| C3 | smoke `dynamic-tab-load.spec.ts` N=1 → N=3 격상 | T3 code review M2 + Codex 최종 P2-1 | P2 |
| C4 | `void switchTab(...).then()` 패턴 → `async/await` 스타일 리팩토링 | T3 code review M3 | P3 |
| C5 | `<mark>` CSS 토큰화 (`--accent-soft` 등) — 다크/라이트 듀얼 테마 통합 | T2 code review N1 | P3 |
| C6 | rapid-switch race spec을 controlled deferred loader로 강화 | Codex 최종 P2-1 | P2 |
| C7 | first-load 시 all handlers import 명시 (Promise.all `main.ts:28-33`) — 추후 home만 eager / 나머지 navigate 시점 lazy로 재구성 | Codex 최종 P2-2 | P2 |
| C8 | F6-TZ `src/ui/onboarding.ts:228` UTC slice — onboardedAt UX 무영향이라 격하 보류 | Codex 사전 P2-2 | P3 |
| C9 | 검색 fuzzy (1-char tolerance) — bundle 여유로 가능성 ↑ (T3 over-perform 효과) | v3.29 out-of-scope | P2 |
| C10 | Plant action layer 구현 | v3.21 carry | P2 |

### 7.6 차기 사이클 후보

- **v3.30 cleanup**: C1~C7 + C9~C10 묶음. T3 over-perform으로 bundle 여유 17 KB → 검색 fuzzy / Plant action / 신규 기능 가능. 10~13 task.
- **v3.30 polish**: C2/C4/C5 묶음만 (소규모) — 코드 ergonomics 위주.
- **v3.30 trim 재시도**: 사실상 필요 X (이미 23605 B로 GREEN strict). C1 stats handler 분리만 추가 trim 가능 — 별도 사이클 시도 우선순위 ↓.

### 7.7 Codex 최종 review 흡수 detail

**APPROVED_WITH_NOTES** (`a3e0f7d358893342e` agentId, `--resume` thread `a1b1baad858237076`):

- P2-1 (rapid-switch spec strengthening): T3 race fix spec(`tests/unit/nav.spec.ts:31-37`)이 두 번의 `switchTab` 호출 후 final state 확인만 함. controlled deferred loader로 out-of-order resolve를 강제 simulate하면 더 강함. → **C6 (P2 carry)**
- P2-2 (first-load handler import 패턴): `main.ts:28-33`의 Promise.all이 home만 아닌 archive/stats/missions handler까지 모두 import. C1 stats handler 분리와 합쳐서 v3.30에서 home-only-eager + lazy rest 패턴으로 재구성 가능. → **C7 (P2 carry)**

---

**작성**: 2026-05-12 (v3.29 사이클 완료)
**baseline commit**: `75f64b2` (v3.28 T7 retro)
**최종 commit**: `9ee39d0` (T4) + 본 retro append (T7)
**Codex thread**: `a1b1baad858237076` (사전 → 최종, --resume 단일 thread)
**디스크립터**: cleanup+trim 묶음 — C1(bundle −19485 B) + C2(insight pinned backfill) + F2(검색 highlight) + 진짜 dynamic import / 사전 P1=3 흡수 / per-task review I1+I2 catch / Codex 최종 APPROVED_WITH_NOTES P2=2

---

**작성**: 2026-05-12 (v3.28 T7 retro `75f64b2` 후속 사전 예약 commit)
**baseline commit**: `75f64b2` (v3.28 T7 retro)
**Codex thread**: `a1b1baad858237076` (사전 review, --resume으로 최종 연결 예정)
**디스크립터**: cleanup+trim 묶음 — C1 회수 + C2 invariant + F2 UX + 진짜 dynamic import / 사전 P1=3 흡수 / L2 mitigation 명시
