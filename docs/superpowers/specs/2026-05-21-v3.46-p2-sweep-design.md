# v3.46 — P2 Sweep (v3.45 Codex 최종 carry P2-A/B/C)

**작성**: 2026-05-21 KST
**Type**: Lightweight P2 sweep cycle (잠재 버그 가드 1건 + wording staleness 정정 2건). 신규 schema 0 / 신규 기능 0.
**Precedent**: v3.42 carry-cleanup, v3.44.1 cleanup-patch (small cycle).
**Roadmap**: v3.46~v3.50 sprint (A안) 1번째 — v3.45 carry P2 청산.

---

## §0. 범위 (사용자 결정 2026-05-21)

- ✅ **scope: P2-A/B/C만** (lightweight ~30min). 사용자 confirm.
- ✅ C4 (briefing thrash) / C5 (layer cycle) — 재개 트리거 대기, v3.46 범위 밖.
- ✅ C7+ (GCP endpoint) — 신규 endpoint 도입 시.
- ✅ v3.17 §7.2 (식물 detail modal / Streak Freeze) — v3.47+ 신규 기능 cycle.
- ✅ L4 lesson ("사이클 amend 시 grep-based wording sweep 의무") — **본 사이클에서 실천 적용**, COMMON_MISTAKES.md 정식 graduate는 사례 추가 누적까지 보류 (사용자 결정).

---

## §1. Existing Landscape (변경 경계면)

### P2-A — `_handleSendAnswerDm` body guard

`functions/src/sendAnswerDm.ts:55`:

```ts
const body = req.body as Partial<AnswerDmRequest>;
if (typeof body.email !== 'string' || !body.email.endsWith(ALLOWED_DOMAIN)) { ... }
```

- App Check verify (44~54) 통과 후 `req.body`가 `null`/`undefined`/non-object이면 `body.email` 접근 시 `TypeError` → onRequest 미처리 500.
- 현 가드는 `typeof body.email !== 'string'` 인데, `body`가 `null`이면 그 이전에 throw.
- contract test (`functions/src/__tests__/sendAnswerDm-contract.spec.ts`)는 항상 object body를 makeReq로 주입 — null body 경로 미검증.

### P2-B — `src/services/appCheck.ts:7` 주석 staleness

```
 * Phase B (T8 retro 후 별도 deploy): backend `enforceAppCheck: true` + Console
 *                 Enforce ON. controller가 사용자 명시 후 진행.
```

- 실제 v3.45 구현은 `enforceAppCheck: true`(HttpsOptions 미존재)가 아니라 `firebase-admin/app-check` `verifyToken()` manual middleware (`_handleSendAnswerDm` 44~54). 주석이 미구현 옵션을 가리켜 오해 소지.

### P2-C — v3.45 plan.md rollback wording staleness

`docs/superpowers/plans/2026-05-21-v3.45-app-check-phase-b-plan.md`:

- **585행**: `onRequest endpoint는 코드 옵션 enforceAppCheck:true (functions/src/sendAnswerDm.ts:31)가 primary` — stale. 실제 primary = `verifyToken()` 수동 검증 (line 44~54), line 31은 `SLACK_BOT_TOKEN` secret.
- **593행** (Rollback 표 A1/B1): `enforceAppCheck: false + firebase deploy --only functions` — stale rollback 메커니즘. 실제 rollback = `_handleSendAnswerDm`의 App Check verify 블록 제거/우회 후 deploy.
- **7행 / 17행** (Architecture 요약 + 파일 표): `enforceAppCheck:true backend SDK enforce` 표현 정합 (254행은 이미 정정 note 존재).

### Wording sweep 경계 (L4 실천)

`rg "enforceAppCheck"` 전수 결과 중:

- **수정 대상 (현재 production/운영 오도)**: `src/services/appCheck.ts:7` + v3.45 plan.md (585/593/7/17).
- **건드리지 않음 (역사적 기록 — 작성 당시 정확)**: v3.41~v3.44 spec/plan, v3.45 spec §retro (P0 catch 경위 기록), codex-*.txt. 재작성 시 history 왜곡.

---

## §2. Tasks

### T1 — P2-A: req.body null/undefined guard + contract test

- `_handleSendAnswerDm`에서 App Check verify 통과 직후, `body` cast 전:
  ```ts
  const rawBody = req.body;
  if (rawBody === null || typeof rawBody !== 'object') {
    res.status(400).json({ error: 'invalid_body' });
    return;
  }
  const body = rawBody as Partial<AnswerDmRequest>;
  ```
- contract test +1: `req.body = null` (또는 undefined) → 400 `invalid_body`. App Check token은 valid (가드가 verify 이후 동작 확인).
- 기존 6 → 7 contract test. functions vitest 30 → 31.

### T2 — P2-B: appCheck.ts 주석 블록 정정 (Codex 사전 P2-pre-1 흡수)

- line 7의 `enforceAppCheck: true`뿐 아니라 **주석 블록 전체(1~14행) 정정**: `Phase A (현재)` framing도 이제 stale (Phase B 배포 완료).
  - Phase A = v3.41 monitor mode (과거형).
  - Phase B = v3.45 manual `verifyToken()` middleware (배포 완료) + Console Enforce.
- comment-only → 번들 영향 0 (주석 strip).

### T3 — P2-C: v3.45 plan.md wording 정정

- 585행: primary 메커니즘을 `verifyToken()` 수동 검증으로 정정.
- 593행: rollback A1/B1 액션을 verify 블록 제거/우회 deploy로 정정.
- 7행 / 17행: `enforceAppCheck:true` 표현을 실제 구현(verifyToken)으로 정합.
- 본 사이클 spec/plan에는 amend 후 `rg "enforceAppCheck"` 재검사로 잔여 0(역사적 기록 제외) 확인.

---

## §3. DoD (3종 검증)

- TS strict (root + functions): `npx tsc --noEmit` 0 errors.
- ESLint: `npm run lint` 0 errors.
- functions vitest: 31 PASS (+1 T1).
- frontend vitest: 1548 PASS 양쪽 TZ (변동 0 — src 로직 변경 없음, appCheck 주석만).
- Playwright smoke: 109 PASS (변동 0).
- Bundle: frontend 변경 0 (comment-only) — index/archive/translateToast/appCheck/settings 5-chunk Δ ≈ noise. quint record.
- L4 실천 (Codex 사전 P2-pre-2 흡수 — grep 대상 정정): amend 후 `rg "enforceAppCheck" src/services/appCheck.ts docs/superpowers/plans/2026-05-21-v3.45-app-check-phase-b-plan.md` 잔여 0 (역사 기록 제외) 확인. (실제 P2-C stale wording은 v3.45 plan에 있음.)

---

## §4. Risks

| 리스크 | 완화 |
|---|---|
| body guard가 정상 요청 경로 회귀 | 기존 6 contract test 전부 유지 PASS 확인 (object body 정상 통과) |
| `invalid_body` error code가 client 호환성 깨짐 | client는 ok/실패만 분기 — 신규 400 코드는 정상 사용자에 발생 불가 (정상 flow는 항상 JSON body POST) |
| 주석/문서 수정이 코드에 영향 | T2/T3 comment+docs-only, 빌드/테스트 무영향 — git diff로 src 로직 변경 0 확인 |

---

## §5. Codex 2-pass

- **사전 (T0c)**: small cycle (code 1 file + test + comment + docs). direct CLI 1차 시도 의무. 한도/process 중단 시 controller self-review fallback (v3.43 small cycle exception). graduated checklist 5항목은 schema/신규기능 0이라 대부분 N/A.
- **최종 (deploy gate)**: direct CLI `< /dev/null` 의무. fallback 6단계 정책.

---

## §6. v3.47 Carry-Forward

| ID | 항목 | 출처 | 우선순위 |
|---|---|---|---|
| C4 | first briefing storage thrash | v3.41 carry | L |
| C5 | state↔ui layer cycle 정리 | v3.41 carry | L |
| C7+ | API restrictions + dev key 분리 | v3.45 deferred | L (endpoint 도입 시) |
| v3.17 §7.2 | 식물 detail modal / Streak Freeze 등 | v3.17 retro | M (v3.47 본체) |
| L1+L2+L3+L4 | App Check 2-phase + handler DI + silent failure UX + wording sweep | v3.45+v3.46 자체 | L (사례 누적 후 graduate) |

---

## §7. Retro (2026-05-21 KST)

### 7.1 Cycle 요약

- **Type**: Lightweight P2 sweep (잠재 버그 가드 1 + wording staleness 정정 2). schema 0 / 신규 기능 0.
- **변경**: code 1 file (sendAnswerDm.ts) + test 1 file (+1 case) + comment 1 file (appCheck.ts) + docs 2 file (v3.45 plan + 본 spec).
- **Time**: spec/review/impl/verify ≈ 40min.

### 7.2 검증 결과

| 항목 | 결과 |
|---|---|
| TS strict (root + functions) | 0 errors |
| ESLint | 0 errors |
| Functions vitest | 31 PASS (+1 T1) |
| Vitest TZ=KST | 1548 PASS (drift 0) |
| Vitest TZ=NY | 1548 PASS (drift 0) |
| Playwright smoke | 109 PASS (20.8s, cold-start fresh fork) |

### 7.3 Bundle Quint Record

| Chunk | v3.45 | v3.46 | Δ | 사유 |
|---|---:|---:|---:|---|
| index | 9377 | 9377 | 0 | 변경 없음 |
| archive (3 chunk 합) | 8963 | 8963 | 0 | 변경 없음 |
| translateToast | 2135 | 2135 | 0 | 변경 없음 |
| appCheck | 13215 | 13215 | 0 | 주석만 (strip) |
| settings | 3972 | 3972 | 0 | 변경 없음 |

전 chunk Δ 0 — 본 사이클 frontend 변경은 comment-only.

### 7.4 Codex 2-pass Review

- **사전 (T0c)**: direct CLI `< /dev/null` **PROCEED_AFTER_FIXES** P0=0/P1=0/P2=2 (모두 in-cycle 흡수):
  - P2-pre-1: P2-B를 line 7만이 아니라 주석 블록 전체로 확장 ("Phase A (현재)" framing도 stale) → T2 확장 흡수.
  - P2-pre-2: spec DoD grep 대상이 `v3.46*` 오류 — 실제 P2-C stale은 v3.45 plan → DoD grep 정정.
- **최종 (deploy gate)**: direct CLI `< /dev/null` **DEPLOY_APPROVED** P0=0/P1=0. Codex가 firebase-functions@6.6.0 `HttpsOptions`에 `enforceAppCheck` 부재 독립 재확인 + contract test 7/7 직접 실행.

### 7.5 신규 Lessons / 메타

- **L4 실천 적용 (graduate 보류)**: 사이클 amend 시 grep-based wording sweep (`rg "enforceAppCheck"`)을 실천. 단 "현재 production/운영 오도 문구"(코드 주석 + 운영 rollback/요약)만 수정, v3.41~v3.44 역사 spec/plan + 원본 task-step 제목(inline 정정 있음)은 보존 — history 왜곡 회피 경계 확립. 사용자 결정으로 COMMON_MISTAKES.md 정식 graduate는 사례 추가 누적까지 보류.
- **사전 review ROI 13사이클 연속**: small cycle에서도 Codex 사전이 P2-pre-1/2 (scope 확장 + DoD grep 오류) catch — controller가 놓친 인접 staleness.

### 7.6 v3.47 Carry-Forward

§6 그대로 (C4 / C5 / C7+ / v3.17 §7.2 / L1~L4). v3.47 본체 = 식물 detail modal (v3.21 carry).
