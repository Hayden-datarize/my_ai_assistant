# v3.30 Archive Entity UI + Cleanup Carry (Design Spec)

**작성일**: 2026-05-12
**baseline**: `e6288ac` (v3.29 T7 retro, deploy 완료)
**디스크립터**: archive entity chip 신규 UI (segmented + 카운트) + v3.29 carry 6건 묶음 (중형 cleanup 사이클)

---

## §1. Motivation

### 1.1 사용자 신규 요구 (2026-05-12, v3.29 배포 직후)

> "아카이브 탭에 전체/답변/스크랩/인사이트 entity 필터가 너무 작고 안 예쁨"

원인: `.archive-entity-chip` CSS 정의 0줄. v3.27 T2b에서 component 도입 + 핸들러 wiring 후 styling 누락 상태. 기본 `<button>`으로 렌더링되어 작고 unstyled.

### 1.2 v3.29 carry-forward (T7 retro §7.5)

| # | 항목 | 출처 | 우선순위 | 본 사이클 처리 |
|---|---|---|---|---|
| C1 | `src/ui/handlers/stats.ts` static import 분리 (T3 dynamic split 완성) | T3 review I3 | P1 | ✅ T3 포함 |
| C2 | `vite.config.ts` 빈 `manualChunks:{}` placeholder drop | T3 review M1 | P3 | ✅ T6 묶음 |
| C3 | smoke `dynamic-tab-load.spec.ts` N=1 → N=3 격상 | T3 review M2 + Codex P2-1 | P2 | ✅ T5 묶음 |
| C4 | `void switchTab().then()` → async/await | T3 review M3 | P3 | ✅ T6 묶음 |
| C5 | `<mark>` CSS 토큰화 (`--accent-soft` 다크/라이트 통합) | T2 review N1 | P3 | ✅ T6 묶음 |
| C6 | rapid-switch race spec controlled deferred loader 강화 | Codex 최종 P2-1 | P2 | ✅ T5 묶음 |
| C7 | first-load handler import 패턴 재구성 (home eager / rest lazy) | Codex 최종 P2-2 | P2 | ⏭️ v3.31 carry (controller self-review 격하 — listener 등록 분리 리팩토링 동반 필요, 단순 import lazy로는 cross-tab listener 누락) |
| C8 | F6-TZ `onboarding.ts:228` UTC slice | Codex 사전 P2-2 | P3 | ⏭️ v3.31 (UX 무영향) |
| C9 | 검색 fuzzy (1-char tolerance) | v3.29 out-of-scope | P2 | ⏭️ v3.31 (별도 기능, bundle +200~400 B) |
| C10 | Plant action layer 구현 | v3.21 carry | P2 | ⏭️ v3.31+ (별도 design 필요) |

### 1.3 본 사이클 out-of-scope

- C8/C9/C10 (v3.31+ carry).
- Plant action layer — 별도 design 의무.
- 검색 fuzzy — bundle +200~400 B 별도 사이클이 더 안전.

---

## §2. Task Breakdown

| Task | 내용 | 예상 Δ bundle |
|---|---|---|
| T0 | spec + plan v1/v2 + Codex 사전 review (mandatory) | docs |
| T1 | Archive entity CSS 신설 — `.archive-entity-row` + `.archive-entity-chip` + `.archive-entity-count` (segmented + 카운트 + 모바일 안전망) + WCAG AA 검증 | +200~300 B |
| T2 | `renderEntityChipRow(active, counts)` 시그니처 확장 — counts nullable, null 시 카운트 span 비표시. `src/ui/handlers/archive.ts` count 계산 + DOM 라벨 1회 갱신. | +100 B |
| T3 | C1 — `src/ui/handlers/stats.ts` 의 `scrollToGardenSection` (또는 동등 helper) 별도 module 추출. `home.ts:32` + `welcome-garden.ts:5` static import 해소. T3 dynamic split 완성. | −50 ~ +0 B (chunk 재배치 net) |
| T4 | ⏭️ **v3.31 carry-forward** (C7 격하 — listener mount-time 의존성, §2.1 T4 detail 참고) | 0 B |
| T5 | C6 + C3 — nav race spec controlled deferred loader 강화 + smoke `dynamic-tab-load.spec.ts` N=1 → N=3 격상 | docs/test |
| T6 | C2 + C4 + C5 P3 폴리시 묶음 — `vite.config.ts` `manualChunks:{}` drop + `void switchTab().then()` → async/await + `<mark>` 토큰화 (`--accent-soft` 다크/라이트 통합) | −20 ~ +20 B |
| T7 | 3종 검증 (`npx tsc --noEmit && npm run lint && npm test && npm run test:smoke`) + 양쪽 TZ vitest + smoke 98/98 + N=3 cold-start + bundle CLI gzip 측정 + Codex 최종 review | docs |
| T8 | retro + 메모리 file + Obsidian sync (`02-Cycles/` + `03-Lessons/`) + (사용자 "배포해줘" 명시 시) `npm run build && firebase deploy --only hosting` | docs |

**예상 net Δ bundle**: +200 ~ +420 B (T4 격하로 entry trim 효과 제거. current 23605 → 23805~24025 B, GREEN strict 41500 한참 아래).

### 2.1 Task별 detail

#### T1 — Archive entity CSS (신설)

`src/styles/pages/archive.css`에 추가:

```css
.archive-entity-row {
  display: flex; gap: 0; margin-bottom: 12px;
  border: 1px solid var(--border); border-radius: 12px;
  overflow: hidden;  /* outer radius 보존 */
  background: var(--bg-card);
}
.archive-entity-chip {
  flex: 1 1 0; min-width: 0;  /* 균등 분할, 라벨 줄임 가능 */
  padding: 8px 12px;
  border: 0; border-right: 1px solid var(--border);
  background: transparent; color: var(--text-primary);
  font-family: var(--font-body); font-size: 0.9rem; font-weight: 500;
  cursor: pointer;
  transition: background 0.18s var(--ease-out-soft), color 0.18s var(--ease-out-soft);
}
.archive-entity-chip:last-child { border-right: 0; }
.archive-entity-chip:hover:not(.active) { background: var(--bg-input); }
.archive-entity-chip.active {
  background: var(--primary); color: #fff;
  box-shadow: var(--shadow-sm) inset;
}
.archive-entity-count {
  margin-left: 4px;
  color: var(--text-tertiary); font-weight: 400;
}
.archive-entity-chip.active .archive-entity-count {
  color: rgba(255, 255, 255, 0.85);  /* WCAG AA: primary 배경 + 흰 글자 contrast 만족 */
}

/* mobile 1줄 강제 + 안전망 scroll */
@media (max-width: 480px) {
  .archive-entity-chip {
    padding: 6px 10px; font-size: 0.85rem;
  }
}
@media (max-width: 360px) {
  .archive-entity-row {
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    border-radius: 12px;
  }
  .archive-entity-chip { scroll-snap-align: start; flex: 0 0 auto; }
}
```

#### T2 — Count wiring

`archive-entity-chip.ts`:

```typescript
export function renderEntityChipRow(
  active: EntityFilter = 'all',
  counts: Record<EntityFilter, number> | null = null,
): string {
  const chips = ENTITY_ORDER.map((id) => {
    const checked = id === active ? 'true' : 'false';
    const activeClass = id === active ? ' active' : '';
    const countSpan = counts !== null
      ? `<span class="archive-entity-count" data-entity-count="${id}">${counts[id]}</span>`
      : '';
    return `<button type="button" role="radio" class="archive-entity-chip${activeClass}" data-entity="${id}" aria-checked="${checked}">${ENTITY_LABELS[id]}${countSpan}</button>`;
  }).join('');
  return `<div id="archiveEntityFilters" role="radiogroup" aria-label="아카이브 카테고리" class="archive-entity-row">${chips}</div>`;
}
```

`src/ui/handlers/archive.ts`에서:

- 단일 helper `getEntityCounts()` 신설 (closure 내부) — `all` = total / `answer` = 답변만 / `scrap` = 스크랩 / `insight` = 인사이트.
- DOM 갱신 helper `refreshEntityCounts()` 신설 — `document.querySelectorAll('[data-entity-count]').forEach((el) => { const id = el.getAttribute('data-entity-count'); el.textContent = String(counts[id]); })`.
- **호출 시점**:
  1. 첫 `renderArchive()` 시점에 counts=null로 chip row 렌더 → 데이터 load 직후 `refreshEntityCounts()` 1회 (R1 race).
  2. 답변/스크랩/인사이트 **추가/삭제 후** — 실재 이벤트 `dg:archive:updated` (단일, [archive.ts:74](src/ui/handlers/archive.ts#L74) `dispatchEvent(new CustomEvent('dg:archive:updated', { detail: { entity, id } }))`) 1개에 hook. `refreshEntityCounts()` 재호출 (chip row 재생성 X, textContent만).
- chip row 자체는 재생성 X — text 노드만 update (DOM 노드 재사용 + ARIA state 보존).

#### T3 — C1 stats handler 분리

`src/ui/handlers/stats.ts`에서 `home.ts` / `welcome-garden.ts`가 import하는 helper(`scrollToGardenSection` 또는 동등) → 별도 module (예: `src/ui/handlers/stats-shared.ts`) 추출.

**Plan 단계 의무**: `grep -rn "from.*handlers/stats" src/`로 모든 importer 재확인 후 분리 대상 helper 식별.

#### T4 — C7 first-load 재구성

**실측 정정 (controller self-review, 2026-05-12)**:

`src/main.ts:28-37` 현재 코드:

```typescript
// v3.29 T3 (Codex 사전 P1-3): handler mount lazy — boot 시점에 listener 등록을
// 보존하기 위해 Promise.all 병렬 await. 향후 truly first-mount eager-only로
// 줄이려면 handler가 dg:* listener 등록을 tab-changed 시점으로 미루도록 리팩토링 필요.
const [home, archive, stats, missions] = await Promise.all([
  import('./ui/handlers/home'),
  import('./ui/handlers/archive'),
  import('./ui/handlers/stats'),
  import('./ui/handlers/missions'),
]);
home.mountHomeHandlers();
archive.mountArchiveHandlers();
stats.mountStatsHandlers();
missions.mountMissionsHandlers();
```

v3.29 T3 코멘트에 명시: **truly lazy mount는 listener 등록 분리 (`registerListeners` vs `mountHandlers`) 리팩토링 동반 필요**. 단순 `mount*` 호출만 nav.ts switchTab 시점으로 옮기면 cross-tab listener (예: home의 답변 save → archive 카운트 update 이벤트 hook) 누락.

**본 사이클 T4 scope 격하**: import만 직렬화/병렬 미세 polish 또는 listener 분리 사전 작업. truly lazy mount는 **v3.31+ carry**로 이연 (R4 명시).

선택지 (T4 plan v1 시점 controller 결정):

- **옵션 A (안전)**: T4를 **v3.31+ carry-forward**로 이연. 본 사이클 T4 = 0 task. 사이클 task 수 T0~T8 → T0~T7 (8개).
- **옵션 B (사전 작업)**: T4에서 listener 등록을 `register*Listeners()` 함수로 분리만 수행 (mount 분리 X). v3.31에서 mount-lazy 시점에 자연 reuse. trim 효과 ~0.

내 추천: **옵션 A**. 본 사이클은 archive UI(사용자 신규 요구) + carry 6건(C1/C2/C3/C5/C6 + T6 폴리시) 집중. T4(C7) 이연.

#### T5 — C6 + C3 spec 강화

**C6 (race spec)**: 현재 `tests/unit/nav.spec.ts:31-37`는 `switchTab` 두 번 호출 후 final state만 단언. controlled deferred loader (Promise resolver 외부 노출)로 out-of-order resolve를 강제 시뮬.

**C3 (smoke N=3)**: `tests/smoke/dynamic-tab-load.spec.ts`에 `test.describe.configure({ retries: 0, repeatEach: 3 })` 또는 `test.describe('… (N=3)', () => { for (let i = 0; i < 3; i++) test(\`run #${i+1}\`, …); })` 패턴 — controller 선택 (Playwright 정식 API). T5 plan 단계에서 기존 spec 1회 → 3회 cold-start 호출 확정.

#### T6 — P3 폴리시 묶음

- **C2**: `vite.config.ts`의 `output: { manualChunks: {} }` placeholder drop (rollup 기본 동작 + 명시 chunk 0개).
- **C4**: `void switchTab(...).then(...)` 패턴 — async function으로 변경 + `await switchTab(...)` 직접 호출. T6 plan 단계에서 `grep -rn "void switchTab\|switchTab.*\.then" src/` 의무로 모든 호출처 확인 후 일괄 마이그레이션.
- **C5**: `<mark>` 배경/글자색 hardcoded → `var(--accent-soft)` / `var(--text-on-accent)` 토큰화. `src/styles/tokens.css`에 다크/라이트 variant 추가.

---

## §3. DoD

- **bundle CLI gzip 절대 ≤ 24500 B strict** (current 23605, +895 B 여유. soft AMBER 24000 B).
- TS strict 0 errors / lint baseline 30 유지 (v3.27 T2b 잔존).
- **vitest 양쪽 TZ regression 0** — baseline 1239 + 신규 spec **4건** (T2 count wiring 3 + T5 race 1) → **1243 KST/NY 동일**.
- smoke 98/98 + N=3 cold-start (T5 격상 자체).
- WCAG AA 4.5:1 contrast — active 카운트 색조 `rgba(255,255,255,0.85)` on primary 배경 검증.
- Codex 2-pass review 둘 다 dispatch (CLI 한도 시 controller self-review fallback — v3.14.5/v3.20/v3.24 precedent).
- T1 CSS / T2 wiring / T5 spec 강화 명시.
- 3종 검증 (`npx tsc --noEmit && npm run lint && npm test && npm run test:smoke`) 일괄 pass.

---

## §4. Risk

| # | Risk | Mitigation | Priority |
|---|---|---|---|
| R1 | T2 count wiring race — 비동기 데이터 load 전 chip 렌더되면 count 깜빡거림 | `counts === null` 시 카운트 span 비표시, 데이터 load 직후 1회 textContent 주입 (chip 재생성 X) | P1 |
| R2 | T1 segmented separator 1px이 모바일 360px에서 chip 너비 침범 → 라벨 wrap | `min-width: 0` + `flex: 1 1 0` + 360px breakpoint scroll fallback | P2 |
| R3 | T3 stats helper 분리 시 import path 다수 변경 → 회귀 | plan 단계에서 `grep -rn "from.*handlers/stats" src/` 전수 확인, 분리 대상 helper 명확화 | P1 |
| R4 | ~~T4 lazy handler init FOUC~~ → **격하** | C7 자체를 v3.31 carry로 이연 (listener mount-time 의존성, controller self-review 발견). 본 사이클 risk N/A. | — |
| R5 | Codex 사전 review CLI 한도 | controller self-review fallback (v3.14.5/v3.20/v3.24 precedent) — 본 사이클 dispatch는 T0 직후 의무 시도 | P3 |
| R6 | T1 CSS jsdom assertion 어려움 → 회귀 spec 부재 | T2 count wiring spec (jsdom 가능) + smoke 시각 회귀 보완 (`dynamic-tab-load.spec.ts` 등에서 entity row 가시성/active class 확인 추가) | P2 |
| R7 | T2 count 계산 시 entity별 count source-of-truth 분리 (`getAllAnswers().length` vs `getScraps().length` vs `getInsights().length`) → 데이터 변경 시 sync race | 단일 `getEntityCounts()` helper 추가 — `handlers/archive.ts` 내부 closure. caller-side는 helper만 호출. | P2 |

---

## §5. Codex 사전 Review

**T0 직후 mandatory dispatch 시도** (CLAUDE.md graduate v3.24 T1).

### 5.1 Dispatch 결과 (2026-05-12, design phase)

`codex:codex-rescue` subagent로 dispatch → **background로 빠짐 (5분 36초 후 결과 회수 불가)**. `SendMessage` 도구 미노출로 agent session 이어가기 차단.

**Controller self-review fallback** (v3.14.5/v3.20/v3.24/v3.29 precedent):

| # | Priority | Finding (controller 발견) | 흡수 위치 |
|---|---|---|---|
| 1 | P0 | T2 spec에 적은 이벤트 이름 `dg:archive:answerDeleted` 등은 가상 — 실재는 `dg:archive:updated` 단일 ([archive.ts:74](src/ui/handlers/archive.ts#L74)) | T2 detail 정정 완료 |
| 2 | P1 | T4 (C7) 단순 lazy mount는 cross-tab listener 누락 위험 — v3.29 T3 코멘트 명시 ("listener 등록 분리 리팩토링 동반 필요") | C7 v3.31 carry로 격하, §1.2/§2/§4 R4/§6 정정 완료 |
| 3 | P2 | T5 C3 N=3 격상 API 표기 부정확 (`test.repeats.skip()` X) — Playwright는 `test.describe.configure({ repeatEach: 3 })` | T5 detail 정정 완료 |
| 4 | P2 | T6 C4 `void switchTab().then()` 위치 미명시 | T6 plan 단계 grep 의무 추가 완료 |
| 5 | P3 | T2 count wiring race 명확화 부족 (1회 갱신 + 이벤트 hook 시점) | T2 detail에 `dg:archive:updated` hook 명시 + R7 single helper 추가 완료 |

### 5.2 Codex 최종 Review (T7 시점)

T7 검증 단계에서 `codex exec` 직접 호출로 재시도 (subagent background 회피). CLI 한도 시 controller self-review fallback.

---

## §6. 디스크립터

archive entity chip CSS 신설 (사용자 1차 신규 요구) + v3.29 carry 6건(C1/C2/C3/C4/C5/C6) 중형 cleanup 묶음. C7(controller self-review 격하)/C8(P3)/C9(별도 기능)/C10(별도 design)은 v3.31+ carry. T3 over-perform 17 KB 여유로 신규 UI 자연 흡수.

---

**작성**: 2026-05-12
**baseline commit**: `e6288ac`
**예상 task**: T0~T8 (9개)
**예상 commit**: 9~12 (T0/T7/T8 docs + main work + review fix 여유)
