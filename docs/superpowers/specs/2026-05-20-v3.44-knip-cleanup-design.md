# v3.44 — Knip Cleanup + Codex CLI stdin Graduate (Lightweight)

**Date**: 2026-05-20
**Type**: Lightweight cycle — Knip finding 9건 → 0건, devDep 2건 제거, CLAUDE.md wording 1건 추가. 신규 기능 0, schema migration 0, 코드 변경 0줄.
**Reference precedent**: v3.43 (lesson graduate + Knip 도입), v3.42 (light carry cleanup), v3.40 (lesson graduate batch).

---

## §0. 배경

### 0.1 사용자 결정 (2026-05-20)

v3.43 deploy 직후 controller 추천 후 사용자 "B 여기서 바로 진행" 명시. v3.43 carry P3 (Knip finding 9건) cleanup + v3.43 carry D (Codex CLI stdin redirect 의무 graduate) 묶음 lightweight cycle.

| 결정 항목 | 결정 | 근거 |
|---|---|---|
| v3.44 scope | ✅ Knip cleanup + Codex CLI stdin graduate | v3.43 carry P3 + D 묶음, S effort |
| prettier devDep | ✅ **제거** | .prettierrc 없음 + scripts 등록 안 됨, editor 자체 format으로 대체 가능 |
| http-server devDep | ✅ **제거** (controller 결정) | grep 결과 사용처 0, vite preview로 대체됨 |
| knip config 정밀화 | ✅ `ignoreExportsUsedInFile: true` + workspace entry `src/main.ts` 추가 | 7 unused exports false-positive 모두 해결 |
| Codex CLI stdin graduate | ✅ in-cycle 흡수 | v3.43 retro L4, XS effort |
| C1 Phase B | ❌ 제외 | 24h monitor 후 별도 (2026-05-21+) |
| C7 재진입 | ❌ 제외 | C1 Phase B 도입 후 묶기 |
| v3.17 §7.2 신규 기능 | ❌ 제외 | 별도 신규 기능 cycle 권장 |

### 0.2 T0 인벤토리 (실측, 2026-05-20)

#### 0.2.1 Knip finding 9건 분석 결과

**7 Unused exports — 모두 false-positive (same-file internal usage)**:

| # | Export | 파일 | Internal usage |
|---|---|---|---|
| 1 | `emitEvents` | `src/state/achievements.ts:173` | line 209 same-file call |
| 2 | `getKSTWindowStart` | `src/state/missionEngine.ts:33` | line 88, 106 same-file call |
| 3 | `ENTITY_LABELS` | `src/ui/components/archive-entity-chip.ts:10` | line 36 same-file reference |
| 4 | `mountHomeHandlers` | `src/ui/handlers/home.ts:249` | **`src/main.ts:26` 외부 호출 (knip entry 미인식)** + test 내 `@internal` |
| 5 | `hydrateHome` | `src/ui/handlers/home.ts:324` | line 313 same-file call |
| 6 | `buildLegacyInterestEmpty` | `src/ui/modals/stats-range-modal.ts:112` | line 181 same-file call |
| 7 | `TOAST_DURATIONS` | `src/ui/rewards.ts:22` | line 146, 161, 168, 177 same-file reference |

해결: knip.json `ignoreExportsUsedInFile: true` + workspace `.`.entry에 `src/main.ts` 추가.

**2 Unused devDependencies**:

| # | devDep | grep 결과 | 결정 |
|---|---|---|---|
| 1 | `http-server` (package.json:23) | scripts/.github/code 0 사용 | **제거** (controller 결정) |
| 2 | `prettier` (package.json:26) | .prettierrc 없음, scripts 없음 | **제거** (사용자 confirm) |

#### 0.2.2 v3.43 carry D — Codex CLI stdin redirect 의무

**현황**: v3.43 retro §신규 lessons L4 — `codex exec "$PROMPT"` foreground 호출 시 `< /dev/null` redirect 안 하면 "Reading additional input from stdin..." 메시지로 stall. background dispatch도 동일 영향. v3.43 T3 1차 시도가 background로 stall, 2차 시도가 `< /dev/null` foreground로 성공.

**Graduate target**: `CLAUDE.md "v3.31 L1 graduation — Codex CLI direct dispatch"` 섹션에 wording 한 줄 추가.

#### 0.2.3 현재 baseline

- vitest 양쪽 TZ: 1546/1546
- functions vitest: 24/24
- smoke: 109/109
- TS strict: 0 / lint: 0
- Knip finding: 9 (7 unused exports + 2 unused devDeps)
- Bundle: index 9,379 / archive 8,968 / appCheck 13,215 / translateToast 2,136

### 0.3 비목표 (Non-goals)

- **C1 Phase B** (24h monitor 후 별도 사이클 — 최소 2026-05-21)
- **C7 재진입** (C1 Phase B 도입 후 묶기)
- **C4/C5 carry** (L effort, 별도 사이클)
- **v3.17 §7.2 신규 기능** (식물 detail modal 등, 신규 기능 cycle)
- 신규 schema migration / production 코드 신규 작성
- prettier 재도입 (사용자 결정으로 명시 제거)

---

## §1. T1 — knip.json Config 정밀화

### 1.1 Finding 요약

7 unused exports false-positive 원인:

- knip default가 "named export는 외부 import 없으면 unused"라고 잡음
- 7건 중 6건은 same-file internal usage (`emitEvents` line 173↔209 등)
- 1건 (`mountHomeHandlers`)은 `src/main.ts:26` 외부 호출이지만 knip이 main.ts entry를 못 인식

### 1.2 결정 — config 정밀화

- **`ignoreExportsUsedInFile: true`** 추가: same-file usage를 unused로 잡지 않음
- **workspace `.`.entry에 `src/main.ts` 추가**: index.html → main.ts vite chain 명시
- 기존 ignore 패턴 유지

### 1.3 변경 범위

```json
{
  "$schema": "https://unpkg.com/knip@latest/schema.json",
  "ignore": [
    "tests/helpers/**",
    "tests/smoke/_helpers/**",
    "functions/lib/**"
  ],
  "ignoreBinaries": ["firebase"],
  "ignoreExportsUsedInFile": true,
  "workspaces": {
    ".": {
      "entry": ["src/main.ts", "public/sw.js"]
    },
    "functions": {
      "entry": ["src/index.ts", "src/sendAnswerDm.ts", "vitest.config.ts"]
    }
  }
}
```

### 1.4 Risk

| Risk | Severity | Mitigation |
|---|---|---|
| `ignoreExportsUsedInFile: true`가 진짜 unused same-file export까지 가림 | M | grep + Codex 최종에서 catch. T2 cleanup 시점에 finding count로 검증 |
| `src/main.ts` entry 명시가 다른 entry 자동 인식 망가뜨림 | L | knip dry-run으로 sanity check |

---

## §2. T2 — devDep 제거 + Codex CLI stdin Graduate

### 2.1 변경 범위

**package.json**:
- `devDependencies`에서 `"http-server": "^14.1.1"` 제거
- `devDependencies`에서 `"prettier": "^3.2.0"` 제거
- `npm install` 재실행 → `package-lock.json` 갱신

**CLAUDE.md** (v3.43 carry D 흡수):
- "v3.31 L1 graduation — Codex CLI direct dispatch" 섹션 끝에 한 줄 추가:
  - "**stdin redirect 의무 (v3.44 graduate, v3.43 L4 ROI)**: `codex exec` foreground 호출 시 `< /dev/null` redirect 명시. 미명시면 "Reading additional input from stdin..." stall로 timeout 발생 (v3.43 T3 1차 시도 사례)."

### 2.2 Risk

| Risk | Severity | Mitigation |
|---|---|---|
| http-server 또는 prettier가 외부 도구 (editor extension, CI)에서 transitive 의존 | L | 사용자 production 검증 — editor format 동작 확인 |
| CLAUDE.md wording이 다른 cycle 충돌 | L | 기존 "v3.31 L1" 섹션 끝에 한 줄 추가 (separate sub-section 아님) |

---

## §3. T3 — Verify + Codex 최종 + Bundle

### 3.1 검증 명령

```bash
npx tsc --noEmit                                          # 0 errors
npm run lint                                              # 0 errors
TZ=Asia/Seoul npm test                                    # 1546/1546
TZ=America/New_York npm test                              # 1546/1546
cd functions && npm test                                  # 24/24
npm run test:smoke                                        # 109/109 cold-start N=3
npm run knip                                              # finding 0~2건 (expected)
```

Docs/config-only이므로 vitest/functions/smoke 모두 baseline 유지 예상.

### 3.2 Bundle Quad Record (v3.43 → v3.44)

| Chunk | v3.43 | v3.44 예상 | Δ | DoD |
|---|---:|---:|---:|---|
| index | 9,379 | 9,379 | 0 | ±50 B |
| archive | 8,968 | 8,968 | 0 | ±50 B |
| appCheck | 13,215 | 13,215 | 0 | ±50 B |
| translateToast | 2,136 | 2,136 | 0 | ±50 B |

devDep 제거 + config 정밀화는 dist 영향 0.

### 3.3 Codex 사전 review (T0 직후, small cycle optional)

- 1차: direct CLI dispatch + **stdin redirect `< /dev/null` 의무** (v3.43 L4 적용)
  - `codex exec --skip-git-repo-check -o /tmp/v3.44-codex-pre.txt "<prompt>" < /dev/null`
- 한도 시 fallback: controller `pr-review-toolkit:code-reviewer` (small cycle exception, v3.43 graduate)

### 3.4 Codex 최종 review

- direct CLI dispatch + `< /dev/null`:
  - `codex exec --skip-git-repo-check -o /tmp/v3.44-codex-final.txt "<prompt>" < /dev/null`
- 분류: DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT
- 한도 시 fallback: `pr-review-toolkit:code-reviewer`

---

## §4. T4 — Retro + 배포 + Obsidian Sync

### 4.1 Retro 작성

`/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v344.md` 신규:

- §1 Commit summary (v3.43 → v3.44)
- §2 Codex 2-pass 결과
- §3 검증 결과
- §4 Bundle quad record (실측)
- §5 신규 lessons (있다면)
- §6 v3.45 carry-forward (C1 Phase B HIGH / C7 재진입 / v3.17 §7.2 / 기타)
- §7 사용자 결정 사항

### 4.2 배포

사용자 "배포해줘" 명시 시점에:

- `npm run build`
- `firebase deploy --only hosting`
- Codex 최종 APPROVED 후만

### 4.3 Obsidian Sync

- `02-Cycles/2026-05-20-v3.44-knip-cleanup-design.md`
- `03-Lessons/project_daily_growth_v344.md` + MEMORY.md
- `01-Workflow/CLAUDE.md` (v3.44 stdin redirect wording 반영)
- `00-Index.md` v3.44 한 줄 — 사용자 편집 (controller readonly 권고)

---

## §5. Risk Heatmap

| 영역 | Severity | Likelihood | Mitigation |
|---|---|---|---|
| T1 `ignoreExportsUsedInFile: true`가 진짜 unused export 가림 | M | Mid | grep + Codex 최종 catch |
| T2 prettier/http-server 외부 사용 발견 | L | Low | 사용자 production 검증 (editor format) |
| Codex CLI stdin stall (v3.43 재발) | L | Low | `< /dev/null` 명시 (v3.43 L4 적용) |
| 최종 Bundle drift | L | Low | docs/config only이라 ±50 B 이내 예상 |

---

## §6. DoD (Verification Matrix)

| 항목 | 기준 | Task |
|---|---|---|
| knip.json 정밀화 | `ignoreExportsUsedInFile: true` + `src/main.ts` entry 추가 | T1 |
| Knip finding | 9건 → 0~2건 (false-positive 모두 해소) | T1 |
| devDep 제거 | http-server + prettier 둘 다 package.json에서 제거 | T2 |
| CLAUDE.md stdin redirect wording | "v3.31 L1 graduation" 섹션 끝에 한 줄 추가 | T2 |
| TS strict | 0 errors | T3 |
| Lint | 0 errors | T3 |
| Vitest 양쪽 TZ | 1546/1546 each | T3 |
| Functions vitest | 24/24 | T3 |
| Smoke | 109/109 cold-start N=3 | T3 |
| Bundle | quad record ±50 B 이내 | T3 |
| Codex 최종 | APPROVED or APPROVED_WITH_NOTES | T3 |
| Retro + Obsidian sync | memory 신규 + vault sync | T4 |

---

## §7. v3.45 Carry-Forward 후보

| ID | 항목 | 출처 | 우선순위 |
|---|---|---|---|
| C1 | T1 Phase B backend `enforceAppCheck` + 202 + UX copy | v3.41 carry | **HIGH** (2026-05-21+ 24h monitor 완료 후) |
| C7 | Firebase API key GCP referrer 절차 + 검증 script | v3.43 v2 강등 | M (C1 묶음 권장) |
| C4 | First briefing storage thrash | v3.41 carry | L |
| C5 | state↔ui layer cycle 정리 | v3.41 carry | L (별도 사이클) |
| v3.17 §7.2 | 식물 detail modal / Streak Freeze 등 | v3.17 retro | M (신규 기능 cycle) |
| Codex CLI retroactive | v3.43 사전 review direct CLI 재시도 | CLAUDE.md "fallback 정책 6번" | L (권고) |

---

## §8. 사용자 결정 사항 기록

- ✅ v3.44 scope 채택: Knip cleanup + Codex CLI stdin graduate 묶음 (v3.43 carry P3 + D)
- ✅ prettier devDep **제거** (사용자 명시) — .prettierrc 없음, scripts 등록 안 됨, editor 자체 format으로 대체
- ✅ http-server devDep **제거** (controller 결정) — grep 결과 사용처 0, vite preview로 대체
- ✅ knip config 정밀화 (`ignoreExportsUsedInFile: true` + workspace entry `src/main.ts`) — controller 결정
- ✅ Codex CLI stdin redirect graduate in-cycle 흡수 (CLAUDE.md 한 줄 추가) — controller 결정
- ❌ C1 Phase B (24h monitor 후 별도, 2026-05-21+)
- ❌ C7 재진입 (C1 Phase B 도입 후 묶기)
