# v3.39 — Answer/Briefing `interestId` Schema Migration

> **Type**: Schema migration + write-time classification + archive exact filter. 단독 사이클 (v3.38 §9.6 C1 carry, v3.36 brainstorming deferred). Schema v8→v9 lazy. CSP/migration storage rewrite 0줄.

**Cycle size**: L (8~12 task 예상)
**Reference precedent**: v3.25 T1~T9 (Insight.interestId schema v6→v7 도입 — chain superset / write-time Gemini 분류 / archive exact filter 패턴 완비)

---

## §0. 배경

v3.38 retro §9.6 carry-forward에서 C1을 v3.39 단독 사이클로 명시:

> | C1 | Answer/Scrap interestId schema migration (v3.39 단독) | v3.36 brainstorming | High (v3.39 1st) |

v3.36 brainstorming 시점 사이클 크기 L로 평가되어 별도 사이클로 deferred. v3.25 Insight 도입이 검증된 reference이므로 본 사이클은 동일 패턴을 Answer + Briefing(=Scrap) 두 entity에 미러링한다.

### 0.1 진단된 latent issue (v3.39가 동시에 해결)

`src/utils/statsAggregate.ts:67-82` 의 `byInterest` aggregate가 **`Answer.type` (질문 유형: 분석/전환/실무/성장/트렌드) 을 분야별 통계로 잘못 사용 중**. v3.24 T6에서 type을 "정밀화"했으나 이는 사용자 관심분야와 별개 축이다. v3.39 schema migration이 `byInterest` 의미를 진짜 관심분야로 바로잡는다 (R5 UX impact 명시 의무).

### 0.2 비목표 (Non-goals)

- archive UI 재구성 (v3.27 T1에서 완료된 entity chip 구조 그대로 사용).
- bundle trim (v3.40 dedicated carry — v3.28 lesson "main+trim 묶음 ROI 한계").
- archive-nav-chip 공용 helper 추출 (3 callsite 트리거 미달 — v3.40 carry).
- 안내 toast UX (R5 mitigation — P2 분리 가능, v3.40 carry).

---

## §1. Goals & Success Criteria

### G1. Schema v8→v9 lazy migration
- `Answer.interestId: string` 필수 (legacy → `'unknown'` 백필).
- `Briefing.interestId: string` 필수 (legacy → `'unknown'` 백필).
- `User.schemaVersion: 9` (Answer/Briefing은 별도 storage이므로 User 자체 mutation 최소).
- Chain superset 보장: V2~V8 + v9 early-return guard 7 + 1 = 8 spot.

### G2. Write-time interestId 채움 (신규 데이터)
- **Daily question 답변** ([home.ts:900 submitAnswer](src/ui/handlers/home.ts#L900)): `generateQuestion` 응답에 `targetInterestId` 추가 → question DOM `data-interest-id` → `makeAnswer({ interestId })`. Gemini 추가 호출 0.
- **chat-summary 답변** ([home.ts:1046 handleSummarizeChat](src/ui/handlers/home.ts#L1046)): `loadChatHistory(today)` user 메시지들을 `matchKeyword(text, interestId)` user.interests 순서로 첫 match → interestId. Gemini 추가 호출 0.
- **Briefing scrape** ([home.ts:629 loadDailyBriefings](src/ui/handlers/home.ts#L629)): title/summary `matchKeyword` 1순위 + `feedToInterests(feedUrl, user.interests)[0]` 2순위 + `'unknown'` 3순위. Gemini 추가 호출 0.

### G3. Archive read-side exact filter
- `statsAggregate.byInterest` — `a.type` → `a.interestId` 교체 (의미 정정).
- archive entity filter — `e.interestId === selectedId` exact 우선 + legacy 'unknown' fallback keyword.
- entity-summary-chip count — `interestId` exact count.

### Success Criteria (DoD)
| 항목 | 기준 |
|---|---|
| Vitest | +50~80 신규 (migration 7 + Answer 8 + Briefing 8 + stats 6 + filter 4 + integration ~5). 양쪽 TZ PASS. |
| Smoke | baseline 97 유지. 영향 spec 추가 0~+2. cold-start fresh fork PASS. |
| Bundle (canonical index) | +200~400 B 예상 (소프트 AMBER zone — accept). +500 B over는 P0 stop. |
| Bundle (archive chunk) | +100~300 B 예상 (filter 분기 추가). |
| Codex 사전 | REQUIRES_FIX 흡수 후 plan v2. P0 모두 in-cycle. |
| Codex 최종 | DEPLOY_APPROVED 또는 APPROVED_WITH_NOTES (P0=0, P1 all in-cycle). |

---

## §2. Architecture

### §2.1 Schema 변경

**File**: `src/state/schema.ts`

```ts
export interface Answer extends Versioned {
  ...
  type?: string;                   // 기존: 질문 유형 (분석/전환/실무/성장/트렌드) — 의미 보존
  interestId: string;              // 🆕 v3.39 — INTERESTS.id 또는 'unknown' (validateInterestId 통과)
  pinned: boolean;
}

export function makeAnswer(input: Omit<Answer, 'schemaVersion' | 'createdAt' | 'pinned' | 'interestId'>
  & { pinned?: boolean; interestId?: string }): Answer {
  // 기본값 'unknown' (caller가 명시하지 않은 chat-summary edge 대비)
  return {
    pinned: false,
    interestId: 'unknown',
    ...input,
    schemaVersion: CURRENT_SCHEMA_VERSION,
    createdAt: new Date().toISOString(),
  };
}
```

**File**: `src/state/briefings.ts`

```ts
export interface Briefing {
  ...
  interestId: string;              // 🆕 v3.39 — INTERESTS.id 또는 'unknown'
  pinned: boolean;
}
```

**File**: `src/state/user.ts`

```ts
schemaVersion: 9;                  // 8→9 (Answer.interestId + Briefing.interestId)
```

### §2.2 Migration (lazy v8→v9)

**File**: `src/state/migration.ts`

```ts
/**
 * v3.39 T1: schema v8 → v9 — Answer/Briefing interestId 도입 (User storage는 schemaVersion만 lift).
 * - Answer/Briefing은 별도 storage (loadAnswers/loadBriefings boundary에서 normalize) → User 객체 자체엔 ripple 0.
 * - chain superset (v3.25 T2 패턴): V2~V8 모든 early-return이 v9 user에 same-reference pass-through.
 */
export function migrateUserToV9(u: unknown): User {
  const r = u as Record<string, unknown> & { schemaVersion?: number };

  if (r.schemaVersion === 9) {
    return r as unknown as User; // idempotent
  }

  // v8까지 lift
  const v8 = migrateUserToV8(u);
  return { ...(v8 as unknown as Record<string, unknown>), schemaVersion: 9 } as unknown as User;
}
```

**Answer migrate** — `loadAnswers` boundary에 정규화 1줄 추가:
```ts
// src/state/persistence.ts loadAnswers
function migrateAnswer(raw: unknown): Answer {
  const r = (raw ?? {}) as Record<string, unknown>;
  return {
    ...r,
    interestId: typeof r.interestId === 'string' ? r.interestId : 'unknown',
    pinned: typeof r.pinned === 'boolean' ? r.pinned : false,
    schemaVersion: CURRENT_SCHEMA_VERSION,
  } as Answer;
}
```

**Briefing migrate** — `loadBriefings` map 단계 확장 (이미 v3.38 T4에서 memo 백필 패턴 있음):
```ts
// src/state/briefings.ts loadBriefings
const normalized: Briefing = {
  ...briefing,
  pinned: typeof briefing.pinned === 'boolean' ? briefing.pinned : false,
  memo: typeof briefing.memo === 'string' ? briefing.memo : '',
  interestId: typeof briefing.interestId === 'string' ? briefing.interestId : 'unknown',  // 🆕 v3.39
};
```

### §2.3 Validation (entry-level guard)

- `validateInterestId(id: string): string` (`src/state/user.ts:64` 기존 helper) **재사용**. INTERESTS.find(i => i.id === id) 통과 시 그대로, 아니면 `'unknown'`.
- `normalizeAnswerInterestIds(list: Answer[]): void` 신규 — `loadAnswers` 후 1회 정규화. v3.26 T2 `normalizeInsightInterestIds` 패턴 미러.
- `normalizeBriefingInterestIds(list: Briefing[]): void` 신규.
- 둘 다 invalid string (INTERESTS에 없는 id) → `'unknown'` 정정 + `console.warn` 출력.

---

## §3. Components (write-time 채움 경로)

### §3.1 Answer write-time

**§3.1.1 daily question 답변 — `generateQuestion` 응답 확장**

`src/services/gemini.ts` `QuestionOut` 인터페이스 확장:
```ts
export interface QuestionOut {
  type: string;
  question: string;
  hint: string;
  targetInterestId: string;  // 🆕 v3.39 — Gemini가 분류한 분야 (parse fail → user.interests[0])
}
```

Prompt 한 줄 추가 ("응답 첫 줄에 `interestId: <id>` 표기. 사용자 분야 id 중 하나: ..."), `generateQuestion` 응답 parse에 `parseQuestionResponse`(신규 또는 기존 parse 확장).

`renderQuestion` ([home.ts:788](src/ui/handlers/home.ts#L788)) — DOM 노출:
```ts
qTextEl.dataset['interestId'] = q.targetInterestId;
```

`submitAnswer` ([home.ts:900](src/ui/handlers/home.ts#L900)) — `makeAnswer` 호출에 interestId 추가:
```ts
const interestId = qTextEl?.dataset['interestId'] ?? 'unknown';
const answer = makeAnswer({ id, questionId, text, questionText, authorId, type, date, interestId });
```

**폴백 chain**:
1. `generateQuestion` 성공 + `targetInterestId` valid → 사용.
2. parse fail → `user.interests[0] ?? 'unknown'`.
3. fallbackQuestion path ([home.ts:788](src/ui/handlers/home.ts#L788)) — `user.interests[0] ?? 'unknown'`.

**§3.1.2 chat-summary 답변 — `handleSummarizeChat`**

`handleSummarizeChat` ([home.ts:1046](src/ui/handlers/home.ts#L1046)) onPrimary 콜백 내부에서 interestId 결정 (콜백 안에 이미 `getCachedUser()` 호출이 있는 것은 아니므로 outer scope에서 캡처):

```ts
// handleSummarizeChat outer scope (try 진입 직후, history 확보 후)
const u = getCachedUser();
const userMsgs = history.filter(m => m.role === 'user').map(m => m.text);
const userInterests = u?.interests ?? [];
let inferredInterestId = 'unknown';
for (const id of userInterests) {
  if (userMsgs.some(t => matchKeyword(t, id))) {
    inferredInterestId = id;
    break;
  }
}
// onPrimary 콜백은 closure로 inferredInterestId 캡처
renderChatPreviewBubble({
  ...
  onPrimary: () => {
    const answer = makeAnswer({
      id: crypto.randomUUID(), questionId: 'chat-summary', text: trimmed, authorId: 'self',
      interestId: inferredInterestId,
    });
    appendAnswer(answer);
    ...
  },
});
```

Gemini 추가 호출 0. `matchKeyword`은 v3.16부터 검증된 word-boundary helper. user.interests 0건 (legacy onboarding 미완) → `'unknown'`.

### §3.2 Briefing write-time

`loadDailyBriefings` ([home.ts:629](src/ui/handlers/home.ts#L629)) — feed 메타 보존 + interestId 결정:

**B.2.1 `feedToInterests` 신규 helper** (`src/ui/handlers/home.ts`):
```ts
/** interestToFeeds 역매핑. 1 feed → N interests (user.interests 우선순위 순). */
export function feedToInterests(feedUrl: string, userInterests: string[]): string[] {
  return userInterests.filter(id => interestToFeeds(id).includes(feedUrl));
}
```

**B.2.2 storage 단계 interestId 결정**:
```ts
const stored: Briefing[] = chosen.map(({ item, sourceTitle, feedUrl }, i) => {
  // 1순위: title/summary keyword match (user.interests 순)
  let interestId = 'unknown';
  for (const id of user.interests) {
    if (matchKeyword(item.title, id) || matchKeyword(stripTags(item.description), id)) {
      interestId = id;
      break;
    }
  }
  // 2순위: feed origin (user.interests 우선순위 따른 첫 매핑)
  if (interestId === 'unknown') {
    const origins = feedToInterests(feedUrl, user.interests);
    if (origins.length > 0) interestId = origins[0];
  }
  return {
    id: `b_${Date.now()}_${i}`,
    ...
    interestId,
    pinned: false,
    ...
  };
});
```

**B.2.3 pickBriefings 시그니처 확장**: 현재 `pickBriefings(filtered, 5)` 결과는 `{ item, sourceTitle }`만 노출. `feedUrl` 추가 expose 필요 — `pickBriefings` 시그니처 또는 `filtered` map 단계에서 feedUrl 보존. T0 plan에서 actual signature 확인 후 정정.

### §3.3 Archive read-side

**§3.3.1 stats `byInterest` 의미 정정** (`src/utils/statsAggregate.ts:67-82`):
```diff
- const raw = a.type;
- const id = typeof raw === 'string' && raw.trim().length > 0 ? raw : 'unknown';
+ const id = a.interestId ?? 'unknown';
```

기존 `'unknown' bucket 제외` 정책 유지.

**§3.3.2 archive entity filter** — handlers/archive.ts 분기 추가:
```ts
// 기존: keyword fuzzy match
// 신규: interestId === selectedId 우선, fallback keyword
const matches = entities.filter(e => {
  if (selectedId === 'all') return true;
  if ('interestId' in e && e.interestId === selectedId) return true;
  // legacy fallback for migrate 직후 'unknown' 항목
  return matchKeyword(getEntityText(e), selectedId);
});
```

**§3.3.3 entity-summary-chip count** — 동일 분기 적용. 현재 v3.38 T7a~T7b에서 chip 구조 완비됨, count 계산만 정정.

---

## §4. 영향받는 파일 (예상)

| 분류 | 파일 | 변경 |
|---|---|---|
| Schema | `src/state/schema.ts` | Answer.interestId + makeAnswer 시그니처 |
| Schema | `src/state/briefings.ts` | Briefing.interestId + loadBriefings normalize |
| Schema | `src/state/user.ts` | schemaVersion 9 + chain |
| Migration | `src/state/migration.ts` | migrateUserToV9 + chain superset 8 spot |
| Migration | `src/state/persistence.ts` | migrateAnswer interestId 백필 |
| Validation | `src/state/user.ts` | normalize*InterestIds 2 helper 신규 |
| Service | `src/services/gemini.ts` | QuestionOut.targetInterestId + prompt |
| Service | `src/utils/gemini-parse.ts` | parseQuestionResponse 또는 generateQuestion parse 확장 |
| UI write | `src/ui/handlers/home.ts` | submitAnswer + handleSummarizeChat + loadDailyBriefings + feedToInterests |
| Aggregate | `src/utils/statsAggregate.ts` | byInterest 의미 정정 |
| Filter | `src/ui/handlers/archive.ts` | exact filter 분기 |
| Filter | `src/ui/components/archive-search-summary.ts` | count 정정 |
| Filter | `src/ui/components/archive-entity-chip.ts` | count 정정 |
| 신규 spec | `tests/state/migrationV9.spec.ts` | chain superset 7 + idempotency 2 |
| 신규 spec | `tests/state/persistence.spec.ts` (확장) | migrateAnswer interestId backfill |
| 신규 spec | `tests/state/briefings.spec.ts` (확장) | loadBriefings interestId backfill |
| 신규 spec | `tests/ui/handlers/home.spec.ts` (확장) | submitAnswer + chat-summary + Briefing interestId 결정 |
| 신규 spec | `tests/utils/statsAggregate.spec.ts` (확장) | byInterest interestId 분기 |
| 신규 spec | `tests/services/gemini.spec.ts` (확장) | generateQuestion targetInterestId parse + fallback |

총 ~16 file. Schema/Migration 5 + Service 2 + UI 5 + spec 6.

---

## §5. Risks

### §5.1 P0 후보 (사전 Codex 우선 catch 대상)

| ID | Risk | Mitigation |
|---|---|---|
| R1 | v9 migration chain superset (V2~V8 + v9 early-return) 누락 | `migrateUserToV2~V8` 모두 v9 user에 same-reference pass-through 확인. `migrationV9-chain.spec.ts` 7 case (v0~v8 시작점 × v9 종점 chain pass-through). |
| R2 | Answer/Briefing migrate가 in-place mutation (idempotency 위반) | v3.27 T1 패턴 — copy-based map + already-v9 idempotent path. spec 2 case (idempotent + invariant restore). |
| R3 | `generateQuestion` Gemini 응답 parse 실패 시 silent corruption | 폴백 chain: parse fail → `user.interests[0]` → `'unknown'`. spec 5 case (success / parse fail / empty response / invalid id / fallbackQuestion path). |
| R4 | Briefing keyword match 시 매칭 0건 발생 → 모두 'unknown' 흘림 | `feedToInterests` 2순위 폴백이 결정성 보장. lint rule: 모든 feed가 user.interests 중 1개는 매핑. spec 3 case. |
| R5 | stats `byInterest` semantic 변경 → 사용자 혼란 (latent issue fix) | retro §UX impact 명시 + 첫 진입 안내 toast P2 carry (v3.40). 본 사이클은 정정만 진행. |

### §5.2 P1 후보

| ID | Risk | Mitigation |
|---|---|---|
| R6 | bundle 영향 (canonical AMBER zone) | +200~400 B 목표. trim은 v3.40 분리 (v3.28 lesson). |
| R7 | Insight `'unknown'` 정책과 정합성 | Answer/Briefing도 동일 `validateInterestId` 공유. normalize 3 entity 모두 동일 API. |
| R8 | smoke 영향 — entity filter 동작 변경 | spec 회귀: exact match → fallback keyword 둘 다 통과 case 추가. |
| R9 | `pickBriefings` 시그니처 변경 ripple | T0 plan에서 actual signature 확인 후 정정. spec 영향 measure. |

### §5.3 P2 후보 (v3.40 carry)

| ID | 항목 | 트리거 |
|---|---|---|
| P2-1 | 사용자 안내 toast ("v3.39부터 분야별 통계 정확해짐") | R5 mitigation |
| P2-2 | archive-nav-chip 공용 helper 추출 | v3.38 T6 carry, 3 callsite 도달 시 |
| P2-3 | bundle trim dedicated | R6 over by +500 B 시 |
| P2-4 | NFD 자모 분리 검출 rule | v3.38 §2.1.1 carry |
| P2-5 | fuzzy threshold A/B UI | v3.38 §2.2.2 carry |

---

## §6. Implementation Order (proposed T1~T8)

| Task | 내용 | 추정 commits |
|---|---|---|
| T0 | spec + plan v1 + Codex 사전 review + plan v2 in-cycle 흡수 | 1~2 |
| T1 | schema v8→v9 migration + chain superset + idempotency spec | 1~2 |
| T2 | Answer/Briefing migrate + normalize helper + boundary spec | 1 |
| T3 | `generateQuestion` targetInterestId + parse + dataset wiring | 1 |
| T4 | `submitAnswer` + chat-summary interestId 결정 + spec | 1 |
| T5 | `feedToInterests` helper + Briefing storage interestId 결정 + spec | 1 |
| T6 | stats `byInterest` 의미 정정 + entity filter exact match + chip count | 1 |
| T7 | smoke 회귀 + bundle 측정 + per-task review 흡수 | 1 |
| T8 | Codex 최종 review + retro (배포 직전) | 1 |

총 ~8~10 commits.

---

## §7. Verification

### §7.1 Cycle workflow 3종 (각 main task 완료 시)
- `npx tsc --noEmit` → 0 errors
- `npm run lint` → 0 errors
- `npm test -- <영향 범위>` → PASS, 신규 spec N=5 5/5

### §7.2 Bundle 측정 (v3.14.5 graduation, v3.35 dual record)
- Canonical: `gzip -c dist/assets/index-*.js | wc -c` (deploy gate)
- Archive chunk: `gzip -c dist/assets/archive-*.js | wc -c` (archive 영역 변경 시 dual record)
- T7 wrap-up retro §Bundle 측정에 두 값 모두 기록.

### §7.3 Smoke (사이클 끝)
- `npm run lint && npm test && npm run test:smoke` 3종 일괄.
- cold-start fresh fork 1회.

### §7.4 Codex 2-pass
- 사전 (T0): direct CLI `codex exec --skip-git-repo-check -o /tmp/v3.39-codex-pre.txt`. REQUIRES_FIX → plan v2 in-cycle 흡수.
- 최종 (T8): direct CLI 동일. DEPLOY_APPROVED 또는 APPROVED_WITH_NOTES.

---

## §8. 이후 사이클 후보 (v3.40+)

| ID | 항목 | 출처 | 우선순위 |
|---|---|---|---|
| F1 | bundle trim dedicated (canonical + archive) | v3.38 §9.6 C6 + v3.39 R6 | High |
| F2 | archive-nav-chip 공용 helper 추출 | v3.38 §9.6 C7 (3 callsite 트리거 도달 시) | Medium |
| F3 | 사용자 안내 toast ("v3.39 분야별 통계 정확") | v3.39 §5.3 P2-1 | Medium |
| F4 | NFD 자모 분리 검출 rule | v3.38 §9.6 C3 | Low |
| F5 | fuzzy threshold A/B UI | v3.38 §9.6 C4 | Low |
| F6 | plant-detail seed fixture 추출 | v3.38 §9.6 C2 (4th smoke 트리거) | Low |
| F7 | entity-summary-chip 색상 토큰 | v3.38 §9.6 C5 | Low |

---

## §9. Retro (작성 예정 — T8)

T8 완료 시 사이클 결과 / Codex 2-pass 분류 / Bundle 측정 / 신규 lessons / v3.40+ carry-forward 기록.

---

**작성일**: 2026-05-19
**작성자**: Claude Opus 4.7 + Hayden 합의 (§A/§B/§C 3-stage approval)
**상태**: Draft v1 — Codex 사전 review 대기
