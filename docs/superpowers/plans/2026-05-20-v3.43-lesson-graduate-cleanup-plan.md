# v3.43 Lesson Graduate Batch + GCP Referrer 절차 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v3.41 §11.5 + v3.42 §7.5 누적 9 lesson을 CLAUDE.md / COMMON_MISTAKES.md / LEARNINGS_INDEX.md로 정착하고, Knip dead-code 도구를 main task로 도입 (T2). C7 GCP referrer는 v2 사전 review로 v3.44 carry 강등.

**Architecture:** Lightweight cycle — 코드 변경 최소 (config 1개). T1 docs only (CLAUDE.md / COMMON_MISTAKES / LEARNINGS_INDEX 갱신), T2 main Knip dead-code 도구 (`knip.json` + `package.json` devDep), T3 verify + Codex 최종 + Bundle, T4 retro + 배포 + Obsidian sync. 신규 schema migration 0, production 코드 0줄.

**Tech Stack:** Markdown docs (`.claude/`, `docs/`), Node 22+ native fetch (`scripts/`), Knip (optional devDep).

**Spec reference:** [docs/superpowers/specs/2026-05-20-v3.43-lesson-graduate-cleanup-design.md](../specs/2026-05-20-v3.43-lesson-graduate-cleanup-design.md)

---

## File Structure (v2 — Option B 채택)

**Created (T2 — Knip main 승격)**:

- `knip.json` — entry-based dead-code 분석 config

**Modified (T1)**:

- `CLAUDE.md` — 신규 ## "보안 기능 도입 default 패턴" (Phase A/B wording 추상화), "Bundle 측정 표준" 확장, "Codex 2-pass Review" small cycle exception
- `.claude/COMMON_MISTAKES.md` — §13~§18 6 신규 lesson (§3↔§15 / §14↔§16 cross-link)
- `.claude/LEARNINGS_INDEX.md` — Last Updated + Recent Graduates + CLAUDE.md Policy Updates 섹션 신설

**Modified (T2 — Knip main)**:

- `package.json` — devDependencies + `npm run knip` script

**~~Removed (v2 사전 review P0)~~**: ~~`docs/security/2026-05-20-firebase-api-key-referrer.md` + `scripts/verify-referrer.mjs`~~ → v3.44 carry로 강등

---

## Task 0: 사전 Codex Review (완료 — v2 결과 기록)

**Files:**
- Output: `/tmp/v3.43-codex-pre.txt` (39 B, killed 직후 보존 — 부분)
- Fallback review: controller `pr-review-toolkit:code-reviewer` subagent (in-conversation)

- [x] **Step 1: Codex 사전 review CLI 1차 시도** — `codex exec` background dispatch (`boghh91x1`) — **killed** (process 중단)
- [x] **Step 2: Fallback 진입** — `pr-review-toolkit:code-reviewer` subagent dispatch (CLAUDE.md "fallback 단계 정책 2번")
- [x] **Step 3: 사용자 즉시 알림** — controller 메시지로 fallback 진입 + REQUIRES_FIX 결과 통지 (정책 3번)
- [x] **Step 4: 결과 분류** — **REQUIRES_FIX**
  - **P0 ×2**: 영역 4 endpoint mismatch (verify script가 prove하는 Identity Toolkit endpoint를 production이 미사용) + 영역 7 abort 분기 발동
  - **P1 ×10+**: LEARNINGS_INDEX row / CLAUDE.md Phase A·B wording 추상화 / COMMON_MISTAKES §3↔§15 + §14↔§16 cross-link / localhost wildcard caveat / rollback propagation 5분 / verify status code fallback / multi-allow-referer / Knip abort 우선순위 / Codex CLI heredoc placeholder / v3.42 retro count 정정
  - **P2 / DoD / QUESTION ×다수**
- [x] **Step 5: 사용자 confirm — Option B 채택** (T2 C7 → v3.44 carry 강등, T3 Knip → T2 main 승격)
- [x] **Step 6: spec v2 + plan v2 in-cycle 흡수** + commit
- [x] **Step 7: Deploy gate 정합** — CLAUDE.md "fallback 정책 4번" — fallback review가 사전 review gate 충족 인정 (small cycle exception, retro §verification에 시도 결과 명시 의무)
- [ ] **Step 8: Codex CLI retroactive 재시도** — 권고/carry (필수 X). v3.44 시작 시점에 시도 권장.

---

## Task 1: Lesson Graduate Batch (9건 + P2-2)

**Files:**
- Modify: `CLAUDE.md` (3 위치 — 신규 ## section, Bundle 측정 표준 확장, Codex 2-pass wording)
- Modify: `.claude/COMMON_MISTAKES.md` (§13~§18 신규)
- Modify: `.claude/LEARNINGS_INDEX.md` (Last Updated + Learning Topics 갱신)

### 1.1 CLAUDE.md 신규 ## "보안 기능 도입 default 패턴"

- [ ] **Step 1: 신규 ## section 추가 (Single Fix Point Preference 다음, Documentation Navigation 이전)**

`CLAUDE.md`에서 `### Single Fix Point Preference (v3.31 L5)` 섹션 직후 + `## Documentation Navigation` 직전에 신규 ## section 삽입.

Edit operation:

```
old_string:
mutation 이후 여러 caller가 같은 UI 갱신을 필요로 하면 caller마다 patch하지 말고 `rerenderList()` 같은 단일 진입점에 side-effect를 추가할 수 있는지 먼저 검토한다. v3.30 T7 P1 fix는 `rerenderList()` 1줄로 13 caller의 archive count stale 문제를 닫았다.

---

## Documentation Navigation

new_string:
mutation 이후 여러 caller가 같은 UI 갱신을 필요로 하면 caller마다 patch하지 말고 `rerenderList()` 같은 단일 진입점에 side-effect를 추가할 수 있는지 먼저 검토한다. v3.30 T7 P1 fix는 `rerenderList()` 1줄로 13 caller의 archive count stale 문제를 닫았다.

---

## 보안 기능 도입 default 패턴 (v3.43 graduate, v3.41 L2 ROI)

외부 SDK 보안 기능 (App Check, reCAPTCHA Enterprise, Cloud Armor 등) 도입 시 **2-phase rollout** 표준 적용:

1. **Phase A — 관측**: SDK client 배포 + 측정 모드 (token/signal 발급률 ≥ 99% 24h 모니터링). race 차단 + production-first 안전. 구체 toggle은 SDK별 console 참조.
2. **Phase B — 강제**: backend 측 enforce 활성. 정보 누출 회피 위한 generic 응답 정합 (예: not-found / auth-fail 경로는 202 응답).

**적용 사례**: v3.41 T1 App Check (console "Monitor mode" → "Enforce" 2-phase 패턴) + v3.41 carry C1 (Phase B 24h monitor 후 별도 cycle).

**다중 보안층 정합**: 인증/검증 SDK + 정적 GCP 제한 (예: HTTP referrer)은 redundant가 아닌 다중 layer. SDK가 strict하지만 정적 제한은 abuse 안전망. 둘 다 적용 권장 (v3.43 v2 결정으로 GCP referrer는 endpoint coverage 확장 시점에 도입 — v3.44+ carry).

---

## Documentation Navigation
```

- [ ] **Step 2: 변경 확인**

```bash
grep -n "보안 기능 도입 default 패턴" CLAUDE.md
```

Expected: 1 line, 위치는 "Single Fix Point Preference" 이후.

### 1.2 CLAUDE.md "Bundle 측정 표준" 확장

- [ ] **Step 1: Bundle 측정 표준 섹션 끝에 신규 항목 추가**

`CLAUDE.md`에서 line 91 `**경위 (v3.35 T4 graduation)**:` 단락 다음에 신규 항목 추가.

Edit operation:

```
old_string:
**경위 (v3.35 T4 graduation)**: v3.34에서 ranking 코드가 archive dynamic chunk로 emitted됐으나 canonical(index entry)은 size impact를 underestimate (+1.25 kB가 +3 B로만 reflected). archive 영역에 집중되는 변경은 dual record로 가시화 필수.

new_string:
**경위 (v3.35 T4 graduation)**: v3.34에서 ranking 코드가 archive dynamic chunk로 emitted됐으나 canonical(index entry)은 size impact를 underestimate (+1.25 kB가 +3 B로만 reflected). archive 영역에 집중되는 변경은 dual record로 가시화 필수.

**외부 SDK 추가 시 lazy chunk 위치 실측 의무 (v3.43 graduate, v3.41 L4 + v3.42 L9)**:

vite vendor chunking이 외부 SDK를 자동으로 별도 chunk에 emit. 보안/외부 SDK 추가 시 chunk 위치 확인 의무:

- caller 함수 내부 `await import(...)`로 옮기면 별도 chunk emit → first-paint 영향 0
- 실측: `gzip -c dist/assets/<chunk-name>-*.js | wc -c`
- 사례: v3.41 firebase SDK 정적 import → `translateToast` lazy chunk에 자동 흡수 (15,044 B), v3.42 App Check caller 내부 `await import` → 별도 `appCheck` chunk 분리 (13,215 B)

dual record (index + archive) 부족 시 quad record (index + archive + 신규 SDK chunk + translateToast 등)로 확장. 신규 chunk가 dynamic chain (home/settings 등)이면 first-paint 영향 0이라 별도 threshold 불요, 단순 기록.
```

- [ ] **Step 2: 변경 확인**

```bash
grep -n "외부 SDK 추가 시 lazy chunk" CLAUDE.md
```

Expected: 1 line.

### 1.3 CLAUDE.md "Codex 2-pass Review" small cycle wording 보완

- [ ] **Step 1: graduate 명문화 sub-section에 small cycle exception 추가**

`CLAUDE.md` line 180 `- **CLI 한도 fallback**: ...` 다음에 small cycle exception 한 줄 추가.

Edit operation:

```
old_string:
- **CLI 한도 fallback**: controller self-review (`pr-review-toolkit:code-reviewer` 등) 허용 (v3.14.5 / v3.20 / v3.24 선례), 단 retro에 명시.

new_string:
- **CLI 한도 fallback**: controller self-review (`pr-review-toolkit:code-reviewer` 등) 허용 (v3.14.5 / v3.20 / v3.24 선례), 단 retro에 명시.
- **small cycle exception (v3.43 graduate, v3.42 P2-2)**: ≤4 task의 lightweight cycle (docs only, lesson graduate, hotfix bundle 등)에서 CLI 1차 시도가 한도/권한 issue로 실패하면 controller self-review로 사전 review 대체 OK. 최종 review는 사이클 type 무관 의무. graduated checklist 5항목은 코드 변경 없는 task에서 N/A로 명시 가능.
```

- [ ] **Step 2: 변경 확인**

```bash
grep -n "small cycle exception" CLAUDE.md
```

Expected: 1 line.

### 1.4 COMMON_MISTAKES.md §13~§18 6 신규 lesson

- [ ] **Step 1: §12 끝 위치 찾기**

```bash
grep -n "^## 12\." .claude/COMMON_MISTAKES.md
tail -30 .claude/COMMON_MISTAKES.md
```

Expected: §12 위치 line number + EOF 30줄 (§12 본문 끝 확인).

- [ ] **Step 2: §13~§18 본문 추가**

`.claude/COMMON_MISTAKES.md` 파일 끝에 (§12 본문 직후) 6 section 추가:

```markdown

---

## 13. Dead control 제거 시 정의-사용 8-layer grep 의무 (2026-05-20, v3.43 graduate from v3.41 T6)

**Symptom**: UI 제어 dead code 제거 후 일부 layer에 id mismatch가 잔존 → 부분 작동 또는 silent break.

**Root cause**: v3.41 T6 archive period filter 제거 시 인벤토리는 `archivePeriodFilter`로 기록했으나 실제 id는 `archivePeriod` (suffix 차이). 8-layer 중 일부만 동기화 → 다른 layer에 dead reference 잔존.

**Fix/Prevention**:

1. Dead control 인벤토리 작성 전 **exact id 실측**: `grep -rn "<id>" src/ tests/ public/` — 정의된 id와 사용된 id 차이 catch.
2. 8-layer checklist (각 layer grep으로 잔여 확인):
   - EventMap 타입 정의 (`src/ui/events.ts`)
   - EVENT_NAMES const
   - V32_DEFERRED 세트 (deferred event resilience)
   - dispatcher 호출 sites
   - listener 등록 sites
   - handler 본문
   - test mock path
   - CSS selector / id
3. spec §변경 범위에 layer별 grep 결과 행으로 명시.

**관련 파일**: `src/ui/events.ts`, `src/ui/tabs/*.ts`, `src/style/`

---

## 14. 3-layer URL guard: write + read + render (2026-05-20, v3.43 graduate from v3.41 T4)

**Symptom**: write boundary에서 URL validate 추가했지만, 손상된 localStorage / import된 데이터의 render sink에서 그대로 출력 → XSS / open redirect 잔여 risk.

**Root cause**: 단일 boundary (write-only) guard는 production 진입 후 변형된 데이터를 catch 못 함. 3-layer 적용 필수: write (input validate) + read (load-time validate) + render (sink-time escape).

**Fix/Prevention**:

1. URL helper 정책 분리 (목적별):
   - `isSafeUrl(url)`: `http://` + `https://` 허용 (legacy/import 호환)
   - `isHttpsUrl(url)`: `https://` strict (image src 등 mixed content 차단)
2. 3-layer 적용:
   - **write**: 사용자 입력 / API 응답 처리 시점 (`saveUser`, `parseBriefing` 등)
   - **read**: localStorage / migration `hydrate` 시점 — fast-path bypass 차단 의무 (§16 paired)
   - **render**: DOM 삽입 직전 (`href`, `src` setter / template insertion)
3. 신규 spec: 각 layer 1+ assertion으로 backward + forward 변형 catch.

**§16 (Migrate fast-path normalize)와 paired** — read-layer guard가 fast-path bypass되면 무효. 두 항목 정합 의무. (v3.43 v2 사전 review 영역 3 P1 흡수)

**관련 파일**: `src/utils/url.ts`, `src/state/user.ts`, `src/ui/cards/*.ts`

---

## 15. 외부 SDK 추가 시 CSP 3-layer 동시 갱신 (2026-05-20, v3.43 graduate from v3.41 T1; §3 확장)

**§3 행위 root cause 위에 외부 SDK 시나리오 체크리스트를 얹은 항목.** §3 우선 진리, §15는 operational extension. (v3.43 v2 사전 review 영역 3 P1 흡수)

**Symptom**: 외부 SDK 코드 작성 + 단위 테스트 PASS / fetch mock spec PASS / smoke PASS이지만 production에서 silently 차단 — CSP violation으로 SDK fetch 실패.

**Root cause**: CSP는 production HTML response header (또는 meta) 기반 — fetch mock / page.route intercept는 우회 (§3 graduated). 외부 도메인 fetch / script load 변경 시 CSP allowlist 미갱신이면 production-only fail.

**Fix/Prevention**:

1. 외부 SDK 도입 PR 체크리스트:
   - `index.html` `<meta http-equiv="Content-Security-Policy">` connect-src/script-src에 외부 도메인 추가
   - `firebase.json` `hosting.headers[].headers[]`에서 동일 CSP header 갱신
   - CSP regression spec 추가 (정적 lint — `grep -E "connect-src" index.html firebase.json` assertion)
2. 정적 assertion spec 필수 — fetch mock으로는 CSP 못 잡는다 (§3 root cause).
3. SDK가 추가 endpoint (e.g. App Check `firebaseappcheck.googleapis.com`)를 호출하면 dist build 후 production preview로 1회 실측 권장.

**관련 파일**: `index.html`, `firebase.json`, `tests/regression/csp.spec.ts`

---

## 16. Migrate fast-path normalize 일관성 (2026-05-20, v3.43 graduate from v3.41)

**Symptom**: 신규 read-time guard / normalize 로직을 schema migration chain에 추가했으나, `isVersioned + version === CURRENT_SCHEMA` early return branch가 normalize 우회 → 손상된 current-version 데이터가 그대로 통과.

**Root cause**: migration 함수는 보통 "legacy version만 처리"라 가정하지만, 실제로는 hydrate path 통합 진입점 — current-version 데이터도 normalize 의무가 있다. fast-path early return이 한 layer를 bypass하면 보안 contract 무효.

**Fix/Prevention**:

1. read-time guard 추가 시 **legacy + current 양 path 모두 적용 의무**: fast-path early return 직전에도 normalize 호출.
2. spec §변경 범위에 "3-layer (parse / migrate / render)"라 적어도, **각 layer 안의 fast-path bypass 여부 명시 의무**.
3. 검증: corrupt current-version fixture (정상 version + 손상 field) test case 추가.

**§14 (3-layer URL guard)와 paired** — URL guard도 fast-path bypass 시 무효. 두 항목 정합 의무. (v3.43 v2 사전 review 영역 3 P1 흡수)

**관련 파일**: `src/state/user.ts` (hydrate / persist / migrate)

---

## 17. Smoke flake → synthetic isolated test 우선 (2026-05-20, v3.43 graduate from v3.42 T2)

**Symptom**: CSS pseudo-class (`:disabled`, `:focus-visible` 등) smoke test에서 첫 매칭 element 의존 (`button.btn.first()`) → DOM order shift / hydration timing에 따라 intermittent fail.

**Root cause**: Production DOM의 "첫 element"는 컴포넌트 추가/제거 / 비동기 mount에 따라 변동. CSS 행동 검증과 컴포넌트 layout 검증이 한 spec에 섞이면 flake 진단 어려움.

**Fix/Prevention**:

1. CSS pseudo-class / 행동 검증 시 **synthetic isolated DOM** 우선:
   - `await page.evaluate(() => { const b = document.createElement('button'); b.className = 'btn'; b.disabled = true; document.body.appendChild(b); return b; })`
   - 검증 후 cleanup
2. Production DOM 사용은 "사용자 flow 통합 smoke"에만 한정 (특정 UX path).
3. CSS 행동 spec과 컴포넌트 mount spec 분리.

**관련 파일**: `tests/smoke/polish.spec.ts`, `tests/smoke/<feature>.spec.ts`

---

## 18. Dynamic import mock factory export pattern (2026-05-20, v3.43 graduate from v3.42 T3)

**Symptom**: `vi.mock('./module', () => ({ ... }))` factory 안에서 함수 mock 정의 → test 본문에서 `mockResolvedValueOnce(...)` 호출 불가 (factory return 함수 reference 회수 불가).

**Root cause**: vi.mock factory는 module-level scope — closure에서 함수를 외부로 export하지 않으면 test마다 시나리오별 토큰/응답 주입 불가능. v3.41 App Check token mock 무력화 회귀 사례.

**Fix/Prevention**:

1. factory에서 mock 함수를 **외부 변수로 export**:
   ```ts
   const mockGetToken = vi.fn();
   vi.mock('firebase/app-check', () => ({ getToken: mockGetToken, /* ... */ }));
   
   // 이후 test 본문에서:
   mockGetToken.mockResolvedValueOnce({ token: 'fake-token' });
   ```
2. `vi.hoisted(() => ({ ... }))`로 mock 함수 정의 → factory + test 양쪽에서 reference 가능 (vitest 권장).
3. 신규 SDK mock 도입 시 `mockResolvedValueOnce` 가능 여부 1회 검증.

**관련 파일**: `tests/setup/firebase-mock.ts`, `tests/<feature>.spec.ts`
```

위 내용을 `.claude/COMMON_MISTAKES.md` 파일 끝에 append. 정확한 명령:

```bash
# Edit tool로 §12 마지막 줄 직후에 위 markdown block 추가
```

Edit operation의 `old_string`은 §12 본문 끝 줄을 정확히 찾아 anchor로 사용. §12 끝 줄 grep:

```bash
grep -A 30 "^## 12\." .claude/COMMON_MISTAKES.md | tail -5
```

위 grep 결과로 §12 마지막 안정 anchor 행 확인 후 Edit으로 그 다음에 위 6 section 삽입.

- [ ] **Step 2: 변경 확인**

```bash
grep -c "^## " .claude/COMMON_MISTAKES.md
```

Expected: 기존 13 (§1~§12 + "이 파일을 업데이트하는 기준") → 19 (+6).

```bash
grep -nE "^## (13|14|15|16|17|18)\." .claude/COMMON_MISTAKES.md
```

Expected: 6 line, §13~§18 모두 존재.

### 1.5 LEARNINGS_INDEX.md 갱신

- [ ] **Step 1: 전체 파일 교체** (stub 상태이므로 replace OK)

`.claude/LEARNINGS_INDEX.md`를 다음 내용으로 Write tool로 덮어쓰기:

```markdown
# Learnings Index

**Lightweight index - detailed content in `.claude/COMMON_MISTAKES.md` and `CLAUDE.md`.**

---

## Quick References (Always Available - ~800 tokens)

- **Common Mistakes**: `.claude/COMMON_MISTAKES.md` ⚠️ **READ AT SESSION START** (§1~§18, v3.43 기준)
- **Quick Start**: `.claude/QUICK_START.md`
- **Architecture**: `.claude/ARCHITECTURE_MAP.md`
- **Maintenance**: `.claude/DOCUMENTATION_MAINTENANCE.md`

## Recent Graduates (v3.40 ~ v3.43)

- §12 interest id ≠ keyword 토큰 (v3.40)
- §13 Dead control 제거 8-layer grep 의무 (v3.43 from v3.41 T6)
- §14 3-layer URL guard (write+read+render) (v3.43 from v3.41 T4)
- §15 외부 SDK 추가 시 CSP 3-layer 동시 갱신 (v3.43 from v3.41 T1, §3 확장)
- §16 Migrate fast-path normalize 일관성 (v3.43 from v3.41)
- §17 Smoke flake → synthetic isolated test 우선 (v3.43 from v3.42 T2)
- §18 vi.mock factory export pattern (v3.43 from v3.42 T3)

## CLAUDE.md Policy Updates (v3.43)

- **보안 기능 도입 default 패턴** (App Check 2-phase rollout) — 신규 ## section
- **Bundle 측정 표준** 확장: 외부 SDK lazy chunk 위치 실측 의무
- **Codex 2-pass Review** small cycle exception (≤4 task일 때 사전 review CLI fallback OK)

---

**Last Updated**: 2026-05-20
```

- [ ] **Step 2: 변경 확인**

```bash
grep -c "§1" .claude/LEARNINGS_INDEX.md
```

Expected: 7 (§12 + §13~§18).

### 1.6 T1 commit

- [ ] **Step 1: 3 파일 staging + 1 commit**

```bash
git add CLAUDE.md .claude/COMMON_MISTAKES.md .claude/LEARNINGS_INDEX.md
git commit -m "$(cat <<'EOF'
docs(v3.43 T1): lesson graduate batch — 9건 정착

v3.41 §11.5 6건 (Dead control 8-layer / App Check 2-phase / 3-layer URL guard /
SDK lazy chunk 측정 의무 / 외부 SDK CSP 3-layer / migrate fast-path) + v3.42 §7.5 3건
(smoke synthetic test / vi.mock factory / lazy chunk 실측 묶음) + P2-2 small cycle
wording을 CLAUDE.md (3건: 신규 ## + Bundle 측정 표준 확장 + Codex 2-pass small cycle
exception) + COMMON_MISTAKES.md §13~§18 (6건) + LEARNINGS_INDEX.md 갱신으로 정착.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

Expected: 3 files changed, 1 commit.

- [ ] **Step 2: T1 verify**

```bash
npx tsc --noEmit  # 0 errors (docs only, 코드 영향 X)
npm run lint      # 0 errors
```

Expected: 모두 PASS — docs 변경이 lint/TS에 영향 0이어야 함. 만약 fail이면 unrelated regression이므로 별도 조사.

---

## ~~Task 2 (구): C7 Firebase API Key GCP Referrer 절차~~ → **v3.44 carry로 강등** (v2 사전 review P0)

**근거**: Daily Growth가 Identity Toolkit/Firestore/httpsCallable 미사용 — verify script PASS가 production 안전 보장 못 함. App Check 자체가 reCAPTCHA-based strict 인증이라 referrer 제한 ROI 미달. C7 → v3.44 carry로 강등 후 Firestore/Auth/httpsCallable 도입 또는 App Check Phase B 직후 재논의.

**삭제된 산출물**:
- `docs/security/2026-05-20-firebase-api-key-referrer.md`
- `scripts/verify-referrer.mjs`

본 cycle에서 변경 0.

---

## Task 2 (v2 main 승격): Knip Config 도입

**Files:**
- Create: `knip.json`
- Modify: `package.json` (devDep + script)

**Time budget**: 1~2시간 엄수. abort 게이트 우선순위 (spec §3.4) 적용.

### 2.1 사전 점검 (진입 결정)

- [ ] **Step 1: T1 commit 완료 확인 + 잔여 시간 자가 평가**

T1 commit hash 확인 + 잔여 시간이 1시간 미만이면 T2 skip → T3 (verify) 진입.

```bash
git log --oneline -3
```

- [ ] **Step 2: Knip 호환성 1회 dry-run (30분 게이트)**

```bash
npx --yes knip@latest --no-progress --no-exit-code 2>&1 | head -40
```

Expected: dry-run report. entry auto-detect 결과 확인.

**abort 게이트 우선순위** (spec §3.4):
1. **(30분) Entry detect 실패** (Vite index.html / SW 미인식) → 즉시 abort + §2.5 v3.44 carry
2. **(1차 dry-run) False-positive 50건↑** → 즉시 abort + §2.5
3. **(Tuning 30분) 11~50건** → §2.2 진입 + ignore 정밀화 → 그래도 50건↑ 시 abort
4. **(전체 1~2h) Time budget 초과** → 현재 상태 사용자 보고 + 결정
5. **(의존성 충돌)** Node version / peer deps → 즉시 abort + §2.5

### 2.2 knip.json 작성

- [ ] **Step 1: knip.json 신규 작성**

```json
{
  "$schema": "https://unpkg.com/knip@latest/schema.json",
  "entry": [
    "src/main.ts",
    "index.html",
    "functions/src/index.ts",
    "public/sw.js"
  ],
  "project": [
    "src/**/*.ts",
    "functions/src/**/*.ts"
  ],
  "ignore": [
    "dist/**",
    "coverage/**",
    "playwright-report/**",
    "tests/fixtures/**",
    "docs/**",
    "scripts/**",
    "**/*.d.ts"
  ],
  "ignoreBinaries": [
    "firebase",
    "vercel",
    "codex"
  ],
  "ignoreDependencies": []
}
```

- [ ] **Step 2: knip 실제 실행 + report 분석**

```bash
npx knip --no-progress 2>&1 | tee /tmp/v3.43-knip-report.txt
```

Expected: false-positive ≤ 10건이면 진행 가능. 11~50건이면 §2.3 tuning. 50건↑이면 abort.

### 2.3 Report tuning (11~50건 시만, 30분 budget)

- [ ] **Step 1: ignore 패턴 정밀화**

발견된 false-positive 카테고리 분석 후 `knip.json` `ignore` 항목에 추가 패턴.

예시:
- 동적 import target → `ignoreDependencies` 또는 entry 확장
- Test 전용 export → `tests/**/*.ts` ignore (이미 fixtures만 포함, 확장 검토)
- Type-only export → `ignoreExportsUsedInFile: true` 옵션

- [ ] **Step 2: 재실행 + 50건↑ 잔존 시 abort**

```bash
npx knip --no-progress 2>&1 | wc -l
```

50건↑ 잔존 → §2.5 v3.44 carry.

### 2.4 package.json 갱신 (Report ≤ 10건 또는 tuning 후 OK 시만)

- [ ] **Step 1: devDep + script 추가**

```bash
grep -n '"devDependencies"' package.json
```

`package.json` Edit:
- `devDependencies`에 `"knip": "^5.x.x"` 추가 (실제 latest 버전 확인)
- `scripts.knip`: `"knip --no-progress"` 추가

- [ ] **Step 2: install**

```bash
npm install
```

Expected: knip + transitive deps install. `package-lock.json` 갱신.

- [ ] **Step 3: npm run knip 1회 실행**

```bash
npm run knip
```

Expected: clean report (exit 0) 또는 발견 N건 (exit 1, 정상). 발견 dead code fix는 본 사이클 비목표 — v3.44+ P3.

### 2.5 abort 시 처리

- [ ] **Step 1: 사용자 즉시 보고**

abort 사유 + 잔여 작업 보고:
- entry auto-detect 실패 → "Vite index.html 인식 불완전, 30분 시도 후 abort"
- false-positive 50건↑ → "tuning 부담 큼, 실측 N건 보고"
- time budget 초과 → "1~2h 초과, 현재 진행 상태 보고"

- [ ] **Step 2: 사용자 confirm 후 untracked 정리**

사용자가 "untracked 정리해" 또는 동등 명시 후에만:

```bash
git status --short
rm knip.json
```

사용자가 "남겨두자" 시 untracked 그대로 유지 (git이 추적 안 함, v3.44에서 재시도 가능). **CLAUDE.md "되돌리기 어려운 동작 확인" 정합**.

- [ ] **Step 3: v3.44 carry 메모**

retro §carry-forward에 명시:
- C6-K1: Knip entry auto-detect 실패 사유
- C6-K2: false-positive N건 — tuning 부담 큼
- C6-K3: time budget 초과 시점 + 잔여 작업

### 2.6 T2 commit (Report OK 시만)

- [ ] **Step 1: staging + commit**

```bash
git add knip.json package.json package-lock.json
git commit -m "$(cat <<'EOF'
chore(v3.43 T2): knip dead-code 도구 도입 (v2 사전 review로 옵셔널 → main 승격)

v3.41 carry C6 — Vite/Functions/SW entry 인식 + 1회 report. 발견된
dead code fix는 본 사이클 비목표 (v3.44+ P3 분류).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: 3종 검증 + Codex 최종 Review + Bundle Quad Record

**Files:**
- Read: `dist/assets/*.js` (bundle 측정)
- Output: `/tmp/v3.43-codex-final.txt`

### 3.1 3종 검증

- [ ] **Step 1: TS strict**

```bash
npx tsc --noEmit
```

Expected: 0 errors. docs/script-only이므로 baseline 유지.

- [ ] **Step 2: Lint**

```bash
npm run lint
```

Expected: 0 errors.

- [ ] **Step 3: Vitest 양쪽 TZ**

```bash
TZ=Asia/Seoul npm test 2>&1 | tail -10
TZ=America/New_York npm test 2>&1 | tail -10
```

Expected: 각 1546/1546 PASS.

- [ ] **Step 4: Functions vitest**

```bash
cd functions && npm test 2>&1 | tail -10 && cd ..
```

Expected: 24/24 PASS.

- [ ] **Step 5: Smoke (cold-start N=3)**

```bash
npm run test:smoke 2>&1 | tail -20
```

Expected: 109/109 PASS. v3.42 polish flake graduate 후 안정 상태 유지.

### 3.2 Bundle Quad Record

- [ ] **Step 1: 클린 build**

```bash
npm run build 2>&1 | tail -10
```

Expected: success, `dist/` 갱신.

- [ ] **Step 2: 4 chunk gzip 측정**

```bash
echo "index:         $(gzip -c dist/assets/index-*.js | wc -c) B"
echo "archive:       $(gzip -c dist/assets/archive-*.js | wc -c) B"
echo "appCheck:      $(gzip -c dist/assets/appCheck-*.js | wc -c) B"
echo "translateToast: $(gzip -c dist/assets/translateToast-*.js | wc -c) B"
```

Expected (v3.42 → v3.43 ±50 B):
- index: 9,379 → 9,379 ±50
- archive: 8,976 → 8,976 ±50
- appCheck: 13,215 → 13,215 ±50
- translateToast: 2,136 → 2,136 ±50

Δ가 ±50 B 넘으면 retro §Bundle에 원인 분석 (예상 외 코드 변경 확인).

### 3.3 Codex 최종 review

- [ ] **Step 1: CLI dispatch**

```bash
HEAD_HASH=$(git rev-parse HEAD)
codex exec --skip-git-repo-check -o /tmp/v3.43-codex-final.txt "$(cat <<EOF
v3.43 deploy gate review 요청.

이번 사이클은 lightweight (lesson graduate + Knip dead-code 도구). 신규 기능 0,
schema migration 0, 코드 변경 최소 (knip.json + package.json devDep).
C7 GCP referrer는 v2 사전 review로 v3.44 carry 강등됨.

확인 요청:

1. CLAUDE.md 신규 ## "보안 기능 도입 default 패턴" Phase A/B wording이 일반화/future-proof한지 (App Check hardcode 제거 정합)
2. CLAUDE.md "Bundle 측정 표준" 외부 SDK lazy chunk 항목 정합
3. CLAUDE.md "Codex 2-pass Review" small cycle exception wording 합리성
4. COMMON_MISTAKES.md §13~§18 6 신규 lesson의 Symptom/Root cause/Fix·Prevention 4부 구조 일관성 + §3↔§15 / §14↔§16 paired cross-link 정합
5. LEARNINGS_INDEX.md Recent Graduates + CLAUDE.md Policy Updates 섹션 갱신 정합
6. (T2 진입 시) knip.json entry 인식 + report quality + abort 게이트 우선순위 (entry 30분 / FP 50건 / time 1~2h)
7. Bundle quad record ±50 B 유지 + 50 B 초과 escalation 절차 명시 여부

분류: DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT.

Repo HEAD: ${HEAD_HASH}
EOF
)"
```

CLI 한도 시 fallback (사용자에게 즉시 알림 — CLAUDE.md "fallback 단계 정책 3번"):

```bash
# fallback: controller pr-review-toolkit:code-reviewer subagent dispatch
```

- [ ] **Step 2: 분류 + 사용자 보고**

```bash
cat /tmp/v3.43-codex-final.txt
```

분류:
- **DEPLOY_APPROVED** → T4 retro + 사용자 "배포해줘" 대기
- **APPROVED_WITH_NOTES** → P2 carry-forward 명시 후 deploy gate 충족 (P0/P1 없으면 진행 OK)
- **REJECT** → in-cycle fix + 재 review

---

## Task 4: Retro + 배포 + Obsidian Sync

**Files:**
- Create: `/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v343.md`
- Modify: `MEMORY.md` (index entry 추가)
- Update: Obsidian vault `/Users/hayden/Hayden/20_Projects/Dev_Project/SJ_Daily Growth(Ai assistant)/`

### 4.1 Retro 메모리 작성

- [ ] **Step 1: 신규 memory file 작성**

`/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v343.md`:

```markdown
---
name: daily-growth-v343
description: "DG v3.43 사이클 — Lesson graduate batch (v3.41 §11.5 6건 + v3.42 §7.5 3건 + P2-2) + Knip dead-code 도구 main 도입. v2 사전 review fallback으로 C7 GCP referrer 강등. 신규 기능 0, schema migration 0."
metadata:
  node_type: memory
  type: project
---

# DG v3.43 Lesson Graduate + GCP Referrer

**기간**: 2026-05-20 (1 day, lightweight cycle)
**Type**: Lesson graduate + 보안 절차 문서 + 옵셔널 dead-code 도구. 신규 기능 0, schema migration 0.
**Reference precedent**: v3.24 (메타 graduate), v3.40 (13사이클 ROI graduate), v3.42 (light carry).

## 사이클 트리거

v3.42 deploy 직후 사용자가 "C1 Phase B 제외하고 carry/cleanup 진행" 요청. controller가 5개 후보 중 ROI 비교 → 옵션 1+2 묶음 (lesson graduate + Knip 옵셔널) + C7 in-cycle 포함 결정 (v1). v2 사전 review fallback (Codex CLI killed → controller `pr-review-toolkit:code-reviewer`)에서 P0 catch (영역 4 endpoint mismatch + 영역 7 abort 분기) → Option B 채택: C7 → v3.44 carry 강등 + Knip → T2 main 승격.

## Codex 2-pass Review

- 사전 review: <분류 + P0/P1/P2 catch 결과>
- 최종 review: <DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT>

## Commits

| Commit | Task | 내용 |
|---|---|---|
| `08fa3a7` | T0 | spec v1 |
| `60256cd` | T0 | plan v1 |
| `<T0 spec/plan v2>` | T0 | v2 사전 review fallback P0 흡수 (Option B 채택) |
| `<T1>` | T1 | lesson graduate 9건 정착 (CLAUDE.md + COMMON_MISTAKES §13~§18 + LEARNINGS_INDEX) |
| `<T2>` | T2 | Knip config 도입 (또는 abort + v3.44 carry) |
| (commit 없음) | T3 | verify (TS/lint/vitest/functions/smoke) + bundle quad record + Codex 최종 |
| `<T4 retro>` | T4 | retro memo + spec retro §갱신 |

## 검증

- TS strict: 0 errors
- Lint: 0 errors
- Vitest 양쪽 TZ: 1546/1546
- Functions vitest: 24/24
- Smoke: 109/109

## Bundle Quad Record (v3.42 → v3.43)

| Chunk | v3.42 | v3.43 | Δ |
|---|---:|---:|---:|
| index | 9,379 | <측정> | <±50> |
| archive | 8,976 | <측정> | <±50> |
| appCheck | 13,215 | <측정> | <±50> |
| translateToast | 2,136 | <측정> | <±50> |

## 신규 Lessons (있다면)

- (메타 lesson — graduate 작업 자체에서 도출 가능)

## v3.44 Carry-Forward

| ID | 항목 | 출처 | 우선순위 |
|---|---|---|---|
| C1 | T1 Phase B backend enforce + 202 + UX copy | v3.41 carry (24h monitor 후) | **HIGH** |
| C4 | first briefing storage thrash | v3.41 carry | L |
| C5 | state↔ui layer cycle 정리 | v3.41 carry | L (별도 사이클) |
| C6-K1/K2 | Knip abort 사유 (해당 시) | v3.43 T2 | M |
| v3.17 §7.2 | 식물 detail modal / Streak Freeze 등 | v3.17 retro | M |

## 사용자 결정 사항

- ✅ 옵션 1+2 묶음 채택 (lesson graduate + Knip 옵셔널) — v1
- ✅ C7 in-cycle (절차 + 검증 script) — v1 controller 결정
- ✅ **v2: Option B 채택** — C7 → v3.44 carry 강등, Knip → T2 main 승격 (v2 사전 review P0 후 사용자 confirm)
- ✅ Lesson 분배: CLAUDE.md 3건 + COMMON_MISTAKES §13~§18 6건 + LEARNINGS_INDEX 갱신

## 다음 사이클 권고

- C1 Phase B (24h monitor 후 별도 사이클)
- v3.17 §7.2 신규 기능 (식물 detail modal / Streak Freeze 등)
```

- [ ] **Step 2: MEMORY.md index entry 추가**

`/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md` 끝에 한 줄 추가:

```
- [DG v3.43 Lesson Graduate + Knip](project_daily_growth_v343.md) — Lesson graduate batch (v3.41 §11.5 6건 + v3.42 §7.5 3건 + P2-2) + Knip main 도입. v2 사전 review fallback으로 C7 GCP referrer → v3.44 carry 강등. 신규 기능 0, schema 0.
```

### 4.2 retro commit

- [ ] **Step 1: docs/superpowers/specs retro 섹션 갱신**

본 spec 파일에 §10 Retro 추가 (또는 spec 그대로 유지하고 memory만 갱신 — controller 결정).

- [ ] **Step 2: commit**

```bash
git add docs/superpowers/specs/2026-05-20-v3.43-lesson-graduate-cleanup-design.md
git commit -m "$(cat <<'EOF'
docs(v3.43 T4): retro §10 + Codex 2-pass archive

사이클 종료 — lesson graduate batch + Knip main (또는 abort). C7 → v3.44 carry. 신규 기능 0,
schema 0. v3.44 carry-forward: C1 Phase B (24h monitor 후), C4/C5, v3.17 §7.2.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

### 4.3 배포 (사용자 명시 시점)

- [ ] **Step 1: 사용자 "배포해줘" 명시 대기**

CLAUDE.md "절대 규칙": 사용자 명시 전까지 deploy 금지.

- [ ] **Step 2: build + deploy**

```bash
npm run build
firebase deploy --only hosting
```

Expected: Firebase Hosting deploy success. URL 확인: `https://my-ai-assistant-904f3.web.app`.

### 4.4 Obsidian Sync

- [ ] **Step 1: vault 경로 변수**

```bash
VAULT="/Users/hayden/Hayden/20_Projects/Dev_Project/SJ_Daily Growth(Ai assistant)"
```

- [ ] **Step 2: 02-Cycles/ + 03-Lessons/ + 01-Workflow/ + 00-Index.md sync**

```bash
cp docs/superpowers/specs/2026-05-20-v3.43-lesson-graduate-cleanup-design.md "$VAULT/02-Cycles/"
cp /Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v343.md "$VAULT/03-Lessons/"
cp /Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md "$VAULT/03-Lessons/"
cp CLAUDE.md "$VAULT/01-Workflow/CLAUDE-project.md"
cp .claude/COMMON_MISTAKES.md "$VAULT/01-Workflow/"
cp .claude/LEARNINGS_INDEX.md "$VAULT/01-Workflow/"
```

- [ ] **Step 3: 00-Index.md 타임라인 추가**

`$VAULT/00-Index.md`의 사이클 타임라인 표에 v3.43 한 줄 추가:

```
| v3.43 | 2026-05-20 | Lesson graduate batch + Knip main | 9 lesson 정착 / Knip 도입 (또는 abort) / C7 v3.44 carry 강등 | 배포 완료 |
```

- [ ] **Step 4: vault sync 완료 알림**

사용자에게 vault path + sync 완료 한 줄 보고.

---

## Self-Review (post-write 점검)

- [x] **Spec coverage (v2)**: spec §1 (T1 lesson) + §3 (T2 Knip main) + §4 (T3 verify) + §5 (T4 retro) 모두 plan task로 mapping. §0.2.3 (C7) → v3.44 carry로 강등 명시.
- [x] **Placeholder scan**: TBD/TODO/implement later 없음. T0 Codex 사전 review 결과는 fallback (controller `pr-review-toolkit:code-reviewer`)으로 완료 + Step 4에서 REQUIRES_FIX → Option B 채택 → spec v2 + plan v2 흡수.
- [x] **Type consistency**: chunk 이름 (`index-*`, `archive-*`, `appCheck-*`, `translateToast-*`) 일관. Codex 최종 review prompt heredoc shell substitution fix (`HEAD_HASH=$(git rev-parse HEAD)` 사전 캡처 + `<<EOF` no-quote, 부가 P1 흡수).
- [x] **No-code task의 TDD 대안**: T1 docs only — step "Edit → grep으로 변경 확인" 패턴. T2 Knip — `npx knip` 1회 실행 결과로 검증.

---

## v3.44 Carry-Forward (Plan reference)

본 cycle 완료 후 v3.44+ 사이클 후보:

| ID | 항목 | 출처 | 우선순위 |
|---|---|---|---|
| C1 | T1 Phase B backend enforce + 202 + UX copy | v3.41 carry | **HIGH** (24h monitor 후) |
| **C7** (재진입) | **Firebase API key GCP referrer 절차 + 검증 script** | v3.43 v2 강등 | M (Firestore/Auth/httpsCallable 도입 또는 App Check Phase B 직후) |
| C4 | First briefing storage thrash (`fireTriggers` batch) | v3.41 carry | L |
| C5 | state↔ui layer cycle 정리 | v3.41 carry | L (별도 사이클) |
| C6-K1/K2 | Knip abort 사유 (해당 시) | v3.43 T2 | M |
| C6-residual | Knip 발견 dead code fix (진입 + clean report 시) | v3.43 T2 | P3 |
| Codex CLI retroactive | v3.43 사전 review direct CLI 재시도 (권한 회복 시) | CLAUDE.md "fallback 정책 6번" | L (권고) |
| v3.17 §7.2 | 식물 detail modal / Streak Freeze 등 | v3.17 retro | M (신규 기능 cycle) |
