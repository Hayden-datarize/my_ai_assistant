# v3.43 Lesson Graduate Batch + GCP Referrer 절차 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v3.41 §11.5 + v3.42 §7.5 누적 9 lesson을 CLAUDE.md / COMMON_MISTAKES.md / LEARNINGS_INDEX.md로 정착하고, Firebase API key GCP referrer 보안 절차 문서 + 검증 script를 작성하며, 시간 여유 시 Knip dead-code 도구 옵셔널 도입.

**Architecture:** Lightweight cycle — 코드 변경 최소 (script 1개 신규, config 1개 옵셔널). T1 docs only, T2 standalone Node.js script (`scripts/verify-referrer.mjs`), T3 옵셔널 (`knip.json`). 신규 schema migration 0, production 코드 0줄.

**Tech Stack:** Markdown docs (`.claude/`, `docs/`), Node 22+ native fetch (`scripts/`), Knip (optional devDep).

**Spec reference:** [docs/superpowers/specs/2026-05-20-v3.43-lesson-graduate-cleanup-design.md](../specs/2026-05-20-v3.43-lesson-graduate-cleanup-design.md)

---

## File Structure

**Created (T2)**:

- `docs/security/` (신규 디렉토리)
- `docs/security/2026-05-20-firebase-api-key-referrer.md` — GCP Console 절차 + threat model + rollback
- `scripts/` (신규 디렉토리)
- `scripts/verify-referrer.mjs` — Node 22+ fetch 기반 allow/block case 검증

**Created (T3 옵셔널)**:

- `knip.json` — entry-based dead-code 분석 config

**Modified (T1)**:

- `CLAUDE.md` — 신규 ## "보안 기능 도입 default 패턴", "Bundle 측정 표준" 확장, "Codex 2-pass Review" wording 보완
- `.claude/COMMON_MISTAKES.md` — §13~§18 6 신규 lesson
- `.claude/LEARNINGS_INDEX.md` — Last Updated + Learning Topics 섹션 COMMON_MISTAKES reference로 갱신

**Modified (T3 옵셔널)**:

- `package.json` — devDependencies + `npm run knip` script

---

## Task 0: 사전 Codex Review (CLAUDE.md graduated default)

**Files:**
- Read: spec + 본 plan (이미 commit됨)
- Output: `/tmp/v3.43-codex-pre.txt`

CLAUDE.md "Codex 사전 review default checklist" + "graduate 명문화" 의무 — plan v1 작성 직후 dispatch. small cycle이지만 direct CLI 1차 시도 의무 (v3.31 L1).

- [ ] **Step 1: Codex 사전 review CLI dispatch**

```bash
codex exec --skip-git-repo-check -o /tmp/v3.43-codex-pre.txt "$(cat <<'EOF'
다음 spec + plan을 review해. P0/P1/P2/DoD/QUESTION 분류로 보고.

CLAUDE.md graduated checklist:
1. Schema chain superset coverage (본 cycle은 schema 변경 0, N/A)
2. interest id ≠ keyword 토큰 (matchesInterest helper) — 본 cycle 코드 변경 없음 N/A
3. predicate-defined vs wired (caller grep + wiring sites) — 본 cycle docs 위주 N/A
4. mutation paths 전수 grep — 본 cycle 코드 변경 없음 N/A
5. test mock path stale 여부 — 본 cycle 신규 spec 없음 N/A

이 small cycle (≤4 task) 특성상 위 checklist는 대부분 N/A — 대신 다음을 review:
- Lesson 9건 분배 합리성 (CLAUDE.md vs COMMON_MISTAKES vs LEARNINGS_INDEX)
- CLAUDE.md 신규 wording이 일반화/future-proof 한지 (특정 사이클 hardcode 회피)
- GCP referrer 절차 문서가 비개발자 친화 + 검증 우선 강조 패턴 충족
- verify-referrer.mjs allow/block 2-case 설계의 false-positive/negative risk
- T3 Knip 옵셔널 abort 조건 (30분 entry detect / 1~2h budget / 50건 false-positive)이 적정한지
- 누락된 graduate 후보 lesson (v3.41/v3.42 retro 내 다른 항목 누락 여부)

spec: docs/superpowers/specs/2026-05-20-v3.43-lesson-graduate-cleanup-design.md
plan: docs/superpowers/plans/2026-05-20-v3.43-lesson-graduate-cleanup-plan.md
EOF
)"
```

Expected: `/tmp/v3.43-codex-pre.txt`에 P0/P1/P2/DoD/QUESTION 분류 결과. CLI 한도 시 STDERR 캡처 + fallback (controller spec self-review) 진입.

- [ ] **Step 2: 결과 분석**

```bash
cat /tmp/v3.43-codex-pre.txt
```

분류 검토:
- **P0 catch 있음** → plan v2 1:1 흡수 후 commit. T1 진입 X.
- **P1만** → in-cycle 반영 위치 결정 (T1/T2/T3 중 어디서), plan v2 commit. T1 진입 OK.
- **P2/DoD/QUESTION만** → retro §흡수 항목으로 carry. plan 그대로 T1 진입.

- [ ] **Step 3: plan v2 흡수 (P0/P1 catch 있을 시만)**

P0/P1 흡수 위치 명시한 plan v2 작성 → commit.

```bash
git add docs/superpowers/plans/2026-05-20-v3.43-lesson-graduate-cleanup-plan.md
git commit -m "docs(v3.43 T0): plan v2 — Codex 사전 review P0/P1 in-cycle 흡수"
```

- [ ] **Step 4: Codex 사전 review 결과 보고 + 사용자 confirm**

사용자에게 "Codex 사전 review 분류 결과" 한 줄 요약 + plan v2 흡수 commit hash 전달. 사용자 confirm 후 T1 진입.

CLI 한도/fallback 시 즉시 알림 (CLAUDE.md "Codex 2-pass Review fallback 단계 정책 3번").

---

## Task 1: Lesson Graduate Batch (9건 + P2-2)

**Files:**
- Modify: `CLAUDE.md` (3 위치 — 신규 ## section, Bundle 측정 표준 확장, Codex 2-pass wording)
- Modify: `.claude/COMMON_MISTAKES.md` (§13~§18 신규)
- Modify: `.claude/LEARNINGS_INDEX.md` (Last Updated + Learning Topics 갱신)

### 1.1 CLAUDE.md 신규 ## "보안 기능 도입 default 패턴"

- [ ] **Step 1: 신규 ## section 추가 (Single Fix Point Preference 다음, Documentation Navigation 이전)**

`CLAUDE.md`에서 `### Single Fix Point Preference (v3.31 L5)` 섹션 직후 + `## Documentation Navigation` 직전에 신규 ## section 삽입.

Edit operation:

```
old_string:
mutation 이후 여러 caller가 같은 UI 갱신을 필요로 하면 caller마다 patch하지 말고 `rerenderList()` 같은 단일 진입점에 side-effect를 추가할 수 있는지 먼저 검토한다. v3.30 T7 P1 fix는 `rerenderList()` 1줄로 13 caller의 archive count stale 문제를 닫았다.

---

## Documentation Navigation

new_string:
mutation 이후 여러 caller가 같은 UI 갱신을 필요로 하면 caller마다 patch하지 말고 `rerenderList()` 같은 단일 진입점에 side-effect를 추가할 수 있는지 먼저 검토한다. v3.30 T7 P1 fix는 `rerenderList()` 1줄로 13 caller의 archive count stale 문제를 닫았다.

---

## 보안 기능 도입 default 패턴 (v3.43 graduate, v3.41 L2 ROI)

외부 SDK 보안 기능 (App Check, reCAPTCHA Enterprise 등) 도입 시 **2-phase rollout** 표준 적용:

1. **Phase A — frontend monitor**: SDK 코드를 client에 배포 + console "Monitor mode" ON. 24시간 token 발급률 ≥ 99% 모니터링. race condition 차단 + production-first 안전.
2. **Phase B — backend enforce**: Functions/API 측 `enforceAppCheck:true` + console "Enforce" toggle ON. 정보 누출 회피를 위해 not-found/auth-fail 경로는 generic 202 응답 정합.

**적용 사례**: v3.41 T1 App Check Phase A + v3.41 carry C1 (Phase B 24h monitor 후 별도 cycle).

**다중 보안층 정합**: App Check + Firebase API key referrer 제한은 redundant가 아닌 다중 layer. App Check가 strict하지만 referrer 제한은 API key abuse 안전망. 둘 다 적용 권장 ([docs/security/2026-05-20-firebase-api-key-referrer.md](docs/security/2026-05-20-firebase-api-key-referrer.md)).

---

## Documentation Navigation
```

- [ ] **Step 2: 변경 확인**

```bash
grep -n "보안 기능 도입 default 패턴" CLAUDE.md
```

Expected: 1 line, 위치는 "Single Fix Point Preference" 이후.

### 1.2 CLAUDE.md "Bundle 측정 표준" 확장

- [ ] **Step 1: Bundle 측정 표준 섹션 끝에 신규 항목 추가**

`CLAUDE.md`에서 line 91 `**경위 (v3.35 T4 graduation)**:` 단락 다음에 신규 항목 추가.

Edit operation:

```
old_string:
**경위 (v3.35 T4 graduation)**: v3.34에서 ranking 코드가 archive dynamic chunk로 emitted됐으나 canonical(index entry)은 size impact를 underestimate (+1.25 kB가 +3 B로만 reflected). archive 영역에 집중되는 변경은 dual record로 가시화 필수.

new_string:
**경위 (v3.35 T4 graduation)**: v3.34에서 ranking 코드가 archive dynamic chunk로 emitted됐으나 canonical(index entry)은 size impact를 underestimate (+1.25 kB가 +3 B로만 reflected). archive 영역에 집중되는 변경은 dual record로 가시화 필수.

**외부 SDK 추가 시 lazy chunk 위치 실측 의무 (v3.43 graduate, v3.41 L4 + v3.42 L9)**:

vite vendor chunking이 외부 SDK를 자동으로 별도 chunk에 emit. 보안/외부 SDK 추가 시 chunk 위치 확인 의무:

- caller 함수 내부 `await import(...)`로 옮기면 별도 chunk emit → first-paint 영향 0
- 실측: `gzip -c dist/assets/<chunk-name>-*.js | wc -c`
- 사례: v3.41 firebase SDK 정적 import → `translateToast` lazy chunk에 자동 흡수 (15,044 B), v3.42 App Check caller 내부 `await import` → 별도 `appCheck` chunk 분리 (13,215 B)

dual record (index + archive) 부족 시 quad record (index + archive + 신규 SDK chunk + translateToast 등)로 확장. 신규 chunk가 dynamic chain (home/settings 등)이면 first-paint 영향 0이라 별도 threshold 불요, 단순 기록.
```

- [ ] **Step 2: 변경 확인**

```bash
grep -n "외부 SDK 추가 시 lazy chunk" CLAUDE.md
```

Expected: 1 line.

### 1.3 CLAUDE.md "Codex 2-pass Review" small cycle wording 보완

- [ ] **Step 1: graduate 명문화 sub-section에 small cycle exception 추가**

`CLAUDE.md` line 180 `- **CLI 한도 fallback**: ...` 다음에 small cycle exception 한 줄 추가.

Edit operation:

```
old_string:
- **CLI 한도 fallback**: controller self-review (`pr-review-toolkit:code-reviewer` 등) 허용 (v3.14.5 / v3.20 / v3.24 선례), 단 retro에 명시.

new_string:
- **CLI 한도 fallback**: controller self-review (`pr-review-toolkit:code-reviewer` 등) 허용 (v3.14.5 / v3.20 / v3.24 선례), 단 retro에 명시.
- **small cycle exception (v3.43 graduate, v3.42 P2-2)**: ≤4 task의 lightweight cycle (docs only, lesson graduate, hotfix bundle 등)에서 CLI 1차 시도가 한도/권한 issue로 실패하면 controller self-review로 사전 review 대체 OK. 최종 review는 사이클 type 무관 의무. graduated checklist 5항목은 코드 변경 없는 task에서 N/A로 명시 가능.
```

- [ ] **Step 2: 변경 확인**

```bash
grep -n "small cycle exception" CLAUDE.md
```

Expected: 1 line.

### 1.4 COMMON_MISTAKES.md §13~§18 6 신규 lesson

- [ ] **Step 1: §12 끝 위치 찾기**

```bash
grep -n "^## 12\." .claude/COMMON_MISTAKES.md
tail -30 .claude/COMMON_MISTAKES.md
```

Expected: §12 위치 line number + EOF 30줄 (§12 본문 끝 확인).

- [ ] **Step 2: §13~§18 본문 추가**

`.claude/COMMON_MISTAKES.md` 파일 끝에 (§12 본문 직후) 6 section 추가:

```markdown

---

## 13. Dead control 제거 시 정의-사용 8-layer grep 의무 (2026-05-20, v3.43 graduate from v3.41 T6)

**Symptom**: UI 제어 dead code 제거 후 일부 layer에 id mismatch가 잔존 → 부분 작동 또는 silent break.

**Root cause**: v3.41 T6 archive period filter 제거 시 인벤토리는 `archivePeriodFilter`로 기록했으나 실제 id는 `archivePeriod` (suffix 차이). 8-layer 중 일부만 동기화 → 다른 layer에 dead reference 잔존.

**Fix/Prevention**:

1. Dead control 인벤토리 작성 전 **exact id 실측**: `grep -rn "<id>" src/ tests/ public/` — 정의된 id와 사용된 id 차이 catch.
2. 8-layer checklist (각 layer grep으로 잔여 확인):
   - EventMap 타입 정의 (`src/ui/events.ts`)
   - EVENT_NAMES const
   - V32_DEFERRED 세트 (deferred event resilience)
   - dispatcher 호출 sites
   - listener 등록 sites
   - handler 본문
   - test mock path
   - CSS selector / id
3. spec §변경 범위에 layer별 grep 결과 행으로 명시.

**관련 파일**: `src/ui/events.ts`, `src/ui/tabs/*.ts`, `src/style/`

---

## 14. 3-layer URL guard: write + read + render (2026-05-20, v3.43 graduate from v3.41 T4)

**Symptom**: write boundary에서 URL validate 추가했지만, 손상된 localStorage / import된 데이터의 render sink에서 그대로 출력 → XSS / open redirect 잔여 risk.

**Root cause**: 단일 boundary (write-only) guard는 production 진입 후 변형된 데이터를 catch 못 함. 3-layer 적용 필수: write (input validate) + read (load-time validate) + render (sink-time escape).

**Fix/Prevention**:

1. URL helper 정책 분리 (목적별):
   - `isSafeUrl(url)`: `http://` + `https://` 허용 (legacy/import 호환)
   - `isHttpsUrl(url)`: `https://` strict (image src 등 mixed content 차단)
2. 3-layer 적용:
   - **write**: 사용자 입력 / API 응답 처리 시점 (`saveUser`, `parseBriefing` 등)
   - **read**: localStorage / migration `hydrate` 시점 — fast-path bypass 차단 의무 (§16 paired)
   - **render**: DOM 삽입 직전 (`href`, `src` setter / template insertion)
3. 신규 spec: 각 layer 1+ assertion으로 backward + forward 변형 catch.

**관련 파일**: `src/utils/url.ts`, `src/state/user.ts`, `src/ui/cards/*.ts`

---

## 15. 외부 SDK 추가 시 CSP 3-layer 동시 갱신 (2026-05-20, v3.43 graduate from v3.41 T1; §3 확장)

**Symptom**: 외부 SDK 코드 작성 + 단위 테스트 PASS / fetch mock spec PASS / smoke PASS이지만 production에서 silently 차단 — CSP violation으로 SDK fetch 실패.

**Root cause**: CSP는 production HTML response header (또는 meta) 기반 — fetch mock / page.route intercept는 우회 (§3 graduated). 외부 도메인 fetch / script load 변경 시 CSP allowlist 미갱신이면 production-only fail.

**Fix/Prevention**:

1. 외부 SDK 도입 PR 체크리스트:
   - `index.html` `<meta http-equiv="Content-Security-Policy">` connect-src/script-src에 외부 도메인 추가
   - `firebase.json` `hosting.headers[].headers[]`에서 동일 CSP header 갱신
   - CSP regression spec 추가 (정적 lint — `grep -E "connect-src" index.html firebase.json` assertion)
2. 정적 assertion spec 필수 — fetch mock으로는 CSP 못 잡는다 (§3 root cause).
3. SDK가 추가 endpoint (e.g. App Check `firebaseappcheck.googleapis.com`)를 호출하면 dist build 후 production preview로 1회 실측 권장.

**관련 파일**: `index.html`, `firebase.json`, `tests/regression/csp.spec.ts`

---

## 16. Migrate fast-path normalize 일관성 (2026-05-20, v3.43 graduate from v3.41)

**Symptom**: 신규 read-time guard / normalize 로직을 schema migration chain에 추가했으나, `isVersioned + version === CURRENT_SCHEMA` early return branch가 normalize 우회 → 손상된 current-version 데이터가 그대로 통과.

**Root cause**: migration 함수는 보통 "legacy version만 처리"라 가정하지만, 실제로는 hydrate path 통합 진입점 — current-version 데이터도 normalize 의무가 있다. fast-path early return이 한 layer를 bypass하면 보안 contract 무효.

**Fix/Prevention**:

1. read-time guard 추가 시 **legacy + current 양 path 모두 적용 의무**: fast-path early return 직전에도 normalize 호출.
2. spec §변경 범위에 "3-layer (parse / migrate / render)"라 적어도, **각 layer 안의 fast-path bypass 여부 명시 의무**.
3. 검증: corrupt current-version fixture (정상 version + 손상 field) test case 추가.

**관련 파일**: `src/state/user.ts` (hydrate / persist / migrate)

---

## 17. Smoke flake → synthetic isolated test 우선 (2026-05-20, v3.43 graduate from v3.42 T2)

**Symptom**: CSS pseudo-class (`:disabled`, `:focus-visible` 등) smoke test에서 첫 매칭 element 의존 (`button.btn.first()`) → DOM order shift / hydration timing에 따라 intermittent fail.

**Root cause**: Production DOM의 "첫 element"는 컴포넌트 추가/제거 / 비동기 mount에 따라 변동. CSS 행동 검증과 컴포넌트 layout 검증이 한 spec에 섞이면 flake 진단 어려움.

**Fix/Prevention**:

1. CSS pseudo-class / 행동 검증 시 **synthetic isolated DOM** 우선:
   - `await page.evaluate(() => { const b = document.createElement('button'); b.className = 'btn'; b.disabled = true; document.body.appendChild(b); return b; })`
   - 검증 후 cleanup
2. Production DOM 사용은 "사용자 flow 통합 smoke"에만 한정 (특정 UX path).
3. CSS 행동 spec과 컴포넌트 mount spec 분리.

**관련 파일**: `tests/smoke/polish.spec.ts`, `tests/smoke/<feature>.spec.ts`

---

## 18. Dynamic import mock factory export pattern (2026-05-20, v3.43 graduate from v3.42 T3)

**Symptom**: `vi.mock('./module', () => ({ ... }))` factory 안에서 함수 mock 정의 → test 본문에서 `mockResolvedValueOnce(...)` 호출 불가 (factory return 함수 reference 회수 불가).

**Root cause**: vi.mock factory는 module-level scope — closure에서 함수를 외부로 export하지 않으면 test마다 시나리오별 토큰/응답 주입 불가능. v3.41 App Check token mock 무력화 회귀 사례.

**Fix/Prevention**:

1. factory에서 mock 함수를 **외부 변수로 export**:
   ```ts
   const mockGetToken = vi.fn();
   vi.mock('firebase/app-check', () => ({ getToken: mockGetToken, /* ... */ }));
   
   // 이후 test 본문에서:
   mockGetToken.mockResolvedValueOnce({ token: 'fake-token' });
   ```
2. `vi.hoisted(() => ({ ... }))`로 mock 함수 정의 → factory + test 양쪽에서 reference 가능 (vitest 권장).
3. 신규 SDK mock 도입 시 `mockResolvedValueOnce` 가능 여부 1회 검증.

**관련 파일**: `tests/setup/firebase-mock.ts`, `tests/<feature>.spec.ts`
```

위 내용을 `.claude/COMMON_MISTAKES.md` 파일 끝에 append. 정확한 명령:

```bash
# Edit tool로 §12 마지막 줄 직후에 위 markdown block 추가
```

Edit operation의 `old_string`은 §12 본문 끝 줄을 정확히 찾아 anchor로 사용. §12 끝 줄 grep:

```bash
grep -A 30 "^## 12\." .claude/COMMON_MISTAKES.md | tail -5
```

위 grep 결과로 §12 마지막 안정 anchor 행 확인 후 Edit으로 그 다음에 위 6 section 삽입.

- [ ] **Step 2: 변경 확인**

```bash
grep -c "^## " .claude/COMMON_MISTAKES.md
```

Expected: 기존 13 (§1~§12 + "이 파일을 업데이트하는 기준") → 19 (+6).

```bash
grep -nE "^## (13|14|15|16|17|18)\." .claude/COMMON_MISTAKES.md
```

Expected: 6 line, §13~§18 모두 존재.

### 1.5 LEARNINGS_INDEX.md 갱신

- [ ] **Step 1: 전체 파일 교체** (stub 상태이므로 replace OK)

`.claude/LEARNINGS_INDEX.md`를 다음 내용으로 Write tool로 덮어쓰기:

```markdown
# Learnings Index

**Lightweight index - detailed content in `.claude/COMMON_MISTAKES.md` and `CLAUDE.md`.**

---

## Quick References (Always Available - ~800 tokens)

- **Common Mistakes**: `.claude/COMMON_MISTAKES.md` ⚠️ **READ AT SESSION START** (§1~§18, v3.43 기준)
- **Quick Start**: `.claude/QUICK_START.md`
- **Architecture**: `.claude/ARCHITECTURE_MAP.md`
- **Maintenance**: `.claude/DOCUMENTATION_MAINTENANCE.md`

## Recent Graduates (v3.40 ~ v3.43)

- §12 interest id ≠ keyword 토큰 (v3.40)
- §13 Dead control 제거 8-layer grep 의무 (v3.43 from v3.41 T6)
- §14 3-layer URL guard (write+read+render) (v3.43 from v3.41 T4)
- §15 외부 SDK 추가 시 CSP 3-layer 동시 갱신 (v3.43 from v3.41 T1, §3 확장)
- §16 Migrate fast-path normalize 일관성 (v3.43 from v3.41)
- §17 Smoke flake → synthetic isolated test 우선 (v3.43 from v3.42 T2)
- §18 vi.mock factory export pattern (v3.43 from v3.42 T3)

## CLAUDE.md Policy Updates (v3.43)

- **보안 기능 도입 default 패턴** (App Check 2-phase rollout) — 신규 ## section
- **Bundle 측정 표준** 확장: 외부 SDK lazy chunk 위치 실측 의무
- **Codex 2-pass Review** small cycle exception (≤4 task일 때 사전 review CLI fallback OK)

---

**Last Updated**: 2026-05-20
```

- [ ] **Step 2: 변경 확인**

```bash
grep -c "§1" .claude/LEARNINGS_INDEX.md
```

Expected: 7 (§12 + §13~§18).

### 1.6 T1 commit

- [ ] **Step 1: 3 파일 staging + 1 commit**

```bash
git add CLAUDE.md .claude/COMMON_MISTAKES.md .claude/LEARNINGS_INDEX.md
git commit -m "$(cat <<'EOF'
docs(v3.43 T1): lesson graduate batch — 9건 정착

v3.41 §11.5 6건 (Dead control 8-layer / App Check 2-phase / 3-layer URL guard /
SDK lazy chunk 측정 의무 / 외부 SDK CSP 3-layer / migrate fast-path) + v3.42 §7.5 3건
(smoke synthetic test / vi.mock factory / lazy chunk 실측 묶음) + P2-2 small cycle
wording을 CLAUDE.md (3건: 신규 ## + Bundle 측정 표준 확장 + Codex 2-pass small cycle
exception) + COMMON_MISTAKES.md §13~§18 (6건) + LEARNINGS_INDEX.md 갱신으로 정착.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

Expected: 3 files changed, 1 commit.

- [ ] **Step 2: T1 verify**

```bash
npx tsc --noEmit  # 0 errors (docs only, 코드 영향 X)
npm run lint      # 0 errors
```

Expected: 모두 PASS — docs 변경이 lint/TS에 영향 0이어야 함. 만약 fail이면 unrelated regression이므로 별도 조사.

---

## Task 2: C7 Firebase API Key GCP Referrer 절차

**Files:**
- Create: `docs/security/2026-05-20-firebase-api-key-referrer.md`
- Create: `scripts/verify-referrer.mjs`
- Modify: 없음 (코드 변경 0)

### 2.1 선행 실측 (의무 — spec §2.2 결정)

- [ ] **Step 1: Production endpoint 인벤토리 grep**

```bash
grep -rn "firebaseConfig\|apiKey\|FIREBASE_API_KEY" src/ functions/src/ index.html 2>/dev/null | grep -v "node_modules" | head -20
```

Expected: API key가 reference되는 src/ + functions/src/ + index.html 위치 목록.

- [ ] **Step 2: GCP referrer 제한 적용 endpoint 확인**

Daily Growth가 호출하는 Firebase services를 식별:

```bash
grep -rnE "firestore|auth|httpsCallable|app-check|getToken" src/ | head -20
```

Expected: 실제 사용 중인 Firebase API 목록. (예상: App Check `getToken`, Functions `httpsCallable`)

해당 API들이 GCP API key restrictions의 "Website restrictions"에 영향받는지 확인 (Firebase Functions httpsCallable는 referrer 영향 받음, App Check는 token-based로 별도).

- [ ] **Step 3: 결과 분기**

- **Effective endpoint 1+ 있음** → §2.2 plan대로 진행
- **모든 endpoint가 referrer 영향 없음** → 절차 문서 §1 "Why"를 "App Check 대체가 아닌 다중 보안층 안전망" 강조로 재작성 + ROI 노트
- **둘 다 미달** (예외) → T2 abort + v3.44 carry. 사용자에게 보고.

### 2.2 절차 문서 작성

- [ ] **Step 1: 디렉토리 생성 확인**

```bash
mkdir -p docs/security
ls -la docs/security
```

Expected: 빈 디렉토리.

- [ ] **Step 2: 절차 문서 Write**

`docs/security/2026-05-20-firebase-api-key-referrer.md` 신규 작성:

```markdown
# Firebase API Key GCP Referrer 제한 절차

**Date**: 2026-05-20 (v3.43 cycle T2)
**Audience**: 비개발자 친화 + 검증 우선 강조
**관련 cycle**: v3.41 T1 App Check Phase A carry-forward (C7)

---

## 1. Why — 왜 referrer 제한이 필요한가

Firebase Web API key는 client-side에 노출됨 (정상 설계 — public). 그러나 GCP Console에서 **HTTP referrer 제한**을 설정하지 않으면, 외부 abuser가 key를 추출해 자신의 도메인에서 Firebase services를 호출할 수 있다.

**Threat scenarios**:

- API quota 도용 (App Check token / Functions 호출 / Firestore read·write 할당량 소진)
- Functions 호출량 증가 → Cloud Run / Cloud Functions billing 발생
- App Check Phase A (monitor mode) 동안에는 enforce가 약하므로 referrer 제한이 1차 안전망

**다중 보안층 정합**:

- App Check (v3.41 T1 + carry C1 Phase B): token-based strict — primary defense
- Referrer 제한 (본 문서): GCP-level static restriction — secondary safety net
- 둘은 redundant가 아닌 다른 layer. 둘 다 적용 권장.

---

## 2. GCP Console step-by-step

1. https://console.cloud.google.com/ 접속 + 프로젝트 `my-ai-assistant-904f3` 선택.
2. 좌측 메뉴 → **APIs & Services** → **Credentials**.
3. "API keys" 섹션에서 Firebase 사용 중인 key 선택 (보통 "Browser key (auto created by Firebase)" 또는 유사 이름).
4. 페이지 상단 **EDIT API KEY** 클릭.
5. **Application restrictions** 섹션에서 **"HTTP referrers (web sites)"** 라디오 선택.
6. **"Website restrictions"** 아래 **ADD AN ITEM** 클릭 후 다음 패턴 추가 (각 1줄씩):
   - `https://my-ai-assistant-904f3.web.app/*`
   - `https://my-ai-assistant-904f3.firebaseapp.com/*`
   - `http://localhost:*`  (dev — Vite dev server)
   - `http://localhost:*/*` (일부 GCP UI는 trailing slash 요구 — 둘 다 추가 권장)
7. 페이지 하단 **SAVE** 클릭.
8. **즉시 §4 검증 절차로 이동** (production block 확인).

---

## 3. Referrer Allowlist 패턴 — 명세

| 패턴 | 용도 | 비고 |
|---|---|---|
| `https://my-ai-assistant-904f3.web.app/*` | Firebase Hosting production | 메인 도메인 |
| `https://my-ai-assistant-904f3.firebaseapp.com/*` | Firebase 기본 도메인 | Auth callback 등 |
| `http://localhost:*` | Vite dev server | port 5173 + 임의 포트 |
| `http://localhost:*/*` | localhost path 호환 | GCP UI 일부 버전 차이 흡수 |

**wildcard 의미**:

- `*` = 1 segment (단일 path component 또는 port 번호)
- `**` 또는 path 끝의 `/*` = 모든 sub-path 허용
- subdomain 별 분리 필요 시 명시 추가 (예: `https://staging.my-ai-assistant-904f3.web.app/*`)

**커스텀 도메인 추가 시**: 도메인 인증 완료 + Firebase Hosting 등록 후 위 allowlist에 한 줄 추가.

---

## 4. 검증 절차 (사용자 GCP 설정 직후 — 의무)

referrer 잘못 설정 시 production block 즉시 catch. **§2 SAVE 직후 반드시 1회 실행.**

### 방법 A — curl 인라인 (1줄씩)

`<API_KEY>`를 본인 Firebase API key로 치환. Identity Toolkit endpoint는 일반적으로 referrer 제한 적용 대상:

```bash
# allow case — production referrer
curl -s -o /dev/null -w "%{http_code}\n" \
  -H "Referer: https://my-ai-assistant-904f3.web.app/" \
  "https://identitytoolkit.googleapis.com/v1/accounts:lookup?key=<API_KEY>"
```

Expected: `400` (referrer OK, payload 부족으로 인한 정상 application 오류 — referrer block은 `403`)

```bash
# block case — 외부 referrer
curl -s -o /dev/null -w "%{http_code}\n" \
  -H "Referer: https://attacker.example.com/" \
  "https://identitytoolkit.googleapis.com/v1/accounts:lookup?key=<API_KEY>"
```

Expected: `403 PERMISSION_DENIED` (referrer block 성공).

**판정**:

- Allow=400 + Block=403 → ✅ PASS (referrer 제한 작동)
- Allow=403 또는 Block=200 → ❌ FAIL (production block 위험 / referrer 무효)
- 둘 다 200 → 적용 범위 미스매치 (다른 endpoint로 재검증)

### 방법 B — Node.js script (자동화)

```bash
# 환경변수 인라인
FIREBASE_API_KEY=<API_KEY> node scripts/verify-referrer.mjs

# 또는 dotenv (Node 20.6+ native flag)
node --env-file=.env.local scripts/verify-referrer.mjs
```

Expected output:

```
[v3.43 verify-referrer.mjs]
  allow case (https://my-ai-assistant-904f3.web.app/): HTTP 400 → PASS
  block case (https://attacker.example.com/):         HTTP 403 → PASS
Result: 2/2 PASS

```

Exit code: 0 = both PASS, 1 = 1+ FAIL.

---

## 5. Rollback — emergency 시 referrer 제한 제거

production block 발견 시 1분 안에 복구:

1. GCP Console → APIs & Services → Credentials → 해당 API key → EDIT API KEY.
2. **Application restrictions** 섹션에서 **"None"** 라디오 선택.
3. SAVE 클릭.
4. 1~2분 propagation 대기 후 production page 새로 고침 + 정상 동작 확인.

**Rollback 후 재시도 절차**:

- 어떤 allowlist 패턴이 production block 원인이었는지 분석 (HTTP referrer header 실측 — browser DevTools Network 탭)
- 패턴 수정 후 §2.5~§2.8 단계 재실행
- §4 검증 절차 반드시 1회 PASS 후 확정

---

## 6. Future Notes

- **API key rotation 시 의무**: GCP Console에서 신규 key 생성 후 동일 referrer allowlist 재설정 필요.
- **Firebase project console에서 신규 web app 추가 시**: 새 도메인 (예: 별도 환경) 등록되면 위 allowlist에 명시 추가.
- **Custom 도메인 도입 시**: §3 allowlist에 한 줄 추가.
- **App Check Phase B (v3.41 carry C1) 도입 후 redundant 오해**: 본 referrer 제한은 App Check의 보완 layer이지 대체가 아님. App Check가 strict하지만 referrer는 GCP-level static safety net으로 둘 다 유지.

---

## 7. 검증 history

| Date | Operator | Allow status | Block status | Verdict |
|---|---|---|---|---|
| (Pending) | (Hayden) | — | — | — |

> 사용자가 §2 SAVE + §4 검증 완료 후 본 표 1행 추가. controller는 readonly.
```

- [ ] **Step 3: 변경 확인**

```bash
wc -l docs/security/2026-05-20-firebase-api-key-referrer.md
grep -c "^## " docs/security/2026-05-20-firebase-api-key-referrer.md
```

Expected: ~100~150 line + 7 sections (§1~§7).

### 2.3 verify-referrer.mjs 작성

- [ ] **Step 1: 디렉토리 생성 확인**

```bash
mkdir -p scripts
ls -la scripts
```

Expected: 빈 디렉토리.

- [ ] **Step 2: script Write**

`scripts/verify-referrer.mjs` 신규 작성:

```javascript
#!/usr/bin/env node
// v3.43 T2 — Firebase API key GCP referrer 검증
// 사용: FIREBASE_API_KEY=xxx node scripts/verify-referrer.mjs
//   또는: node --env-file=.env.local scripts/verify-referrer.mjs

const API_KEY = process.env.FIREBASE_API_KEY;
if (!API_KEY) {
  console.error("[verify-referrer] FIREBASE_API_KEY env var 미설정.");
  console.error("  인라인:  FIREBASE_API_KEY=xxx node scripts/verify-referrer.mjs");
  console.error("  dotenv:  node --env-file=.env.local scripts/verify-referrer.mjs");
  process.exit(2);
}

const ENDPOINT = `https://identitytoolkit.googleapis.com/v1/accounts:lookup?key=${API_KEY}`;
const ALLOW_REFERER = "https://my-ai-assistant-904f3.web.app/";
const BLOCK_REFERER = "https://attacker.example.com/";

async function probe(referer, label) {
  try {
    const res = await fetch(ENDPOINT, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        Referer: referer,
      },
      body: JSON.stringify({ idToken: "invalid-dummy" }),
    });
    return { label, referer, status: res.status, ok: true };
  } catch (err) {
    return { label, referer, status: null, ok: false, error: String(err) };
  }
}

function verdict(allow, block) {
  // allow: referrer OK → 400 (payload invalid라는 정상 application 오류)
  // block: referrer 외부 → 403 (referrer block)
  const allowPass = allow.status === 400;
  const blockPass = block.status === 403;
  return { allowPass, blockPass, bothPass: allowPass && blockPass };
}

async function main() {
  console.log("[v3.43 verify-referrer.mjs]");
  const allow = await probe(ALLOW_REFERER, "allow");
  const block = await probe(BLOCK_REFERER, "block");

  const allowMark = allow.status === 400 ? "PASS" : "FAIL";
  const blockMark = block.status === 403 ? "PASS" : "FAIL";

  console.log(`  allow case (${ALLOW_REFERER}): HTTP ${allow.status} → ${allowMark}`);
  console.log(`  block case (${BLOCK_REFERER}): HTTP ${block.status} → ${blockMark}`);

  const v = verdict(allow, block);
  const passCount = (v.allowPass ? 1 : 0) + (v.blockPass ? 1 : 0);
  console.log(`Result: ${passCount}/2 PASS`);

  if (!v.bothPass) {
    console.error("\n[diagnostic]");
    if (!v.allowPass) {
      console.error(`  allow case expected HTTP 400 (payload invalid), got ${allow.status}.`);
      if (allow.status === 403) {
        console.error(`  → production referrer가 block됨. allowlist 패턴 확인 필요.`);
      }
    }
    if (!v.blockPass) {
      console.error(`  block case expected HTTP 403 (referrer block), got ${block.status}.`);
      if (block.status === 400) {
        console.error(`  → 외부 referrer가 block되지 않음. GCP Console에서 referrer 제한 활성화 확인.`);
      }
    }
    process.exit(1);
  }
}

main().catch((err) => {
  console.error("[verify-referrer] unexpected error:", err);
  process.exit(2);
});
```

- [ ] **Step 3: script syntax check (실제 실행은 사용자 GCP 설정 후)**

```bash
node --check scripts/verify-referrer.mjs
```

Expected: silent (0 exit code). syntax error 시 file/line 표시.

- [ ] **Step 4: dry-run (no API_KEY env → exit 2 + 사용법 출력)**

```bash
node scripts/verify-referrer.mjs
```

Expected: exit code 2 + STDERR에 사용법 3줄 출력. 실제 fetch 호출 X.

### 2.4 T2 commit

- [ ] **Step 1: 2 파일 staging + 1 commit**

```bash
git add docs/security/2026-05-20-firebase-api-key-referrer.md scripts/verify-referrer.mjs
git commit -m "$(cat <<'EOF'
docs(v3.43 T2): Firebase API key GCP referrer 절차 + 검증 script

v3.41 carry C7 — Firebase Web API key abuse 차단 위한 GCP referrer 제한 절차
문서 + Node 22+ native fetch 기반 verify-referrer.mjs script 작성. 비개발자 친화
step-by-step + 검증 우선 강조 + rollback 절차. App Check (v3.41 T1 / carry C1)와
다중 보안층 정합. 코드 변경 0.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

Expected: 2 files changed, 1 commit.

---

## Task 3 (옵셔널): Knip Config 도입 시도

**Files:**
- Create: `knip.json`
- Modify: `package.json` (devDep + script)

**Time budget**: 1~2시간 엄수. abort 조건 충족 시 즉시 v3.44 carry.

### 3.1 사전 점검 (진입 결정)

- [ ] **Step 1: T1+T2 완료 시점 시간 잔여 확인**

사용자에게 잔여 시간 보고. 30분 미만이면 T3 skip → T4 진입.

- [ ] **Step 2: Knip 호환성 1회 dry-run**

```bash
npx --yes knip@latest --no-progress --no-exit-code 2>&1 | head -40
```

Expected: dry-run report (`knip.json` 없으면 default config로 시도). entry auto-detect 결과 확인.

- **Vite entry 자동 인식 OK** → §3.2 진입
- **entry detect 실패** → abort + v3.44 carry (사용자 보고)
- **report 50줄 이상** → tuning 부담 큼, abort + v3.44 carry

### 3.2 knip.json 작성

- [ ] **Step 1: knip.json 신규 작성**

```json
{
  "$schema": "https://unpkg.com/knip@latest/schema.json",
  "entry": [
    "src/main.ts",
    "index.html",
    "functions/src/index.ts",
    "public/sw.js"
  ],
  "project": [
    "src/**/*.ts",
    "functions/src/**/*.ts"
  ],
  "ignore": [
    "dist/**",
    "coverage/**",
    "playwright-report/**",
    "tests/fixtures/**",
    "docs/**",
    "scripts/**",
    "**/*.d.ts"
  ],
  "ignoreBinaries": [
    "firebase",
    "vercel",
    "codex"
  ],
  "ignoreDependencies": []
}
```

- [ ] **Step 2: knip 실제 실행 + report 분석**

```bash
npx knip --no-progress 2>&1 | tee /tmp/v3.43-knip-report.txt
```

Expected: false-positive ≤ 10건이면 진행 가능. 그 이상이면 tuning 또는 abort.

- [ ] **Step 3: 결과 분기**

- **Report ≤ 10건** → §3.3 진입 (devDep + script 추가)
- **Report 11~50건** → tuning 30분 시도 (ignore 패턴 정밀화) → 그래도 50건↑ 시 abort
- **Report 50건↑** → abort + v3.44 carry. knip.json은 commit 하지 않거나 placeholder로만 commit + comment 추가.

### 3.3 package.json 갱신 (Report OK 시만)

- [ ] **Step 1: devDependencies + script 추가**

`package.json` Edit:

```bash
grep -n '"devDependencies"' package.json
```

old_string은 `package.json`의 `devDependencies` 객체 끝 + `scripts` 객체 변경 위치. 정확한 형식은 현재 파일 보고 결정 (line 단위 Edit).

devDependencies에 추가:
```
"knip": "^5.x.x",
```

scripts에 추가:
```
"knip": "knip --no-progress",
```

- [ ] **Step 2: install**

```bash
npm install
```

Expected: knip + transitive deps install. `package-lock.json` 갱신.

- [ ] **Step 3: npm run knip 1회 실행**

```bash
npm run knip
```

Expected: report 출력. exit code 0 = clean, 1 = issues found (정상).

### 3.4 T3 commit (Report OK 시만)

- [ ] **Step 1: staging + commit**

```bash
git add knip.json package.json package-lock.json
git commit -m "$(cat <<'EOF'
chore(v3.43 T3): knip dead-code 도구 도입 (옵셔널)

v3.41 carry C6 — Vite/Functions/SW entry 인식 + 1회 clean report. 발견된
dead code fix는 본 사이클 비목표 (v3.44+ P3 분류).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

### 3.5 abort 시 처리

- [ ] **Step 1: 사용자 즉시 보고 (abort 진입 사유)**

abort 사유 + 잔여 작업 보고:
- entry auto-detect 실패 → "Vite index.html 인식 불완전, 30분 시도 후 abort"
- false-positive 50건↑ → "tuning 부담 큼, 실측 N건 보고"
- time budget 초과 → "1~2h 초과, 현재 진행 상태 보고"

- [ ] **Step 2: 사용자 confirm 후 untracked 파일 정리**

사용자가 "untracked 정리해" 또는 동등 명시 후에만:

```bash
# git status 확인 후
git status --short
# untracked knip.json만 제거 (예: 신규 작성 후 polluted)
rm knip.json
```

사용자가 "남겨두자" 시 untracked 그대로 유지 (git이 추적 안 함, v3.44에서 재시도 가능).

- [ ] **Step 3: v3.44 carry 메모**

retro §carry-forward에 명시:
- C6-K1: Knip entry auto-detect 실패 사유
- C6-K2: false-positive N건 — tuning 부담 큼
- C6-K3: time budget 초과 시점 + 잔여 작업

---

## Task 4: 3종 검증 + Codex 최종 Review + Bundle Quad Record

**Files:**
- Read: `dist/assets/*.js` (bundle 측정)
- Output: `/tmp/v3.43-codex-final.txt`

### 4.1 3종 검증

- [ ] **Step 1: TS strict**

```bash
npx tsc --noEmit
```

Expected: 0 errors. docs/script-only이므로 baseline 유지.

- [ ] **Step 2: Lint**

```bash
npm run lint
```

Expected: 0 errors.

- [ ] **Step 3: Vitest 양쪽 TZ**

```bash
TZ=Asia/Seoul npm test 2>&1 | tail -10
TZ=America/New_York npm test 2>&1 | tail -10
```

Expected: 각 1546/1546 PASS.

- [ ] **Step 4: Functions vitest**

```bash
cd functions && npm test 2>&1 | tail -10 && cd ..
```

Expected: 24/24 PASS.

- [ ] **Step 5: Smoke (cold-start N=3)**

```bash
npm run test:smoke 2>&1 | tail -20
```

Expected: 109/109 PASS. v3.42 polish flake graduate 후 안정 상태 유지.

### 4.2 Bundle Quad Record

- [ ] **Step 1: 클린 build**

```bash
npm run build 2>&1 | tail -10
```

Expected: success, `dist/` 갱신.

- [ ] **Step 2: 4 chunk gzip 측정**

```bash
echo "index:         $(gzip -c dist/assets/index-*.js | wc -c) B"
echo "archive:       $(gzip -c dist/assets/archive-*.js | wc -c) B"
echo "appCheck:      $(gzip -c dist/assets/appCheck-*.js | wc -c) B"
echo "translateToast: $(gzip -c dist/assets/translateToast-*.js | wc -c) B"
```

Expected (v3.42 → v3.43 ±50 B):
- index: 9,379 → 9,379 ±50
- archive: 8,976 → 8,976 ±50
- appCheck: 13,215 → 13,215 ±50
- translateToast: 2,136 → 2,136 ±50

Δ가 ±50 B 넘으면 retro §Bundle에 원인 분석 (예상 외 코드 변경 확인).

### 4.3 Codex 최종 review

- [ ] **Step 1: CLI dispatch**

```bash
codex exec --skip-git-repo-check -o /tmp/v3.43-codex-final.txt "$(cat <<'EOF'
v3.43 deploy gate review 요청.

이번 사이클은 lightweight (lesson graduate + GCP referrer 절차). 신규 기능 0,
schema migration 0, 코드 변경 최소 (scripts/verify-referrer.mjs 1개 + knip.json
옵셔널).

확인 요청:

1. CLAUDE.md 신규 ## "보안 기능 도입 default 패턴" wording이 일반화/future-proof한지
2. CLAUDE.md "Bundle 측정 표준" 외부 SDK lazy chunk 항목 정합
3. CLAUDE.md "Codex 2-pass Review" small cycle exception (≤4 task) wording 합리성
4. COMMON_MISTAKES.md §13~§18 6 신규 lesson의 Symptom/Root cause/Fix·Prevention 4부 구조 일관성
5. docs/security/2026-05-20-firebase-api-key-referrer.md 비개발자 친화도 + 검증 우선 강조 패턴
6. scripts/verify-referrer.mjs allow/block 2-case 설계의 false-positive/negative risk
7. (T3 진입 시) knip.json entry 인식 + report quality
8. Bundle quad record ±50 B 유지

분류: DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT.

Repo HEAD: <git rev-parse HEAD>
EOF
)"
```

CLI 한도 시 fallback (사용자에게 즉시 알림 — CLAUDE.md "fallback 단계 정책 3번"):

```bash
# fallback: controller pr-review-toolkit:code-reviewer subagent dispatch
```

- [ ] **Step 2: 분류 + 사용자 보고**

```bash
cat /tmp/v3.43-codex-final.txt
```

분류:
- **DEPLOY_APPROVED** → T5 retro + 사용자 "배포해줘" 대기
- **APPROVED_WITH_NOTES** → P2 carry-forward 명시 후 deploy gate 충족 (P0/P1 없으면 진행 OK)
- **REJECT** → in-cycle fix + 재 review

---

## Task 5: Retro + 배포 + Obsidian Sync

**Files:**
- Create: `/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v343.md`
- Modify: `MEMORY.md` (index entry 추가)
- Update: Obsidian vault `/Users/hayden/Hayden/20_Projects/Dev_Project/SJ_Daily Growth(Ai assistant)/`

### 5.1 Retro 메모리 작성

- [ ] **Step 1: 신규 memory file 작성**

`/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v343.md`:

```markdown
---
name: daily-growth-v343
description: "DG v3.43 사이클 — Lesson graduate batch (v3.41 §11.5 6건 + v3.42 §7.5 3건 + P2-2) + Firebase API key GCP referrer 절차 (C7) + Knip 옵셔널 도입. 신규 기능 0, schema migration 0."
metadata:
  node_type: memory
  type: project
---

# DG v3.43 Lesson Graduate + GCP Referrer

**기간**: 2026-05-20 (1 day, lightweight cycle)
**Type**: Lesson graduate + 보안 절차 문서 + 옵셔널 dead-code 도구. 신규 기능 0, schema migration 0.
**Reference precedent**: v3.24 (메타 graduate), v3.40 (13사이클 ROI graduate), v3.42 (light carry).

## 사이클 트리거

v3.42 deploy 직후 사용자가 "C1 Phase B 제외하고 carry/cleanup 진행" 요청. controller가 5개 후보 중 ROI 비교 → 옵션 1+2 묶음 (lesson graduate + Knip 옵셔널) + C7 in-cycle 포함 결정.

## Codex 2-pass Review

- 사전 review: <분류 + P0/P1/P2 catch 결과>
- 최종 review: <DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT>

## Commits

| Commit | Task | 내용 |
|---|---|---|
| `08fa3a7` | T0 | spec v1 |
| `<T0 plan v1>` | T0 | plan v1 |
| `<T0 plan v2>` | T0 | (사전 review P0/P1 흡수 시) |
| `<T1>` | T1 | lesson graduate 9건 정착 |
| `<T2>` | T2 | C7 절차 + verify-referrer.mjs |
| `<T3>` | T3 | (옵셔널) knip config — 또는 abort |
| `<T5 retro>` | T5 | retro memo |

## 검증

- TS strict: 0 errors
- Lint: 0 errors
- Vitest 양쪽 TZ: 1546/1546
- Functions vitest: 24/24
- Smoke: 109/109

## Bundle Quad Record (v3.42 → v3.43)

| Chunk | v3.42 | v3.43 | Δ |
|---|---:|---:|---:|
| index | 9,379 | <측정> | <±50> |
| archive | 8,976 | <측정> | <±50> |
| appCheck | 13,215 | <측정> | <±50> |
| translateToast | 2,136 | <측정> | <±50> |

## 신규 Lessons (있다면)

- (메타 lesson — graduate 작업 자체에서 도출 가능)

## v3.44 Carry-Forward

| ID | 항목 | 출처 | 우선순위 |
|---|---|---|---|
| C1 | T1 Phase B backend enforce + 202 + UX copy | v3.41 carry (24h monitor 후) | **HIGH** |
| C4 | first briefing storage thrash | v3.41 carry | L |
| C5 | state↔ui layer cycle 정리 | v3.41 carry | L (별도 사이클) |
| C6-K1/K2 | Knip abort 사유 (해당 시) | v3.43 T3 | M |
| v3.17 §7.2 | 식물 detail modal / Streak Freeze 등 | v3.17 retro | M |

## 사용자 결정 사항

- ✅ 옵션 1+2 묶음 채택 (lesson graduate + Knip 옵셔널)
- ✅ C7 in-cycle (절차 + 검증 script) — controller 결정
- ✅ T2 검증 script 형식: curl + Node.js 듀얼
- ✅ T2 위치: `docs/security/`
- ✅ Lesson 분배: CLAUDE.md 3건 + COMMON_MISTAKES §13~§18 6건

## 다음 사이클 권고

- C1 Phase B (24h monitor 후 별도 사이클)
- v3.17 §7.2 신규 기능 (식물 detail modal / Streak Freeze 등)
```

- [ ] **Step 2: MEMORY.md index entry 추가**

`/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md` 끝에 한 줄 추가:

```
- [DG v3.43 Lesson Graduate + GCP Referrer](project_daily_growth_v343.md) — Lesson graduate batch (v3.41 §11.5 6건 + v3.42 §7.5 3건 + P2-2) + C7 절차 + Knip 옵셔널. 신규 기능 0, schema 0.
```

### 5.2 retro commit

- [ ] **Step 1: docs/superpowers/specs retro 섹션 갱신**

본 spec 파일에 §10 Retro 추가 (또는 spec 그대로 유지하고 memory만 갱신 — controller 결정).

- [ ] **Step 2: commit**

```bash
git add docs/superpowers/specs/2026-05-20-v3.43-lesson-graduate-cleanup-design.md
git commit -m "$(cat <<'EOF'
docs(v3.43 T5): retro §10 + Codex 2-pass archive

사이클 종료 — lesson graduate batch + C7 절차 + (옵셔널) Knip. 신규 기능 0,
schema 0. v3.44 carry-forward: C1 Phase B (24h monitor 후), C4/C5, v3.17 §7.2.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

### 5.3 배포 (사용자 명시 시점)

- [ ] **Step 1: 사용자 "배포해줘" 명시 대기**

CLAUDE.md "절대 규칙": 사용자 명시 전까지 deploy 금지.

- [ ] **Step 2: build + deploy**

```bash
npm run build
firebase deploy --only hosting
```

Expected: Firebase Hosting deploy success. URL 확인: `https://my-ai-assistant-904f3.web.app`.

### 5.4 Obsidian Sync

- [ ] **Step 1: vault 경로 변수**

```bash
VAULT="/Users/hayden/Hayden/20_Projects/Dev_Project/SJ_Daily Growth(Ai assistant)"
```

- [ ] **Step 2: 02-Cycles/ + 03-Lessons/ + 01-Workflow/ + 00-Index.md sync**

```bash
cp docs/superpowers/specs/2026-05-20-v3.43-lesson-graduate-cleanup-design.md "$VAULT/02-Cycles/"
cp /Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v343.md "$VAULT/03-Lessons/"
cp /Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md "$VAULT/03-Lessons/"
cp CLAUDE.md "$VAULT/01-Workflow/CLAUDE-project.md"
cp .claude/COMMON_MISTAKES.md "$VAULT/01-Workflow/"
cp .claude/LEARNINGS_INDEX.md "$VAULT/01-Workflow/"
```

- [ ] **Step 3: 00-Index.md 타임라인 추가**

`$VAULT/00-Index.md`의 사이클 타임라인 표에 v3.43 한 줄 추가:

```
| v3.43 | 2026-05-20 | Lesson graduate batch + GCP referrer 절차 | 9 lesson 정착 / C7 절차 / Knip 옵셔널 | 배포 완료 |
```

- [ ] **Step 4: vault sync 완료 알림**

사용자에게 vault path + sync 완료 한 줄 보고.

---

## Self-Review (post-write 점검)

- [x] **Spec coverage**: spec §1~§5 + §0.2.2 분배표 + §0.2.3 C7 위치 + §3 Knip 옵셔널 + §4 검증 + §5 retro 모두 plan task로 mapping됨.
- [x] **Placeholder scan**: TBD/TODO/implement later 없음. 모든 step에 실제 명령어 + 예상 출력 명시. T0 Codex 사전 review 결과 분기는 "P0/P1 catch 있을 시" 라는 조건부 분기로 명확.
- [x] **Type consistency**: API key env var (`FIREBASE_API_KEY`) / referrer endpoint (`identitytoolkit.googleapis.com`) / chunk 이름 (`index-*`, `archive-*`, `appCheck-*`, `translateToast-*`) 일관.
- [x] **No-code task의 TDD 대안**: T1은 docs only — TDD 불가. step "Edit → grep으로 변경 확인" 패턴으로 대체. T2 verify-referrer.mjs는 syntax check + dry-run으로 sanity 검증 (실제 fetch 검증은 사용자 GCP 설정 후 production 실측).

---

## v3.44 Carry-Forward (Plan reference)

본 cycle 완료 후 v3.44+ 사이클 후보:

| ID | 항목 | 출처 | 우선순위 |
|---|---|---|---|
| C1 | T1 Phase B backend enforce + 202 + UX copy | v3.41 carry | **HIGH** (24h monitor 후) |
| C4 | First briefing storage thrash (`fireTriggers` batch) | v3.41 carry | L |
| C5 | state↔ui layer cycle 정리 | v3.41 carry | L (별도 사이클) |
| C6-K1/K2 | Knip abort 사유 (해당 시) | v3.43 T3 | M |
| C6-residual | Knip 발견 dead code fix (진입 + clean report 시) | v3.43 T3 | P3 |
| v3.17 §7.2 | 식물 detail modal / Streak Freeze 등 | v3.17 retro | M (신규 기능 cycle) |
