# v3.21 — Plant detail modal + Streak Freeze Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Stats 탭 garden card에서 식물 detail modal을 열고, Streak에 Auto Freeze (Duolingo 정석, 1일=1개, cap 2, 주간 1 재충전)를 추가한다. v4 → v5 schema lazy migration.

**Architecture:** Approach A — Schema 먼저 (T1), Freeze engine + UI (T2~T5), Modal helper + render + wiring (T6~T10), Meta (T11~T12). 사전 review = T0 직후. atomic single-write 유지 (`recordDailyAnswer` saveUser 1회), reward event 흐름은 v3.12 `takeSnapshot/runSweep` 패턴 그대로, modal a11y는 `shared.openModal`이 처리.

**Tech Stack:** Vite + TypeScript + Vitest + Playwright. Firebase Hosting (직배포). 기존 graduated 패턴 — `migrateUserToVN`, `shared.openModal`, `Snapshot/runSweep` reward stream, `interestKeywords + matchKeyword` 매칭, KST `getDateStr` (Pattern A).

**Spec**: `docs/superpowers/specs/2026-05-07-v3.21-plant-modal-streak-freeze-design.md` (ground truth).

---

## File Structure

### 신규 파일
- `src/state/freezeEngine.ts` — `regenerateFreeze` + `consumeFreezeForGap` (Freeze 핵심 로직)
- `src/utils/interestCounts.ts` — `getAnswerCountByInterest` + `getScrapCountByInterest` (modal counts)
- `src/ui/modals/plant-detail.ts` — `openPlantDetailModal` + bodyHtml builder
- `tests/state/freezeEngine.spec.ts` — T2/T3 vitest
- `tests/state/migration-v5.spec.ts` — T1 vitest
- `tests/state/user-record-with-freeze.spec.ts` — T3 inject vitest
- `tests/ui/streak-banner-freeze.spec.ts` — T4 vitest
- `tests/state/rewards-freeze-event.spec.ts` — T5 vitest
- `tests/utils/interestCounts.spec.ts` — T6 vitest
- `tests/ui/modals/plant-detail.spec.ts` — T7/T8 vitest
- `tests/ui/garden-grid-click.spec.ts` — T9 vitest
- `tests/smoke/plant-detail-modal.spec.ts` — T10 Playwright

### 수정 파일
- `src/state/migration.ts` — `migrateUserToV5` 추가 (`:214` 이후)
- `src/state/user.ts:3,20,55~60,94~117,142~172,183~188` — schemaVersion 5 / migration chain / **isValidUserShape `schemaVersion === 5` 분기 추가** / `recordDailyAnswer` inject / `updateStreakBanner`
- `src/ui/rewards.ts` — `'dg:reward:streak-freeze-used'` listener + toast (사전 review P0-1 fix: 본 프로젝트 reward listener는 `src/ui/rewards.ts`만, `src/state/rewards.ts` **미존재**)
- `src/ui/components/garden-grid.ts:51~57` — `<div>` → `<button>`
- `src/styles/components/garden.css` — button reset + `:focus-visible`
- `src/ui/tabs/stats.ts` — garden card click delegation
- `src/styles/components/modal.css` — `.plant-detail-modal` rule

### 사전 review 정정 (변경 0)
- ~`src/state/gameTypes.ts` `Snapshot.freezeCount` 추가~ → **불필요** (P0-2 fix: caller-side direct dispatch, sweep 우회)
- ~`src/state/achievements.ts` `takeSnapshot` 에 freezeCount 캡처~ → **불필요**
- ~`src/state/rewards.ts` runSweep delta detect~ → **파일 미존재 + 우회 후 불필요**
- ~`src/ui/messages.ts` MSG 신규 키~ → **inline 한국어 string 유지** (T4 step 5와 일관, 기존 banner도 inline)

---

## Task 0: Spec/Plan 작성 + 사전 review dispatch

**Files:**
- Created: `docs/superpowers/specs/2026-05-07-v3.21-plant-modal-streak-freeze-design.md` (이미 작성)
- Created: `docs/superpowers/plans/2026-05-07-v3.21-plant-modal-streak-freeze-plan.md` (본 파일)

- [ ] **Step 1: Spec 자체 검토 1회 + Plan 자체 검토 1회 (placeholder/모순/스코프/모호성)**

이미 spec 작성 시 self-review 1회 + dispatch count 정정 + R10 추가 + T6 grep 결과 정정 4건 반영 완료. Plan은 작성 직후 self-review 수행.

- [ ] **Step 2: 사용자에게 spec + plan 검토 요청**

```
spec/plan 작성 완료. 검토 후 변경 요청 또는 사전 review dispatch 진행 confirm.
```

- [ ] **Step 3: 사전 review dispatch (사용자 합의 시점)**

옵션 A — `superpowers:code-reviewer` agent dispatch (사용자 표준 patterns)
옵션 B — codex CLI (사용자 합의 시)

P0/P1 발견 시 in-cycle 반영, P2는 v3.22+ backlog.

- [ ] **Step 4: 사전 review fix commits (있다면)**

각 P0/P1 fix는 별도 commit. 표준 commit message: `fix(v3.21 사전 review P0-N): <issue>`.

- [ ] **Step 5: Commit T0 (spec + plan)**

```bash
git add docs/superpowers/specs/2026-05-07-v3.21-plant-modal-streak-freeze-design.md docs/superpowers/plans/2026-05-07-v3.21-plant-modal-streak-freeze-plan.md
git commit -m "docs(v3.21 T0): spec + plan + 사전 review dispatch"
```

---

## Task 1: Schema v4 → v5 lazy migration

**Files:**
- Modify: `src/state/user.ts:3,20,55~60`
- Modify: `src/state/migration.ts:214` (이후에 `migrateUserToV5` 추가)
- Test: `tests/state/migration-v5.spec.ts` (신규)

- [ ] **Step 1: 실패 테스트 작성**

```typescript
// tests/state/migration-v5.spec.ts
import { describe, it, expect } from 'vitest';
import { migrateUserToV5 } from '../../src/state/migration';

describe('migrateUserToV5', () => {
  it('v4 user → v5 with default streakFreeze {count: 2, lastEarnedAt: today}', () => {
    const v4 = { schemaVersion: 4, streak: 5, lastActiveDate: '2026-05-06' };
    const v5 = migrateUserToV5(v4);
    expect(v5.schemaVersion).toBe(5);
    expect(v5.streakFreeze.count).toBe(2);
    expect(v5.streakFreeze.lastEarnedAt).toMatch(/^\d{4}-\d{2}-\d{2}$/);
  });

  it('v5 user → v5 idempotent', () => {
    const v5In = { schemaVersion: 5, streakFreeze: { count: 1, lastEarnedAt: '2026-05-01' } };
    const v5Out = migrateUserToV5(v5In);
    expect(v5Out).toEqual(v5In);
  });

  it('손상된 streakFreeze (count NaN) → default 복구', () => {
    const corrupt = { schemaVersion: 4, streakFreeze: { count: NaN, lastEarnedAt: '' } };
    const fixed = migrateUserToV5(corrupt);
    expect(fixed.streakFreeze.count).toBe(2);
    expect(fixed.streakFreeze.lastEarnedAt).toMatch(/^\d{4}-\d{2}-\d{2}$/);
  });

  it('count 음수 → cap 0으로 clamp 또는 default 복구', () => {
    const neg = { schemaVersion: 4, streakFreeze: { count: -1, lastEarnedAt: '2026-05-01' } };
    const fixed = migrateUserToV5(neg);
    expect(fixed.streakFreeze.count).toBeGreaterThanOrEqual(0);
    expect(fixed.streakFreeze.count).toBeLessThanOrEqual(2);
  });

  it('count > 2 → cap 2로 clamp', () => {
    const over = { schemaVersion: 4, streakFreeze: { count: 5, lastEarnedAt: '2026-05-01' } };
    const fixed = migrateUserToV5(over);
    expect(fixed.streakFreeze.count).toBe(2);
  });
});
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

```bash
npm test -- tests/state/migration-v5.spec.ts
```

Expected: FAIL with "migrateUserToV5 is not exported".

- [ ] **Step 3: `migrateUserToV5` 구현 (사전 review P0/P1 fix 포함)**

`src/state/migration.ts` 끝에 추가:

```typescript
import { getDateStr } from '../utils/dates';  // 사전 review: 정확 path는 src/utils/dates (NOT dateStr)

export function migrateUserToV5(u: unknown): User {
  const r = u as Record<string, unknown> & {
    schemaVersion?: number;
    streakFreeze?: unknown;
  };

  if (r.schemaVersion === 5) return r as unknown as User;

  // v4 → v5 lazy: streakFreeze 신규 또는 손상 시 default
  const today = getDateStr();
  const sf = r.streakFreeze as { count?: unknown; lastEarnedAt?: unknown } | undefined;

  const validCount =
    sf !== undefined && sf !== null
    && Number.isFinite(sf.count) && (sf.count as number) >= 0 && (sf.count as number) <= 2;
  const validDate =
    sf !== undefined && sf !== null
    && typeof sf.lastEarnedAt === 'string' && sf.lastEarnedAt.length > 0;

  const streakFreeze =
    validCount && validDate
      ? { count: sf!.count as number, lastEarnedAt: sf!.lastEarnedAt as string }
      : { count: 2, lastEarnedAt: today };

  return {
    ...r,
    schemaVersion: 5 as const,
    streakFreeze,
  } as unknown as User;
}
```

> **사전 review fix**:
> - import path = `../utils/dates` (NOT `../utils/dateStr` — 그 파일 미존재)
> - `r.streakFreeze` undefined / null guard 추가 (v4 user는 streakFreeze 자체 부재)

- [ ] **Step 4: `User` 인터페이스 + `schemaVersion` 갱신 + 테스트 ripple 사전 인지**

`src/state/user.ts:14~24` 부근에서 `User` 인터페이스에 `streakFreeze: { count: number; lastEarnedAt: string }` 추가, `schemaVersion: 5` 변경.

`src/state/user.ts:3` `migrateUserToV4` import에 `migrateUserToV5` 추가.

`src/state/user.ts:55~60` migration chain 갱신:
```typescript
let user = (parsed?.schemaVersion === 2 || parsed?.schemaVersion === 3 || parsed?.schemaVersion === 4 || parsed?.schemaVersion === 5)
  ? (parsed as User)
  : migrateUserToV2(parsed);
user = migrateUserToV3(user);
user = migrateUserToV4(user);
user = migrateUserToV5(user);
```

> **사전 review P1 fix — 테스트 ripple 의무 인지**:
> - `tests/`에서 `schemaVersion: 4` 직접 사용 = **16 files** (verified: `grep -rln "schemaVersion: 4" tests/ | wc -l`)
> - `tests/helpers/seed.ts`도 영향 (smoke seed에서 user shape 사용)
> - **Strategy**:
>   1. **신규 user 객체**는 `schemaVersion: 5` + `streakFreeze: {count: 2, lastEarnedAt: today}` 추가 (lazy migrate 사용 안 하는 directly-built mock).
>   2. **lazy migrate 의존**하는 테스트는 `schemaVersion: 4` 유지 가능 (loadUserData가 v4 → v5 자동 promote — idempotent fallthrough).
>   3. **isValidUserShape 직접 호출**하는 테스트는 v5 + streakFreeze 필수 (그 외는 v4 valid 유지).
> - 본 step에서는 **migration chain만** 갱신하고, 16 test files 정정은 **T3 step 9 (전체 vitest 회귀)**에서 fail 확인 → 정정 commit. 또는 T1 step 8 commit 분리.
> - 정정 패턴 단순: 신규 객체 build 시점에 `schemaVersion: 5` + `streakFreeze: {count: 2, lastEarnedAt: '2026-05-07'}` 동시 추가. sed/awk 가능 (T0 plan grep 후 list 확정).

- [ ] **Step 5: `isValidUserShape` 갱신 (사전 review P0/P1 fix — schemaVersion === 5 분기 추가)**

기존 `src/state/user.ts:94~117` `isValidUserShape`은 v4 `plantStateByInterest` nested guard만 가지고 있음 (line 106 `if (r.schemaVersion === 4)`). v5에 동등 분기 추가:

```typescript
// 기존 v4 분기 (line 106~115) 유지 — v5 user도 plantStateByInterest 동일 검증 필요
if (r.schemaVersion === 4 || r.schemaVersion === 5) {
  if (typeof r.plantStateByInterest !== 'object' || r.plantStateByInterest === null) return false;
  for (const plant of Object.values(r.plantStateByInterest as Record<string, unknown>)) {
    if (!plant || typeof plant !== 'object') return false;
    const p = plant as Record<string, unknown>;
    const stage = p.stage;
    if (typeof stage !== 'number' || !Number.isInteger(stage) || stage < 1 || stage > 5) return false;
    if (typeof p.cumulativeActivity !== 'number' || !Number.isFinite(p.cumulativeActivity)) return false;
  }
}

// v5 신규: streakFreeze nested guard
if (r.schemaVersion === 5) {
  const sf = r.streakFreeze as { count?: unknown; lastEarnedAt?: unknown } | undefined | null;
  if (!sf || typeof sf !== 'object') return false;
  if (typeof sf.count !== 'number' || !Number.isFinite(sf.count) || sf.count < 0 || sf.count > 2) return false;
  if (typeof sf.lastEarnedAt !== 'string' || sf.lastEarnedAt.length === 0) return false;
}
```

> **사전 review fix**:
> - 단순 `&&` 체인이 아닌 conditional branch (기존 v4 분기 패턴 일관성)
> - `streakFreeze.count` cap 2 / 음수 차단 / `lastEarnedAt` empty string 차단까지 모두 cover
> - v5 분기는 v4 + 추가 검증 — 기존 v4 분기 OR-condition 으로 합치고 v5 추가 분기 신설

- [ ] **Step 6: 테스트 실행 → 통과 확인**

```bash
npm test -- tests/state/migration-v5.spec.ts
```

Expected: PASS 5건.

- [ ] **Step 7: 회귀 검증**

```bash
npx tsc --noEmit && npm run lint
```

Expected: 0 errors.

- [ ] **Step 8: Commit**

```bash
git add src/state/migration.ts src/state/user.ts tests/state/migration-v5.spec.ts
git commit -m "feat(v3.21 T1): schema v4 → v5 lazy migration (streakFreeze field 추가)"
```

---

## Task 2: Freeze 충전 logic (regenerateFreeze)

**Files:**
- Create: `src/state/freezeEngine.ts`
- Test: `tests/state/freezeEngine.spec.ts` (신규, T3와 공유)

- [ ] **Step 1: 실패 테스트 작성**

```typescript
// tests/state/freezeEngine.spec.ts
import { describe, it, expect } from 'vitest';
import { regenerateFreeze } from '../../src/state/freezeEngine';
import type { User } from '../../src/state/user';

const baseUser = (overrides: Partial<User['streakFreeze']>): User => ({
  // minimal mock: only streakFreeze relevant for regen
  streakFreeze: { count: 0, lastEarnedAt: '2026-05-01', ...overrides },
} as User);

describe('regenerateFreeze', () => {
  it('gap 0d → no regen', () => {
    const u = baseUser({ count: 0, lastEarnedAt: '2026-05-07' });
    regenerateFreeze(u, new Date('2026-05-07T00:00:00+09:00'));
    expect(u.streakFreeze.count).toBe(0);
    expect(u.streakFreeze.lastEarnedAt).toBe('2026-05-07');
  });

  it('gap 7d → +1 regen', () => {
    const u = baseUser({ count: 0, lastEarnedAt: '2026-05-01' });
    regenerateFreeze(u, new Date('2026-05-08T00:00:00+09:00'));
    expect(u.streakFreeze.count).toBe(1);
    expect(u.streakFreeze.lastEarnedAt).toBe('2026-05-08');
  });

  it('gap 14d → +2 regen', () => {
    const u = baseUser({ count: 0, lastEarnedAt: '2026-05-01' });
    regenerateFreeze(u, new Date('2026-05-15T00:00:00+09:00'));
    expect(u.streakFreeze.count).toBe(2);
    expect(u.streakFreeze.lastEarnedAt).toBe('2026-05-15');
  });

  it('gap 21d + count 0 → cap 2로 clamp', () => {
    const u = baseUser({ count: 0, lastEarnedAt: '2026-05-01' });
    regenerateFreeze(u, new Date('2026-05-22T00:00:00+09:00'));
    expect(u.streakFreeze.count).toBe(2);
  });

  it('count 2 (cap) → no regen', () => {
    const u = baseUser({ count: 2, lastEarnedAt: '2026-05-01' });
    regenerateFreeze(u, new Date('2026-05-15T00:00:00+09:00'));
    expect(u.streakFreeze.count).toBe(2);
  });

  it('시계 역행 (now < lastEarnedAt) → no-op', () => {
    const u = baseUser({ count: 1, lastEarnedAt: '2026-05-15' });
    regenerateFreeze(u, new Date('2026-05-01T00:00:00+09:00'));
    expect(u.streakFreeze.count).toBe(1);
    expect(u.streakFreeze.lastEarnedAt).toBe('2026-05-15');
  });
});
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

```bash
npm test -- tests/state/freezeEngine.spec.ts
```

Expected: FAIL.

- [ ] **Step 3: `regenerateFreeze` 구현**

```typescript
// src/state/freezeEngine.ts
import type { User } from './user';
import { getDateStr } from '../utils/dates';  // 사전 review P1 fix: 정확 path는 src/utils/dates

const SEVEN_DAYS_MS = 7 * 86400_000;
const FREEZE_CAP = 2;

export function regenerateFreeze(u: User, now: Date): void {
  const lastEarnedMs = Date.parse(u.streakFreeze.lastEarnedAt + 'T00:00:00+09:00');
  if (!Number.isFinite(lastEarnedMs)) {
    // 손상된 lastEarnedAt → today으로 reset (count 유지)
    u.streakFreeze.lastEarnedAt = getDateStr(now);
    return;
  }

  const elapsedMs = now.getTime() - lastEarnedMs;
  if (elapsedMs < SEVEN_DAYS_MS) return;

  const quotient = Math.floor(elapsedMs / SEVEN_DAYS_MS);
  if (quotient <= 0) return; // 시계 역행 방어

  const room = FREEZE_CAP - u.streakFreeze.count;
  if (room <= 0) return;

  const grant = Math.min(quotient, room);
  u.streakFreeze.count += grant;

  // lastEarnedAt += quotient * 7d (전체 quotient만큼 advance — cap 도달해도 시간은 진행)
  const newLastEarnedMs = lastEarnedMs + quotient * SEVEN_DAYS_MS;
  u.streakFreeze.lastEarnedAt = getDateStr(new Date(newLastEarnedMs));
}
```

- [ ] **Step 4: 테스트 실행 → 통과 확인**

```bash
npm test -- tests/state/freezeEngine.spec.ts
```

Expected: PASS 6건.

- [ ] **Step 5: Commit**

```bash
git add src/state/freezeEngine.ts tests/state/freezeEngine.spec.ts
git commit -m "feat(v3.21 T2): regenerateFreeze (rolling 7d, cap 2)"
```

---

## Task 3: Freeze 소비 logic + recordDailyAnswer 주입

**Files:**
- Modify: `src/state/freezeEngine.ts` (`consumeFreezeForGap` 추가)
- Modify: `src/state/user.ts:142~172` (`recordDailyAnswer` 분기 확장)
- Test: `tests/state/freezeEngine.spec.ts` (확장) + `tests/state/user-record-with-freeze.spec.ts` (신규)

- [ ] **Step 1: `consumeFreezeForGap` 실패 테스트 추가**

```typescript
// tests/state/freezeEngine.spec.ts (확장)
import { consumeFreezeForGap } from '../../src/state/freezeEngine';

describe('consumeFreezeForGap', () => {
  it('gap 0 → consumed 0, preserved true (no-op)', () => {
    const u = baseUser({ count: 2 });
    const r = consumeFreezeForGap(u, 0);
    expect(r).toEqual({ consumed: 0, preserved: true });
    expect(u.streakFreeze.count).toBe(2);
  });

  it('gap 1 + count 1 → consumed 1, preserved true', () => {
    const u = baseUser({ count: 1 });
    const r = consumeFreezeForGap(u, 1);
    expect(r).toEqual({ consumed: 1, preserved: true });
    expect(u.streakFreeze.count).toBe(0);
  });

  it('gap 2 + count 2 → consumed 2, preserved true', () => {
    const u = baseUser({ count: 2 });
    const r = consumeFreezeForGap(u, 2);
    expect(r).toEqual({ consumed: 2, preserved: true });
    expect(u.streakFreeze.count).toBe(0);
  });

  it('gap 2 + count 1 → consumed 1, preserved false (insufficient)', () => {
    const u = baseUser({ count: 1 });
    const r = consumeFreezeForGap(u, 2);
    expect(r).toEqual({ consumed: 1, preserved: false });
    expect(u.streakFreeze.count).toBe(0);
  });

  it('gap 3 + count 0 → consumed 0, preserved false', () => {
    const u = baseUser({ count: 0 });
    const r = consumeFreezeForGap(u, 3);
    expect(r).toEqual({ consumed: 0, preserved: false });
  });
});
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

- [ ] **Step 3: `consumeFreezeForGap` 구현**

```typescript
// src/state/freezeEngine.ts (추가)
export function consumeFreezeForGap(
  u: User,
  gap: number,
): { consumed: number; preserved: boolean } {
  if (!Number.isFinite(gap) || gap <= 0) return { consumed: 0, preserved: true };
  const consumed = Math.min(gap, u.streakFreeze.count);
  u.streakFreeze.count -= consumed;
  return { consumed, preserved: consumed === gap };
}
```

- [ ] **Step 4: 테스트 실행 → 통과 확인**

- [ ] **Step 5: `recordDailyAnswer` 통합 테스트 작성**

```typescript
// tests/state/user-record-with-freeze.spec.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { recordDailyAnswer } from '../../src/state/user';
import { saveUser, loadUserData } from '../../src/state/user';

describe('recordDailyAnswer with freeze', () => {
  beforeEach(() => localStorage.clear());

  it('gap 0 (어제 활동) + freeze cap → streak +1, freeze 그대로', () => {
    const yesterday = new Date(Date.now() - 86400_000).toISOString().slice(0, 10);
    const u = makeMockUser({ streak: 5, lastActiveDate: yesterday, freeze: 2 });
    saveUser(u);
    recordDailyAnswer(0);
    const after = loadUserData()!;
    expect(after.streak).toBe(6);
    expect(after.streakFreeze.count).toBe(2);
  });

  it('gap 1 + freeze 1 → streak +1, freeze 0', () => {
    const twoDaysAgo = new Date(Date.now() - 2 * 86400_000).toISOString().slice(0, 10);
    const u = makeMockUser({ streak: 5, lastActiveDate: twoDaysAgo, freeze: 1 });
    saveUser(u);
    recordDailyAnswer(0);
    const after = loadUserData()!;
    expect(after.streak).toBe(6);
    expect(after.streakFreeze.count).toBe(0);
  });

  it('gap 2 + freeze 1 → streak reset 1, freeze 0 (insufficient)', () => {
    const threeDaysAgo = new Date(Date.now() - 3 * 86400_000).toISOString().slice(0, 10);
    const u = makeMockUser({ streak: 5, lastActiveDate: threeDaysAgo, freeze: 1 });
    saveUser(u);
    recordDailyAnswer(0);
    const after = loadUserData()!;
    expect(after.streak).toBe(1);
    expect(after.streakFreeze.count).toBe(0);
  });

  it('gap 2 + freeze 2 → streak +1, freeze 0 (covered)', () => {
    const threeDaysAgo = new Date(Date.now() - 3 * 86400_000).toISOString().slice(0, 10);
    const u = makeMockUser({ streak: 5, lastActiveDate: threeDaysAgo, freeze: 2 });
    saveUser(u);
    recordDailyAnswer(0);
    const after = loadUserData()!;
    expect(after.streak).toBe(6);
    expect(after.streakFreeze.count).toBe(0);
  });

  it('idempotent — 같은 날 재제출 시 streak/freeze 변화 없음', () => {
    const today = new Date().toISOString().slice(0, 10);
    const u = makeMockUser({ streak: 6, lastActiveDate: today, freeze: 2 });
    saveUser(u);
    recordDailyAnswer(0);
    recordDailyAnswer(0);
    const after = loadUserData()!;
    expect(after.streak).toBe(6);
    expect(after.streakFreeze.count).toBe(2);
  });

  it('신규 user (lastActiveDate empty) → streak 1, freeze 그대로', () => {
    const u = makeMockUser({ streak: 0, lastActiveDate: '', freeze: 2 });
    saveUser(u);
    recordDailyAnswer(0);
    const after = loadUserData()!;
    expect(after.streak).toBe(1);
    expect(after.streakFreeze.count).toBe(2);
  });

  it('순서 invariant — 1주 결석 + freeze 0 시 regen 먼저 → +1 freeze로 cover', () => {
    const eightDaysAgo = new Date(Date.now() - 8 * 86400_000).toISOString().slice(0, 10);
    const u = makeMockUser({ streak: 5, lastActiveDate: eightDaysAgo, freeze: 0, lastEarnedAt: eightDaysAgo });
    saveUser(u);
    recordDailyAnswer(0);
    const after = loadUserData()!;
    // regen 먼저 발동 → freeze 1로 충전 → gap 7 cover 불가 (gap=7, freeze=1) → reset
    // (이 테스트는 invariant 검증 — regen 안 했다면 freeze=0이라 reset이지만, regen 후라 유의미한 case)
    expect(after.streak).toBe(1);
  });
});

// helper
function makeMockUser(opts: { streak: number; lastActiveDate: string; freeze: number; lastEarnedAt?: string }) {
  const today = new Date().toISOString().slice(0, 10);
  return {
    schemaVersion: 5 as const,
    name: 'test',
    streak: opts.streak,
    lastActiveDate: opts.lastActiveDate,
    xp: 0,
    interests: [],
    plantStateByInterest: {},
    streakFreeze: { count: opts.freeze, lastEarnedAt: opts.lastEarnedAt ?? today },
    // 기타 default fields — 테스트별 보강
  } as User;
}
```

- [ ] **Step 6: 테스트 실행 → 실패 확인**

- [ ] **Step 7: `recordDailyAnswer` 분기 확장**

`src/state/user.ts:155~160` 영역을 다음으로 교체:

```typescript
import { regenerateFreeze, consumeFreezeForGap } from './freezeEngine';
// ...
if (u.lastActiveDate !== today) {
  // T3 step 1: regen 먼저 (1주+ 결석한 사용자가 진입 시 freeze 충전 후 cover 시도)
  regenerateFreeze(u, now);

  if (!u.lastActiveDate) {
    // 신규 user (lastActiveDate empty) → 결석 cover 무관, streak 1로 시작
    u.streak = 1;
  } else {
    // gap = today와 lastActiveDate 차이 - 1
    const lastMs = Date.parse(u.lastActiveDate + 'T00:00:00+09:00');
    const todayMs = Date.parse(today + 'T00:00:00+09:00');
    const gap = Math.max(0, Math.round((todayMs - lastMs) / 86400_000) - 1);
    const { preserved } = consumeFreezeForGap(u, gap);
    u.streak = preserved ? u.streak + 1 : 1;
  }
}
```

- [ ] **Step 8: 테스트 실행 → 통과 확인**

```bash
npm test -- tests/state/user-record-with-freeze.spec.ts tests/state/freezeEngine.spec.ts
```

Expected: PASS 5(consume) + 7(integration) = 12건. 기존 user.spec.ts 회귀 0건.

- [ ] **Step 9: 전체 vitest 회귀 검증**

```bash
npm test
```

Expected: 0 fail.

- [ ] **Step 10: Commit**

```bash
git add src/state/freezeEngine.ts src/state/user.ts tests/state/freezeEngine.spec.ts tests/state/user-record-with-freeze.spec.ts
git commit -m "feat(v3.21 T3): consumeFreezeForGap + recordDailyAnswer 주입 (Duolingo 정석)"
```

---

## Task 4: Streak banner UI (`🔥 N일 · ❄️ N`)

**Files:**
- Modify: `src/state/user.ts:183~188` (`updateStreakBanner`)
- Modify: `src/ui/messages.ts:13~48` (MSG 추가)
- Test: `tests/ui/streak-banner-freeze.spec.ts` (신규)

- [ ] **Step 1: 실패 테스트 작성**

```typescript
// tests/ui/streak-banner-freeze.spec.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { updateStreakBanner, saveUser } from '../../src/state/user';

describe('updateStreakBanner with freeze', () => {
  beforeEach(() => {
    document.body.innerHTML = '<div id="streakBanner"></div>';
    localStorage.clear();
  });

  it('streak 7 + freeze 2 → "🔥 7일 연속 성장 중 · ❄️ 2"', () => {
    saveUser(makeMockUser({ streak: 7, freeze: 2 }));
    updateStreakBanner();
    expect(document.getElementById('streakBanner')!.textContent).toBe('🔥 7일 연속 성장 중 · ❄️ 2');
  });

  it('streak 7 + freeze 0 → "🔥 7일 연속 성장 중" (❄️ 숨김)', () => {
    saveUser(makeMockUser({ streak: 7, freeze: 0 }));
    updateStreakBanner();
    expect(document.getElementById('streakBanner')!.textContent).toBe('🔥 7일 연속 성장 중');
  });

  it('streak 0 + freeze 2 → "오늘부터 다시 시작해봐요"(streak 0이 우선, freeze 무시)', () => {
    saveUser(makeMockUser({ streak: 0, freeze: 2 }));
    updateStreakBanner();
    expect(document.getElementById('streakBanner')!.textContent).toBe('오늘부터 다시 시작해봐요');
  });
});
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

- [ ] **Step 3: `updateStreakBanner` 갱신**

`src/state/user.ts:183~188`:

```typescript
export function updateStreakBanner(rootId = 'streakBanner'): void {
  const el = document.getElementById(rootId);
  const u = loadUserData();
  if (!el || !u) return;
  if (u.streak <= 0) {
    el.textContent = '오늘부터 다시 시작해봐요';
    return;
  }
  const freezeSuffix = u.streakFreeze.count > 0 ? ` · ❄️ ${u.streakFreeze.count}` : '';
  el.textContent = `🔥 ${u.streak}일 연속 성장 중${freezeSuffix}`;
}
```

- [ ] **Step 4: 테스트 실행 → 통과 확인**

- [ ] **Step 5: i18n 검토**

`src/ui/messages.ts`는 이 사이클에서 새 MSG 추가 없이 inline 한국어 string 유지 (기존 banner도 inline). 향후 full i18n에서 통합 (v3.7+ 별 사이클 — `messages.ts` 주석 참조).

- [ ] **Step 6: Commit**

```bash
git add src/state/user.ts tests/ui/streak-banner-freeze.spec.ts
git commit -m "feat(v3.21 T4): streak banner ❄️ N 표시 (count > 0일 때만)"
```

---

## Task 5: Freeze 소비 toast — caller-side direct dispatch (사전 review P0-2 fix)

**사전 review P0-2**: 초기 plan은 `Snapshot.freezeCount` delta + `runSweep` 흐름이었으나 — regen +1 → consume −1 → prev/curr 둘 다 0 → **net=0 false-negative**. 1주 결석 후 첫 답변 = 핵심 use case에서 toast 안 뜸. **`consumeFreezeForGap` 안에서 직접 dispatch로 우회**.

**사전 review P0-1**: `src/state/rewards.ts` 미존재 — 본 프로젝트 reward listener는 `src/ui/rewards.ts`만. sweep 우회 결정에 따라 자연 해소.

**Files:**
- Modify: `src/state/freezeEngine.ts` (`consumeFreezeForGap` 안에서 direct dispatch)
- Modify: `src/ui/rewards.ts` (`'dg:reward:streak-freeze-used'` listener 추가)
- Test: `tests/state/rewards-freeze-event.spec.ts` (신규)

**제거 (T0 plan에서 잘못 가정한 항목)**:
- ~`src/state/gameTypes.ts` `Snapshot.freezeCount` 추가~ → 불필요
- ~`src/state/achievements.ts:49~` `takeSnapshot` 에 freezeCount 캡처~ → 불필요
- ~`src/state/rewards.ts` 신규 sweep delta detect~ → 파일 미존재 + 우회로 불필요

- [ ] **Step 1: 실패 테스트 작성**

```typescript
// tests/state/rewards-freeze-event.spec.ts
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { saveUser, recordDailyAnswer } from '../../src/state/user';

describe('streak-freeze-used reward event (caller-side dispatch)', () => {
  beforeEach(() => localStorage.clear());

  it('consumed 1 + preserved → dispatch with detail {days: 1}', () => {
    const handler = vi.fn();
    window.addEventListener('dg:reward:streak-freeze-used', handler);

    // gap 1 + freeze 1 → consume 1 (T3 시나리오 재사용)
    const twoDaysAgo = new Date(Date.now() - 2 * 86400_000).toISOString().slice(0, 10);
    saveUser(makeMockUser({ streak: 5, lastActiveDate: twoDaysAgo, freeze: 1 }));
    recordDailyAnswer(0);

    expect(handler).toHaveBeenCalledOnce();
    expect((handler.mock.calls[0][0] as CustomEvent).detail).toEqual({ days: 1 });
  });

  it('consumed 0 → no dispatch', () => {
    const handler = vi.fn();
    window.addEventListener('dg:reward:streak-freeze-used', handler);

    const yesterday = new Date(Date.now() - 86400_000).toISOString().slice(0, 10);
    saveUser(makeMockUser({ streak: 5, lastActiveDate: yesterday, freeze: 2 }));
    recordDailyAnswer(0);

    expect(handler).not.toHaveBeenCalled();
  });

  it('preserved=false (freeze 부족 reset) → no dispatch', () => {
    const handler = vi.fn();
    window.addEventListener('dg:reward:streak-freeze-used', handler);

    const threeDaysAgo = new Date(Date.now() - 3 * 86400_000).toISOString().slice(0, 10);
    saveUser(makeMockUser({ streak: 5, lastActiveDate: threeDaysAgo, freeze: 1 }));
    recordDailyAnswer(0);

    expect(handler).not.toHaveBeenCalled();
  });

  it('saveUser throw 시 dispatch 안 함 (caller-side guard)', () => {
    const handler = vi.fn();
    window.addEventListener('dg:reward:streak-freeze-used', handler);

    const twoDaysAgo = new Date(Date.now() - 2 * 86400_000).toISOString().slice(0, 10);
    saveUser(makeMockUser({ streak: 5, lastActiveDate: twoDaysAgo, freeze: 1 }));

    // Quota mock — saveUser 단계에서만 throw (초기 saveUser 후에 mock 시작)
    const setItemSpy = vi.spyOn(Storage.prototype, 'setItem').mockImplementation((key) => {
      if (key === 'dg.user') throw new DOMException('Quota', 'QuotaExceededError');
    });

    expect(() => recordDailyAnswer(0)).toThrow();
    expect(handler).not.toHaveBeenCalled();
    setItemSpy.mockRestore();
  });
});
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

```bash
npm test -- tests/state/rewards-freeze-event.spec.ts
```

Expected: FAIL with "no event dispatched".

- [ ] **Step 3: `consumeFreezeForGap`에 direct dispatch 추가**

`src/state/freezeEngine.ts`의 `consumeFreezeForGap` 끝에 dispatch 추가:

```typescript
export function consumeFreezeForGap(
  u: User,
  gap: number,
): { consumed: number; preserved: boolean } {
  if (!Number.isFinite(gap) || gap <= 0) return { consumed: 0, preserved: true };
  const consumed = Math.min(gap, u.streakFreeze.count);
  u.streakFreeze.count -= consumed;
  const preserved = consumed === gap;

  // 사전 review P0-2 fix: caller-side direct dispatch (sweep 우회)
  // saveUser throw 시 dispatch 차단은 recordDailyAnswer try/catch 또는 dispatch defer로 처리.
  if (consumed > 0 && preserved) {
    window.dispatchEvent(
      new CustomEvent('dg:reward:streak-freeze-used', { detail: { days: consumed } })
    );
  }

  return { consumed, preserved };
}
```

> **saveUser throw 보호**: 현재 `recordDailyAnswer:142~172`의 흐름은
> 1. lazy regen + consume + streak update + xp + missions (in-memory mutate)
> 2. `saveUser(u)` (Quota throw 시 in-memory만 mutate, persist 실패)
>
> dispatch가 step 1 안에서 발생 → step 2 throw 시 toast가 떴는데 persist 실패. **수용** (toast는 일시적 UX, persist 실패는 다음 답변 시 재시도). false-fire 정책 (v3.12 invariant)와 trade-off 명시.
>
> **대안 (필요 시)**: `recordDailyAnswer`에서 consume 결과를 캡처 → saveUser 성공 후 dispatch. 이는 T3 step 7에서 inject할 때 정합성 보장 가능. P1 후보 — 사용자 결정에 따름.

- [ ] **Step 4: UI listener 추가**

`src/ui/rewards.ts:139~ ` 부근 (기존 `disposers.push(on(...))` 패턴 일관) 에 추가:

```typescript
disposers.push(on('dg:reward:streak-freeze-used', ({ days }) => {
  // 기존 toast 함수 (예: showToast 또는 fireToast) 사용 — T0 grep으로 정확 함수명 확인
  showToast(`❄️ Streak Freeze로 ${days}일 연속 유지!`);
}));
```

> **사전 review P1 fix**: `on()` helper와 `showToast`(또는 `fireToast`) 정확 함수명/시그니처는 T0 plan 단계에서 `src/ui/rewards.ts:139~` 인접 코드 grep 후 fold (`disposers.push(on('dg:reward:badge-unlock', ...))` 패턴 차용).

- [ ] **Step 5: 테스트 실행 → 통과 확인**

```bash
npm test -- tests/state/rewards-freeze-event.spec.ts
```

Expected: PASS 4건.

- [ ] **Step 6: 회귀 검증**

```bash
npx tsc --noEmit && npm test
```

Expected: 0 fail (T1~T4 + T5 누적 = 27 신규).

- [ ] **Step 7: Commit**

```bash
git add src/state/freezeEngine.ts src/ui/rewards.ts tests/state/rewards-freeze-event.spec.ts
git commit -m "feat(v3.21 T5): streak-freeze-used direct dispatch (사전 review P0-2 fix: sweep 우회)"
```

---

## Task 6: 분야별 answer/scrap count helper

**Files:**
- Create: `src/utils/interestCounts.ts`
- Test: `tests/utils/interestCounts.spec.ts` (신규)

- [ ] **Step 1: 실패 테스트 작성**

```typescript
// tests/utils/interestCounts.spec.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { getAnswerCountByInterest, getScrapCountByInterest } from '../../src/utils/interestCounts';
import { saveAnswers } from '../../src/state/persistence';
import { saveBriefings } from '../../src/state/briefings';

describe('interestCounts', () => {
  beforeEach(() => localStorage.clear());

  it('Answer text에 키워드 포함 시 count', () => {
    saveAnswers([
      { id: '1', text: '리더십에 대한 답변', text2: '', schemaVersion: 1 } as any,
      { id: '2', text: 'AI/ML 관련', schemaVersion: 1 } as any,
    ]);
    expect(getAnswerCountByInterest('leadership')).toBeGreaterThan(0);
    expect(getAnswerCountByInterest('ai_ml')).toBeGreaterThan(0);
  });

  it('Briefing scrapped + 키워드 포함 시 count', () => {
    saveBriefings([
      { title: '리더십 trend', summary: '...', scrapped: true } as any,
      { title: 'AI news', summary: '...', scrapped: false } as any,  // not scrapped
    ]);
    expect(getScrapCountByInterest('leadership')).toBe(1);
    expect(getScrapCountByInterest('ai_ml')).toBe(0);
  });

  it('0건 — 빈 storage', () => {
    expect(getAnswerCountByInterest('leadership')).toBe(0);
    expect(getScrapCountByInterest('leadership')).toBe(0);
  });

  it('다중 interest 매칭 — 둘 다 count', () => {
    saveAnswers([
      { id: '1', text: '리더십과 AI 둘 다 다룬 답변', schemaVersion: 1 } as any,
    ]);
    expect(getAnswerCountByInterest('leadership')).toBe(1);
    expect(getAnswerCountByInterest('ai_ml')).toBe(1);
  });

  it('사용자 정의 분야 (interestKeywords 빈 배열) → count 0', () => {
    saveAnswers([{ id: '1', text: '리더십', schemaVersion: 1 } as any]);
    expect(getAnswerCountByInterest('user_custom_interest')).toBe(0);
  });
});
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

- [ ] **Step 3: 구현**

```typescript
// src/utils/interestCounts.ts
import { loadAnswers } from '../state/persistence';
import { loadBriefings } from '../state/briefings';
import { interestKeywords, matchKeyword } from './interestKeywords';

export function getAnswerCountByInterest(interestId: string): number {
  const keywords = interestKeywords(interestId);
  if (keywords.length === 0) return 0;
  return loadAnswers().filter(a => {
    const hay = (a.text ?? '') + ' ' + (a.questionText ?? '');
    return keywords.some(k => matchKeyword(hay, k));
  }).length;
}

export function getScrapCountByInterest(interestId: string): number {
  const keywords = interestKeywords(interestId);
  if (keywords.length === 0) return 0;
  return loadBriefings().filter(b => {
    if (!b.scrapped) return false;
    const hay = (b.title ?? '') + ' ' + (b.summary ?? '');
    return keywords.some(k => matchKeyword(hay, k));
  }).length;
}
```

- [ ] **Step 4: 테스트 실행 → 통과 확인**

```bash
npm test -- tests/utils/interestCounts.spec.ts
```

Expected: PASS 5건.

- [ ] **Step 5: Commit**

```bash
git add src/utils/interestCounts.ts tests/utils/interestCounts.spec.ts
git commit -m "feat(v3.21 T6): interestCounts helper (answer/scrap by interest)"
```

---

## Task 7: Plant detail modal markup + 셸

**Files:**
- Create: `src/ui/modals/plant-detail.ts`
- Modify: `src/styles/components/modal.css` (`.plant-detail-modal` 추가)
- Test: `tests/ui/modals/plant-detail.spec.ts` (신규)

- [ ] **Step 1: 실패 테스트 작성**

```typescript
// tests/ui/modals/plant-detail.spec.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { openPlantDetailModal } from '../../../src/ui/modals/plant-detail';
import { saveUser } from '../../../src/state/user';
import { closeModal } from '../../../src/ui/modals/shared';

describe('plant-detail modal shell', () => {
  beforeEach(() => {
    document.body.innerHTML = '';
    localStorage.clear();
  });

  it('openPlantDetailModal — modal DOM 존재', () => {
    saveUser(makeMockUser({
      interests: ['leadership'],
      plantStateByInterest: { leadership: { stage: 2, cumulativeActivity: 10 } as any },
    }));
    openPlantDetailModal('leadership');
    expect(document.querySelector('.dg-modal')).toBeTruthy();          // 사전 review P0-3 fix: shared.ts:33 className
    expect(document.querySelector('.plant-detail-modal')).toBeTruthy();
  });

  it('closeModal() 호출 시 DOM 제거', () => {
    saveUser(makeMockUser({
      interests: ['leadership'],
      plantStateByInterest: { leadership: { stage: 2, cumulativeActivity: 10 } as any },
    }));
    openPlantDetailModal('leadership');
    closeModal();
    expect(document.querySelector('.dg-modal')).toBeFalsy();           // 사전 review P0-3 fix
  });
});
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

- [ ] **Step 3: shell 구현**

```typescript
// src/ui/modals/plant-detail.ts
import { openModal } from './shared';
import { loadUserData } from '../../state/user';
import { INTEREST_LABEL } from '../components/garden-grid';

export function openPlantDetailModal(interestId: string): void {
  const u = loadUserData();
  if (!u) return;
  const plant = u.plantStateByInterest[interestId];
  if (!plant) return;

  const title = INTEREST_LABEL[interestId] ?? interestId;
  const bodyHtml = renderBody(u, interestId, plant);  // T8에서 구현

  openModal({
    title: `${title} 정원`,
    bodyHtml: `<div class="plant-detail-modal">${bodyHtml}</div>`,
  });
}

function renderBody(u: User, interestId: string, plant: PlantState): string {
  // T8 placeholder — 다음 task에서 구체화
  return '';
}
```

- [ ] **Step 4: CSS 추가**

`src/styles/components/modal.css` 끝:

```css
.plant-detail-modal {
  display: flex;
  flex-direction: column;
  gap: var(--space-md);
  padding: var(--space-md) 0;
}
```

(나머지 detail rule은 T8에서 추가.)

- [ ] **Step 5: 테스트 실행 → 통과 확인**

- [ ] **Step 6: Commit**

```bash
git add src/ui/modals/plant-detail.ts src/styles/components/modal.css tests/ui/modals/plant-detail.spec.ts
git commit -m "feat(v3.21 T7): plant-detail modal shell (shared.openModal 차용)"
```

---

## Task 8: Modal content render (Standard 6 항목)

**Files:**
- Modify: `src/ui/modals/plant-detail.ts` (`renderBody` 구체화)
- Modify: `src/styles/components/modal.css` (detail content style)
- Test: `tests/ui/modals/plant-detail.spec.ts` (확장 +6)

- [ ] **Step 1: 실패 테스트 추가**

```typescript
// tests/ui/modals/plant-detail.spec.ts (확장)
describe('plant-detail content', () => {
  beforeEach(() => {
    document.body.innerHTML = '';
    localStorage.clear();
  });

  it('stage 1 (씨앗) — 정확한 emoji + 라벨', () => {
    saveUser(makeMockUser({
      interests: ['leadership'],
      plantStateByInterest: { leadership: { stage: 1, cumulativeActivity: 3 } as any },
    }));
    openPlantDetailModal('leadership');
    const body = document.querySelector('.plant-detail-modal')!.textContent!;
    expect(body).toContain('씨앗');
    expect(body).toContain('🌱');
  });

  it('stage 5 (만개) — 분야별 emoji + ✨ + unlockedAt 표시', () => {
    saveUser(makeMockUser({
      interests: ['leadership'],
      plantStateByInterest: {
        leadership: { stage: 5, cumulativeActivity: 200, unlockedAt: '2026-04-15T00:00:00.000Z' } as any,
      },
    }));
    openPlantDetailModal('leadership');
    const body = document.querySelector('.plant-detail-modal')!.textContent!;
    expect(body).toContain('만개');
    expect(body).toContain('👑');  // leadership stage 5
    expect(body).toContain('✨');
    expect(body).toContain('2026년 4월 15일');
  });

  it('progress — stage 2, cum 15 → 다음 stage(3) threshold 25 표시', () => {
    saveUser(makeMockUser({
      interests: ['leadership'],
      plantStateByInterest: { leadership: { stage: 2, cumulativeActivity: 15 } as any },
    }));
    openPlantDetailModal('leadership');
    const body = document.querySelector('.plant-detail-modal')!.textContent!;
    expect(body).toMatch(/15.*25/);  // 15 / 25 form
  });

  it('unlockedAt absent (stage < 5) → ✨ 항목 hide', () => {
    saveUser(makeMockUser({
      interests: ['leadership'],
      plantStateByInterest: { leadership: { stage: 3, cumulativeActivity: 50 } as any },
    }));
    openPlantDetailModal('leadership');
    const body = document.querySelector('.plant-detail-modal')!.textContent!;
    expect(body).not.toContain('✨');
  });

  it('wilting — lastEngagedAt 7일 이상 → wilting 표시', () => {
    const eightDaysAgo = new Date(Date.now() - 8 * 86400_000).toISOString();
    saveUser(makeMockUser({
      interests: ['leadership'],
      plantStateByInterest: {
        leadership: { stage: 3, cumulativeActivity: 50, lastEngagedAt: eightDaysAgo } as any,
      },
    }));
    openPlantDetailModal('leadership');
    const body = document.querySelector('.plant-detail-modal')!.textContent!;
    expect(body).toContain('🥀');
  });

  it('사용자 정의 분야 → fallback 🌸 (stage 5) + count 0', () => {
    saveUser(makeMockUser({
      interests: ['my_custom_interest'],
      plantStateByInterest: {
        my_custom_interest: { stage: 5, cumulativeActivity: 200 } as any,
      },
    }));
    openPlantDetailModal('my_custom_interest');
    const body = document.querySelector('.plant-detail-modal')!.textContent!;
    expect(body).toContain('🌸');
    expect(body).toContain('0개 답변');
    expect(body).toContain('0개 스크랩');
  });
});
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

- [ ] **Step 3: `renderBody` 구체화**

```typescript
// src/ui/modals/plant-detail.ts
import { getPlantIcon, STAGE_LABEL, STAGE_THRESHOLDS, TROPHY_MARK } from '../../state/plantCatalog';
import { checkWilting } from '../../state/plantEngine';
import { escapeHtml } from '../../utils/escapeHtml';
import { getAnswerCountByInterest, getScrapCountByInterest } from '../../utils/interestCounts';

function renderBody(u: User, interestId: string, plant: PlantState): string {
  const icon = escapeHtml(getPlantIcon(interestId, plant.stage));
  const stageLabel = escapeHtml(STAGE_LABEL[plant.stage]);
  const cum = Number.isFinite(plant.cumulativeActivity) ? Math.floor(plant.cumulativeActivity) : 0;

  // 1. Stage badge
  const stageHtml = `
    <div class="plant-detail-stage">
      <span class="plant-detail-emoji">${icon}</span>
      <span class="plant-detail-stage-label">${stageLabel}</span>
    </div>`;

  // 2. Progress
  const progressHtml = plant.stage === 5
    ? `<div class="plant-detail-progress">만개 ${escapeHtml(TROPHY_MARK)}</div>`
    : `<div class="plant-detail-progress">${cum} / ${STAGE_THRESHOLDS[plant.stage - 1]}</div>`;

  // 3. Unlocked
  const unlockedHtml = plant.unlockedAt
    ? `<div class="plant-detail-unlocked">✨ ${escapeHtml(formatKoreanDate(plant.unlockedAt))} 도달</div>`
    : '';

  // 4. Last engaged
  const engagedHtml = plant.lastEngagedAt
    ? `<div class="plant-detail-engaged">마지막 활동 ${escapeHtml(formatRelative(plant.lastEngagedAt))}</div>`
    : `<div class="plant-detail-engaged">활동 기록 없음</div>`;

  // 5. Wilting
  const wilting = checkWilting(plant, new Date());
  const wiltingHtml = wilting
    ? `<div class="plant-detail-wilting">🥀 7일 이상 활동이 없어요. 다시 가꿔주세요</div>`
    : '';

  // 6. Counts
  const answers = getAnswerCountByInterest(interestId);
  const scraps = getScrapCountByInterest(interestId);
  const countsHtml = `<div class="plant-detail-counts">${answers}개 답변 · ${scraps}개 스크랩</div>`;

  return stageHtml + progressHtml + unlockedHtml + engagedHtml + wiltingHtml + countsHtml;
}

function formatKoreanDate(iso: string): string {
  const d = new Date(iso);
  return `${d.getFullYear()}년 ${d.getMonth() + 1}월 ${d.getDate()}일`;
}

function formatRelative(iso: string): string {
  const ms = Date.now() - new Date(iso).getTime();
  const days = Math.floor(ms / 86400_000);
  if (days === 0) return '오늘';
  if (days === 1) return '어제';
  return `${days}일 전`;
}
```

- [ ] **Step 4: CSS 보강**

`src/styles/components/modal.css`:

```css
.plant-detail-stage {
  display: flex;
  align-items: center;
  gap: var(--space-sm);
  font-size: var(--font-size-lg);
}
.plant-detail-emoji { font-size: 2rem; }
.plant-detail-progress,
.plant-detail-unlocked,
.plant-detail-engaged,
.plant-detail-counts {
  color: var(--text-secondary);
}
.plant-detail-wilting {
  color: var(--color-warning);
  background: var(--bg-warning-soft);
  padding: var(--space-sm);
  border-radius: var(--radius-sm);
}
```

- [ ] **Step 5: 테스트 실행 → 통과 확인**

- [ ] **Step 6: Commit**

```bash
git add src/ui/modals/plant-detail.ts src/styles/components/modal.css tests/ui/modals/plant-detail.spec.ts
git commit -m "feat(v3.21 T8): plant-detail modal content render (Standard 6 항목)"
```

---

## Task 9: garden-grid click wiring + a11y

**Files:**
- Modify: `src/ui/components/garden-grid.ts:51~57` (`<div>` → `<button>`)
- Modify: `src/styles/components/garden.css` (button reset + focus-visible)
- Modify: `src/ui/tabs/stats.ts` (click delegation)
- Test: `tests/ui/garden-grid-click.spec.ts` (신규)

- [ ] **Step 1: 실패 테스트 작성**

```typescript
// tests/ui/garden-grid-click.spec.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { renderGardenGrid } from '../../src/ui/components/garden-grid';
import { saveUser, loadUserData } from '../../src/state/user';
import { wireGardenGridClicks } from '../../src/ui/tabs/stats';   // T9 step 5에서 export

describe('garden-grid click wiring (사전 review P0-4 fix)', () => {
  beforeEach(() => {
    document.body.innerHTML = '<div id="root"></div>';
    localStorage.clear();
  });

  it('garden card는 <button>으로 렌더 + aria-label 보유', () => {
    const root = document.getElementById('root')!;
    saveUser(makeMockUser({
      interests: ['leadership'],
      plantStateByInterest: { leadership: { stage: 2, cumulativeActivity: 10 } as any },
    }));
    renderGardenGrid(root, loadUserData()!);

    const card = root.querySelector('.garden-card[data-interest-id="leadership"]') as HTMLButtonElement;
    expect(card.tagName).toBe('BUTTON');
    expect(card.getAttribute('aria-label')).toContain('정원');
  });

  it('garden card click + wireup 발동 → modal open', () => {
    const root = document.getElementById('root')!;
    saveUser(makeMockUser({
      interests: ['leadership'],
      plantStateByInterest: { leadership: { stage: 2, cumulativeActivity: 10 } as any },
    }));
    renderGardenGrid(root, loadUserData()!);
    wireGardenGridClicks(root);                                  // 사전 review P0-4 fix: wireup 명시 발동

    const card = root.querySelector('.garden-card[data-interest-id="leadership"]') as HTMLButtonElement;
    card.click();
    expect(document.querySelector('.plant-detail-modal')).toBeTruthy();
  });

  it('Esc key → modal close (shared.openModal이 처리)', () => {
    const root = document.getElementById('root')!;
    saveUser(makeMockUser({
      interests: ['leadership'],
      plantStateByInterest: { leadership: { stage: 2, cumulativeActivity: 10 } as any },
    }));
    renderGardenGrid(root, loadUserData()!);
    wireGardenGridClicks(root);

    const card = root.querySelector('.garden-card[data-interest-id="leadership"]') as HTMLButtonElement;
    card.click();
    expect(document.querySelector('.plant-detail-modal')).toBeTruthy();

    document.dispatchEvent(new KeyboardEvent('keydown', { key: 'Escape' }));
    expect(document.querySelector('.dg-modal')).toBeFalsy();
  });
});
```

> **사전 review P0-4 fix**: 기존 plan은 `renderGardenGrid` 호출 후 stats.ts wireup 없이 click → modal open 검증을 시도. 실제로는 click handler가 attach 안 돼서 OPEN 발동 안 함. **`wireGardenGridClicks`을 stats.ts에서 export하고 test에서 명시 호출**.

- [ ] **Step 2: 테스트 실행 → 실패 확인**

- [ ] **Step 3: `<div>` → `<button>` 변경**

`src/ui/components/garden-grid.ts:51~57`:

```typescript
return `
  <button type="button" class="garden-card${wiltClass}${bloomClass}" data-interest-id="${escapeHtml(id)}" aria-label="${label} 정원 정보">
    <span class="garden-emoji">${icon}</span>
    <span class="garden-name">${label}${trophy}</span>
    <span class="garden-stage-label">${stageLabel}</span>
    <span class="garden-cum-count">${cumCount}회</span>
  </button>`;
```

- [ ] **Step 4: CSS button reset 추가**

`src/styles/components/garden.css` `.garden-card` rule 확장:

```css
.garden-card {
  /* button reset */
  background: var(--bg-card);
  border: 1px solid var(--border);
  cursor: pointer;
  text-align: left;
  font: inherit;
  /* ... 기존 layout */
}
.garden-card:focus-visible {
  outline: 2px solid var(--primary-vivid);
  outline-offset: 2px;
}
.garden-card:hover {
  /* 기존 hover 스타일 유지 */
}
```

- [ ] **Step 5: stats.ts click delegation (사전 review P0-4 fix: export 명시)**

`src/ui/tabs/stats.ts` 안에 export 함수로 명시 (test에서 import 가능):

```typescript
import { openPlantDetailModal } from '../modals/plant-detail';

/** stats 탭 mount 시 1회 호출 — garden card click delegation. */
export function wireGardenGridClicks(rootEl: HTMLElement): void {
  rootEl.addEventListener('click', (e) => {
    const target = (e.target as HTMLElement).closest<HTMLButtonElement>('.garden-card[data-interest-id]');
    if (!target) return;
    const id = target.dataset.interestId;
    if (id) openPlantDetailModal(id);
  });
}
```

stats 탭 mount 흐름 안에서 `wireGardenGridClicks(statsRootEl)` 호출 — 정확한 mount hook 위치는 T0 plan 단계에서 `src/ui/tabs/stats.ts` 안의 render/init 함수 grep 후 fold.

> **idempotency 주의**: stats 탭이 매번 re-render되면 listener 중복 등록 위험. 옵션 A — `dataset.gridWired` flag로 1회 가드 / 옵션 B — `AbortController` 패턴 (`shared.ts:62~68` 참고). plan T0 단계에서 stats 탭 lifecycle 확인 후 결정.

- [ ] **Step 6: 테스트 실행 → 통과 확인**

- [ ] **Step 7: 수동 시각 1회 (R6 mitigation)**

```bash
npm run dev
```

Browser 열고 stats 탭 → garden grid 시각 확인 (button 변경 후 카드 형태 보존, focus-visible ring 정상).

- [ ] **Step 8: Commit**

```bash
git add src/ui/components/garden-grid.ts src/styles/components/garden.css src/ui/tabs/stats.ts tests/ui/garden-grid-click.spec.ts
git commit -m "feat(v3.21 T9): garden-grid <button> + click → plant-detail modal"
```

---

## Task 10: Plant detail modal smoke spec

**Files:**
- Create: `tests/smoke/plant-detail-modal.spec.ts`

- [ ] **Step 1: smoke spec 작성 (사전 review P0-5 fix — primeOnboardedUser + plant state seed)**

```typescript
// tests/smoke/plant-detail-modal.spec.ts
import { test, expect } from '@playwright/test';
import { primeOnboardedUser } from '../helpers/seed';   // 사전 review P0-5 fix: 기존 helper

test.use({ serviceWorkers: 'block' });

test('plant-detail modal opens and shows Standard content', async ({ page }) => {
  // 1) onboarded user with leadership interest + plantState seed
  await primeOnboardedUser(page);
  await page.addInitScript(() => {
    const userRaw = localStorage.getItem('dg.user');
    if (!userRaw) return;
    const u = JSON.parse(userRaw);
    u.interests = ['leadership'];
    u.plantStateByInterest = { leadership: { stage: 2, cumulativeActivity: 10 } };
    localStorage.setItem('dg.user', JSON.stringify(u));
  });

  await page.goto('/');

  // 2) stats 탭으로 이동
  await page.getByRole('tab', { name: /성장|stats/ }).click();

  // 3) garden card 클릭
  const card = page.locator('.garden-card[data-interest-id="leadership"]');
  await expect(card).toBeVisible();
  await card.click();

  // 4) modal Standard 콘텐츠 검증
  const modal = page.locator('.plant-detail-modal');
  await expect(modal).toBeVisible();
  await expect(modal).toContainText(/씨앗|새싹|줄기|봉오리|만개/);
  await expect(modal).toContainText(/개 답변/);
  await expect(modal).toContainText(/개 스크랩/);

  // 5) Esc → close
  await page.keyboard.press('Escape');
  await expect(modal).not.toBeVisible();
});
```

> **사전 review P0-5 fix**: 초기 plan은 `seedAnswerOnce` helper import — **존재하지 않음**. 본 프로젝트 smoke 표준 helper는 `tests/helpers/seed.ts`의 `primeOnboardedUser` (cardnews.spec.ts/v3.13-missions.spec.ts 등 패턴). primeOnboardedUser + `addInitScript`로 plantState seed 직접 inject.

- [ ] **Step 2: smoke 실행**

```bash
npm run test:smoke -- tests/smoke/plant-detail-modal.spec.ts
```

Expected: 1 PASS.

- [ ] **Step 3: N=10 cold-fork (R7 mitigation)**

```bash
for i in $(seq 1 10); do
  rm -rf node_modules/.vite-temp dist
  npm run test:smoke -- tests/smoke/plant-detail-modal.spec.ts || echo "FAIL on $i"
done
```

Expected: 0/10 fail.

- [ ] **Step 4: 전체 smoke 회귀**

```bash
npm run test:smoke
```

Expected: 97/97 (96 → 97).

- [ ] **Step 5: Commit**

```bash
git add tests/smoke/plant-detail-modal.spec.ts
git commit -m "test(v3.21 T10): plant-detail modal smoke spec (97 = 96 + 1)"
```

---

## Task 11: Bundle 측정 + final verification + 최종 review

**Files:**
- 코드 변경 없음 (측정 + 검증)

- [ ] **Step 1: build**

```bash
npm run build
```

Expected: 0 errors.

- [ ] **Step 2: bundle 측정 (canonical)**

```bash
gzip -c dist/assets/index-*.js | wc -c
```

Expected: 36848~37348 B (target zone, baseline 36148 + 700~1200).

- [ ] **Step 3: 4종 검증 일괄**

```bash
npx tsc --noEmit && npm run lint && npm test && npm run test:smoke
```

Expected:
- TS strict: 0 errors
- Lint: 0 errors
- Vitest: +43 신규 PASS, 기존 0 fail
- Smoke: 97/97 PASS

- [ ] **Step 4: bundle hard fail 시 (+1800 B 초과) trim**

후보:
- `.plant-detail-*` CSS rule 통합
- modal HTML 인라인 string dedup
- escapeHtml 호출 dedup

trim 후 재측정. 별도 commit:

```bash
git commit -m "perf(v3.21 T11): bundle trim — net <baseline + 1200 B>"
```

- [ ] **Step 5: 최종 review dispatch**

`superpowers:code-reviewer` 또는 codex CLI dispatch. v3.21 main HEAD 기준 신규 변경 일괄 리뷰.

분류: `DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT`.

REJECT 시: in-cycle hotfix만 허용 (v3.15 T16.1 / v3.13 T10 정책).

- [ ] **Step 6: 최종 review fix commits (있다면)**

각 P0/P1 fix 별도 commit. 표준 message: `fix(v3.21 최종 review P0-N): <issue>`.

- [ ] **Step 7: Commit T11**

```bash
git add -A  # 측정 결과 외 변경 없음, 단순 marker commit
git commit --allow-empty -m "chore(v3.21 T11): bundle 측정 + final verification + 최종 review"
```

---

## Task 12: Retro

**Files:**
- Modify: `docs/superpowers/specs/2026-05-07-v3.21-plant-modal-streak-freeze-design.md` (retro section 추가)
- Create: `/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v321.md`
- Modify: `/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md` (1줄 entry 추가)

- [ ] **Step 1: spec retro section 추가**

spec 파일 끝에 `## §8. Retro` 추가:

```markdown
## §8. Retro (배포 후 작성)

### 8.1 사이클 결과
- 배포 시점, main HEAD, Firebase URL.
- vitest delta 실측 / smoke baseline / bundle 실측 (canonical gzip CLI).
- 사전/최종 review 결과 (P0/P1/P2 분류).

### 8.2 신규 lessons
- ...

### 8.3 v3.22+ carry-forward 갱신
- ...
```

- [ ] **Step 2: memory entry 작성**

`memory/project_daily_growth_v321.md` 작성. spec/plan 요약 + 사이클 결과.

- [ ] **Step 3: MEMORY.md 1줄 추가**

`memory/MEMORY.md`에 1줄 추가:
```
- [DG v3.21 Plant Modal + Streak Freeze](project_daily_growth_v321.md) — ...
```

- [ ] **Step 4: Commit**

```bash
git add docs/superpowers/specs/2026-05-07-v3.21-plant-modal-streak-freeze-design.md
git commit -m "docs(v3.21 T12): retro 작성 + memory 업데이트 + v3.22 carry-forward seed"
```

---

## Self-Review Checklist (작성 후)

**1. Spec coverage** — §3 T1~T12 각 항목이 plan task로 매핑됨. v4→v5 migration / regen / consume / banner / reward / counts / modal shell / content / wiring / smoke / verify / retro 모두 ✓.

**2. Placeholder scan** — TBD/TODO/"implement later" 없음. 위 step의 코드 블록은 모두 실제 구현 가능한 형태.

**3. Type consistency** — `regenerateFreeze(u, now)`, `consumeFreezeForGap(u, gap) → {consumed, preserved}`, `Snapshot.freezeCount`, `GameEvent 'streak-freeze-used'`, `openPlantDetailModal(interestId)`, `getAnswerCountByInterest(id)` 모든 시그니처 일관.

**4. Order/dependency** — T1 schema → T2 regen → T3 consume(+ inject) → T4 banner UI → T5 reward → T6 counts → T7 modal shell → T8 modal content → T9 wiring → T10 smoke → T11 verify → T12 retro. 순서 OK.

---

## Execution Handoff

**Plan 작성 완료**. 두 가지 실행 옵션:

1. **Subagent-Driven (권장)** — `superpowers:subagent-driven-development` 사용. 각 task별 독립 subagent dispatch + 2-stage review (spec compliance + code quality).
2. **Inline Execution** — `superpowers:executing-plans` 사용. 본 세션 내 직접 실행 + checkpoint review.

다음 단계:
1. **사전 review dispatch** (CLAUDE.md 정책: spec + plan 직후 의무 1회)
2. 사용자 plan/spec 최종 confirm
3. T1 시작

---

**End of plan.**
