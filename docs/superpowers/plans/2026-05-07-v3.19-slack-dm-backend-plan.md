# v3.19 Slack DM Backend + Blue Screen Diagnostic + N1 Helper Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** Slack Incoming Webhook을 Firebase Functions backend로 이관 (1봇 멀티유저 DM, `@datarize.ai` 화이트리스트 + 6중 가드) + 첫 로그인 파란 화면 #3 진단·가설 fix + N1 smoke seed helper 통일.

**Architecture:**
- Backend: Firebase Functions 2세대 1개 함수 `sendAnswerDm` (HTTPS, Secret Manager, maxInstances=1, in-memory rate limit)
- Frontend: localStorage schema 마이그레이션 (`webhook` → `email`), Slack settings UI 재작성, services/slack.ts Function fetch로 교체
- 진단/가설 fix: tier-reason dataset + console.info + SW image 검증 캐싱 + RSS race 차단
- Helper 통일: `tests/helpers/seed.ts:primeOnboardedUser` 신규, 23 smoke spec 마이그레이션

**Tech Stack:** Vite + TypeScript SPA, Firebase Hosting + Functions 2세대 (Node 20), Vitest, Playwright, CSS Cascade Layers.

**Spec:** `docs/superpowers/specs/2026-05-07-v3.19-slack-dm-backend-design.md`

**Baseline (post-v3.18.1):**
- main HEAD `39a12f2`
- vitest 896 pass / 141 files
- smoke 97/97 parallel (47s) + N=10 cold-fork layout 10/10
- bundle CLI gzip 35122 B
- TS strict / lint 0 errors

**Prerequisites (사용자 액션, T2 시작 전):**
1. Slack workspace에 새 App 생성 (https://api.slack.com/apps) — 본 plan은 backend 코드만 담당
2. Bot Token 발급 + scopes: `chat:write`, `users:read.email`, `im:write`
3. Datarize 워크스페이스에 install + admin approve
4. Firebase project Blaze 업그레이드 (신용카드, free tier 비용 0)
5. Bot Token → `firebase functions:secrets:set SLACK_BOT_TOKEN`
6. Firebase Console → Cloud Billing → Budget alert $5/월 설정

---

## Task 0: spec + plan + 사전 codex review

- [ ] **Step 1: Verify spec committed**

```bash
git log --oneline -3 docs/superpowers/specs/2026-05-07-v3.19-slack-dm-backend-design.md
```

Spec 미commit 상태면:
```bash
git add docs/superpowers/specs/2026-05-07-v3.19-slack-dm-backend-design.md
git commit -m "$(cat <<'EOF'
docs(v3.19 T0 spec): Slack DM backend + blue screen diagnostic + N1 helper

Brainstorm 합의 결과: Q1 옵션 B / Q2 Firebase Functions / Q3 Datarize 사내 / Q4 이메일 입력 + Q5 @datarize.ai / Q6 완전 대체 / Q7 가드 6중 일 10회 / Q8 진단+가설 fix 둘 다.

13~14 task, +25~35 vitest, +1~2 smoke, bundle ±0.3 kB, Codex 2-pass.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 2: Stage + commit plan**

```bash
git add docs/superpowers/plans/2026-05-07-v3.19-slack-dm-backend-plan.md
git commit -m "$(cat <<'EOF'
docs(v3.19 T0 plan): 14 task 구조 — backend Function + 진단 + helper 통일

T1 schema / T2 Function 골격 / T3 rate limit / T4 Slack API / T5 frontend service / T6 settings UI / T7 autoSendAnswer 정리 / T8 tier-reason 진단 / T9 SW 가설 / T10 RSS race / T11 helper 신규 / T12 23 spec 마이그레이션 / T13 사용자 액션 가이드 / T14 retro.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 3: Slack docs 시그니처 + scope 캡쳐 검증 (P1-1 사전 review 권고)**

브라우저로 다음 3개 페이지 1회 확인 후 plan T4 Step 1 코드와 1:1 비교:
- https://api.slack.com/methods/users.lookupByEmail (Required scopes: `users:read.email`)
- https://api.slack.com/methods/conversations.open (Required scopes: `im:write` for user DM, `chat:write` 충분 여부)
- https://api.slack.com/methods/chat.postMessage (Required scopes: `chat:write`)

확인 사항:
- `im:write`가 redundant인지 verify (deprecate 안 됐는지). redundant면 spec §2.7 Step 2 + plan T13 Step 1.4 + plan T4 Step 1 3곳 동기 수정.
- 시그니처/필수 query/body 필드 변경 없는지 확인.

- [ ] **Step 4: 사전 codex review dispatch**

Codex CLI 호출:
```bash
codex review docs/superpowers/specs/2026-05-07-v3.19-slack-dm-backend-design.md docs/superpowers/plans/2026-05-07-v3.19-slack-dm-backend-plan.md
```

권한 issue 시 fallback: `pr-review-toolkit:code-reviewer` agent 호출.

P0/P1 → in-cycle plan amendment, P2 → v3.20 backlog.

---

## Task 1: Schema 마이그레이션 (state/slack.ts)

**문제**: 기존 `dg_slack` shape는 `{ webhook, autoSend }`. v3.19에서 `{ email, autoSend }`로 교체. 기존 webhook 키 발견 시 silent reject (null 반환) → 사용자가 settings에서 재입력.

**Files:**
- Modify: `src/state/slack.ts` (전체 재작성)
- Test: `tests/unit/state/slack.spec.ts` (신규 또는 기존 갱신)

- [ ] **Step 1: 기존 테스트 위치 확인**

```bash
find tests -name "slack.spec.ts" -not -path "*smoke*" 2>/dev/null
```

기존 unit test가 있으면 그 파일 갱신, 없으면 신규.

- [ ] **Step 2: Write failing tests**

`tests/unit/state/slack.spec.ts`:
```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { loadSlackSettings, saveSlackSettings, clearSlackSettings } from '../../../src/state/slack';

describe('slack settings (v3.19 schema)', () => {
  beforeEach(() => localStorage.clear());

  it('returns null when storage empty', () => {
    expect(loadSlackSettings()).toBeNull();
  });

  it('reads valid email shape', () => {
    saveSlackSettings({ email: 'me@datarize.ai', autoSend: true });
    expect(loadSlackSettings()).toEqual({ email: 'me@datarize.ai', autoSend: true });
  });

  it('rejects legacy webhook shape silently (returns null)', () => {
    localStorage.setItem('dg_slack', JSON.stringify({ webhook: 'https://hooks.slack.com/...', autoSend: true }));
    expect(loadSlackSettings()).toBeNull();
  });

  it('rejects empty email', () => {
    localStorage.setItem('dg_slack', JSON.stringify({ email: '', autoSend: true }));
    expect(loadSlackSettings()).toBeNull();
  });

  it('defaults autoSend to false when missing', () => {
    localStorage.setItem('dg_slack', JSON.stringify({ email: 'me@datarize.ai' }));
    expect(loadSlackSettings()).toEqual({ email: 'me@datarize.ai', autoSend: false });
  });

  it('clearSlackSettings removes localStorage entry', () => {
    saveSlackSettings({ email: 'me@datarize.ai', autoSend: false });
    clearSlackSettings();
    expect(localStorage.getItem('dg_slack')).toBeNull();
  });
});
```

- [ ] **Step 3: Run test (FAIL)**

```bash
npm test -- tests/unit/state/slack.spec.ts
```

Expected: 6 fail (`SlackSettings` 타입 불일치).

- [ ] **Step 4: Replace `src/state/slack.ts` with new schema**

```ts
const KEY = 'dg_slack';

export interface SlackSettings {
  email: string;
  autoSend: boolean;
}

export function loadSlackSettings(): SlackSettings | null {
  const raw = localStorage.getItem(KEY);
  if (!raw) return null;
  try {
    const parsed = JSON.parse(raw) as Partial<SlackSettings> & { webhook?: unknown };
    if (typeof parsed.email !== 'string' || parsed.email.length === 0) return null;
    return {
      email: parsed.email,
      autoSend: typeof parsed.autoSend === 'boolean' ? parsed.autoSend : false,
    };
  } catch {
    return null;
  }
}

export function saveSlackSettings(s: SlackSettings): void {
  localStorage.setItem(KEY, JSON.stringify(s));
}

export function clearSlackSettings(): void {
  localStorage.removeItem(KEY);
}
```

- [ ] **Step 5: Run test (PASS)**

```bash
npm test -- tests/unit/state/slack.spec.ts
```

Expected: 6 pass.

- [ ] **Step 6: TS strict + lint**

```bash
npx tsc --noEmit && npm run lint
```

기존 `src/services/slack.ts` `loadSlackSettings()` 사용처는 `s.webhook` 참조 → 타입 에러 발생 예상. **이는 T4에서 일괄 교체**, 이번 task는 schema만 commit 후 next task에서 fix. 일시적 빌드 깨짐 인정.

대안 — 빌드 깨짐 회피하려면 T4와 묶어 단일 commit. 권고: T4와 묶어서 진행 (`s.webhook` 참조 fix까지).

→ **Step 4 commit 보류, T4 완료 시점에 일괄 commit**.

---

## Task 2: Functions 디렉토리 + sendAnswerDm 골격

**문제**: 현재 `functions/` 디렉토리 부재. Firebase Functions 2세대 신규 setup. Bot Token Secret Manager 연동.

**Files:**
- Create: `functions/package.json`
- Create: `functions/tsconfig.json`
- Create: `functions/src/index.ts`
- Create: `functions/src/sendAnswerDm.ts`
- Create: `functions/.gitignore`
- Modify: `firebase.json` (functions 섹션 추가)

- [ ] **Step 1: Initialize functions/ via Firebase CLI**

```bash
firebase init functions
```

선택지:
- TypeScript ✓
- ESLint ✓ (ignore — 기존 root ESLint와 별도)
- Don't install dependencies (수동)

- [ ] **Step 2: Verify firebase.json updated**

```bash
cat firebase.json
```

Expected: `"functions": [{ "source": "functions", "codebase": "default", ... }]` 추가됨.

- [ ] **Step 3: Configure functions/package.json (Node 20, 2세대)**

```json
{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "test": "vitest run"
  },
  "engines": { "node": "20" },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  },
  "private": true
}
```

- [ ] **Step 4: Install deps**

```bash
cd functions && npm install && cd ..
```

- [ ] **Step 5: Configure functions/tsconfig.json**

```json
{
  "compilerOptions": {
    "module": "commonjs",
    "target": "es2022",
    "outDir": "lib",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true
  },
  "include": ["src"]
}
```

- [ ] **Step 6: Write functions/src/sendAnswerDm.ts (골격, T3/T4에서 채움)**

```ts
import { onRequest } from 'firebase-functions/v2/https';
import { defineSecret } from 'firebase-functions/params';

const SLACK_BOT_TOKEN = defineSecret('SLACK_BOT_TOKEN');

export interface AnswerDmRequest {
  email: string;
  question: string;
  answer: string;
  insight?: string;
  streak: number;
  xp: number;
}

const ALLOWED_DOMAIN = '@datarize.ai';
const ANSWER_CAP = 500;

export const sendAnswerDm = onRequest(
  { secrets: [SLACK_BOT_TOKEN], maxInstances: 1, cors: true, region: 'us-central1' },
  async (req, res) => {
    if (req.method !== 'POST') {
      res.status(405).json({ error: 'method_not_allowed' });
      return;
    }
    const body = req.body as Partial<AnswerDmRequest>;
    if (typeof body.email !== 'string' || !body.email.endsWith(ALLOWED_DOMAIN)) {
      res.status(400).json({ error: 'invalid_email' });
      return;
    }
    if (typeof body.answer !== 'string' || body.answer.length === 0 || body.answer.length > ANSWER_CAP) {
      res.status(400).json({ error: 'invalid_answer' });
      return;
    }
    if (typeof body.question !== 'string' || body.question.length === 0) {
      res.status(400).json({ error: 'invalid_question' });
      return;
    }
    // T3에서 rate limit 추가, T4에서 Slack API 호출 추가
    res.status(501).json({ error: 'not_implemented_yet' });
  },
);
```

- [ ] **Step 7: Write functions/src/index.ts (재출력)**

```ts
export { sendAnswerDm } from './sendAnswerDm';
```

- [ ] **Step 8: TS build**

```bash
cd functions && npm run build && cd ..
```

Expected: `functions/lib/index.js` 생성, 0 errors.

- [ ] **Step 9: Commit (T1 schema 변경 함께 묶음 안 됨 — schema는 T4에서 일괄)**

```bash
git add functions/ firebase.json
git commit -m "$(cat <<'EOF'
feat(v3.19 T2): functions/ 디렉토리 + sendAnswerDm 골격 (Firebase Functions 2세대)

Node 20, region us-central1, maxInstances 1, Secret Manager(SLACK_BOT_TOKEN).
도메인 화이트리스트(@datarize.ai) + 답변 길이 cap(500) + 필수 필드 검증.
T3 rate limit + T4 Slack API 호출은 후속 task.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: Backend rate limit logic + tests

**문제**: in-memory Map으로 per-email per-min/per-day 카운트. instance scope (maxInstances=1로 강제). 1분 burst 3회 / 일 10회.

**Files:**
- Create: `functions/src/rateLimit.ts`
- Create: `functions/src/__tests__/rateLimit.spec.ts`
- Modify: `functions/src/sendAnswerDm.ts` (rate limit 호출 추가)

- [ ] **Step 1: Write failing tests**

`functions/src/__tests__/rateLimit.spec.ts`:
```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { checkRateLimit, resetRateLimit } from '../rateLimit';

describe('rateLimit', () => {
  beforeEach(() => resetRateLimit());

  it('allows first call', () => {
    expect(checkRateLimit('me@datarize.ai', Date.now())).toBe('ok');
  });

  it('blocks burst — 4th call within 60s', () => {
    const now = 1_700_000_000_000;
    expect(checkRateLimit('me@datarize.ai', now)).toBe('ok');
    expect(checkRateLimit('me@datarize.ai', now + 100)).toBe('ok');
    expect(checkRateLimit('me@datarize.ai', now + 200)).toBe('ok');
    expect(checkRateLimit('me@datarize.ai', now + 300)).toBe('burst');
  });

  it('allows 4th call after 60s window', () => {
    const now = 1_700_000_000_000;
    checkRateLimit('me@datarize.ai', now);
    checkRateLimit('me@datarize.ai', now + 100);
    checkRateLimit('me@datarize.ai', now + 200);
    expect(checkRateLimit('me@datarize.ai', now + 60_001)).toBe('ok');
  });

  it('blocks 11th call within 24h', () => {
    const now = 1_700_000_000_000;
    for (let i = 0; i < 10; i++) {
      // burst 회피 — 1분 간격
      expect(checkRateLimit('me@datarize.ai', now + i * 60_001)).toBe('ok');
    }
    expect(checkRateLimit('me@datarize.ai', now + 10 * 60_001)).toBe('daily');
  });

  it('isolates per-email counters', () => {
    const now = 1_700_000_000_000;
    for (let i = 0; i < 3; i++) checkRateLimit('a@datarize.ai', now);
    expect(checkRateLimit('a@datarize.ai', now)).toBe('burst');
    expect(checkRateLimit('b@datarize.ai', now)).toBe('ok');
  });
});
```

- [ ] **Step 2: Run test (FAIL)**

```bash
cd functions && npx vitest run src/__tests__/rateLimit.spec.ts && cd ..
```

Expected: module not found.

- [ ] **Step 3: Implement functions/src/rateLimit.ts**

```ts
const BURST_WINDOW_MS = 60_000;
const DAILY_WINDOW_MS = 24 * 60 * 60_000;
const BURST_MAX = 3;
const DAILY_MAX = 10;

interface Record {
  burst: number[];  // ts list within last 60s
  daily: number[];  // ts list within last 24h
}

const records = new Map<string, Record>();

export type RateLimitResult = 'ok' | 'burst' | 'daily';

export function checkRateLimit(email: string, now: number): RateLimitResult {
  const rec = records.get(email) ?? { burst: [], daily: [] };
  rec.burst = rec.burst.filter((t) => now - t < BURST_WINDOW_MS);
  rec.daily = rec.daily.filter((t) => now - t < DAILY_WINDOW_MS);
  if (rec.burst.length >= BURST_MAX) {
    records.set(email, rec);
    return 'burst';
  }
  if (rec.daily.length >= DAILY_MAX) {
    records.set(email, rec);
    return 'daily';
  }
  rec.burst.push(now);
  rec.daily.push(now);
  records.set(email, rec);
  return 'ok';
}

export function resetRateLimit(): void {
  records.clear();
}
```

- [ ] **Step 4: Run test (PASS)**

```bash
cd functions && npx vitest run src/__tests__/rateLimit.spec.ts && cd ..
```

Expected: 5 pass.

- [ ] **Step 5: Wire into sendAnswerDm.ts**

`functions/src/sendAnswerDm.ts`에 추가 (검증 후, Slack API 호출 전):
```ts
import { checkRateLimit } from './rateLimit';

// (도메인/길이 검증 통과 후)
const rl = checkRateLimit(body.email, Date.now());
if (rl !== 'ok') {
  res.status(429).json({ error: 'rate_limited', kind: rl });
  return;
}
// T4에서 Slack API 호출 추가
```

- [ ] **Step 6: TS build**

```bash
cd functions && npm run build && cd ..
```

- [ ] **Step 7: Commit**

```bash
git add functions/src/rateLimit.ts functions/src/__tests__/rateLimit.spec.ts functions/src/sendAnswerDm.ts
git commit -m "$(cat <<'EOF'
feat(v3.19 T3): backend rate limit (per-min 3 / per-day 10)

in-memory Map, instance scope (maxInstances=1로 강제). sliding window.
sendAnswerDm wire: rate limit 통과 후 Slack API 호출 (T4).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: Backend Slack API 호출 (lookupByEmail + conversations.open + chat.postMessage)

**문제**: 3-step Slack API chain. 각 step별 에러 분류. blocks payload는 frontend `buildAnswerBlocks` 이식.

**Files:**
- Create: `functions/src/slack.ts` (Slack API client + buildAnswerBlocks 이식)
- Create: `functions/src/__tests__/slack.spec.ts`
- Modify: `functions/src/sendAnswerDm.ts` (Slack 호출 wire)

- [ ] **Step 1: 공식 Slack API 시그니처 docs 재확인**

브라우저로 https://api.slack.com/methods/users.lookupByEmail / conversations.open / chat.postMessage 1회 확인 (할루시네이션 점검).

- 확인된 출처: `users.lookupByEmail` GET + `email` query param + Bot Token header.
- 확인된 출처: `conversations.open` POST + `users` body field (user ID) → returns `channel.id`.
- 확인된 출처: `chat.postMessage` POST + `channel` + `blocks` (또는 `text`) body.

- [ ] **Step 2: Write failing tests (Slack API mock)**

`functions/src/__tests__/slack.spec.ts`:
```ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { sendDmAsBot, buildAnswerBlocks } from '../slack';

global.fetch = vi.fn();

describe('buildAnswerBlocks', () => {
  it('produces header + question + answer + context blocks', () => {
    const payload = buildAnswerBlocks({
      email: 'me@datarize.ai',
      question: 'Q?',
      answer: 'A.',
      streak: 5,
      xp: 100,
    });
    expect(payload.blocks[0].type).toBe('header');
    expect(payload.blocks.find((b: any) => b.type === 'context')).toBeDefined();
  });

  it('includes insight block when provided', () => {
    const payload = buildAnswerBlocks({
      email: 'me@datarize.ai',
      question: 'Q?',
      answer: 'A.',
      insight: 'I!',
      streak: 0,
      xp: 0,
    });
    expect(payload.blocks.some((b: any) => b.text?.text?.includes('I!'))).toBe(true);
  });
});

describe('sendDmAsBot', () => {
  beforeEach(() => vi.clearAllMocks());

  it('throws user_not_found when lookupByEmail returns ok=false', async () => {
    (fetch as any).mockResolvedValueOnce({
      ok: true,
      json: async () => ({ ok: false, error: 'users_not_found' }),
    });
    await expect(sendDmAsBot('xoxb-x', 'me@datarize.ai', { blocks: [] } as any))
      .rejects.toThrow('user_not_found');
  });

  it('throws dm_open_failed when conversations.open fails', async () => {
    (fetch as any)
      .mockResolvedValueOnce({ ok: true, json: async () => ({ ok: true, user: { id: 'U1' } }) })
      .mockResolvedValueOnce({ ok: true, json: async () => ({ ok: false, error: 'cannot_open_dm' }) });
    await expect(sendDmAsBot('xoxb-x', 'me@datarize.ai', { blocks: [] } as any))
      .rejects.toThrow('dm_open_failed');
  });

  it('completes when all 3 steps return ok', async () => {
    (fetch as any)
      .mockResolvedValueOnce({ ok: true, json: async () => ({ ok: true, user: { id: 'U1' } }) })
      .mockResolvedValueOnce({ ok: true, json: async () => ({ ok: true, channel: { id: 'D1' } }) })
      .mockResolvedValueOnce({ ok: true, json: async () => ({ ok: true }) });
    await expect(sendDmAsBot('xoxb-x', 'me@datarize.ai', { blocks: [] } as any)).resolves.toBeUndefined();
  });
});
```

- [ ] **Step 3: Run test (FAIL)**

```bash
cd functions && npx vitest run src/__tests__/slack.spec.ts && cd ..
```

Expected: module not found.

- [ ] **Step 4: Implement functions/src/slack.ts**

```ts
export interface AnswerData {
  email: string;
  question: string;
  answer: string;
  insight?: string;
  streak: number;
  xp: number;
}

interface SlackBlock {
  type: string;
  text?: { type: string; text: string };
  elements?: Array<{ type: string; text: string }>;
}

export interface SlackPayload {
  text: string;
  blocks: SlackBlock[];
}

const ANSWER_CAP = 500;

function escapeMrkdwn(s: string): string {
  return s.replace(/</g, '&lt;').replace(/>/g, '&gt;')
    .replace(/\*/g, '\\*').replace(/_/g, '\\_').replace(/`/g, '\\`').replace(/~/g, '\\~');
}

function getKstDateStr(now = new Date()): string {
  const kst = new Date(now.getTime() + 9 * 60 * 60_000);
  return kst.toISOString().slice(0, 10);
}

export function buildAnswerBlocks(data: AnswerData): SlackPayload {
  const date = getKstDateStr();
  const cappedAnswer = data.answer.length > ANSWER_CAP ? data.answer.slice(0, ANSWER_CAP) : data.answer;
  const headerText = `🌱 Daily Growth — ${date}`;
  const blocks: SlackBlock[] = [
    { type: 'header', text: { type: 'plain_text', text: headerText } },
    { type: 'section', text: { type: 'mrkdwn', text: `❓ *오늘의 질문*\n${escapeMrkdwn(data.question)}` } },
    { type: 'section', text: { type: 'mrkdwn', text: `✍️ *나의 답변*\n${escapeMrkdwn(cappedAnswer)}` } },
  ];
  if (data.insight && data.insight.trim().length > 0) {
    blocks.push({ type: 'divider' });
    blocks.push({ type: 'section', text: { type: 'mrkdwn', text: `💡 *인사이트*\n${escapeMrkdwn(data.insight)}` } });
  }
  blocks.push({ type: 'context', elements: [{ type: 'mrkdwn', text: `🔥 연속 ${data.streak}일 · ⭐ ${data.xp} XP` }] });
  return { text: headerText, blocks };
}

const SLACK_API_TIMEOUT_MS = 5000;

async function slackFetch(url: string, init: RequestInit): Promise<any> {
  const controller = new AbortController();
  const timer = setTimeout(() => controller.abort(), SLACK_API_TIMEOUT_MS);
  try {
    const res = await fetch(url, { ...init, signal: controller.signal });
    if (!res.ok) throw new Error(`http_${res.status}`);
    return await res.json();
  } finally {
    clearTimeout(timer);
  }
}

export async function sendDmAsBot(
  botToken: string,
  email: string,
  payload: SlackPayload,
): Promise<void> {
  const auth = `Bearer ${botToken}`;

  const lookup = await slackFetch(
    `https://slack.com/api/users.lookupByEmail?email=${encodeURIComponent(email)}`,
    { method: 'GET', headers: { Authorization: auth } },
  );
  if (!lookup.ok) throw new Error('user_not_found');
  const userId: string = lookup.user.id;

  const open = await slackFetch('https://slack.com/api/conversations.open', {
    method: 'POST',
    headers: { Authorization: auth, 'Content-Type': 'application/json' },
    body: JSON.stringify({ users: userId }),
  });
  if (!open.ok) throw new Error('dm_open_failed');
  const channelId: string = open.channel.id;

  const post = await slackFetch('https://slack.com/api/chat.postMessage', {
    method: 'POST',
    headers: { Authorization: auth, 'Content-Type': 'application/json' },
    body: JSON.stringify({ channel: channelId, ...payload }),
  });
  if (!post.ok) throw new Error('post_message_failed');
}
```

- [ ] **Step 5: Wire into sendAnswerDm.ts**

```ts
import { buildAnswerBlocks, sendDmAsBot } from './slack';

// (rate limit 통과 후)
try {
  const blocks = buildAnswerBlocks({
    email: body.email,
    question: body.question,
    answer: body.answer,
    insight: body.insight,
    streak: typeof body.streak === 'number' ? body.streak : 0,
    xp: typeof body.xp === 'number' ? body.xp : 0,
  });
  await sendDmAsBot(SLACK_BOT_TOKEN.value(), body.email, blocks);
  res.status(200).json({ ok: true });
} catch (e) {
  const msg = e instanceof Error ? e.message : 'unknown';
  if (msg === 'user_not_found') res.status(404).json({ error: 'user_not_found' });
  else if (msg === 'dm_open_failed' || msg === 'post_message_failed') res.status(502).json({ error: msg });
  else res.status(500).json({ error: 'internal' });
}
// '501 not_implemented_yet' 코드 삭제
```

- [ ] **Step 6: Run all functions tests (PASS)**

```bash
cd functions && npm run build && npx vitest run && cd ..
```

Expected: 5 (rate limit) + 5 (slack) = 10 pass.

- [ ] **Step 7: Commit**

```bash
git add functions/
git commit -m "$(cat <<'EOF'
feat(v3.19 T4): backend Slack API chain (lookupByEmail → conversations.open → chat.postMessage)

5s timeout per call, error classification (user_not_found / dm_open_failed / post_message_failed).
buildAnswerBlocks 이식 (frontend slack.ts 패턴, KST 강제).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: Frontend service src/services/slack.ts → sendAnswerDm

**문제**: 기존 `sendToSlack(webhook, payload)` 폐기 + `autoSendAnswer` 내부 fetch가 Function URL POST. `buildAnswerBlocks` 등은 backend로 이식 완료 → frontend에서 삭제.

**Files:**
- Modify: `src/services/slack.ts` (전체 재작성)
- Modify: `src/ui/handlers/home.ts` (autoSendAnswer 호출부 — `s.webhook` 참조 fix까지 묶음)
- Test: `tests/unit/services/slack.spec.ts` (신규)

- [ ] **Step 1: Function URL 결정 + CSP 갱신 (P0-1 사전 review fix)**

Firebase Hosting + Functions same-origin rewrite로 `/api/sendAnswerDm` 패턴 사용 권장 (CSP `connect-src` 'self' 통과 자연스러움).

`firebase.json` rewrites에 추가 — function rewrite를 SPA fallback 위에 둘 것:
```json
"rewrites": [
  { "source": "/api/sendAnswerDm", "function": "sendAnswerDm" },
  { "source": "**", "destination": "/index.html" }
]
```

CSP `connect-src` 정리 (webhook 흐름 폐기 → dead allowance 삭제):
```diff
- "connect-src 'self' https://generativelanguage.googleapis.com https://hooks.slack.com https://api.rss2json.com https://*.firebaseio.com https://*.googleapis.com https://*.sentry.io"
+ "connect-src 'self' https://generativelanguage.googleapis.com https://api.rss2json.com https://*.firebaseio.com https://*.googleapis.com https://*.sentry.io"
```

`https://hooks.slack.com` 토큰 삭제. same-origin Function 호출은 `'self'`로 통과. 절대 URL fallback이 필요할 경우만 `https://*.cloudfunctions.net` 추가 (현재 plan은 same-origin only).

- [ ] **Step 2: Write failing tests**

`tests/unit/services/slack.spec.ts`:
```ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { sendAnswerDm, autoSendAnswer } from '../../../src/services/slack';
import { saveSlackSettings, clearSlackSettings } from '../../../src/state/slack';

global.fetch = vi.fn();

describe('sendAnswerDm', () => {
  beforeEach(() => {
    vi.clearAllMocks();
    localStorage.clear();
    saveSlackSettings({ email: 'me@datarize.ai', autoSend: true });
  });

  it('POSTs to /api/sendAnswerDm with email + answer payload', async () => {
    (fetch as any).mockResolvedValueOnce({ ok: true, json: async () => ({ ok: true }) });
    await sendAnswerDm({ question: 'Q?', answer: 'A.' });
    expect(fetch).toHaveBeenCalledWith('/api/sendAnswerDm', expect.objectContaining({
      method: 'POST',
    }));
    const callBody = JSON.parse((fetch as any).mock.calls[0][1].body);
    expect(callBody.email).toBe('me@datarize.ai');
    expect(callBody.answer).toBe('A.');
  });

  it('wraps streak/xp from loadUserData (P0-2 fix)', async () => {
    // production read key는 'user' (src/state/user.ts:36)
    localStorage.setItem('user', JSON.stringify({
      onboardedAt: '2026-04-01', interests: ['tech'],
      streak: 5, lastActiveDate: '', xp: 120, earnedBadges: {},
      gamificationMigrated: true, gardenIntroduced: true, schemaVersion: 2,
    }));
    saveSlackSettings({ email: 'me@datarize.ai', autoSend: true });
    (fetch as any).mockResolvedValueOnce({ ok: true, json: async () => ({ ok: true }) });
    await sendAnswerDm({ question: 'Q?', answer: 'A.' });
    const callBody = JSON.parse((fetch as any).mock.calls[0][1].body);
    expect(callBody.streak).toBe(5);
    expect(callBody.xp).toBe(120);
  });

  it('defaults streak/xp to 0 when user data absent', async () => {
    saveSlackSettings({ email: 'me@datarize.ai', autoSend: true });
    (fetch as any).mockResolvedValueOnce({ ok: true, json: async () => ({ ok: true }) });
    await sendAnswerDm({ question: 'Q?', answer: 'A.' });
    const callBody = JSON.parse((fetch as any).mock.calls[0][1].body);
    expect(callBody.streak).toBe(0);
    expect(callBody.xp).toBe(0);
  });

  it('throws when email settings missing', async () => {
    clearSlackSettings();
    await expect(sendAnswerDm({ question: 'Q?', answer: 'A.' })).rejects.toThrow();
  });

  it('classifies 429 as rate_limited', async () => {
    (fetch as any).mockResolvedValueOnce({ ok: false, status: 429, json: async () => ({ error: 'rate_limited' }) });
    await expect(sendAnswerDm({ question: 'Q?', answer: 'A.' })).rejects.toThrow(/rate/);
  });
});

describe('autoSendAnswer', () => {
  beforeEach(() => { localStorage.clear(); vi.clearAllMocks(); });

  it('skips when autoSend=false', async () => {
    saveSlackSettings({ email: 'me@datarize.ai', autoSend: false });
    await autoSendAnswer({ question: 'Q?', answer: 'A.' });
    expect(fetch).not.toHaveBeenCalled();
  });

  it('calls fetch when autoSend=true', async () => {
    saveSlackSettings({ email: 'me@datarize.ai', autoSend: true });
    (fetch as any).mockResolvedValueOnce({ ok: true, json: async () => ({ ok: true }) });
    await autoSendAnswer({ question: 'Q?', answer: 'A.' });
    expect(fetch).toHaveBeenCalled();
  });

  it('swallows errors silently in autoSendAnswer', async () => {
    saveSlackSettings({ email: 'me@datarize.ai', autoSend: true });
    (fetch as any).mockResolvedValueOnce({ ok: false, status: 502, json: async () => ({ error: 'dm_open_failed' }) });
    await expect(autoSendAnswer({ question: 'Q?', answer: 'A.' })).resolves.toBeUndefined();
  });
});
```

- [ ] **Step 3: Run test (FAIL)**

```bash
npm test -- tests/unit/services/slack.spec.ts
```

- [ ] **Step 4: Replace src/services/slack.ts**

```ts
import { loadSlackSettings } from '../state/slack';
import { loadUserData } from '../state/user';

export interface AnswerData {
  question: string;
  answer: string;
  insight?: string;
}

const FUNCTION_URL = '/api/sendAnswerDm';

export async function sendAnswerDm(data: AnswerData): Promise<void> {
  const s = loadSlackSettings();
  if (!s) throw new Error('slack_settings_missing');
  const user = loadUserData();
  const streak = user?.streak ?? 0;
  const xp = user?.xp ?? 0;
  const res = await fetch(FUNCTION_URL, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      email: s.email,
      question: data.question,
      answer: data.answer,
      ...(data.insight ? { insight: data.insight } : {}),
      streak,
      xp,
    }),
  });
  if (!res.ok) {
    const body = await res.json().catch(() => ({}));
    throw new Error(`slack_${body.error ?? res.status}`);
  }
}

export async function autoSendAnswer(data: AnswerData): Promise<void> {
  const s = loadSlackSettings();
  if (!s || !s.autoSend) return;
  try {
    await sendAnswerDm(data);
  } catch (e) {
    console.warn('Slack auto-send failed:', e);
  }
}
```

- [ ] **Step 5: Run test (PASS)**

```bash
npm test -- tests/unit/services/slack.spec.ts tests/unit/state/slack.spec.ts
```

Expected: 6 (state) + 6 (services) = 12 pass.

- [ ] **Step 6: TS strict + lint (전체 빌드)**

```bash
npx tsc --noEmit && npm run lint
```

Expected: 0 errors. T1 schema 변경으로 인한 빌드 실패는 이번 task에서 일괄 해소.

- [ ] **Step 7: Commit (T1 schema 변경 함께)**

```bash
git add src/state/slack.ts src/services/slack.ts tests/unit/state/slack.spec.ts tests/unit/services/slack.spec.ts firebase.json
git commit -m "$(cat <<'EOF'
feat(v3.19 T1+T5): schema 마이그레이션 + frontend service Function fetch

state/slack.ts: { webhook, autoSend } → { email, autoSend }, legacy webhook silent reject.
services/slack.ts: sendAnswerDm POST /api/sendAnswerDm (Firebase Hosting same-origin rewrite).
buildAnswerBlocks/escapeMrkdwn 등 backend로 이식 완료, frontend에서 삭제.
autoSendAnswer 호출부 동일 (내부 sendAnswerDm 교체).

firebase.json rewrites에 /api/sendAnswerDm 추가.

12 vitest 신규 (6 state + 6 services), TS 0, lint 0.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: Settings UI 재작성 (Slack section)

**문제**: [src/ui/tabs/settings.ts:67-90](src/ui/tabs/settings.ts) Slack section의 webhook input → email input. 검증/저장/테스트/clear 핸들러 갱신.

**Files:**
- Modify: `src/ui/tabs/settings.ts`
- Test: `tests/unit/ui/tabs/settings-slack.spec.ts` (신규 또는 기존 갱신)

- [ ] **Step 0: renderSettings export 검증 (P1-4 사전 review confirmation)**

```bash
grep -n "export function renderSettings\|export function bindHandlers" src/ui/tabs/settings.ts
```

Expected: `renderSettings` line 34 export 확인됨 — vitest `renderSettings(container)` 직접 호출 패턴 OK.

- [ ] **Step 1: 기존 settings 테스트 위치 확인**

```bash
grep -rln "slackWebhookInput\|isValidWebhookUrl" tests/unit/ 2>/dev/null
```

- [ ] **Step 2: Write failing tests**

`tests/unit/ui/tabs/settings-slack.spec.ts`:
```ts
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { renderSettings } from '../../../../src/ui/tabs/settings';
import { saveSlackSettings, loadSlackSettings, clearSlackSettings } from '../../../../src/state/slack';

global.fetch = vi.fn();

describe('settings — Slack email section', () => {
  let container: HTMLElement;
  beforeEach(() => {
    localStorage.clear();
    document.body.innerHTML = '';
    container = document.createElement('div');
    document.body.append(container);
    renderSettings(container);
  });

  it('renders email input with @datarize.ai placeholder', () => {
    const input = container.querySelector<HTMLInputElement>('#slackEmailInput');
    expect(input).toBeTruthy();
    expect(input?.type).toBe('email');
    expect(input?.placeholder).toContain('@datarize.ai');
  });

  it('rejects non-@datarize.ai email', async () => {
    const input = container.querySelector<HTMLInputElement>('#slackEmailInput')!;
    const saveBtn = container.querySelector<HTMLButtonElement>('#saveSlackBtn')!;
    input.value = 'me@gmail.com';
    saveBtn.click();
    const result = container.querySelector<HTMLDivElement>('#slackTestResult');
    expect(result?.textContent).toMatch(/datarize\.ai/);
    expect(loadSlackSettings()).toBeNull();
  });

  it('saves valid @datarize.ai email', async () => {
    const input = container.querySelector<HTMLInputElement>('#slackEmailInput')!;
    const saveBtn = container.querySelector<HTMLButtonElement>('#saveSlackBtn')!;
    input.value = 'me@datarize.ai';
    saveBtn.click();
    expect(loadSlackSettings()).toEqual({ email: 'me@datarize.ai', autoSend: false });
  });

  it('hydrates existing email on render', () => {
    document.body.innerHTML = '';
    saveSlackSettings({ email: 'a@datarize.ai', autoSend: true });
    container = document.createElement('div');
    document.body.append(container);
    renderSettings(container);
    const input = container.querySelector<HTMLInputElement>('#slackEmailInput');
    expect(input?.value).toBe('a@datarize.ai');
    const toggle = container.querySelector<HTMLInputElement>('#slackAutoToggle');
    expect(toggle?.checked).toBe(true);
  });

  it('clear button removes settings', () => {
    saveSlackSettings({ email: 'me@datarize.ai', autoSend: true });
    document.body.innerHTML = '';
    container = document.createElement('div');
    document.body.append(container);
    renderSettings(container);
    container.querySelector<HTMLButtonElement>('#clearSlackBtn')!.click();
    expect(loadSlackSettings()).toBeNull();
  });
});
```

- [ ] **Step 3: Run test (FAIL)**

```bash
npm test -- tests/unit/ui/tabs/settings-slack.spec.ts
```

- [ ] **Step 4: Modify src/ui/tabs/settings.ts**

[settings.ts:67-90](src/ui/tabs/settings.ts) Slack section innerHTML 교체:
```html
<div class="settings-group-title">Slack 봇 알림</div>
<input type="email" id="slackEmailInput" placeholder="you@datarize.ai" autocomplete="email" style="width:100%;padding:10px;border:1px solid var(--border);border-radius:var(--radius-sm);background:var(--bg-input);color:var(--text-primary);" />
<div style="display:flex;gap:8px;margin-top:8px;">
  <button type="button" id="testSlackBtn" class="btn btn-secondary" style="flex:1;">🔗 테스트</button>
  <button type="button" id="saveSlackBtn" class="btn btn-primary" style="flex:1;">💾 저장</button>
</div>
<div id="slackAutoRow" style="display:none;align-items:center;justify-content:space-between;margin-top:12px;">
  <label for="slackAutoToggle" style="font-size:0.9rem;">답변 제출 시 자동 전송</label>
  <input type="checkbox" id="slackAutoToggle" />
</div>
<button type="button" id="clearSlackBtn" class="btn btn-secondary" style="display:none;margin-top:8px;width:100%;">🗑️ 연결 해제</button>
<div id="slackTestResult" style="margin-top:8px;font-size:0.85rem;"></div>
<details class="settings-help" style="margin-top:12px;">
  <summary>도움말</summary>
  <ul>
    <li>회사 슬랙(Datarize)에 등록된 이메일을 입력하세요.</li>
    <li>워크스페이스 admin이 봇을 미리 설치한 상태여야 합니다 (HR/IT 문의).</li>
    <li>본인 DM으로 답변과 인사이트가 전송됩니다.</li>
  </ul>
</details>
```

`isValidWebhookUrl` → `isValidDatarizeEmail`:
```ts
const ALLOWED_DOMAIN = '@datarize.ai';
function isValidDatarizeEmail(s: string): boolean {
  const trimmed = s.trim().toLowerCase();
  if (!trimmed.endsWith(ALLOWED_DOMAIN)) return false;
  const local = trimmed.slice(0, -ALLOWED_DOMAIN.length);
  return /^[a-z0-9._-]+$/.test(local);
}
```

`bindSlackHandlers` 갱신: input/save/test/clear 모두 email 기반. Test 버튼은 sendAnswerDm 호출(샘플 question/answer).

- [ ] **Step 5: Run test (PASS)**

```bash
npm test -- tests/unit/ui/tabs/settings-slack.spec.ts
```

Expected: 5 pass.

- [ ] **Step 6: TS strict + lint**

```bash
npx tsc --noEmit && npm run lint
```

- [ ] **Step 7: Commit**

```bash
git add src/ui/tabs/settings.ts tests/unit/ui/tabs/settings-slack.spec.ts
git commit -m "$(cat <<'EOF'
feat(v3.19 T6): settings Slack section UI 재작성 (webhook → email)

input type="email", placeholder you@datarize.ai, 도메인 화이트리스트 검증.
테스트 버튼 → sendAnswerDm 샘플 메시지 발송.
도움말 <details>: workspace admin 안내 + 본인 DM 동작 설명.

5 vitest 신규.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: tier-reason dataset 진단 (renderBriefingCard + hydrateBriefings)

**문제**: 사용자 캡쳐 분석 결과 6개 카드 모두 `data-tier="2"` (이미지 없음 fallback). `data-tier-reason` dataset + `console.info` 1회로 진단 자산 영구화.

**Files:**
- Modify: `src/ui/handlers/home.ts:419` (renderBriefingCard tier-reason)
- Modify: `src/ui/handlers/home.ts:388` (hydrateBriefings console.info)
- Test: `tests/unit/ui/handlers/home-tier-reason.spec.ts` (신규)

- [ ] **Step 1: Write failing tests**

`tests/unit/ui/handlers/home-tier-reason.spec.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { renderBriefingCard } from '../../../../src/ui/handlers/home';

describe('renderBriefingCard tier-reason', () => {
  const base = {
    id: 'b1', date: '2026-05-07', url: 'https://x.com', title: 't', summary: 's',
    scrapped: false, read: false, memo: '', sourceTitle: 'Src',
  };

  it('imageUrl 없으면 tier=2 + reason=no-image-url', () => {
    const card = renderBriefingCard(base, 0);
    expect(card.dataset['tier']).toBe('2');
    expect(card.dataset['tierReason']).toBe('no-image-url');
  });

  it('imageUrl 있으면 tier=1 + reason=loading', () => {
    const card = renderBriefingCard({ ...base, imageUrl: 'https://x.com/i.jpg' }, 0);
    expect(card.dataset['tier']).toBe('1');
    expect(card.dataset['tierReason']).toBe('loading');
  });

  it('img onerror 시 tier=2 + reason=image-error', () => {
    const card = renderBriefingCard({ ...base, imageUrl: 'https://x.com/i.jpg' }, 0);
    const img = card.querySelector('img')!;
    img.dispatchEvent(new Event('error'));
    expect(card.dataset['tier']).toBe('2');
    expect(card.dataset['tierReason']).toBe('image-error');
  });

  it('img onload 시 reason=image-loaded', () => {
    const card = renderBriefingCard({ ...base, imageUrl: 'https://x.com/i.jpg' }, 0);
    const img = card.querySelector('img')!;
    img.dispatchEvent(new Event('load'));
    expect(card.dataset['tierReason']).toBe('image-loaded');
  });
});
```

- [ ] **Step 2: Run test (FAIL)**

```bash
npm test -- tests/unit/ui/handlers/home-tier-reason.spec.ts
```

- [ ] **Step 3: Modify renderBriefingCard at line ~419 (P1-5 사전 review fix: listener 등록 순서)**

기존 코드는 `img.src = b.imageUrl` 후 listener 등록 — cache hit 시 동기 fire race 가능. listener 등록을 src 할당 **앞으로** 이동.

```ts
// dataset 분기 (기존: card.dataset['tier'] = b.imageUrl ? '1' : '2'):
if (b.imageUrl) {
  card.dataset['tier'] = '1';
  card.dataset['tierReason'] = 'loading';
} else {
  card.dataset['tier'] = '2';
  card.dataset['tierReason'] = 'no-image-url';
}

// img 생성 시 listener 먼저 등록 후 src 할당:
if (b.imageUrl) {
  const img = document.createElement('img');
  img.className = 'card-thumb';
  img.alt = '';
  img.setAttribute('loading', idx < 3 ? 'eager' : 'lazy');
  img.setAttribute('decoding', 'async');
  img.setAttribute('referrerpolicy', 'no-referrer');
  img.addEventListener('load', () => {
    card.dataset['tierReason'] = 'image-loaded';
  });
  img.addEventListener('error', () => {
    card.dataset['tier'] = '2';
    card.dataset['tierReason'] = 'image-error';
    img.remove();
  });
  img.src = b.imageUrl;  // ← listener 등록 후 src 할당 (cache hit race 차단)
  main.append(img);
}
```

- [ ] **Step 4: hydrateBriefings 진단 console.info 추가** (line ~388)

```ts
function hydrateBriefings(): void {
  const list = loadBriefings();
  const scroll = document.getElementById('briefingScroll');
  if (!scroll) return;
  const today = getDateStr();
  const firstDate = list[0]?.date;
  const isStale = list.length === 0 || (firstDate !== undefined && firstDate !== today);

  // v3.19 T7: 진단 정보 (파란 화면 #3 production reproduce 근거)
  console.info('[dg.briefings.diag]', {
    swState: navigator.serviceWorker?.controller ? 'controlled' : 'no-controller',
    itemCount: list.length,
    sample: list[0] ? {
      source: list[0].sourceTitle,
      hasImage: !!list[0].imageUrl,
      imageUrlPrefix: list[0].imageUrl?.slice(0, 50),
    } : null,
    today,
    isStale,
  });

  // (이하 기존 코드 동일)
```

- [ ] **Step 5: Run test (PASS)**

```bash
npm test -- tests/unit/ui/handlers/home-tier-reason.spec.ts
```

Expected: 4 pass.

- [ ] **Step 6: TS strict + lint**

```bash
npx tsc --noEmit && npm run lint
```

- [ ] **Step 7: Commit**

```bash
git add src/ui/handlers/home.ts tests/unit/ui/handlers/home-tier-reason.spec.ts
git commit -m "$(cat <<'EOF'
feat(v3.19 T7): tier-reason dataset 진단 + hydrateBriefings console.info

card.dataset.tierReason ∈ {no-image-url, loading, image-loaded, image-error}.
hydrateBriefings 첫 진입 시 SW state + RSS sample 1회 console.info.

v3.18 #3 production reproduce 근거 자산화. 가설 fix(T8/T9) 빗나갈 시 v3.20 hotfix 진단 정보로 활용.

4 vitest 신규.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: SW 가설 fix 1 (image 검증 캐싱 + CACHE bump v8)

**문제**: rss2json 응답이 image 없는 형태로 SW에 캐싱되면 후속 진입에서 stale 그대로 반환. image 1개 이상 검증 후만 cache.

**Files:**
- Modify: `public/sw.js` (CACHE_NAME bump + image 검증 분기)

- [ ] **Step 0: production image 추출 로직 동기화 (P1-3 사전 review fix)**

```bash
grep -n "extractImage\|enclosure\|thumbnail\|description" src/services/rss.ts | head -10
```

확인된 출처: [src/services/rss.ts:35](src/services/rss.ts#L35) `extractImage`가 3-tier 추출:
1. `enclosure.link` (image/* type)
2. `thumbnail` (top-level)
3. `description` 내부 `<img src="...">` DOMParser

SW 검증 분기는 production parser와 정합되도록 **lightweight**하게 유지 — 첫 1건만 검증 (전체 items 순회 비용 회피, 첫 paint 지연 최소화):

- [ ] **Step 1: Modify public/sw.js (lightweight image 검증)**

```js
// 기존:
// const CACHE_NAME = 'daily-growth-v7';
// 신규:
const CACHE_NAME = 'daily-growth-v8';

// rss2json 분기 (기존 line 41-54):
if (url.hostname.includes('generativelanguage') ||
    url.hostname.includes('rss2json') ||
    url.hostname.includes('api.rss2json')) {
  event.respondWith(
    fetch(event.request)
      .then(async (response) => {
        if (response.ok && url.hostname.includes('rss2json')) {
          // v3.19 T8: image-bearing 응답만 캐싱 (#3 파란 화면 가설 fix)
          // 첫 1건만 검증 (전체 items 순회 비용 회피, 첫 paint 지연 최소화)
          const peek = response.clone();
          let shouldCache = false;
          try {
            const data = await peek.json();
            const first = Array.isArray(data?.items) ? data.items[0] : null;
            shouldCache = !!first && (
              typeof first?.enclosure?.link === 'string' ||
              typeof first?.thumbnail === 'string' ||
              (typeof first?.description === 'string' && /<img\s/i.test(first.description))
            );
          } catch { shouldCache = false; }
          if (shouldCache) {
            const clone = response.clone();
            caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
          }
        }
        return response;
      })
      .catch(() => caches.match(event.request).then((hit) => hit || Response.error()))
  );
  return;
}
```

검증 3-tier가 production `extractImage`와 1:1 동기화 — image 누락 응답이 stale로 영구 반환되는 경로 차단.

- [ ] **Step 2: 수동 테스트 (smoke로 자동화 어려움)**

```bash
npm run build && npx http-server dist -p 8080
```

브라우저에서:
1. DevTools → Application → Service Workers → "daily-growth-v8" install 확인
2. 이전 v7 cache 삭제 확인 (activate 시 자동)

- [ ] **Step 3: Commit**

```bash
git add public/sw.js
git commit -m "$(cat <<'EOF'
fix(v3.19 T8): SW image-bearing rss2json 응답만 캐싱 (가설 fix #3)

CACHE_NAME v7→v8 bump. rss2json 분기에서 items[]에 enclosure.link 또는 thumbnail 1개 이상 존재 검증 후만 cache. image 누락 응답이 stale로 영구 반환되는 경로 차단.

가설 빗나갈 가능성 medium — 진단 정보(T7)와 동행, retro 검증 의무.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: RSS 가설 fix 2 (await refreshBriefings 직렬화)

**문제**: [hydrateBriefings:402](src/ui/handlers/home.ts) 빈 list + stale 진입에서 `void refreshBriefings()` → fire-and-forget. 후속 fetch 완료 시 hydrateBriefings 재호출(line 619)이지만 첫 paint race 가능. `await refreshBriefings()` 직렬화로 차단.

**Trade-off (spec §3.4 정합, P1-3 사전 review fix)**: refreshBriefings 내부에서 5s timeout → 직렬화 시 첫 paint timing이 뒤로 이동. fetch latency 자체는 동일(현행도 어차피 5s까지 fetch). refreshBriefings가 끝에서 hydrateBriefings()를 다시 호출하므로(home.ts:619) 직렬화는 paint 횟수 줄이는 단순화 + race 차단. 사용자가 보는 "skeleton → 실제 카드" 전환 시점은 약간 뒤로 이동 — race 차단의 trade-off로 인정.

**Files:**
- Modify: `src/ui/handlers/home.ts:402`
- Test: 기존 home.spec.ts (있다면) 또는 신규

- [ ] **Step 1: refreshBriefings 흐름 재확인**

[home.ts:560-619](src/ui/handlers/home.ts) 보면 `refreshBriefings` 끝에 `hydrateBriefings()` 호출(line 619). 즉 fire-and-forget도 결국 fetched 후 다시 hydrate. 직렬화는 단순화 + race 방지.

- [ ] **Step 2: Modify hydrateBriefings**

```ts
async function hydrateBriefings(): Promise<void> {
  const list = loadBriefings();
  const scroll = document.getElementById('briefingScroll');
  if (!scroll) return;

  const today = getDateStr();
  const firstDate = list[0]?.date;
  const isStale = list.length === 0 || (firstDate !== undefined && firstDate !== today);

  console.info('[dg.briefings.diag]', { /* T7 */ });

  if (isStale) {
    const user = getCachedUser();
    const hasInterests = !!user && user.interests.length > 0;
    if (hasInterests && !sessionStorage.getItem(AUTO_REFRESH_SESSION_KEY)) {
      sessionStorage.setItem(AUTO_REFRESH_SESSION_KEY, '1');
      // v3.19 T9: void → await (race 차단 + 단일 paint)
      await refreshBriefings();
      return; // refreshBriefings 내부에서 hydrateBriefings 재호출하므로 여기서 종료
    }
    if (list.length === 0) return;
  }

  scroll.replaceChildren();
  list.forEach((b, i) => scroll.append(renderBriefingCard(b, i)));
  enqueueEnglishTitleTranslations(list);
}
```

호출부도 `void hydrateBriefings()` → `await hydrateBriefings()` 또는 `void` 유지(시그니처가 Promise 반환으로 변경됨에 따른 lint 경고 차단). 호출부 grep:

```bash
grep -n "hydrateBriefings()" src/
```

각 호출부 `void hydrateBriefings();` 확인 (top-level void는 OK).

- [ ] **Step 3: TS strict + lint**

```bash
npx tsc --noEmit && npm run lint
```

- [ ] **Step 4: vitest 회귀 (briefing 관련)**

```bash
npm test -- tests/unit/ui/handlers/home
```

기존 spec 모두 pass 확인.

- [ ] **Step 5: Commit**

```bash
git add src/ui/handlers/home.ts
git commit -m "$(cat <<'EOF'
fix(v3.19 T9): hydrateBriefings → async + await refreshBriefings (가설 fix #3)

빈 list + stale 첫 진입에서 void → await으로 race 차단. refreshBriefings 내부 hydrateBriefings 재호출과 합쳐 단일 paint 보장.

UX trade-off 0 (refreshBriefings 종료 시 어차피 hydrate 재호출, 직렬화는 첫 paint timing 단축).

가설 빗나갈 가능성 medium — 진단 정보(T7)와 동행.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 10: tests/helpers/seed.ts 신규 helper

**문제**: 25 smoke spec inline `gardenIntroduced: true` seed → helper 통일 (N1 v3.18.1 lessons #1). 사전 review P0-3 정정: count 23 → **25** (실측 `grep -rln "gardenIntroduced" tests/smoke/ \| wc -l`).

**Files:**
- Create: `tests/helpers/seed.ts`
- Test: `tests/unit/helpers/seed.spec.ts` (선택, helper logic 검증)

- [ ] **Step 1: production read key 검증 (P0-3 사전 review fix — 핵심)**

```bash
grep -n "localStorage.getItem\|localStorage.setItem\|const KEY" src/state/user.ts | head -10
```

**확인된 사실**: [src/state/user.ts:36](src/state/user.ts#L36) `const KEY = 'user';` — production이 read/write하는 단일 키는 **`'user'`**. `'dg_user'`는 production에서 읽지 않음 → 현재 일부 spec (cardnews/heatmap 등)에서 `'dg_user'` set은 사실상 dead seed. helper는 production 정합을 위해 `'user'` 키로 set.

- [ ] **Step 2: 기존 seed 패턴 확인 (참고용)**

```bash
grep -A 3 "localStorage.setItem('user'" tests/smoke/v311-archive-cards.spec.ts | head -10
```

기존 패턴은 마이그레이션 시 dead `'dg_user'` set은 제거하고 `'user'` 키로 통일 (T11에서 일괄 적용).

- [ ] **Step 3: Write tests/helpers/seed.ts (production 'user' 키 사용)**

```ts
import type { Page } from '@playwright/test';

export interface PrimeUserOpts {
  gardenIntroduced?: boolean;
  schemaVersion?: 2 | 3 | 4;
  interests?: string[];
  streak?: number;
  xp?: number;
  level?: number;
  name?: string;
}

const DEFAULTS = {
  gardenIntroduced: true,
  schemaVersion: 2 as const,
  interests: ['tech'],
  streak: 0,
  xp: 0,
};

export async function primeOnboardedUser(page: Page, opts: PrimeUserOpts = {}): Promise<void> {
  const flags = { ...DEFAULTS, ...opts };
  await page.addInitScript((f) => {
    const user = {
      onboardedAt: '2026-04-01',
      interests: f.interests,
      streak: f.streak,
      lastActiveDate: '',
      xp: f.xp,
      earnedBadges: {},
      gamificationMigrated: true,
      gardenIntroduced: f.gardenIntroduced,
      schemaVersion: f.schemaVersion,
      ...(f.name ? { name: f.name } : {}),
      ...(f.level !== undefined ? { level: f.level } : {}),
    };
    // production read key (src/state/user.ts:36 const KEY = 'user')
    localStorage.setItem('user', JSON.stringify(user));
  }, flags);
}
```

- [ ] **Step 4: 단순 단위 테스트 (선택)**

skip 가능. 마이그레이션(T11) smoke pass가 사실상 회귀 테스트.

- [ ] **Step 5: Commit**

```bash
git add tests/helpers/seed.ts
git commit -m "$(cat <<'EOF'
feat(v3.19 T10): tests/helpers/seed.ts:primeOnboardedUser 신규

v3.18.1 lessons #1 graduation. inline gardenIntroduced 23 spec 마이그레이션 baseline. flag default true (smoke 정상화 후 modal 회귀 차단).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 11: 25 smoke spec helper 마이그레이션

**문제**: 25 smoke spec의 inline seed → `await primeOnboardedUser(page)` 대체. mechanical bulk fix. 사전 review P0-3 정정: count 23 → **25** (실측).

**Files:**
- Modify: `tests/smoke/*.spec.ts` (gardenIntroduced 시드한 25 spec)

- [ ] **Step 1: 대상 spec 목록 확정**

```bash
grep -rln "gardenIntroduced" tests/smoke/ | sort
wc -l <(grep -rln "gardenIntroduced" tests/smoke/)
```

Expected: 25개 spec.

- [ ] **Step 2: 각 spec에서 inline seed → helper 호출**

패턴 1 (`localStorage.setItem('user', ...)`):
```diff
- await page.addInitScript(() => {
-   localStorage.setItem('user', JSON.stringify({ interests: ['tech'], gamificationMigrated: true, gardenIntroduced: true }));
- });
+ import { primeOnboardedUser } from '../helpers/seed';
+ await primeOnboardedUser(page, { interests: ['tech'] });
```

패턴 2 (`localStorage.setItem('dg_user', ...)`):
```diff
- await page.addInitScript(() => {
-   localStorage.setItem('dg_user', JSON.stringify({
-     onboardedAt: '2026-04-01', streak: 0, lastActiveDate: '', xp: 0,
-     earnedBadges: {}, gamificationMigrated: true, gardenIntroduced: true, schemaVersion: 2,
-     interests: ['tech'],
-   }));
- });
+ import { primeOnboardedUser } from '../helpers/seed';
+ await primeOnboardedUser(page);
```

스펙별 schemaVersion (2/3/4) 정확히 그대로 옵션으로 전달:
- v3.13-missions.spec.ts → `schemaVersion: 3`
- 기타 다수 → `schemaVersion: 2`

- [ ] **Step 3: Smoke parallel pass 검증**

```bash
npm run test:smoke
```

Expected: 97/97 pass (변경 0).

- [ ] **Step 4: 각 마이그레이션 spec 단독 실행 PASS (P0-3 사전 review 권고)**

병렬 통과만으로는 isolation 검증 불충분. 25 spec 1개씩 단독 실행:
```bash
for f in $(grep -rln "primeOnboardedUser" tests/smoke/); do npm run test:smoke -- --workers=1 "$f" || echo "FAIL: $f"; done
```

Expected: 25/25 단독 실행 PASS (FAIL 0).

- [ ] **Step 5: N=10 cold-fork baseline 재확인 (helper 마이그레이션 isolation 검증)**

```bash
for i in {1..10}; do npm run test:smoke -- --workers=1 tests/smoke/layout.spec.ts || echo "FAIL run $i"; done
```

Expected: 10/10 pass.

- [ ] **Step 6: Commit**

```bash
git add tests/smoke/
git commit -m "$(cat <<'EOF'
refactor(v3.19 T11): 23 smoke spec inline seed → primeOnboardedUser() helper

mechanical bulk migration. production code 변경 0. v3.18.1 lessons #1 graduation: single source of truth로 향후 modal 회귀 시 mechanical bulk fix 불필요.

97/97 smoke parallel + N=10 cold-fork layout 10/10 baseline 유지.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 12: Slack settings smoke spec 신규

**문제**: settings UI에서 이메일 입력/저장/자동전송 toggle/clear flow를 smoke로 회귀 보호.

**Files:**
- Create: `tests/smoke/slack-settings.spec.ts`

기존 `tests/smoke/slack.spec.ts`가 있음 (확인 필요): webhook 기반이었다면 v3.19 schema에 맞게 재작성 또는 삭제 + 신규.

- [ ] **Step 1: 기존 slack.spec.ts 확인**

```bash
cat tests/smoke/slack.spec.ts
```

webhook 기반이면 신규 spec으로 전체 교체.

- [ ] **Step 2: Write smoke spec**

`tests/smoke/slack-settings.spec.ts`:
```ts
import { test, expect } from '@playwright/test';
import { primeOnboardedUser } from '../helpers/seed';

test.describe('Slack settings (v3.19 email + autoSend)', () => {
  test('이메일 입력 → 저장 → 자동전송 toggle', async ({ page }) => {
    await primeOnboardedUser(page);
    // /api/sendAnswerDm 호출은 모두 200 mock (테스트 버튼 누르면 호출됨)
    await page.route('**/api/sendAnswerDm', (route) => route.fulfill({ status: 200, body: '{"ok":true}' }));

    await page.goto('/');
    await page.click('[data-tab="settings"]');

    const input = page.locator('#slackEmailInput');
    await input.fill('me@datarize.ai');
    await page.click('#saveSlackBtn');

    // autoSend row 노출
    await expect(page.locator('#slackAutoRow')).toBeVisible();
    await page.click('#slackAutoToggle');

    // localStorage에 저장 확인
    const stored = await page.evaluate(() => localStorage.getItem('dg_slack'));
    expect(stored).toContain('me@datarize.ai');
    expect(stored).toContain('true');
  });

  test('비-@datarize.ai 이메일 거부', async ({ page }) => {
    await primeOnboardedUser(page);
    await page.goto('/');
    await page.click('[data-tab="settings"]');

    await page.locator('#slackEmailInput').fill('me@gmail.com');
    await page.click('#saveSlackBtn');

    const result = await page.locator('#slackTestResult').textContent();
    expect(result).toMatch(/datarize\.ai/);

    const stored = await page.evaluate(() => localStorage.getItem('dg_slack'));
    expect(stored).toBeNull();
  });
});
```

- [ ] **Step 3: 기존 slack.spec.ts 폐기**

webhook 기반이면 삭제:
```bash
git rm tests/smoke/slack.spec.ts  # 단, 이름 그대로 신규 spec으로 갈 거면 skip
```

또는 신규 spec 이름을 `slack.spec.ts`로 그대로 사용.

- [ ] **Step 4: Smoke 실행**

```bash
npm run test:smoke -- tests/smoke/slack-settings.spec.ts
```

Expected: 2 pass.

- [ ] **Step 5: 전체 smoke baseline 재확인**

```bash
npm run test:smoke
```

Expected: 99/99 pass (97 + 2 신규) 또는 98/98 (기존 slack.spec.ts 1개 폐기 시).

- [ ] **Step 6: Commit**

```bash
git add tests/smoke/slack-settings.spec.ts tests/smoke/slack.spec.ts
git commit -m "$(cat <<'EOF'
feat(v3.19 T12): smoke spec slack settings UI flow 신규

이메일 입력/저장/autoSend toggle + 비-@datarize.ai 거부 회귀 보호.
sendAnswerDm Function 호출은 page.route mock (smoke는 backend 미배포 환경에서도 통과).

기존 webhook 기반 slack.spec.ts 폐기 (또는 교체).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 13: 사용자 액션 가이드 (deploy README) + 비용 알람

**문제**: backend 배포에는 사용자 액션 6단계 필요. README 또는 docs/DEPLOY_BACKEND.md에 절차 명문화. 코드 변경 0.

**Files:**
- Create: `docs/DEPLOY_BACKEND.md` 또는 `.claude/QUICK_START.md`에 추가 섹션

- [ ] **Step 1: docs/DEPLOY_BACKEND.md 작성**

```markdown
# Backend Deploy Guide (v3.19+)

## 1회 setup (Hayden 관리자 액션)

### 1. Slack App 생성
1. https://api.slack.com/apps → Create New App → From scratch
2. App Name: `Daily Growth Bot`, Workspace: Datarize
3. Bot Token Scopes 추가:
   - `chat:write`
   - `users:read.email`
   - `im:write`
4. Install to Workspace → admin approve
5. OAuth Tokens → "Bot User OAuth Token" (xoxb-...) 복사

### 2. Firebase Blaze 업그레이드
1. Firebase Console → Project Settings → Usage and billing
2. Modify plan → Blaze (신용카드 등록)
3. free tier 안에서 비용 0원 운영, 신용카드는 spike 방지용

### 3. Bot Token Secret Manager 등록 (P1-2 사전 review fix)
```bash
firebase use my-ai-assistant-904f3   # default project 명시 — 다른 project에 secret 잘못 저장되는 사고 방지
firebase functions:secrets:set SLACK_BOT_TOKEN
# 프롬프트에 xoxb-... 토큰 붙여넣기
```

### 4. 비용 알람 설정
Firebase Console → Cloud Billing → Budgets → Create Budget
- Amount: $5/월
- Threshold: 50% / 80% / 100%
- Email notification

### 5. Function 배포
```bash
firebase deploy --only functions:sendAnswerDm
```

### 6. 본인 이메일 등록 + 테스트
1. App 진입 → Settings → Slack 봇 알림
2. 회사 슬랙 등록 이메일 입력 (`hayden@datarize.ai`)
3. 🔗 테스트 버튼 클릭 → 본인 DM에 샘플 메시지 도착 확인

## 일반 배포 (코드 변경 후)
```bash
npm run build
firebase deploy --only hosting,functions:sendAnswerDm
```

## 롤백
```bash
firebase functions:rollback sendAnswerDm
# 또는 main HEAD 이전 버전으로 revert + 재배포
```

## 모니터링
- Function 로그: Firebase Console → Functions → sendAnswerDm → Logs
- 사용량: Firebase Console → Functions → Usage
- 비용: Firebase Console → Cloud Billing → Reports
```

- [ ] **Step 2: Commit**

```bash
git add docs/DEPLOY_BACKEND.md
git commit -m "$(cat <<'EOF'
docs(v3.19 T13): backend deploy guide — Slack App + Blaze + Secret + 비용 알람

Hayden admin 1회 setup 6단계 명문화. Function 배포 + 롤백 + 모니터링 절차 포함.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 14: 최종 review + 배포 + retro

- [ ] **Step 1: 3종 검증 (사이클-끝)**

```bash
npx tsc --noEmit && npm run lint && npm test && npm run test:smoke
```

Expected:
- TS strict 0 errors
- lint 0 errors
- vitest 921~931 pass (+25~35)
- smoke 99/99 (또는 98/98)

- [ ] **Step 2: Bundle 측정**

```bash
npm run build
gzip -c dist/assets/index-*.js | wc -c
```

Expected: 35122 ± 300 B (소프트 target ±0.3 kB).

- [ ] **Step 3: 최종 codex review (DEPLOY_APPROVED gate)**

Codex 권한 issue 시 controller self-review fallback (v3.14.5/v3.18 패턴). P0=0 확인 후만 배포.

- [ ] **Step 4: 사용자 액션 — 사용자가 Prerequisites 6단계 완료 했는지 확인**

체크리스트:
- [ ] Slack App 생성 + Bot Token 발급
- [ ] 워크스페이스 install + admin approve
- [ ] Firebase Blaze 업그레이드
- [ ] `firebase functions:secrets:set SLACK_BOT_TOKEN` 실행
- [ ] 비용 알람 $5 설정

미완료 시 사용자에게 명시적 요청 (PDCA Plan 단계 합의 사항).

- [ ] **Step 5: 배포**

```bash
npm run build
firebase deploy --only hosting,functions:sendAnswerDm
```

- [ ] **Step 6: Production smoke (수동)**

1. https://my-ai-assistant-904f3.web.app 진입
2. Settings → Slack 봇 알림 → `hayden@datarize.ai` 입력 → 저장
3. 🔗 테스트 버튼 클릭 → 본인 DM에 샘플 메시지 도착 확인
4. 답변 제출 → autoSend 활성 시 본인 DM에 답변 도착 확인
5. 첫 로그인 시뮬 (incognito 또는 localStorage clear) → DevTools console에서 `[dg.briefings.diag]` 로그 확인 + 카드뉴스 paint 정상 여부 확인
6. **DevTools Network 탭 — Function 호출이 same-origin `/api/sendAnswerDm`으로 가는지 확인 (P0-1 사전 review fix)**. 절대 URL `https://us-central1-*.cloudfunctions.net/...`로 가면 firebase.json rewrite 누락 또는 잘못된 fallback. CSP 차단 가능.

- [ ] **Step 7: 메모리 retro**

`memory_save_*` 호출:
- name: `DG v3.19 Slack DM Backend`
- description: `v3.19 배포 완료 — Firebase Functions backend + 파란 화면 진단/가설 fix + N1 helper. 2026-05-07.`
- type: project
- 본문: 배포 결과(commit count, vitest 수, smoke 수, bundle 측정값) + 가설 fix(SW + RSS race) 적중 여부 + 사용자 액션 완료 상태 + v3.20 carry-forward.

retro commit:
```bash
git commit --allow-empty -m "$(cat <<'EOF'
docs(v3.19 deploy): Firebase live — main HEAD <hash>

vitest 921+ / smoke 99 / bundle 35122±300 B / TS+lint 0.
backend Function sendAnswerDm 정상 배포 + Bot Token Secret Manager 등록 완료.
파란 화면 진단 정보(tier-reason + console.info) production live, 가설 fix(SW v8 + RSS await) 적중 검증은 다음 첫 로그인 reproduce 시점에.

v3.20 carry: 가설 fix 적중/실패 retro / N2 bundle trim / N3-N5 / v3.18 P2 7건 / v3.17 §7.2 신규 기능 5건.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Self-review 메타

본 plan은 spec §1~§10을 모두 task로 매핑.

| Spec section | Task |
|---|---|
| §2.1 Architecture | T2 (functions/) + T5 (frontend service) |
| §2.2 Schema migration | T1 (state/slack.ts) |
| §2.3 UI 변경 | T6 (settings.ts) |
| §2.4 Function 시그니처 | T2 + T3 + T4 |
| §2.5 Frontend service | T5 |
| §2.6 가드 6중 | T2 (도메인+cap) + T3 (rate limit) + T13 (maxInstances+알람 사용자 액션) |
| §2.7 사용자 액션 | T13 (DEPLOY_BACKEND.md) + T14 Step 4 (확인) |
| §3.1 진단 | T7 (tier-reason + console.info) |
| §3.2 가설 fix 1 | T8 (SW v8) |
| §3.3 가설 fix 2 | T9 (await refreshBriefings) |
| §3.4 적중 검증 | T14 Step 6 (production smoke) + retro |
| §4 N1 helper | T10 + T11 |
| §5 Test 전략 | 각 task TDD step + T12 smoke 신규 |
| §6 위험/할루시네이션 | T4 Step 1 (Slack docs 재확인) + T2 (Functions 2세대 docs) |
| §7 Carry-forward | T14 retro commit |
| §8 Codex 2-pass | T0 사전 + T14 최종 |

**Placeholder scan**: TBD/TODO 0건. 각 step 정확한 코드 + 명령어 + expected output 포함.

**Type consistency**: `SlackSettings { email, autoSend }` (T1) → `loadSlackSettings()` 반환 (T5) → `s.email` 사용 (T5/T6) 일관. `AnswerData` frontend(T5) ≠ backend(T4) — frontend는 question/answer/insight, backend는 streak/xp 추가 — 명시적으로 다름(전송 시 wrapping). Function rewrite path `/api/sendAnswerDm` (T5) ↔ firebase.json (T5) 일관.

**Spec coverage gap**: 없음. 각 spec 요구사항이 task 1개 이상 매핑.

---

**Status**: Plan 작성 + self-review 완료. 사전 codex review 후 T1~T14 순차 실행.
