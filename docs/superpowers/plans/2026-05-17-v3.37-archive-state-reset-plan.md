# v3.37 Archive State Reset + Focus Carry — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Plant detail action chip → archive 진입 시 stale filter 결과 0건을 회피하고 `#archiveSearch`에 자동 focus를 부여한다. counter-click 진입점과 동일 reset 시맨틱을 공유한다.

**Architecture:** archive handler에 `resetArchiveFilters()` 공용 export를 신설. module state 5종(`currentFilter` / `currentTokens` / `currentEntity` / `selectedIds` / `selectMode`) + DOM chip ARIA + question type row + select mode UI를 default('all')로 일괄 복귀. counter-click handler 안 inline 5줄을 흡수해 단일 진입점화. plant-detail `navigateToInterestArchive` 시퀀스에 dynamic import 후 reset 호출 + 시퀀스 끝에 `input.focus({ preventScroll: true })` 추가.

**Tech Stack:** TypeScript strict, Vitest (jsdom), Playwright, Vite, ESLint. 외부 라이브러리 0줄. Schema/CSP/Migration 0줄.

**Spec**: `docs/superpowers/specs/2026-05-17-v3.37-archive-state-reset-design.md`

---

## File Structure

| File | Type | Responsibility |
|---|---|---|
| `src/ui/handlers/archive.ts` | Modify | `resetArchiveFilters()` export 신설 + counter-click handler 내 inline reset 통합 |
| `src/ui/modals/plant-detail.ts` | Modify | `navigateToInterestArchive` 시퀀스에 dynamic import 후 `resetArchiveFilters()` 호출 + 끝에 `input.focus({ preventScroll: true })` |
| `tests/ui/handlers/archive-reset.spec.ts` | Create | `resetArchiveFilters` 단독 spec (state + DOM + counter-click 회귀) |
| `tests/ui/modals/plant-detail.spec.ts` | Modify | navigateToInterestArchive sequence spy (reset → switchTab → handleArchiveSearch → focus) |
| `tests/smoke/v3.36-plant-action-end-to-end.spec.ts` | Modify | 사전 entity/filter chip 활성 setup + reset/focus assertion 1 case 확장 |

신규 spec 파일을 분리한 이유: 기존 `tests/ui/handlers/archive.spec.ts`가 비대해질 위험. small purpose의 reset 단위 spec은 별도 파일로 격리하면 향후 다른 entry point 추가 시 동일 파일 확장.

---

## Task 0 — Plan v1 commit + Codex 사전 review

**Files:**
- Create: `docs/superpowers/plans/2026-05-17-v3.37-archive-state-reset-plan.md` (이 문서)
- Read: `docs/superpowers/specs/2026-05-17-v3.37-archive-state-reset-design.md`

- [ ] **Step 1: 본 plan 문서를 commit**

Plan 문서가 작성된 직후 stage + commit:

```bash
git add docs/superpowers/plans/2026-05-17-v3.37-archive-state-reset-plan.md
git commit -m "docs(v3.37 T0): plan v1 — task breakdown T1~T5 + Codex 사전 review dispatch 직전"
```

Expected: 새 commit 1개 (plan v1).

- [ ] **Step 2: Codex 사전 review CLI direct dispatch**

```bash
codex exec --skip-git-repo-check -o /tmp/v3.37-codex-pre.txt "review v3.37 spec (docs/superpowers/specs/2026-05-17-v3.37-archive-state-reset-design.md) and plan (docs/superpowers/plans/2026-05-17-v3.37-archive-state-reset-plan.md) against current main (HEAD). Focus: (1) resetArchiveFilters semantics consistency with counter-click integration — especially selectMode UI cleanup (archiveSelectToggle aria-pressed, archive--select-mode class, filter-chip disabled state); (2) plant-detail sequence: closeModal/resetArchiveFilters/switchTab/focus ordering and race conditions; (3) test coverage gaps (selectMode mid-state, mobile focus preventScroll fallback); (4) bundle impact estimate. Classify findings P0/P1/P2 with line references."
```

Expected: `/tmp/v3.37-codex-pre.txt` 생성, exit code 0. 한도/권한 issue 시 fallback 정책(spec §6.3) 적용.

- [ ] **Step 3: review 결과 분류**

`/tmp/v3.37-codex-pre.txt` 내용을 읽고 P0/P1/P2로 분류한다.

- **P0**: 차단 — plan v2 in-cycle 흡수 (예: spec contradiction, race 누락, schema 변경 누락)
- **P1**: in-cycle 흡수 (예: select mode UI cleanup 누락, focus 옵션 부재)
- **P2**: 검토 후 in-cycle 흡수 또는 v3.38 carry

분류 결과를 spec §6.6 "Pre-review findings" 표에 기록 (T5 retro 단계에서 채움 — T0에서는 결과만 기록 in plan v2).

- [ ] **Step 4: P0/P1 흡수 plan v2 commit (필요 시)**

P0/P1 발견 시 plan을 수정하고:

```bash
git add docs/superpowers/plans/2026-05-17-v3.37-archive-state-reset-plan.md \
        docs/superpowers/specs/2026-05-17-v3.37-archive-state-reset-design.md
git commit -m "docs(v3.37 T0 fix): Codex 사전 review P0/P1 흡수 — plan v2 + spec 보강"
```

P0/P1 없음 시 이 step skip. 그대로 T1 진입.

---

## Task 1 — `resetArchiveFilters()` 신설 + counter-click 통합

**Files:**
- Create: `tests/ui/handlers/archive-reset.spec.ts`
- Modify: `src/ui/handlers/archive.ts:26-33, 460-479` (module state + counter-click handler)

- [ ] **Step 1: 신규 spec 파일 작성 — `resetArchiveFilters` state 검증**

`tests/ui/handlers/archive-reset.spec.ts` 신설. 첫 spec은 state reset만 검증:

```typescript
import { describe, it, expect, beforeEach } from 'vitest';
import { JSDOM } from 'jsdom';

describe('resetArchiveFilters — module state', () => {
  beforeEach(async () => {
    const dom = new JSDOM('<!DOCTYPE html><html><body></body></html>', { url: 'http://localhost/' });
    (globalThis as { document?: Document }).document = dom.window.document;
    (globalThis as { window?: Window }).window = dom.window as unknown as Window;
    vi.resetModules();
  });

  it('state 5종이 default 값으로 복귀한다', async () => {
    const mod = await import('../../../src/ui/handlers/archive');
    // 사전 setup: 모든 state를 non-default로 mutate
    mod.handleArchiveFilter('memo');
    // currentTokens는 handleArchiveSearch가 input value로 갱신하므로 별도 mock
    const input = document.createElement('input');
    input.id = 'archiveSearch';
    input.value = 'foo bar';
    document.body.appendChild(input);
    mod.handleArchiveSearch();

    mod.resetArchiveFilters();

    // 검증: 외부에서 module state를 직접 읽지 못하므로 rerenderList side effect로 간접 검증
    // → 후속 spec(DOM 동기화)으로 보강. 본 spec은 export 시그니처 + throw 안 함만 가드.
    expect(typeof mod.resetArchiveFilters).toBe('function');
  });
});
```

> **Note**: archive.ts module state는 closure이라 직접 read 불가. state 검증은 다음 step의 DOM 동기화 spec으로 위임 (DOM이 state의 가시 표현).

- [ ] **Step 2: 위 spec 실행해서 fail 확인**

```bash
npx vitest run tests/ui/handlers/archive-reset.spec.ts
```

Expected: FAIL with "resetArchiveFilters is not a function" (export 부재).

- [ ] **Step 3: `archive.ts`에 `resetArchiveFilters` export 신설**

`src/ui/handlers/archive.ts`에 `selectedIds`/`selectMode` 선언(`:107-108`) 직후 신규 함수 추가:

```typescript
/**
 * v3.37 T1: archive module state + DOM chip ARIA 일괄 default('all') 복귀.
 * 진입점: counter-click handler / plant-detail navigateToInterestArchive.
 *
 * Reset 대상:
 *   - currentFilter / currentTokens / currentEntity / selectedIds / selectMode
 *
 * DOM 동기화:
 *   - .archive-entity-chip → 'all'만 active + aria-checked='true'
 *   - .filter-chip → 'all'만 active + disabled=false (selectMode 중간 lock 해제)
 *   - #archiveFilters question type row → display='' (entity 'scrap'/'insight' 시 숨김 상태 복구)
 *   - #archiveSelectToggle aria-pressed='false'
 *   - #archiveTab .archive--select-mode 제거
 *   - .archive-card.selected 클래스 제거
 *
 * 명시적 비-호출:
 *   - rerenderList() 호출 안 함 — caller가 명시 호출 (counter-click rerenderList / plant-action handleArchiveSearch)
 *   - updateBulkButton() 호출 안 함 — caller가 필요 시 호출 (현행 reset 진입점에서는 불필요)
 *
 * `__archiveMounted`는 reset 대상 아님 — mount 가드는 1회성.
 */
export function resetArchiveFilters(): void {
  // Module state
  currentFilter = 'all';
  currentTokens = [];
  currentEntity = 'all';
  selectedIds.clear();
  selectMode = false;

  // Entity chip ARIA
  document.querySelectorAll<HTMLButtonElement>('.archive-entity-chip').forEach((b) => {
    const active = b.dataset['entity'] === 'all';
    b.classList.toggle('active', active);
    b.setAttribute('aria-checked', active ? 'true' : 'false');
  });

  // Filter chip active + selectMode lock 해제
  document.querySelectorAll<HTMLButtonElement>('.filter-chip').forEach((chip) => {
    chip.classList.toggle('active', chip.dataset['filter'] === 'all');
    chip.disabled = false;
    chip.removeAttribute('aria-disabled');
  });

  // Question type row 표시 복구 (entity 'scrap'/'insight'에서 display:none 상태 가능)
  const q = document.getElementById('archiveFilters');
  if (q) q.style.display = '';

  // Select mode UI 흔적 제거
  document.getElementById('archiveSelectToggle')?.setAttribute('aria-pressed', 'false');
  document.getElementById('archiveTab')?.classList.remove('archive--select-mode');
  document.querySelectorAll('.archive-card.selected').forEach((c) =>
    c.classList.remove('selected')
  );
}
```

- [ ] **Step 4: Step 1 spec 재실행해서 PASS**

```bash
npx vitest run tests/ui/handlers/archive-reset.spec.ts
```

Expected: PASS (1 test).

- [ ] **Step 5: DOM 동기화 spec 추가**

`tests/ui/handlers/archive-reset.spec.ts`에 spec 4개 추가:

```typescript
describe('resetArchiveFilters — DOM 동기화', () => {
  beforeEach(async () => {
    const dom = new JSDOM('<!DOCTYPE html><html><body></body></html>', { url: 'http://localhost/' });
    (globalThis as { document?: Document }).document = dom.window.document;
    (globalThis as { window?: Window }).window = dom.window as unknown as Window;
    vi.resetModules();
  });

  function setupArchiveDom(): void {
    document.body.innerHTML = `
      <div id="archiveTab" class="archive--select-mode">
        <div id="entityChips">
          <button class="archive-entity-chip active" data-entity="scrap" aria-checked="true">스크랩</button>
          <button class="archive-entity-chip" data-entity="all" aria-checked="false">전체</button>
        </div>
        <div id="archiveFilters" style="display: none;">
          <button class="filter-chip active" data-filter="memo" disabled aria-disabled="true">메모</button>
          <button class="filter-chip" data-filter="all" disabled aria-disabled="true">전체</button>
        </div>
        <button id="archiveSelectToggle" aria-pressed="true">선택</button>
        <div class="archive-card selected" data-answer-id="a1"></div>
      </div>
    `;
  }

  it('entity chip 동기화 — all만 active + aria-checked=true', async () => {
    setupArchiveDom();
    const mod = await import('../../../src/ui/handlers/archive');
    mod.resetArchiveFilters();

    const all = document.querySelector<HTMLButtonElement>('.archive-entity-chip[data-entity="all"]')!;
    const scrap = document.querySelector<HTMLButtonElement>('.archive-entity-chip[data-entity="scrap"]')!;
    expect(all.classList.contains('active')).toBe(true);
    expect(all.getAttribute('aria-checked')).toBe('true');
    expect(scrap.classList.contains('active')).toBe(false);
    expect(scrap.getAttribute('aria-checked')).toBe('false');
  });

  it('filter chip 동기화 — all active + disabled 해제', async () => {
    setupArchiveDom();
    const mod = await import('../../../src/ui/handlers/archive');
    mod.resetArchiveFilters();

    const all = document.querySelector<HTMLButtonElement>('.filter-chip[data-filter="all"]')!;
    const memo = document.querySelector<HTMLButtonElement>('.filter-chip[data-filter="memo"]')!;
    expect(all.classList.contains('active')).toBe(true);
    expect(all.disabled).toBe(false);
    expect(memo.classList.contains('active')).toBe(false);
    expect(memo.disabled).toBe(false);
    expect(memo.hasAttribute('aria-disabled')).toBe(false);
  });

  it('question type row display 복구 + select mode UI 흔적 제거', async () => {
    setupArchiveDom();
    const mod = await import('../../../src/ui/handlers/archive');
    mod.resetArchiveFilters();

    expect(document.getElementById('archiveFilters')!.style.display).toBe('');
    expect(document.getElementById('archiveSelectToggle')!.getAttribute('aria-pressed')).toBe('false');
    expect(document.getElementById('archiveTab')!.classList.contains('archive--select-mode')).toBe(false);
    expect(document.querySelectorAll('.archive-card.selected').length).toBe(0);
  });

  it('archive DOM이 mount 안 된 상태(다른 탭)에서 silent no-op', async () => {
    // setupArchiveDom 호출 없음 — empty body
    const mod = await import('../../../src/ui/handlers/archive');
    expect(() => mod.resetArchiveFilters()).not.toThrow();
  });
});
```

- [ ] **Step 6: 위 spec 실행해서 모두 PASS**

```bash
npx vitest run tests/ui/handlers/archive-reset.spec.ts
```

Expected: 5 tests PASS (Step 1의 1건 + Step 5의 4건).

- [ ] **Step 7: counter-click handler 통합**

`src/ui/handlers/archive.ts:460-479`의 inline reset을 새 함수 호출로 교체:

**Before** (`archive.ts:460-479`):

```typescript
  // v3.27 T4: counter click → entity 'all' 전환 (다른 카테고리 핀 가시화 후 진입).
  document.addEventListener('click', (e) => {
    if (!(e.target as HTMLElement).closest('.other-pin-counter')) return;
    currentEntity = 'all';
    // v3.27 T4 (Codex P1-2): currentFilter 도 'all' 리셋 — 질문 chip 활성 상태에서 진입 시
    // 'all' merge가 아닌 type 분기로 빠져 다른 카테고리 핀 항목이 여전히 숨김 상태가 되는 버그 방지.
    currentFilter = 'all';
    document.querySelectorAll<HTMLButtonElement>('.archive-entity-chip').forEach((b) => {
      const active = b.dataset['entity'] === 'all';
      b.classList.toggle('active', active);
      b.setAttribute('aria-checked', active ? 'true' : 'false');
    });
    document.querySelectorAll<HTMLButtonElement>('.filter-chip').forEach((btn) => {
      btn.classList.toggle('active', btn.dataset['filter'] === 'all');
    });
    // 2차 row(질문 type) 숨김 해제
    const q = document.getElementById('archiveFilters');
    if (q) q.style.display = '';
    rerenderList();
  });
```

**After**:

```typescript
  // v3.27 T4 → v3.37 T1: counter click → resetArchiveFilters() 진입점 통합.
  // plant-detail navigateToInterestArchive과 동일 reset 시맨틱 공유.
  document.addEventListener('click', (e) => {
    if (!(e.target as HTMLElement).closest('.other-pin-counter')) return;
    resetArchiveFilters();
    rerenderList();
  });
```

- [ ] **Step 8: counter-click 회귀 spec 추가 (`archive-reset.spec.ts`)**

```typescript
describe('counter-click → resetArchiveFilters 통합', () => {
  beforeEach(async () => {
    const dom = new JSDOM('<!DOCTYPE html><html><body></body></html>', { url: 'http://localhost/' });
    (globalThis as { document?: Document }).document = dom.window.document;
    (globalThis as { window?: Window }).window = dom.window as unknown as Window;
    vi.resetModules();
  });

  it('.other-pin-counter click → entity/filter all active 복귀 + question row 표시', async () => {
    document.body.innerHTML = `
      <div id="archiveTab">
        <button class="archive-entity-chip active" data-entity="scrap" aria-checked="true">스크랩</button>
        <button class="archive-entity-chip" data-entity="all" aria-checked="false">전체</button>
        <div id="archiveFilters" style="display: none;"></div>
        <div class="other-pin-counter">다른 카테고리 보기</div>
      </div>
    `;
    const mod = await import('../../../src/ui/handlers/archive');
    mod.mountArchiveHandlers();

    document.querySelector<HTMLElement>('.other-pin-counter')!.click();

    const all = document.querySelector<HTMLButtonElement>('.archive-entity-chip[data-entity="all"]')!;
    expect(all.classList.contains('active')).toBe(true);
    expect(all.getAttribute('aria-checked')).toBe('true');
    expect(document.getElementById('archiveFilters')!.style.display).toBe('');
  });
});
```

> **Note**: `mountArchiveHandlers` export 명이 다를 수 있음 — `src/ui/handlers/archive.ts`의 mount 함수 이름을 grep해서 확인 후 호출 (`grep -n "export function mount" src/ui/handlers/archive.ts`).

- [ ] **Step 9: 3종 검증**

```bash
npx tsc --noEmit
npm run lint
npx vitest run tests/ui/handlers/archive-reset.spec.ts
```

Expected: 모두 0 errors / 모든 spec PASS.

- [ ] **Step 10: 영향 범위 vitest 회귀 확인**

```bash
npx vitest run tests/ui/handlers/
```

Expected: 기존 archive handler spec 영향 없음, 모두 PASS.

- [ ] **Step 11: T1 commit**

```bash
git add src/ui/handlers/archive.ts tests/ui/handlers/archive-reset.spec.ts
git commit -m "feat(v3.37 T1): resetArchiveFilters() export + counter-click handler 통합 (v3.36 P2-1 흡수)"
```

Expected: 새 commit, files: 2.

---

## Task 2 — plant-detail 시퀀스 변경

**Files:**
- Modify: `src/ui/modals/plant-detail.ts:98-112` (navigateToInterestArchive)
- Modify: `tests/ui/modals/plant-detail.spec.ts` (sequence spy)

- [ ] **Step 1: plant-detail.spec.ts에 신규 sequence spec 추가**

`tests/ui/modals/plant-detail.spec.ts` 끝에 describe 블록 추가:

```typescript
describe('navigateToInterestArchive sequence (v3.37 T2)', () => {
  beforeEach(async () => {
    const dom = new JSDOM('<!DOCTYPE html><html><body></body></html>', { url: 'http://localhost/' });
    (globalThis as { document?: Document }).document = dom.window.document;
    (globalThis as { window?: Window }).window = dom.window as unknown as Window;
    vi.resetModules();
  });

  it('reset → switchTab → handleArchiveSearch → focus 순서로 호출', async () => {
    document.body.innerHTML = `<input id="archiveSearch" />`;

    const resetSpy = vi.fn();
    const handleSearchSpy = vi.fn();
    const switchTabSpy = vi.fn().mockResolvedValue(undefined);
    const focusSpy = vi.fn();

    // mock handlers/archive module
    vi.doMock('../../../src/ui/handlers/archive', () => ({
      resetArchiveFilters: resetSpy,
      handleArchiveSearch: handleSearchSpy,
    }));
    vi.doMock('../../../src/ui/nav', () => ({
      switchTab: switchTabSpy,
    }));
    vi.doMock('../../../src/ui/modals/shared', () => ({
      openModal: vi.fn(),
      closeModal: vi.fn(),
    }));
    vi.doMock('../../../src/utils/interestKeywords', () => ({
      interestKeywords: () => ['전략', 'strategy'],
    }));

    // jsdom input.focus를 spy로 교체
    const input = document.querySelector<HTMLInputElement>('#archiveSearch')!;
    input.focus = focusSpy;

    const callOrder: string[] = [];
    resetSpy.mockImplementation(() => callOrder.push('reset'));
    switchTabSpy.mockImplementation(async () => { callOrder.push('switchTab'); });
    handleSearchSpy.mockImplementation(() => callOrder.push('handleArchiveSearch'));
    focusSpy.mockImplementation(() => callOrder.push('focus'));

    const { openPlantDetailModal } = await import('../../../src/ui/modals/plant-detail');
    // 정원 카드 클릭 흐름 emulate — openPlantDetailModal은 sequence 진입 전이라 직접 호출 어려움
    // → navigateToInterestArchive를 spec에서 직접 import 가능하도록 @internal export 추가 필요
    // (Step 3에서 export 추가)

    // 본 spec은 일단 placeholder — Step 3에서 navigateToInterestArchive @internal export로 확정
    expect(callOrder).toEqual(['reset', 'switchTab', 'handleArchiveSearch', 'focus']);
  });

  it('focus 옵션 preventScroll=true로 호출', async () => {
    // Step 3 export 후 채움
    expect(true).toBe(true); // placeholder
  });

  it('#archiveSearch 미존재 시 silent return (focus 호출 안 함)', async () => {
    // Step 3 export 후 채움
    expect(true).toBe(true); // placeholder
  });
});
```

> **Note**: 본 step에서는 spec scaffold만 작성. Step 3에서 `navigateToInterestArchive`를 `@internal` export로 노출하면서 본격적 sequence assertion 실현.

- [ ] **Step 2: 위 spec 실행해서 fail/skip 확인**

```bash
npx vitest run tests/ui/modals/plant-detail.spec.ts
```

Expected: 첫 spec FAIL (sequence 검증 실패 또는 import error). Step 3 진입.

- [ ] **Step 3: `plant-detail.ts` 변경 — navigateToInterestArchive 시퀀스 + `@internal` export**

`src/ui/modals/plant-detail.ts:98-112` 교체:

```typescript
/**
 * v3.37 T2: action chip → archive 진입 시 stale filter reset + focus 부여.
 * 시퀀스: closeModal → (dynamic import) resetArchiveFilters → switchTab → input.value=keyword → handleArchiveSearch → input.focus({ preventScroll: true })
 * @internal — spec 직접 호출용 export.
 */
export async function navigateToInterestArchive(interestId: string): Promise<void> {
  const keyword = pickSearchKeyword(interestId);
  if (!keyword) return;

  closeModal();
  // Codex v3.36 P1-1 흡수: 동적 import — plant-detail이 stats chunk이라 archive handler를 끌어오지 않도록.
  const { resetArchiveFilters, handleArchiveSearch } = await import('../handlers/archive');
  resetArchiveFilters();
  await switchTab('archive');

  const input = document.querySelector<HTMLInputElement>('#archiveSearch');
  if (!input) return;
  input.value = keyword;
  handleArchiveSearch();
  input.focus({ preventScroll: true });
}
```

> **변경 포인트**:
> 1. 함수 시그니처 `async function` → `export async function` (spec 직접 호출용 `@internal`).
> 2. dynamic import 객체에 `resetArchiveFilters` 추가.
> 3. `closeModal()` 직후 `resetArchiveFilters()` 호출 — `switchTab` 진입 전 state reset.
> 4. 끝에 `input.focus({ preventScroll: true })` 추가.

- [ ] **Step 4: Step 1 spec 보강 — navigateToInterestArchive 직접 호출**

`tests/ui/modals/plant-detail.spec.ts`의 신규 describe 블록을 다음으로 교체:

```typescript
describe('navigateToInterestArchive sequence (v3.37 T2)', () => {
  beforeEach(async () => {
    const dom = new JSDOM('<!DOCTYPE html><html><body></body></html>', { url: 'http://localhost/' });
    (globalThis as { document?: Document }).document = dom.window.document;
    (globalThis as { window?: Window }).window = dom.window as unknown as Window;
    vi.resetModules();
  });

  it('reset → switchTab → handleArchiveSearch → focus 순서로 호출', async () => {
    document.body.innerHTML = `<input id="archiveSearch" />`;

    const callOrder: string[] = [];
    const resetSpy = vi.fn(() => { callOrder.push('reset'); });
    const handleSearchSpy = vi.fn(() => { callOrder.push('handleArchiveSearch'); });
    const switchTabSpy = vi.fn(async () => { callOrder.push('switchTab'); });

    vi.doMock('../../../src/ui/handlers/archive', () => ({
      resetArchiveFilters: resetSpy,
      handleArchiveSearch: handleSearchSpy,
    }));
    vi.doMock('../../../src/ui/nav', () => ({ switchTab: switchTabSpy }));
    vi.doMock('../../../src/ui/modals/shared', () => ({ openModal: vi.fn(), closeModal: vi.fn() }));
    vi.doMock('../../../src/utils/interestKeywords', () => ({
      interestKeywords: () => ['전략', 'strategy'],
    }));

    const input = document.querySelector<HTMLInputElement>('#archiveSearch')!;
    const originalFocus = input.focus.bind(input);
    input.focus = vi.fn(() => { callOrder.push('focus'); originalFocus(); }) as typeof input.focus;

    const { navigateToInterestArchive } = await import('../../../src/ui/modals/plant-detail');
    await navigateToInterestArchive('investing');

    expect(callOrder).toEqual(['reset', 'switchTab', 'handleArchiveSearch', 'focus']);
  });

  it('focus 옵션 preventScroll=true로 호출', async () => {
    document.body.innerHTML = `<input id="archiveSearch" />`;
    vi.doMock('../../../src/ui/handlers/archive', () => ({
      resetArchiveFilters: vi.fn(), handleArchiveSearch: vi.fn(),
    }));
    vi.doMock('../../../src/ui/nav', () => ({ switchTab: vi.fn().mockResolvedValue(undefined) }));
    vi.doMock('../../../src/ui/modals/shared', () => ({ openModal: vi.fn(), closeModal: vi.fn() }));
    vi.doMock('../../../src/utils/interestKeywords', () => ({ interestKeywords: () => ['전략'] }));

    const input = document.querySelector<HTMLInputElement>('#archiveSearch')!;
    const focusSpy = vi.fn();
    input.focus = focusSpy as typeof input.focus;

    const { navigateToInterestArchive } = await import('../../../src/ui/modals/plant-detail');
    await navigateToInterestArchive('investing');

    expect(focusSpy).toHaveBeenCalledWith({ preventScroll: true });
  });

  it('#archiveSearch 미존재 시 silent return (focus 호출 안 함)', async () => {
    // input element를 mount 안 함
    document.body.innerHTML = '';

    vi.doMock('../../../src/ui/handlers/archive', () => ({
      resetArchiveFilters: vi.fn(), handleArchiveSearch: vi.fn(),
    }));
    vi.doMock('../../../src/ui/nav', () => ({ switchTab: vi.fn().mockResolvedValue(undefined) }));
    vi.doMock('../../../src/ui/modals/shared', () => ({ openModal: vi.fn(), closeModal: vi.fn() }));
    vi.doMock('../../../src/utils/interestKeywords', () => ({ interestKeywords: () => ['전략'] }));

    const { navigateToInterestArchive } = await import('../../../src/ui/modals/plant-detail');
    await expect(navigateToInterestArchive('investing')).resolves.toBeUndefined();
  });
});
```

- [ ] **Step 5: spec 재실행해서 모두 PASS**

```bash
npx vitest run tests/ui/modals/plant-detail.spec.ts
```

Expected: 신규 3 spec PASS. 기존 plant-detail spec 회귀 없음.

- [ ] **Step 6: 3종 검증**

```bash
npx tsc --noEmit
npm run lint
npx vitest run tests/ui/modals/ tests/ui/handlers/
```

Expected: 모두 0 errors / 모든 spec PASS.

- [ ] **Step 7: T2 commit**

```bash
git add src/ui/modals/plant-detail.ts tests/ui/modals/plant-detail.spec.ts
git commit -m "feat(v3.37 T2): plant-detail navigateToInterestArchive — resetArchiveFilters 호출 + input.focus preventScroll (v3.36 P2-2 흡수)"
```

Expected: 새 commit, files: 2.

---

## Task 3 — smoke 확장 (stale filter regression guard + focus)

**Files:**
- Modify: `tests/smoke/v3.36-plant-action-end-to-end.spec.ts` (1 case 추가)

- [ ] **Step 1: 기존 smoke spec 끝에 case 추가**

`tests/smoke/v3.36-plant-action-end-to-end.spec.ts` 마지막 `test(...)` 뒤에 다음 case 추가:

```typescript
test('stale filter setup 후 plant action → archive entity/filter all reset + focus', async ({ page }) => {
  // 1) home 진입 + 사전 시드 (기존 smoke fixture 동일 패턴 사용)
  await page.goto('/');
  // ... (기존 spec과 동일한 사전 시드 단계 — fixture import 또는 page.evaluate seed)

  // 2) archive 탭 진입 + entity '스크랩' chip + filter '메모' chip 활성화 (UI 클릭으로 production 행동 일치)
  await page.getByRole('button', { name: /archive/i }).click();
  await page.locator('.archive-entity-chip[data-entity="scrap"]').click();
  // 사전 검증: entity 'scrap' active
  await expect(page.locator('.archive-entity-chip[data-entity="scrap"]')).toHaveClass(/active/);

  // filter chip은 entity 'scrap' 진입 시 숨김 — entity 'answer'로 잠깐 전환 후 filter 적용
  // (또는 entity 'all'에서 직접 filter '메모' 적용)
  await page.locator('.archive-entity-chip[data-entity="all"]').click();
  // filter chip 'memo' 선택 (data-filter 값은 archive.ts 정의 따름 — 'memo' 가정, 실제 grep 필요)
  const memoChip = page.locator('.filter-chip[data-filter="memo"]');
  if (await memoChip.count() > 0) {
    await memoChip.click();
    await expect(memoChip).toHaveClass(/active/);
  }

  // 3) 정원 탭 진입 → 식물 카드 클릭 → modal open
  await page.getByRole('button', { name: /정원/i }).click();
  await page.locator('.garden-grid .garden-cell').first().click();
  // modal 가시 대기
  await expect(page.locator('.plant-detail-modal')).toBeVisible({ timeout: 10_000 });

  // 4) "📚 이 분야로 archive 탐색" chip 클릭
  await page.locator('.plant-action-chip').click();

  // 5) 검증
  // archive 탭 활성
  await expect(page.locator('#archiveTab')).toBeVisible({ timeout: 10_000 });

  // entity chip 'all' active
  const entityAll = page.locator('.archive-entity-chip[data-entity="all"]');
  await expect(entityAll).toHaveClass(/active/);
  await expect(entityAll).toHaveAttribute('aria-checked', 'true');

  // filter chip 'all' active
  await expect(page.locator('.filter-chip[data-filter="all"]')).toHaveClass(/active/);

  // question type row 표시
  await expect(page.locator('#archiveFilters')).toBeVisible();

  // search input value + focus
  const searchInput = page.locator('#archiveSearch');
  await expect(searchInput).not.toHaveValue('');
  await expect(searchInput).toBeFocused();
});
```

> **Note**: 기존 v3.36 spec의 fixture / 사전 시드 패턴을 spec 안 다른 case에서 grep해 동일하게 적용. `data-filter` 값(`memo`/`scrap`/`all` 등)은 `src/ui/handlers/archive.ts` 또는 archive 렌더 코드에서 직접 grep해 확인 (현재 spec에 placeholder로 `memo` 사용).

- [ ] **Step 2: cold-start fresh fork 실행**

```bash
rm -rf node_modules/.vite-temp dist
npm run test:smoke -- tests/smoke/v3.36-plant-action-end-to-end.spec.ts
```

Expected: 신규 case 포함 모두 PASS.

- [ ] **Step 3: N=3 cold-start 안정성 확인**

```bash
for i in 1 2 3; do rm -rf node_modules/.vite-temp dist && npm run test:smoke -- tests/smoke/v3.36-plant-action-end-to-end.spec.ts 2>&1 | tail -5; done
```

Expected: 3/3 PASS, flake 0회.

- [ ] **Step 4: T3 commit**

```bash
git add tests/smoke/v3.36-plant-action-end-to-end.spec.ts
git commit -m "test(v3.37 T3): smoke 확장 — stale filter setup → plant action → entity/filter all reset + #archiveSearch focus 회귀 가드"
```

Expected: 새 commit, files: 1.

---

## Task 4 — 사이클 끝 검증 + bundle dual record + Codex 최종 review

**Files:** (commit 없음)

- [ ] **Step 1: 3종 검증 일괄**

```bash
npx tsc --noEmit && npm run lint && npm test
```

Expected: 모두 0 errors / 모든 spec PASS. test count 출력 캡처.

- [ ] **Step 2: 양쪽 TZ vitest**

```bash
TZ=America/New_York npm test 2>&1 | tail -5
TZ=Asia/Seoul npm test 2>&1 | tail -5
```

Expected: 양쪽 동일 count + PASS.

- [ ] **Step 3: cold-start fresh fork smoke 1회**

```bash
rm -rf node_modules/.vite-temp dist && npm run test:smoke 2>&1 | tail -10
```

Expected: 102/102 PASS (기존 101 + 신규 1).

- [ ] **Step 4: production build + bundle dual record**

```bash
npm run build
echo "--- index canonical ---"
ls dist/assets/index-*.js
gzip -c dist/assets/index-*.js | wc -c
echo "--- archive chunk ---"
ls dist/assets/archive-*.js
for f in dist/assets/archive-*.js; do
  echo "$f: $(gzip -c "$f" | wc -c) bytes"
done
```

Expected:
- index canonical: variation 거의 없음 (≤ +50 B over v3.36 baseline)
- archive chunk: +50~150 B (GREEN ≤ +200 B ceiling)

- [ ] **Step 5: Codex 최종 review CLI direct**

```bash
codex exec --skip-git-repo-check -o /tmp/v3.37-codex-final.txt "review v3.37 cycle commits (parent fe3282b → tip $(git rev-parse HEAD)). Verify: (1) resetArchiveFilters export + counter-click integration semantics correct; (2) plant-detail navigateToInterestArchive sequence (reset/switchTab/handleArchiveSearch/focus) correct; (3) smoke regression guard coverage; (4) bundle dual record within budget; (5) no schema/CSP/migration changes. Classify DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT with P0/P1/P2 line refs."
```

Expected: `/tmp/v3.37-codex-final.txt` 생성, exit code 0.

- [ ] **Step 6: review 결과 분류 + 처리**

`/tmp/v3.37-codex-final.txt` 내용 분류:

- `DEPLOY_APPROVED` → T5 진입
- `APPROVED_WITH_NOTES` (P0=0) → P2 항목 in-cycle 흡수 결정 또는 v3.38 carry → T5 진입
- `REJECT` (P0 존재) → in-cycle hotfix → T4 Step 1부터 재실행

Fallback: CLI 한도/권한 issue 시 사용자에게 즉시 알림 + `pr-review-toolkit:code-reviewer` controller fallback 진입 (spec §6.3).

---

## Task 5 — retro + memory + vault sync

**Files:**
- Modify: `docs/superpowers/specs/2026-05-17-v3.37-archive-state-reset-design.md` (§6 retro 채움)
- Create: `~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v337.md`
- Modify: `~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md`

- [ ] **Step 1: spec §6 retro 채움**

`docs/superpowers/specs/2026-05-17-v3.37-archive-state-reset-design.md`에 다음 섹션 추가:

```markdown
## §6. Retro (T5 작성)

### 6.1 Cycle 결과

- Commits: T0(spec+plan v1) / T0 fix(plan v2 if any) / T1 / T2 / T3 / T5 retro
- vitest 총합 (양쪽 TZ): {count}
- smoke 총합 (cold-start fresh fork): 102/102
- TS strict 0 / lint 0
- Bundle: index canonical {bytes} / archive chunk {bytes} (Δ from v3.36)
- Codex 사전 review: {결과 + P0/P1/P2 분류}
- Codex 최종 review: {DEPLOY_APPROVED / APPROVED_WITH_NOTES}

### 6.2 Carry-forward (v3.38+)

| # | 항목 | 출처 | 우선순위 |
|---|---|---|---|
| C1 | 옵션 2 — 3 chip 묶음 navigation | v3.36 brainstorming deferred | P3 |
| C2 | 옵션 3 — Answer/Scrap interestId schema | v3.36 brainstorming deferred | P3 |
| C3 | briefings normalize | v3.35 carry | P2 |
| C4 | fuzzy 확장 — Levenshtein | v3.32 carry | P3 |
| C5 | stats 카드 → archive entry point | 본 사이클 §4.2 deferred | P3 |

### 6.3 Lessons (있다면)

[T1~T4 진행 중 발견한 lesson — codex pre/final review에서 catch한 invariant gap 등]
```

- [ ] **Step 2: memory file 작성**

`~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v337.md`:

```markdown
---
name: project_daily_growth_v337
description: DG v3.37 archive state reset + focus carry — v3.36 P2-1+P2-2 small cycle 청산, resetArchiveFilters() 단일 진입점
metadata:
  type: project
---

# DG v3.37 Archive State Reset + Focus Carry

**Status**: [✅ 배포 완료 또는 사이클 완료, 배포 대기] (YYYY-MM-DD, main HEAD `{sha}`)
**Cycle range**: parent fe3282b (v3.36) → tip {sha}

## 핵심 변경

- `src/ui/handlers/archive.ts`에 `resetArchiveFilters()` export 신설 — module state 5종 + DOM chip ARIA + question type row + select mode UI 일괄 default 복귀
- `.other-pin-counter` click handler 내 inline 5줄 reset → `resetArchiveFilters()` 호출로 통합 (DRY)
- `src/ui/modals/plant-detail.ts`의 `navigateToInterestArchive` 시퀀스: closeModal → (dynamic import) resetArchiveFilters → switchTab → input.value=keyword → handleArchiveSearch → input.focus({ preventScroll: true })
- smoke `v3.36-plant-action-end-to-end.spec.ts` 확장 1 case (사전 stale filter setup → chip → reset/focus 회귀 가드)

## 검증

- vitest: {count} ({신규 +N}), 양쪽 TZ 통과
- smoke: 102/102 cold-start fresh fork
- Bundle: index canonical {bytes} / archive chunk {bytes}
- Codex 2-pass: 사전 {결과} / 최종 {결과}

## Carry-forward (v3.38+)

[6.2 표 동일 항목]

## Lessons

[Step 1 §6.3 동일]
```

- [ ] **Step 3: MEMORY.md 인덱스 한 줄 추가**

`~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md`에 다음 한 줄 추가 (v3.36 항목 다음 위치):

```markdown
- [DG v3.37 archive state reset + focus carry](project_daily_growth_v337.md) — **✅ ...** — resetArchiveFilters() 단일 진입점 + plant-detail focus 부여 + v3.36 P2-1/P2-2 청산.
```

> MEMORY.md가 이미 30.6KB라 line이 200+이면 truncate됨. v3.18~v3.20 archive 같은 오래된 항목을 `MEMORY-archive.md`로 이전 검토 (T5에서 결정 — 본 사이클 의무 X).

- [ ] **Step 4: vault sync (CLAUDE.md 의무)**

```bash
VAULT="/Users/hayden/Hayden/20_Projects/Dev_Project/SJ_Daily Growth(Ai assistant)"

# 02-Cycles: 신규 spec retro
cp docs/superpowers/specs/2026-05-17-v3.37-archive-state-reset-design.md "$VAULT/02-Cycles/"

# 03-Lessons: 신규 memory file + MEMORY.md 갱신본
cp ~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v337.md "$VAULT/03-Lessons/"
cp ~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md "$VAULT/03-Lessons/"
```

> 00-Index.md 사이클 타임라인 표에 v3.37 한 줄 추가는 manual 또는 별도 hook 미설치 상태이므로 controller가 직접 수정.

- [ ] **Step 5: T5 commit**

```bash
git add docs/superpowers/specs/2026-05-17-v3.37-archive-state-reset-design.md
git commit -m "docs(v3.37 T5 retro): 사이클 완료 — resetArchiveFilters() + plant-detail focus, v3.36 P2-1+P2-2 청산"
```

Expected: 새 commit, files: 1 (memory file과 vault는 git-tracked X).

- [ ] **Step 6: 배포 (사용자 "배포해줘" 명시 시점)**

사용자 승인 받은 후:

```bash
npm run build && firebase deploy --only hosting
```

Expected: Firebase Hosting deploy 성공, live URL `https://my-ai-assistant-904f3.web.app` 갱신.

배포 후 사용자에게 production 검증 요청 (정원 → 식물 카드 → action chip → archive 화면 entity/filter all + 검색창 focus 확인).

---

## Self-Review

본 plan에 대한 self-review:

### 1. Spec coverage

| spec 요구사항 | plan task | 검증 |
|---|---|---|
| `resetArchiveFilters()` 신설 + module state 5종 reset | T1 Step 3 | ✅ 명시 |
| DOM chip ARIA + question type row + select mode UI 동기화 | T1 Step 3 + Step 5 spec | ✅ 명시 |
| counter-click inline 5줄 흡수 | T1 Step 7 | ✅ 명시 |
| plant-detail 시퀀스: closeModal → reset → switchTab → input.value → handleArchiveSearch → focus | T2 Step 3 | ✅ 명시 |
| `input.focus({ preventScroll: true })` | T2 Step 3 + Step 4 spec | ✅ 명시 |
| smoke 사전 entity/filter setup + 회귀 가드 | T3 Step 1 | ✅ 명시 |
| Codex 2-pass (CLI direct) | T0 Step 2 + T4 Step 5 | ✅ 명시 |
| Bundle dual record (index + archive chunk) | T4 Step 4 | ✅ 명시 |
| 양쪽 TZ vitest | T4 Step 2 | ✅ 명시 |
| cold-start fresh fork smoke | T4 Step 3 | ✅ 명시 |
| schema/CSP/migration 0줄 | 본 plan에서 schema/migration 파일 미포함 | ✅ implicit |
| retro + memory + vault sync | T5 Step 1~4 | ✅ 명시 |

Spec §4.3 Open Questions의 사전 결정 사항이 plan에 반영됨:
- `rerenderList` 호출 안 함 → T1 Step 3 주석 명시 ✅
- selectedIds/selectMode 포함 → T1 Step 3 코드 ✅
- preventScroll: true → T2 Step 3 코드 ✅
- 기존 v3.36 smoke spec 확장 → T3 Step 1 ✅
- UI chip 클릭으로 사전 setup → T3 Step 1 코드 ✅

### 2. Placeholder scan

- T3 Step 1 spec 안 `// ... (기존 spec과 동일한 사전 시드 단계)` — implementer가 기존 spec을 grep해 동일 fixture 패턴을 적용해야 함. **`memo` chip data-filter 값**도 grep으로 확인 의무. 본 plan에서 직접 코드 명시는 어렵지만(기존 spec helper 파일 의존), implementer 행동 지시는 명확.
- T5 Step 1/2 안 `{count}` / `{bytes}` / `{sha}` — T5 실행 시점에 실측 값으로 채움. retro 표준 패턴.

다른 TBD/TODO 없음.

### 3. Type / 시그니처 일관성

- `resetArchiveFilters(): void` — T1/T2/spec 모두 동일 ✅
- `navigateToInterestArchive(interestId: string): Promise<void>` — T2 Step 3 export 시그니처 ✅
- spec Step 4의 mock target path (`../../../src/ui/handlers/archive`)와 T2 Step 3 plant-detail 안 dynamic import path (`../handlers/archive`) — vi.doMock은 절대 path 또는 spec 파일 기준 상대 path 둘 다 OK. spec 동일 일관 ✅
- `mountArchiveHandlers` 명 — T1 Step 8 "Note"에 grep 의무 명시 (실제 export 명 다를 가능성 인지)

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-05-17-v3.37-archive-state-reset-plan.md`.**

권장 실행 방식 (small cycle 특성 고려):

- **현재 세션**: T0 Step 1~4 (plan commit + Codex 사전 review + P0/P1 흡수 + plan v2)
- **다음 세션**: T1~T5 (구현 + 검증 + Codex 최종 review + retro + 배포)

다음 세션 진입 명령: `"v3.37 T1 시작"` 또는 `subagent-driven-development` skill로 자동 진입.
