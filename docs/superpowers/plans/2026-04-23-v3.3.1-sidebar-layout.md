# v3.3.1 Sidebar Drawer + Layout Fix + Settings Max-width Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 데스크탑 sidebar가 본문을 가리는 이슈 해결 + 햄버거 drawer UX 도입 + 설정 탭 max-width 적용. 모바일 bottom-nav 경험은 보존.

**Architecture:** 기존 `@layer layout` 내 `.bottom-nav` 데스크탑 분기를 제거하고, 데스크탑에서는 `.bottom-nav`도 hidden. 햄버거 버튼을 좌상단 fixed로 배치하고, 클릭 시 `<aside class="sidebar-drawer">`와 backdrop을 `data-open="true"`로 토글하는 overlay drawer 패턴. Drawer 내부에도 `.nav-item[data-tab-id]`를 재사용해 기존 `switchTab()` 동작을 그대로 승계. `focus-trap` 유틸로 접근성 보장. 설정은 `.settings-section`에 `max-width: 560px; margin: 0 auto` 적용.

**Tech Stack:** Vite + Vanilla TypeScript, CSS Cascade Layers, vitest (jsdom), Playwright.

---

## 0. Existing Landscape (현 코드 실측 — feedback_plan_discovery 준수)

Spec §2.1 / §7.1은 v3.2b-ui 시점 CSS 위치를 가정하지만, v3.3.0 refactor 이후 실제 상태는 아래와 같다. 모든 Task는 이 실측을 기준으로 한다.

### 0.1 HTML 셸 (index.html:16-20)

```html
<body>
  <main id="app"></main>
  <nav class="bottom-nav" id="bottomNav"></nav>
  <div id="modalRoot"></div>
  <script type="module" src="/src/main.ts"></script>
</body>
```

- **`.app-container` 클래스는 DOM 어디에도 부여되지 않음.** [src/styles/layout.css:9-14](src/styles/layout.css#L9) 와 [src/styles/layout.css:87-94](src/styles/layout.css#L87) 의 `.app-container` 룰은 **현재 죽은 CSS**. v3.3.1에서 제거한다.
- `#app`은 탭 렌더 컨테이너 ([src/ui/nav.ts:44-46](src/ui/nav.ts#L44)). Tab 전환 시 `replaceChildren()` + `tab.render(app)`.
- `#bottomNav`는 `<main>` 바깥에 존재 → fixed 포지셔닝만으로 본문과 독립 레이어.

### 0.2 현재 데스크탑 레이아웃 메커니즘 (≥768px)

[src/styles/layout.css:53-123](src/styles/layout.css#L53):
- `.bottom-nav { position: fixed; bottom: 0; left: 50%; transform: translateX(-50%); width: 100%; max-width: 480px; }` — **모바일 기본**: 하단 중앙 고정.
- `@media (min-width: 768px)` 분기에서:
  - `.bottom-nav { top: 0; bottom: 0; left: 0; transform: none; width: var(--sidebar-width); flex-direction: column; ... }` — **데스크탑**: 좌측 전체 세로 사이드바 (240px).
- `#homeTab, #archiveTab, ...` 에 `padding-bottom: var(--content-bottom-pad)`. 768+ 에서 `--content-bottom-pad: 24px` ([src/styles/tokens.css:121-123](src/styles/tokens.css#L121)).
- **본문 가림 메커니즘 추정**: `#app` (폭 100%) → 내부 탭 컨텐츠가 viewport 전체를 사용 → 좌측 240px는 `.bottom-nav`가 overlap. `.app-container` padding-left는 **적용되지 않음** (클래스 없음).

### 0.3 briefing-scroll 이관 상태

- [src/styles/components/cards.css:29-36](src/styles/components/cards.css#L29) — `.briefing-scroll { display: flex; overflow-x: auto; padding: 0 20px; scroll-snap-type: x mandatory; }`
- [src/styles/pages/home.css:142](src/styles/pages/home.css#L142) — `#homeTab > .briefing-scroll` tab-scoped 오버라이드 (있으면 읽기)

### 0.4 설정 탭 구조 ([src/ui/tabs/settings.ts:7-47](src/ui/tabs/settings.ts#L7))

```
.settings-section#settingsTab
├── h2 "⚙️ 설정"
├── section.settings-group (Gemini API 키)
│   ├── input#apiKeyInput (inline style: width: 100%)
│   ├── button#saveApiKeyBtn.btn.btn-primary.btn-block
│   └── div#apiKeyStatus
└── section.settings-group (Slack Webhook)
    ├── input#slackWebhookInput (inline style: width: 100%)
    ├── div (flex: 테스트 / 저장)
    ├── div#slackAutoRow (auto send toggle row)
    ├── button#clearSlackBtn (inline: width: 100%)
    ├── div#slackTestResult
    └── details (webhook 생성 가이드)
```

- **inline `style=` 속성 다수.** max-width는 부모 `.settings-section`에 걸면 자식 width: 100%는 자동으로 560px 제한됨 (CSS 계산상 OK).
- 폼 handler wiring은 `bindHandlers` / `bindSlackHandlers` 모두 `container.querySelector` 기반 → DOM shape 변경 없이 부모 max-width만 조정하면 wiring 보존.

### 0.5 탭 active state 동기화 메커니즘 ([src/ui/nav.ts:41-57](src/ui/nav.ts#L41))

```ts
export function switchTab(id: TabId): void {
  // ...
  const buttons = document.querySelectorAll<HTMLButtonElement>('#bottomNav .nav-item');
  buttons.forEach((b) => b.classList.toggle('active', b.dataset['tabId'] === id));
  document.dispatchEvent(new CustomEvent('dg:nav:tab-changed', { detail: { tab: id } }));
}
```

- **selector가 `#bottomNav .nav-item`로 scope**. Drawer 추가 시 두 가지 선택:
  - (a) selector를 `.nav-item`로 넓히기 (간단, 단일 책임 유지)
  - (b) `dg:nav:tab-changed` 이벤트 구독으로 drawer 쪽에서 active 토글
- Plan 채택: **(a)** — `document.querySelectorAll('.nav-item')`로 변경. `.nav-item`은 v3.3.0 이후 bottom-nav 전용이라 충돌 없음 확인 ([src/styles/layout.css:63](src/styles/layout.css#L63)).
- DOM attribute 명: `dataset.tabId` → HTML attribute `data-tab-id`. **Spec §6.6의 `data-tab`은 오류.** Plan에서는 `data-tab-id` 사용.

### 0.6 다크 모드 토글 상태

- [src/styles/tokens.css:36-54](src/styles/tokens.css#L36) + [src/styles/tokens.css:110-116](src/styles/tokens.css#L110) 에 `[data-theme="dark"]` 팔레트는 선언되어 있으나, **이를 토글하는 JS 코드는 repo 전체에 0개** (`grep "data-theme" src/**/*.ts` = 0 hits).
- v3.3.1 범위: **다크 모드 토글 구현 안 함.** 기존 없음 구조 존중. v3.3.2 또는 별도 phase로 연기. 단, 본 Plan에서 추가하는 CSS는 `[data-theme="dark"]` 오버라이드 호환 구조로 작성.

### 0.7 데드코드 CSS 정리 대상

- `.app-container` 룰 ([src/styles/layout.css:9-14](src/styles/layout.css#L9), [:87-94](src/styles/layout.css#L87)) — DOM 부여 0곳. **삭제**.

### 0.8 v3.3.0 Open Item P1-A 범위

- [project_daily_growth_v33_0.md 참조](../../.claude/completions/2026-04-22-v3.3.0-design-system.md) — `--text-*` 네이밍 충돌 (color `--text-primary/secondary/tertiary` vs font-size `--text-xs/sm/base/lg/xl/2xl/3xl`).
- 현 시점 `--text-xs` 등 font-size 토큰은 **선언만 존재, consumer 0.** rename은 tokens.css + 참조 시 자동 갱신(해당 없음).
- **Plan 기본 권장: 포함.** Task 11 optional 으로 배치, skip 시 메모만 남기고 v3.3.2로 이연.

### 0.9 테스트 자산 위치

- vitest 단위: `tests/unit/**/*.spec.ts` (jsdom 환경)
- Playwright smoke: `tests/smoke/**/*.spec.ts`
- Playwright lint: `tests/lint/**/*.spec.ts` (CSP 등 정책 검증)
- 기존 layout 관련: [tests/smoke/layout.spec.ts](tests/smoke/layout.spec.ts), [tests/smoke/polish.spec.ts](tests/smoke/polish.spec.ts) — 회귀 베이스라인.

---

## File Structure (신규/수정 맵)

### 신규 파일

- `src/ui/utils/focus-trap.ts` — Tab/Shift+Tab wrap 유틸 (~60줄)
- `src/ui/sidebar.ts` — Drawer 마운트 + open/close + localStorage + focus-trap + ESC 핸들러 (~150줄)
- `tests/unit/focus-trap.spec.ts` — 유틸 단위 테스트
- `tests/unit/sidebar.spec.ts` — sidebar 모듈 단위 테스트 (jsdom)
- `tests/smoke/sidebar-drawer.spec.ts` — Playwright drawer 동작
- `tests/smoke/desktop-layout.spec.ts` — viewport 1440/1920 briefing x 위치

### 수정 파일

- `src/main.ts` — `mountSidebar()` 호출 추가
- `src/ui/nav.ts` — active toggle selector 확장 (`#bottomNav .nav-item` → `.nav-item`)
- `src/styles/layout.css` — `.app-container` 데드코드 제거 + `.bottom-nav` 데스크탑 분기 제거 + sidebar drawer CSS 추가
- `src/styles/components/forms.css` — `.settings-section` max-width 추가
- `src/styles/tokens.css` — (optional Task 11) `--text-xs/sm/base/...` → `--font-size-*` rename
- `index.html` — drawer/backdrop/토글 버튼 DOM 추가

### 영향 없음 (읽기 전용)

- `src/ui/tabs/settings.ts` — max-width는 부모 CSS로만 처리
- `src/state/*` — 상태 스키마 변경 없음 (localStorage `dg-sidebar-last-state` key는 값만 추가)

---

## Task Decomposition

### Task 1: Baseline regression reproduction

실제 버그 메커니즘 확인. Spec §2.1 가설(`.app-container padding-left`)은 이미 죽은 CSS로 판명 → 새 가설은 "`.bottom-nav` 데스크탑 fixed left가 `#app` 컨텐츠와 overlap". 이를 재현하는 failing smoke 테스트로 진짜 버그 shape을 고정한다.

**Files:**
- Create: `tests/smoke/desktop-layout.spec.ts`

- [ ] **Step 1: Write the failing test**

```ts
// tests/smoke/desktop-layout.spec.ts
import { test, expect } from '@playwright/test';

test.describe('Desktop layout (≥768px) — briefing card not occluded by sidebar', () => {
  for (const viewport of [
    { width: 1440, height: 900 },
    { width: 1920, height: 1080 },
  ]) {
    test(`viewport ${viewport.width}x${viewport.height} — first briefing card x >= 240`, async ({ page }) => {
      await page.setViewportSize(viewport);
      await page.goto('/');
      // seed onboarding to skip first-run
      await page.evaluate(() => {
        localStorage.setItem('user', JSON.stringify({ interests: ['growth'] }));
      });
      await page.reload();
      const firstCard = page.locator('.briefing-scroll .briefing-card').first();
      await firstCard.waitFor({ state: 'visible', timeout: 5000 });
      const navBox = await page.locator('#bottomNav').boundingBox();
      const cardBox = await firstCard.boundingBox();
      expect(cardBox).not.toBeNull();
      // bottom-nav가 데스크탑에서 좌측 컬럼(240px)으로 표시되면 overlap 발생.
      // 버그 상태: nav fixed left column, card x=0부터 시작 → nav 뒤에 가림.
      // 수정 후: nav는 display:none, drawer는 translateX(-100%) → card 가림 없음.
      if (navBox && navBox.width > 0 && navBox.width < viewport.width) {
        expect(cardBox!.x).toBeGreaterThanOrEqual(navBox.x + navBox.width - 1);
      }
    });
  }
});
```

- [ ] **Step 2: Run test to verify it fails (reproduces occlusion)**

Run: `npx playwright test tests/smoke/desktop-layout.spec.ts --reporter=list`
Expected: FAIL — `cardBox.x < navBox.x + navBox.width` because `.bottom-nav` (v3.3.0 기준 ≥768px에서 fixed left column 240px)이 briefing 영역과 overlap.

> 만약 PASS면 버그 가설이 틀림 → 즉시 중단하고 Hayden과 재브리프. "할루시네이션 가능성" 경고.

- [ ] **Step 3: Commit failing test as regression guard**

```bash
git checkout -b feat/v3.3.1-sidebar-layout
git add tests/smoke/desktop-layout.spec.ts
git commit -m "test(smoke): add desktop layout regression guard (currently failing)"
```

---

### Task 2: Remove dead `.app-container` CSS

Spec §2.1이 범인으로 지목한 `.app-container` 룰 2개 모두 DOM 클래스 없음. 제거는 시각 변화 0 (확인 후 커밋).

**Files:**
- Modify: `src/styles/layout.css:9-14, 87-94`

- [ ] **Step 1: Write failing unit test (ensures rule absence)**

```ts
// tests/unit/css-tokens.spec.ts 에 append
import { readFileSync } from 'node:fs';
import { resolve } from 'node:path';
import { describe, it, expect } from 'vitest';

describe('layout.css dead code removal (v3.3.1)', () => {
  const css = readFileSync(resolve('src/styles/layout.css'), 'utf-8');
  it('no .app-container declarations remain', () => {
    expect(css).not.toMatch(/\.app-container\s*\{/);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- tests/unit/css-tokens.spec.ts`
Expected: FAIL with "expected ... not to match /\\.app-container\\s*\\{/"

- [ ] **Step 3: Remove dead rules**

Edit `src/styles/layout.css`:
- Delete lines 6-8 ("App Shell" 섹션 헤더 주석 블록 — `.app-container` 없어지면 orphan)
- Delete lines 9-14 (`.app-container { max-width: 480px; margin: 0 auto; min-height: 100dvh; position: relative; }`)
- Delete line 88 (`.app-container { border-left: ...; border-right: ...; }` inside `@media (min-width: 481px)`)
- Delete lines 91-94 (`.app-container { max-width: 1200px; padding-left: var(--sidebar-width); }` inside `@media (min-width: 768px)`)

After edit, the 481+ block becomes empty → delete whole `@media (min-width: 481px) { }` block too.

- [ ] **Step 4: Run test to verify it passes**

Run: `npm test -- tests/unit/css-tokens.spec.ts` → PASS.
Run: `npm test` (전체) → 기존 146/146 PASS 유지 확인.

- [ ] **Step 5: Commit**

```bash
git add src/styles/layout.css tests/unit/css-tokens.spec.ts
git commit -m "refactor(css): remove dead .app-container rules

DOM never had .app-container class. Removing unused rules
before v3.3.1 sidebar work to avoid confusion."
```

---

### Task 3: Remove `.bottom-nav` desktop branch (fix layout bug)

모바일 bottom-nav는 유지, 768+ 에서는 `display: none`로 숨기고 sidebar drawer로 대체할 준비.

**Files:**
- Modify: `src/styles/layout.css` (남은 `@media (min-width: 768px)` 블록)

- [ ] **Step 1: Write failing Playwright test**

```ts
// tests/smoke/desktop-layout.spec.ts 에 append
test('viewport 1440×900 — bottom-nav hidden on desktop', async ({ page }) => {
  await page.setViewportSize({ width: 1440, height: 900 });
  await page.goto('/');
  await page.evaluate(() => {
    localStorage.setItem('user', JSON.stringify({ interests: ['growth'] }));
  });
  await page.reload();
  const nav = page.locator('#bottomNav');
  await expect(nav).toBeHidden();
});

test('viewport 375×667 — bottom-nav visible on mobile', async ({ page }) => {
  await page.setViewportSize({ width: 375, height: 667 });
  await page.goto('/');
  await page.evaluate(() => {
    localStorage.setItem('user', JSON.stringify({ interests: ['growth'] }));
  });
  await page.reload();
  const nav = page.locator('#bottomNav');
  await expect(nav).toBeVisible();
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx playwright test tests/smoke/desktop-layout.spec.ts --reporter=list`
Expected: desktop hidden 테스트 FAIL (nav still visible).

- [ ] **Step 3a: Replace desktop branch with `display: none`**

Edit `src/styles/layout.css`, 기존 `@media (min-width: 768px) { .bottom-nav { ... } .nav-item { ... } }` 블록 전체를 다음으로 대체:

```css
@media (min-width: 768px) {
  .bottom-nav { display: none; }
}
```

- [ ] **Step 3b: Update orphan `.app-container` comment (Task 2 review I1)**

기존 `src/styles/layout.css`의 "Tab Containers — bottom safe-area padding" 블록 내부 주석 (Task 2 이후 현재 파일 기준 대략 line 63-67):

```css
/* ============================================
   Tab Containers — bottom safe-area padding
   v3.2b-ui: tabs carry the bottom safe-area pad so .app-container
   doesn't double it.
   ============================================ */
```

내용에서 `.app-container`가 더 이상 존재하지 않음. 주석을 다음과 같이 갱신:

```css
/* ============================================
   Tab Containers — bottom safe-area padding
   Tabs carry the bottom safe-area pad so the app shell doesn't double it.
   (historical: .app-container was removed in v3.3.1)
   ============================================ */
```

- [ ] **Step 3c: Relax `--sidebar-width` drift guard (Task 2 review M3)**

`tests/unit/css-tokens.spec.ts`의 기존 드리프트 가드:

```ts
it('--sidebar-width used at least twice (max-width + padding-left + width)', () => {
  const matches = layoutCss.match(/var\(--sidebar-width\)/g) ?? [];
  expect(matches.length).toBeGreaterThanOrEqual(2);
});
```

Task 3 Step 3a에서 `.bottom-nav` 데스크탑 분기 제거 → `layoutCss`의 `var(--sidebar-width)` 사용 0회. 토큰 자체는 v3.3.1 drawer(Task 5) 및 tokens.css에 여전히 선언/사용 예정. 가드를 **최소 1회**로 완화하고 주석 갱신:

```ts
it('--sidebar-width consumed at least once (drawer width in v3.3.1+)', () => {
  const matches = layoutCss.match(/var\(--sidebar-width\)/g) ?? [];
  expect(matches.length).toBeGreaterThanOrEqual(1);
});
```

> Task 3 commit 직후 시점 일시적으로 `matches.length === 0` 상태가 됨. 이 Task 내에서 가드는 완화되지만 still 0이면 FAIL. 허용하거나, Task 3 commit을 Task 5 drawer CSS와 묶기. **선택**: 이 가드 테스트를 Task 3에서 **일시적으로 skip** (`it.skip(...)`) 하고 Task 5 drawer 추가 후 원복 — 가장 honest. 또는 Task 3에서 가드 제거, Task 5에서 drawer-specific 새 가드 추가.

**Plan 채택**: **임시 skip** 방식. Step 3c에서 `it` → `it.skip`, 주석 `// Re-enabled in Task 5 after sidebar drawer CSS lands`. Task 5 Step에서 다시 `it`으로 복원 + 새 assertion 포함.

- [ ] **Step 4: Run tests to verify mobile/desktop both correct**

Run: `npx playwright test tests/smoke/desktop-layout.spec.ts --reporter=list`
Expected: desktop hidden PASS, mobile visible PASS. briefing x 테스트는 Task 1 기준 여전히 FAIL (drawer 없음; Task 1 테스트는 Task 6 끝에 PASS 예상이라 scope 벗어남 — 해당 테스트는 `test.fixme` 또는 Task 6 완료까지 기대치 유지).

> **Wiring check**: `.bottom-nav` 모바일 스타일(`.bottom-nav { position: fixed; bottom: 0; ... }`, lines 53-71)은 미수정 — 모바일 UX 보존 확인.

- [ ] **Step 5: Commit**

```bash
git add src/styles/layout.css tests/smoke/desktop-layout.spec.ts
git commit -m "fix(layout): hide bottom-nav on desktop (≥768px)

Replaces old sidebar-mode branch with display:none.
Desktop sidebar will be provided by new drawer (next commit).
Mobile bottom-nav behavior unchanged."
```

---

### Task 4: focus-trap utility

Drawer open 시 Tab 키가 drawer 내부에서만 순환하도록 보장. 의존성 0, 순수 TS.

**Files:**
- Create: `src/ui/utils/focus-trap.ts`
- Create: `tests/unit/focus-trap.spec.ts`

- [ ] **Step 1: Write failing unit tests**

```ts
// tests/unit/focus-trap.spec.ts
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { createFocusTrap } from '../../src/ui/utils/focus-trap';

function buildContainer(): HTMLElement {
  const div = document.createElement('div');
  div.innerHTML = `
    <button id="a">A</button>
    <a href="#" id="b">B</a>
    <input id="c" />
  `;
  document.body.append(div);
  return div;
}

describe('createFocusTrap', () => {
  let container: HTMLElement;
  beforeEach(() => { container = buildContainer(); });
  afterEach(() => { container.remove(); });

  it('activate() focuses first focusable element', () => {
    const trap = createFocusTrap(container);
    trap.activate();
    expect(document.activeElement?.id).toBe('a');
    trap.deactivate();
  });

  it('Tab on last element wraps to first', () => {
    const trap = createFocusTrap(container);
    trap.activate();
    const last = container.querySelector<HTMLInputElement>('#c')!;
    last.focus();
    const ev = new KeyboardEvent('keydown', { key: 'Tab', bubbles: true, cancelable: true });
    last.dispatchEvent(ev);
    expect(document.activeElement?.id).toBe('a');
    trap.deactivate();
  });

  it('Shift+Tab on first wraps to last', () => {
    const trap = createFocusTrap(container);
    trap.activate();
    const first = container.querySelector<HTMLButtonElement>('#a')!;
    first.focus();
    const ev = new KeyboardEvent('keydown', { key: 'Tab', shiftKey: true, bubbles: true, cancelable: true });
    first.dispatchEvent(ev);
    expect(document.activeElement?.id).toBe('c');
    trap.deactivate();
  });

  it('deactivate() removes keydown listener', () => {
    const trap = createFocusTrap(container);
    trap.activate();
    trap.deactivate();
    const last = container.querySelector<HTMLInputElement>('#c')!;
    last.focus();
    const ev = new KeyboardEvent('keydown', { key: 'Tab', bubbles: true, cancelable: true });
    last.dispatchEvent(ev);
    // no wrap after deactivate — focus stays
    expect(document.activeElement?.id).toBe('c');
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npm test -- tests/unit/focus-trap.spec.ts`
Expected: FAIL — module not found.

- [ ] **Step 3: Implement focus-trap**

```ts
// src/ui/utils/focus-trap.ts
const FOCUSABLE = [
  'a[href]',
  'button:not([disabled])',
  'input:not([disabled])',
  'select:not([disabled])',
  'textarea:not([disabled])',
  '[tabindex]:not([tabindex="-1"])',
].join(',');

export interface FocusTrap {
  activate(): void;
  deactivate(): void;
}

export function createFocusTrap(container: HTMLElement): FocusTrap {
  let handler: ((e: KeyboardEvent) => void) | null = null;

  function getFocusable(): HTMLElement[] {
    return Array.from(container.querySelectorAll<HTMLElement>(FOCUSABLE));
  }

  return {
    activate() {
      const items = getFocusable();
      if (items.length > 0) items[0].focus();
      handler = (e: KeyboardEvent) => {
        if (e.key !== 'Tab') return;
        const focusable = getFocusable();
        if (focusable.length === 0) return;
        const first = focusable[0];
        const last = focusable[focusable.length - 1];
        if (e.shiftKey && document.activeElement === first) {
          e.preventDefault();
          last.focus();
        } else if (!e.shiftKey && document.activeElement === last) {
          e.preventDefault();
          first.focus();
        }
      };
      container.addEventListener('keydown', handler);
    },
    deactivate() {
      if (handler) {
        container.removeEventListener('keydown', handler);
        handler = null;
      }
    },
  };
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `npm test -- tests/unit/focus-trap.spec.ts` → PASS (4 tests).

- [ ] **Step 5: Commit**

```bash
git add src/ui/utils/focus-trap.ts tests/unit/focus-trap.spec.ts
git commit -m "feat(ui): add focus-trap utility for modal/drawer a11y"
```

---

### Task 5: Sidebar drawer CSS

`@layer layout`에 drawer / toggle button / backdrop 스타일 추가. 모바일에서는 `.sidebar-toggle { display: none; }` 로 보이지 않음.

**Files:**
- Modify: `src/styles/layout.css`

- [ ] **Step 1: Write failing unit test (CSS content-level)**

```ts
// tests/unit/css-tokens.spec.ts 에 append
it('sidebar drawer styles exist in layout.css', () => {
  const css = readFileSync(resolve('src/styles/layout.css'), 'utf-8');
  expect(css).toMatch(/\.sidebar-drawer\s*\{/);
  expect(css).toMatch(/\.sidebar-backdrop\s*\{/);
  expect(css).toMatch(/\.sidebar-toggle\s*\{/);
  expect(css).toMatch(/transform:\s*translateX\(-100%\)/);
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `npm test -- tests/unit/css-tokens.spec.ts` → FAIL.

- [ ] **Step 3: Add CSS to layout.css end**

Append to `src/styles/layout.css`:

```css
/* ============================================
   Sidebar Drawer (v3.3.1)
   Mobile: hidden. Desktop (≥768px): overlay drawer.
   ============================================ */
.sidebar-toggle {
  display: none; /* mobile hidden */
  position: fixed;
  top: var(--space-3);
  left: var(--space-3);
  width: 40px;
  height: 40px;
  border-radius: var(--radius-sm);
  background: var(--bg-card);
  box-shadow: var(--shadow-sm);
  border: var(--border-thin) solid var(--border);
  cursor: pointer;
  z-index: var(--z-sidebar-button);
  align-items: center;
  justify-content: center;
  font-size: 1.1rem;
}
.sidebar-toggle:hover { background: var(--primary-ultra-light); }
.sidebar-toggle:focus-visible {
  outline: none;
  box-shadow: var(--focus-ring), var(--shadow-sm);
}

.sidebar-backdrop {
  display: none; /* mobile hidden */
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.4);
  opacity: 0;
  pointer-events: none;
  transition: opacity var(--duration-base) var(--ease-standard);
  z-index: calc(var(--z-sidebar) - 1);
}
.sidebar-backdrop[data-open="true"] {
  opacity: 1;
  pointer-events: auto;
}

.sidebar-drawer {
  display: none; /* mobile hidden */
  position: fixed;
  top: 0;
  left: 0;
  width: var(--sidebar-width);
  height: 100dvh;
  background: var(--bg-card);
  box-shadow: var(--shadow-lg);
  border-right: 1px solid var(--border);
  transform: translateX(-100%);
  transition: transform var(--duration-base) var(--ease-standard);
  z-index: var(--z-sidebar);
  overflow-y: auto;
  padding: 24px 0;
}
.sidebar-drawer[data-open="true"] { transform: translateX(0); }

/* Drawer uses the same .nav-item primitives as bottom-nav */
.sidebar-drawer .nav-item {
  flex: 0 0 auto;
  flex-direction: row;
  gap: 12px;
  justify-content: flex-start;
  padding: 12px 20px 12px 16px;
  font-size: 0.85rem;
  border-left: 4px solid transparent;
  width: 100%;
  color: var(--text-tertiary);
  cursor: pointer;
  background: none;
  border-top: none;
  border-right: none;
  border-bottom: none;
  font-family: var(--font-body);
}
.sidebar-drawer .nav-item.active {
  border-left-color: var(--primary);
  background: var(--primary-ultra-light);
  color: var(--primary);
}
.sidebar-drawer .nav-item:hover {
  background: var(--primary-ultra-light);
  color: var(--primary);
}
.sidebar-drawer .nav-icon { font-size: 1.15rem; }

@media (min-width: 768px) {
  .sidebar-toggle { display: inline-flex; }
  .sidebar-backdrop { display: block; }
  .sidebar-drawer { display: block; }
}
```

- [ ] **Step 4: Run tests**

Run: `npm test` (전체) → 146 + 기존 Task 2/4 신규 테스트 전부 PASS.

- [ ] **Step 5: Commit**

```bash
git add src/styles/layout.css tests/unit/css-tokens.spec.ts
git commit -m "feat(css): add sidebar drawer / backdrop / toggle styles"
```

---

### Task 6: Sidebar module (`src/ui/sidebar.ts`) — open/close + a11y + persistence

Drawer DOM을 body에 mount, 햄버거/ESC/backdrop 인터랙션 처리, localStorage로 상태 영속, focus-trap 적용.

**Files:**
- Create: `src/ui/sidebar.ts`
- Modify: `src/ui/nav.ts` (active toggle selector 확장)
- Modify: `src/main.ts` (mountSidebar 호출)
- Modify: `index.html` (DOM 슬롯 — 선택)
- Create: `tests/unit/sidebar.spec.ts`

- [ ] **Step 1: Write failing unit tests**

```ts
// tests/unit/sidebar.spec.ts
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';
import { mountSidebar } from '../../src/ui/sidebar';

const LS_KEY = 'dg-sidebar-last-state';

function setupDom(): void {
  document.body.innerHTML = '<main id="app"></main><nav class="bottom-nav" id="bottomNav"></nav>';
  localStorage.clear();
}

describe('mountSidebar', () => {
  beforeEach(setupDom);
  afterEach(() => { document.body.innerHTML = ''; localStorage.clear(); });

  it('injects toggle button, backdrop, drawer into document.body', () => {
    mountSidebar();
    expect(document.querySelector('#sidebarToggle')).not.toBeNull();
    expect(document.querySelector('#sidebarBackdrop')).not.toBeNull();
    expect(document.querySelector('#sidebarDrawer')).not.toBeNull();
  });

  it('drawer starts closed when no stored state', () => {
    mountSidebar();
    const drawer = document.querySelector<HTMLElement>('#sidebarDrawer')!;
    expect(drawer.dataset['open']).toBe('false');
    expect(drawer.getAttribute('aria-hidden')).toBe('true');
  });

  it('drawer restores open state from localStorage', () => {
    localStorage.setItem(LS_KEY, 'open');
    mountSidebar();
    const drawer = document.querySelector<HTMLElement>('#sidebarDrawer')!;
    expect(drawer.dataset['open']).toBe('true');
    expect(drawer.getAttribute('aria-hidden')).toBe('false');
  });

  it('clicking toggle opens drawer and persists state', () => {
    mountSidebar();
    const toggle = document.querySelector<HTMLButtonElement>('#sidebarToggle')!;
    toggle.click();
    const drawer = document.querySelector<HTMLElement>('#sidebarDrawer')!;
    expect(drawer.dataset['open']).toBe('true');
    expect(toggle.getAttribute('aria-expanded')).toBe('true');
    expect(localStorage.getItem(LS_KEY)).toBe('open');
  });

  it('ESC key closes open drawer', () => {
    localStorage.setItem(LS_KEY, 'open');
    mountSidebar();
    const drawer = document.querySelector<HTMLElement>('#sidebarDrawer')!;
    expect(drawer.dataset['open']).toBe('true');
    document.dispatchEvent(new KeyboardEvent('keydown', { key: 'Escape' }));
    expect(drawer.dataset['open']).toBe('false');
    expect(localStorage.getItem(LS_KEY)).toBe('closed');
  });

  it('backdrop click closes drawer', () => {
    localStorage.setItem(LS_KEY, 'open');
    mountSidebar();
    const backdrop = document.querySelector<HTMLElement>('#sidebarBackdrop')!;
    backdrop.click();
    const drawer = document.querySelector<HTMLElement>('#sidebarDrawer')!;
    expect(drawer.dataset['open']).toBe('false');
  });

  it('drawer renders one .nav-item per tab (5 total)', () => {
    mountSidebar();
    const items = document.querySelectorAll('#sidebarDrawer .nav-item');
    expect(items.length).toBe(5);
  });

  it('clicking a drawer nav-item closes drawer and dispatches dg:nav:tab-changed', () => {
    localStorage.setItem(LS_KEY, 'open');
    mountSidebar();
    const archiveBtn = document.querySelector<HTMLButtonElement>(
      '#sidebarDrawer .nav-item[data-tab-id="archive"]'
    )!;
    const spy = vi.fn();
    document.addEventListener('dg:nav:tab-changed', spy);
    archiveBtn.click();
    const drawer = document.querySelector<HTMLElement>('#sidebarDrawer')!;
    expect(drawer.dataset['open']).toBe('false');
    expect(spy).toHaveBeenCalled();
    document.removeEventListener('dg:nav:tab-changed', spy);
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npm test -- tests/unit/sidebar.spec.ts` → FAIL (module not found).

- [ ] **Step 3: Implement sidebar.ts**

```ts
// src/ui/sidebar.ts
import { createFocusTrap, type FocusTrap } from './utils/focus-trap';
import { switchTab, TABS } from './nav';

const LS_KEY = 'dg-sidebar-last-state';

export function mountSidebar(): void {
  if (document.getElementById('sidebarDrawer')) return; // idempotent

  const toggle = document.createElement('button');
  toggle.id = 'sidebarToggle';
  toggle.className = 'sidebar-toggle';
  toggle.type = 'button';
  toggle.setAttribute('aria-label', '메뉴 열기');
  toggle.setAttribute('aria-expanded', 'false');
  toggle.setAttribute('aria-controls', 'sidebarDrawer');
  toggle.textContent = '☰';

  const backdrop = document.createElement('div');
  backdrop.id = 'sidebarBackdrop';
  backdrop.className = 'sidebar-backdrop';
  backdrop.setAttribute('aria-hidden', 'true');

  const drawer = document.createElement('aside');
  drawer.id = 'sidebarDrawer';
  drawer.className = 'sidebar-drawer';
  drawer.setAttribute('role', 'navigation');
  drawer.setAttribute('aria-label', '주 내비게이션');
  drawer.setAttribute('aria-hidden', 'true');

  const nav = document.createElement('nav');
  for (const t of TABS) {
    const btn = document.createElement('button');
    btn.type = 'button';
    btn.className = 'nav-item';
    btn.dataset['tabId'] = t.id;
    const icon = document.createElement('span');
    icon.className = 'nav-icon';
    icon.textContent = t.icon;
    btn.append(icon);
    const label = document.createElement('span');
    label.textContent = t.label;
    btn.append(label);
    btn.addEventListener('click', () => {
      switchTab(t.id);
      close();
    });
    nav.append(btn);
  }
  drawer.append(nav);

  document.body.append(toggle, backdrop, drawer);

  const trap: FocusTrap = createFocusTrap(drawer);
  let lastFocused: HTMLElement | null = null;

  function open(): void {
    drawer.dataset['open'] = 'true';
    drawer.setAttribute('aria-hidden', 'false');
    backdrop.dataset['open'] = 'true';
    toggle.setAttribute('aria-expanded', 'true');
    toggle.setAttribute('aria-label', '메뉴 닫기');
    lastFocused = document.activeElement instanceof HTMLElement ? document.activeElement : null;
    trap.activate();
    try { localStorage.setItem(LS_KEY, 'open'); } catch { /* storage quota */ }
  }

  function close(): void {
    drawer.dataset['open'] = 'false';
    drawer.setAttribute('aria-hidden', 'true');
    backdrop.dataset['open'] = 'false';
    toggle.setAttribute('aria-expanded', 'false');
    toggle.setAttribute('aria-label', '메뉴 열기');
    trap.deactivate();
    if (lastFocused) { try { lastFocused.focus(); } catch { /* detached */ } }
    try { localStorage.setItem(LS_KEY, 'closed'); } catch { /* */ }
  }

  toggle.addEventListener('click', () => {
    if (drawer.dataset['open'] === 'true') close();
    else open();
  });
  backdrop.addEventListener('click', close);
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && drawer.dataset['open'] === 'true') close();
  });

  // initial state restore
  let stored: string | null = null;
  try { stored = localStorage.getItem(LS_KEY); } catch { /* */ }
  if (stored === 'open') open();
  else {
    drawer.dataset['open'] = 'false';
    backdrop.dataset['open'] = 'false';
  }
}
```

- [ ] **Step 4a: Verify `.nav-item` class is not used outside bottom-nav / drawer**

Run: `grep -rnE 'class="?nav-item"?|className\s*=\s*.nav-item.' src/`
Expected: only `src/ui/nav.ts` (bottom-nav mount) and the upcoming `src/ui/sidebar.ts` (drawer mount). If any other file references `.nav-item`, stop and re-brief — broadening the selector will cause collateral active-state toggling.

- [ ] **Step 4b: Modify `src/ui/nav.ts` — export TABS + broaden active selector**

Two edits to `src/ui/nav.ts`:

1. Change [src/ui/nav.ts:10](src/ui/nav.ts#L10) declaration from `const TABS:` to `export const TABS:` (sidebar.ts will reuse the same metadata — DRY).
2. Change [src/ui/nav.ts:48](src/ui/nav.ts#L48) selector from `#bottomNav .nav-item` to `.nav-item` so drawer items toggle in sync:

```ts
const buttons = document.querySelectorAll<HTMLButtonElement>('.nav-item');
```

- [ ] **Step 5: Update main.ts bootMainApp**

Edit [src/main.ts:19-25](src/main.ts#L19):

```ts
import { mountSidebar } from './ui/sidebar';
// ...
function bootMainApp(): void {
  mountHomeHandlers();
  mountArchiveHandlers();
  mountStatsHandlers();
  mountNav();
  mountSidebar();
  switchTab('home');
}
```

- [ ] **Step 6: Run tests**

Run: `npm test -- tests/unit/sidebar.spec.ts` → all 8 PASS.
Run: `npm test` → 146 + focus-trap(4) + sidebar(8) + css-tokens(+2) = 160 PASS.

- [ ] **Step 7: Commit**

```bash
git add src/ui/sidebar.ts src/ui/nav.ts src/main.ts tests/unit/sidebar.spec.ts
git commit -m "feat(ui): sidebar drawer with focus-trap + localStorage + tab sync

- New mountSidebar() injects toggle/backdrop/drawer DOM, wires
  open/close via click, ESC, backdrop
- switchTab() now updates .nav-item across bottom-nav and drawer
  via broadened selector
- Drawer persists open/closed state in localStorage
  (dg-sidebar-last-state)
- focus-trap utility keeps Tab inside drawer while open"
```

---

### Task 7: Playwright drawer smoke + layout regression PASS

Task 1의 `briefing x >= 240` 테스트가 이 단계에서 PASS해야 함 (drawer가 overlay이므로 본문 x=0부터 시작 OK).

**Files:**
- Create: `tests/smoke/sidebar-drawer.spec.ts`

- [ ] **Step 1: Write Playwright tests**

```ts
// tests/smoke/sidebar-drawer.spec.ts
import { test, expect } from '@playwright/test';

async function seedOnboarding(page: import('@playwright/test').Page): Promise<void> {
  await page.goto('/');
  await page.evaluate(() => {
    localStorage.setItem('user', JSON.stringify({ interests: ['growth'] }));
    localStorage.removeItem('dg-sidebar-last-state');
  });
  await page.reload();
}

test.describe('Sidebar drawer (desktop)', () => {
  test.beforeEach(async ({ page }) => {
    await page.setViewportSize({ width: 1440, height: 900 });
    await seedOnboarding(page);
  });

  test('starts closed, hamburger click opens', async ({ page }) => {
    const drawer = page.locator('#sidebarDrawer');
    const toggle = page.locator('#sidebarToggle');
    await expect(drawer).toHaveAttribute('data-open', 'false');
    await toggle.click();
    await expect(drawer).toHaveAttribute('data-open', 'true');
    await expect(toggle).toHaveAttribute('aria-expanded', 'true');
  });

  test('ESC closes drawer and refocuses toggle', async ({ page }) => {
    await page.locator('#sidebarToggle').click();
    await page.keyboard.press('Escape');
    const drawer = page.locator('#sidebarDrawer');
    await expect(drawer).toHaveAttribute('data-open', 'false');
    // focus returns to toggle
    await expect(page.locator('#sidebarToggle')).toBeFocused();
  });

  test('backdrop click closes drawer', async ({ page }) => {
    await page.locator('#sidebarToggle').click();
    await page.locator('#sidebarBackdrop').click({ position: { x: 1000, y: 500 } });
    await expect(page.locator('#sidebarDrawer')).toHaveAttribute('data-open', 'false');
  });

  test('drawer tab click switches tab and closes drawer', async ({ page }) => {
    await page.locator('#sidebarToggle').click();
    await page.locator('#sidebarDrawer .nav-item[data-tab-id="archive"]').click();
    await expect(page.locator('#sidebarDrawer')).toHaveAttribute('data-open', 'false');
    await expect(page.locator('#archiveTab')).toBeVisible();
    await expect(page.locator('#sidebarDrawer .nav-item[data-tab-id="archive"]')).toHaveClass(/active/);
  });

  test('state persists across reload (open)', async ({ page }) => {
    await page.locator('#sidebarToggle').click();
    await page.reload();
    await expect(page.locator('#sidebarDrawer')).toHaveAttribute('data-open', 'true');
  });
});

test.describe('Sidebar hidden on mobile', () => {
  test('viewport 375×667 — toggle/backdrop/drawer all hidden, bottom-nav visible', async ({ page }) => {
    await page.setViewportSize({ width: 375, height: 667 });
    await seedOnboarding(page);
    await expect(page.locator('#sidebarToggle')).toBeHidden();
    await expect(page.locator('#sidebarBackdrop')).toBeHidden();
    await expect(page.locator('#sidebarDrawer')).toBeHidden();
    await expect(page.locator('#bottomNav')).toBeVisible();
  });
});
```

- [ ] **Step 2: Run tests**

Run: `npx playwright test tests/smoke/sidebar-drawer.spec.ts --reporter=list`
Expected: all PASS.

Run: `npx playwright test tests/smoke/desktop-layout.spec.ts`
Expected: briefing x >= 240 ← **PASS** now (drawer is overlay; briefing starts at x=0 and parent has no padding-left; 240 은 viewport ≥ 768의 여유 폭 기준 — briefing-scroll padding 0 20px 감안 x >= 20 이면 occlusion 없음). 수치 재검토 필요.

> **주의 — 할루시네이션 가능성**: 위 `x >= 240` 임계치는 이전 sidebar 너비를 가정한 것. 이제 drawer는 overlay이므로 briefing은 x=0부터 렌더 OK. **assertion을 `x >= 0` + "no overlap with `.sidebar-drawer` bounding box when drawer closed"로 바꿔야** 의미 맞음. Task 1 테스트를 같이 수정:

```ts
// tests/smoke/desktop-layout.spec.ts 의 briefing 테스트 assertion 업데이트
const drawerBox = await page.locator('#sidebarDrawer').boundingBox();
const cardBox = await firstCard.boundingBox();
expect(cardBox).not.toBeNull();
// drawer closed → drawerBox.x < 0 (translateX(-100%))
expect(cardBox!.x).toBeGreaterThanOrEqual(0);
if (drawerBox && drawerBox.width > 0) {
  // card left edge should not be inside drawer visible area
  expect(cardBox!.x + 10).toBeGreaterThan(drawerBox.x + drawerBox.width);
}
```

- [ ] **Step 3: Commit**

```bash
git add tests/smoke/sidebar-drawer.spec.ts tests/smoke/desktop-layout.spec.ts
git commit -m "test(smoke): sidebar drawer + desktop layout regression

Covers open/close paths (click/ESC/backdrop/tab-click), reload
persistence, mobile hidden state, and briefing card
non-occlusion on 1440/1920 viewports."
```

---

### Task 8: Settings section max-width

부모 `.settings-section`에 `max-width: 560px; margin: 0 auto` 적용. 자식 width: 100% inline style은 부모 제한 자동 승계.

**Files:**
- Modify: `src/styles/components/forms.css`

- [ ] **Step 1: Write failing unit test**

```ts
// tests/unit/css-tokens.spec.ts 에 append
it('settings-section has max-width 560px', () => {
  const css = readFileSync(resolve('src/styles/components/forms.css'), 'utf-8');
  expect(css).toMatch(/\.settings-section\s*\{[^}]*max-width:\s*560px/);
  expect(css).toMatch(/\.settings-section\s*\{[^}]*margin:\s*0\s+auto/);
});
```

- [ ] **Step 2: Run → FAIL**

Run: `npm test -- tests/unit/css-tokens.spec.ts` → FAIL.

- [ ] **Step 3: Update forms.css**

Edit [src/styles/components/forms.css:9](src/styles/components/forms.css#L9):

```css
.settings-section {
  padding: 20px;
  max-width: 560px;
  margin: 0 auto;
}
```

- [ ] **Step 4: Add Playwright assertion**

Append to `tests/smoke/desktop-layout.spec.ts`:

```ts
test('settings tab — section width ≤ 560px on wide viewport', async ({ page }) => {
  await page.setViewportSize({ width: 1920, height: 1080 });
  await seedOnboarding(page);
  // open settings via drawer
  await page.locator('#sidebarToggle').click();
  await page.locator('#sidebarDrawer .nav-item[data-tab-id="settings"]').click();
  const section = page.locator('#settingsTab');
  const box = await section.boundingBox();
  expect(box).not.toBeNull();
  expect(box!.width).toBeLessThanOrEqual(560);
});
```

(heading of file already declares `seedOnboarding`; if missing, copy from sidebar-drawer.spec.ts.)

- [ ] **Step 5: Run tests**

Run: `npm test -- tests/unit/css-tokens.spec.ts` → PASS.
Run: `npx playwright test tests/smoke/desktop-layout.spec.ts --reporter=list` → PASS.

- [ ] **Step 6: Commit**

```bash
git add src/styles/components/forms.css tests/unit/css-tokens.spec.ts tests/smoke/desktop-layout.spec.ts
git commit -m "fix(ui): settings-section max-width 560px centered

Prevents inputs/save buttons from stretching to viewport width
on large monitors. Mobile unchanged (viewport < 560px)."
```

---

### Task 9: Full regression sweep

vitest + Playwright 전체 돌려 기존 assertion 전부 통과 확인.

- [ ] **Step 1: Run vitest**

Run: `npm test`
Expected: 146 기존 + focus-trap(4) + sidebar(8) + css-tokens(+4) = **162 PASS**. 실패 시 실패 테스트 파일명/케이스 기록 후 중단.

- [ ] **Step 2: Run Playwright**

Run: `npm run test:smoke`
Expected: 22 기존 + desktop-layout(4) + sidebar-drawer(6) = **32 PASS**.

- [ ] **Step 3: Run lint**

Run: `npm run lint`
Expected: 0 errors.

- [ ] **Step 4: Build**

Run: `npm run build`
Expected: success. dist 사이즈 출력 캡처. gzip 증가 < 3KB 확인.

- [ ] **Step 5: Local preview sanity**

Run: `npm run preview` → Hayden 로컬 확인 요청 지점. drawer 열고 닫기 / settings 폭 / 탭 전환 / 모바일 responsive 수동 점검.

> **No commit needed** (검증만). 실패 발견 시 해당 Task로 돌아가 fix.

---

### Task 10: `--text-*` → `--font-size-*` rename (Hayden confirmed 2026-04-23)

v3.3.0 open item P1-A. 현 시점 consumer 0 → 파급 0. 리뷰 대상에 포함시키기 위해 Task 11 (독립 리뷰)보다 앞에 배치.

**Files:**
- Modify: `src/styles/tokens.css`
- Modify: `tests/unit/css-tokens.spec.ts`

- [ ] **Step 1: Unit test (absence of old names, presence of new)**

```ts
// tests/unit/css-tokens.spec.ts 에 append
it('font-size tokens use --font-size-* naming (not --text-*)', () => {
  const css = readFileSync(resolve('src/styles/tokens.css'), 'utf-8');
  expect(css).toMatch(/--font-size-base:\s*16px/);
  expect(css).not.toMatch(/--text-base:\s*16px/);
});

it('color tokens preserve --text-primary/secondary/tertiary', () => {
  const css = readFileSync(resolve('src/styles/tokens.css'), 'utf-8');
  expect(css).toMatch(/--text-primary:\s*#[0-9A-F]{6}/i);
});
```

- [ ] **Step 2: Run → FAIL**

Run: `npm test -- tests/unit/css-tokens.spec.ts`
Expected: FAIL (new font-size tokens not present yet).

- [ ] **Step 3: Rename in tokens.css**

Edit [src/styles/tokens.css:71-77](src/styles/tokens.css#L71): rename `--text-xs/sm/base/lg/xl/2xl/3xl` → `--font-size-xs/sm/base/lg/xl/2xl/3xl`. Color tokens `--text-primary/secondary/tertiary` 그대로 유지.

- [ ] **Step 4: Grep confirm no stale consumers**

Run: `grep -rnE 'var\(--text-(xs|sm|base|lg|xl|2xl|3xl)\)' src/ tests/`
Expected: 0 matches (아직 consumer 없음 — 확인 필수).

추가 안전망으로 new name이 이미 어딘가에 중복 정의됐는지도 확인:

Run: `grep -rn '--font-size-' src/`
Expected: only tokens.css (정의 한 곳만).

- [ ] **Step 5: PASS + commit**

Run: `npm test` → PASS 전체.

```bash
git add src/styles/tokens.css tests/unit/css-tokens.spec.ts
git commit -m "refactor(tokens): rename --text-* font-size tokens to --font-size-*

Resolves v3.3.0 open item P1-A (naming collision with
--text-primary/secondary/tertiary color tokens). No consumers
yet — zero blast radius."
```

---

### Task 11: Independent code review

- [ ] **Step 1: Dispatch superpowers:code-reviewer (standard review)**

v3.3.1은 UX 로직 + state 포함 → 전역 CLAUDE.md "표준 리뷰" 기준.
커밋 범위: `feat/v3.3.1-sidebar-layout` 브랜치 전체 (Task 1~10, rename 포함).

- [ ] **Step 2: Triage findings**

- P0 (차단): 배포 전 fix (새 커밋).
- P1 (곧 수정): 같은 브랜치에서 fix 또는 별도 hotfix PR 합의.
- P2 ("user should decide"): 옵션 제시만, 자동 적용 금지 ([feedback_review_p2_autonomy.md] 준수).

- [ ] **Step 3: Apply P0/P1 fixes + commit**

```bash
git commit -m "fix(review): address P0/P1 findings"
```

---

### Task 12: Merge + deploy (Hayden trigger only)

- [ ] **Step 1: Hayden 독립 리뷰 결과 확인 + merge 승인 요청**

> "v3.3.1 작업 완료, 독립 리뷰 P0=0. main merge 승인 요청드립니다."

Hayden 승인 시:

```bash
git checkout main
git merge --no-ff feat/v3.3.1-sidebar-layout -m "Merge feat/v3.3.1-sidebar-layout (v3.3.1 sidebar + layout + settings)"
```

- [ ] **Step 2: Build + Firebase deploy (Hayden "배포해줘" 명시 시만)**

```bash
npm run build
firebase deploy --only hosting
```

- [ ] **Step 3: Completion doc**

Create `.claude/completions/2026-04-23-v3.3.1-sidebar-layout.md` (CLAUDE.md 템플릿 따름).

- [ ] **Step 4: Memory update**

Append to `project_daily_growth_uiux_overhaul.md`:
- v3.3.1 배포 SHA + date
- v3.3.2 heatmap 다음 시작점

Create/update `project_daily_growth_v33_1.md` with 배포 내역 + next-phase open items.

---

## Self-Review Checklist

**Spec coverage map**:

| Spec section | Plan task |
|---|---|
| §6.1 상태 모델 | Task 6 (open/close state) |
| §6.2 DOM 구조 | Task 6 (mountSidebar), Task 5 (CSS) |
| §6.3 CSS | Task 5 |
| §6.4 JS 모듈 | Task 6 |
| §6.5 Focus trap | Task 4 |
| §6.6 탭 active sync | Task 6 step 4 (nav.ts selector 확장) |
| §6.7 localStorage 영속 | Task 6 (dg-sidebar-last-state) |
| §7.1 Layout 수정 | Task 2 (.app-container 삭제) + Task 3 (.bottom-nav 데스크탑 분기 삭제) — **단, spec 가설과 실제 메커니즘 차이 기록** |
| §7.2 회귀 테스트 | Task 1, Task 7 |
| §7.3 drift guard 주석 | Task 3에서 layout.css 재작성 시 주석 포함 |
| §8.1 Settings CSS | Task 8 |
| §8.3 DOM 점검 | §0.4 에서 완료 |
| §11.1 vitest 신규 | Task 4, Task 6 (+12개 신규) |
| §11.2 Playwright 신규 | Task 1, Task 7, Task 8 (+10개 신규) |
| §11.4 접근성 수동 | Task 9 step 5 |
| §12 배포 | Task 11, Task 12 |

**Placeholder scan**: 완료. TBD/TODO/"fill in details" 없음. 모든 code 스텝에 실제 코드 포함.

**Type consistency**: `TabId`는 [src/ui/nav.ts:8](src/ui/nav.ts#L8)에서 import, `FocusTrap` 인터페이스는 Task 4에서 export 후 Task 6에서 import. 일관.

**Drift from spec explicit**: §0.2 / §0.5 / §6.6 data-tab vs data-tab-id, §0.6 다크 모드 미구현 → Plan에서 명시적으로 기록.

**할루시네이션 가능성 경고**:
- Task 7의 briefing x 임계치는 초기 오추정 → 수정본 assertion 명시
- CSS Cascade Layers iOS 15.3+ 타깃은 spec §15 Open Q#1에서 확정 (iOS 16+)
