# v3.45 App Check Phase B + GCP Referrer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** App Check enforce 활성 + 응답 contract 정합 + GCP referrer 다중 보안층 + v3.44.1 carry P2 2건 흡수.

**Architecture:** Phase A 24h monitor 통과 후 Phase B `enforceAppCheck:true` backend SDK enforce + 정보 누출 경로 3건 → 202 generic. C7은 GCP Console manual + Node.js verify script + docs.

**Tech Stack:** firebase-functions v6.6 (onRequest), Vitest, Playwright, Node.js (verify script), GCP Console (manual step).

---

## File Structure

| Path | 변경 | 책임 |
|---|---|---|
| `functions/src/sendAnswerDm.ts` | Modify | T1: `enforceAppCheck:true` + 3 error → 202. T4: JSDoc wording 정정 |
| `functions/src/__tests__/sendAnswerDm-contract.spec.ts` | **Create** | T1: 응답 contract 4 unit |
| `src/services/slack.ts` | Read | T1 caller grep target (변경 0 예상) |
| `src/ui/handlers/home.ts` | Read/Modify | T1 caller grep target — toast wording 정합 검토 |
| `src/ui/tabs/settings.ts` | Modify | T2: Slack 도움말 `<details>` UX copy |
| `tests/unit/settings-slack-help.spec.ts` | **Create** | T2: UX copy 2 unit |
| `scripts/verify-referrer.mjs` | **Create** | T3: HTTP referrer 검증 Node.js script |
| `docs/security/referrer.md` | **Create** | T3: 사용자 manual step 문서 |
| `package.json` | Modify | T3: `verify:referrer` script |
| `docs/superpowers/plans/2026-05-01-v3.14.1-p2-cleanup-plan.md` | Modify | T5: `/Users/hayden/...` → `<repo-root>` placeholder |

---

## Pre-Check (T0c — Codex 사전 Review, mandatory)

CLAUDE.md graduated 11사이클 ROI. plan v1 commit 후 dispatch.

- [ ] **Step 0c.0: plan v1 commit (T0c 진입 전 의무)**

```bash
git add docs/superpowers/plans/2026-05-21-v3.45-app-check-phase-b-plan.md
git commit -m "docs(v3.45 T0): plan v1 — 8 task ordering + TDD steps + Rollback runbook"
```

- [ ] **Step 0c.1: Codex 사전 review prompt 작성**

`/tmp/v3.45-codex-pre-prompt.txt`에 prompt 저장 (spec + plan 요약, P0/P1 catch 요청). 검토 영역:
1. firebase-functions v6.6 `enforceAppCheck:true` 옵션 정확한 동작 + Console 토글 관계
2. 응답 contract 202 변환 — frontend `slack.ts:46-49` `!res.ok` 분기 정합 (caller grep 검증)
3. App Check token 캐싱 TTL + getToken 호출 시점
4. GCP HTTP referrer + `verify-referrer.mjs` 검증 endpoint 현실성
5. emulator + App Check debug token 시뮬 가능 여부
6. Rollback runbook 2-scenario 우선순위 확정

- [ ] **Step 0c.2: Codex direct CLI 실행**

```bash
codex exec --skip-git-repo-check -o /tmp/v3.45-codex-pre.txt "$(cat /tmp/v3.45-codex-pre-prompt.txt)" < /dev/null
```

- [ ] **Step 0c.3: P0/P1 흡수 결정**

P0/P1 있으면 plan v2로 in-cycle 반영, commit `docs(v3.45 T0): plan v2 — Codex 사전 P0/P1 흡수`. P0=0/P1=0 면 plan v1 그대로 T1 진입.

- [ ] **Step 0c.4: small cycle exception 적용 여부**

Codex CLI 한도/실패 시 controller `pr-review-toolkit:code-reviewer` fallback. retro §verification에 명시.

---

## Task 1: C1 Backend `enforceAppCheck:true` + 3 Error → 202 Generic

**Files:**
- Modify: `functions/src/sendAnswerDm.ts:29-79`
- Create: `functions/src/__tests__/sendAnswerDm-contract.spec.ts`

### Step 1.1: caller grep + ripple 검토 의무

- [ ] **Step 1.1.1: src/ 전수 grep 실행**

```bash
rg -n "sendAnswerDm|autoSendAnswer|slack_user_not_found|slack_lookup_failed|slack_dm_open_failed" src/
```

Expected output:
- `src/services/slack.ts` (자체)
- `src/ui/tabs/settings.ts` (autoSend toggle 호출)
- `src/ui/handlers/home.ts` (답변 제출 후 호출)

- [ ] **Step 1.1.2: home.ts caller toast wording 확인**

`src/ui/handlers/home.ts`에서 `sendAnswerDm` catch 블록의 toast wording 확인. 만약 `e.message.startsWith('slack_user_not_found')` 같은 분기가 있으면 → 제거 (어차피 202로 호출 안 됨). 만약 generic catch면 → 변경 0.

수정 필요 시: 같은 commit (T1)에서 caller wording 같이 정합.

### Step 1.2: 응답 contract TDD

- [ ] **Step 1.2.1: 신규 spec 파일 작성 (failing tests)**

`functions/src/__tests__/sendAnswerDm-contract.spec.ts`:

```ts
import { describe, expect, it, vi, beforeEach } from 'vitest';
import * as slackModule from '../slack';
import * as rateLimitModule from '../rateLimit';
import { sendAnswerDm } from '../sendAnswerDm';

vi.mock('../slack');
vi.mock('../rateLimit');

function makeReq(body: Record<string, unknown>) {
  return { method: 'POST', body } as unknown as Parameters<typeof sendAnswerDm.run>[0];
}

function makeRes() {
  const res = {
    statusCode: 0,
    body: undefined as unknown,
    status(this: { statusCode: number; body: unknown }, code: number) {
      this.statusCode = code;
      return this;
    },
    json(this: { body: unknown }, payload: unknown) {
      this.body = payload;
      return this;
    },
  };
  return res as unknown as Parameters<typeof sendAnswerDm.run>[1];
}

const baseBody = {
  email: 'test@datarize.ai',
  question: 'Q',
  answer: 'A',
  streak: 1,
  xp: 100,
};

describe('sendAnswerDm 응답 contract — Phase B 정보 누출 차단', () => {
  beforeEach(() => {
    vi.mocked(rateLimitModule.checkRateLimit).mockReturnValue('ok');
    vi.mocked(slackModule.buildAnswerBlocks).mockReturnValue([] as unknown as ReturnType<typeof slackModule.buildAnswerBlocks>);
  });

  it('user_not_found → 202 generic {ok:true} (Slack 멤버 enumeration 차단)', async () => {
    vi.mocked(slackModule.sendDmAsBot).mockRejectedValue(new Error('user_not_found'));
    const req = makeReq(baseBody);
    const res = makeRes();
    await sendAnswerDm.run(req, res);
    expect((res as { statusCode: number }).statusCode).toBe(202);
    expect((res as { body: unknown }).body).toEqual({ ok: true });
  });

  it('lookup_failed → 202 generic {ok:true}', async () => {
    vi.mocked(slackModule.sendDmAsBot).mockRejectedValue(new Error('lookup_failed'));
    const req = makeReq(baseBody);
    const res = makeRes();
    await sendAnswerDm.run(req, res);
    expect((res as { statusCode: number }).statusCode).toBe(202);
    expect((res as { body: unknown }).body).toEqual({ ok: true });
  });

  it('dm_open_failed → 202 generic {ok:true}', async () => {
    vi.mocked(slackModule.sendDmAsBot).mockRejectedValue(new Error('dm_open_failed'));
    const req = makeReq(baseBody);
    const res = makeRes();
    await sendAnswerDm.run(req, res);
    expect((res as { statusCode: number }).statusCode).toBe(202);
    expect((res as { body: unknown }).body).toEqual({ ok: true });
  });

  it('post_message_failed → 502 (실제 send 시도 후 실패, enumeration 불가 → 유지)', async () => {
    vi.mocked(slackModule.sendDmAsBot).mockRejectedValue(new Error('post_message_failed'));
    const req = makeReq(baseBody);
    const res = makeRes();
    await sendAnswerDm.run(req, res);
    expect((res as { statusCode: number }).statusCode).toBe(502);
    expect((res as { body: unknown }).body).toEqual({ error: 'post_message_failed' });
  });
});
```

**Note**: spec의 `sendAnswerDm.run` invocation은 firebase-functions v2 internal API — 실제 mock 패턴은 기존 `sendAnswerDm-cors.spec.ts`에서 사용한 helper 따라가야 함. implementer는 cors spec 패턴 먼저 확인:

```bash
cat functions/src/__tests__/sendAnswerDm-cors.spec.ts
```

만약 cors spec이 다른 패턴 (예: HTTP supertest로 onRequest call) 사용하면 동일 패턴 따라가기.

- [ ] **Step 1.2.2: 실패 확인**

```bash
cd functions && npm test -- sendAnswerDm-contract
```

Expected: FAIL (현재 응답은 404/502, 신규 spec은 202 기대)

### Step 1.3: 응답 contract + `enforceAppCheck:true` 구현

- [ ] **Step 1.3.1: `functions/src/sendAnswerDm.ts` 수정**

`sendAnswerDm.ts:29-44` 변경:

```ts
/** @public — Firebase Cloud Function HTTPS endpoint via Hosting rewrite. firebase deploy로 us-central1에 mounted, client는 same-origin rewrite (`/api/sendAnswerDm`)로 호출. App Check Phase B (v3.45): enforceAppCheck:true backend SDK level. v3.44.1 T4 audit 명령에서 unused로 잡힌 expected noise 차단. */
export const sendAnswerDm = onRequest(
  {
    secrets: [SLACK_BOT_TOKEN],
    maxInstances: 1,
    cors: CORS_ORIGINS,
    region: 'us-central1',
    enforceAppCheck: true,  // v3.45 Phase B: 인증 실패 시 자동 401
  },
  async (req, res) => {
    // ... validation 그대로 ...
```

`sendAnswerDm.ts:65-77` catch 블록 변경:

```ts
    } catch (e) {
      const msg = e instanceof Error ? e.message : 'unknown';
      // v3.45 Phase B: 정보 누출 회피 위해 3 error → 202 generic {ok:true}
      if (msg === 'user_not_found' || msg === 'lookup_failed' || msg === 'dm_open_failed') {
        res.status(202).json({ ok: true });
      } else if (msg === 'post_message_failed') {
        res.status(502).json({ error: 'post_message_failed' });
      } else if (msg.startsWith('http_5')) {
        res.status(502).json({ error: 'slack_upstream' });
      } else {
        res.status(500).json({ error: 'internal' });
      }
    }
```

T4 (P2-1)도 같은 commit에서 흡수: JSDoc wording "HTTPS callable export" → "HTTPS endpoint via Hosting rewrite".

- [ ] **Step 1.3.2: 통과 확인**

```bash
cd functions && npm test -- sendAnswerDm-contract
```

Expected: PASS 4/4

- [ ] **Step 1.3.3: 회귀 확인 (functions 전체)**

```bash
cd functions && npm test
```

Expected: PASS 28/28 (기존 24 + 신규 4)

- [ ] **Step 1.3.4: home.ts caller 검토 결과 적용**

Step 1.1.2 결과에 따라 home.ts toast wording 정합. 같은 commit에 staging.

### Step 1.4: T1 Commit

- [ ] **Step 1.4.1: 검증**

```bash
cd /Users/hayden/Hayden_AX_Project/my_ai_assistance && npx tsc --noEmit && npm run lint
```

Expected: 0 errors / 0 warnings.

- [ ] **Step 1.4.2: Commit**

```bash
git add functions/src/sendAnswerDm.ts functions/src/__tests__/sendAnswerDm-contract.spec.ts
# home.ts 변경 있으면 추가
git commit -m "fix(v3.45 T1+T4): App Check Phase B enforce + 3 error → 202 generic + JSDoc wording

- functions/src/sendAnswerDm.ts: enforceAppCheck:true + (user_not_found / lookup_failed / dm_open_failed) → 202 {ok:true}
- post_message_failed / slack_upstream → 502 유지 (enumeration 불가)
- v3.44.1 carry P2-1: JSDoc 'HTTPS callable' → 'HTTPS endpoint via Hosting rewrite' (T4 흡수)
- 신규 spec: sendAnswerDm-contract.spec.ts (4 cases)

caller grep: src/services/slack.ts (변경 0, !res.ok 분기에서 202는 자동 success) + home.ts (caller-side toast wording 정합)"
```

---

## Task 2: C1 Settings UX Copy

**Files:**
- Modify: `src/ui/tabs/settings.ts`
- Create: `tests/unit/settings-slack-help.spec.ts`

### Step 2.1: 신규 spec 작성 (failing tests)

- [ ] **Step 2.1.1: 현재 settings.ts 구조 확인**

```bash
grep -n "slack\|Slack\|autoSend\|<details>" src/ui/tabs/settings.ts | head -20
```

Slack 연동 section 위치 파악 후 그 하단에 `<details>` 삽입.

- [ ] **Step 2.1.2: 신규 unit spec**

`tests/unit/settings-slack-help.spec.ts`:

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { renderSettings } from '../../src/ui/tabs/settings';

describe('settings — Slack 도움말 UX copy (v3.45 C1)', () => {
  beforeEach(() => {
    document.body.innerHTML = '<div id="settingsTab"></div>';
  });

  it('Slack 연동 section 하단에 <details class="slack-help"> 노출', () => {
    renderSettings(document.querySelector<HTMLDivElement>('#settingsTab')!);
    const help = document.querySelector('.slack-help');
    expect(help).not.toBeNull();
    expect(help?.tagName.toLowerCase()).toBe('details');
  });

  it('<details>에 도움말 4단계 항목 포함', () => {
    renderSettings(document.querySelector<HTMLDivElement>('#settingsTab')!);
    const items = document.querySelectorAll('.slack-help ol li');
    expect(items.length).toBe(4);
    expect(items[0].textContent).toContain('워크스페이스');
    expect(items[3].textContent).toContain('HR');
  });
});
```

**Note**: `renderSettings` export 가정. 실제 export name은 settings.ts 확인 후 정합 (initializeSettings / setupSettings / renderSettings 등).

- [ ] **Step 2.1.3: 실패 확인**

```bash
npm test -- settings-slack-help
```

Expected: FAIL (`.slack-help` 미존재)

### Step 2.2: 구현

- [ ] **Step 2.2.1: settings.ts UX copy 추가**

Slack 연동 section 하단 (autoSend toggle 뒤)에:

```ts
// v3.45 C1: Phase B enforce 후 202 generic 응답으로 Slack 멤버 enumeration 차단.
// DM이 안 도착하면 사용자 self-troubleshooting 안내.
const slackHelpHtml = `
  <details class="slack-help">
    <summary>DM이 안 온다면?</summary>
    <ol>
      <li>Slack 워크스페이스(@datarize.ai)에 가입되어 있는지 확인</li>
      <li>입력한 이메일이 Slack 가입 이메일과 일치하는지 확인</li>
      <li>새로고침 후 다시 시도</li>
      <li>그래도 안 되면 HR/GA에 문의 (sukjoon.hwang@datarize.ai)</li>
    </ol>
  </details>
`;
```

구체 삽입 위치는 implementer가 현재 settings.ts 구조 따라 결정.

- [ ] **Step 2.2.2: CSS 토큰 (필요 시)**

만약 `<details>` default 스타일이 디자인 시스템과 어색하면 `src/styles/main.css` 또는 settings 관련 css에 최소 추가. 그렇지 않으면 default 사용.

- [ ] **Step 2.2.3: 통과 확인**

```bash
npm test -- settings-slack-help
```

Expected: PASS 2/2

### Step 2.3: T2 Commit

- [ ] **Step 2.3.1: Commit**

```bash
git add src/ui/tabs/settings.ts tests/unit/settings-slack-help.spec.ts
# CSS 변경 있으면 추가
git commit -m "feat(v3.45 T2): settings — Slack DM 도움말 UX copy (App Check Phase B 202 generic 응답 정합)

- src/ui/tabs/settings.ts: Slack 연동 section 하단 <details class='slack-help'> 4단계 안내
- 신규 spec: settings-slack-help.spec.ts (2 cases)"
```

---

## Task 3: C7 GCP HTTP Referrer + verify Script + Docs

**Files:**
- Create: `scripts/verify-referrer.mjs`
- Create: `docs/security/referrer.md`
- Modify: `package.json` (scripts.verify:referrer)

### Step 3.1: verify-referrer.mjs 작성

- [ ] **Step 3.1.1: `scripts/verify-referrer.mjs`**

```js
#!/usr/bin/env node
// v3.45 C7: GCP HTTP referrer 제한 검증
// Firebase API key가 production 도메인에서만 사용 가능한지 확인.
// 검증 대상: Firebase Identity Toolkit (현재 미사용이지만 referrer 제한 동작 검증용).

import { config } from 'dotenv';
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, resolve } from 'node:path';

const __dirname = dirname(fileURLToPath(import.meta.url));
config({ path: resolve(__dirname, '../.env.local') });

const API_KEY = process.env.VITE_FIREBASE_API_KEY;
if (!API_KEY) {
  console.error('❌ VITE_FIREBASE_API_KEY missing in .env.local');
  process.exit(1);
}

// Identity Toolkit REST endpoint — referrer 제한 검증용
const ENDPOINT = `https://identitytoolkit.googleapis.com/v1/projects?key=${API_KEY}`;

const ALLOWED_REFERER = 'https://my-ai-assistant-904f3.web.app/';
const BLOCKED_REFERER = 'https://attacker.example/';

async function check(referer, expectedBlocked) {
  const res = await fetch(ENDPOINT, {
    method: 'GET',
    headers: { Referer: referer },
  });
  const status = res.status;
  const blocked = status === 403 || status === 401;
  const label = expectedBlocked ? 'blocked' : 'allowed';
  const ok = expectedBlocked === blocked;
  console.log(`${ok ? '✓' : '✗'} ${label} referrer (${referer}) → ${status}`);
  return ok;
}

const allowed = await check(ALLOWED_REFERER, false);
const blocked = await check(BLOCKED_REFERER, true);

if (allowed && blocked) {
  console.log('\n✅ HTTP referrer 제한 정상 작동');
  process.exit(0);
} else {
  console.log('\n⚠️ HTTP referrer 제한 검증 실패 — Console 설정 또는 적용 대기 확인');
  process.exit(1);
}
```

**Note**: `dotenv` package 필요. 이미 설치돼 있는지 확인:

```bash
node -e "import('dotenv').then(() => console.log('OK')).catch(() => console.log('MISSING'))"
```

미설치 시:
```bash
npm install --save-dev dotenv
```

- [ ] **Step 3.1.2: chmod**

```bash
chmod +x scripts/verify-referrer.mjs
```

### Step 3.2: docs/security/referrer.md

- [ ] **Step 3.2.1: `docs/security/referrer.md`**

```markdown
# Firebase API Key HTTP Referrer 제한 + App Check Phase B

## 배경
Firebase Web API key는 client-side public이지만, GCP Credentials에서 HTTP referrer 제한 미설정 시
abuser가 본인 도메인에서 Firebase services 호출 가능. App Check Phase B와 함께 다중 보안층 구성.

---

## 1. GCP Console — HTTP referrer 제한 (사용자 manual)

1. https://console.cloud.google.com/apis/credentials?project=my-ai-assistant-904f3 접속
2. **Browser key** (Firebase API key) 선택 — 일반적으로 "Browser key (auto created by Firebase)"
3. **Application restrictions** → "HTTP referrers (web sites)" 선택
4. 다음 referrer 추가:
   - `https://my-ai-assistant-904f3.web.app/*`
   - `https://my-ai-assistant-904f3.firebaseapp.com/*`
   - `http://localhost:*/*` (dev 환경)
5. 저장 → 적용까지 약 5분 대기

---

## 2. 검증 — verify-referrer.mjs

```bash
npm run verify:referrer
```

Expected 출력:
```
✓ allowed referrer (https://my-ai-assistant-904f3.web.app/) → 200 또는 400
✓ blocked referrer (https://attacker.example/) → 403

✅ HTTP referrer 제한 정상 작동
```

설정 직후 적용 대기 (~5분) 후 재실행.

---

## 3. App Check Phase B — Enforce 토글 (사용자 manual)

1. https://console.firebase.google.com/project/my-ai-assistant-904f3/appcheck/apps 접속
2. **Web 앱** 선택 → "API enforcement" 탭
3. Cloud Functions (`sendAnswerDm`) 항목 → "Monitor" → "Enforce" 토글
4. 24h 모니터 결과 ≥ 99% 확인 후 진행 (v3.41 Phase A 결과 인용, v3.45 deploy 직전 확인 완료)

**Note**: onRequest endpoint는 코드 옵션 `enforceAppCheck:true` (functions/src/sendAnswerDm.ts:31)가 primary. Console 토글은 미래 Firestore/RTDB/Storage 도입 대비 deep defense.

---

## 4. Rollback

| 우선순위 | 액션 | 시간 | 시나리오 |
|---|---|---|---|
| A1 / B1 | `enforceAppCheck: false` + `firebase deploy --only functions` | 2~3분 | 시나리오 A: Console 토글 영향 X / 시나리오 B (확정) |
| A2 / B2 | Console "Enforce" → "Monitor" 토글 | 즉시 | 시나리오 A (Console 영향 O) 1순위 |
| 3 | GCP referrer 제한 제거 (Console) | 즉시 | 정상 사용자 차단 시 |
| 4 | 202 → 404/502 응답 contract revert + deploy | 2~3분 | client 호환성 회귀 |

(시나리오는 T1 implementer가 firebase-functions docs 확인 후 확정 — spec §9.2)

---

## 5. v3.46+ 확장

- API restrictions (App Check API + Identity Toolkit API만 허용) — Daily Growth가 다른 endpoint 도입 시
- dev key 분리 (localhost referrer 대신 별도 dev API key)
```

### Step 3.3: package.json scripts

- [ ] **Step 3.3.1: `package.json` 수정**

```json
"scripts": {
  ...,
  "verify:referrer": "node scripts/verify-referrer.mjs"
}
```

### Step 3.4: T3 Commit

- [ ] **Step 3.4.1: 사전 검증 (script syntax)**

```bash
node --check scripts/verify-referrer.mjs
```

Expected: no syntax error

- [ ] **Step 3.4.2: Commit**

```bash
git add scripts/verify-referrer.mjs docs/security/referrer.md package.json
# dotenv 설치 시 package-lock.json도 추가
git commit -m "feat(v3.45 T3): C7 GCP HTTP referrer 제한 + verify script + 사용자 manual docs

- scripts/verify-referrer.mjs: Identity Toolkit endpoint로 referrer 제한 검증 (allowed/blocked 2 case)
- docs/security/referrer.md: GCP Console + App Check Phase B 토글 + Rollback runbook 단계별 절차
- package.json: npm run verify:referrer"
```

---

## Task 4: P2-1 JSDoc Wording 정정 (T1에 흡수됨, separate commit 불요)

T1 Step 1.3.1에서 JSDoc wording "HTTPS callable" → "HTTPS endpoint via Hosting rewrite" 정합 흡수. 별도 commit 없음. T4 task는 spec 기록용으로만 유지 (실제 변경은 T1 commit에 포함).

- [ ] **Step 4.1: T1 commit에 JSDoc wording 변경 포함 확인**

```bash
git show HEAD -- functions/src/sendAnswerDm.ts | grep -A2 "@public"
```

Expected: "HTTPS endpoint via Hosting rewrite" wording 확인.

---

## Task 5: P2-2 v3.14.1 Plan File Absolute Path Scrub

**Files:**
- Modify: `docs/superpowers/plans/2026-05-01-v3.14.1-p2-cleanup-plan.md`

### Step 5.1: absolute path 검색 + 치환

- [ ] **Step 5.1.1: 현재 absolute path 위치 확인**

```bash
grep -n "/Users/hayden" docs/superpowers/plans/2026-05-01-v3.14.1-p2-cleanup-plan.md
```

- [ ] **Step 5.1.2: 치환**

```bash
sed -i '' 's|/Users/hayden/Hayden_AX_Project/my_ai_assistance|<repo-root>|g' docs/superpowers/plans/2026-05-01-v3.14.1-p2-cleanup-plan.md
```

(macOS sed: `-i ''` 빈 문자열 backup suffix)

- [ ] **Step 5.1.3: 결과 검증**

```bash
grep -n "/Users/hayden" docs/superpowers/plans/2026-05-01-v3.14.1-p2-cleanup-plan.md
```

Expected: 0 matches.

```bash
grep -n "<repo-root>" docs/superpowers/plans/2026-05-01-v3.14.1-p2-cleanup-plan.md
```

Expected: 이전 absolute path 개수만큼 매칭.

### Step 5.2: T5 Commit

- [ ] **Step 5.2.1: Commit**

```bash
git add docs/superpowers/plans/2026-05-01-v3.14.1-p2-cleanup-plan.md
git commit -m "chore(v3.45 T5): v3.14.1 plan absolute path scrub (v3.44.1 carry P2-2)

/Users/hayden/Hayden_AX_Project/my_ai_assistance → <repo-root>
repo hygiene nit, secret 0 (Codex 최종 P2-2)."
```

---

## Task 6: Verify (3종 + smoke + bundle quint)

### Step 6.1: 3종 검증

- [ ] **Step 6.1.1: TS strict**

```bash
npx tsc --noEmit
```

Expected: 0 errors.

- [ ] **Step 6.1.2: Lint**

```bash
npm run lint
```

Expected: 0 errors.

- [ ] **Step 6.1.3: Vitest TZ=KST**

```bash
npm test
```

Expected: 1546+2 = 1548 PASS (T2 +2).

- [ ] **Step 6.1.4: Vitest TZ=NY**

```bash
TZ=America/New_York npm test
```

Expected: 1548 PASS (TZ drift 0).

- [ ] **Step 6.1.5: Functions vitest**

```bash
cd functions && npm test && cd ..
```

Expected: 24+4 = 28 PASS (T1 +4).

- [ ] **Step 6.1.6: Knip**

```bash
npx knip --no-progress
```

Expected: 0 findings (config hints 2건 허용).

### Step 6.2: Smoke + SW + App Check 정합

- [ ] **Step 6.2.1: 기존 smoke 109건 PASS 확인**

```bash
npm run test:smoke
```

Expected: 109 PASS. 회귀 시 즉시 fix.

- [ ] **Step 6.2.2: SW + App Check 정합 spec 추가 (deferral 가능)**

`tests/smoke/csp-app-check.spec.ts` 또는 신규 `tests/smoke/v3.45-app-check-phase-b.spec.ts`에 다음 검증 추가:

```ts
import { test, expect } from '@playwright/test';

test.describe('v3.45 App Check Phase B', () => {
  test('production deploy 후 정상 App Check token 발급 + 202/200 응답', async ({ page }) => {
    await page.goto('/');
    // App Check token 발급 path 검증 (간접: console.warn '[appCheck]' 없음)
    const warns: string[] = [];
    page.on('console', (msg) => {
      if (msg.type() === 'warning' && msg.text().includes('[appCheck]')) warns.push(msg.text());
    });
    await page.waitForLoadState('networkidle');
    expect(warns.length).toBe(0);
  });
});
```

**Note**: emulator 모드에서 enforce 검증은 firebase-functions emulator + App Check debug token 환경 변수 필요. 1차 implementer가 emulator 시뮬 가능 여부 확인 후 spec 작성. 어려우면 deferral (T8 사용자 production 검증으로 대체).

- [ ] **Step 6.2.3: deferral 결정 시 명시**

만약 emulator 시뮬 불가로 spec 작성 보류 시: retro §verification에 "smoke SW+App Check 정합 자동화는 emulator 한계로 deferral, T8 사용자 production 검증으로 대체" 명시.

### Step 6.3: Bundle Quint Record

- [ ] **Step 6.3.1: production build**

```bash
npm run build
```

Expected: vite build 성공.

- [ ] **Step 6.3.2: quint measurement**

```bash
echo "=== index ===" && gzip -c dist/assets/index-*.js | wc -c
echo "=== archive (sum) ===" && for f in dist/assets/archive-*.js; do gzip -c "$f" | wc -c; done | awk '{s+=$1} END {print s}'
echo "=== translateToast ===" && gzip -c dist/assets/translateToast-*.js | wc -c
echo "=== appCheck ===" && gzip -c dist/assets/appCheck-*.js | wc -c
echo "=== settings ===" && gzip -c dist/assets/settings-*.js | wc -c
```

Expected:
- index: 9379 (Δ 0)
- archive: 8968 (Δ 0)
- translateToast: 2136 (Δ 0)
- appCheck: 13215 (Δ 0)
- settings: ≤ 3960 (+<100 B from baseline 3860)

---

## Task 7: Codex 최종 Review

### Step 7.1: prompt 작성

- [ ] **Step 7.1.1: `/tmp/v3.45-codex-final-prompt.txt`**

spec 인용 + plan 인용 + 검증 결과 + bundle quint record + caller grep 결과 + risk 자가 진단.

분류: DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT.

### Step 7.2: direct CLI 실행

- [ ] **Step 7.2.1: Codex 최종 review**

```bash
codex exec --skip-git-repo-check -o /tmp/v3.45-codex-final.txt "$(cat /tmp/v3.45-codex-final-prompt.txt)" < /dev/null
```

- [ ] **Step 7.2.2: 분류 결과 처리**

- REJECT (P0/P1 있음): in-cycle fix → 재검증 → 재dispatch
- APPROVED_WITH_NOTES (P2 only): v3.46 carry로 명시
- DEPLOY_APPROVED: 그대로 T8 진입

---

## Task 8: Retro + 배포 + Sync

### Step 8.1: spec retro section + memory file

- [ ] **Step 8.1.1: spec §14 retro 섹션 append**

`docs/superpowers/specs/2026-05-21-v3.45-app-check-phase-b-design.md` 끝에:

```markdown
---

## §14. Retro

- 검증 결과: ...
- Codex 사전: ...
- Codex 최종: ...
- Bundle quint Δ: ...
- 신규 lessons: ...
```

- [ ] **Step 8.1.2: memory file 신규**

`/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v345.md`:

```markdown
---
name: daily-growth-v345
description: "DG v3.45 — App Check Phase B + GCP HTTP referrer 다중 보안층 + v3.44.1 carry P2 2건"
metadata:
  type: project
---

# DG v3.45 App Check Phase B + GCP Referrer
...
```

- [ ] **Step 8.1.3: MEMORY.md entry append**

상단 (v3.44.1 위)에 한 줄.

### Step 8.2: 배포 (사용자 manual 포함)

- [ ] **Step 8.2.1: production build + deploy**

```bash
npm run build && firebase deploy --only hosting,functions:sendAnswerDm
```

- [ ] **Step 8.2.2: 사용자 manual — Firebase Console "Enforce" 토글**

사용자에게 안내:
> "https://console.firebase.google.com/project/my-ai-assistant-904f3/appcheck/apps → Cloud Functions sendAnswerDm "Enforce" 활성"

- [ ] **Step 8.2.3: 사용자 manual — GCP HTTP referrer 추가**

사용자에게 안내:
> "https://console.cloud.google.com/apis/credentials?project=my-ai-assistant-904f3 → Browser key → HTTP referrers 추가 (.web.app/*, .firebaseapp.com/*, localhost:*/*)"

- [ ] **Step 8.2.4: verify-referrer.mjs 실행**

```bash
npm run verify:referrer
```

Expected: ✅ HTTP referrer 제한 정상 작동.

- [ ] **Step 8.2.5: production 답변 작성 → Slack DM 수신 + Settings UX copy 확인**

사용자 manual 검증. 통과 시 T8 종료.

### Step 8.3: Obsidian Vault Sync

- [ ] **Step 8.3.1: 표준 5-folder sync**

```bash
VAULT="/Users/hayden/Hayden/20_Projects/Dev_Project/SJ_Daily Growth(Ai assistant)"
SRC="/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory"
cp "$SRC/project_daily_growth_v345.md" "$VAULT/03-Lessons/"
cp "$SRC/MEMORY.md" "$VAULT/03-Lessons/"
cp /Users/hayden/Hayden_AX_Project/my_ai_assistance/docs/superpowers/specs/2026-05-21-v3.45-app-check-phase-b-design.md "$VAULT/02-Cycles/"
cp /Users/hayden/Hayden_AX_Project/my_ai_assistance/docs/superpowers/plans/2026-05-21-v3.45-app-check-phase-b-plan.md "$VAULT/02-Cycles/"
# lesson graduate 발생 시 01-Workflow 갱신
```

### Step 8.4: T8 Retro Commit

- [ ] **Step 8.4.1: Commit**

```bash
git add docs/superpowers/specs/2026-05-21-v3.45-app-check-phase-b-design.md
git commit -m "docs(v3.45 T8): retro — App Check Phase B + GCP referrer 다중 보안층 배포 완료"
```

---

## Risk + DoD

### DoD

- [ ] T0c Codex 사전 review P0/P1 흡수 (또는 small cycle exception)
- [ ] T1 backend enforce + 3 unit + caller grep done
- [ ] T2 UX copy + 2 unit
- [ ] T3 verify script + docs + package.json
- [ ] T4 JSDoc wording 정정 (T1 흡수)
- [ ] T5 absolute path scrub
- [ ] T6 3종 + smoke + bundle quint
- [ ] T7 Codex 최종 APPROVED 이상
- [ ] T8 deploy + Console toggle + verify + UX 확인 + retro + sync

### Risk

| 영역 | Severity | Likelihood | Mitigation |
|---|---|---|---|
| T1 Phase B production 차단 | **H** | L | Phase A ≥ 99% + Rollback runbook |
| T1 silent failure (DM 안 받아도 toast 성공) | M | L | caller grep + UX copy |
| T1 firebase-functions deps `enforceAppCheck` 미지원 | M | L | Pre-check (v6.6 확인 완료) |
| T3 dev localhost 차단 | M | M | `http://localhost:*/*` referrer 포함 |
| T7 Codex CLI 실패 | L | L | controller fallback |
| T8 사용자 manual step 누락 | M | M | docs/security/referrer.md 단계별 명시 |

---

## v3.46 Carry-Forward

| ID | 항목 | 출처 | 우선순위 |
|---|---|---|---|
| C4 | first briefing storage thrash | v3.41 carry | L |
| C5 | state↔ui layer cycle 정리 | v3.41 carry | L |
| C7+ | API restrictions + dev key 분리 | v3.45 deferred | L |
| v3.17 §7.2 | 식물 detail modal / Streak Freeze 등 | v3.17 retro | M (신규 기능 cycle) |
| L1 | App Check Phase B 2-phase rollout — CLAUDE.md graduate 후보 | v3.45 자체 | L (3 사례 누적 후) |
