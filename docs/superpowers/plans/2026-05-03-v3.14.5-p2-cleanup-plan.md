# v3.14.5 P2 Cleanup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v3.14.4 carry-forward P2 5건 + TZ guard sweep 9 spec 일괄 정리. v3.14 시리즈 종료.

**Architecture:** 신규 모듈/유틸/CSS 0. 기존 패턴 적용만 — `src/utils/dates.ts`의 `getDateStr()` import + `Number.isFinite`/`>= 0`/`Math.floor` 가드 + array element shape guard (v3.14.3 firstSeenAt graduation pattern 동일).

**Tech Stack:** TypeScript / Vite / Vitest / Playwright (smoke). 빌드 산출물 `dist/assets/index-*.js` CLI gzip 단일 표준 (T5 graduated).

**Spec:** `docs/superpowers/specs/2026-05-03-v3.14.5-p2-cleanup-design.md`

**실행 순서**: T0 → T6 (Codex 사전, gate) → T1 → T2 → T3 → T4 → T5 → T7 (Codex 최종, deploy gate) → 사용자 deploy 명시 → T8 → T9.

---

## Baseline (T0 후 채움)

```
vitest:    711 pass / 711 total (T0 실측: 711, drift vs 711 = 0, 완전 일치)
build:     CLI gzip 31.28 kB (T0 실측: 31.28 kB, drift vs 31.28 = 0.00, 완전 일치)
smoke N=3:
  Run 1: 93/93 PASS (34.1s)
  Run 2: 93/93 PASS (32.7s)
  Run 3: 93/93 PASS (32.4s)
cold-2 cascade: N=3 모두 PASS — 등장 패턴 없음 (cold-2 cascade 트리거 없음)
```

---

## Task 0: Baseline 측정 (spec T0)

**Files:**
- Modify: `docs/superpowers/plans/2026-05-03-v3.14.5-p2-cleanup-plan.md` (this file의 §Baseline 섹션)

- [ ] **Step 1: vitest count 측정**

```bash
cd /Users/hayden/Hayden_AX_Project/my_ai_assistance
npm test 2>&1 | tail -10
```

Expected: `Tests  N passed (N)` 형식. N=711 (claimed) reconcile.

- [ ] **Step 2: build + bundle CLI gzip 측정**

```bash
cd /Users/hayden/Hayden_AX_Project/my_ai_assistance
npm run build 2>&1 | tail -5
ls -la dist/assets/index-*.js
gzip -c dist/assets/index-*.js | wc -c | awk '{printf "%.2f kB\n", $1/1024}'
```

Expected: 31.28 kB ±0.05 (claimed reconcile).

- [ ] **Step 3: smoke cold-start N=3 fresh fork 측정**

```bash
cd /Users/hayden/Hayden_AX_Project/my_ai_assistance
# Run 1
npm run test:smoke 2>&1 | tail -5
# Wait (no caching), Run 2
npm run test:smoke 2>&1 | tail -5
# Run 3
npm run test:smoke 2>&1 | tail -5
```

Expected: 93/93 PASS in each run. Capture failed test names if any (cold-2 cascade detection).

- [ ] **Step 4: known intermittent fail triage (Hotfix-A1 패턴, v3.14.4 graduated)**

If any unit test fails: identify cause (KST date dependency / mock leak / fixture drift). If real bug → hotfix slot before T1. If retro mask → document as drift.

- [ ] **Step 5: Plan §Baseline 채우기**

Replace `TBD` placeholders in plan §Baseline with measured values:
- vitest: actual count + drift vs 711
- build: actual CLI gzip kB + drift vs 31.28
- smoke N=3: result matrix (Run 1 / 2 / 3 → PASS or FAIL list)
- cold-2 cascade: list of inconsistent failures across N=3 (for §6 v3.15+ backlog status update)

- [ ] **Step 6: Commit**

```bash
git add docs/superpowers/plans/2026-05-03-v3.14.5-p2-cleanup-plan.md
git commit -m "docs(v3.14.5 T0): baseline 측정 (vitest / bundle / smoke N=3 cold-2 cascade)"
```

---

## Task 6: Codex 사전 review (spec T6)

**Files:**
- External dispatch (codex:rescue agent or `/codex:rescue`)
- Modify: `docs/superpowers/specs/2026-05-03-v3.14.5-p2-cleanup-design.md` (in-cycle 반영)
- Modify: `docs/superpowers/plans/2026-05-03-v3.14.5-p2-cleanup-plan.md` (in-cycle 반영)

- [ ] **Step 1: Codex agent dispatch**

Prompt:
```
v3.14.5 P2 Cleanup spec + plan 사전 review.

Spec: /Users/hayden/Hayden_AX_Project/my_ai_assistance/docs/superpowers/specs/2026-05-03-v3.14.5-p2-cleanup-design.md
Plan: /Users/hayden/Hayden_AX_Project/my_ai_assistance/docs/superpowers/plans/2026-05-03-v3.14.5-p2-cleanup-plan.md

배경:
- v3.14.4 carry-forward P2 5건 (IR-1 heatmap KST seed / usage.count negative+fractional / migration progressDates+shape) + TZ guard sweep 9 spec
- 옵션 B+ / TZ sweep A=단일 task
- v3.14.4 codex 최종 P2 = IR-1, T3 reviewer P1 deferred = TZ sweep 9 spec, per-task review 4건 = numeric P2

Review 관점:
1. Q1: T2/T3 numeric guard 패턴이 v3.14.3 firstSeenAt + v3.14.4 T1/T2 graduation pattern과 정합한가? edge case 누락?
2. Q2: T1 IR-1 fix가 v3.4 cardnews + v3.14.4 T3 review fix precedent와 1:1 매칭인가?
3. Q3: T4 TZ sweep 9 spec / 15 sites가 mechanical sweep으로 충분한가? 패턴 spillover 위험?
4. Q4: vitest 산식 +6~9 = 717~720이 task 분담과 일치하는가? scope-faithful no-op trade-off 정상?
5. Q5: Bundle 단일 표준 채택 (T5)이 v3.15+ baseline에 정확히 영향?
6. Q6: Codex 권한 issue fallback (사전 self-review / 최종 cycle wrap-up)이 v3.14.2 패턴 충실?
7. Q7: §1.2 명시 제외 항목이 합리적? smoke flake centralize 트리거 판단 기준?

P0/P1/P2 분류로 출력. P0/P1는 in-cycle 반영, P2는 §6 backlog로 carry-forward.
```

Expected: Codex 출력 (P0=N / P1=N / P2=N).

- [ ] **Step 2: P0/P1 in-cycle 반영**

If P0/P1 발견:
- Spec §9 변경 이력 row 추가
- Spec/plan 해당 섹션 수정
- Commit: `docs(v3.14.5 spec v2): codex 사전 review N건 P1 in-cycle 반영`

If 권한 issue로 미실행: spec self-review로 대체 (v3.14.2 fallback 패턴), 최종 review는 cycle wrap-up 시점 재시도. Spec §9에 "Codex 사전 권한 issue → self-review 대체" 명시.

- [ ] **Step 3: P2 carry-forward 등록**

P2 항목을 spec §6 v3.14.5+ Backlog에 추가. (T8 retro에서 v3.15 carry-forward로 정리.)

- [ ] **Step 4: Commit (변경 있을 때만)**

```bash
git add docs/superpowers/specs/2026-05-03-v3.14.5-p2-cleanup-design.md docs/superpowers/plans/2026-05-03-v3.14.5-p2-cleanup-plan.md
git commit -m "docs(v3.14.5 spec v2): codex 사전 review 반영 (P0=N P1=N P2=N)"
```

---

## Task 1: IR-1 heatmap KST seed false-green fix (spec T1)

**Files:**
- Modify: `tests/smoke/heatmap.spec.ts` — `seedAndGoto` 헬퍼의 `addInitScript` callback이 브라우저 context이므로 Node-side `getDateStr()`로 KST today 계산 후 typed `args`로 전달 (v3.14.4 T3 review fix `a61d55e` precedent)

**배경**: `seedAndGoto`의 `addInitScript(() => { ... new Date().toISOString().slice(0,10) ... })` 콜백은 **브라우저 context**에서 실행되므로 Node-side `getDateStr()` 직접 참조 불가. v3.14.4 T3 review fix `a61d55e`의 desktop-layout.spec.ts / tier-downgrade.spec.ts 동일 패턴 적용.

- [ ] **Step 1: 현재 heatmap.spec.ts 확인**

```bash
sed -n '1,5p;19,40p;83,95p' tests/smoke/heatmap.spec.ts
```

Expected: line 22 `await page.addInitScript(() => {` + line 29 `const today = new Date().toISOString().slice(0, 10);` (브라우저 context 내부) + line 85 `test('today cell has .is-today class', ...)`.

- [ ] **Step 2: import getDateStr 추가 — Node-side helper**

`tests/smoke/heatmap.spec.ts` 상단 (line 1 직후):

Before:
```typescript
import { test, expect } from '@playwright/test';
```

After:
```typescript
import { test, expect } from '@playwright/test';
import { getDateStr } from '../../src/utils/dates';
```

- [ ] **Step 3: `seedAndGoto` 헬퍼 — Node-side today + args 전달 패턴**

Before (line 21-38):
```typescript
async function seedAndGoto(page: import('@playwright/test').Page): Promise<void> {
  await page.addInitScript(() => {
    localStorage.setItem('user', JSON.stringify({
      name: 'tester',
      interests: ['growth'],
      onboardedAt: '2026-04-01',
      streak: 1, lastActiveDate: '', xp: 10, earnedBadges: {}, gamificationMigrated: true, schemaVersion: 2,
    }));
    const today = new Date().toISOString().slice(0, 10);
    localStorage.setItem('dg.answers', JSON.stringify([
      { date: today, text: 'today', type: '감정' },
    ]));
    sessionStorage.removeItem('dg-heatmap-animated');
    localStorage.removeItem('dg-sidebar-last-state');
  });
  await page.goto('/');
  ...
}
```

After:
```typescript
async function seedAndGoto(page: import('@playwright/test').Page): Promise<void> {
  // v3.14.5 T1 (Codex IR-1): KST today를 Node-side에서 계산해 args로 전달.
  // browser-context의 toISOString()은 UTC라 KST 새벽엔 1일 어긋나 .is-today cell 0개로 false-green.
  const today = getDateStr();
  await page.addInitScript((args: { today: string }) => {
    localStorage.setItem('user', JSON.stringify({
      name: 'tester',
      interests: ['growth'],
      onboardedAt: '2026-04-01',
      streak: 1, lastActiveDate: '', xp: 10, earnedBadges: {}, gamificationMigrated: true, schemaVersion: 2,
    }));
    localStorage.setItem('dg.answers', JSON.stringify([
      { date: args.today, text: 'today', type: '감정' },
    ]));
    sessionStorage.removeItem('dg-heatmap-animated');
    localStorage.removeItem('dg-sidebar-last-state');
  }, { today });
  await page.goto('/');
  ...
}
```

- [ ] **Step 4: today cell test에 data-date 일치 assertion 추가**

Before (line 85-88):
```typescript
test('today cell has .is-today class', async ({ page }) => {
    await seedAndGoto(page);
    await expect(page.locator('.heatmap-cell.is-today')).toHaveCount(1);
  });
```

After:
```typescript
test('today cell has .is-today class', async ({ page }) => {
    await seedAndGoto(page);
    const todayCell = page.locator('.heatmap-cell.is-today');
    await expect(todayCell).toHaveCount(1);
    // v3.14.5 T1 (Codex IR-1): cell의 [data-date]가 Node-side getDateStr()과 일치하는지
    // 명시 검증 — KST-shifted host에서 false-green 차단.
    await expect(todayCell).toHaveAttribute('data-date', getDateStr());
  });
```

- [ ] **Step 5: smoke run (warm)**

```bash
npx playwright test tests/smoke/heatmap.spec.ts --workers=1
```

Expected: 6/6 (또는 현재 PASS count) PASS.

- [ ] **Step 6: cold-start fresh fork N=5 (소규모 spec이므로 빠름)**

```bash
for i in 1 2 3 4 5; do echo "=== Run $i ==="; npx playwright test tests/smoke/heatmap.spec.ts --workers=1 2>&1 | tail -3; done
```

Expected: 5/5 PASS.

- [ ] **Step 7: Commit**

```bash
git add tests/smoke/heatmap.spec.ts
git commit -m "fix(v3.14.5 T1): heatmap KST seed false-green fix (Codex IR-1)"
```

---

## Task 2: usage.count negative + fractional guard (spec T2)

**Files:**
- Modify: `tests/unit/state/usage-numeric-guard.spec.ts` (3 신규 it-block append)
- Modify: `src/state/usage.ts:37-39` (load 함수 numeric guard 강화)

- [ ] **Step 1: Failing tests 추가 — 기존 spec 끝에 3 it-block append**

Append (after line 37, before `});`):

```typescript
  it('negative count → 0으로 normalize (LS 편집으로 cap bypass 차단)', () => {
    const today = getDateStr();
    localStorage.setItem(
      'dg_translate_usage',
      JSON.stringify({ date: today, count: -1000 }),
    );
    expect(getTodayCount()).toBe(0);
  });

  it('fractional count → Math.floor (정수화)', () => {
    const today = getDateStr();
    localStorage.setItem(
      'dg_translate_usage',
      JSON.stringify({ date: today, count: 5.7 }),
    );
    expect(getTodayCount()).toBe(5);
  });

  it('count 0 boundary 보존 (>=0 경계값)', () => {
    const today = getDateStr();
    localStorage.setItem(
      'dg_translate_usage',
      JSON.stringify({ date: today, count: 0 }),
    );
    expect(getTodayCount()).toBe(0);
    expect(checkAndIncrement()).toBe(true);
    expect(getTodayCount()).toBe(1);
  });
```

- [ ] **Step 2: Run tests, expect FAIL**

```bash
npx vitest run tests/unit/state/usage-numeric-guard.spec.ts
```

Expected: 3 신규 case FAIL — `negative` returns -1000 instead of 0, `fractional` returns 5.7 instead of 5.

- [ ] **Step 3: Modify src/state/usage.ts:37-39 numeric guard 강화**

Before (line 35-39):
```typescript
    // v3.14.4 T1: count가 NaN/Infinity/non-number이면 silent corruption — 오늘 카운트로 reset
    // (hand-edited LS 또는 future migration JSON-bypass 대비, seen.ts firstSeenAt와 동일 패턴)
    if (typeof parsed.count !== 'number' || !Number.isFinite(parsed.count)) {
      return { date: today, count: 0 };
    }
    return parsed;
```

After:
```typescript
    // v3.14.4 T1 + v3.14.5 T2: count가 NaN/Infinity/negative이면 silent corruption — 오늘 카운트로 reset
    // (hand-edited LS 또는 future migration JSON-bypass 대비, seen.ts firstSeenAt와 동일 패턴).
    // v3.14.5: negative (cap bypass 차단) + fractional (Math.floor 정수화).
    if (typeof parsed.count !== 'number' || !Number.isFinite(parsed.count) || parsed.count < 0) {
      return { date: today, count: 0 };
    }
    return { date: parsed.date, count: Math.floor(parsed.count) };
```

- [ ] **Step 4: Run tests, expect PASS**

```bash
npx vitest run tests/unit/state/usage-numeric-guard.spec.ts
```

Expected: 6 PASS (3 기존 + 3 신규).

- [ ] **Step 5: 전체 vitest run + lint + ts (회귀 검증)**

```bash
npx tsc --noEmit && npm run lint && npx vitest run
```

Expected: 0 TS errors / 0 lint errors / vitest +3 over baseline.

- [ ] **Step 6: N=5 신규 spec 안정성**

```bash
for i in 1 2 3 4 5; do echo "=== Run $i ==="; npx vitest run tests/unit/state/usage-numeric-guard.spec.ts 2>&1 | tail -3; done
```

Expected: 5/5 PASS.

- [ ] **Step 7: Commit**

```bash
git add tests/unit/state/usage-numeric-guard.spec.ts src/state/usage.ts
git commit -m "fix(v3.14.5 T2): usage.count negative + fractional guard"
```

---

## Task 3: migration.ts progressDates + MissionInstance shape guard (spec T3)

**Files:**
- Modify: `tests/unit/state/migration-mission-numeric-guard.spec.ts` (4~6 신규 it-block append)
- Modify: `src/state/migration.ts:90-96` (sanitizeMissionInstance 강화) + `:122` (active 분기에서 shape guard 적용)

**배경**: 현재 `sanitizeMissionInstance`는 `windowStart`/`progress` numeric만 가드. v3.14.4 T2 review backlog: (a) `progressDates` array 내부 corruption (null/non-string) 미가드, (b) `MissionInstance` 자체 shape guard (missing `defId` 등) 미적용. v3.14.3 firstSeenAt graduation pattern 동일 적용.

- [ ] **Step 1: 현재 MissionInstance 타입 + sanitize 확인**

```bash
grep -n "interface MissionInstance\|type MissionInstance" src/state/missionTypes.ts
sed -n '85,100p' src/state/migration.ts
```

확인할 필드: `defId`, `period`, `windowStart`, `progress`, `completed`, `progressDates?`.

- [ ] **Step 2: Failing tests 추가 — describe 끝에 4~6 it-block append**

Append (after line 63, before `});`):

```typescript
  it('progressDates 내부 null element → string filter', () => {
    const raw = {
      ...baseUser,
      missions: {
        active: [
          { defId: 'daily-answer-1', period: 'daily', windowStart: 1700000000000, progress: 1, completed: false, progressDates: ['2026-05-01', null, '2026-05-02'] },
        ],
        cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 },
        lastDailySeed: '', currentWeekIso: '', currentMonthIso: '',
      },
    };
    const u = migrateUserToV3(raw);
    expect(u.missions.active[0]!.progressDates).toEqual(['2026-05-01', '2026-05-02']);
  });

  it('progressDates 내부 non-string element → filter', () => {
    const raw = {
      ...baseUser,
      missions: {
        active: [
          { defId: 'daily-answer-2', period: 'daily', windowStart: 1700000000000, progress: 1, completed: false, progressDates: ['2026-05-01', 42 as unknown as string, '2026-05-02'] },
        ],
        cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 },
        lastDailySeed: '', currentWeekIso: '', currentMonthIso: '',
      },
    };
    const u = migrateUserToV3(raw);
    expect(u.missions.active[0]!.progressDates).toEqual(['2026-05-01', '2026-05-02']);
  });

  it('progressDates undefined → 보존 (optional 필드)', () => {
    const raw = {
      ...baseUser,
      missions: {
        active: [
          { defId: 'daily-answer-3', period: 'daily', windowStart: 1700000000000, progress: 1, completed: false },
        ],
        cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 },
        lastDailySeed: '', currentWeekIso: '', currentMonthIso: '',
      },
    };
    const u = migrateUserToV3(raw);
    expect(u.missions.active[0]!.progressDates).toBeUndefined();
  });

  it('shape guard: missing defId → mission element 제거', () => {
    const raw = {
      ...baseUser,
      missions: {
        active: [
          { period: 'daily', windowStart: 1700000000000, progress: 0, completed: false } as unknown as object,
          { defId: 'daily-answer-4', period: 'daily', windowStart: 1700000000000, progress: 0, completed: false },
        ],
        cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 },
        lastDailySeed: '', currentWeekIso: '', currentMonthIso: '',
      },
    };
    const u = migrateUserToV3(raw);
    expect(u.missions.active).toHaveLength(1);
    expect(u.missions.active[0]!.defId).toBe('daily-answer-4');
  });

  it('shape guard: defId non-string → element 제거', () => {
    const raw = {
      ...baseUser,
      missions: {
        active: [
          { defId: 42 as unknown as string, period: 'daily', windowStart: 1700000000000, progress: 0, completed: false },
        ],
        cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 },
        lastDailySeed: '', currentWeekIso: '', currentMonthIso: '',
      },
    };
    const u = migrateUserToV3(raw);
    expect(u.missions.active).toHaveLength(0);
  });
```

- [ ] **Step 3: Run tests, expect FAIL**

```bash
npx vitest run tests/unit/state/migration-mission-numeric-guard.spec.ts
```

Expected: 5 신규 case FAIL.

- [ ] **Step 4: Modify src/state/migration.ts 가드 강화**

Replace lines 90-96 + adjust line 136 active map:

Before (line 90-96):
```typescript
function sanitizeMissionInstance(m: MissionInstance): MissionInstance {
  return {
    ...m,
    windowStart: typeof m.windowStart === 'number' && Number.isFinite(m.windowStart) ? m.windowStart : 0,
    progress: typeof m.progress === 'number' && Number.isFinite(m.progress) ? m.progress : 0,
  };
}
```

After:
```typescript
function isValidMissionInstance(m: unknown): m is MissionInstance {
  if (typeof m !== 'object' || m === null) return false;
  const r = m as Record<string, unknown>;
  return typeof r.defId === 'string';
}

function sanitizeMissionInstance(m: MissionInstance): MissionInstance {
  // v3.14.4 T2: numeric guard. v3.14.5 T3: progressDates array element guard.
  // codex 사전 P1-1: progressDates는 readonly string[] (missionTypes.ts:28) — 별도 할당 시
  // TS strict readonly 충돌. object literal 안에서 처리해야 함.
  return {
    ...m,
    windowStart: typeof m.windowStart === 'number' && Number.isFinite(m.windowStart) ? m.windowStart : 0,
    progress: typeof m.progress === 'number' && Number.isFinite(m.progress) ? m.progress : 0,
    progressDates: Array.isArray(m.progressDates)
      ? m.progressDates.filter((d): d is string => typeof d === 'string')
      : m.progressDates,
  };
}
```

Replace line 136 (active map):

Before:
```typescript
        active: (m.active as MissionInstance[]).map(sanitizeMissionInstance),
```

After:
```typescript
        // v3.14.5 T3: shape guard로 malformed element (missing/non-string defId) 차단,
        // 이후 numeric/progressDates sanitize 적용.
        active: (m.active as unknown[]).filter(isValidMissionInstance).map(sanitizeMissionInstance),
```

- [ ] **Step 5: Run tests, expect PASS**

```bash
npx vitest run tests/unit/state/migration-mission-numeric-guard.spec.ts
```

Expected: 8 PASS (3 기존 + 5 신규).

- [ ] **Step 6: TS strict + lint 회귀**

```bash
npx tsc --noEmit && npm run lint
```

Expected: 0 errors. (`progressDates?: readonly string[]`인 경우 readonly 충돌 없는지 확인 — `MissionInstance.progressDates`가 mutable string[]이면 OK; readonly이면 `Array.from(...)` 필요.)

- [ ] **Step 7: 전체 vitest run (회귀 검증, 다른 migration 관련 spec 영향 없는지)**

```bash
npx vitest run tests/unit/state/
```

Expected: state 디렉토리 전체 PASS, +5 over baseline.

- [ ] **Step 8: N=5 신규 spec 안정성**

```bash
for i in 1 2 3 4 5; do echo "=== Run $i ==="; npx vitest run tests/unit/state/migration-mission-numeric-guard.spec.ts 2>&1 | tail -3; done
```

Expected: 5/5 PASS.

- [ ] **Step 9: Commit**

```bash
git add tests/unit/state/migration-mission-numeric-guard.spec.ts src/state/migration.ts
git commit -m "fix(v3.14.5 T3): migration progressDates + MissionInstance shape guard"
```

---

## Task 4: TZ guard sweep — 8 spec / 14 sites (spec T4, T1과 중복 1 제외)

**Files (14 sites across 8 files, T1 heatmap 제외)**:
- Modify: `tests/smoke/card-translation.spec.ts` 4 sites (Pattern C — existing 2nd arg `seedUser` + eval)
- Modify: `tests/smoke/modal-overhaul.spec.ts` 1 site (Pattern A — no 2nd arg)
- Modify: `tests/smoke/v3.12-game-feedback.spec.ts` 1 site (Pattern A)
- Modify: `tests/smoke/chat-bubble-action.spec.ts` 1 site (Pattern A)
- Modify: `tests/smoke/interests-edit.spec.ts` 1 site (Pattern B — existing 2nd arg `ids`)
- Modify: `tests/smoke/disabled-button.spec.ts` 1 site (Pattern A)
- Modify: `tests/unit/modals-interests.spec.ts` 1 site (Pattern D — Node/jsdom)
- Modify: `tests/unit/settings-interests-section.spec.ts` 3 sites (Pattern D)

**Pattern 분류 (codex 사전 P1-2 generalization, v3.14.4 T3 review fix `a61d55e` precedent)**:

- **Pattern A (browser context, no existing 2nd arg)**: Node-side `getDateStr()` 호출 후 typed args로 전달.
  ```typescript
  const today = getDateStr();
  await page.addInitScript((args: { today: string }) => {
    // browser code uses args.today
  }, { today });
  ```
- **Pattern B (browser context, existing 2nd arg)**: 기존 args를 typed 객체로 묶어 today 추가.
  ```typescript
  const today = getDateStr();
  await page.addInitScript((args: { ids: string[]; today: string }) => {
    // browser code uses args.ids and args.today
  }, { ids: interests, today });
  ```
- **Pattern C (browser context, eval(seedUser) + existing args)**: seedUser 문자열과 today를 묶음.
  ```typescript
  const today = getDateStr();
  await page.addInitScript((args: { init: string; today: string }) => {
    // eslint-disable-next-line no-eval
    eval(args.init);
    // browser code uses args.today
  }, { init: seedUser, today });
  ```
- **Pattern D (Node/jsdom, unit test)**: import + 직접 substitution (no args 패턴 필요 없음).
  ```typescript
  import { getDateStr } from '../../src/utils/dates';
  // ...
  lastActiveDate: getDateStr(),
  ```

- [ ] **Step 1: dry-run grep으로 패턴 재확인 (spillover 위험 점검)**

```bash
cd /Users/hayden/Hayden_AX_Project/my_ai_assistance
grep -n "new Date().toISOString().slice(0" tests/smoke/*.spec.ts tests/unit/*.spec.ts
```

Expected: T1 처리 후 heatmap.spec.ts 0 hit, 나머지 14 hit. **다른 패턴 발견 시** (예: `Date.now() / 1000`, `.slice(0, 7)` 월 단위) → spillover task T4-2 격상.

- [ ] **Step 2: card-translation.spec.ts (Pattern C, 4 sites)**

상단 import 추가:
```typescript
import { test, expect } from '@playwright/test';
import { getDateStr } from '../../src/utils/dates';
```

각 4 site (test 함수 4개)를 동일 패턴으로 변경. 예시 (line 26-42 주변, 다른 3 site도 동일):

Before:
```typescript
await page.addInitScript((init) => {
    // eslint-disable-next-line no-eval
    eval(init);
    localStorage.setItem('dg_gemini_key', 'TEST_KEY');
    sessionStorage.setItem('dg.briefings.auto-refresh-tried', '1');
    const today = new Date().toISOString().slice(0, 10);
    localStorage.setItem('briefings', JSON.stringify([{
      id: 'card-1', date: today, ...
    }]));
  }, seedUser);
```

After:
```typescript
const today = getDateStr();
  await page.addInitScript((args: { init: string; today: string }) => {
    // eslint-disable-next-line no-eval
    eval(args.init);
    localStorage.setItem('dg_gemini_key', 'TEST_KEY');
    sessionStorage.setItem('dg.briefings.auto-refresh-tried', '1');
    localStorage.setItem('briefings', JSON.stringify([{
      id: 'card-1', date: args.today, ...
    }]));
  }, { init: seedUser, today });
```

4 sites 모두 동일하게 적용 (line ~35, 66, 84, 117 위치 — `eval(init)` 콜백 4개).

- [ ] **Step 3: modal-overhaul.spec.ts (Pattern A, 1 site)**

상단 import 추가 + `seedUserAndAnswer` helper 변경.

Before:
```typescript
import { test, expect, type Page } from '@playwright/test';

async function seedUserAndAnswer(page: Page): Promise<void> {
  await page.addInitScript(() => {
    localStorage.setItem('user', JSON.stringify({
      name: '테스트', interests: ['ai_ml'], onboardedAt: new Date().toISOString(),
      streak: 0, lastActiveDate: new Date().toISOString(), xp: 0, earnedBadges: {}, gamificationMigrated: true, schemaVersion: 2,
    }));
    localStorage.setItem('dg.answers', JSON.stringify([{
      id: 'a1', questionId: 'q1', text: '테스트 답변 내용', authorId: 'self',
      type: 'reflection', date: new Date().toISOString().slice(0, 10),
      createdAt: new Date().toISOString(), schemaVersion: 1,
    }]));
    sessionStorage.setItem('dg.briefings.auto-refresh-tried', '1');
  });
}
```

After:
```typescript
import { test, expect, type Page } from '@playwright/test';
import { getDateStr } from '../../src/utils/dates';

async function seedUserAndAnswer(page: Page): Promise<void> {
  const today = getDateStr();
  await page.addInitScript((args: { today: string }) => {
    localStorage.setItem('user', JSON.stringify({
      name: '테스트', interests: ['ai_ml'], onboardedAt: new Date().toISOString(),
      streak: 0, lastActiveDate: new Date().toISOString(), xp: 0, earnedBadges: {}, gamificationMigrated: true, schemaVersion: 2,
    }));
    localStorage.setItem('dg.answers', JSON.stringify([{
      id: 'a1', questionId: 'q1', text: '테스트 답변 내용', authorId: 'self',
      type: 'reflection', date: args.today,
      createdAt: new Date().toISOString(), schemaVersion: 1,
    }]));
    sessionStorage.setItem('dg.briefings.auto-refresh-tried', '1');
  }, { today });
}
```

- [ ] **Step 4: v3.12-game-feedback.spec.ts (Pattern A, 1 site, beforeEach)**

상단 import 추가:
```typescript
import { test, expect } from '@playwright/test';
import { getDateStr } from '../../src/utils/dates';
```

`beforeEach` 변경 (line 22-30 주변):

Before:
```typescript
test.beforeEach(async ({ page }) => {
    await page.addInitScript(() => {
      const today = new Date().toISOString().slice(0, 10);
      localStorage.setItem('user', JSON.stringify({ ... lastActiveDate: today, ... }));
      ...
    });
```

After:
```typescript
test.beforeEach(async ({ page }) => {
    const today = getDateStr();
    await page.addInitScript((args: { today: string }) => {
      localStorage.setItem('user', JSON.stringify({ ... lastActiveDate: args.today, ... }));
      ...
    }, { today });
```

- [ ] **Step 5: chat-bubble-action.spec.ts (Pattern A, 1 site)**

상단 import + `seedNoApiKey` 변경 (line 18 주변).

Before:
```typescript
import { test, expect, type Page } from '@playwright/test';
// ...
async function seedNoApiKey(page: Page): Promise<void> {
  await page.addInitScript(() => {
    const today = new Date().toISOString().slice(0, 10);
    localStorage.setItem('user', JSON.stringify({ ... lastActiveDate: today, ... }));
  });
}
```

After:
```typescript
import { test, expect, type Page } from '@playwright/test';
import { getDateStr } from '../../src/utils/dates';
// ...
async function seedNoApiKey(page: Page): Promise<void> {
  const today = getDateStr();
  await page.addInitScript((args: { today: string }) => {
    localStorage.setItem('user', JSON.stringify({ ... lastActiveDate: args.today, ... }));
  }, { today });
}
```

- [ ] **Step 6: interests-edit.spec.ts (Pattern B, 1 site, existing `ids` arg)**

상단 import + `seedUser` 변경.

Before:
```typescript
async function seedUser(page: import('@playwright/test').Page, interests: string[]): Promise<void> {
  await page.addInitScript((ids) => {
    const today = new Date().toISOString().slice(0,10);
    localStorage.setItem('user', JSON.stringify({
      name: '테', interests: ids,
      onboardedAt: new Date().toISOString(),
      streak: 0, lastActiveDate: today, ...
    }));
    sessionStorage.setItem('dg.briefings.auto-refresh-tried', '1');
  }, interests);
}
```

After:
```typescript
import { getDateStr } from '../../src/utils/dates';
// ...
async function seedUser(page: import('@playwright/test').Page, interests: string[]): Promise<void> {
  const today = getDateStr();
  await page.addInitScript((args: { ids: string[]; today: string }) => {
    localStorage.setItem('user', JSON.stringify({
      name: '테', interests: args.ids,
      onboardedAt: new Date().toISOString(),
      streak: 0, lastActiveDate: args.today, ...
    }));
    sessionStorage.setItem('dg.briefings.auto-refresh-tried', '1');
  }, { ids: interests, today });
}
```

- [ ] **Step 7: disabled-button.spec.ts (Pattern A, 1 site)**

상단 import + `seedOnboardedUser` 변경.

Before:
```typescript
async function seedOnboardedUser(page: import('@playwright/test').Page): Promise<void> {
  await page.addInitScript(() => {
    const today = new Date().toISOString().slice(0, 10);
    localStorage.setItem('user', JSON.stringify({ ... lastActiveDate: today, ... }));
    sessionStorage.setItem('dg.briefings.auto-refresh-tried', '1');
  });
}
```

After:
```typescript
import { getDateStr } from '../../src/utils/dates';
// ...
async function seedOnboardedUser(page: import('@playwright/test').Page): Promise<void> {
  const today = getDateStr();
  await page.addInitScript((args: { today: string }) => {
    localStorage.setItem('user', JSON.stringify({ ... lastActiveDate: args.today, ... }));
    sessionStorage.setItem('dg.briefings.auto-refresh-tried', '1');
  }, { today });
}
```

- [ ] **Step 8: unit modals-interests.spec.ts (Pattern D, 1 site)**

상단 import 추가:
```typescript
import { getDateStr } from '../../src/utils/dates';
```

라인 18:
Before:
```typescript
lastActiveDate: new Date().toISOString().slice(0, 10),
```

After:
```typescript
lastActiveDate: getDateStr(),
```

- [ ] **Step 9: unit settings-interests-section.spec.ts (Pattern D, 3 sites)**

상단 import 추가 + line 18, 75, 97 모두 동일 교체:
```typescript
lastActiveDate: getDateStr(),
```

- [ ] **Step 10: dry-run grep로 sweep 완료 확인**

```bash
grep -n "new Date().toISOString().slice(0" tests/smoke/*.spec.ts tests/unit/*.spec.ts
```

Expected: 0 hit (15 sites 모두 sweep).

- [ ] **Step 11: TS strict + lint 회귀**

```bash
npx tsc --noEmit && npm run lint
```

Expected: 0 errors.

- [ ] **Step 12: Unit specs run (modals-interests + settings-interests-section)**

```bash
npx vitest run tests/unit/modals-interests.spec.ts tests/unit/settings-interests-section.spec.ts
```

Expected: PASS (변경 0건 의미적, 의존만 변경).

- [ ] **Step 13: 전체 vitest run (회귀)**

```bash
npx vitest run
```

Expected: all PASS, count = T3 직후와 동일 (0 신규 it-block).

- [ ] **Step 14: smoke run (warm)**

```bash
npm run test:smoke
```

Expected: 93/93 PASS.

- [ ] **Step 15: smoke cold-start fresh fork N=5 (전체)**

```bash
for i in 1 2 3 4 5; do echo "=== Run $i ==="; npm run test:smoke 2>&1 | tail -3; done
```

Expected: 5/5 모두 93/93 PASS. 만약 cold-2 cascade 일부가 PASS로 전환되면 §6 backlog status update에 기록 (T8 retro).

- [ ] **Step 16: Commit**

```bash
git add tests/smoke/card-translation.spec.ts tests/smoke/modal-overhaul.spec.ts tests/smoke/v3.12-game-feedback.spec.ts tests/smoke/chat-bubble-action.spec.ts tests/smoke/interests-edit.spec.ts tests/smoke/disabled-button.spec.ts tests/unit/modals-interests.spec.ts tests/unit/settings-interests-section.spec.ts
git commit -m "test(v3.14.5 T4): TZ guard sweep — 8 spec / 14 sites UTC ISO → getDateStr"
```

---

## Task 5: Bundle 측정 단일 표준 declaration (spec T5)

**Files:**
- Verify: `docs/superpowers/specs/2026-05-03-v3.14.5-p2-cleanup-design.md` (§3.3 + §6 이미 작성됨)
- Modify: 선택적 — `CLAUDE.md` (project root)에 1줄 컨벤션 추가

**배경**: spec §3.3에서 이미 "본 사이클부터 CLI gzip을 single-record canonical로 채택, vite reporter dual-record 폐기"를 declaration. T5 task는 이 declaration이 향후 사이클에 strict하게 적용되도록 visibility 보장.

- [ ] **Step 1: Spec §3.3 declaration 재확인**

```bash
grep -A 5 "canonical 선언" docs/superpowers/specs/2026-05-03-v3.14.5-p2-cleanup-design.md
```

Expected: "canonical 선언: 본 사이클부터 CLI gzip을 v3.14.5 retro single-record" 문구 확인.

- [ ] **Step 2: CLAUDE.md "Cycle Workflow" 섹션에 1줄 추가 (선택적)**

If CLAUDE.md `## Cycle Workflow` 섹션에 bundle 측정 표준이 없으면:

```markdown
## Cycle Workflow (v3.14.4 graduation)
... 기존 내용 ...

**Bundle 측정 표준 (v3.14.5 graduation)**: 사이클 retro에 bundle 절대값 기록 시 `gzip -c dist/assets/index-*.js | wc -c` 단일 stream을 canonical로 기록한다. vite reporter chunked estimation은 dual-record 폐기 (v3.14.4가 마지막 사례).
```

If 이미 충분 (spec §3.3 declaration만으로) → step 3로 skip하고 T5 무코드 task로 마침.

- [ ] **Step 3: 변경 있을 시 commit, 없으면 T5는 spec §3.3 declaration으로 충족**

If CLAUDE.md 수정:
```bash
git add CLAUDE.md
git commit -m "docs(v3.14.5 T5): bundle 측정 단일 표준 graduation (CLI gzip canonical)"
```

If 무코드: T8 retro에서 "T5 = spec §3.3 declaration으로 충족, 별도 commit 없음" 명시.

---

## Task 7: Codex 최종 review (spec T7, deploy gate)

**Files:**
- External dispatch (codex:rescue agent)
- Output: spec §8.2 retro에 결과 기록 (T8에서 처리)

- [ ] **Step 1: 모든 main work commit (T1~T5) 완료 검증**

```bash
git log --oneline | head -10
git status
```

Expected: T1, T2, T3, T4, (T5 if commit) 모두 main에 commit. Working tree clean.

- [ ] **Step 2: 사이클-끝 3종 검증 (spec §3.5)**

```bash
npm run lint && npm test && npm run test:smoke
```

Expected: lint 0 / vitest +6~9 over 711 / smoke 93/93.

- [ ] **Step 3: build + bundle 측정**

```bash
npm run build 2>&1 | tail -5
gzip -c dist/assets/index-*.js | wc -c | awk '{printf "%.2f kB\n", $1/1024}'
```

Expected: Δ ±0.10 kB vs 31.28 baseline.

- [ ] **Step 4: Codex 최종 agent dispatch**

Prompt:
```
v3.14.5 P2 Cleanup 최종 review (deploy gate).

Spec: /Users/hayden/Hayden_AX_Project/my_ai_assistance/docs/superpowers/specs/2026-05-03-v3.14.5-p2-cleanup-design.md
Plan: /Users/hayden/Hayden_AX_Project/my_ai_assistance/docs/superpowers/plans/2026-05-03-v3.14.5-p2-cleanup-plan.md

git log --oneline (T1~T5 commits) 확인 후, 다음 영역 review:

1. T1: tests/smoke/heatmap.spec.ts — KST seed fix가 v3.4 cardnews / v3.14.4 T3 review fix precedent와 1:1 매칭? data-date assertion이 false-green 차단?
2. T2: src/state/usage.ts + tests/unit/state/usage-numeric-guard.spec.ts — negative + fractional guard 정합성, edge case (Math.floor 음수 처리는 차단됨)?
3. T3: src/state/migration.ts + tests/unit/state/migration-mission-numeric-guard.spec.ts — sanitizeMissionInstance + isValidMissionInstance가 invariant C6 (caller 객체 사용) 보존? readonly 충돌?
4. T4: 9 spec / 15 sites mechanical sweep 완전성 — 패턴 잔존 0?
5. T5: bundle CLI gzip canonical declaration이 v3.15+ baseline에 명확히 영향?
6. 사이클-끝 검증 (lint/vitest/smoke) PASS 확인.
7. 신규 dormant 위험 (regression / silent corruption)?

분류: DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT.
P0/P1/P2로 항목 분류.
```

Expected: Codex 출력 (DEPLOY_APPROVED 또는 APPROVED_WITH_NOTES + P0=N P1=N P2=N).

- [ ] **Step 5: P0 발견 시 in-cycle 반영**

If P0:
- 해당 task 수정 → 새 commit (T7-fix-N)
- Codex 재dispatch (Step 4 반복)

If P1: 사용자 합의 후 in-cycle 반영 또는 backlog carry-forward.
If P2: §8.4 retro carry-forward.
If REJECT: 사용자에게 보고, 사이클 hold.

- [ ] **Step 6: 권한 issue fallback (v3.14.2 패턴)**

If Codex 권한 issue:
- 사용자에게 보고
- self-review로 사이클-끝 3종 검증 + bundle 측정으로 임시 게이트 통과
- spec §9에 "Codex 최종 권한 issue → cycle wrap-up 시점 재시도" 명시
- T8 retro에 "Codex 최종 미실행 → carry-forward" 기록

---

## (배포 게이트 — 사용자 명시 대기)

T7 = DEPLOY_APPROVED 또는 APPROVED_WITH_NOTES (P0=0) 후 사용자 "배포해줘" 명시 시점에:

```bash
npm run build
firebase deploy --only hosting
```

main 직배포 패턴 (origin remote 미등록).

---

## Task 8: 사이클 retro (spec T8)

**Files:**
- Modify: `docs/superpowers/specs/2026-05-03-v3.14.5-p2-cleanup-design.md` (§8 retro 채움 + §9 변경 이력)

- [ ] **Step 1: §8.0 핵심 결과 작성**

Tasks: T0~T9 (실제 commit 수)
vitest: baseline → 최종 count (Δ +N) — task별 분담
Playwright: 결과 + cold-start fresh fork 시간
Bundle: CLI gzip 절대값 (canonical, single-record)
TS / Lint: 0 / 0
Hotfix slot 활용 여부 (T0 hotfix 발생 시 기록)

- [ ] **Step 2: §8.1 vitest count drift 작성**

Baseline 711 → T0 실측 → T2 (+3) → T3 (+5) → 최종 += N

- [ ] **Step 3: §8.2 Codex review 결과 작성**

| 시점 | 결과 | 분류 | 처리 |
|---|---|---|---|
| 사전 (T6) | ... | P0=N/P1=N/P2=N | in-cycle / carry-forward |
| 최종 (T7) | DEPLOY_APPROVED 또는 ... | P0=N/P1=N/P2=N | ... |

- [ ] **Step 4: §8.3 신규 lessons (있을 시)**

본 사이클 중 발견된 패턴/실수 1~3개 기록.

- [ ] **Step 5: §8.4 v3.14.5+ Backlog (carry-forward)**

기존 §6 + 본 사이클 신규 발견:
- T0 측정 결과 cold-2 cascade 일관 등장 / 비등장
- Codex 최종 P2 (있을 시)
- 격하 P3/P4 (있을 시)

- [ ] **Step 6: §8.5 차기 사이클 후보**

- v3.15 Game World — 트리거: `"v3.15 Game World brainstorm"`
- v3.14.5 hotfix (필요 시)

- [ ] **Step 7: §9 변경 이력 row 추가**

```
| 2026-05-XX retro | T8 §8 채움 | 사이클 종료 |
```

- [ ] **Step 8: Commit**

```bash
git add docs/superpowers/specs/2026-05-03-v3.14.5-p2-cleanup-design.md
git commit -m "docs(v3.14.5 T8): 사이클 retro 작성"
```

---

## Task 9: Memory 갱신 (spec T9)

**Files:**
- Create: `/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v3145.md`
- Modify: `/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md` (포인터 1줄 추가)

- [ ] **Step 1: project_daily_growth_v3145.md 작성**

Frontmatter + 요약 + lessons + carry-forward (v3.14.4 패턴 동일).

```markdown
---
name: DG v3.14.5 P2 Cleanup
description: v3.14.4 carry-forward P2 5건 + TZ guard sweep 9 spec 정리. v3.14 시리즈 종료. 2026-05-XX Codex DEPLOY_APPROVED 후 배포.
type: project
originSessionId: brainstorming + writing-plans cycle 2026-05-03
---

**✅ 배포 완료 (2026-05-XX, main HEAD `<hash>`, Firebase live)** — 10 task / N commits · vitest 711+M=N pass + 93/93 Playwright cold-start fresh fork (Xs) · per-task review N+ + Codex 사전 (P0=0/P1=N/P2=N → in-cycle) + Codex 최종 (DEPLOY_APPROVED, P0=0/P1=N/P2=N).

핵심 변경:

- T1 IR-1 heatmap KST seed false-green fix (Codex IR-1 carry-forward)
- T2 usage.count negative + fractional guard 강화 (`>= 0` + `Math.floor`)
- T3 migration.ts progressDates filter + MissionInstance shape guard (isValidMissionInstance)
- T4 TZ guard sweep — 8 spec / 14 sites UTC ISO → getDateStr() (smoke 6 + unit 2)
- T5 bundle 측정 단일 표준 graduation (CLI gzip canonical)

Bundle (CLI gzip): 31.28 → X.XX kB (Δ +0.0X, DoD ±0.10 ✅).

## v3.15+ Backlog (carry-forward)

- (T8 retro에서 채움)

## 차기 사이클 후보

- **v3.15 Game World** (게임화 시리즈 3/3, 캐릭터/정원). 트리거: `"v3.15 Game World brainstorm"`.
```

- [ ] **Step 2: MEMORY.md 포인터 추가**

`/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md` 끝에 1줄 추가:

```markdown
- [DG v3.14.5 P2 Cleanup](project_daily_growth_v3145.md) — **✅ 배포 완료 (2026-05-XX, main HEAD `<hash>`, Firebase live)** — 10 task · IR-1 heatmap KST seed + usage.count negative/fractional + migration progressDates/shape + TZ sweep 9 spec/15 sites + bundle CLI gzip canonical graduation. v3.14 시리즈 종료.
```

- [ ] **Step 3: Memory 파일은 git tracking 외부 (commit 없음)**

Memory 파일은 `~/.claude/projects/...` 경로로 git repo 외부. 별도 commit 없음.

---

## Self-Review (write-plans 스킬 체크리스트)

**1. Spec coverage**:
- T0 → Task 0 ✓
- T1 → Task 1 ✓
- T2 → Task 2 ✓
- T3 → Task 3 ✓
- T4 → Task 4 ✓
- T5 → Task 5 ✓
- T6 → Task 6 ✓
- T7 → Task 7 ✓
- T8 → Task 8 ✓
- T9 → Task 9 ✓

**2. Placeholder scan**: §Baseline의 TBD는 T0 step 5에서 채움 (의도). 그 외 TODO/TBD 0.

**3. Type consistency**:
- `getDateStr()` import 경로: smoke spec → `'../../src/utils/dates'`, unit spec → `'../../src/utils/dates'` (동일 depth)
- `isValidMissionInstance` Task 3에서 정의, 같은 파일 내 사용 ✓
- `sanitizeMissionInstance` 시그니처 유지 ✓

수정 0건. Plan ready.

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-05-03-v3.14.5-p2-cleanup-plan.md`.

**Two execution options**:

1. **Subagent-Driven (recommended)** — fresh subagent per task, review between tasks
2. **Inline Execution** — execute in this session with checkpoints

어느 쪽으로 진행할까요?
