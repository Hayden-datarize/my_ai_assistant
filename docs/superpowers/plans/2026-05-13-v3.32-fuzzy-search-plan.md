# v3.32 Fuzzy Search + Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** archive 검색을 단일 substring에서 다중 토큰 AND + 한글 초성 매칭으로 확장하고, v3.30 carry polish 3건 (mount idempotence / scroll-margin strict / deferred-loader helper)을 일괄 청산한다.

**Architecture:** `src/utils/fuzzy.ts` 신설 (tokenize + 초성 + matchesAllTokens) → `src/utils/highlight.ts` signature clean break (`tokens: string[]`) → `src/ui/handlers/archive.ts` 4 filter spot + 2 highlight caller 교체. Polish는 기존 guard/spec 강화 + helper 추출.

**Tech Stack:** TypeScript / Vitest / Playwright / Vite. 외부 라이브러리 추가 없음.

**Baseline:** `97af348` (v3.31 retro commit). bundle CLI gzip = `19078 B`, vitest = `1279 PASS`, smoke = `100 PASS`.

---

## File Structure

**Create:**
- `src/utils/fuzzy.ts` — tokenize / 초성 / matchesAllTokens helper
- `tests/unit/fuzzy.spec.ts` — fuzzy helper 단위 spec
- `tests/unit/archive-mount-idempotence.spec.ts` — T4 polish spec
- `tests/helpers/deferred-loader.ts` — T6 polish helper (nav.spec.ts에서 추출)

**Modify:**
- `src/utils/highlight.ts` — signature `highlightHtml(text, tokens: string[])`
- `tests/utils/highlight.spec.ts` — 모든 케이스 tokens 배열 형태 마이그레이션 + 신규 케이스 (alternation longest-first / 초성-only skip)
- `src/ui/handlers/archive.ts` — `currentTokens` 도입, 4 filter spot + 2 highlight caller 교체
- `tests/ui/archive-search.spec.ts` — 신규 case (다중 토큰 AND / 초성 / 혼합)
- `tests/unit/scroll-margin-coverage.spec.ts` — T5 polish (per-file assertion 강화)
- `tests/unit/nav.spec.ts` — T6 polish (`createDeferredLoader`를 `tests/helpers/deferred-loader.ts`에서 import)

---

## Task 0: Baseline 확인 (T0 후속, plan 직후)

**Files:** N/A (verification only)

- [ ] **Step 0.1: working tree 깨끗한지 확인**

Run: `git status --short`
Expected: spec/plan commit 외 비변경 (혹은 신규 untracked는 있어도 무방)

- [ ] **Step 0.2: vitest baseline (KST + NY 양쪽)**

Run:
```bash
npm test -- --run 2>&1 | tail -5
TZ=America/New_York npm test -- --run 2>&1 | tail -5
```
Expected: 양쪽 모두 `1279 passed` 또는 그 근접 (v3.31 retro 기준).

- [ ] **Step 0.3: smoke baseline**

Run: `npm run test:smoke 2>&1 | tail -5`
Expected: `100 passed`.

- [ ] **Step 0.4: bundle baseline 측정**

Run:
```bash
npm run build 2>&1 | tail -5
gzip -c dist/assets/index-*.js | wc -c
```
Expected: `19078` 근접 (v3.31 retro 일치 확인).

- [ ] **Step 0.5: caller 검증**

Run:
```bash
grep -rn "from.*highlight\|import.*highlight" src/ tests/
```
Expected: `src/ui/handlers/archive.ts` + `tests/utils/highlight.spec.ts` 만 (welcome-garden은 local var, 무관).

- [ ] **Step 0.6: Codex 사전 review dispatch**

Run:
```bash
codex exec --skip-git-repo-check -o /tmp/v3.32-codex-pre.txt "다음 spec/plan을 review하고 P0/P1/P2로 분류해줘. spec: $(cat docs/superpowers/specs/2026-05-13-v3.32-fuzzy-search-design.md). plan: $(cat docs/superpowers/plans/2026-05-13-v3.32-fuzzy-search-plan.md)."
```

실패 시 (v3.31 sandbox permission carry):
- controller fallback: `pr-review-toolkit:code-reviewer` agent로 동일 spec/plan dispatch
- 결과: P0/P1는 plan v2로 in-cycle 흡수 후 T1 진입. P2는 retro carry 또는 in-cycle 결정.

---

## Task 1: `src/utils/fuzzy.ts` 신설 + spec

**Files:**
- Create: `src/utils/fuzzy.ts`
- Test: `tests/unit/fuzzy.spec.ts`

- [ ] **Step 1: spec red 작성**

`tests/unit/fuzzy.spec.ts`:

```ts
import { describe, it, expect } from 'vitest';
import {
  tokenizeQuery,
  getInitialConsonants,
  isInitialOnlyToken,
  matchesAllTokens,
} from '../../src/utils/fuzzy';

describe('tokenizeQuery (v3.32 T1)', () => {
  it('빈 입력 → 빈 배열', () => {
    expect(tokenizeQuery('')).toEqual([]);
    expect(tokenizeQuery('   ')).toEqual([]);
  });

  it('단일 토큰 — NFC + lowercase + trim', () => {
    expect(tokenizeQuery('  Hello  ')).toEqual(['hello']);
  });

  it('다중 토큰 — whitespace split', () => {
    expect(tokenizeQuery('리액트 훅')).toEqual(['리액트', '훅']);
    expect(tokenizeQuery('a  b\tc\nd')).toEqual(['a', 'b', 'c', 'd']);
  });

  it('NFC 먼저 → 한글 NFD 입력도 정상 처리', () => {
    const nfd = '한국'.normalize('NFD');
    expect(tokenizeQuery(nfd)).toEqual(['한국']);
  });

  it('영문 + 한글 혼합', () => {
    expect(tokenizeQuery('React 훅')).toEqual(['react', '훅']);
  });
});

describe('getInitialConsonants (v3.32 T1)', () => {
  it('한글 음절 → 초성 19자 자음 매핑', () => {
    expect(getInitialConsonants('프로젝트')).toBe('ㅍㄹㅈㅌ');
  });

  it('영문/숫자는 그대로 통과', () => {
    expect(getInitialConsonants('React 123')).toBe('React 123');
  });

  it('빈 문자열 → 빈 문자열', () => {
    expect(getInitialConsonants('')).toBe('');
  });

  it('한글 + 영문 혼합', () => {
    expect(getInitialConsonants('React 훅')).toBe('React ㅎ');
  });

  it('19자 자음 테이블 — 경계값', () => {
    expect(getInitialConsonants('각')).toBe('ㄱ'); // 첫 음절 AC00
    expect(getInitialConsonants('힣')).toBe('ㅎ'); // 마지막 음절 D7A3
    expect(getInitialConsonants('까')).toBe('ㄲ'); // 쌍자음
  });
});

describe('isInitialOnlyToken (v3.32 T1)', () => {
  it('한글 자음만 구성 → true', () => {
    expect(isInitialOnlyToken('ㅍㄹ')).toBe(true);
    expect(isInitialOnlyToken('ㄱ')).toBe(true);
  });

  it('한글 음절 포함 → false', () => {
    expect(isInitialOnlyToken('프로')).toBe(false);
  });

  it('영문 포함 → false', () => {
    expect(isInitialOnlyToken('ㅋproject')).toBe(false);
  });

  it('빈 문자열 → false (regex `+` requires 1자)', () => {
    expect(isInitialOnlyToken('')).toBe(false);
  });
});

describe('matchesAllTokens (v3.32 T1)', () => {
  it('빈 토큰 배열 → vacuous true (전체 통과)', () => {
    expect(matchesAllTokens('anything', [])).toBe(true);
  });

  it('단일 substring 토큰 — case-insensitive', () => {
    expect(matchesAllTokens('Hello World', ['hello'])).toBe(true);
    expect(matchesAllTokens('Hello', ['xyz'])).toBe(false);
  });

  it('다중 토큰 AND — 모두 hit해야 true', () => {
    expect(matchesAllTokens('리액트 훅 사용법', ['리액트', '훅'])).toBe(true);
    expect(matchesAllTokens('리액트 컴포넌트', ['리액트', '훅'])).toBe(false);
  });

  it('초성-only 토큰 — 한글 텍스트 초성 매칭', () => {
    expect(matchesAllTokens('프로젝트', ['ㅍㄹ'])).toBe(true);
    expect(matchesAllTokens('프로젝트', ['ㅈㅌ'])).toBe(true); // contiguous
    expect(matchesAllTokens('프로젝트', ['ㅍㅈ'])).toBe(false); // non-contiguous
  });

  it('초성 + substring 혼합 AND', () => {
    expect(matchesAllTokens('리액트 프로젝트', ['ㅍㄹ', 'react'])).toBe(false); // react 없음
    expect(matchesAllTokens('react 프로젝트', ['ㅍㄹ', 'react'])).toBe(true);
  });

  it('NFC 정규화 — text NFD 입력도 매칭', () => {
    const textNfd = '한국'.normalize('NFD');
    expect(matchesAllTokens(textNfd, ['한국'])).toBe(true);
  });
});
```

- [ ] **Step 2: spec red 확인**

Run: `npm test -- --run fuzzy.spec`
Expected: FAIL with "Cannot find module '../../src/utils/fuzzy'" 또는 동등.

- [ ] **Step 3: `src/utils/fuzzy.ts` 구현**

```ts
/**
 * v3.32 T1: archive 검색용 fuzzy helper.
 *
 * - 다중 토큰 AND (whitespace split)
 * - 한글 초성 매칭 (음절 → 19자 자음 compatibility jamo)
 * - 외부 라이브러리 미사용, bundle 친화 단순 ASCII regex.
 *
 * 정규화 순서: NFC → toLowerCase → trim → split (NFC 먼저 — NFD 입력 lowercase 회피).
 */

const HANGUL_INITIAL_CONSONANTS = 'ㄱㄲㄴㄷㄸㄹㅁㅂㅃㅅㅆㅇㅈㅉㅊㅋㅌㅍㅎ';
const HANGUL_SYLLABLE_START = 0xac00;
const HANGUL_SYLLABLE_END = 0xd7a3;
const INITIAL_ONLY_RE = /^[ㄱ-ㅎ]+$/;

export function tokenizeQuery(raw: string): string[] {
  return raw
    .normalize('NFC')
    .toLowerCase()
    .trim()
    .split(/\s+/)
    .filter((t) => t.length > 0);
}

export function getInitialConsonants(s: string): string {
  let out = '';
  for (const ch of s) {
    const code = ch.codePointAt(0)!;
    if (code >= HANGUL_SYLLABLE_START && code <= HANGUL_SYLLABLE_END) {
      const idx = Math.floor((code - HANGUL_SYLLABLE_START) / 588);
      out += HANGUL_INITIAL_CONSONANTS[idx];
    } else {
      out += ch;
    }
  }
  return out;
}

export function isInitialOnlyToken(t: string): boolean {
  return INITIAL_ONLY_RE.test(t);
}

export function matchesAllTokens(text: string, tokens: string[]): boolean {
  if (tokens.length === 0) return true;
  const normalized = text.normalize('NFC').toLowerCase();
  const initials = getInitialConsonants(normalized);
  return tokens.every((token) =>
    isInitialOnlyToken(token) ? initials.includes(token) : normalized.includes(token),
  );
}
```

- [ ] **Step 4: spec green 확인**

Run: `npm test -- --run fuzzy.spec`
Expected: 모든 케이스 PASS.

- [ ] **Step 5: 양쪽 TZ 확인**

Run: `TZ=America/New_York npm test -- --run fuzzy.spec`
Expected: KST와 동일 PASS (TZ 무관).

- [ ] **Step 6: per-task reviewer dispatch**

Use Agent tool: `subagent_type: pr-review-toolkit:code-reviewer`. Focus: `src/utils/fuzzy.ts` + `tests/unit/fuzzy.spec.ts` (T1 신규 코드).

P0/P1 발견 시 in-cycle fix, P2는 retro carry.

- [ ] **Step 7: Commit T1**

```bash
git add src/utils/fuzzy.ts tests/unit/fuzzy.spec.ts
git commit -m "feat(v3.32 T1): src/utils/fuzzy.ts — tokenizeQuery + 초성 + matchesAllTokens

다중 토큰 AND + 한글 초성 매칭 helper 신설. NFC → lowercase → trim → split 정규화.
19자 compatibility jamo 자음 테이블로 음절→초성 변환. matchesAllTokens는
초성-only 토큰은 initials.includes, 아니면 substring includes. 빈 배열 vacuous true.

신규 spec 22 case (양쪽 TZ).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: `src/utils/highlight.ts` signature clean break + spec migration

**Files:**
- Modify: `src/utils/highlight.ts`
- Modify: `tests/utils/highlight.spec.ts`

- [ ] **Step 1: highlight.spec.ts red 마이그 (tokens 배열 + 신규 케이스)**

`tests/utils/highlight.spec.ts` 전체 재작성:

```ts
import { describe, it, expect } from 'vitest';
import { highlightHtml } from '../../src/utils/highlight';

describe('highlightHtml (v3.32 T2 — tokens 배열 signature)', () => {
  it('빈 배열 → 원문 escape만, mark 없음', () => {
    expect(highlightHtml('Hello <b>X</b>', [])).toBe('Hello &lt;b&gt;X&lt;/b&gt;');
  });

  it('match 없으면 원문 escape', () => {
    expect(highlightHtml('Hello world', ['xyz'])).toBe('Hello world');
  });

  it('단일 토큰 match — <mark> 감싸기 + 외부 escape 유지', () => {
    expect(highlightHtml('Hello world', ['world'])).toBe('Hello <mark>world</mark>');
  });

  it('case-insensitive match (NFC 후 lower)', () => {
    expect(highlightHtml('Hello World', ['world'])).toBe('Hello <mark>World</mark>');
  });

  it('multi-occurrence — 모두 wrap', () => {
    expect(highlightHtml('aba aba', ['a'])).toBe(
      '<mark>a</mark>b<mark>a</mark> <mark>a</mark>b<mark>a</mark>',
    );
  });

  it('XSS: text 안에 <script> → escape', () => {
    expect(highlightHtml('<script>alert(1)</script>', ['alert'])).toBe(
      '&lt;script&gt;<mark>alert</mark>(1)&lt;/script&gt;',
    );
  });

  it('XSS: token 안에 regex special → 리터럴 매치', () => {
    expect(highlightHtml('a.b.c', ['.'])).toBe('a<mark>.</mark>b<mark>.</mark>c');
  });

  it('XSS: token 안에 < > → escape + literal regex', () => {
    expect(highlightHtml('a<b>c', ['<b>'])).toBe('a<mark>&lt;b&gt;</mark>c');
  });

  it('XSS: text + token 모두에 <img onerror> → script 실행 0', () => {
    const out = highlightHtml('<img onerror="x">', ['img']);
    expect(out).not.toContain('<img');
    expect(out).not.toContain('"x"');
    expect(out).toContain('&lt;');
    expect(out).toContain('&quot;');
    expect(out).toContain('<mark>img</mark>');
  });

  it('NFC normalize — 합성된/풀어쓴 한글 동일 매치', () => {
    const composed = '한국'.normalize('NFC');
    const decomposedToken = '한국'.normalize('NFD');
    expect(highlightHtml(composed, [decomposedToken])).toContain('<mark>');
  });

  // v3.32 신규
  it('다중 substring 토큰 — alternation 단일 sweep 모두 wrap', () => {
    expect(highlightHtml('리액트 훅 사용', ['리액트', '훅'])).toBe(
      '<mark>리액트</mark> <mark>훅</mark> 사용',
    );
  });

  it('다중 토큰 longest-first sort — overlap에서 긴 토큰 우선', () => {
    // 토큰 ["프", "프로"] → 길이 desc로 "프로" 먼저 매칭
    expect(highlightHtml('프로젝트', ['프', '프로'])).toBe('<mark>프로</mark>젝트');
  });

  it('초성-only 토큰 — mark 없음 (filter pass-only)', () => {
    expect(highlightHtml('프로젝트', ['ㅍㄹ'])).toBe('프로젝트');
  });

  it('초성 + substring 혼합 — substring 토큰만 mark', () => {
    expect(highlightHtml('react 프로젝트', ['ㅍㄹ', 'react'])).toBe(
      '<mark>react</mark> 프로젝트',
    );
  });
});
```

- [ ] **Step 2: spec red 확인**

Run: `npm test -- --run highlight.spec`
Expected: FAIL — 기존 signature와 호출 불일치 (`highlightHtml(text, query: string)` → `[]`).

- [ ] **Step 3: `src/utils/highlight.ts` 신규 구현**

`src/utils/highlight.ts` 전체 재작성:

```ts
/**
 * v3.32 T2: archive 검색 keyword `<mark>` highlight helper (tokens 배열 signature).
 *
 * 보안 invariant (v3.29 P1-2 유지):
 * - text는 escapeHtml로 모두 sanitize
 * - 각 substring 토큰은 escapeRegex로 리터럴 매칭 (regex injection 차단)
 * - `<mark>` span만 raw HTML — 외부 입력 전체가 escape 후 mark만 inject
 * - NFC normalize → case-insensitive
 *
 * 토큰 정책:
 * - 초성-only 토큰 (`/^[ㄱ-ㅎ]+$/`)은 highlight skip (filter pass-only)
 * - substring 토큰은 길이 desc sort → alternation 단일 sweep regex (overlap 시 긴 토큰 우선)
 * - 빈 배열 또는 substring 토큰 0개 → escapeHtml(text)만 반환
 */

import { escapeHtml } from './escapeHtml';
import { isInitialOnlyToken } from './fuzzy';

function escapeRegex(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

export function highlightHtml(text: string, tokens: string[]): string {
  const substringTokens = tokens.filter((t) => t.length > 0 && !isInitialOnlyToken(t));
  if (substringTokens.length === 0) return escapeHtml(text);

  // 길이 desc sort — alternation에서 긴 토큰이 먼저 매칭
  const sorted = [...substringTokens].sort((a, b) => b.length - a.length);
  const pattern = sorted.map((t) => escapeRegex(t.normalize('NFC'))).join('|');
  const re = new RegExp(pattern, 'gi');

  const t = text.normalize('NFC');
  if (!re.test(t)) return escapeHtml(text);

  re.lastIndex = 0;
  let out = '';
  let lastIdx = 0;
  let m: RegExpExecArray | null;
  while ((m = re.exec(t)) !== null) {
    out += escapeHtml(t.slice(lastIdx, m.index));
    out += `<mark>${escapeHtml(m[0])}</mark>`;
    lastIdx = m.index + m[0].length;
    if (m[0].length === 0) re.lastIndex++;
  }
  out += escapeHtml(t.slice(lastIdx));
  return out;
}
```

- [ ] **Step 4: spec green 확인**

Run: `npm test -- --run highlight.spec`
Expected: 모든 케이스 PASS.

- [ ] **Step 5: TS strict 확인**

Run: `npx tsc --noEmit`
Expected: 0 errors (archive.ts에서 `currentQuery` string을 highlightHtml에 넘기는 호출은 T3에서 마이그 — 이 시점에서는 컴파일 fail 가능. Step 6에서 처리)

- [ ] **Step 6: archive.ts 호출 사이트 임시 어댑테이션**

이 task는 아직 archive.ts integration을 안 하지만, TS strict이 깨지므로 T2 commit 직전 archive.ts의 2 호출 사이트 ([currentQuery] 배열 wrap)만 임시 변환:

`src/ui/handlers/archive.ts` 2개 위치 (현재 line 582, 614):
```ts
// 기존
body.innerHTML = highlightHtml(a.text, currentQuery);
// → T2 임시 어댑테이션 (T3에서 currentTokens로 교체)
body.innerHTML = highlightHtml(a.text, currentQuery ? [currentQuery] : []);
```

및

```ts
body.innerHTML = highlightHtml(i.text, currentQuery ? [currentQuery] : []);
```

- [ ] **Step 7: TS + 전체 spec 확인**

Run:
```bash
npx tsc --noEmit
npm test -- --run
```
Expected: TS 0 errors, vitest baseline 1279 + 신규 fuzzy 22 + 신규 highlight 추가 case = ~1305 PASS.

- [ ] **Step 8: per-task reviewer dispatch**

Use Agent tool `pr-review-toolkit:code-reviewer`. Focus: `src/utils/highlight.ts` 변경 + 임시 어댑테이션. 보안 invariant (escapeHtml + escapeRegex + mark only) 회귀 X 확인.

- [ ] **Step 9: Commit T2**

```bash
git add src/utils/highlight.ts tests/utils/highlight.spec.ts src/ui/handlers/archive.ts
git commit -m "refactor(v3.32 T2): highlightHtml signature → tokens 배열 (clean break)

highlightHtml(text, query: string) → highlightHtml(text, tokens: string[]).
- 초성-only 토큰은 skip (filter pass-only)
- substring 토큰은 길이 desc sort 후 alternation 단일 sweep regex
- 보안 invariant 유지 (escapeHtml + escapeRegex + mark only inject)

기존 highlight.spec.ts 11 case → tokens 형태 마이그 + 신규 4 case
(다중 토큰 alternation / longest-first sort / 초성-only skip / 초성+substring 혼합).

archive.ts 2 호출 사이트는 임시 [currentQuery] 래핑 — T3에서 currentTokens로 교체.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: `archive.ts` integration — `currentTokens` + 4 filter spot + 2 highlight caller

**Files:**
- Modify: `src/ui/handlers/archive.ts`
- Modify: `tests/ui/archive-search.spec.ts`

- [ ] **Step 1: 신규 spec red 작성**

`tests/ui/archive-search.spec.ts` 끝에 신규 describe 블록 추가 (기존 case 유지):

```ts
describe('archive 검색 — 다중 토큰 + 초성 (v3.32 T3)', () => {
  beforeEach(() => {
    // 기존 spec의 fixture 셋업 패턴 따름 (T3 구현 시점에 file 시작부 fixture 확인 후 정합 맞춤)
  });

  it('다중 토큰 AND — 두 단어 모두 포함 항목만 표시', async () => {
    // fixture: '리액트 훅 사용법' / '리액트 컴포넌트' / '훅 정리'
    // 검색 "리액트 훅" → '리액트 훅 사용법'만 표시
    // (구현 시점에 실제 fixture와 정합 맞춰 정확 assertion 작성)
  });

  it('초성 only — 한글 텍스트 매칭 (mark 없음)', async () => {
    // fixture: '프로젝트 정리'
    // 검색 "ㅍㄹ" → 매칭, <mark> 없음
  });

  it('초성 + substring 혼합 — substring 토큰만 mark', async () => {
    // fixture: 'react 프로젝트'
    // 검색 "ㅍㄹ react" → 매칭, react만 <mark>
  });

  it('빈 토큰만 (공백 다수) — 전체 표시 (UX 회귀 차단)', async () => {
    // 검색 "   " → tokenizeQuery [] → 전체 표시
  });

  it('영문 substring — 기존 회귀 없음', async () => {
    // 검색 "react" → 기존과 동일 매칭
  });
});
```

> **구현 시 주의**: 실제 fixture 셋업은 `tests/ui/archive-search.spec.ts` 첫 부분 beforeEach 패턴 (load fixture, mount handlers, render)을 그대로 재사용해야 함. 구현 시점에 file 읽고 정확한 assertion 작성.

- [ ] **Step 2: spec red 확인 (구현 진행 시 hit율 변화 확인)**

Run: `npm test -- --run archive-search.spec`
Expected: 기존 case PASS, 신규 case는 일부 PASS / 일부 FAIL (다중 토큰은 fail, 초성은 fail — currentQuery 단일 substring으로는 매칭 안 됨).

- [ ] **Step 3: `src/ui/handlers/archive.ts` integration**

3.1) module 상단 import 추가:
```ts
import { matchesAllTokens, tokenizeQuery } from '../../utils/fuzzy';
```

3.2) `currentQuery` 옆에 `currentTokens` 신설 (현재 line ~24):
```ts
let currentQuery = '';
// v3.32 T3: single source of truth = currentTokens (matchesAllTokens 입력).
// currentQuery는 raw input 보존용 write-only (debugging/관찰 보존).
let currentTokens: string[] = [];
```

3.3) `handleArchiveSearch` (현재 line ~83) 변경:
```ts
export function handleArchiveSearch(): void {
  const input = document.getElementById('archiveSearch') as HTMLInputElement | null;
  const raw = input?.value ?? '';
  currentQuery = raw.trim().toLowerCase().normalize('NFC');
  currentTokens = tokenizeQuery(raw);
  rerenderList();
}
```

3.4) 4 filter spot 교체 (현재 line ~700, ~716, ~743~747, ~768):

```ts
// (a) byInterest briefing filter — 기존: b.title/summary normalize+lower+includes(currentQuery)
//     교체:
if (currentTokens.length > 0) {
  pool = pool.filter((b) =>
    matchesAllTokens(b.title ?? '', currentTokens) ||
    matchesAllTokens(b.summary ?? '', currentTokens),
  );
}

// (b) byInterest insight filter — 기존: i.text normalize+lower+includes(currentQuery)
//     교체:
if (currentTokens.length > 0) {
  pool = pool.filter((i) => matchesAllTokens(i.text, currentTokens));
}

// (c) all entity filter (briefing+answer+insight 3 분기) — 기존:
//     - e.answer.text.normalize+lower+includes(currentQuery)
//     - e.insight.text.normalize+lower+includes(currentQuery)
//     - e.briefing.title/summary normalize+lower+includes(currentQuery)
//     교체: 각 분기를 matchesAllTokens(text, currentTokens) 호출로

if (currentTokens.length > 0) {
  filtered = filtered.filter((e) =>
    e.type === 'answer'
      ? matchesAllTokens(e.answer.text, currentTokens)
      : e.type === 'insight'
        ? matchesAllTokens(e.insight.text, currentTokens)
        : matchesAllTokens(e.briefing.title ?? '', currentTokens) ||
          matchesAllTokens(e.briefing.summary ?? '', currentTokens),
  );
}

// (d) answers entity filter — 기존: a.text normalize+lower+includes(currentQuery)
//     교체:
if (currentTokens.length > 0) {
  filtered = filtered.filter((a) => matchesAllTokens(a.text, currentTokens));
}
```

3.5) 2 highlight caller (현재 line ~582, ~614) 교체:
```ts
// T2 임시 어댑테이션 [currentQuery ? [currentQuery] : []] → currentTokens
body.innerHTML = highlightHtml(a.text, currentTokens);
// 및
body.innerHTML = highlightHtml(i.text, currentTokens);
```

3.6) `if (currentQuery)` 가드 (highlight 호출 직전, 현재 line ~580, ~611)는 `if (currentTokens.length > 0)`로 변경 — 의미 정합 + 회귀 X.

- [ ] **Step 4: 신규 archive-search spec 실제 case 작성 (fixture 보고 정합)**

Run: `head -40 tests/ui/archive-search.spec.ts`로 기존 fixture 패턴 확인 후 Step 1의 placeholder case들을 실제 fixture + DOM assertion으로 작성. 예: 검색 input dispatch event → rerender 후 `document.querySelectorAll('.archive-card')` length / `<mark>` 존재 검증.

- [ ] **Step 5: 전체 spec PASS 확인**

Run:
```bash
npm test -- --run archive-search.spec
npm test -- --run  # 전체
```
Expected: 신규 5 case 모두 PASS, 기존 case 회귀 0, 전체 baseline 1279 + fuzzy 22 + highlight 4 + archive 5 = ~1310 PASS.

- [ ] **Step 6: 양쪽 TZ + smoke**

Run:
```bash
TZ=America/New_York npm test -- --run
npm run test:smoke 2>&1 | tail -5
```
Expected: NY TZ도 동일 ~1310 PASS, smoke 100 PASS.

- [ ] **Step 7: bundle 1차 측정 (T3 직후)**

Run:
```bash
npm run build 2>&1 | tail -5
gzip -c dist/assets/index-*.js | wc -c
```
Expected: baseline 19078 + Δ. GREEN ≤ +300 B (≤ 19378), AMBER ≤ +500 B (≤ 19578). 측정값을 retro 작성 시 기록.

- [ ] **Step 8: per-task reviewer dispatch**

Use Agent tool `pr-review-toolkit:code-reviewer`. Focus: archive.ts integration (4 filter spot + 2 highlight caller + currentTokens 추가). cross-file invariant (currentQuery write-only 확인 / currentTokens read 경로 누락 X) 중심 review.

- [ ] **Step 9: Commit T3**

```bash
git add src/ui/handlers/archive.ts tests/ui/archive-search.spec.ts
git commit -m "feat(v3.32 T3): archive.ts fuzzy 통합 — currentTokens 4 filter spot + 2 highlight

C9 fuzzy 통합 완료. currentTokens SoT 도입 (currentQuery write-only 유지).
- handleArchiveSearch: tokenizeQuery로 currentTokens 갱신
- byInterest briefing/insight, all 3-way, answers 4 filter spot → matchesAllTokens 교체
- 2 highlight caller → currentTokens 전달 (T2 임시 어댑테이션 정리)
- if (currentQuery) 가드 → if (currentTokens.length > 0)

신규 archive-search spec 5 case (다중 토큰 AND / 초성 only / 초성+substring 혼합 /
공백 다수 / 영문 회귀). bundle 1차: <byte 수> (Δ +<delta>).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: T2 P2-3 — `mountArchiveHandlers` idempotence 단위 spec

**Files:**
- Create: `tests/unit/archive-mount-idempotence.spec.ts`

- [ ] **Step 1: spec red 작성**

```ts
import { describe, it, expect, beforeEach, vi } from 'vitest';
import {
  mountArchiveHandlers,
  resetArchiveHandlersForTest,
} from '../../src/ui/handlers/archive';

describe('mountArchiveHandlers idempotence (v3.32 T4 — v3.30 T2 P2-3 carry)', () => {
  beforeEach(() => {
    document.body.replaceChildren();
    resetArchiveHandlersForTest();
  });

  it('단일 mount — document.addEventListener 6 호출 (현재 6 listener)', () => {
    const spy = vi.spyOn(document, 'addEventListener');
    mountArchiveHandlers();
    // 현재 mount 6 listener (handleCardDeleteClick / handleCardClick / select toggle /
    // bulk delete / pin toggle / other-pin-counter). 신규 listener 추가 시 baseline 갱신 의무.
    const clickCalls = spy.mock.calls.filter(([type]) => type === 'click').length;
    expect(clickCalls).toBe(6);
    spy.mockRestore();
  });

  it('double mount — guard로 두 번째는 0 listener 추가 (idempotent)', () => {
    mountArchiveHandlers(); // 1차 mount
    const spy = vi.spyOn(document, 'addEventListener');
    mountArchiveHandlers(); // 2차 mount — guard로 early return
    const clickCalls = spy.mock.calls.filter(([type]) => type === 'click').length;
    expect(clickCalls).toBe(0);
    spy.mockRestore();
  });

  it('resetArchiveHandlersForTest 후 재mount — listener 다시 등록', () => {
    mountArchiveHandlers();
    resetArchiveHandlersForTest();
    const spy = vi.spyOn(document, 'addEventListener');
    mountArchiveHandlers();
    const clickCalls = spy.mock.calls.filter(([type]) => type === 'click').length;
    expect(clickCalls).toBe(6);
    spy.mockRestore();
  });
});
```

- [ ] **Step 2: spec red 확인 (baseline 6이 정확한지)**

Run: `npm test -- --run archive-mount-idempotence`
Expected: 3 case 모두 PASS — guard가 v3.30 T2 review fix `2598f16`에서 이미 도입됨. Listener 수가 6이 아니면 baseline 갱신 (실제 grep 후 정확 수 기록).

> **분기**: Step 2 결과가 FAIL (guard 결함 발견)이면 in-cycle fix:
> - archive.ts의 `__archiveMounted` 확인 + early return 정합
> - listener 수 mismatch면 spec baseline 수정 (grep `addEventListener` 수 카운트)

- [ ] **Step 3: 양쪽 TZ 확인**

Run: `TZ=America/New_York npm test -- --run archive-mount-idempotence`
Expected: 동일 PASS (TZ 무관).

- [ ] **Step 4: per-task reviewer dispatch**

Use Agent tool `pr-review-toolkit:code-reviewer`. Focus: T4 신규 spec — guard 회귀 spec의 의미 검증, listener count baseline의 적절성 확인.

- [ ] **Step 5: Commit T4**

```bash
git add tests/unit/archive-mount-idempotence.spec.ts
git commit -m "test(v3.32 T4 / v3.30 T2 P2-3 carry): mountArchiveHandlers idempotence regression

vi.spyOn(document, 'addEventListener')로 호출 수 측정.
- 단일 mount: click listener 6 추가 (baseline)
- double mount: guard 작동, 2차 mount는 0 listener 추가
- reset 후 재mount: 다시 6 listener 추가

v3.30 T2 review fix 2598f16의 __archiveMounted guard 회귀 spec 격상.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: T3 P2 — scroll-margin-coverage per-file invariant 강화

**Files:**
- Modify: `tests/unit/scroll-margin-coverage.spec.ts`

- [ ] **Step 1: 현 case 확인 + assertion 강화**

기존 (line 29-39):
```ts
it('scrollIntoView call site count baseline = 3 (regression guard)', () => {
  const homeMatches = (homeHandlersTs.match(/scrollIntoView/g) ?? []).length;
  const statsMatches = (statsHandlersTs.match(/scrollIntoView/g) ?? []).length;
  const sharedMatches = (statsSharedTs.match(/scrollIntoView/g) ?? []).length;
  expect(homeMatches + statsMatches + sharedMatches).toBe(3);
});
```

강화 (per-file assertion):
```ts
it('scrollIntoView call site count baseline (per-file 강화 — v3.32 T5)', () => {
  // v3.30 T3에서 stats.ts의 scrollToGardenSection이 stats-shared.ts로 분리됨.
  // 본 spec은 per-file 분포 invariant까지 강제: stats.ts는 더 이상 scrollIntoView를 직접 호출하지 않음.
  const homeMatches = (homeHandlersTs.match(/scrollIntoView/g) ?? []).length;
  const statsMatches = (statsHandlersTs.match(/scrollIntoView/g) ?? []).length;
  const sharedMatches = (statsSharedTs.match(/scrollIntoView/g) ?? []).length;
  expect(homeMatches).toBeGreaterThan(0); // home에는 scrollIntoView 호출 존재
  expect(statsMatches).toBe(0); // v3.30 T3 분리 후 stats.ts 0건 invariant
  expect(homeMatches + statsMatches + sharedMatches).toBe(3); // 총합 baseline 유지
});
```

- [ ] **Step 2: spec 실행**

Run: `npm test -- --run scroll-margin-coverage`
Expected: PASS (v3.30 T3 분리 결과 stats.ts에 scrollIntoView가 없으므로). 만약 FAIL이면 stats.ts에 잔존 호출이 있다는 의미 → in-cycle fix (stats-shared.ts로 이동).

- [ ] **Step 3: Commit T5**

```bash
git add tests/unit/scroll-margin-coverage.spec.ts
git commit -m "test(v3.32 T5 / v3.30 T3 P2 carry): scroll-margin-coverage per-file 강화

기존 총합 3 invariant에 per-file 분포 강화:
- statsMatches === 0 (v3.30 T3 분리 결과)
- homeMatches > 0
- 총합 3 유지

stats.ts에 scrollIntoView 회귀 추가 시 즉시 fail로 감지.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: T5 P2 — `createDeferredLoader`를 `tests/helpers/deferred-loader.ts`로 추출

**Files:**
- Create: `tests/helpers/deferred-loader.ts`
- Modify: `tests/unit/nav.spec.ts`

- [ ] **Step 1: helper 추출**

`tests/helpers/deferred-loader.ts`:

```ts
/**
 * v3.32 T6 (v3.30 T5 P2 carry): nav.spec.ts에서 추출된 controlled deferred loader helper.
 *
 * 용도: dynamic import 기반 lazy-loaded module의 out-of-order resolve를 결정론으로 시뮬레이션.
 * - load(): Promise를 반환 (외부 resolve로 제어)
 * - resolve(): 해당 promise를 settled로 전환
 *
 * parallel safety: 각 호출이 독립 Promise/resolver를 생성하므로 spec간 간섭 없음.
 */
export function createDeferredLoader<T>(
  payload: T,
): { load: () => Promise<T>; resolve: () => void } {
  let resolveFn!: () => void;
  const pending = new Promise<T>((res) => {
    resolveFn = () => res(payload);
  });
  return { load: () => pending, resolve: resolveFn };
}
```

> 참고: nav.spec.ts의 기존 시그니처는 `(render: (c: HTMLElement) => void) => { load, resolve }` (render 콜백을 payload로 사용). Generic으로 일반화 — render 콜백을 payload로 전달.

- [ ] **Step 2: nav.spec.ts에서 helper import + 로컬 helper 제거**

`tests/unit/nav.spec.ts` 변경:
```ts
import { createDeferredLoader } from '../helpers/deferred-loader';

// 기존 로컬 createDeferredLoader 함수 (line ~4-14) 삭제
```

호출부 (현재 `createDeferredLoader((c) => { ... })`)는 시그니처가 동일하므로 변경 불필요 — generic이 `(c: HTMLElement) => void` 타입을 자동 추론.

- [ ] **Step 3: nav.spec 실행 확인**

Run: `npm test -- --run nav.spec`
Expected: 기존 case 회귀 0, PASS.

- [ ] **Step 4: 양쪽 TZ**

Run: `TZ=America/New_York npm test -- --run nav.spec`
Expected: PASS.

- [ ] **Step 5: Commit T6**

```bash
git add tests/helpers/deferred-loader.ts tests/unit/nav.spec.ts
git commit -m "refactor(v3.32 T6 / v3.30 T5 P2 carry): createDeferredLoader → tests/helpers 격상

nav.spec.ts 로컬 helper를 tests/helpers/deferred-loader.ts로 추출.
Generic <T> 일반화 (render 콜백 외 임의 payload 재사용 가능, parallel safety).
호출부 변경 없음.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 7: Codex 최종 review + 사이클 끝 검증

**Files:** N/A (verification + review)

- [ ] **Step 1: TS strict 확인**

Run: `npx tsc --noEmit`
Expected: 0 errors.

- [ ] **Step 2: Lint 확인**

Run: `npm run lint`
Expected: baseline 30 problems 유지 (신규 error 0). 만약 신규 error 발생 → in-cycle fix.

- [ ] **Step 3: vitest 양쪽 TZ**

Run:
```bash
npm test -- --run 2>&1 | tail -5
TZ=America/New_York npm test -- --run 2>&1 | tail -5
```
Expected: 양쪽 동일 ~1310 PASS (baseline 1279 + fuzzy 22 + highlight 4 + archive 5 + mount 3 = 1313).

- [ ] **Step 4: smoke**

Run: `npm run test:smoke 2>&1 | tail -5`
Expected: 100 PASS.

- [ ] **Step 5: 신규 spec N=5**

Run:
```bash
for i in 1 2 3 4 5; do
  npm test -- --run fuzzy.spec highlight.spec archive-search.spec archive-mount-idempotence 2>&1 | tail -1
done
```
Expected: 5/5 모두 PASS.

- [ ] **Step 6: build + bundle 2차 측정**

Run:
```bash
npm run build 2>&1 | tail -5
gzip -c dist/assets/index-*.js | wc -c
```
Expected: T3 직후 1차 측정과 동일 또는 ±5 B 내. Δ 기록.

- [ ] **Step 7: Codex 최종 review dispatch**

Run:
```bash
codex exec --skip-git-repo-check -o /tmp/v3.32-codex-final.txt "다음 사이클 전체 diff를 review하고 DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT로 분류 + P0/P1/P2 list. diff: $(git diff 97af348..HEAD)."
```

실패 시: controller fallback `pr-review-toolkit:code-reviewer` agent에 동일 dispatch.

- [ ] **Step 8: P0/P1 in-cycle fix 또는 carry 결정**

- P0: 반드시 in-cycle fix 후 T7 재실행
- P1: in-cycle 또는 v3.32.1 hotfix
- P2: retro carry 또는 in-cycle (소형)

- [ ] **Step 9: git diff --check**

Run: `git diff --check`
Expected: 0 issues (whitespace error 없음).

---

## Task 8: Retro + MEMORY.md + Obsidian sync

**Files:**
- Modify: `docs/superpowers/specs/2026-05-13-v3.32-fuzzy-search-design.md` (§7 Retro 작성)
- Modify: `/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md` + `memory/project_daily_growth_v332.md` (신규)
- Optional: Obsidian vault sync (사용자 명시 시점)

- [ ] **Step 1: 사이클 spec §7 Retro 작성**

template:
- 7.1 결과 요약 (HEAD / vitest / smoke / TS / lint / bundle / Codex 결과 / 핵심 변경 한 줄 요약)
- 7.2 Task별 결과 표
- 7.3 Verification Log 표 (각 검증 명령 + 결과)
- 7.4 Codex Review Notes (사전 / 최종 / fallback 적용 여부 / P0/P1/P2 disposition)
- 7.5 Lessons Graduated (있다면 CLAUDE.md / COMMON_MISTAKES.md 갱신 안내)
- 7.6 Carry-forward (v3.33)
- 7.7 Next recommended step

- [ ] **Step 2: memory 신규 파일 + index 갱신**

`memory/project_daily_growth_v332.md`:
```markdown
---
name: project-daily-growth-v332
description: DG v3.32 fuzzy search + polish — 다중 토큰 AND + 한글 초성 + v3.30 carry 3건 청산
metadata:
  type: project
---
[retro 1줄 요약]
```

`memory/MEMORY.md` 신규 라인 추가 (~150 char):
```
- [DG v3.32 Fuzzy Search + Polish](project_daily_growth_v332.md) — <한 줄 요약>
```

- [ ] **Step 3: Obsidian sync (사용자 명시 시점)**

CLAUDE.md global 정책 따름. 5-folder 구조:
- 02-Cycles/ ← `docs/superpowers/specs/2026-05-13-v3.32-fuzzy-search-design.md` 복사
- 03-Lessons/ ← `memory/project_daily_growth_v332.md` + MEMORY.md 복사
- 00-Index.md ← 사이클 타임라인 표에 한 줄 추가

cp 단방향 (vault 덮어쓰기 X).

- [ ] **Step 4: 사이클-끝 commit**

```bash
git add docs/superpowers/specs/2026-05-13-v3.32-fuzzy-search-design.md
git commit -m "docs(v3.32 T8 retro): 사이클-끝 + Codex 최종 <APPROVED|...> + v3.33 carry

[Retro 요약 한 단락]

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Verification Summary

| Gate | Command | 기대치 |
|---|---|---|
| TS strict | `npx tsc --noEmit` | 0 errors |
| Lint | `npm run lint` | baseline 30 유지 |
| Vitest KST | `npm test -- --run` | ~1313 PASS |
| Vitest NY | `TZ=America/New_York npm test -- --run` | KST 동일 |
| Smoke | `npm run test:smoke` | 100 PASS |
| Spec N=5 | 신규 spec 5회 반복 | 5/5 PASS |
| Build | `npm run build` | PASS |
| Bundle | `gzip -c dist/assets/index-*.js \| wc -c` | GREEN ≤ 19378 / AMBER ≤ 19578 |
| Git diff check | `git diff --check` | 0 issues |
| Codex 2-pass | CLI direct or controller fallback | P0=0 (in-cycle fix or carry) |

---

## Self-Review Checklist (실행 전 controller가 자체 점검)

- [ ] 모든 task가 spec §2 architecture 한 줄 이상에 대응 (fuzzy.ts / highlight.ts / archive.ts / 3 polish)
- [ ] code block 안에 placeholder ("TBD", "implement later", "add validation") 없음
- [ ] 타입/함수명 일관성: `tokenizeQuery` / `getInitialConsonants` / `isInitialOnlyToken` / `matchesAllTokens` / `highlightHtml(text, tokens[])` / `currentTokens` 모든 task에서 동일 spelling
- [ ] 각 main task 마다 red gate → green → commit 순서 (T1/T2/T3/T4)
- [ ] T5/T6는 spec assertion 강화 / refactor만 — red gate 없음 (의도적)
- [ ] per-task reviewer dispatch는 T1/T2/T3/T4에 명시 (T5/T6 self-review 적용)
- [ ] Codex 사전 review는 T0 후속 Step 0.6에 명시
- [ ] Codex 최종 review는 T7 Step 7에 명시
- [ ] bundle 1차 (T3) / 2차 (T7) 측정 명시
- [ ] baseline commit (`97af348`) + 수치 (`19078 B`, `1279 PASS`, `100 PASS`) 명시
