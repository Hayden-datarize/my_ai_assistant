# v3.14 Missions Tab + T10-B View Triggers + Playwright Sync — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 미션을 홈 탭에서 떼어내 별도 `missions` 탭으로 분리하고, T10-B(briefing-view + cross-interest-view) 미션 트리거를 활성화하며, v3.13 통합 시 노후화된 Playwright 2건 fixture를 동기화한다.

**Architecture:** (1) `nav.ts` `TABS` 배열에 `missions` 6번째 entry 추가 + `tabs/missions.ts` 신규 (renderMissionsSection 재사용) + `handlers/missions.ts` 신규 (tab-changed에서 hydrate). (2) `home.ts` 브리핑 카드 click 핸들러에서 `fireBriefingViewTrigger` (sessionStorage 카드별 dedup) + `fireCrossInterestTrigger` (sessionStorage 일별 dedup) 호출. (3) `interestKeywords` helper를 `achievements.ts`에서 `utils/interestKeywords.ts`로 추출하여 home·achievements 양쪽에서 import. (4) `missionEngine.ts` `DEFERRED_DEFIDS`에서 두 미션 제거 → DAILY pool 5→7 entries.

**Tech Stack:** Vite + TypeScript SPA, Vitest (unit/integration), Playwright (smoke), Firebase Hosting. Schema 변경 없음, localStorage 키 변경 없음, CSP 변경 없음, 마이그레이션 0줄.

---

## §0. Existing Landscape (작성 전 매핑 — Plan Discovery Lesson)

### 탭 구조
- [src/ui/nav.ts:8](src/ui/nav.ts#L8) `TabId = 'home' | 'archive' | 'stats' | 'insights' | 'settings'` (5탭)
- [src/ui/nav.ts:10-16](src/ui/nav.ts#L10-L16) `TABS` 배열 — id/label/icon/render
- 탭 전환 시 `dg:nav:tab-changed` CustomEvent dispatch ([nav.ts:56](src/ui/nav.ts#L56))
- 기존 listeners: `home.ts:293`, `archive.ts:323`, `stats.ts:54` — 각자 자기 탭으로 돌아왔을 때 hydrate

### 미션 현재 위치
- 마크업: [src/ui/tabs/home.ts:77-78](src/ui/tabs/home.ts#L77) `<div id="missionsContainer"></div>` (질문 섹션 ↔ AI 채팅 사이)
- 렌더 함수: [src/ui/missions-section.ts](src/ui/missions-section.ts) `renderMissionsSection(root, active)` — daily/weekly/monthly 3 그룹, chevron toggle 내장
- Hydrate 진입점: [src/ui/handlers/home.ts:320](src/ui/handlers/home.ts#L320) `hydrateMissions()` — `getActiveMissions()` 호출, dirty이면 `saveUser` (Quota toast guard) 후 렌더
- `hydrateMissions()` 호출 위치: `hydrateHome()` 내부 ([home.ts:310](src/ui/handlers/home.ts#L310)) + `submitAnswer` 후 ([home.ts:806](src/ui/handlers/home.ts#L806))

### T10-B deferred (현 상태)
- [src/state/missionEngine.ts:74-77](src/state/missionEngine.ts#L74-L77) `DEFERRED_DEFIDS = {'daily-briefing-5', 'daily-cross-interest-1'}` — picker에서 제외
- [src/state/missionCatalog.ts:7-8](src/state/missionCatalog.ts#L7-L8) DAILY_POOL에 두 entry 정의 그대로 (catalog 보존)
- [src/ui/handlers/home.ts:455-466](src/ui/handlers/home.ts#L455-L466) TODO(T10-deferred) 주석으로 wire-up 위치 명시
- [src/ui/handlers/missions-triggers.ts:23,25](src/ui/handlers/missions-triggers.ts#L23) `fireBriefingViewTrigger`/`fireCrossInterestTrigger` 이미 구현 완료 — atomic single-now sweep pattern
- `interestKeywords(id: string): string[]` — [src/state/achievements.ts:23](src/state/achievements.ts#L23) **private** function (export 안 됨). `INTERESTS` catalog 한국어 label에서 keyword 추출

### Bottom-nav 시각 제약
- [src/styles/layout.css:43-52](src/styles/layout.css#L43-L52) `.bottom-nav { display: flex; max-width: 480px; padding: 6px 0; }` — `.nav-item { flex: 1 }`로 균등 분할
- 480/6 = 80px/탭 (현재 480/5 = 96px/탭). icon 1.3rem + label 0.65rem
- 라벨 길이: `홈(1)`, `아카이브(4)`, `통계(2)`, `인사이트(4)`, `설정(2)`, `미션(2)` — 6번째도 폭 압박 없음
- Desktop ≥768px: `display: none` (sidebar로 대체) → `src/ui/sidebar.ts`도 `TABS` 재사용해야 자동 반영

### Playwright 실패 2건 — Root cause
- [tests/smoke/v3.12-game-feedback.spec.ts:60](tests/smoke/v3.12-game-feedback.spec.ts#L60) `.toast--badge` strict-mode violation: v3.13에서 `daily-answer-1` 미션이 추가되며 첫 답변에 `첫 답변` + `미션 입문` 뱃지 2개 동시 unlock
- [tests/smoke/v3.12-game-feedback.spec.ts:70](tests/smoke/v3.12-game-feedback.spec.ts#L70) `.badges-category h4` got 6 expected 5: v3.13에서 `Mission` 카테고리(4뱃지) 추가로 5→6
- 둘 다 production 코드 버그가 아니라 v3.13 통합 시 spec 누락. `toBe(22)` 뱃지 총합도 동시 점검 필요

### LocalStorage / Schema / CSP
- 변경 없음. schema v3 유지. 마이그레이션 0줄.

### 영향받지 않는 영역
- `state/missionEngine.ts` regen 로직, `missionsCumulative`, `missions-triggers.ts` atomic 패턴 — 모두 그대로
- `runSweep` / `detectEvents` — 그대로 (mission-complete 이벤트는 fireTrigger가 발화)
- `welcome-gamification` modal — 홈 탭 진입 시만 발화 ([home.ts:296-300](src/ui/handlers/home.ts#L296-L300)). 미션 탭 진입과 무관

---

## §1. File Structure

| 분류 | 경로 | 책임 |
|---|---|---|
| Create | `src/ui/tabs/missions.ts` | 미션 탭 정적 마크업 (heading + `<div id="missionsContainer">` 컨테이너) + handler dispatch hook |
| Create | `src/ui/handlers/missions.ts` | `dg:nav:tab-changed { tab:'missions' }` 수신 시 `hydrateMissions()` 호출 — home.ts에서 옮긴 함수 재사용 |
| Create | `src/utils/interestKeywords.ts` | `achievements.ts`에서 추출한 pure helper — `home.ts`와 `achievements.ts` 양쪽 import |
| Modify | `src/ui/nav.ts:8,10-16` | `TabId`에 `'missions'` 추가, TABS 배열에 6번째 entry (icon `🎯`, label `미션`) |
| Modify | `src/ui/tabs/home.ts:77-78` | `<div id="missionsContainer"></div>` 제거 |
| Modify | `src/ui/handlers/home.ts` | (a) `hydrateMissions()` 함수 export를 `handlers/missions.ts`로 이전 (re-export 유지하지 않음) (b) `hydrateHome` 내부 호출 제거 (c) `submitAnswer` 후 호출은 그대로 유지 — 단, missions 탭이 mounted된 경우만 렌더 (no-op safe) (d) `renderBriefingCard` click 핸들러 TODO 자리에 `fireBriefingViewTrigger` + `fireCrossInterestTrigger` wiring |
| Modify | `src/state/achievements.ts:23-30` | private `interestKeywords` 삭제 → `import` from `utils/interestKeywords.ts` |
| Modify | `src/state/missionEngine.ts:74-77` | `DEFERRED_DEFIDS` 빈 Set으로 축소 (or const 자체 제거) — 두 미션 picker pool 합류 |
| Modify | `tests/smoke/v3.12-game-feedback.spec.ts:60,70,73` | fixture sync (toast filter, count 5→6, 18→22) |
| Create | `tests/unit/interestKeywords.spec.ts` | 추출된 helper unit tests (parametric) |
| Create | `tests/unit/nav.spec.ts` (또는 기존에 추가) | 6 tabs, missions entry 검증 |
| Create | `tests/unit/handlers-missions.spec.ts` | tab-changed 시 hydrateMissions 호출 검증 |
| Create | `tests/unit/home-briefing-view-trigger.spec.ts` | 클릭 시 fireBriefingViewTrigger 호출 + sessionStorage dedup |
| Create | `tests/unit/home-cross-interest-trigger.spec.ts` | interest-mismatch 시만 fireCrossInterestTrigger + 일별 dedup |
| Create | `tests/smoke/missions-tab.spec.ts` | 6탭 nav 표시 + 미션 탭 클릭 → `#missionsSection` 가시 + 모바일 375px 라벨 잘림 없음 |

### Bundle delta budget
- 신규 코드: tabs/missions.ts (~0.3 kB) + handlers/missions.ts (~0.3 kB) + utils/interestKeywords.ts (~0.1 kB, 기존 코드 이동) + nav.ts entry 1줄 + briefing click wiring (~0.4 kB)
- 추정 총합: **+0.7 ~ +1.0 kB gzip JS** (DoD ceiling **+1.2 kB**)
- 31.47 → 32.4~32.6 kB 예상

### N=5 stability 대상
- `tests/unit/home-briefing-view-trigger.spec.ts` (sessionStorage dedup)
- `tests/unit/home-cross-interest-trigger.spec.ts` (interest 매칭)
- `tests/smoke/missions-tab.spec.ts` (탭 전환)

---

## §2. Version Naming

권고 = **v3.14**. 근거:
- 새 탭 추가 = UI 구조 변경
- T10-B 활성화 = deferred 미션 2개 graduation (feature 추가)
- Playwright fixture sync는 부수
- 메모리 hint "v3.14 Game World"의 출발선으로 자연스러움

대안 = `v3.13.2 hotfix` 명칭 유지. 단, scope가 hotfix를 넘어서 (새 탭 + feature 추가) 명칭 불일치. 사용자가 v3.13.2 유지를 원하면 task-by-task 메모만 동기화.

---

## §3. Tasks

### Task 1: Playwright fixture sync (선행 — green baseline 확보)

**Files:**
- Modify: `tests/smoke/v3.12-game-feedback.spec.ts:60-63, 70, 73`

- [ ] **Step 1: 기존 실패 확인**

Run: `npx playwright test tests/smoke/v3.12-game-feedback.spec.ts --reporter=line`
Expected: 2 failed (line 60 strict-mode violation, line 70 count 5/6)

- [ ] **Step 2: spec fixture 수정**

`tests/smoke/v3.12-game-feedback.spec.ts:58-63` 교체:

```ts
    // answers-1 badge unlock toast (v3.13 미션 입문 뱃지와 공존 — 첫 답변 toast로 명시 필터)
    const toast = page.locator('.toast--badge').filter({ hasText: '첫 답변' });
    await expect(toast).toBeVisible({ timeout: 2000 });
    // 토스트 안에 "첫 답변" (badge name) 또는 "뱃지 획득" (suffix)
    await expect(toast).toContainText(/첫 답변|뱃지 획득/);
```

`tests/smoke/v3.12-game-feedback.spec.ts:69-73` 교체:

```ts
    // 6 카테고리 heading (streak / volume / tier / diversity / engagement / mission)
    await expect(page.locator('.badges-category h4')).toHaveCount(6);

    // 22 뱃지 모두 grid에 (v3.12 18 + v3.13 mission 4)
    await expect(page.locator('#badgesGrid .badge')).toHaveCount(22);
```

- [ ] **Step 3: Playwright pass 확인**

Run: `npx playwright test tests/smoke/v3.12-game-feedback.spec.ts --reporter=line`
Expected: 2 passed

- [ ] **Step 4: 전체 Playwright pass 확인 (다른 fail 없음)**

Run: `npx playwright test --reporter=line 2>&1 | tail -10`
Expected: `91 passed`

- [ ] **Step 5: Commit**

```bash
git add tests/smoke/v3.12-game-feedback.spec.ts
git commit -m "test(v3.14 T1): v3.12 game-feedback fixture sync — v3.13 mission 카테고리·뱃지 반영 (P0 후보 → P3 fixture 노후화 확정)"
```

---

### Task 2: `interestKeywords` helper 추출 (pure refactor)

**Files:**
- Create: `src/utils/interestKeywords.ts`
- Modify: `src/state/achievements.ts:23-30` (delete + import)
- Test: `tests/unit/interestKeywords.spec.ts`

- [ ] **Step 1: 실패 테스트 작성**

`tests/unit/interestKeywords.spec.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { interestKeywords } from '../../src/utils/interestKeywords';

describe('interestKeywords', () => {
  it('returns id lowercase + label keywords for known interest', () => {
    // 'ai_ml' label에 한국어 + AI/ML 같은 토큰 포함 가정 (categories.ts 참조)
    const k = interestKeywords('ai_ml');
    expect(k).toContain('ai_ml');
    expect(k.length).toBeGreaterThan(1);
  });

  it('returns just lowercased id for unknown interest', () => {
    expect(interestKeywords('UNKNOWN_X')).toEqual(['unknown_x']);
  });

  it('strips emoji prefix from label and splits on whitespace/slash', () => {
    // hr_system label: '🏢 인사제도' (또는 유사) — emoji 제거 후 split
    const k = interestKeywords('hr_system');
    expect(k.every(s => !/\p{Extended_Pictographic}/u.test(s))).toBe(true);
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run tests/unit/interestKeywords.spec.ts`
Expected: FAIL — `Cannot find module '../../src/utils/interestKeywords'`

- [ ] **Step 3: helper 추출**

`src/utils/interestKeywords.ts` 신규:

```ts
import { INTERESTS } from './categories';

/**
 * interest ID(snake_case)를 RSS 텍스트에 매칭 가능한 keyword 배열로 확장.
 * v3.14 T2: achievements.ts 내부에서 추출 — home·achievements 양쪽 재사용.
 */
export function interestKeywords(id: string): string[] {
  const meta = INTERESTS.find(c => c.id === id);
  if (!meta) return [id.toLowerCase()];
  const label = meta.label.replace(/^\p{Extended_Pictographic}+\s*/u, '').toLowerCase();
  return [id.toLowerCase(), ...label.split(/[\s/]+/).filter(Boolean)];
}
```

`src/state/achievements.ts:23-30` private function 제거 + 상단 import 추가:

```ts
import { interestKeywords } from '../utils/interestKeywords';
```

(함수 정의 13줄 삭제, import 1줄 추가)

- [ ] **Step 4: 통과 확인**

Run: `npx vitest run tests/unit/interestKeywords.spec.ts && npx vitest run tests/unit/`
Expected: 신규 3 spec PASS + 기존 vitest 회귀 0

- [ ] **Step 5: Commit**

```bash
git add src/utils/interestKeywords.ts src/state/achievements.ts tests/unit/interestKeywords.spec.ts
git commit -m "refactor(v3.14 T2): extract interestKeywords helper to utils — home·achievements 공유 (T10-B 준비)"
```

---

### Task 3: `nav.ts` 6번째 탭 추가 + `tabs/missions.ts` 정적 마크업

**Files:**
- Modify: `src/ui/nav.ts:8, 10-16`
- Create: `src/ui/tabs/missions.ts`
- Test: `tests/unit/nav.spec.ts` (기존 존재 확인 — 없으면 신규)

- [ ] **Step 1: nav.spec.ts 실패 테스트**

기존 `tests/unit/nav.spec.ts`가 있으면 case 추가, 없으면 신규:

```ts
import { describe, it, expect } from 'vitest';
import { TABS } from '../../src/ui/nav';

describe('nav TABS catalog', () => {
  it('includes 6 tabs in fixed order', () => {
    expect(TABS.map(t => t.id)).toEqual(['home', 'archive', 'stats', 'insights', 'settings', 'missions']);
  });

  it('missions tab carries 미션 label and 🎯 icon', () => {
    const m = TABS.find(t => t.id === 'missions');
    expect(m?.label).toBe('미션');
    expect(m?.icon).toBe('🎯');
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run tests/unit/nav.spec.ts`
Expected: FAIL — `Expected length: 6, Received: 5`

- [ ] **Step 3: `tabs/missions.ts` 신규**

`src/ui/tabs/missions.ts`:

```ts
/**
 * Missions 탭 정적 마크업.
 * v3.14: 홈에서 분리. handlers/missions.ts가 dg:nav:tab-changed 수신 시 hydrate.
 */
export function renderMissions(container: HTMLElement): void {
  // eslint-disable-next-line no-restricted-syntax -- trusted static template, no interpolation
  container.innerHTML = `
    <div id="missionsTab">
      <div class="header">
        <div class="header-left">
          <h1>🎯 미션</h1>
        </div>
      </div>
      <div id="missionsContainer"></div>
    </div>
  `;
}
```

- [ ] **Step 4: `nav.ts` 6번째 entry 추가**

`src/ui/nav.ts:1-16` 교체:

```ts
import { qs } from '../utils/dom';
import { renderHome } from './tabs/home';
import { renderArchive } from './tabs/archive';
import { renderStats } from './tabs/stats';
import { renderInsights } from './tabs/insights';
import { renderSettings } from './tabs/settings';
import { renderMissions } from './tabs/missions';

export type TabId = 'home' | 'archive' | 'stats' | 'insights' | 'settings' | 'missions';

export const TABS: Array<{ id: TabId; label: string; icon: string; render: (c: HTMLElement) => void }> = [
  { id: 'home', label: '홈', icon: '🏠', render: renderHome },
  { id: 'archive', label: '아카이브', icon: '📚', render: renderArchive },
  { id: 'stats', label: '통계', icon: '📊', render: renderStats },
  { id: 'insights', label: '인사이트', icon: '💡', render: renderInsights },
  { id: 'settings', label: '설정', icon: '⚙️', render: renderSettings },
  { id: 'missions', label: '미션', icon: '🎯', render: renderMissions },
];
```

- [ ] **Step 5: nav 테스트 통과 + 빌드 확인**

Run: `npx vitest run tests/unit/nav.spec.ts && npm run build`
Expected: PASS + 빌드 성공

- [ ] **Step 6: Commit**

```bash
git add src/ui/tabs/missions.ts src/ui/nav.ts tests/unit/nav.spec.ts
git commit -m "feat(v3.14 T3): add missions tab to nav — 🎯 6번째 entry + tabs/missions.ts 정적 마크업"
```

---

### Task 4: `handlers/missions.ts` — tab-changed에서 hydrate

**Files:**
- Create: `src/ui/handlers/missions.ts`
- Modify: `src/ui/handlers/home.ts:316-335` (hydrateMissions export 유지하지만 import는 missions.ts에서)
- Modify: `src/main.ts` (handlers 등록 추가)
- Test: `tests/unit/handlers-missions.spec.ts`

- [ ] **Step 1: 실패 테스트 작성**

`tests/unit/handlers-missions.spec.ts`:

```ts
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { JSDOM } from 'jsdom';

describe('handlers/missions tab-changed wiring', () => {
  beforeEach(() => {
    const dom = new JSDOM('<!doctype html><body><div id="missionsContainer"></div></body>');
    (globalThis as any).document = dom.window.document;
    (globalThis as any).window = dom.window;
    (globalThis as any).localStorage = dom.window.localStorage;
    localStorage.clear();
  });

  it('hydrates missions when nav switches to missions tab', async () => {
    const { registerMissionsHandlers } = await import('../../src/ui/handlers/missions');
    const renderSpy = vi.fn();
    // 의존성 주입 또는 spyOn renderMissionsSection — 모듈 mock 패턴 따라
    vi.doMock('../../src/ui/missions-section', () => ({
      renderMissionsSection: renderSpy,
    }));

    registerMissionsHandlers();
    document.dispatchEvent(new CustomEvent('dg:nav:tab-changed', { detail: { tab: 'missions' } }));

    // user 없으면 early return (renderSpy 호출 안 됨) — 본 테스트는 user 존재 시
    // user fixture를 setup해서 renderSpy 1회 호출 확인
    // (구체 fixture는 구현 시 jsdom user fixture 패턴 따라)
    expect(true).toBe(true); // placeholder — Step 3 구현 후 실 assertion
  });
});
```

> 주의: 위 테스트는 의존성 주입 패턴 적용 후 구체화. handlers/missions.ts 구현 모양이 결정된 다음 spec finalize.

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run tests/unit/handlers-missions.spec.ts`
Expected: FAIL — module not found

- [ ] **Step 3: `handlers/missions.ts` 작성**

`src/ui/handlers/missions.ts`:

```ts
import { on } from '../events';
import { hydrateMissions } from './home';

/**
 * Missions 탭 hydrate handler.
 * v3.14: 홈에서 분리됐으므로 tab-changed 시 별도 hydrate 필요.
 * hydrateMissions() 자체는 home.ts에 이미 export되어 있어 재사용.
 */
export function registerMissionsHandlers(): void {
  on('dg:nav:tab-changed', ({ tab }) => {
    if (tab === 'missions') {
      hydrateMissions();
    }
  });
}
```

- [ ] **Step 4: `main.ts`에서 handler 등록**

`src/main.ts`에 (기존 handler 등록 패턴 참고):

```ts
import { registerMissionsHandlers } from './ui/handlers/missions';
// ... 기존 register 호출들 사이에 추가
registerMissionsHandlers();
```

- [ ] **Step 5: 통과 확인**

Run: `npx vitest run tests/unit/handlers-missions.spec.ts && npm run build`
Expected: PASS + 빌드 성공

- [ ] **Step 6: 순환 import 검증 (codex P1-6)**

Run: `npx tsc --noEmit`
Expected: 0 error. 특히 `handlers/missions.ts → handlers/home.ts → handlers/missions.ts` 순환 없는지 확인.

만약 순환 발견 시 (`tsc`가 type-only면 통과하지만 esbuild/vite가 런타임 module record cycle 경고 가능): `hydrateMissions`를 `handlers/home.ts`에서 떼어내 별도 module `handlers/missions-hydrate.ts`로 분리하고, home.ts와 handlers/missions.ts 양쪽이 거기서 import.

- [ ] **Step 7: Commit**

```bash
git add src/ui/handlers/missions.ts src/main.ts tests/unit/handlers-missions.spec.ts
git commit -m "feat(v3.14 T4): missions handler — dg:nav:tab-changed 시 hydrateMissions 호출 (tsc cycle clean — codex P1-6)"
```

---

### Task 5: 홈에서 미션 컨테이너 + hydrateMissions 호출 제거

**Files:**
- Modify: `src/ui/tabs/home.ts:77-78` (마크업 제거)
- Modify: `src/ui/handlers/home.ts:310, 320-335, 806` (hydrateHome 내부 호출 제거 + submitAnswer 후 호출은 dirty event로 위임)
- Modify: `tests/unit/` 기존 home 관련 spec — `#missionsContainer`를 home에서 찾던 케이스 보정 (있으면)

- [ ] **Step 1: home.ts 마크업에서 미션 섹션 제거**

`src/ui/tabs/home.ts:76-78` 삭제:

```html
      <!-- Missions Section (v3.13 T9) -->
      <div id="missionsContainer"></div>
```

- [ ] **Step 2: hydrateHome 내부 hydrateMissions() 호출 제거**

`src/ui/handlers/home.ts:310` 한 줄 삭제:

```ts
// 삭제: hydrateMissions();
```

`hydrateHome` 결과:

```ts
export async function hydrateHome(container: HTMLElement): Promise<void> {
  void container;
  hydrateGreetingAndStreak();
  hydrateBriefings();
  await hydrateQuestion();
  hydrateChatHistory();
  applyTheme();
}
```

- [ ] **Step 3: submitAnswer 후 hydrateMissions 호출은 그대로 유지**

`src/ui/handlers/home.ts:806`은 `hydrateMissions()` 호출. 이는 이제 `#missionsContainer`가 missions 탭에만 존재하므로:

- 사용자가 home 탭에서 답변 제출 → `hydrateMissions()` 호출 → `document.getElementById('missionsContainer')` returns null → `renderMissionsSection` 미호출 ([home.ts:333-334](src/ui/handlers/home.ts#L333) 이미 null guard 존재)
- 단, `getActiveMissions(now, u)` + `tickMissionProgress` (in submitAnswer 내부 호출들)은 그대로 동작 → 데이터 갱신 OK
- 다음에 사용자가 미션 탭 클릭 → `dg:nav:tab-changed` → `hydrateMissions()` → DOM 렌더 (Task 4)

→ **변경 없음** (null guard 덕분에 자연 동작). 단 주석 한 줄 갱신:

`src/ui/handlers/home.ts:806`:

```ts
hydrateMissions();  // v3.14: missions 탭이 mounted 안 된 경우 no-op (null guard). 데이터 sweep만 수행.
```

- [ ] **Step 4: vitest 회귀 확인**

Run: `npx vitest run`
Expected: 모든 기존 spec PASS (home에서 #missionsContainer를 찾던 spec이 있으면 fail — 발견 시 fixture 수정)

발견되면 해당 spec을 수정: `<div id="missionsContainer"></div>` 직접 주입 (jsdom)

- [ ] **Step 5: 빌드 + Playwright 회귀 확인**

Run: `npm run build && npx playwright test --reporter=line 2>&1 | tail -20`
Expected: 빌드 성공 + Playwright 91 passed (T1 + T5 누적)

> 주의: `tests/smoke/v3.13-missions.spec.ts`가 home 탭에서 `.mission-card--completed` 검증 — fail 가능. 그러면 spec을 missions 탭으로 nav한 뒤 검증하도록 수정 (Task 11에서 본격 정리).

- [ ] **Step 6: Commit**

```bash
git add src/ui/tabs/home.ts src/ui/handlers/home.ts
git commit -m "refactor(v3.14 T5): remove missions section from home — missionsContainer/hydrateMissions 호출 제거 (null guard 자연 no-op)"
```

---

### Task 6: `briefing-view` 미션 트리거 wiring (T10-B 1/2) — single-now param 도입 + try/catch

**Files:**
- Modify: `src/ui/handlers/missions-triggers.ts` (fireTrigger + 3 wrappers에 optional `now?: Date` 파라미터 추가 — codex P1-7)
- Modify: `src/ui/handlers/home.ts` (renderBriefingCard click 핸들러 + import)
- Test: `tests/unit/home-briefing-view-trigger.spec.ts`
- Test: `tests/unit/missions-triggers-now-param.spec.ts` (single-now invariant 검증)

- [ ] **Step 1: missions-triggers.ts 단일-now 파라미터 추가 (codex P1-7)**

기존:
```ts
function fireTrigger(action: MissionAction): void {
  const u = getCachedUser();
  if (!u) return;
  const now = new Date();   // 매 호출 새 capture — 두 trigger 연속 호출 시 KST 자정 race
  ...
}
export function fireBriefingViewTrigger(): void { fireTrigger('briefing-view'); }
export function fireArchiveRevisitTrigger(): void { fireTrigger('archive-revisit'); }
export function fireCrossInterestTrigger(): void { fireTrigger('cross-interest-view'); }
```

신규 — `now?: Date` 파라미터 추가:
```ts
function fireTrigger(action: MissionAction, now: Date = new Date()): void {
  const u = getCachedUser();
  if (!u) return;
  // single-now (v3.13.1 T14 / codex P1-1): caller 가 now 전달 시 두 trigger 연속 호출이
  // 같은 KST window를 공유 — 자정 경계에서 다른 날짜로 sweep되는 race 차단.
  getActiveMissions(now, u);
  const prev = takeSnapshot();
  tickMissionProgress(u, action, now);
  saveUser(u);
  const curr = takeSnapshot();
  runSweep(prev, curr);
}
export function fireBriefingViewTrigger(now?: Date): void { fireTrigger('briefing-view', now); }
export function fireArchiveRevisitTrigger(now?: Date): void { fireTrigger('archive-revisit', now); }
export function fireCrossInterestTrigger(now?: Date): void { fireTrigger('cross-interest-view', now); }
```

`tests/unit/missions-triggers-now-param.spec.ts`:
```ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { JSDOM } from 'jsdom';

describe('missions-triggers single-now invariant (codex P1-7)', () => {
  beforeEach(() => {
    const dom = new JSDOM('<!doctype html><body></body>');
    (globalThis as any).document = dom.window.document;
    (globalThis as any).window = dom.window;
    (globalThis as any).localStorage = dom.window.localStorage;
    localStorage.clear();
    localStorage.setItem('dg.user', JSON.stringify({
      name: 'T', interests: ['ai_ml'], onboardedAt: 1, streak: 0,
      lastActiveDate: '', xp: 0, earnedBadges: {}, gamificationMigrated: true,
      schemaVersion: 3,
    }));
  });

  it('two triggers called with shared `now` use the same Date instance', async () => {
    // tickMissionProgress를 spy해서 받은 now가 동일 instance인지 확인
    const tickSpy = vi.fn();
    vi.doMock('../../src/state/missionEngine', async () => {
      const actual = await vi.importActual<any>('../../src/state/missionEngine');
      return { ...actual, tickMissionProgress: tickSpy };
    });
    const { fireBriefingViewTrigger, fireCrossInterestTrigger } = await import('../../src/ui/handlers/missions-triggers');
    const sharedNow = new Date('2026-05-01T14:59:59.999+09:00');
    fireBriefingViewTrigger(sharedNow);
    fireCrossInterestTrigger(sharedNow);
    expect(tickSpy.mock.calls[0][2]).toBe(sharedNow);
    expect(tickSpy.mock.calls[1][2]).toBe(sharedNow);
  });
});
```

- [ ] **Step 2: 실행 → 통과 확인**

Run: `npx vitest run tests/unit/missions-triggers-now-param.spec.ts`
Expected: PASS (signature 변경만으로 충분 — 기존 caller는 default param으로 동작)

기존 archive trigger 회귀 확인:
Run: `npx vitest run`
Expected: 기존 missions/archive spec 회귀 0

- [ ] **Step 3: 실패 테스트 작성 — briefing-view click**

`tests/unit/home-briefing-view-trigger.spec.ts`:

```ts
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { JSDOM } from 'jsdom';

describe('briefing-view trigger wiring', () => {
  beforeEach(() => {
    const dom = new JSDOM('<!doctype html><body></body>');
    (globalThis as any).document = dom.window.document;
    (globalThis as any).window = dom.window;
    (globalThis as any).sessionStorage = dom.window.sessionStorage;
    sessionStorage.clear();
  });

  it('fires fireBriefingViewTrigger on first click for a card', async () => {
    const fireSpy = vi.fn();
    vi.doMock('../../src/ui/handlers/missions-triggers', () => ({
      fireBriefingViewTrigger: fireSpy,
      fireCrossInterestTrigger: vi.fn(),
      fireArchiveRevisitTrigger: vi.fn(),
    }));

    const { renderBriefingCard } = await import('../../src/ui/handlers/home');
    const card = renderBriefingCard({
      id: 'b1', date: '2026-05-01', title: 'T', summary: 'S',
      url: 'https://x.test/1', read: false, scrapped: false, sourceTitle: 'X',
    } as any, 0);

    document.body.append(card);
    card.querySelector<HTMLAnchorElement>('a.card-main')!.click();

    expect(fireSpy).toHaveBeenCalledTimes(1);
  });

  it('does NOT fire twice for the same card on the same KST day', async () => {
    // 동일 카드 재클릭 — sessionStorage 'briefing-view-fired-${KSTDateIso}-0' 로 차단
    const fireSpy = vi.fn();
    vi.doMock('../../src/ui/handlers/missions-triggers', () => ({
      fireBriefingViewTrigger: fireSpy,
      fireCrossInterestTrigger: vi.fn(),
      fireArchiveRevisitTrigger: vi.fn(),
    }));

    const { renderBriefingCard } = await import('../../src/ui/handlers/home');
    const card = renderBriefingCard({
      id: 'b1', date: '2026-05-01', title: 'T', summary: 'S',
      url: 'https://x.test/1', read: false, scrapped: false, sourceTitle: 'X',
    } as any, 0);
    document.body.append(card);

    const link = card.querySelector<HTMLAnchorElement>('a.card-main')!;
    link.click();
    link.click();
    link.click();

    expect(fireSpy).toHaveBeenCalledTimes(1);
  });
});
```

- [ ] **Step 4: 실패 확인 (briefing-view click)**

Run: `npx vitest run tests/unit/home-briefing-view-trigger.spec.ts`
Expected: FAIL — `fireSpy not called` (현재 TODO 상태)

- [ ] **Step 5: `home.ts:455-466` TODO 자리에 wire-up — try/catch + 성공 후 flag (codex P1-3, archive 패턴 mirror)**

`src/ui/handlers/home.ts` 상단 import에 추가:

```ts
import { fireBriefingViewTrigger, fireCrossInterestTrigger } from './missions-triggers';
import { getKSTDateIso } from '../../utils/dates';  // archive.ts:193과 동일 helper
import { getSaveErrorMessage } from '../../state/user';  // 이미 있을 가능성 — 검색 후 합치기
```

`renderBriefingCard` click 핸들러 교체:

```ts
  // Mark as read when the link is opened
  main.addEventListener('click', () => {
    setRead(idx);
    card.dataset['read'] = 'true';

    // v3.14 T6/T7: 두 trigger가 같은 KST window를 공유하도록 single-now 캡처 (codex P1-7).
    // archive 패턴 (archive.ts:194-201) — 성공 후에만 sessionStorage flag 세팅, 실패 시 retry 허용.
    const now = new Date();
    const todayIso = getKSTDateIso(now);

    // briefing-view trigger (daily-briefing-5, target=5)
    const viewKey = `briefing-view-fired-${todayIso}-${idx}`;
    if (!sessionStorage.getItem(viewKey)) {
      try {
        fireBriefingViewTrigger(now);
        sessionStorage.setItem(viewKey, '1');
      } catch (err) {
        showToast(getSaveErrorMessage(err));   // Quota 등 — flag 미세팅 → 다음 click 재시도
      }
    }

    // v3.14 T7에서 cross-interest wiring 같은 try/catch 패턴으로 추가 예정 (now 공유)
  });
```

- [ ] **Step 6: 통과 확인**

Run: `npx vitest run tests/unit/home-briefing-view-trigger.spec.ts`
Expected: 2 PASS

- [ ] **Step 7: N=5 stability 확인**

Run: `for i in {1..5}; do npx vitest run tests/unit/home-briefing-view-trigger.spec.ts tests/unit/missions-triggers-now-param.spec.ts || break; done`
Expected: 5/5 PASS (no flake)

- [ ] **Step 8: Commit**

```bash
git add src/ui/handlers/missions-triggers.ts src/ui/handlers/home.ts tests/unit/missions-triggers-now-param.spec.ts tests/unit/home-briefing-view-trigger.spec.ts
git commit -m "feat(v3.14 T6): wire briefing-view trigger + fireTrigger now? param (codex P1-7) + try/catch flag-after-fire (codex P1-3)"
```

---

### Task 7: `cross-interest-view` 미션 트리거 wiring (T10-B 2/2) — single-now 공유 + false-negative 검증

**Files:**
- Modify: `src/ui/handlers/home.ts` (위 click 핸들러에 cross-interest 분기 추가, T6에서 캡처한 `now` 재사용 — codex P1-7)
- Test: `tests/unit/home-cross-interest-trigger.spec.ts` (false-negative + match + dedup 3 case)

- [ ] **Step 1: 실패 테스트 작성**

`tests/unit/home-cross-interest-trigger.spec.ts`:

```ts
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { JSDOM } from 'jsdom';

describe('cross-interest-view trigger wiring', () => {
  beforeEach(() => {
    const dom = new JSDOM('<!doctype html><body></body>');
    (globalThis as any).document = dom.window.document;
    (globalThis as any).window = dom.window;
    (globalThis as any).sessionStorage = dom.window.sessionStorage;
    (globalThis as any).localStorage = dom.window.localStorage;
    localStorage.clear();
    sessionStorage.clear();
    // user fixture: interests = ['ai_ml']만 선택
    localStorage.setItem('dg.user', JSON.stringify({
      name: 'T', interests: ['ai_ml'], onboardedAt: 1, streak: 0,
      lastActiveDate: '', xp: 0, earnedBadges: {}, gamificationMigrated: true,
      schemaVersion: 3,
    }));
  });

  it('fires fireCrossInterestTrigger when card text matches NO user interest', async () => {
    const fireSpy = vi.fn();
    vi.doMock('../../src/ui/handlers/missions-triggers', () => ({
      fireBriefingViewTrigger: vi.fn(),
      fireCrossInterestTrigger: fireSpy,
      fireArchiveRevisitTrigger: vi.fn(),
    }));

    const { renderBriefingCard } = await import('../../src/ui/handlers/home');
    const card = renderBriefingCard({
      id: 'b1', date: '2026-05-01', title: '인사제도 개편 동향',
      summary: '...', url: 'https://x.test/1', read: false, scrapped: false,
      sourceTitle: 'HR Insider',
    } as any, 0);
    document.body.append(card);
    card.querySelector<HTMLAnchorElement>('a.card-main')!.click();

    expect(fireSpy).toHaveBeenCalledTimes(1);
  });

  // codex P1-4: 짧은 한국어 RSS 텍스트에서 interestKeywords 과다 매칭 가능성 검증.
  // ai_ml interest 사용자가 '재테크 ETF 동향' 같은 무관 카드 클릭 시 cross-interest로 판정돼야 함.
  it('cross-interest fires for realistic short Korean RSS title with no interest keyword', async () => {
    const fireSpy = vi.fn();
    vi.doMock('../../src/ui/handlers/missions-triggers', () => ({
      fireBriefingViewTrigger: vi.fn(),
      fireCrossInterestTrigger: fireSpy,
      fireArchiveRevisitTrigger: vi.fn(),
    }));
    const { renderBriefingCard } = await import('../../src/ui/handlers/home');
    const card = renderBriefingCard({
      id: 'b3', date: '2026-05-01',
      title: '재테크 ETF 시장 동향',
      summary: '연금저축과 IRP를 활용한 자산배분 전략',
      url: 'https://finance.test/1', read: false, scrapped: false,
      sourceTitle: '경제 일보',
    } as any, 0);
    document.body.append(card);
    card.querySelector<HTMLAnchorElement>('a.card-main')!.click();
    expect(fireSpy).toHaveBeenCalledTimes(1);
  });

  // codex P1-4: interest='ai_ml' 인 사용자가 영어 'AI' 토큰만 본문에 있는 카드 클릭 시 매칭 — fire 안 함.
  it('cross-interest does NOT fire when only the bare keyword (AI) appears in summary', async () => {
    const fireSpy = vi.fn();
    vi.doMock('../../src/ui/handlers/missions-triggers', () => ({
      fireBriefingViewTrigger: vi.fn(),
      fireCrossInterestTrigger: fireSpy,
      fireArchiveRevisitTrigger: vi.fn(),
    }));
    const { renderBriefingCard } = await import('../../src/ui/handlers/home');
    const card = renderBriefingCard({
      id: 'b4', date: '2026-05-01',
      title: '신기술 동향',
      summary: 'GPT 같은 AI 도구가 업무에 미치는 영향',
      url: 'https://tech.test/1', read: false, scrapped: false,
      sourceTitle: 'Tech News',
    } as any, 0);
    document.body.append(card);
    card.querySelector<HTMLAnchorElement>('a.card-main')!.click();
    expect(fireSpy).not.toHaveBeenCalled();
  });

  it('does NOT fire when card text matches a user interest', async () => {
    const fireSpy = vi.fn();
    vi.doMock('../../src/ui/handlers/missions-triggers', () => ({
      fireBriefingViewTrigger: vi.fn(),
      fireCrossInterestTrigger: fireSpy,
      fireArchiveRevisitTrigger: vi.fn(),
    }));

    const { renderBriefingCard } = await import('../../src/ui/handlers/home');
    const card = renderBriefingCard({
      id: 'b2', date: '2026-05-01', title: 'AI 모델 최신 동향',
      summary: 'ML 엔지니어링', url: 'https://x.test/2', read: false, scrapped: false,
      sourceTitle: 'AI Weekly',
    } as any, 1);
    document.body.append(card);
    card.querySelector<HTMLAnchorElement>('a.card-main')!.click();

    expect(fireSpy).not.toHaveBeenCalled();
  });

  it('fires AT MOST once per KST day across multiple cross-interest cards', async () => {
    const fireSpy = vi.fn();
    vi.doMock('../../src/ui/handlers/missions-triggers', () => ({
      fireBriefingViewTrigger: vi.fn(),
      fireCrossInterestTrigger: fireSpy,
      fireArchiveRevisitTrigger: vi.fn(),
    }));

    const { renderBriefingCard } = await import('../../src/ui/handlers/home');
    for (let i = 0; i < 3; i++) {
      const card = renderBriefingCard({
        id: `b${i}`, date: '2026-05-01', title: '인사 정책 개편',
        summary: '회계', url: `https://x.test/${i}`, read: false, scrapped: false,
        sourceTitle: 'HR Insider',
      } as any, i);
      document.body.append(card);
      card.querySelector<HTMLAnchorElement>('a.card-main')!.click();
    }
    expect(fireSpy).toHaveBeenCalledTimes(1);
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run tests/unit/home-cross-interest-trigger.spec.ts`
Expected: 5 FAIL (3 base + 2 codex P1-4 false-negative cases)

- [ ] **Step 3: `home.ts` click 핸들러에 cross-interest 로직 추가 — T6에서 캡처한 `now` 재사용**

상단 import 추가:

```ts
import { interestKeywords } from '../../utils/interestKeywords';
import { getCachedUser } from '../../state/user';  // 이미 있으면 합치기
```

`renderBriefingCard` click 핸들러를 갱신 (T6 결과에 cross-interest 분기 append, **`now`는 T6의 single-now 그대로 재사용**):

```ts
  main.addEventListener('click', () => {
    setRead(idx);
    card.dataset['read'] = 'true';

    // T6/T7 single-now (codex P1-7) — 두 trigger가 같은 KST window 공유
    const now = new Date();
    const todayIso = getKSTDateIso(now);

    // T6: briefing-view trigger
    const viewKey = `briefing-view-fired-${todayIso}-${idx}`;
    if (!sessionStorage.getItem(viewKey)) {
      try {
        fireBriefingViewTrigger(now);
        sessionStorage.setItem(viewKey, '1');
      } catch (err) {
        showToast(getSaveErrorMessage(err));
      }
    }

    // T7: cross-interest-view trigger — 일별 1회 dedup
    // 카드 텍스트(sourceTitle/title/summary)가 user.interests 어느 것과도 매칭 안 되면 cross-interest 판정.
    const crossKey = `cross-interest-fired-${todayIso}`;
    if (!sessionStorage.getItem(crossKey)) {
      const u = getCachedUser();
      const interests = u?.interests ?? [];
      if (interests.length > 0) {
        const hay = `${b.sourceTitle ?? ''} ${b.title} ${b.summary}`.toLowerCase();
        const matched = interests.some(i => interestKeywords(i).some(k => hay.includes(k)));
        if (!matched) {
          try {
            fireCrossInterestTrigger(now);   // shared now
            sessionStorage.setItem(crossKey, '1');
          } catch (err) {
            showToast(getSaveErrorMessage(err));
          }
        }
      }
    }
  });
```

- [ ] **Step 4: 통과 확인**

Run: `npx vitest run tests/unit/home-cross-interest-trigger.spec.ts`
Expected: 5 PASS (3 base + 2 codex P1-4)

- [ ] **Step 5: 매칭 의외 케이스 발견 시 결정 분기**

Step 4의 P1-4 fail이 발생하면:
- "AI" 같은 짧은 영어 토큰이 RSS 한국어 본문에 자주 나타나 false-positive (interest match로 잘못 판정 → cross-interest fire 안 됨) 가능성 → 사용자 확인 필요
- 옵션 A: keyword min-length filter (예: `k.length >= 3` && 한국어는 통과) — interestKeywords 출력 후 filter
- 옵션 B: 테스트의 fixture가 비현실적 → 테스트 자체 보정
- → **사용자에게 옵션 제시 후 결정** (CLAUDE.md feedback_review_p2_autonomy 준수)

- [ ] **Step 6: N=5 stability 확인**

Run: `for i in {1..5}; do npx vitest run tests/unit/home-cross-interest-trigger.spec.ts tests/unit/home-briefing-view-trigger.spec.ts tests/unit/missions-triggers-now-param.spec.ts || break; done`
Expected: 5/5 PASS

- [ ] **Step 7: Commit**

```bash
git add src/ui/handlers/home.ts tests/unit/home-cross-interest-trigger.spec.ts
git commit -m "feat(v3.14 T7): wire cross-interest-view trigger — single-now shared with T6 + false-negative coverage (codex P1-4)"
```

---

### Task 8: `DEFERRED_DEFIDS`에서 두 미션 graduation

**Files:**
- Modify: `src/state/missionEngine.ts:72-77`
- Test: `tests/unit/missionEngine.spec.ts` 또는 신규 `tests/unit/missionEngine-pool.spec.ts`

- [ ] **Step 1: 실패 테스트 작성**

`tests/unit/missionEngine-pool.spec.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { pickDailyMissions } from '../../src/state/missionEngine';

describe('pickDailyMissions pool size after T10-B graduation', () => {
  it('pool now contains 7 daily entries (DEFERRED_DEFIDS empty)', () => {
    // Different seeds로 호출해서 union of picked.defId 가 7가지 모두 포함되는지 확인 (확률적 sampling)
    const seen = new Set<string>();
    for (let day = 1; day <= 200; day++) {
      const dateIso = `2026-${String((day % 12) + 1).padStart(2, '0')}-${String((day % 28) + 1).padStart(2, '0')}`;
      const picks = pickDailyMissions(dateIso, new Date(`${dateIso}T09:00:00+09:00`));
      picks.forEach(p => seen.add(p.defId));
    }
    expect(seen).toContain('daily-briefing-5');
    expect(seen).toContain('daily-cross-interest-1');
    expect(seen.size).toBe(7);
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run tests/unit/missionEngine-pool.spec.ts`
Expected: FAIL — seen.size === 5 (DEFERRED 2개 빠진 상태)

- [ ] **Step 3: `DEFERRED_DEFIDS` 비우기**

`src/state/missionEngine.ts:72-77` 교체:

```ts
// v3.14 T8: T10-B wiring 완료 (briefing-view: home.ts T6, cross-interest-view: home.ts T7).
// DEFERRED_DEFIDS는 비웠지만 const는 향후 deferred 용도 가능성을 위해 유지.
const DEFERRED_DEFIDS = new Set<string>();
```

- [ ] **Step 4: 통과 확인**

Run: `npx vitest run tests/unit/missionEngine-pool.spec.ts && npx vitest run`
Expected: 신규 spec PASS + 기존 missionEngine spec 회귀 0

- [ ] **Step 5: Commit**

```bash
git add src/state/missionEngine.ts tests/unit/missionEngine-pool.spec.ts
git commit -m "feat(v3.14 T8): graduate daily-briefing-5 + daily-cross-interest-1 from DEFERRED_DEFIDS — pool 5→7 entries"
```

---

### Task 9: 미션 탭 Playwright smoke

**Files:**
- Create: `tests/smoke/missions-tab.spec.ts`

- [ ] **Step 1: smoke spec 작성**

`tests/smoke/missions-tab.spec.ts`:

```ts
import { test, expect } from '@playwright/test';

test.describe('v3.14 Missions Tab', () => {
  test('bottom-nav shows 6 items including 미션 (mobile 375×667)', async ({ page }) => {
    await page.setViewportSize({ width: 375, height: 667 });
    await page.addInitScript(() => {
      localStorage.setItem('dg.user', JSON.stringify({
        name: 'T', interests: ['ai_ml'], onboardedAt: 1, streak: 0,
        lastActiveDate: '', xp: 0, earnedBadges: {}, gamificationMigrated: true,
        schemaVersion: 3,
      }));
    });
    await page.goto('/');
    await expect(page.locator('.bottom-nav .nav-item')).toHaveCount(6);
    const missionNav = page.locator('.bottom-nav .nav-item', { hasText: '미션' });
    await expect(missionNav).toBeVisible();
  });

  test('switching to missions tab renders #missionsSection with 3 period groups', async ({ page }) => {
    await page.addInitScript(() => {
      const today = new Intl.DateTimeFormat('en-CA', { timeZone: 'Asia/Seoul' }).format(new Date());
      localStorage.setItem('dg.user', JSON.stringify({
        name: 'T', interests: ['ai_ml'], onboardedAt: 1, streak: 0,
        lastActiveDate: '', xp: 0, earnedBadges: {}, gamificationMigrated: true,
        schemaVersion: 3,
        missions: {
          active: [{ defId: 'daily-answer-1', period: 'daily', windowStart: 0, progress: 0, completed: false }],
          cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 },
          lastDailySeed: today, lastWeeklyIso: '', currentMonthIso: today.slice(0, 7),
        },
      }));
    });
    await page.goto('/');
    await page.locator('.bottom-nav .nav-item', { hasText: '미션' }).click();
    await expect(page.locator('#missionsSection')).toBeVisible();
    await expect(page.locator('.mission-group')).toHaveCount(1); // daily만 fixture에 active
    await expect(page.locator('.mission-card')).toContainText('답변 1개');
  });
});
```

- [ ] **Step 2: 실행**

Run: `npx playwright test tests/smoke/missions-tab.spec.ts --reporter=line`
Expected: 2 PASS

- [ ] **Step 3: N=5 stability**

Run: `for i in {1..5}; do npx playwright test tests/smoke/missions-tab.spec.ts --reporter=line || break; done`
Expected: 5/5 PASS

- [ ] **Step 4: Commit**

```bash
git add tests/smoke/missions-tab.spec.ts
git commit -m "test(v3.14 T9): missions tab smoke — 6탭 nav + 미션 탭 클릭 → #missionsSection 가시"
```

---

### Task 10: `tests/smoke/v3.13-missions.spec.ts` 갱신 (홈→미션 탭)

**Files:**
- Modify: `tests/smoke/v3.13-missions.spec.ts`

- [ ] **Step 1: 현재 동작 확인**

Run: `npx playwright test tests/smoke/v3.13-missions.spec.ts --reporter=line`
Expected: 0 또는 2 fail (기존 spec이 home에서 `.mission-card` 찾는다면 fail — Task 5 이후 미션 탭 이동 필요)

- [ ] **Step 2: spec 보정 — 답변 제출 후 미션 탭으로 이동해서 검증**

각 test case `await page.goto('/')` 다음에 `await page.locator('.bottom-nav .nav-item', { hasText: '미션' }).click()`를 답변 제출 **이후** 추가 (또는 처음에 한 번 클릭하고 답변 → 다시 미션 탭 이동).

구체 패턴:

```ts
// 답변 제출 (홈에서)
await page.locator('#answerArea').fill(ANSWER_TEXT);
await page.locator('#submitBtn').click();
await page.waitForTimeout(300); // submit 후 sweep 대기

// 미션 탭으로 이동해서 progress 검증
await page.locator('.bottom-nav .nav-item', { hasText: '미션' }).click();
await expect(page.locator('.mission-card--completed')).toBeVisible();
```

- [ ] **Step 3: 통과 확인**

Run: `npx playwright test tests/smoke/v3.13-missions.spec.ts --reporter=line`
Expected: 2 PASS

- [ ] **Step 4: 전체 Playwright 회귀**

Run: `npx playwright test --reporter=line 2>&1 | tail -10`
Expected: `93 passed` (89 baseline + 2 신규 missions-tab + 2 v3.13-missions 갱신, 단 v3.12 fix 2건은 베이스라인에 이미 합산)

- [ ] **Step 5: Commit**

```bash
git add tests/smoke/v3.13-missions.spec.ts
git commit -m "test(v3.14 T10): v3.13-missions spec — 답변 제출 후 미션 탭 nav → 검증 (홈에서 분리됨)"
```

---

### Task 11: Bundle delta 측정 + 종합 회귀

**Files:**
- (no code change)

- [ ] **Step 1: vitest 전체 + count 확인**

Run: `npx vitest run 2>&1 | tail -5`
Expected: PASS — count baseline 646 + 신규 (interestKeywords 3 + nav 2 + handlers-missions ~1 + briefing-view 2 + cross-interest 3 + missionEngine-pool 1 = ~12) ≈ **658 vitest**

- [ ] **Step 2: Playwright 전체**

Run: `npx playwright test --reporter=line 2>&1 | tail -10`
Expected: **93 passed** (89 + 2 missions-tab + 2 v3.13-missions 변경 (count 동일))

- [ ] **Step 3: 빌드 + bundle 측정**

Run: `npm run build 2>&1 | tail -20`
Expected: 빌드 성공. dist/assets/index-*.js gzip 크기 출력 확인.

baseline 31.47 kB → 신규 추정 32.0~32.3 kB. **DoD ceiling: +1.2 kB → 32.67 kB**

- [ ] **Step 4: lint**

Run: `npm run lint`
Expected: 0 error

- [ ] **Step 5: 결과 요약 commit (docs)**

`docs/superpowers/plans/2026-05-01-v3.14-missions-tab-and-view-triggers.md`에 결과 섹션 append:

```markdown
## §4. Implementation Results
- vitest: 646 → 658 (+12)
- Playwright: 91 → 93 (+2 missions-tab; v3.13-missions count 동일)
- Bundle: 31.47 → XX.XX kB gzip JS (Δ +X.XX, DoD ceiling +1.2 ✅)
- N=5 신규 specs: 5/5
- Schema migration: 0줄
- LocalStorage 키 변경: 0
- CSP 변경: 0
```

```bash
git add docs/superpowers/plans/2026-05-01-v3.14-missions-tab-and-view-triggers.md
git commit -m "docs(v3.14 T11): implementation results — bundle delta + test counts"
```

---

### Task 12: 독립 표준 코드 리뷰

**Trigger condition:** 11개 task 완료 (총 12 commits 예상). 사용자가 "표준 리뷰" 또는 "심층 리뷰" 결정.

- [ ] **Step 1: 사용자에게 리뷰 깊이 제안**

CLAUDE.md 기준:
- 12 commits + 새 탭 + T10-B feature → **표준 리뷰** (4~10 commits 범위 초과지만 되돌리기 어려운 작업 없음 → 표준 + 사용자 옵션으로 심층 가능)

권고: **표준 리뷰** (`superpowers:code-reviewer` 1회 — spec 준수 + 명백한 버그 중심).

심층이 필요한 trigger: bundle delta가 DoD ceiling 근접 시, 또는 v3.13처럼 codex 2nd pass가 P1 catch한 경험 반복 시.

- [ ] **Step 2: 리뷰 결과 P0/P1/P2 분류 + in-cycle 처리**

- P0 차단: 발견 시 즉시 fix
- P1 곧 수정: 같은 사이클에 fix
- P2 백로그: v3.15 carry-forward로 메모리 기록

- [ ] **Step 3: Final commit + memory update**

리뷰 fix가 있으면 별도 commit. 메모리 `project_daily_growth_v314.md` 신규 작성.

---

## §4. Self-Review Checklist (Plan 작성자 본인 점검)

**Spec coverage**:
- 미션 탭 분리 ✓ (T3, T4, T5, T9)
- T10-B carry-forward ✓ (T2, T6, T7, T8)
- Playwright 2 fail fix ✓ (T1)
- Bundle / migration / CSP 영향 ✓ (§1, §3 T11)
- bottom-nav 6탭 시각 검증 ✓ (T9 mobile viewport)

**Placeholder scan**: T4 Step 1 spec에 "placeholder — Step 3 구현 후 실 assertion" 명시 있음 — 이는 의존성 주입 패턴이 결정된 후 spec 보정해야 한다는 정확한 지침이라 placeholder가 아닌 작성 가이드. OK.

**Type consistency**:
- `TabId`에 `'missions'` 추가 — TABS array, switchTab 모두 동일 ✓
- `interestKeywords` signature `(id: string): string[]` — utils 추출 후 achievements에서 동일 시그니처로 import ✓
- `fireBriefingViewTrigger` / `fireCrossInterestTrigger` — missions-triggers.ts 기존 export 그대로 ✓

**리스크 요약**:
- T5에서 `submitAnswer` 후 `hydrateMissions()` 호출이 home 탭에서 no-op이 되는 점 — 데이터는 sweep되므로 정합성 OK, 단 사용자 즉시 피드백은 미션 탭 재방문 시. UX 손실 미미
- T10에서 v3.13-missions.spec 보정 시 기존 spec이 의존하던 `lastDailySeed` 트릭이 깨질 가능성 — 발견 시 fixture 재조정
- 6탭 mobile 라벨 폭은 추정상 OK이지만 Playwright T9에서 실측 — 잘림 발견 시 layout.css 미세 조정 필요 (Task 9 후속 hotfix step)

---

## §7. Implementation Results (T1~T11)

### Commits (12 task commits + 2 fixup)

```
8d078ed chore(v3.14 T11): lint cleanup — v3.14 신규 4파일 lint clean
8c39f78 test(v3.14 T10): v3.13-missions spec — 답변 제출 후 미션 탭 nav → 검증
9fc1eb2 test(v3.14 T9):  missions tab smoke — 6탭 nav + 미션 탭 클릭 → #missionsSection
0addb5f feat(v3.14 T8):  graduate daily-briefing-5 + daily-cross-interest-1 from DEFERRED_DEFIDS
0c7d163 feat(v3.14 T7):  wire cross-interest-view trigger — single-now shared with T6 (codex P1-4)
461ed6b feat(v3.14 T6):  wire briefing-view trigger + fireTrigger now? param (codex P1-7) + try/catch (codex P1-3)
0a65a57 refactor(v3.14 T5): remove missions section from home — null guard 자연 no-op
b513059 style(v3.14 T4 fix): rename registerMissionsHandlers → mountMissionsHandlers — mount* 컨벤션 정렬
f6123cc feat(v3.14 T4):  missions handler — dg:nav:tab-changed 시 hydrateMissions 호출 (tsc cycle clean — codex P1-6)
15ba882 feat(v3.14 T3):  add missions tab to nav — 🎯 6번째 entry + tabs/missions.ts
38c507b refactor(v3.14 T2): extract interestKeywords helper to utils — home·achievements 공유
e762a98 test(v3.14 T1): v3.12 game-feedback fixture sync — v3.13 mission 카테고리·뱃지 반영
```

### Metrics

| 지표 | Baseline (v3.13.1) | v3.14 결과 | Δ |
|---|---|---|---|
| vitest | 646 | 663 | **+17** (T2 +3, T3 +5, T4 +2, T6 +4, T7 +5, T8 +1; carry-forward 누락분 흡수) |
| Playwright | 91 (89 pass + 2 fail) | 93 pass / 0 fail | +2 (missions-tab.spec) + 2건 회복 (v3.12-game-feedback fixture sync) + 2건 마이그레이션 (v3.13-missions) |
| Bundle gzip JS | 31.47 kB | 31.74 kB | **+0.27 kB** (DoD ceiling +1.2 ✅) |
| Lint errors | 20 (v3.14 4 + pre-existing 16) | 16 (v3.14 0 + pre-existing 16) | -4 (v3.14 신규 부분 0 net 도입) |
| Schema migration | 0 | 0 | 0 |
| LocalStorage 키 변경 | 0 | 0 | 0 |
| CSP 변경 | 0 | 0 | 0 |
| N=5 stability (T6/T7/T9/T10) | n/a | 5/5 each | ✅ |

### Codex P1 사전 반영 검증

| Codex P1 | 적용 위치 | 검증 결과 |
|---|---|---|
| #3 try/catch + flag-after-fire (archive 패턴) | T6 home.ts click handler + T7 cross-interest 분기 | spec PASS, archive.ts:194-201 패턴 동일 |
| #4 cross-interest false-negative coverage | T7 home-cross-interest-trigger.spec.ts +2 case | 5/5 PASS, production 변경 불필요 (interestKeywords 정확) |
| #6 import 순환 (handlers/missions ↔ home) | T4 tsc --noEmit + grep | one-way edge (missions → home), cycle 없음 |
| #7 single-now race (KST 자정 경계) | T6 fireTrigger now? param + T7 shared now 재사용 | 1 spec 검증 (sharedNow instance 동일성) |

### Plan 작성자(controller) 메모해야 할 정정 사항

T6/T7/T9에서 implementer가 plan 의 fixture를 정정. 차기 plan 작성 시 반영:
- LocalStorage user KEY = `'user'` (NOT `'dg.user'`) — `src/state/user.ts:31`
- `onboardedAt`은 number/string 둘 다 허용 (isValidUserShape 검증 안 함) — 기존 spec 패턴 따라 number `1` 가능
- Mission schema 필드명 = `currentWeekIso` (NOT `lastWeeklyIso`) — `src/state/missionTypes.ts`
- `getKSTDateIso`는 `src/state/missionEngine.ts`에서 export (NOT `src/utils/dates.ts`)

### Pre-existing P2 후보 (T11 관찰)

- `tests/unit/ui/hydrate-badges.spec.ts:103` — 50ms lazy-import timeout flake (cold-start 시 surface, isolation 시 PASS) — v3.15 P2 후보
- `tests/smoke/v3.12-game-feedback.spec.ts` "답변 제출 → toast--badge (첫 답변) + xp-float visible" — first run flake on full suite, retry PASS — v3.15 P2 후보
- 16 pre-existing lint errors in tests (innerHTML 패턴) — v3.15 batch eslint-disable cleanup 후보

---

## §6. Codex 사전 리뷰 결과 (2026-05-01)

`codex:rescue` (gpt-5.4-codex default) — P0=0, P1=4, P2=3. 모두 in-cycle 사전 반영 완료.

| # | 분류 | 항목 | 반영 위치 |
|---|---|---|---|
| 1 | P2 | sidebar.ts가 TABS 재사용 OK / dead-export 도구 부재만 인지 | §0 보강, 액션 없음 |
| 2 | P2 | T1/T8 순서 OK, `lastDailySeed` 트릭 영향 없음 | 액션 없음 |
| 3 | **P1** | T6/T7 click handler — `try/catch` + 성공 후 sessionStorage flag (archive.ts:194-201 mirror) | T6 Step 5, T7 Step 3 |
| 4 | **P1** | T7 cross-interest false-negative 검증 부족 | T7 spec에 case 2건 추가 (재테크 ETF / 'AI'만 있는 본문) + Step 5 옵션 분기 |
| 5 | P2 | bundle delta +1.2 kB ceiling 안전 | §1 표 그대로 |
| 6 | **P1-UNVERIFIABLE** | handlers/missions ↔ handlers/home 순환 import 위험 | T4 Step 6 `tsc --noEmit` 추가, 순환 시 `handlers/missions-hydrate.ts` 분리 fallback 명시 |
| 7 | **P1** | 두 trigger 연속 호출 시 KST 자정 race (각자 `new Date()`) | T6 Step 1: `fireTrigger(action, now?)` 시그니처 추가 + T7에서 shared `now` 재사용 |

대화 trace 보존: codex `agentId: a511e5214c326c130` (필요 시 SendMessage로 follow-up).

---

## §5. Execution Handoff

**다음 결정 (사용자)**:
1. 버전 명칭 `v3.14` (권고) vs `v3.13.2 hotfix` 유지
2. 실행 모드 — **Subagent-Driven (권고)** 또는 Inline
3. T10-B 트리거 방식 — **click 기반 (권고)** vs IntersectionObserver (사용자가 "끝까지 보기"의 의미를 viewport entry로 보고 싶다면)

선택 주시면 Task 1부터 진행합니다.
