# DG v3.13.1 — P2 Cleanup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v3.13 T14 P2 8건 + per-task carry-forward 4건 + v3.13 사이클로 graduation 시점이 된 v3.12.1/v3.12 P2 2건 = **총 14건** 일괄 정리. 핵심은 미션 데이터 layer 무결성 강화 (`progressDates` deep clone, Snapshot 타입 강화, `assertNever` exhaustiveness) + UX edge guard (`hydrateMissions` Quota 토스트).

**Architecture:** 단일 파일 위주 소규모 변경 14건 — 신규 모듈 0개, 신규 함수 1개 (`parseKSTYMD`). 모든 변경은 기존 함수 시그니처/동작 유지하거나 strict하게 강화. saveUser 1회 invariant (v3.13 spec C6) 보존. TDD — 각 task 실패 테스트 → 구현 → 통과 → 커밋.

**Tech Stack:** TypeScript (strict) · Vite · Vitest jsdom · Playwright smoke (변동 없음).

**Spec:** 별도 spec 미작성 — 본 plan + memory `project_daily_growth_v313.md` T14 P2 backlog + v3.12.1 P2-NEW + v3.12 #3 분류가 spec 역할. v3.12.1 패턴 그대로 cleanup-style.

**Series:** v3.13 (게임화 시리즈 2/3) hotfix-style cleanup → v3.14 캐릭터/정원 진입 전 미션 layer 정리.

---

## §0 Existing Landscape (touched files + 라인)

### State layer (`src/state/`)

- **`missionEngine.ts:8-10`** — `getKSTDateIso(now)` returns 'YYYY-MM-DD'.
- **`missionEngine.ts:18`** — `const [y, m, d] = getKSTDateIso(now).split('-').map(Number) as [...]` (in `getKSTWeekIso`).
- **`missionEngine.ts:28`** — 동일 패턴 (in `getKSTWindowStart`). **dedup 대상 (carry-forward T3)**.
- **`missionEngine.ts:27-41`** — `getKSTWindowStart(now, period)` — `if/if/<implicit weekly>` 분기. **`assertNever` exhaustiveness 부재 (P2-5)**.
- **`missionEngine.ts:114-135`** — `getActiveMissions(now, user)` — user.missions를 mutate하고 활성 배열 반환. mutate 여부 caller가 모름 → `JSON.stringify` 비교 의존 (P2-2). `getMonthIso !== monthIso` 등 3 분기.
- **`missionEngine.ts:163-165`** — `tickMissionProgress`의 `const key = ${m.period}Count as 'dailyCount' | ...` cast — **assertNever 미사용 (P2-5)**. cumulative key 타입 안전화 대상.
- **`missionTypes.ts`** — `MissionInstance.progressDates?: string[]` mutable.
- **`migration.ts:87-110`** — `migrateUserToV3(raw: unknown)`. idempotent path (89-98)는 `m`을 직접 mutate (`m.cumulative = ...`, `m.cumulative.dailyCount = 0`) → **mutation→spread 패턴 권고 (carry-forward T1)**. `(raw as User & { schemaVersion?: number })` cast → unknown narrowing 권고 (carry-forward T1).
- **`achievements.ts:65-100`** — `takeSnapshot()` — `(u?.missions?.active ?? []).map(m => ({ ...m }))` shallow clone. **`progressDates` 배열은 같은 reference 공유** → tick의 `progressDates.push` 가 prev/curr 동시 mutate 가능 (P2-4 deep clone).
- **`achievements.ts:102-141`** — `detectEvents(prev, curr)` — line 131 `(curr.missionsActive ?? [])`, line 133 `(prev.missionsActive ?? [])` redundant `?? []` (P2-7). Snapshot 타입에서 미션 필드 ReadonlyArray로 강화하면 자연 해소.
- **`achievements.ts:168-188`** — `emitEvents(events)` switch 5 케이스 + **default 없음 (P2-6 — assertNever)**.
- **`gameTypes.ts`** — `Snapshot.missionsActive: MissionInstance[]` (carry-forward T5 ReadonlyArray<Readonly<MissionInstance>> 강화).

### UI layer (`src/ui/`)

- **`handlers/home.ts:316-324`** — `hydrateMissions()` — `getActiveMissions` 호출 + `JSON.stringify` 비교 + `saveUser(u)` 호출. **try/catch 없음, Quota 시 silent fail (P2-1)**.
- **`handlers/stats.ts:248-256`** — `CATEGORY_LABELS` Record + `CATEGORY_ORDER` 6요소 배열. `BadgeDef.category` union 6멤버와 1:1 매칭. **타입 일치 assertion 부재 (carry-forward T8)**.
- **`tabs/home.ts:77-78`** — `<div id="missionsContainer"></div>` 위치 = Question Section 다음, AI Chat 이전. **spec §7.1 mockup과 시퀀스 다름 (P2-8)**.

### State user (`src/state/user.ts`)

- **`user.ts:42-54`** (v3.12.1 T1 도입) — `isValidUserShape(u): u is User` — `interests: Array<unknown>` 검증 시 요소 타입 미체크 (Recommended P2-NEW-3).

### Migration (Recommended v3.12 #3)

- v3.12 #3 "lazy migrate atomic" — v3.12.1 메모에 *"v3.13 미션 mutate 패턴 점검과 함께"* 예약. v3.13 미션 mutate 점검 결과: `migrateUserToV3` idempotent path가 직접 mutate (carry-forward T1과 동일 root). T1 fix가 atomic 만족 → **이번 사이클에서 자연 해소 (별도 task 불필요, T3 사후 sync로 closeout)**.

### Spec (P2-8)

- **`docs/superpowers/specs/2026-04-30-v3.13-game-missions-design.md:469-481`** — §7.1 홈 탭 위치 mockup. 실제 DOM (`tabs/home.ts:77`) 은 Briefing → Question → **Missions** → AI Chat 순서. spec mockup은 헤더 → 답변 카드 → 미션 → 카드뉴스 → 관심분야. **mockup이 home tab 실제 구조와 무관** (그런 카드뉴스/관심분야 섹션은 home에 없음, 별도 탭). 결론 = retroactive sync (docs only, DOM 이동 안 함).

---

## §1 Task 분포 + 예상 vitest delta + 위험도

| Task | 카테고리 | 핵심 변경 | 예상 it +Δ | 위험도 |
|---|---|---|---:|---|
| T0 | Pre-flight (baseline + plan commit) | 측정 + commit | 0 | 0 |
| T1 | `parseKSTYMD` util 분리 (carry-forward T3) | missionEngine.ts 1 신규 함수 + 2 site replace | 0 | 0 (DRY refactor) |
| T2 | `getActiveMissions` → `{active, dirty}` (P2-2) | missionEngine + home.ts (caller) | +2 | low (caller 1) |
| T3 | `migrateUserToV3` idempotent spread (carry-forward T1, v3.12 #3 closeout) | migration.ts 89-98 mutation→spread | +1 | low (idempotent strict eq test) |
| T4 | `takeSnapshot` `progressDates` deep clone (P2-4) | achievements.ts 93 | +1 | low |
| T5 | `Snapshot.missionsActive` ReadonlyArray (carry-forward T5) + `?? []` 제거 (P2-7) | gameTypes.ts 1줄 + achievements.ts 131,133 | 0 | 0 (type-only) |
| T6 | `assertNever` exhaustiveness 3곳 (P2-5) | missionEngine.ts:27, 163 + stats.ts:282 | +0 | low (TS compile-time) |
| T7 | `emitEvents` switch default: assertNever (P2-6) | achievements.ts:168-188 | 0 | low (TS compile-time) |
| T8 | Midnight rollover invariant 테스트 (P2-3) | tests + 주석 | +1 | 0 |
| T9 | `hydrateMissions` saveUser Quota 토스트 (P2-1) | home.ts:316-324 + 토스트 | +2 | low |
| T10 | `CATEGORY_ORDER` ↔ `BadgeDef.category` 커버리지 assertion (carry-forward T8) | tests/unit/state/badge-coverage.spec.ts 신규 | +1 | 0 |
| T11 | `isValidUserShape` interests 요소 type 검증 (Recommended P2-NEW-3) | user.ts:42-54 | +2 | low (graduation) |
| T12 | Spec §7.1 retroactive sync (P2-8) | spec docs only | 0 | 0 |
| T13 | DoD retro sync — v3.13 spec/plan/memory bundle baseline 확인 | docs only | 0 | 0 |
| T14 | Verification + 독립 표준 리뷰 dispatch | 측정 + 리뷰 | 0 | 0 |
| **합계** | | | **+10** | |

**DoD**:
- vitest +10 ±2 (635 → ~645)
- Playwright smoke 변동 0 (91 유지)
- tsc strict 0 errors
- bundle gzip JS main ±0.2 kB (T0 baseline = **31.28 kB** → 31.08 ~ 31.48 kB 범위)
- 독립 표준 리뷰 (codex) APPROVE (P0=0)

---

## Phase 0 — Pre-flight

### Task 0: Baseline 확정 + plan commit

**Files:**
- Create: `docs/superpowers/plans/2026-05-01-v3.13.1-p2-cleanup-plan.md` (본 파일)

- [ ] **Step 1: vitest baseline 확인**

Run: `npx vitest run --reporter=basic 2>&1 | tail -3`
Expected: `Test Files 94 passed (94)` + `Tests 635 passed (635)`. (다르면 §1 표 갱신)

- [ ] **Step 2: tsc strict 확인**

Run: `npx tsc --noEmit`
Expected: 0 errors. (errors 있으면 v3.13.1 cycle 시작 부적합 — stop)

- [ ] **Step 3: bundle baseline 측정**

Run: `npm run build 2>&1 | grep "dist/assets/index.*\.js"`
Expected: `index-*.js` gzip 크기 기록 (예: 31.26 kB). 본 plan §1 DoD `±0.2 kB`의 anchor.

- [ ] **Step 4: Plan 파일 commit**

```bash
git add docs/superpowers/plans/2026-05-01-v3.13.1-p2-cleanup-plan.md
git commit -m "docs(v3.13.1 T0): plan — v3.13 P2 cleanup 14건 (T14 P2 8 + carry-forward 4 + recommended 2)"
```

---

## Phase 1 — missionEngine refactor + caller 정합

### Task 1: `parseKSTYMD` util 분리 (carry-forward T3)

**Files:**
- Modify: `src/state/missionEngine.ts:18, 28`
- No test 신규 — 기존 `getKSTWeekIso` / `getKSTWindowStart` 테스트가 회귀 보장.

**Background:** `missionEngine.ts:18` 과 `missionEngine.ts:28` 둘 다 `const [y, m, d] = getKSTDateIso(now).split('-').map(Number) as [number, number, number]` 패턴을 그대로 반복. DRY.

- [ ] **Step 1: `parseKSTYMD` 함수 추가**

`src/state/missionEngine.ts:8` 다음 (export `getKSTDateIso` 직후), 또는 파일 상단 helper 영역에 추가:

```ts
function parseKSTYMD(now: Date): [number, number, number] {
  return getKSTDateIso(now).split('-').map(Number) as [number, number, number];
}
```

- [ ] **Step 2: `getKSTWeekIso` line 18 교체**

Before:
```ts
const [y, m, d] = getKSTDateIso(now).split('-').map(Number) as [number, number, number];
```

After:
```ts
const [y, m, d] = parseKSTYMD(now);
```

(주의: 변수명 `m`이 `month` index — 1-based, 그대로 유지.)

- [ ] **Step 3: `getKSTWindowStart` line 28 교체**

동일하게 `parseKSTYMD(now)` 로 교체.

- [ ] **Step 4: 기존 테스트로 회귀 검증**

Run: `npx vitest run tests/unit/state/missionEngine 2>&1 | tail -5`
Expected: 모든 테스트 PASS (delta 0).

- [ ] **Step 5: 전체 vitest 회귀 검증**

Run: `npx vitest run --reporter=basic 2>&1 | tail -3`
Expected: `Tests 635 passed (635)` (delta 0).

- [ ] **Step 6: Commit**

```bash
git add src/state/missionEngine.ts
git commit -m "refactor(v3.13.1 T1): extract parseKSTYMD util — dedup KST date split (carry-forward T3)"
```

---

### Task 2: `getActiveMissions` → `{active, dirty}` 반환 (P2-2)

**Files:**
- Modify: `src/state/missionEngine.ts:114-135`
- Modify: `src/ui/handlers/home.ts:316-324` (caller)
- Test: `tests/unit/state/missionEngine.spec.ts` (또는 기존 spec — `getActiveMissions` covers)

**Background:** 현재 `hydrateMissions`는 `JSON.stringify(u.missions)` 전후 비교로 lazy regen 발생 여부를 확인. O(n) overhead + `progressDates`/`active` 배열 길이 증가 시 비용 ↑. `getActiveMissions`가 mutate 사실을 직접 알리는 게 정직.

- [ ] **Step 1: 실패 테스트 작성**

`tests/unit/state/missionEngine.spec.ts` (기존 file에 it 2개 추가):

```ts
it('getActiveMissions: lastDailySeed 갱신 시 dirty=true', () => {
  const u: User = makeFreshV3User();
  u.missions.lastDailySeed = '';
  const result = getActiveMissions(new Date('2026-05-01T03:00:00Z'), u);
  expect(result.dirty).toBe(true);
  expect(result.active.length).toBeGreaterThan(0);
});

it('getActiveMissions: 모든 ISO 일치 시 dirty=false (lazy regen 없음)', () => {
  const now = new Date('2026-05-01T03:00:00Z');
  const u: User = makeFreshV3User();
  // 1차 호출로 모든 ISO 채움
  getActiveMissions(now, u);
  // 2차 호출은 dirty=false
  const result = getActiveMissions(now, u);
  expect(result.dirty).toBe(false);
});
```

(`makeFreshV3User` helper는 기존 test fixture에 있음 — 없으면 inline.)

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run tests/unit/state/missionEngine 2>&1 | tail -10`
Expected: 위 2 it FAIL — `result.dirty` undefined (현재 반환은 `MissionInstance[]`).

- [ ] **Step 3: 시그니처 + 본문 변경**

`src/state/missionEngine.ts:114-135` 교체:

```ts
export function getActiveMissions(
  now: Date,
  user: User,
): { active: MissionInstance[]; dirty: boolean } {
  const todayIso = getKSTDateIso(now);
  const weekIso = getKSTWeekIso(now);
  const monthIso = getKSTMonthIso(now);
  let dirty = false;

  if (user.missions.lastDailySeed !== todayIso) {
    const newDaily = pickDailyMissions(todayIso, now);
    user.missions.active = user.missions.active.filter(m => m.period !== 'daily').concat(newDaily);
    user.missions.lastDailySeed = todayIso;
    dirty = true;
  }
  if (user.missions.currentWeekIso !== weekIso) {
    const newWeekly = makeFixed(WEEKLY_FIXED, 'weekly', now);
    user.missions.active = user.missions.active.filter(m => m.period !== 'weekly').concat(newWeekly);
    user.missions.currentWeekIso = weekIso;
    dirty = true;
  }
  if (user.missions.currentMonthIso !== monthIso) {
    const newMonthly = makeFixed(MONTHLY_FIXED, 'monthly', now);
    user.missions.active = user.missions.active.filter(m => m.period !== 'monthly').concat(newMonthly);
    user.missions.currentMonthIso = monthIso;
    dirty = true;
  }
  return { active: user.missions.active, dirty };
}
```

- [ ] **Step 4: caller (`hydrateMissions`) 변경**

`src/ui/handlers/home.ts:316-324`:

Before:
```ts
function hydrateMissions(): void {
  const u = getCachedUser();
  if (!u) return;
  const before = JSON.stringify(u.missions);
  const active = getActiveMissions(new Date(), u);
  if (JSON.stringify(u.missions) !== before) saveUser(u);  // lazy regen이 발생한 경우에만 저장
  const root = document.getElementById('missionsContainer');
  if (root) renderMissionsSection(root, active);
}
```

After:
```ts
function hydrateMissions(): void {
  const u = getCachedUser();
  if (!u) return;
  const { active, dirty } = getActiveMissions(new Date(), u);
  if (dirty) saveUser(u);  // lazy regen이 발생한 경우에만 저장
  const root = document.getElementById('missionsContainer');
  if (root) renderMissionsSection(root, active);
}
```

- [ ] **Step 5: 다른 caller 검색 + 갱신**

Run: `grep -rn "getActiveMissions" src/ tests/ 2>&1`
Expected: 모든 호출이 `{ active, dirty }` 디스트럭처 또는 `.active` 접근으로 갱신됐는지 확인. caller가 `hydrateMissions` 단 1곳이면 통과. test fixture 호출도 update.

- [ ] **Step 6: 테스트 통과 확인**

Run: `npx vitest run --reporter=basic 2>&1 | tail -3`
Expected: `Tests 637 passed (637)` (+2).

- [ ] **Step 7: tsc strict 확인**

Run: `npx tsc --noEmit 2>&1 | tail -5`
Expected: 0 errors.

- [ ] **Step 8: Commit**

```bash
git add src/state/missionEngine.ts src/ui/handlers/home.ts tests/unit/state/missionEngine.spec.ts
git commit -m "refactor(v3.13.1 T2): getActiveMissions returns {active, dirty} — drop JSON.stringify caller compare (P2-2)"
```

---

### Task 3: `migrateUserToV3` idempotent path mutation→spread (carry-forward T1, v3.12 #3 closeout)

**Files:**
- Modify: `src/state/migration.ts:87-100`
- Test: `tests/unit/state/user-migration.spec.ts` (기존 file +1 it)

**Background:** 현재 idempotent path (line 89-98)는 `m.cumulative = m.cumulative ?? {...}` 등 raw 객체를 직접 mutate. 외부 caller가 raw를 보유하고 있다면 silent mutate 위험. spread 기반 immutable 패턴이 atomic guarantee 명확.

또한 line 88 `(raw as User & { schemaVersion?: number })` cast 는 raw가 unknown임에도 단일 cast — type-narrow 패턴으로 변경하면 안전성 ↑.

- [ ] **Step 1: 실패 테스트 작성**

`tests/unit/state/user-migration.spec.ts` 에 추가:

```ts
it('migrateUserToV3: idempotent path 가 raw 객체를 mutate하지 않는다 (atomic)', () => {
  const raw: any = {
    name: 'A', interests: [], streak: 0, lastActiveDate: '', xp: 0,
    earnedBadges: {}, gamificationMigrated: true, schemaVersion: 3,
    missions: {
      active: [],
      cumulative: { dailyCount: 1, weeklyCount: 2, monthlyCount: 3 },
      lastDailySeed: '2026-05-01',
      currentWeekIso: '2026-W18',
      currentMonthIso: '2026-05',
    },
  };
  const cumRef = raw.missions.cumulative;
  const missionsRef = raw.missions;
  const result = migrateUserToV3(raw);
  // 결과는 같은 값이지만 raw의 missions / cumulative 객체는 변형되지 않아야 한다
  expect(result.missions.cumulative.dailyCount).toBe(1);
  // raw의 cumulative reference는 mutate되지 않음 (deep equality)
  expect(raw.missions.cumulative).toBe(cumRef);
  expect(raw.missions).toBe(missionsRef);
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run tests/unit/state/user-migration 2>&1 | tail -10`
Expected: 위 it는 현재 PASS (idempotent path는 raw를 mutate하지만 같은 reference 유지). FAIL이 아니면 step 3로 넘어가서 spread로 바꾼 뒤에도 PASS 유지 확인 (사실상 이 it는 회귀 가드 역할).

`expect(raw.missions.cumulative).toBe(cumRef)` 는 reference 유지를 검증 — spread 후에도 같은 reference 유지 안 되면 FAIL이므로, 실패 테스트는 의미 다르게 구성:

**대체 (실패하는) 테스트**:

```ts
it('migrateUserToV3: idempotent 결과는 raw와 다른 missions reference를 반환한다 (immutable)', () => {
  const raw: any = {
    name: 'A', interests: [], streak: 0, lastActiveDate: '', xp: 0,
    earnedBadges: {}, gamificationMigrated: true, schemaVersion: 3,
    missions: {
      active: [],
      cumulative: { dailyCount: 1, weeklyCount: 2, monthlyCount: 3 },
      lastDailySeed: '2026-05-01',
      currentWeekIso: '2026-W18',
      currentMonthIso: '2026-05',
    },
  };
  const result = migrateUserToV3(raw);
  // immutable 패턴: result는 raw와 다른 missions 객체
  expect(result.missions).not.toBe(raw.missions);
  expect(result.missions.cumulative).not.toBe(raw.missions.cumulative);
  expect(result.missions.cumulative.dailyCount).toBe(1);
});
```

이 테스트는 현재 mutate 패턴에서 FAIL (`result.missions === raw.missions`).

Run: `npx vitest run tests/unit/state/user-migration 2>&1 | tail -10`
Expected: 위 신규 it FAIL (`Expected: not be raw.missions / Received: same reference`).

- [ ] **Step 3: idempotent path 구현 변경**

`src/state/migration.ts:87-99` 교체:

```ts
export function migrateUserToV3(raw: unknown): User {
  const v = raw as Partial<User> & { schemaVersion?: number; missions?: Partial<User['missions']> };
  if (v?.schemaVersion === 3 && v.missions && Array.isArray(v.missions.active)) {
    const m = v.missions;
    const cumulative = {
      dailyCount: typeof m.cumulative?.dailyCount === 'number' ? m.cumulative.dailyCount : 0,
      weeklyCount: typeof m.cumulative?.weeklyCount === 'number' ? m.cumulative.weeklyCount : 0,
      monthlyCount: typeof m.cumulative?.monthlyCount === 'number' ? m.cumulative.monthlyCount : 0,
    };
    return {
      ...(v as User),
      missions: {
        active: m.active,                                                  // T2 unchanged: active 배열 reference 유지 (in-place mutate path는 caller가 책임)
        cumulative,
        lastDailySeed: typeof m.lastDailySeed === 'string' ? m.lastDailySeed : '',
        currentWeekIso: typeof m.currentWeekIso === 'string' ? m.currentWeekIso : '',
        currentMonthIso: typeof m.currentMonthIso === 'string' ? m.currentMonthIso : '',
      },
    };
  }
  return {
    ...(v as User),
    schemaVersion: 3,
    missions: {
      active: [] as MissionInstance[],
      cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 },
      lastDailySeed: '',
      currentWeekIso: '',
      currentMonthIso: '',
    },
  };
}
```

**주석 추가 (active 배열 reference 유지 이유)**: `tickMissionProgress` 가 `user.missions.active`를 직접 mutate하므로, idempotent path에서 active 배열 자체는 reference 유지하면 caller가 보유한 user 객체가 같은 active를 가리킴. cumulative만 새 객체 — 어차피 atomic write 1회 단위가 작아 영향 없음.

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run tests/unit/state/user-migration 2>&1 | tail -5`
Expected: 신규 it 포함 PASS. 기존 idempotent it (있다면)도 PASS.

- [ ] **Step 5: tsc strict 확인**

Run: `npx tsc --noEmit 2>&1 | tail -5`
Expected: 0 errors.

- [ ] **Step 6: 전체 vitest 회귀**

Run: `npx vitest run --reporter=basic 2>&1 | tail -3`
Expected: `Tests 638 passed (638)` (+1 from T2's 637).

- [ ] **Step 7: Commit**

```bash
git add src/state/migration.ts tests/unit/state/user-migration.spec.ts
git commit -m "refactor(v3.13.1 T3): migrateUserToV3 idempotent path mutation→spread + unknown narrow (carry-forward T1, v3.12 #3 closeout)"
```

---

## Phase 2 — Snapshot/event hardening

### Task 4: `takeSnapshot` `progressDates` deep clone (P2-4)

**Files:**
- Modify: `src/state/achievements.ts:93`
- Test: `tests/unit/state/achievements.spec.ts` (또는 missionSnapshot 관련 spec)

**Background:** 현재 `(u?.missions?.active ?? []).map(m => ({ ...m }))` 는 shallow clone. `m.progressDates` 배열 (string[]) 은 reference 공유. 이후 `tickMissionProgress`가 `progressDates.push(todayIso)` 하면 prev/curr Snapshot이 같은 배열을 가리켜 `detectEvents`의 prev 비교가 false-negative 가능 (현재는 우연히 active-day 미션이 false→true 전환 외 progress 비교를 안 해서 문제 안 보임). Defense-in-depth.

- [ ] **Step 1: 실패 테스트 작성**

`tests/unit/state/achievements.spec.ts` 에 추가:

```ts
it('takeSnapshot: progressDates 배열 deep clone — 이후 user mutation이 snapshot에 영향 없음', () => {
  __setUserForTest({
    ...makeFreshV3User(),
    missions: {
      active: [{
        defId: 'weekly-active-5days', period: 'weekly',
        windowStart: 0, progress: 1, completed: false,
        progressDates: ['2026-05-01'],
      }],
      cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 },
      lastDailySeed: '', currentWeekIso: '', currentMonthIso: '',
    },
  });
  const snap = takeSnapshot();
  const u = loadUserData()!;
  u.missions.active[0]!.progressDates!.push('2026-05-02');                 // mutate 후
  expect(snap.missionsActive[0]!.progressDates).toEqual(['2026-05-01']);   // snapshot은 변화 없음
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run tests/unit/state/achievements 2>&1 | tail -10`
Expected: 신규 it FAIL — `progressDates` 가 `['2026-05-01', '2026-05-02']` (shallow clone 영향).

- [ ] **Step 3: 구현 변경**

`src/state/achievements.ts:93`:

Before:
```ts
missionsActive: (u?.missions?.active ?? []).map(m => ({ ...m })),
```

After:
```ts
missionsActive: (u?.missions?.active ?? []).map(m => ({
  ...m,
  progressDates: m.progressDates ? [...m.progressDates] : undefined,
})),
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run tests/unit/state/achievements 2>&1 | tail -5`
Expected: 신규 it PASS.

- [ ] **Step 5: 전체 vitest 회귀**

Run: `npx vitest run --reporter=basic 2>&1 | tail -3`
Expected: `Tests 639 passed (639)` (+1).

- [ ] **Step 6: Commit**

```bash
git add src/state/achievements.ts tests/unit/state/achievements.spec.ts
git commit -m "fix(v3.13.1 T4): takeSnapshot progressDates deep clone — defense-in-depth (P2-4)"
```

---

### Task 5: `Snapshot.missionsActive` ReadonlyArray + `?? []` 제거 (carry-forward T5 + P2-7)

**Files:**
- Modify: `src/state/gameTypes.ts` (Snapshot 타입)
- Modify: `src/state/achievements.ts:131, 133` (redundant `?? []` 제거)

**Background:** Snapshot은 immutable read-only view 의도. 타입이 `MissionInstance[]` (mutable) 라서 caller가 push할 위험. `ReadonlyArray<Readonly<MissionInstance>>` 로 강화 → TS가 mutate 차단 + `(curr.missionsActive ?? [])` 의 `?? []` 가 불필요해짐 (non-optional).

- [ ] **Step 1: Snapshot 타입 강화**

`src/state/gameTypes.ts` — `Snapshot.missionsActive` 라인 찾아서:

Before:
```ts
missionsActive: MissionInstance[];
```

After:
```ts
missionsActive: ReadonlyArray<Readonly<MissionInstance>>;
```

- [ ] **Step 2: tsc strict 확인 (어떤 caller가 mutate하는지 확인)**

Run: `npx tsc --noEmit 2>&1 | tail -20`
Expected: 만약 어떤 곳에서 `missionsActive.push(...)` 하면 TS error. 0 errors여야 안전.

- [ ] **Step 3: `detectEvents` redundant `?? []` 제거**

`src/state/achievements.ts:131, 133`:

Before:
```ts
for (const cm of (curr.missionsActive ?? [])) {
  if (!cm.completed) continue;
  const pm = (prev.missionsActive ?? []).find(p => p.defId === cm.defId);
```

After:
```ts
for (const cm of curr.missionsActive) {
  if (!cm.completed) continue;
  const pm = prev.missionsActive.find(p => p.defId === cm.defId);
```

(이유: 타입이 non-optional ReadonlyArray라 항상 정의됨.)

- [ ] **Step 4: 전체 vitest 회귀**

Run: `npx vitest run --reporter=basic 2>&1 | tail -3`
Expected: `Tests 639 passed (639)` (delta 0).

- [ ] **Step 5: tsc strict 확인**

Run: `npx tsc --noEmit 2>&1 | tail -5`
Expected: 0 errors.

- [ ] **Step 6: Commit**

```bash
git add src/state/gameTypes.ts src/state/achievements.ts
git commit -m "refactor(v3.13.1 T5): Snapshot.missionsActive ReadonlyArray<Readonly> + drop redundant ?? [] (carry-forward T5 + P2-7)"
```

---

### Task 6: `assertNever` exhaustiveness 3곳 (P2-5)

**Files:**
- Create: `src/utils/assertNever.ts` (신규 1줄 helper, 없을 시)
- Modify: `src/state/missionEngine.ts:27-41` (getKSTWindowStart)
- Modify: `src/state/missionEngine.ts:163-165` (cumulative key cast)
- Modify: `src/ui/handlers/stats.ts:282-288` (CATEGORY_ORDER loop label fallback)

**Background:** `getKSTWindowStart`는 `if (period === 'daily') / if (period === 'monthly') / <implicit weekly>` 분기. Period union이 확장되면 silently weekly로 처리. assertNever로 explicit가 안전. cumulative key cast도 exhaustiveness check 경로로 강화.

stats.ts `CATEGORY_LABELS[cat] ?? cat` — fallback이 silent하지만, T10에서 `BadgeDef.category` 와 `CATEGORY_ORDER` 일치를 type-level로 보장하면 fallback 이 dead path가 됨. 본 task에서는 assertNever보다 type-level 강화가 자연스러움 → T10에서 처리, 본 task는 missionEngine 2곳만.

- [ ] **Step 1: `assertNever` helper 확인 / 생성**

Run: `grep -rn "assertNever\|assert.*never" src/ 2>&1 | head -5`
Expected: 없음 (현재). 

`src/utils/assertNever.ts` 신규 작성:

```ts
export function assertNever(x: never): never {
  throw new Error(`Unhandled discriminated union: ${JSON.stringify(x)}`);
}
```

- [ ] **Step 2: `getKSTWindowStart` exhaustiveness 적용**

`src/state/missionEngine.ts:27-41`:

Before:
```ts
export function getKSTWindowStart(now: Date, period: MissionPeriod): number {
  const [y, m, d] = parseKSTYMD(now);
  if (period === 'daily') {
    return Date.UTC(y, m - 1, d) - 9 * 3600 * 1000;
  }
  if (period === 'monthly') {
    return Date.UTC(y, m - 1, 1) - 9 * 3600 * 1000;
  }
  // weekly: KST 월요일 00:00
  const utc = new Date(Date.UTC(y, m - 1, d));
  const day = utc.getUTCDay() || 7;
  utc.setUTCDate(utc.getUTCDate() - (day - 1));
  return utc.getTime() - 9 * 3600 * 1000;
}
```

After:
```ts
import { assertNever } from '../utils/assertNever';
// ...
export function getKSTWindowStart(now: Date, period: MissionPeriod): number {
  const [y, m, d] = parseKSTYMD(now);
  switch (period) {
    case 'daily':
      return Date.UTC(y, m - 1, d) - 9 * 3600 * 1000;
    case 'monthly':
      return Date.UTC(y, m - 1, 1) - 9 * 3600 * 1000;
    case 'weekly': {
      const utc = new Date(Date.UTC(y, m - 1, d));
      const day = utc.getUTCDay() || 7;
      utc.setUTCDate(utc.getUTCDate() - (day - 1));
      return utc.getTime() - 9 * 3600 * 1000;
    }
    default:
      return assertNever(period);
  }
}
```

- [ ] **Step 3: cumulative key 강화**

`src/state/missionEngine.ts:163-165`:

Before:
```ts
const key = `${m.period}Count` as 'dailyCount' | 'weeklyCount' | 'monthlyCount';
user.missions.cumulative[key]++;
```

After:
```ts
switch (m.period) {
  case 'daily':   user.missions.cumulative.dailyCount++; break;
  case 'weekly':  user.missions.cumulative.weeklyCount++; break;
  case 'monthly': user.missions.cumulative.monthlyCount++; break;
  default:        assertNever(m.period);
}
```

- [ ] **Step 4: 전체 vitest 회귀**

Run: `npx vitest run --reporter=basic 2>&1 | tail -3`
Expected: `Tests 639 passed (639)` (delta 0 — TS compile-time 강화).

- [ ] **Step 5: tsc strict 확인**

Run: `npx tsc --noEmit 2>&1 | tail -5`
Expected: 0 errors.

- [ ] **Step 6: Commit**

```bash
git add src/utils/assertNever.ts src/state/missionEngine.ts
git commit -m "refactor(v3.13.1 T6): assertNever exhaustiveness — getKSTWindowStart switch + cumulative key (P2-5)"
```

---

### Task 7: `emitEvents` switch default: assertNever (P2-6)

**Files:**
- Modify: `src/state/achievements.ts:168-188`

**Background:** `emitEvents` switch는 5케이스 (xp-gained / level-up / streak-milestone / badge / mission-complete) 다루지만 default 없음. `GameEvent` union 확장 시 silent skip. T6에서 만든 `assertNever` 재사용.

- [ ] **Step 1: 시그니처 + default 추가**

`src/state/achievements.ts:168-188`:

Before:
```ts
export function emitEvents(events: GameEvent[]): void {
  for (const e of events) {
    switch (e.kind) {
      case 'xp-gained':
        dispatch('dg:reward:xp-float', { amount: e.amount, at: e.at });
        break;
      // ... 4 more cases
      case 'mission-complete':
        dispatch('dg:reward:mission-complete', { defId: e.defId, period: e.period, rewardXp: e.rewardXp, at: e.at });
        break;
    }
  }
}
```

After:
```ts
import { assertNever } from '../utils/assertNever';
// ... 기존 import 다음
export function emitEvents(events: GameEvent[]): void {
  for (const e of events) {
    switch (e.kind) {
      case 'xp-gained':
        dispatch('dg:reward:xp-float', { amount: e.amount, at: e.at });
        break;
      case 'level-up':
        dispatch('dg:reward:level-up', { tierId: e.tierId, at: e.at });
        break;
      case 'streak-milestone':
        dispatch('dg:reward:streak-milestone', { days: e.days, at: e.at });
        break;
      case 'badge':
        dispatch('dg:reward:badge-unlock', { badgeId: e.badgeId, at: e.at });
        break;
      case 'mission-complete':
        dispatch('dg:reward:mission-complete', { defId: e.defId, period: e.period, rewardXp: e.rewardXp, at: e.at });
        break;
      default:
        assertNever(e);
    }
  }
}
```

- [ ] **Step 2: tsc strict 확인 (모든 케이스 망라 검증)**

Run: `npx tsc --noEmit 2>&1 | tail -5`
Expected: 0 errors. (한 case 누락 시 `assertNever(e)` 가 e가 never 아니라고 TS error.)

- [ ] **Step 3: 전체 vitest 회귀**

Run: `npx vitest run --reporter=basic 2>&1 | tail -3`
Expected: `Tests 639 passed (639)` (delta 0).

- [ ] **Step 4: Commit**

```bash
git add src/state/achievements.ts
git commit -m "refactor(v3.13.1 T7): emitEvents switch default: assertNever (P2-6)"
```

---

### Task 8: Midnight rollover invariant 테스트 (P2-3)

**Files:**
- Test: `tests/unit/state/missionEngine.spec.ts` (또는 신규 spec)

**Background:** prev/curr Snapshot은 같은 saveUser 사이클 내에서 찍는 게 invariant — 자정을 가로질러 찍으면 lastDailySeed가 다르고 active 배열이 regen되어 prev/curr defId 매칭 실패 → mission-complete event miss 가능. 명시적 테스트로 invariant 보호.

- [ ] **Step 1: 실패 또는 안전 테스트 작성**

`tests/unit/state/missionEngine.spec.ts` 에 추가:

```ts
it('자정 rollover: 같은 사이클 내 prev/curr Snapshot은 동일 미션 active를 본다 (invariant)', () => {
  // 자정 직전 — daily 미션 1개 progress=0
  const beforeMidnight = new Date('2026-05-01T14:59:59Z');                  // KST 23:59:59
  const u = makeFreshV3User();
  const r1 = getActiveMissions(beforeMidnight, u);
  expect(r1.active.filter(m => m.period === 'daily')).toHaveLength(3);
  
  // 같은 saveUser 사이클: prev/curr Snapshot은 모두 자정 이전 시각의 동일 active를 봐야 함.
  // (caller invariant: now=Date.now() 한 번 캡처 후 prev/tick/curr 모두 사용)
  const snap1 = takeSnapshot();
  // tick (active-day 미션이 진행)
  tickMissionProgress(u, 'answer', beforeMidnight);
  const snap2 = takeSnapshot();
  
  expect(snap1.missionsActive.length).toBe(snap2.missionsActive.length);
  // 자정 후 hydrate가 별도 사이클이면 lastDailySeed 갱신되어 새 daily 등장 — 별도 cycle 동작.
});
```

- [ ] **Step 2: 테스트 실행**

Run: `npx vitest run tests/unit/state/missionEngine 2>&1 | tail -10`
Expected: PASS — invariant 이 코드에서 이미 성립함을 명시적 가드.

- [ ] **Step 3: 주석 추가 (코드 위치)**

`src/state/missionEngine.ts:114` (`getActiveMissions` 시그니처 직전 JSDoc 추가):

기존 JSDoc에 다음 줄 추가:
```
 * **invariant**: caller는 prev/curr Snapshot 사이에 같은 `now`를 사용해야 한다.
 * 자정을 가로질러 호출하면 `lastDailySeed`가 갱신되어 daily 미션이 regen되고
 * prev/curr defId 매칭이 깨질 수 있다. recordDailyAnswer / fireTrigger 등 모든 sweep 진입점은 단일 `now` 캡처 후 사용.
```

- [ ] **Step 4: 전체 vitest 회귀**

Run: `npx vitest run --reporter=basic 2>&1 | tail -3`
Expected: `Tests 640 passed (640)` (+1).

- [ ] **Step 5: Commit**

```bash
git add src/state/missionEngine.ts tests/unit/state/missionEngine.spec.ts
git commit -m "test(v3.13.1 T8): midnight rollover invariant — prev/curr Snapshot 단일 now 캡처 (P2-3)"
```

---

## Phase 3 — UX edge guard

### Task 9: `hydrateMissions` saveUser Quota 토스트 (P2-1)

**Files:**
- Modify: `src/ui/handlers/home.ts:316-324`
- Test: `tests/unit/ui/home-hydrate-missions.spec.ts` (신규 또는 기존)

**Background:** `hydrateMissions` 의 `saveUser(u)` (line 321) 는 try/catch 없음. localStorage Quota / serialization 실패 시 silent throw — UI 진입 막힘 가능. v3.7 에서 도입한 `getSaveErrorMessage` + 토스트 패턴 그대로 적용.

- [ ] **Step 1: 실패 테스트 작성**

`tests/unit/ui/home-hydrate-missions.spec.ts` (신규):

```ts
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { hydrateMissions } from '../../../src/ui/handlers/home';                  // @internal export 필요 가능
// 또는 hydrateMissions를 export 안 한다면 hydrateHome 호출로 통합 검증

describe('hydrateMissions Quota guard (P2-1)', () => {
  let toastSpy: ReturnType<typeof vi.fn>;
  beforeEach(() => {
    toastSpy = vi.fn();
    document.addEventListener('dg:toast', (e: any) => toastSpy(e.detail));
    // localStorage setItem mock으로 Quota throw
    const orig = Storage.prototype.setItem;
    Storage.prototype.setItem = vi.fn(() => { throw new DOMException('Quota', 'QuotaExceededError'); });
    return () => { Storage.prototype.setItem = orig; };
  });
  
  it('saveUser 실패 시 Quota 토스트 dispatch (silent throw 차단)', () => {
    __setUserForTest(makeFreshV3UserWithStaleSeed());                              // dirty=true 강제
    expect(() => hydrateMissions()).not.toThrow();
    expect(toastSpy).toHaveBeenCalled();
    expect(toastSpy.mock.calls[0][0].text).toMatch(/저장.*공간|Quota|용량/i);
  });
  
  it('정상 path: dirty=true + saveUser 성공 시 토스트 없음', () => {
    Storage.prototype.setItem = vi.fn();                                           // 정상
    __setUserForTest(makeFreshV3UserWithStaleSeed());
    hydrateMissions();
    expect(toastSpy).not.toHaveBeenCalled();
  });
});
```

(헬퍼 `makeFreshV3UserWithStaleSeed`는 `lastDailySeed: ''` 로 dirty=true 강제. inline 또는 fixture에 추가.)

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run tests/unit/ui/home-hydrate-missions 2>&1 | tail -10`
Expected: 첫 it FAIL — `hydrateMissions` 가 throw하거나 토스트 안 뜸.

- [ ] **Step 3: hydrateMissions Quota guard 구현**

`src/ui/handlers/home.ts:316-324`:

Before (T2 후 상태):
```ts
function hydrateMissions(): void {
  const u = getCachedUser();
  if (!u) return;
  const { active, dirty } = getActiveMissions(new Date(), u);
  if (dirty) saveUser(u);
  const root = document.getElementById('missionsContainer');
  if (root) renderMissionsSection(root, active);
}
```

After:
```ts
/** @internal — exported for unit test (P2-1 Quota guard). production caller는 hydrateHome 내부. */
export function hydrateMissions(): void {
  const u = getCachedUser();
  if (!u) return;
  const { active, dirty } = getActiveMissions(new Date(), u);
  if (dirty) {
    try {
      saveUser(u);
    } catch (e) {
      dispatch('dg:toast', { text: getSaveErrorMessage(e), variant: 'error' });
      // saveUser 실패해도 active는 in-memory mutate된 상태 → 렌더는 진행 (다음 진입 시 재시도)
    }
  }
  const root = document.getElementById('missionsContainer');
  if (root) renderMissionsSection(root, active);
}
```

`getSaveErrorMessage` import 확인 (v3.7에서 도입). 없으면:
```ts
import { getSaveErrorMessage } from '../../state/saveError';                       // 또는 실제 경로
```

`dispatch` 는 `src/ui/events.ts` 의 helper.

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run tests/unit/ui/home-hydrate-missions 2>&1 | tail -5`
Expected: 2 it PASS.

- [ ] **Step 5: 전체 vitest 회귀**

Run: `npx vitest run --reporter=basic 2>&1 | tail -3`
Expected: `Tests 642 passed (642)` (+2).

- [ ] **Step 6: tsc strict 확인**

Run: `npx tsc --noEmit 2>&1 | tail -5`
Expected: 0 errors.

- [ ] **Step 7: Commit**

```bash
git add src/ui/handlers/home.ts tests/unit/ui/home-hydrate-missions.spec.ts
git commit -m "fix(v3.13.1 T9): hydrateMissions saveUser try/catch + Quota toast (P2-1)"
```

---

## Phase 4 — Type coverage + recommended graduation

### Task 10: `CATEGORY_ORDER` ↔ `BadgeDef.category` 커버리지 assertion (carry-forward T8)

**Files:**
- Test: `tests/unit/state/badge-coverage.spec.ts` (신규)

**Background:** `CATEGORY_ORDER` 6요소 와 `BadgeDef.category` union 6멤버는 1:1 매칭이지만 type-level boost 없음. `BadgeDef.category` 에 새 멤버 추가 시 stats.ts CATEGORY_ORDER에 누락 → silent rendering miss. 컴파일 타임 / 런타임 assertion 으로 가드.

- [ ] **Step 1: 신규 spec 작성**

`tests/unit/state/badge-coverage.spec.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { BADGE_CATALOG, type BadgeDef } from '../../../src/state/badgeCatalog';

// stats.ts의 CATEGORY_ORDER를 type-import (테스트에서만 import. 외부 export가 없으면 inline 복제 + 동기화 가드)
import { CATEGORY_ORDER } from '../../../src/ui/handlers/stats';                   // @internal export 필요

describe('CATEGORY_ORDER ↔ BadgeDef.category 커버리지 (carry-forward T8)', () => {
  it('CATEGORY_ORDER는 BADGE_CATALOG의 모든 category를 빠짐없이 포함한다', () => {
    const categoriesInCatalog = new Set(BADGE_CATALOG.map(b => b.category));
    const ordered = new Set<string>(CATEGORY_ORDER);
    for (const cat of categoriesInCatalog) {
      expect(ordered.has(cat)).toBe(true);
    }
  });
  
  it('CATEGORY_ORDER의 모든 항목은 실제 BadgeDef.category 값과 매칭된다 (dead category 차단)', () => {
    const categoriesInCatalog = new Set(BADGE_CATALOG.map(b => b.category));
    for (const cat of CATEGORY_ORDER) {
      expect(categoriesInCatalog.has(cat as BadgeDef['category'])).toBe(true);
    }
  });
});
```

(`CATEGORY_ORDER` 는 stats.ts에서 `export const` 로 변경 — 단순 한 줄. 또는 별도 파일 `src/ui/handlers/badgeCategories.ts` 로 분리. **본 task 1번 옵션**.)

- [ ] **Step 2: stats.ts에서 CATEGORY_ORDER + CATEGORY_LABELS export**

`src/ui/handlers/stats.ts:248-256`:

Before:
```ts
const CATEGORY_LABELS: Record<string, string> = {
  ...
};
const CATEGORY_ORDER = ['streak', 'volume', 'tier', 'diversity', 'engagement', 'mission'] as const;
```

After:
```ts
import type { BadgeDef } from '../../state/badgeCatalog';

export const CATEGORY_ORDER: ReadonlyArray<BadgeDef['category']> = ['streak', 'volume', 'tier', 'diversity', 'engagement', 'mission'] as const;
export const CATEGORY_LABELS: Record<BadgeDef['category'], string> = {
  streak:      '🔥 Streak',
  volume:      '📚 Volume',
  tier:        '🌳 Tier',
  diversity:   '🎨 Diversity',
  engagement:  '✏️ Engagement',
  mission:     '🎯 Mission',
};
```

(타입 강화 효과: `BadgeDef.category` 멤버 추가 시 `CATEGORY_LABELS` 타입에서 누락 컴파일 에러. **이게 진짜 가드** — 런타임 assertion은 보조.)

- [ ] **Step 3: stats.ts hydrateBadges line 288 fallback 정리**

`src/ui/handlers/stats.ts:288`:

Before:
```ts
h4.textContent = CATEGORY_LABELS[cat] ?? cat;
```

After:
```ts
h4.textContent = CATEGORY_LABELS[cat];
```

(이유: `CATEGORY_LABELS` 가 `Record<BadgeDef['category'], string>` 라 모든 키 강제 — fallback dead.)

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run tests/unit/state/badge-coverage 2>&1 | tail -5`
Expected: 2 it PASS.

- [ ] **Step 5: 전체 vitest 회귀**

Run: `npx vitest run --reporter=basic 2>&1 | tail -3`
Expected: `Tests 644 passed (644)` (+2 — Step 4의 1 it는 코드에서는 1개지만 위 spec에 2 it).

(예상 +Δ는 §1 표에서 +1로 표기했으나 실제 +2 → 사후 sync 가능, ±2 buffer 내.)

- [ ] **Step 6: tsc strict 확인**

Run: `npx tsc --noEmit 2>&1 | tail -5`
Expected: 0 errors.

- [ ] **Step 7: Commit**

```bash
git add src/ui/handlers/stats.ts tests/unit/state/badge-coverage.spec.ts
git commit -m "refactor(v3.13.1 T10): CATEGORY_LABELS Record<BadgeDef.category> + coverage spec — type-level dead category guard (carry-forward T8)"
```

---

### Task 11: `isValidUserShape` interests 요소 type 검증 (Recommended P2-NEW-3 graduation)

**Files:**
- Modify: `src/state/user.ts:42-54`
- Test: `tests/unit/state/user-migration.spec.ts` (T1 spec에 추가)

**Background:** v3.12.1 T1에서 `isValidUserShape` 도입 시 `interests: Array<unknown>` 만 검증 (요소 타입 미체크) — *"v3.13 미션 interest predicate 도입 시 함께 강화"* 예약. v3.13 미션이 `interestKeywords` 등에서 interests를 string union처럼 사용 → graduation 시점.

- [ ] **Step 1: 실패 테스트 추가**

`tests/unit/state/user-migration.spec.ts` 에 추가:

```ts
it('isValidUserShape: interests 배열에 비-string 요소 있으면 null 반환', () => {
  localStorage.setItem('user', JSON.stringify({
    name: 'A', interests: ['hr_system', 123 as any, null as any],                   // 비-string 섞임
    streak: 0, lastActiveDate: '', xp: 0,
    earnedBadges: {}, gamificationMigrated: true, schemaVersion: 2,
  }));
  expect(getCachedUser()).toBeNull();
});

it('isValidUserShape: interests 배열의 모든 요소가 string이면 정상 반환', () => {
  localStorage.setItem('user', JSON.stringify({
    name: 'A', interests: ['hr_system', 'self_dev'],
    streak: 0, lastActiveDate: '', xp: 0,
    earnedBadges: {}, gamificationMigrated: true, schemaVersion: 2,
  }));
  const u = getCachedUser();
  expect(u).not.toBeNull();
  expect(u!.interests).toEqual(['hr_system', 'self_dev']);
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run tests/unit/state/user-migration 2>&1 | tail -10`
Expected: 첫 it FAIL — 현재 `isValidUserShape` 가 `Array.isArray` 만 체크하고 요소 타입 미검증.

- [ ] **Step 3: `isValidUserShape` 강화**

`src/state/user.ts:42-54` 부근 — 기존:

```ts
function isValidUserShape(u: unknown): u is User {
  if (!u || typeof u !== 'object') return false;
  const c = u as User;
  return typeof c.name === 'string'
    && Array.isArray(c.interests)
    && typeof c.streak === 'number' && Number.isFinite(c.streak)
    && typeof c.lastActiveDate === 'string'
    && typeof c.xp === 'number' && Number.isFinite(c.xp);
}
```

After:
```ts
function isValidUserShape(u: unknown): u is User {
  if (!u || typeof u !== 'object') return false;
  const c = u as User;
  return typeof c.name === 'string'
    && Array.isArray(c.interests)
    && c.interests.every((i) => typeof i === 'string')                              // ✅ P2-NEW-3 graduation
    && typeof c.streak === 'number' && Number.isFinite(c.streak)
    && typeof c.lastActiveDate === 'string'
    && typeof c.xp === 'number' && Number.isFinite(c.xp);
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run tests/unit/state/user-migration 2>&1 | tail -5`
Expected: 2 신규 it PASS.

- [ ] **Step 5: 전체 vitest 회귀**

Run: `npx vitest run --reporter=basic 2>&1 | tail -3`
Expected: `Tests 646 passed (646)` (+2).

- [ ] **Step 6: tsc strict 확인**

Run: `npx tsc --noEmit 2>&1 | tail -5`
Expected: 0 errors.

- [ ] **Step 7: Commit**

```bash
git add src/state/user.ts tests/unit/state/user-migration.spec.ts
git commit -m "fix(v3.13.1 T11): isValidUserShape interests element string check (Recommended P2-NEW-3 graduation)"
```

---

## Phase 5 — Docs + verification

### Task 12: Spec §7.1 retroactive sync (P2-8)

**Files:**
- Modify: `docs/superpowers/specs/2026-04-30-v3.13-game-missions-design.md:469-481` (§7.1)

**Background:** 현재 `tabs/home.ts:77` 의 미션 섹션 위치 = Briefing → **Question → Missions** → AI Chat. spec §7.1 mockup은 헤더 → 답변 카드 → 미션 → 카드뉴스 → 관심분야. mockup이 home tab 실제 구조와 무관 (카드뉴스/관심분야 섹션은 home에 없음 — archive 탭 별도). 결론 = retroactive sync (DOM 이동 안 함).

- [ ] **Step 1: spec §7.1 갱신**

`docs/superpowers/specs/2026-04-30-v3.13-game-missions-design.md` lines 469-481:

Before:
```markdown
## §7. UI / UX (`missions-section.ts`)

### 7.1 홈 탭 위치

\`\`\`
[헤더 (인사 + #xpBadge + Streak fire)]
[오늘의 답변 카드]
[★ 미션 섹션]                 ← 신규
[카드뉴스 5장]
[관심분야 글 (스크랩)]
\`\`\`

답변 카드 직후 배치 — 답변 시 +XP floating + Daily 미션 progress bar 시각 연결.
```

After:
```markdown
## §7. UI / UX (`missions-section.ts`)

### 7.1 홈 탭 위치 (retroactive sync — v3.13.1 T12)

실제 home tab 구조 (`src/ui/tabs/home.ts:43-93`):
\`\`\`
[헤더 (인사 + #xpBadge + Streak fire) — line 32-41]
[📰 오늘의 브리핑 (briefingScroll) — line 43-53]
[🧠 오늘의 질문 (questionSection) — line 55-75]
[★ 미션 섹션 (missionsContainer)]              ← 신규 (T9 wiring)
[💬 AI Chat (chatContainer) — line 80-93]
\`\`\`

질문 섹션 직후 배치 — 답변 제출 (`hydrateMissions()` re-call at home.ts:795) 시 Daily 미션 progress bar 즉시 갱신.

(원래 mockup 의 "답변 카드 / 카드뉴스 / 관심분야 글" 표기는 home tab과 무관 — 답변 카드는 archive 탭, 카드뉴스/스크랩은 home brief/archive 탭. v3.13 T12 구현 결과 기준 정정.)
```

- [ ] **Step 2: Commit**

```bash
git add docs/superpowers/specs/2026-04-30-v3.13-game-missions-design.md
git commit -m "docs(v3.13.1 T12): spec §7.1 retroactive sync — actual DOM placement (P2-8)"
```

---

### Task 13: DoD retro sync — bundle baseline 측정값 정정

**Files:**
- Modify: `docs/superpowers/plans/2026-04-30-v3.13-game-missions-plan.md` (bundle baseline 라인)
- Modify: `/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v313.md` (memory)

**Background:** v3.13 memory 의 `bundle +2.95 kB JS gzip (+0.85 over)` 가 정확한 baseline anchor가 됐다. v3.13.1 시작 시점 (T0 step 3) 의 측정값과 비교해 v3.13 tip 측정 sync.

- [ ] **Step 1: T0 step 3에서 기록한 baseline 확인**

Run: `git diff HEAD --stat docs/superpowers/plans/2026-05-01-v3.13.1-p2-cleanup-plan.md` — T0 측정값을 plan 본문에 메모로 추가했으면 그 값.

또는: `npm run build 2>&1 | grep "dist/assets/index.*\.js"` 다시 실행하여 v3.13.1 시작 직전 값 확인.

- [ ] **Step 2: v3.13 plan/memory 의 bundle 표기 정정 (필요 시)**

만약 measure 값이 v3.13 memory `28.31 kB → 31.26 kB (+2.95)` 와 다르면 (build 환경 변화 가능), v3.13 plan + memory에 *"v3.13.1 T0 재측정: NN.NN kB (Δ from v3.12.1: ±X.XX)"* 라인 추가.

- [ ] **Step 3: Commit (변경 있을 시만)**

```bash
git add docs/superpowers/plans/2026-04-30-v3.13-game-missions-plan.md
git commit -m "docs(v3.13.1 T13): v3.13 bundle baseline retro sync — v3.13.1 T0 재측정값 anchor"
```

(변경 없으면 skip + step 4로.)

---

### Task 14: Verification + 독립 표준 리뷰

**Files:**
- Read-only verification.

- [ ] **Step 1: 전체 vitest 통과**

Run: `npx vitest run --reporter=basic 2>&1 | tail -3`
Expected: `Tests 646 passed (646)` (635 + 11 — §1 표 +10 ±2 within range).

- [ ] **Step 2: Playwright smoke**

Run: `npx playwright test 2>&1 | tail -10`
Expected: `91 passed`. (변동 0 — UI 동작 동일).

- [ ] **Step 3: tsc strict**

Run: `npx tsc --noEmit 2>&1 | tail -5`
Expected: 0 errors.

- [ ] **Step 4: Bundle 측정**

Run: `npm run build 2>&1 | grep "dist/assets/index.*\.js"`
Expected: T0 baseline ±0.2 kB. (변경된 파일이 작아 negligible — 약간 줄어들 수도, T1/T5/T10/T11 type-only가 다수.)

- [ ] **Step 5: N=5 신규 spec 안정성**

Run (5회 반복):
```bash
for i in 1 2 3 4 5; do npx vitest run tests/unit/state/missionEngine tests/unit/state/achievements tests/unit/state/user-migration tests/unit/state/badge-coverage tests/unit/ui/home-hydrate-missions --reporter=basic 2>&1 | tail -1; done
```
Expected: 5/5 PASS.

- [ ] **Step 6: 독립 표준 리뷰 dispatch**

`pr-review-toolkit:code-reviewer` agent 호출:
- 입력: 변경된 파일 (git diff main...HEAD)
- 깊이: 표준 (T14 P2 cleanup → 4~10 commits scope에 매치 — 실제 14 commits이지만 각 task 단위가 작아 표준 적합)
- 기대: P0=0 / P1 0~1 / P2 0~5 (모두 v3.14 백로그)

- [ ] **Step 7: 사용자 보고 + 배포 결정**

리뷰 결과 + DoD measurement 표 사용자에게 보고. 배포는 사용자 명령 (`firebase deploy` / `npm run deploy`) — 자동 실행 안 함. (CLAUDE.md 절대 규칙)

---

## §2 Self-Review (writing-plans skill)

### Spec coverage
- T14 P2 8건: P2-1 (T9) / P2-2 (T2) / P2-3 (T8) / P2-4 (T4) / P2-5 (T6) / P2-6 (T7) / P2-7 (T5) / P2-8 (T12) — ✅ 8/8
- Per-task carry-forward 4건: T1 (T3) / T3 (T1) / T5 (T5) / T8 (T10) — ✅ 4/4
- Recommended 2건: v3.12 #3 (T3 sub-effect) / v3.12.1 P2-NEW-3 (T11) — ✅ 2/2
- 합계: ✅ 14/14 항목 매칭

### Placeholder scan
- "TBD"/"TODO"/"implement later" 검색: 0개
- "Add appropriate error handling": 0개 (T9는 구체적 try/catch + dispatch 코드 있음)
- "Similar to Task N": 0개 (T7이 T6의 assertNever 재사용을 명시하나 import 라인 명시)

### Type consistency
- `parseKSTYMD` (T1) ↔ `getKSTWindowStart` (T6): ✅ 동일 시그니처 사용
- `getActiveMissions` 반환 (T2) ↔ `hydrateMissions` caller (T2 step 4 + T9 step 3): ✅ `{ active, dirty }` 일관
- `Snapshot.missionsActive` 타입 (T5) ↔ `detectEvents` 사용 (T5 step 3): ✅ ReadonlyArray 일관
- `assertNever` import 경로 (T6 + T7): ✅ `'../utils/assertNever'` 동일
- `CATEGORY_ORDER` export (T10) ↔ test import (T10): ✅ 일관
- `isValidUserShape` interests 검증 (T11) ↔ `getCachedUser` caller (변경 없음, 단일 entry guard 그대로): ✅

---

## §3 Task carry-over policy

본 plan 실행 중 추가 발견되는 사소한 issue (예: assertNever import path 정리 등) 는 in-cycle fix가 원칙. 다만 **scope creep 차단**:
- 신규 feature ❌ — defer to v3.14
- 신규 P2 발견 시 ✅ commit 메시지에 fix 사유만 명시 + memory v3.14 백로그
- DoD ±2 it / ±0.2 kB 위반 시 사용자 보고 후 결정

---

**End of plan.**
