# v3.34 archive 검색 ranking + cleanup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** archive 검색 결과를 시간순 → 관련도순으로 재정렬 (pinned 우선 보존). 부수적으로 Answer filter에 questionText 결합 + tests cleanup 4 sites.

**Architecture:** binary max-field-weight score (외부 라이브러리 0줄). 신규 `ranking.ts` + `archiveSort.ts` helper 2개 + archive.ts 4 filter spot integration. 검색 토큰 ≥1일 때만 발동, 빈 검색은 기존 createdAt desc 그대로.

**Tech Stack:** Vite, TypeScript, Vitest, Playwright. 기존 `src/utils/fuzzy.ts` (v3.32 graduated) helper 재사용.

**Spec:** [`docs/superpowers/specs/2026-05-13-v3.34-ranking-cleanup-design.md`](../specs/2026-05-13-v3.34-ranking-cleanup-design.md)

---

## Task 0: T0 baseline + Codex 사전 review

**Files:**
- Verify: 작업 트리 cleanliness, baseline 지표

- [ ] **Step 1: main HEAD 확인 (v3.33 deploy 일치)**

```bash
git log -1 --oneline
```
Expected: `9e718b7 docs(v3.33 T7 retro):...`

- [ ] **Step 2: baseline 지표 측정**

```bash
npm run build && \
  echo "=== bundle ===" && gzip -c dist/assets/index-*.js | wc -c && \
  echo "=== vitest KST ===" && npm test -- --run 2>&1 | tail -3 && \
  echo "=== lint ===" && npm run lint 2>&1 | tail -5
```
Expected: bundle 19076 B / vitest 1312 PASS / lint 0 problems.

- [ ] **Step 3: Codex 사전 review CLI direct dispatch**

spec + plan v1 경로:
- `docs/superpowers/specs/2026-05-13-v3.34-ranking-cleanup-design.md`
- `docs/superpowers/plans/2026-05-13-v3.34-ranking-cleanup.md`

```bash
codex exec --skip-git-repo-check -o /tmp/v3.34-codex-pre.txt "다음 spec과 plan을 독립 review해줘.

spec: docs/superpowers/specs/2026-05-13-v3.34-ranking-cleanup-design.md
plan: docs/superpowers/plans/2026-05-13-v3.34-ranking-cleanup.md

archive 검색 ranking 도입 사이클. binary max-field-weight score, 2:1 field weight (questionText/title 2, text/summary 1), pinned 우선 보존. 외부 라이브러리 0줄.

다음 관점에서 P0(차단) / P1(곧 수정) / P2(개선 여지) 분류해줘:
- 알고리즘 정합성 (binary max / cap=1 / token이 모든 field에 hit 안 할 때 edge case)
- 정렬 정책 안정성 (pinned > score > date tie-break, stable sort)
- archive.ts integration 4 filter spot 누락 여부
- Answer filter 대상 확장(questionText 결합)의 regression risk
- tests cleanup toHaveProperty 패턴 정합 (Vitest deep equal)
- bundle 추정 +200~400 B 현실성
- TZ 영향 / schema 영향 / SW 영향 (모두 비변경 영역 0줄 주장 검증)
"
```

- [ ] **Step 4: review 결과 분류 + plan v2 in-cycle 흡수**

`/tmp/v3.34-codex-pre.txt` 읽어 P0/P1/P2 분류. P0/P1은 plan에 1:1 흡수 (별도 commit), P2는 retro §7.5에 carry. CLI 실패 시 controller `pr-review-toolkit:code-reviewer` fallback + 사용자 즉시 알림.

- [ ] **Step 5: T0 commit (spec + plan v2)**

```bash
git add docs/superpowers/specs/2026-05-13-v3.34-ranking-cleanup-design.md \
        docs/superpowers/plans/2026-05-13-v3.34-ranking-cleanup.md
git commit -m "docs(v3.34 T0): spec + plan v1 + Codex 사전 review 흡수"
```

---

## Task 1: `src/utils/ranking.ts` helper + 단위 spec

**Files:**
- Create: `src/utils/ranking.ts`
- Test: `tests/unit/ranking.spec.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

```ts
// tests/unit/ranking.spec.ts
import { describe, it, expect } from 'vitest';
import { scoreEntry } from '../../src/utils/ranking';

describe('scoreEntry (v3.34 T1)', () => {
  it('빈 토큰 → 0', () => {
    expect(scoreEntry([{ text: 'hello', weight: 1 }], [])).toBe(0);
  });

  it('단일 토큰이 단일 field에 hit → field weight', () => {
    expect(scoreEntry([{ text: 'hello world', weight: 2 }], ['hello'])).toBe(2);
  });

  it('토큰이 hit 안 함 → 0 (AND filter 우회 케이스)', () => {
    expect(scoreEntry([{ text: 'hello', weight: 2 }], ['xyz'])).toBe(0);
  });

  it('multi-field hit → max(weight) 한 번만 (binary)', () => {
    // token이 title(2)과 summary(1) 둘 다에 있어도 max(2,1)=2 한 번만 인정
    expect(
      scoreEntry(
        [
          { text: 'react hooks', weight: 2 },
          { text: 'react guide', weight: 1 },
        ],
        ['react'],
      ),
    ).toBe(2);
  });

  it('multi-token 합산', () => {
    // 'react' → max(2,1)=2, 'hooks' → only title(2) → 2. total 4.
    expect(
      scoreEntry(
        [
          { text: 'react hooks', weight: 2 },
          { text: 'guide', weight: 1 },
        ],
        ['react', 'hooks'],
      ),
    ).toBe(4);
  });

  it('occurrence cap=1 (binary) — 같은 토큰 여러 번 나와도 1회만', () => {
    // 'react' 5번 나타나도 binary 1 × weight 1 = 1
    expect(scoreEntry([{ text: 'react react react react react', weight: 1 }], ['react'])).toBe(1);
  });

  it('초성 토큰 — substring 토큰과 동일하게 max(weight) 기여', () => {
    // '리액트' → 초성 'ㄹㅇㅌ'. token 'ㄹㅇ' → initial substring hit.
    expect(scoreEntry([{ text: '리액트 가이드', weight: 2 }], ['ㄹㅇ'])).toBe(2);
  });

  it('NFC 정규화 + lowercase', () => {
    expect(scoreEntry([{ text: 'HELLO', weight: 2 }], ['hello'])).toBe(2);
    const nfd = '한국'.normalize('NFD');
    expect(scoreEntry([{ text: nfd, weight: 1 }], ['한국'])).toBe(1);
  });

  // Codex 사전 P2-2 흡수: 중복 토큰 dedupe 안 함 invariant.
  it('중복 토큰 — raw 가산 (dedupe 안 함)', () => {
    // 'react' 토큰이 두 번 들어와도 각 토큰별로 max(weight) 계산해 가산.
    // → 2 + 2 = 4. AND filter 측면에선 중복이 영향 없으므로 score만 2배 가중.
    expect(scoreEntry([{ text: 'react hooks', weight: 2 }], ['react', 'react'])).toBe(4);
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

```bash
npm test -- --run tests/unit/ranking.spec.ts
```
Expected: FAIL — `ranking` module not found.

- [ ] **Step 3: 최소 구현 작성**

```ts
// src/utils/ranking.ts
/**
 * v3.34 T1: archive 검색 ranking helper.
 *
 * 알고리즘: score = Σ_token max(weight | token hits field)
 * - token이 모든 field에 hit 안 하면 → 0 기여 (AND filter 통과 entry는 도달 불가)
 * - field 여러 곳 hit해도 max(weight) 한 번만 인정 (binary, cap=1)
 * - substring / 초성-only 토큰 둘 다 동일 평가 (max(weight) binary)
 */
import { getInitialConsonants, isInitialOnlyToken } from './fuzzy';

export interface ScoredField {
  text: string;
  weight: number;
}

export function scoreEntry(fields: ScoredField[], tokens: string[]): number {
  if (tokens.length === 0) return 0;
  const normalized = fields.map((f) => ({
    weight: f.weight,
    body: f.text.normalize('NFC').toLowerCase(),
    initials: getInitialConsonants(f.text.normalize('NFC').toLowerCase()),
  }));
  let total = 0;
  for (const rawToken of tokens) {
    const token = rawToken.normalize('NFC').toLowerCase();
    const isInitial = isInitialOnlyToken(token);
    let maxHitWeight = 0;
    for (const f of normalized) {
      const hit = isInitial ? f.initials.includes(token) : f.body.includes(token);
      if (hit && f.weight > maxHitWeight) maxHitWeight = f.weight;
    }
    total += maxHitWeight;
  }
  return total;
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
npm test -- --run tests/unit/ranking.spec.ts
```
Expected: PASS, 9/9.

- [ ] **Step 5: 양쪽 TZ + 전체 test PASS 확인**

```bash
npm test -- --run && TZ=America/New_York npm test -- --run
```
Expected: 양쪽 PASS, count +9.

- [ ] **Step 6: Commit**

```bash
git add src/utils/ranking.ts tests/unit/ranking.spec.ts
git commit -m "feat(v3.34 T1): scoreEntry helper — binary max field weight ranking"
```

---

## Task 2: `src/utils/archiveSort.ts` helper + 단위 spec

**Files:**
- Create: `src/utils/archiveSort.ts`
- Test: `tests/unit/archiveSort.spec.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

```ts
// tests/unit/archiveSort.spec.ts
import { describe, it, expect } from 'vitest';
import { sortPinThenScoreDesc } from '../../src/utils/archiveSort';

interface Item { id: string; pinned: boolean; score: number; date: string }

describe('sortPinThenScoreDesc (v3.34 T2)', () => {
  const items: Item[] = [
    { id: 'a', pinned: false, score: 5, date: '2026-05-10' },
    { id: 'b', pinned: true,  score: 1, date: '2026-05-12' },
    { id: 'c', pinned: false, score: 5, date: '2026-05-13' },
    { id: 'd', pinned: false, score: 3, date: '2026-05-11' },
    { id: 'e', pinned: true,  score: 4, date: '2026-05-09' },
  ];

  it('pinned 항목이 모두 상단 (score 무관)', () => {
    const sorted = sortPinThenScoreDesc(
      items,
      (i) => i.pinned,
      (i) => i.score,
      (i) => i.date,
    );
    expect(sorted.slice(0, 2).map((i) => i.id).sort()).toEqual(['b', 'e']);
  });

  it('pinned group 내 score desc → date desc', () => {
    const sorted = sortPinThenScoreDesc(
      items,
      (i) => i.pinned,
      (i) => i.score,
      (i) => i.date,
    );
    // pinned: e (score 4) → b (score 1)
    expect(sorted.slice(0, 2).map((i) => i.id)).toEqual(['e', 'b']);
  });

  it('non-pinned group 내 score desc → date desc tie-break', () => {
    const sorted = sortPinThenScoreDesc(
      items,
      (i) => i.pinned,
      (i) => i.score,
      (i) => i.date,
    );
    // non-pinned: c (score 5, 2026-05-13) → a (score 5, 2026-05-10) → d (score 3)
    expect(sorted.slice(2).map((i) => i.id)).toEqual(['c', 'a', 'd']);
  });

  it('빈 배열 → 빈 배열', () => {
    expect(
      sortPinThenScoreDesc<Item>([], (i) => i.pinned, (i) => i.score, (i) => i.date),
    ).toEqual([]);
  });

  it('원본 배열 mutate 하지 않음 (stable copy)', () => {
    const original = [...items];
    sortPinThenScoreDesc(items, (i) => i.pinned, (i) => i.score, (i) => i.date);
    expect(items).toEqual(original);
  });

  // Codex 사전 P2-1 흡수: 완전 동률 입력 순서 보존 (stable sort).
  it('완전 동률(pinned/score/date) — 입력 순서 보존 (ES2019 stable sort)', () => {
    const same: Item[] = [
      { id: 'x', pinned: false, score: 3, date: '2026-05-10' },
      { id: 'y', pinned: false, score: 3, date: '2026-05-10' },
      { id: 'z', pinned: false, score: 3, date: '2026-05-10' },
    ];
    const sorted = sortPinThenScoreDesc(
      same,
      (i) => i.pinned,
      (i) => i.score,
      (i) => i.date,
    );
    expect(sorted.map((i) => i.id)).toEqual(['x', 'y', 'z']);
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

```bash
npm test -- --run tests/unit/archiveSort.spec.ts
```
Expected: FAIL — `archiveSort` module not found.

- [ ] **Step 3: 최소 구현 작성**

```ts
// src/utils/archiveSort.ts
/**
 * v3.34 T2: archive 검색 ranking 적용용 sort helper.
 * 3-tier: pinned desc → score desc → date desc.
 * Stable sort (ES2019).
 */
export function sortPinThenScoreDesc<T>(
  items: T[],
  getPinned: (t: T) => boolean,
  getScore: (t: T) => number,
  getDateKey: (t: T) => string,
): T[] {
  return [...items].sort((a, b) => {
    const pa = getPinned(a) ? 1 : 0;
    const pb = getPinned(b) ? 1 : 0;
    if (pa !== pb) return pb - pa;
    const sa = getScore(a);
    const sb = getScore(b);
    if (sa !== sb) return sb - sa;
    const da = getDateKey(a);
    const db = getDateKey(b);
    return db.localeCompare(da);
  });
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
npm test -- --run tests/unit/archiveSort.spec.ts
```
Expected: PASS, 6/6.

- [ ] **Step 5: 양쪽 TZ + 전체 test PASS 확인**

```bash
npm test -- --run && TZ=America/New_York npm test -- --run
```
Expected: 양쪽 PASS, count +6 (T1 +9 / T2 +6 누계 +15).

- [ ] **Step 6: Commit**

```bash
git add src/utils/archiveSort.ts tests/unit/archiveSort.spec.ts
git commit -m "feat(v3.34 T2): sortPinThenScoreDesc 3-tier sort helper"
```

---

## Task 3: `archive.ts` integration + 통합 spec

**Files:**
- Modify: `src/ui/handlers/archive.ts` (4 filter spot + answer filter 대상 확장)
- Test: `tests/ui/archive-ranking.spec.ts` (신규 DOM-level 통합 — Codex 사전 P1 흡수)

- [ ] **Step 1: DOM-level 통합 spec 작성 (Codex 사전 P1 흡수)**

`tests/ui/archive-ranking.spec.ts` (unit이 아니라 ui). `tests/ui/archive-pin-sort.spec.ts` + `tests/ui/archive-search.spec.ts` 패턴 합본.

```ts
// tests/ui/archive-ranking.spec.ts
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';
import { renderArchive } from '../../src/ui/tabs/archive';
import { mountArchiveHandlers } from '../../src/ui/handlers/archive';
import { saveUser, type Insight } from '../../src/state/user';
import { mkUser } from '../unit/state/userFixture';
import { saveAnswers, saveBriefings } from '../../src/state/persistence';
import type { Answer, Briefing } from '../../src/state/schema';

function mkAnswer(over: Partial<Answer>): Answer {
  return {
    id: 'a', questionId: '', text: 'answer', authorId: 'self',
    createdAt: '2026-05-10T10:00:00.000Z', pinned: false,
    schemaVersion: 1 as Answer['schemaVersion'], ...over,
  };
}

function mkInsight(over: Partial<Insight>): Insight {
  return {
    id: 'i', text: 'insight body', interestId: 'ai_ml',
    createdAt: '2026-05-10T10:00:00.000Z', pinned: false, ...over,
  };
}

function mkBriefing(over: Partial<Briefing>): Briefing {
  return {
    id: 'b', date: '2026-05-10', url: '', title: 'title', summary: 'summary',
    scrapped: true, read: false, memo: '', pinned: false, ...over,
  };
}

function setup(): void {
  // eslint-disable-next-line no-restricted-syntax -- jsdom fixture, no user interpolation
  document.body.innerHTML = '<div id="archiveTab"></div>';
  const container = document.getElementById('archiveTab')!;
  renderArchive(container);
  mountArchiveHandlers();
}

function clickEntity(entity: 'all' | 'answer' | 'scrap' | 'insight'): void {
  document.querySelector<HTMLButtonElement>(`.archive-entity-chip[data-entity="${entity}"]`)!.click();
}

function search(query: string): void {
  const input = document.querySelector<HTMLInputElement>('#archiveSearch')!;
  input.value = query;
  input.dispatchEvent(new Event('input'));
  vi.advanceTimersByTime(200); // archive.ts:92~107 debounce
}

describe('v3.34 T3: archive ranking integration (DOM-level)', () => {
  beforeEach(() => {
    vi.useFakeTimers();
    localStorage.clear();
    sessionStorage.clear();
    // eslint-disable-next-line no-restricted-syntax -- jsdom fixture cleanup, no user interpolation
    document.body.innerHTML = '';
  });

  afterEach(() => {
    vi.useRealTimers();
  });

  // P1 케이스 1: questionText hit > text hit (정렬 변화 + score 가중치)
  it('answer entity — questionText hit(weight 2) > text hit(weight 1)', () => {
    saveAnswers([
      mkAnswer({ id: 'q-hit', questionText: '리액트 훅 학습', text: '오늘 공부', createdAt: '2026-05-08T00:00:00.000Z' }),
      mkAnswer({ id: 't-hit', questionText: '오늘 학습',     text: '리액트 훅 정리', createdAt: '2026-05-12T00:00:00.000Z' }),
    ]);
    setup();
    clickEntity('answer');
    search('리액트 훅');

    const ids = [...document.querySelectorAll<HTMLElement>('#archiveList .archive-card[data-answer-id]')]
      .map((c) => c.dataset['answerId']);
    // q-hit (score 4) → t-hit (score 2)
    expect(ids).toEqual(['q-hit', 't-hit']);
  });

  // Codex P2-3 흡수: questionText 확장이 result-set 측면에선 regression 아님
  it('answer — 기존 text-only hit 결과 보존 (questionText 확장이 regression 아님)', () => {
    saveAnswers([
      mkAnswer({ id: 'text-only', questionText: '오늘 작업', text: '리액트 훅 정리' }),
    ]);
    setup();
    clickEntity('answer');
    search('리액트');

    const ids = [...document.querySelectorAll<HTMLElement>('#archiveList .archive-card[data-answer-id]')]
      .map((c) => c.dataset['answerId']);
    expect(ids).toEqual(['text-only']);
  });

  it('insight entity — AND filter + 단일 field score', () => {
    saveUser(mkUser({
      insights: [
        mkInsight({ id: 'i-both', text: '리액트 훅 정리', createdAt: '2026-05-08T00:00:00.000Z' }),
        mkInsight({ id: 'i-one',  text: '리액트만',       createdAt: '2026-05-12T00:00:00.000Z' }),
      ],
    }));
    setup();
    clickEntity('insight');
    search('리액트 훅');

    const ids = [...document.querySelectorAll<HTMLElement>('#archiveList .archive-insight-card')]
      .map((c) => c.dataset['insightId']);
    // i-one은 '훅' AND filter 실패 → i-both만 통과
    expect(ids).toEqual(['i-both']);
  });

  it('scrap entity — title hit(weight 2) > summary hit(weight 1)', () => {
    saveBriefings([
      mkBriefing({ id: 'b-title',   title: '리액트 가이드', summary: '튜토리얼',     date: '2026-05-08' }),
      mkBriefing({ id: 'b-summary', title: '튜토리얼',     summary: '리액트 가이드', date: '2026-05-12' }),
    ]);
    setup();
    clickEntity('scrap');
    search('리액트');

    const ids = [...document.querySelectorAll<HTMLElement>('#archiveList .archive-scrap-card')]
      .map((c) => c.dataset['briefingId']);
    expect(ids).toEqual(['b-title', 'b-summary']);
  });

  it('all merge — pinned > score > date 3-tier', () => {
    saveAnswers([
      mkAnswer({ id: 'pin-low',     pinned: true, questionText: '메모',      text: '리액트', createdAt: '2026-05-08T00:00:00.000Z' }),
      mkAnswer({ id: 'unpin-high',                questionText: '리액트 훅', text: 'x',      createdAt: '2026-05-10T00:00:00.000Z' }),
      mkAnswer({ id: 'unpin-low',                 questionText: 'x',         text: '리액트', createdAt: '2026-05-13T00:00:00.000Z' }),
    ]);
    setup();
    clickEntity('all');
    search('리액트');

    const ids = [...document.querySelectorAll<HTMLElement>('#archiveList .archive-card[data-answer-id]')]
      .map((c) => c.dataset['answerId']);
    // pin-low (pinned 우선) → unpin-high (score 2: Q hit) → unpin-low (score 1: text hit)
    expect(ids).toEqual(['pin-low', 'unpin-high', 'unpin-low']);
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인 (archive.ts integration 전이라 fail 의도)**

```bash
npm test -- --run tests/ui/archive-ranking.spec.ts
```
Expected: FAIL — 현재 `sortPinThenDesc(createdAt)` 정렬이라 q-hit/t-hit 순서, b-title/b-summary 순서, pin-low/unpin-high/unpin-low 순서가 모두 createdAt desc 기준으로 어긋남. Step 3 이후 integration이 끝나면 PASS.

- [ ] **Step 3: `archive.ts` integration — scrap entity**

`src/ui/handlers/archive.ts` 첫 import 라인 근처에:
```ts
import { scoreEntry } from '../../utils/ranking';
import { sortPinThenScoreDesc } from '../../utils/archiveSort';
```

scrap entity branch (line ~703~715) 변경:
```ts
  if (currentEntity === 'scrap') {
    let pool = scraps;
    if (currentTokens.length > 0) {
      pool = pool.filter((b) =>
        matchesAllTokens(`${b.title ?? ''} ${b.summary ?? ''}`, currentTokens));
    }
    if (pool.length === 0) {
      list.textContent = '아직 스크랩한 기사가 없어요.';
      return;
    }
    if (currentTokens.length > 0) {
      sortPinThenScoreDesc(
        pool,
        (b) => b.pinned,
        (b) => scoreEntry(
          [{ text: b.title ?? '', weight: 2 }, { text: b.summary ?? '', weight: 1 }],
          currentTokens,
        ),
        (b) => b.date,
      ).forEach((b) => appendScrapCard(b));
    } else {
      sortPinThenDesc(pool, (b) => b.pinned, (b) => b.date).forEach((b) => appendScrapCard(b));
    }
    return;
  }
```

- [ ] **Step 4: insight entity branch 변경 (line ~719~729)**

```ts
  if (currentEntity === 'insight') {
    let pool = insights;
    if (currentTokens.length > 0) {
      pool = pool.filter((i) => matchesAllTokens(i.text, currentTokens));
    }
    if (pool.length === 0) {
      list.textContent = '아직 저장된 인사이트가 없어요.';
      return;
    }
    if (currentTokens.length > 0) {
      sortPinThenScoreDesc(
        pool,
        (i) => i.pinned,
        (i) => scoreEntry([{ text: i.text, weight: 1 }], currentTokens),
        (i) => i.createdAt,
      ).forEach((i) => list.append(renderInsightCard(i)));
    } else {
      sortPinThenDesc(pool, (i) => i.pinned, (i) => i.createdAt).forEach((i) => list.append(renderInsightCard(i)));
    }
    return;
  }
```

- [ ] **Step 5: all merge branch 변경 (line ~733~768)**

```ts
  if (currentEntity === 'all' && currentFilter === 'all') {
    type Entry =
      | { kind: 'answer'; answer: Answer; sortKey: string; pinned: boolean }
      | { kind: 'scrap'; briefing: typeof briefings[number]; sortKey: string; pinned: boolean }
      | { kind: 'insight'; insight: Insight; sortKey: string; pinned: boolean };

    let entries: Entry[] = [
      ...answers.map((a) => ({ kind: 'answer' as const, answer: a, sortKey: a.createdAt, pinned: a.pinned })),
      ...scraps.map((b) => ({ kind: 'scrap' as const, briefing: b, sortKey: b.date, pinned: b.pinned })),
      ...insights.map((i) => ({ kind: 'insight' as const, insight: i, sortKey: i.createdAt, pinned: i.pinned })),
    ];

    if (currentTokens.length > 0) {
      entries = entries.filter((e) =>
        e.kind === 'answer'
          ? matchesAllTokens(`${e.answer.questionText ?? ''} ${e.answer.text}`, currentTokens)
          : e.kind === 'insight'
            ? matchesAllTokens(e.insight.text, currentTokens)
            : matchesAllTokens(`${e.briefing.title ?? ''} ${e.briefing.summary ?? ''}`, currentTokens),
      );
    }

    if (entries.length === 0) {
      list.textContent = '아직 답변이나 스크랩한 기록이 없어요.';
      return;
    }

    function entryScore(e: Entry): number {
      if (currentTokens.length === 0) return 0;
      if (e.kind === 'answer') {
        return scoreEntry(
          [{ text: e.answer.questionText ?? '', weight: 2 }, { text: e.answer.text, weight: 1 }],
          currentTokens,
        );
      }
      if (e.kind === 'insight') {
        return scoreEntry([{ text: e.insight.text, weight: 1 }], currentTokens);
      }
      return scoreEntry(
        [{ text: e.briefing.title ?? '', weight: 2 }, { text: e.briefing.summary ?? '', weight: 1 }],
        currentTokens,
      );
    }

    const sorted = currentTokens.length > 0
      ? sortPinThenScoreDesc(entries, (e) => e.pinned, entryScore, (e) => e.sortKey)
      : sortPinThenDesc(entries, (e) => e.pinned, (e) => e.sortKey);

    for (const e of sorted) {
      if (e.kind === 'answer') list.append(renderAnswerCard(e.answer));
      else if (e.kind === 'insight') list.append(renderInsightCard(e.insight));
      else appendScrapCard(e.briefing);
    }
    return;
  }
```

- [ ] **Step 6: answer entity branch 변경 (line ~770~) + filter 대상 확장**

```ts
  let filtered = answers;
  if (currentFilter !== 'all') filtered = filtered.filter((a) => (a.type ?? '').includes(currentFilter));
  if (currentTokens.length > 0) {
    // v3.34 T3: filter 대상에 questionText 결합 (ranking weight 활성화 정합).
    filtered = filtered.filter((a) =>
      matchesAllTokens(`${a.questionText ?? ''} ${a.text}`, currentTokens));
  }

  if (filtered.length === 0) {
    list.textContent = '조건에 맞는 기록이 없어요.';
    return;
  }

  const sorted = currentTokens.length > 0
    ? sortPinThenScoreDesc(
        filtered,
        (a) => a.pinned,
        (a) => scoreEntry(
          [{ text: a.questionText ?? '', weight: 2 }, { text: a.text, weight: 1 }],
          currentTokens,
        ),
        (a) => a.createdAt,
      )
    : sortPinThenDesc(filtered, (a) => a.pinned, (a) => a.createdAt);

  for (const a of sorted) list.append(renderAnswerCard(a));
```

- [ ] **Step 7: 검증**

```bash
npx tsc --noEmit && \
  npm test -- --run && \
  TZ=America/New_York npm test -- --run && \
  npm run lint && \
  npm run build
```
Expected: tsc 0 errors, vitest 양쪽 PASS (Step 1 DOM-level spec 5건 포함), lint 0 problems, build PASS.

- [ ] **Step 8: bundle 1차 측정**

```bash
gzip -c dist/assets/index-*.js | wc -c
```
Expected: ≤ 19376 B (GREEN ≤ +300). 초과 시 AMBER 인정 ≤ +500, 그 이상은 R4 발동 — implementation 재검토.

- [ ] **Step 9: Commit**

```bash
git add src/ui/handlers/archive.ts tests/ui/archive-ranking.spec.ts
git commit -m "feat(v3.34 T3): archive 4 filter spot ranking + Answer questionText 결합 (DOM-level spec)"
```

---

## Task 4: tests cleanup — `as unknown as Record<string, unknown>` 4 sites

**Files:**
- Modify: `tests/unit/state-user.spec.ts:26`
- Modify: `tests/unit/state/user-migration.spec.ts:13`, `:25`
- Modify: `tests/state/migration-v5.spec.ts:51`

- [ ] **Step 1: `tests/unit/state-user.spec.ts:26` 교체**

```ts
// 변경 전
expect((u as unknown as Record<string, unknown>).level).toBeUndefined();

// 변경 후
expect(u).not.toHaveProperty('level');
```

- [ ] **Step 2: `tests/unit/state/user-migration.spec.ts:13` 교체**

```ts
// 변경 전
expect((v2 as unknown as Record<string, unknown>).level).toBeUndefined();

// 변경 후
expect(v2).not.toHaveProperty('level');
```

- [ ] **Step 3: `tests/unit/state/user-migration.spec.ts:25` 교체**

```ts
// 변경 전
expect((u as unknown as Record<string, unknown>).level).toBeUndefined();

// 변경 후
expect(u).not.toHaveProperty('level');
```

- [ ] **Step 4: `tests/state/migration-v5.spec.ts:51` 교체**

```ts
// 변경 전
expect((out as unknown as Record<string, unknown>).insights).toEqual(v6.insights);

// 변경 후 (Vitest toHaveProperty deep equal)
expect(out).toHaveProperty('insights', v6.insights);
```

- [ ] **Step 5: 검증**

```bash
npm test -- --run tests/unit/state-user.spec.ts \
                  tests/unit/state/user-migration.spec.ts \
                  tests/state/migration-v5.spec.ts
```
Expected: 모두 PASS. grep 0 hits 확인:
```bash
grep -rn "as unknown as Record<string, unknown>" tests/
```
Expected: 0 lines (production migration.ts 3 sites는 out-of-scope이라 grep 명령에서 `src/` 제외).

- [ ] **Step 6: Commit**

```bash
git add tests/unit/state-user.spec.ts tests/unit/state/user-migration.spec.ts tests/state/migration-v5.spec.ts
git commit -m "refactor(v3.34 T4): tests as-unknown-as-Record → toHaveProperty 4 sites"
```

---

## Task 5: Codex 최종 review + 3종 검증 + bundle 2차 측정

**Files:** 본 task는 검증만, code 수정 없음 (review 결과 P0/P1 발견 시 in-cycle fix commit).

- [ ] **Step 1: 사이클-끝 3종 검증**

```bash
npx tsc --noEmit && \
  npm run lint && \
  npm test -- --run && \
  TZ=America/New_York npm test -- --run && \
  npm run test:smoke && \
  npm run build
```
Expected: tsc 0 / lint 0 / vitest 양쪽 PASS / smoke 100 / build PASS.

- [ ] **Step 2: 신규 spec N=5 회귀**

```bash
for i in 1 2 3 4 5; do
  echo "=== N=$i ==="
  npm test -- --run tests/unit/ranking.spec.ts \
                    tests/unit/archiveSort.spec.ts \
                    tests/ui/archive-ranking.spec.ts \
    2>&1 | tail -3
done
```
Expected: 5/5 PASS.

- [ ] **Step 3: bundle 2차 측정**

```bash
echo "baseline: 19076 B"
echo -n "current : "
gzip -c dist/assets/index-*.js | wc -c
```
Expected: ≤ +500 B (AMBER). Δ를 retro §7.4에 절대값 + 부호 모두 기록.

- [ ] **Step 4: Codex 최종 review CLI direct dispatch**

```bash
codex exec --skip-git-repo-check -o /tmp/v3.34-codex-final.txt "v3.34 ranking + cleanup 사이클 최종 review.

main HEAD: <git log -1 --oneline 결과>
spec: docs/superpowers/specs/2026-05-13-v3.34-ranking-cleanup-design.md
plan: docs/superpowers/plans/2026-05-13-v3.34-ranking-cleanup.md
변경 파일: src/utils/ranking.ts, src/utils/archiveSort.ts, src/ui/handlers/archive.ts, tests/unit/{ranking,archiveSort,archive-ranking}.spec.ts, tests/unit/state-user.spec.ts, tests/unit/state/user-migration.spec.ts, tests/state/migration-v5.spec.ts

다음 결과로 분류해줘:
- DEPLOY_APPROVED (P0=0, P1=0)
- APPROVED_WITH_NOTES (P0=0, P1=0, P2 only)
- REJECT (P0 잔존)

검증할 invariant:
- ranking algorithm 정합 (binary max field weight per token / occurrence cap=1 / 초성 토큰 처리)
- archive integration 4 filter spot 누락 없음 (scrap / insight / all merge / answer)
- Answer filter 대상 questionText 결합이 기존 검색 결과를 깨지 않는지 (확장 only)
- bundle 추정 +200~400 B 실측 정합
- TZ / schema / SW 비변경 영역 0줄 주장 검증
- tests cleanup 4 sites 정합 (toHaveProperty deep equal 의미 보존)
"
```

- [ ] **Step 5: 결과 분류 + in-cycle 흡수**

CLI 실패 시 controller `pr-review-toolkit:code-reviewer` fallback + 사용자 즉시 알림 (v3.33 T5 6단계 정책). P0/P1 발견 시 in-cycle fix commit 별도. P2는 retro §7.5에 carry.

- [ ] **Step 6: T5 verification 결과 commit (review 흡수 fix 있으면 별도, 없으면 verification 보고)**

```bash
# review 흡수 fix가 있는 경우
git add <fix files>
git commit -m "fix(v3.34 T5): Codex 최종 review <P0/P1> 흡수"
```

---

## Task 6: Retro + MEMORY + Obsidian vault sync

**Files:**
- Modify: `docs/superpowers/specs/2026-05-13-v3.34-ranking-cleanup-design.md` (§7 retro 채움)
- Modify: `~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md` + 신규 `project_daily_growth_v334.md`
- Sync: Obsidian vault `02-Cycles/` + `03-Lessons/`

- [ ] **Step 1: spec §7 retro 채움 (7.1~7.8)**

각 sub-section에 다음 내용:
- §7.1 Final HEAD: 마지막 commit hash
- §7.2 Task-by-task: T0~T6 commit hash + 핵심 결과
- §7.3 Lint baseline 변화: 0 → 0 (변동 없음 예상)
- §7.4 Bundle 측정: 19076 → ??? B (Δ)
- §7.5 Codex 2-pass 결과: 사전 / 최종 분류 + 흡수 내역
- §7.6 Lessons (graduate 후보)
- §7.7 Carry-forward (v3.35): C10 Plant action + ranking weight tune 후보 + retro 작성 시 발견 P2
- §7.8 Next Recommended Step

- [ ] **Step 2: MEMORY.md entry 추가**

신규 memory file: `project_daily_growth_v334.md`. 형식은 v3.33 entry 참고. MEMORY.md 인덱스에 1줄 (≤200 chars) 추가.

- [ ] **Step 3: Obsidian vault sync**

CLAUDE.md "Obsidian Vault Sync 정책" 따라:
- `02-Cycles/` ← spec retro 복사
- `03-Lessons/` ← memory file 복사 + MEMORY.md 갱신본 복사
- `00-Index.md` 사이클 타임라인 표에 v3.34 한 줄 추가

```bash
VAULT="/Users/hayden/Hayden/20_Projects/Dev_Project/SJ_Daily Growth(Ai assistant)"
cp docs/superpowers/specs/2026-05-13-v3.34-ranking-cleanup-design.md "$VAULT/02-Cycles/"
cp ~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v334.md "$VAULT/03-Lessons/"
cp ~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md "$VAULT/03-Lessons/"
```

- [ ] **Step 4: T6 commit**

```bash
git add docs/superpowers/specs/2026-05-13-v3.34-ranking-cleanup-design.md
git commit -m "docs(v3.34 T6 retro): 사이클 완료 — ranking + cleanup"
```

- [ ] **Step 5: Deploy gate**

사용자 "배포해줘" 명시 시점에만:
```bash
npm run build && firebase deploy --only hosting
```
주의: CLAUDE.md "origin remote 미등록 — main 직배포 패턴". git push 금지.

---

## Self-Review Checklist (writing-plans skill)

- ✅ **Spec coverage**: §1 목표 3건(ranking / Answer questionText / cleanup) 모두 T1+T2+T3+T4 cover
- ✅ **Placeholder scan**: TBD/TODO 없음. retro §7는 T6에서 채움 명시
- ✅ **Type consistency**: `scoreEntry(fields, tokens) → number` / `sortPinThenScoreDesc(items, getPinned, getScore, getDateKey)` 시그니처 T1~T3 동일
- ✅ **Bundle budget**: T3 Step 9에서 1차 측정, T5 Step 3에서 2차 측정. GREEN/AMBER/RED threshold 명시
- ✅ **Codex 2-pass**: T0 Step 3-4 사전 / T5 Step 4-5 최종. CLAUDE.md 6단계 fallback 정책 명시
- ✅ **TZ 양쪽 검증**: T1 Step 5, T2 Step 5, T3 Step 8, T5 Step 1 모두 양쪽 TZ 명시
- ✅ **Commit 단위**: 각 task 마지막에 단일 commit. CLAUDE.md "git add 명시적 파일만" 준수

---

## Execution Choice

Plan complete and saved to `docs/superpowers/plans/2026-05-13-v3.34-ranking-cleanup.md`.

**Next step**: 사용자가 명시한 대로 **Codex 사전 review를 먼저 진행** (T0 Step 3-4) 후 plan v2 흡수. review 결과를 받고 plan을 정정한 뒤에 execution 방식(subagent-driven vs inline)을 결정.
