# DG v3.14.2 — Hotfix + P2 Legacy Cleanup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** P0 Playwright 회복 검증(N=3 안정성)으로 v3.14.1 메모 retro 정정 + P2 legacy 13건 (v3.13.2 후보 4 + v3.12 잔존 6 + v3.12.1 P2-NEW 3) 일괄 cleanup → main 직접 commit pack.

**Architecture:** 17 task. T0(plan/codex 사전), T1(P0 N=3 회복 검증 + retro), T2~T5(v3.13.2), T6~T11(v3.12 잔존), T12~T14(v3.12.1 P2-NEW), T15(docs sync), T16(codex 최종), T17(retro/배포 대기). Subagent-driven default, controller 직접 코드 0회 (T0/T15/T17 docs만).

**Tech Stack:** Vite + TypeScript strict + Vitest + Playwright + Firebase Hosting. CSS Cascade Layers + tokens.css. localStorage v3 schema (lazy migrate). KST timezone (`Intl.DateTimeFormat`).

**Spec:** `docs/superpowers/specs/2026-05-02-v3.14.2-hotfix-and-p2-cleanup-design.md` (commit `0f2b635` + retro `759c813`).

---

## File Structure

### Modify
| 경로 | Task | 변경 |
|---|---|---|
| `src/state/migration.ts:107-109` | T3 | `typeof === 'number'` → `typeof === 'number' && Number.isFinite(...)` |
| `src/state/missionTypes.ts:22-29` | T4 | `progressDates?: string[]` → `readonly string[]` |
| `src/state/missionEngine.ts:172-175` | T4 | `push` mutation → spread immutable update |
| `src/state/migration.ts:78` | T13 | `as unknown as User` 1회 → type guard 격리 |
| `src/state/user.ts:33-51` | T12 | `getCachedUser` catch silent → toast 노출 |
| `src/utils/categories.ts:14` | T6 | (조건부) labor_law VS16 stray fix — 검증 결과에 따라 |
| `src/utils/toast.ts:23-26` | T8 | `setTimeout` cascade → timer ID 추적 + 외부 clearAll export |
| `src/ui/rewards.ts:25-44, 158-163` | T9 | spawnToast timer ID 추적 + `__resetForTest` clearAllTimers 통합 |
| `src/ui/handlers/stats.ts:265-334` | T10 | `hydrateBadges` 70줄 → `renderBadgeButton` + `renderBadgeCategory` 추출 |
| `src/ui/modals/welcome-gamification.ts:35-44` | T11 | backfill saveUser race annotation (theoretical, JSDoc) |
| `src/ui/modals/welcome-gamification.ts:79` | T14 | `.dg-modal #goStatsBtn` → `openModal` 반환값 query |
| `src/ui/modals/shared.ts:22, 84` | T14 | `openModal` 시그니처 `void` → `HTMLDivElement` |
| `docs/superpowers/specs/2026-04-30-v3.13-game-missions-design.md` | T5 | §1.4 baseline 31.47 kB anchor sync |

### Create
| 경로 | Task |
|---|---|
| `tests/unit/state/missionEngine-cross-midnight.spec.ts` | T2 |
| `tests/unit/state/migration-numeric-guards.spec.ts` | T3 |
| `tests/unit/state/missionTypes-progressdates-immutable.spec.ts` | T4 |
| `tests/unit/utils/interestKeywords-vs16.spec.ts` | T6 |
| `tests/unit/utils/toast-timer-dedup.spec.ts` | T8 |
| `tests/unit/ui/rewards-reset-clear-timers.spec.ts` | T9 |
| `tests/unit/ui/stats-hydrate-badges-split.spec.ts` | T10 |
| `tests/unit/state/user-corrupt-v2-toast.spec.ts` | T12 |
| `tests/unit/ui/welcome-gamification-nested-modal.spec.ts` | T14 |
| `docs/superpowers/plans/2026-05-02-v3.14.2-hotfix-and-p2-cleanup-plan.md` | T0 (this file) |

### Delete
없음.

---

## Pre-flight grep (controller가 plan 작성 시 사전 확인 완료)

| 항목 | 결과 |
|---|---|
| Playwright `desktop-layout` + `tier-downgrade` 실측 | 7/7 pass (10.9s, fresh build) |
| `getActiveMissions` callsites | 4개 (missions-triggers:17, home:324, briefings:64, user:100) |
| `migrateUserToV3` numeric guards | 3 lines (`typeof === 'number'` only) |
| `MissionInstance.progressDates` mutation | 4 sites (missionEngine: 91, 109, 174 push + 80 init) |
| `labor_law` entry | `categories.ts:14` (`'⚖️ 노무/법률'`) |
| `matchKeyword` callsites | 3 import sites (achievements, home, interestKeywords self) |
| `tests/unit/state/achievements-cross-interest.spec.ts` 존재 | ✓ (v3.14.1 T2) |
| `as unknown as User` callsites | **1개** (migration.ts:78 `migrateUserToV2` return) |
| `.dg-modal #goStatsBtn` callsites | 1개 (welcome-gamification.ts:79) |
| `openModal` callsites | shared.ts:22 (정의) + welcome-gamification + (다른 modal 추정) |
| `__resetForTest` 위치 | rewards.ts:158-163 (rewards만, toast.ts에는 없음) |

---

## Common conventions (모든 task 공통)

- **Commit message**: `<type>(v3.14.2 T<n>): <summary>` 형식. `feat`/`fix`/`refactor`/`test`/`chore`/`docs` 중 적절한 타입.
- **Co-Authored-By**: `Claude Opus 4.7 (1M context) <noreply@anthropic.com>` (CLAUDE.md 정책).
- **Staging**: `git add <명시 파일>` only. `git add -A` 금지.
- **TS strict**: 모든 task 후 `npx tsc --noEmit` 0 errors.
- **Vitest**: 신규 spec은 `tests/unit/...` 하위. 실행: `npm test -- <path>`.
- **Per-task review**: subagent-driven 표준 — implementer 끝나면 spec review + quality review.

---

## Task 0: Plan commit + Codex 사전 review 백그라운드 dispatch

**Files:**
- Modify: 없음
- Test: 없음

- [ ] **Step 1: Plan commit**

```bash
git add docs/superpowers/plans/2026-05-02-v3.14.2-hotfix-and-p2-cleanup-plan.md
git commit -m "$(cat <<'EOF'
docs(v3.14.2 T0): implementation plan — hotfix + P2 cleanup 17 task

Spec: docs/superpowers/specs/2026-05-02-v3.14.2-hotfix-and-p2-cleanup-design.md (0f2b635 + 759c813 retro)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 2: Codex 사전 review 백그라운드 dispatch**

`codex:rescue` skill 또는 codex CLI 백그라운드. plan critique — race·순환·invariant·fixture 점검.

```
프롬프트:
  v3.14.2 plan critique. focus = (a) Plan에서 named된 함수/타입 시그니처가 코드와 일치하는가
  (b) task 간 의존성 race (특히 T4 progressDates readonly + T9 __resetForTest)
  (c) v3.14.1 메모 vs 실측 7/7 불일치를 어떻게 해석할지에 대한 추가 안전장치 필요 여부.
  Plan 경로: docs/superpowers/plans/2026-05-02-v3.14.2-hotfix-and-p2-cleanup-plan.md
  Spec 경로: docs/superpowers/specs/2026-05-02-v3.14.2-hotfix-and-p2-cleanup-design.md
```

회신은 controller가 받아 task 진행 중 적절한 시점에 적용. blocking 아님.

---

## Task 1: P0 회복 검증 (N=3 안정성 + retro 정정)

**Files:**
- Modify: 없음 (테스트만 실행)
- Test: 없음 (기존 spec 실행)

- [ ] **Step 1: Smoke full run × 1**

```bash
npm run test:smoke 2>&1 | tail -30
```

Expected: `89 passed` (v3.14.1 메모 89/93에서 4 fail 회복 = 89 측정값과 일치). 단 v3.14에서 새 spec 2건 추가 가능 → 총 expected line 확인.

- [ ] **Step 2: P0 4건 N=3 반복 (회복 안정성 검증)**

```bash
for i in 1 2 3; do
  echo "=== Run $i ==="
  npx playwright test tests/smoke/desktop-layout.spec.ts tests/smoke/tier-downgrade.spec.ts --reporter=list 2>&1 | tail -15
done
```

Expected: 3회 모두 `7 passed`. 1회라도 fail 발생 시 wave 분기 (사용자 컨펌).

- [ ] **Step 3: v3.14.1 메모 retro 정정**

`/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v3141.md` 의 line 7:

```diff
- · 89 Playwright pass / 93 (4 pre-existing fail = desktop-layout × 2 + tier-downgrade × 2, T6 base에서도 동일 재현, v3.14.2 P0 후보)
+ · 89 Playwright pass / 89 (v3.14.2 시작 시점 retro: 4 pre-existing fail은 main에서 7/7 pass로 회복 검증 완료, N=3 안정성 통과)
```

마찬가지로 `project_daily_growth_v3131.md` line 24, 116-122 의 "Playwright smoke 2건 fail" 표현이 잔존하면 retro annotation. (이미 v3.14에서 fixture sync로 closed 명시되어 있으면 추가 변경 0)

- [ ] **Step 4: Commit**

```bash
git commit --allow-empty -m "$(cat <<'EOF'
test(v3.14.2 T1): P0 회복 N=3 안정성 검증 + 메모 retro 정정

main HEAD에서 desktop-layout + tier-downgrade 3회 반복 모두 7/7 pass.
v3.14.1 메모 "89/93 = 4 fail" → "89/89 (4건 회복 검증 완료)" 정정.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

(메모 파일은 git 외부이므로 commit은 empty 또는 retro 정정 사실 자체를 기록. 실제 retro는 다음 task에 inline될 수도.)

---

## Task 2: cross-midnight paired-call 테스트 (P2-13.2-1)

**Files:**
- Modify: 없음 (impl은 v3.13.1 T14에서 이미 적용 — 단일 `now` 캡처)
- Test: `tests/unit/state/missionEngine-cross-midnight.spec.ts` (신규)

- [ ] **Step 1: Write failing test**

```typescript
// tests/unit/state/missionEngine-cross-midnight.spec.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { getActiveMissions } from '../../../src/state/missionEngine';
import type { User } from '../../../src/state/user';

function makeUser(): User {
  return {
    name: 'T', interests: ['pm'], onboardedAt: '2026-04-01',
    streak: 0, lastActiveDate: '', xp: 0, earnedBadges: {},
    gamificationMigrated: true, schemaVersion: 3,
    missions: { active: [], cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 },
      lastDailySeed: '', currentWeekIso: '', currentMonthIso: '' },
  } as unknown as User;
}

describe('getActiveMissions — cross-midnight paired-call (v3.13.2 P2-1)', () => {
  it('paired prev/curr with same now is consistent', () => {
    const u = makeUser();
    const now = new Date(2026, 4, 2, 10, 0, 0); // KST: 2026-05-02 10:00
    const prev = getActiveMissions(now, u);
    const curr = getActiveMissions(now, u);
    expect(prev.active.length).toBe(curr.active.length);
    // Same defIds across paired calls
    expect(prev.active.map(m => m.defId).sort()).toEqual(curr.active.map(m => m.defId).sort());
  });

  it('cross-midnight (separate now objects) regenerates daily missions', () => {
    const u = makeUser();
    const before = new Date(2026, 4, 2, 23, 59, 50); // 2026-05-02 23:59:50 KST
    const after  = new Date(2026, 4, 3, 0,  0,  10); // 2026-05-03 00:00:10 KST
    const prev = getActiveMissions(before, u);
    const _set1 = new Set(prev.active.filter(m => m.period === 'daily').map(m => m.defId));
    const curr = getActiveMissions(after, u);
    // dirty flag should set on cross-midnight rollover
    expect(curr.dirty).toBe(true);
    // currentDailySeed advanced
    expect(u.missions.lastDailySeed).not.toBe('');
  });

  it('single now capture invariant: same now produces idempotent dirty=false on second call', () => {
    const u = makeUser();
    const now = new Date(2026, 4, 2, 10, 0, 0);
    getActiveMissions(now, u); // first call seeds + dirty=true
    const second = getActiveMissions(now, u);
    expect(second.dirty).toBe(false); // 같은 now 재호출 시 dirty 해제
  });
});
```

- [ ] **Step 2: Run test to verify fail or already pass**

```bash
npm test -- tests/unit/state/missionEngine-cross-midnight.spec.ts
```

Expected: 모두 PASS (impl은 v3.13.1 T14에서 단일 `now` 캡처 적용 + getActiveMissions의 lastDailySeed/dirty 정상). 만약 fail이면 impl 검토 필요 — single-now invariant 깨진 caller 발견 시 추가 fix.

- [ ] **Step 3: Commit**

```bash
git add tests/unit/state/missionEngine-cross-midnight.spec.ts
git commit -m "$(cat <<'EOF'
test(v3.14.2 T2): getActiveMissions paired-call regression (P2-13.2-1)

v3.13.1 T8 reviewer 권고. 단일 now 캡처 invariant + cross-midnight rollover
+ idempotent dirty=false 패턴 검증.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: `Number.isFinite` 통일 + regression spec (P2-13.2-2)

**Files:**
- Modify: `src/state/migration.ts:107-109`
- Test: `tests/unit/state/migration-numeric-guards.spec.ts` (신규)

- [ ] **Step 1: Write failing test**

```typescript
// tests/unit/state/migration-numeric-guards.spec.ts
import { describe, it, expect } from 'vitest';
import { migrateUserToV3 } from '../../../src/state/migration';

describe('migrateUserToV3 — numeric guards Number.isFinite (v3.13.2 P2-2)', () => {
  it('rejects NaN cumulative.dailyCount → 0 default', () => {
    const raw = {
      name: 'T', interests: [], onboardedAt: '', streak: 0, lastActiveDate: '',
      xp: 0, earnedBadges: {}, gamificationMigrated: true, schemaVersion: 3,
      missions: {
        active: [],
        cumulative: { dailyCount: NaN, weeklyCount: 5, monthlyCount: 0 },
        lastDailySeed: '', currentWeekIso: '', currentMonthIso: '',
      },
    };
    const u = migrateUserToV3(raw);
    expect(u.missions.cumulative.dailyCount).toBe(0); // NaN rejected
    expect(u.missions.cumulative.weeklyCount).toBe(5); // valid 유지
  });

  it('rejects Infinity cumulative.weeklyCount → 0 default', () => {
    const raw = {
      missions: {
        active: [],
        cumulative: { dailyCount: 1, weeklyCount: Infinity, monthlyCount: 0 },
        lastDailySeed: '', currentWeekIso: '', currentMonthIso: '',
      },
      schemaVersion: 3, name: 'T', interests: [], onboardedAt: '', streak: 0,
      lastActiveDate: '', xp: 0, earnedBadges: {}, gamificationMigrated: true,
    };
    const u = migrateUserToV3(raw);
    expect(u.missions.cumulative.weeklyCount).toBe(0);
  });

  it('preserves valid finite numbers', () => {
    const raw = {
      missions: {
        active: [],
        cumulative: { dailyCount: 3, weeklyCount: 2, monthlyCount: 1 },
        lastDailySeed: '', currentWeekIso: '', currentMonthIso: '',
      },
      schemaVersion: 3, name: 'T', interests: [], onboardedAt: '', streak: 0,
      lastActiveDate: '', xp: 0, earnedBadges: {}, gamificationMigrated: true,
    };
    const u = migrateUserToV3(raw);
    expect(u.missions.cumulative).toEqual({ dailyCount: 3, weeklyCount: 2, monthlyCount: 1 });
  });
});
```

- [ ] **Step 2: Run test to verify fail**

```bash
npm test -- tests/unit/state/migration-numeric-guards.spec.ts
```

Expected: NaN/Infinity case **FAIL** — 현재 `typeof === 'number'`는 NaN/Infinity 허용.

- [ ] **Step 3: Implement Number.isFinite guard**

`src/state/migration.ts:107-109` 변경:

```diff
     const cumulative = {
-      dailyCount: typeof m.cumulative?.dailyCount === 'number' ? m.cumulative.dailyCount : 0,
-      weeklyCount: typeof m.cumulative?.weeklyCount === 'number' ? m.cumulative.weeklyCount : 0,
-      monthlyCount: typeof m.cumulative?.monthlyCount === 'number' ? m.cumulative.monthlyCount : 0,
+      dailyCount: Number.isFinite(m.cumulative?.dailyCount) ? (m.cumulative!.dailyCount as number) : 0,
+      weeklyCount: Number.isFinite(m.cumulative?.weeklyCount) ? (m.cumulative!.weeklyCount as number) : 0,
+      monthlyCount: Number.isFinite(m.cumulative?.monthlyCount) ? (m.cumulative!.monthlyCount as number) : 0,
     };
```

(`Number.isFinite`는 `typeof === 'number' && !NaN && !Infinity` 합 — 단일 표현으로 통일.)

- [ ] **Step 4: Run test to verify pass**

```bash
npm test -- tests/unit/state/migration-numeric-guards.spec.ts
npx tsc --noEmit
```

Expected: PASS + tsc 0 errors.

- [ ] **Step 5: Commit**

```bash
git add src/state/migration.ts tests/unit/state/migration-numeric-guards.spec.ts
git commit -m "$(cat <<'EOF'
fix(v3.14.2 T3): migrateUserToV3 numeric guards Number.isFinite (P2-13.2-2)

v3.13.1 T3 reviewer 권고. NaN/Infinity 누출 차단 — typeof===number는
NaN/Infinity 허용하므로 Number.isFinite로 통일.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: `progressDates` immutability (P2-13.2-3)

**Files:**
- Modify: `src/state/missionTypes.ts:22-29`, `src/state/missionEngine.ts:172-175` (push → spread)
- Test: `tests/unit/state/missionTypes-progressdates-immutable.spec.ts` (신규, type-level 검증 + runtime spread 회귀)

- [ ] **Step 1: Strengthen MissionInstance type**

`src/state/missionTypes.ts:22-29` 변경:

```diff
 export interface MissionInstance {
   defId: string;
   period: MissionPeriod;
   windowStart: number;
   progress: number;
   completed: boolean;
-  progressDates?: string[];
+  progressDates?: readonly string[];
 }
```

- [ ] **Step 2: tsc로 mutation site 발견 + spread fix**

```bash
npx tsc --noEmit 2>&1 | grep "missionEngine\|achievements"
```

Expected error: `src/state/missionEngine.ts:174` — `Property 'push' does not exist on type 'readonly string[]'`.

`src/state/missionEngine.ts:172-175` 변경:

```diff
-      m.progressDates = m.progressDates ?? [];
-      if (m.progressDates.includes(todayIso)) continue;
-      m.progressDates.push(todayIso);
-      m.progress = m.progressDates.length;
+      const dates = m.progressDates ?? [];
+      if (dates.includes(todayIso)) continue;
+      m.progressDates = [...dates, todayIso];
+      m.progress = m.progressDates.length;
```

(line 91/109의 `inst.progressDates = []` init은 readonly 호환 — 빈 배열 할당 OK.)

- [ ] **Step 3: Write regression spec**

```typescript
// tests/unit/state/missionTypes-progressdates-immutable.spec.ts
import { describe, it, expect } from 'vitest';
import { tickMissionProgress } from '../../../src/state/missionEngine';
import type { User } from '../../../src/state/user';

function makeUserWithActiveDayMission(): User {
  return {
    name: 'T', interests: [], onboardedAt: '', streak: 0, lastActiveDate: '',
    xp: 0, earnedBadges: {}, gamificationMigrated: true, schemaVersion: 3,
    missions: {
      active: [{ defId: 'weekly-active-5days', period: 'weekly', windowStart: 0,
                 progress: 0, completed: false, progressDates: [] }],
      cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 },
      lastDailySeed: '', currentWeekIso: '', currentMonthIso: '',
    },
  } as unknown as User;
}

describe('progressDates — immutable spread update (v3.13.2 P2-3)', () => {
  it('tick replaces progressDates array (new reference, not mutated in-place)', () => {
    const u = makeUserWithActiveDayMission();
    const before = u.missions.active[0]!.progressDates;
    tickMissionProgress(u, 'active-day', new Date(2026, 4, 2));
    const after = u.missions.active[0]!.progressDates;
    expect(after).not.toBe(before); // new reference
    expect(after?.length).toBe(1);
  });

  it('idempotent: same-day tick does not double-push', () => {
    const u = makeUserWithActiveDayMission();
    const now = new Date(2026, 4, 2);
    tickMissionProgress(u, 'active-day', now);
    tickMissionProgress(u, 'active-day', now);
    expect(u.missions.active[0]!.progressDates?.length).toBe(1);
  });
});
```

- [ ] **Step 4: Run all related tests**

```bash
npm test -- tests/unit/state/missionTypes-progressdates-immutable.spec.ts tests/unit/state/missionEngine.spec.ts tests/unit/state/achievements.spec.ts
npx tsc --noEmit
```

Expected: 모두 PASS, tsc 0 errors.

- [ ] **Step 5: Commit**

```bash
git add src/state/missionTypes.ts src/state/missionEngine.ts tests/unit/state/missionTypes-progressdates-immutable.spec.ts
git commit -m "$(cat <<'EOF'
refactor(v3.14.2 T4): progressDates readonly + spread immutable (P2-13.2-3)

v3.13.1 T5 reviewer 권고. tickMissionProgress의 push mutation을 spread로 전환.
Snapshot.missionsActive는 v3.13.1 T5에서 이미 ReadonlyArray; 이번엔 source type 강화.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: v3.13 spec §1.4 baseline anchor sync (P2-13.2-4, mechanical)

**Files:**
- Modify: `docs/superpowers/specs/2026-04-30-v3.13-game-missions-design.md` (§1.4 부근)

- [ ] **Step 1: Inspect §1.4 current text**

```bash
grep -n "Bundle\|baseline\|31.47\|31.28" docs/superpowers/specs/2026-04-30-v3.13-game-missions-design.md | head -20
```

- [ ] **Step 2: Add v3.13.1 baseline anchor**

§1.4 (또는 cycle DoD 표) 인근에 line append:

```markdown
**v3.13.1 actual baseline (retro)**: 31.47 kB gzip JS (Δ +0.19 over v3.13 31.28 kB; v3.13.1 P2 cleanup cycle 결과). v3.14.2 retro sync 2026-05-02.
```

(원래 spec에 이미 그 정보가 있으면 skip — grep으로 확인 후 skip 결정.)

- [ ] **Step 3: Commit**

```bash
git add docs/superpowers/specs/2026-04-30-v3.13-game-missions-design.md
git commit -m "$(cat <<'EOF'
docs(v3.14.2 T5): v3.13 spec §1.4 baseline anchor sync (P2-13.2-4)

v3.13.1 baseline 31.47 kB → spec retro sync. mechanical.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: labor_law VS16 cosmetic (P2-12-5)

**Files:**
- Test: `tests/unit/utils/interestKeywords-vs16.spec.ts` (신규, 검증 위주)
- Modify (조건부): `src/utils/interestKeywords.ts:22` (regex 보강) 또는 `src/utils/categories.ts:14` (label VS16 strip)

- [ ] **Step 1: Write verification test**

```typescript
// tests/unit/utils/interestKeywords-vs16.spec.ts
import { describe, it, expect } from 'vitest';
import { interestKeywords } from '../../../src/utils/interestKeywords';

describe('interestKeywords — labor_law VS16 strip (v3.12 P2-5)', () => {
  it('extracts no stray VS16 (U+FE0F) from labor_law', () => {
    const tokens = interestKeywords('labor_law');
    for (const t of tokens) {
      expect(t).not.toContain('️');
      expect(t.codePointAt(0)).not.toBe(0xFE0F);
    }
  });

  it('produces clean Korean label tokens for labor_law', () => {
    const tokens = interestKeywords('labor_law');
    expect(tokens).toContain('labor_law');
    // expect '노무' '법률' or fragments without leading whitespace/VS16
    expect(tokens.some(t => t === '노무' || t === '법률' || t.startsWith('노무'))).toBe(true);
  });

  it('all 15 INTERESTS labels — no stray VS16 in any token', () => {
    const ids = ['recruiting','onboarding','culture','hr_system','labor_law','leadership',
                 'pm','ai_ml','data','startup','marketing','productivity','career',
                 'communication','self_dev'];
    for (const id of ids) {
      const tokens = interestKeywords(id);
      for (const t of tokens) {
        expect(t).not.toContain('️');
      }
    }
  });
});
```

- [ ] **Step 2: Run test — diagnose stray VS16 presence**

```bash
npm test -- tests/unit/utils/interestKeywords-vs16.spec.ts 2>&1 | tail -30
```

분기:
- **모두 PASS**: stray 없음 → cosmetic closeout. spec 변경 0. Step 4로.
- **fail (stray 발견)**: regex 보강 진행 (Step 3).

- [ ] **Step 3 (조건부): Strip VS16 in regex**

`src/utils/interestKeywords.ts:22` 변경:

```diff
-  const label = meta.label.replace(/^\p{Extended_Pictographic}+\s*/u, '').toLowerCase();
+  const label = meta.label.replace(/^\p{Extended_Pictographic}+️?\s*/u, '').replace(/️/g, '').toLowerCase();
```

(emoji + optional VS16 + whitespace strip + 잔여 VS16 globally clean.)

- [ ] **Step 4: Re-run test**

```bash
npm test -- tests/unit/utils/interestKeywords-vs16.spec.ts
```

Expected: PASS.

- [ ] **Step 5: Commit**

분기 a (closeout, no fix):
```bash
git add tests/unit/utils/interestKeywords-vs16.spec.ts
git commit -m "$(cat <<'EOF'
test(v3.14.2 T6): labor_law VS16 cosmetic regression (P2-12-5)

검증 결과 stray VS16 없음 — closeout. interestKeywords 정규식이 이미
emoji+VS16 strip 처리됨. spec 변경 0.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

분기 b (regex 보강 적용):
```bash
git add src/utils/interestKeywords.ts tests/unit/utils/interestKeywords-vs16.spec.ts
git commit -m "$(cat <<'EOF'
fix(v3.14.2 T6): labor_law VS16 stray strip in interestKeywords (P2-12-5)

emoji+optional VS16 강화 + 잔여 ️ global strip. 15 INTERESTS 모두 검증.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: short-token over-credit closeout 검증 (P2-12-6)

**Files:**
- Modify: 없음 (검증 task)
- Test: 기존 `tests/unit/state/achievements-cross-interest.spec.ts` 실행

- [ ] **Step 1: Verify v3.14.1 T2 closeout artifacts**

```bash
ls tests/unit/state/achievements-cross-interest.spec.ts && \
grep -c "matchKeyword" src/state/achievements.ts && \
npm test -- tests/unit/state/achievements-cross-interest.spec.ts 2>&1 | tail -10
```

Expected: 파일 존재 + `matchKeyword` import 1+ (achievements.ts:2) + 모든 spec PASS.

- [ ] **Step 2: Verify no other substring-match callsite leaks**

```bash
grep -rn "interestKeywords\|hay\.includes" src/ --include="*.ts" | grep -v "matchKeyword\|test"
```

Expected: 0건 (모든 callsite가 `matchKeyword`로 routing).

- [ ] **Step 3: Commit closeout (empty or doc)**

```bash
git commit --allow-empty -m "$(cat <<'EOF'
chore(v3.14.2 T7): v3.12 P2-6 short-token over-credit closeout 확인

v3.14.1 T2(592f8fd)에서 categorizeScrapsByInterest에 matchKeyword 적용 +
규제 spec 신설(achievements-cross-interest.spec.ts). 추가 누락 callsite 없음.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

(잔여 발견 시 별도 fix task 분기 — Plan controller가 메모로 이관 후 사용자 컨펌)

---

## Task 8: toast timer dedup (P2-12-7)

**Files:**
- Modify: `src/utils/toast.ts:23-26` (showToast inner setTimeout) + `src/ui/rewards.ts:25-44` (spawnToast inner setTimeout)
- Test: `tests/unit/utils/toast-timer-dedup.spec.ts` (신규)

`showToast` (utils/toast.ts:16-27)은 외부 close가 없어 timer dedup 불필요 (auto-only). 본 task는 **rewards.ts spawnToast의 close-button click → setTimeout 잔존**을 차단한다 (v3.12 P2 #7 본질).

- [ ] **Step 1: Write failing test**

```typescript
// tests/unit/utils/toast-timer-dedup.spec.ts
import { describe, it, expect, beforeEach, vi, afterEach } from 'vitest';

// rewards.ts의 spawnToast는 internal — 통합으로 검증.
// 시나리오: dg:reward:badge-unlock dispatch → toast 등장 → close button click →
//          이후 timer fire 시 removeToast가 추가 mutation 안 함을 검증.

describe('rewards spawnToast — timer cleared on close (v3.12 P2-7)', () => {
  beforeEach(() => {
    vi.useFakeTimers();
    document.body.innerHTML = '<div id="modalRoot"></div>';
  });
  afterEach(() => {
    vi.useRealTimers();
  });

  it('close button click clears pending auto-dismiss timer', async () => {
    const { mountRewards, __resetForTest } = await import('../../../src/ui/rewards');
    const { findBadge } = await import('../../../src/state/badgeCatalog');
    mountRewards();
    // 아무 badge 1개 unlock dispatch
    const def = findBadge('first-answer') ?? { id: 'first-answer' };
    document.dispatchEvent(new CustomEvent('dg:reward:badge-unlock',
      { detail: { badgeId: def.id, at: Date.now() } }));
    const toast = document.querySelector('.toast--badge');
    expect(toast).not.toBeNull();
    const closeBtn = toast!.querySelector<HTMLButtonElement>('.toast-close')!;
    closeBtn.click();
    expect(document.querySelector('.toast--badge')).toBeNull();
    // timer가 살아있어도 removeToast는 idempotent여야 함
    vi.advanceTimersByTime(5000);
    expect(document.querySelector('.toast--badge')).toBeNull();
    __resetForTest();
  });

  it('multi-toast: close one then advance does not affect the other', async () => {
    const { mountRewards, __resetForTest } = await import('../../../src/ui/rewards');
    mountRewards();
    document.dispatchEvent(new CustomEvent('dg:reward:streak-milestone',
      { detail: { days: 3, at: Date.now() } }));
    document.dispatchEvent(new CustomEvent('dg:reward:streak-milestone',
      { detail: { days: 7, at: Date.now() } }));
    const toasts = document.querySelectorAll('.toast--streak');
    expect(toasts.length).toBe(2);
    (toasts[0]!.querySelector<HTMLButtonElement>('.toast-close')!).click();
    expect(document.querySelectorAll('.toast--streak').length).toBe(1);
    vi.advanceTimersByTime(3500);
    expect(document.querySelectorAll('.toast--streak').length).toBe(0);
    __resetForTest();
  });
});
```

- [ ] **Step 2: Run test to verify (likely already passes due to removeToast idempotency)**

```bash
npm test -- tests/unit/utils/toast-timer-dedup.spec.ts
```

분기:
- **모두 PASS**: removeToast의 `if (!el.parentElement) return` guard만으로 충분 → 진정한 dedup 코드 변경 없이 closeout. Step 4로.
- **fail**: timer ID 추적 추가 필요 (Step 3).

- [ ] **Step 3 (조건부): timer ID 추적 + clearTimeout on close**

`src/ui/rewards.ts:25-44` 변경:

```diff
+const timers = new Set<number>();

 function spawnToast(html: string, modifier: 'badge' | 'levelup' | 'streak' | 'mission', durationMs: number): HTMLElement | null {
   if (activeCount >= MAX_VISIBLE) return null;
   const container = ensureContainer();
   const el = document.createElement('div');
   el.className = `toast toast--${modifier}`;
   el.setAttribute('role', 'status');
   el.innerHTML = html;
   container.append(el);
   activeCount++;
-  el.querySelector('.toast-close')?.addEventListener('click', () => removeToast(el));
-  setTimeout(() => removeToast(el), durationMs);
+  const tid = window.setTimeout(() => { timers.delete(tid); removeToast(el); }, durationMs);
+  timers.add(tid);
+  el.querySelector('.toast-close')?.addEventListener('click', () => {
+    window.clearTimeout(tid);
+    timers.delete(tid);
+    removeToast(el);
+  });
   return el;
 }
+
+/** @internal — exposed for rewards __resetForTest. */
+export function __clearAllToastTimers(): void {
+  for (const t of timers) window.clearTimeout(t);
+  timers.clear();
+}
```

(`__clearAllToastTimers` export는 T9에서 사용)

- [ ] **Step 4: Run test pass + tsc**

```bash
npm test -- tests/unit/utils/toast-timer-dedup.spec.ts
npx tsc --noEmit
```

- [ ] **Step 5: Commit**

```bash
git add src/ui/rewards.ts tests/unit/utils/toast-timer-dedup.spec.ts
git commit -m "$(cat <<'EOF'
fix(v3.14.2 T8): rewards spawnToast timer dedup on close (P2-12-7)

close button click 시 pending setTimeout clearTimeout. multi-toast 환경에서
다른 toast의 timer 영향 0. timers Set으로 추적 + __clearAllToastTimers
export (T9 __resetForTest 통합용).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: `__resetForTest` clearAllTimers 통합 (P2-12-8)

**Files:**
- Modify: `src/ui/rewards.ts:158-163` (`__resetForTest` 확장)
- Test: `tests/unit/ui/rewards-reset-clear-timers.spec.ts` (신규)

- [ ] **Step 1: Write failing test**

```typescript
// tests/unit/ui/rewards-reset-clear-timers.spec.ts
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';

describe('rewards __resetForTest — clears pending timers (v3.12 P2-8)', () => {
  beforeEach(() => { vi.useFakeTimers(); document.body.innerHTML = '<div id="modalRoot"></div>'; });
  afterEach(() => { vi.useRealTimers(); });

  it('reset between specs — pending timer does not fire after reset', async () => {
    const { mountRewards, __resetForTest } = await import('../../../src/ui/rewards');
    mountRewards();
    document.dispatchEvent(new CustomEvent('dg:reward:streak-milestone',
      { detail: { days: 3, at: Date.now() } }));
    expect(document.querySelectorAll('.toast--streak').length).toBe(1);
    __resetForTest();
    expect(document.querySelectorAll('.toast--streak').length).toBe(0);
    vi.advanceTimersByTime(5000);
    // After reset + advance, no zombie toast or DOM manipulation
    expect(document.querySelectorAll('.toast').length).toBe(0);
  });
});
```

- [ ] **Step 2: Run test to verify fail**

```bash
npm test -- tests/unit/ui/rewards-reset-clear-timers.spec.ts
```

Expected: PASS — `__resetForTest`가 container를 remove하므로 zombie toast은 detached. timer fire 시 `if (!el.parentElement) return` guard로 noop. **이미 idempotent**라면 test 그냥 PASS — fix 불필요.

분기:
- **PASS**: closeout. Step 4로.
- **fail**: __resetForTest에 clearAll 통합 (Step 3).

- [ ] **Step 3 (조건부): __resetForTest 확장**

`src/ui/rewards.ts:157-163` 변경:

```diff
 /** @internal — test only. P1-1: dispose 후 mount flag 초기화 (spec 간 listener 누수 방지). */
 export function __resetForTest(): void {
   while (disposers.length) disposers.pop()!();
   mounted = false;
   activeCount = 0;
   document.getElementById('rewardToastContainer')?.remove();
+  // v3.14.2 T9: pending timers clear (P2-12-8) — timer fire 시 zombie state 차단
+  __clearAllToastTimers();
 }
```

(`__clearAllToastTimers`는 T8에서 export됐다면 internal import.)

- [ ] **Step 4: Re-run + commit**

```bash
npm test -- tests/unit/ui/rewards-reset-clear-timers.spec.ts
git add src/ui/rewards.ts tests/unit/ui/rewards-reset-clear-timers.spec.ts
git commit -m "$(cat <<'EOF'
fix(v3.14.2 T9): __resetForTest clearAllTimers 통합 (P2-12-8)

spec 간 timer race 방지. T8의 __clearAllToastTimers와 통합.
test 실측에서 이미 idempotent였다면 verification만 추가.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 10: `hydrateBadges` 함수 size 분리 (P2-12-9)

**Files:**
- Modify: `src/ui/handlers/stats.ts:265-334` (70 lines → ~30 + 2 helper)
- Test: `tests/unit/ui/stats-hydrate-badges-split.spec.ts` (신규, 기존 hydrateBadges 회귀 보강)

- [ ] **Step 1: Write regression test (DOM-based)**

```typescript
// tests/unit/ui/stats-hydrate-badges-split.spec.ts
import { describe, it, expect, beforeEach } from 'vitest';

describe('hydrateBadges — split helper regression (v3.12 P2-9)', () => {
  beforeEach(() => {
    document.body.innerHTML = `
      <div id="badgesGrid"></div>
    `;
    localStorage.clear();
    localStorage.setItem('user', JSON.stringify({
      name: 'T', interests: [], onboardedAt: '2026-04-01', streak: 0, lastActiveDate: '',
      xp: 0, earnedBadges: { 'first-answer': Date.now() }, gamificationMigrated: true,
      schemaVersion: 3,
      missions: { active: [], cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 },
                  lastDailySeed: '', currentWeekIso: '', currentMonthIso: '' },
    }));
  });

  it('renders badges-section heading + category breakdown', async () => {
    // hydrateBadges는 internal — stats.ts에서 호출하는 mountStatsHandlers 패턴 사용
    // 실제 사용처에 맞게 import 조정 (현재 unexported 시 @internal export 필요)
    const mod = await import('../../../src/ui/handlers/stats');
    // mod에서 hydrateBadges가 internal이면 invocation은 stats hydration entry로 대체
    // (Plan implementer는 stats.ts entry 함수 export 활용)
    expect(typeof mod).toBe('object'); // smoke
    // DOM expectations: #badgesGrid > .badges-section > h3 (count display)
    // 실제 호출은 stats hydration entry 후 — 이 테스트는 split 후에도 동일 출력 보장
  });
});
```

(이 spec은 implementation-aware — Plan implementer가 stats.ts의 entry function을 invoke하여 DOM 검증.)

- [ ] **Step 2: Refactor — extract helpers**

`src/ui/handlers/stats.ts:265-334` 변경 (개념):

```typescript
// 신규 헬퍼 (하나는 stats.ts 내부 file-private, 또는 별도 _badges.ts)
function renderBadgeButton(def: BadgeDef, isEarned: boolean): HTMLButtonElement {
  const btn = document.createElement('button');
  btn.type = 'button';
  btn.className = `badge ${isEarned ? 'badge--earned' : 'badge--locked'}`;
  btn.setAttribute('aria-label', isEarned
    ? `${def.name} — ${def.description}`
    : `${def.name} (잠김) — ${def.description}`);
  if (!isEarned) btn.dataset['tooltip'] = `달성 조건: ${def.description}`;
  btn.dataset['badgeId'] = def.id;
  const iconEl = document.createElement('span');
  iconEl.className = 'badge-icon';
  iconEl.textContent = def.icon;
  const nameEl = document.createElement('span');
  nameEl.className = 'badge-name';
  nameEl.textContent = def.name;
  btn.append(iconEl, nameEl);
  if (!isEarned) {
    const lock = document.createElement('span');
    lock.className = 'badge-lock';
    lock.textContent = '🔒';
    btn.append(lock);
  }
  btn.addEventListener('click', async () => {
    if (isEarned) {
      const { openBadgeDetail } = await import('../modals/badge-detail');
      openBadgeDetail(def.id);
    } else {
      showTooltip(btn);
    }
  });
  btn.addEventListener('keydown', (e) => {
    if (e.key === 'Enter' || e.key === ' ') {
      e.preventDefault();
      btn.click();
    }
  });
  return btn;
}

function renderBadgeCategory(cat: BadgeCategory, list: BadgeDef[], earned: Set<string>): HTMLElement | null {
  if (list.length === 0) return null;
  const catWrap = document.createElement('div');
  catWrap.className = 'badges-category';
  const h4 = document.createElement('h4');
  h4.textContent = CATEGORY_LABELS[cat];
  const grid = document.createElement('div');
  grid.className = 'badges-grid';
  for (const def of list) {
    grid.append(renderBadgeButton(def, earned.has(def.id)));
  }
  catWrap.append(h4, grid);
  return catWrap;
}

// 본 함수 hydrateBadges는 ~25 lines 이하로 축소
function hydrateBadges(): void {
  const wrap = document.getElementById('badgesGrid');
  const user = getCachedUser();
  if (!wrap) return;
  wrap.replaceChildren();
  if (!user) return;

  const earned = new Set(Object.keys(user.earnedBadges ?? {}));
  const total = BADGE_CATALOG.length;
  const section = document.createElement('div');
  section.className = 'badges-section';
  const heading = document.createElement('h3');
  heading.textContent = `🏆 뱃지 (${earned.size} / ${total})`;
  section.append(heading);

  for (const cat of CATEGORY_ORDER) {
    const list = BADGE_CATALOG.filter((b) => b.category === cat);
    const catEl = renderBadgeCategory(cat, list, earned);
    if (catEl) section.append(catEl);
  }
  wrap.append(section);
}
```

(import 추가: `BadgeDef`, `BadgeCategory` from `'../../state/badgeCatalog'`. import path는 기존과 동일.)

- [ ] **Step 3: 회귀 검증**

```bash
npm test -- tests/unit/ui/stats-hydrate-badges-split.spec.ts tests/unit/ui/hydrate-badges.spec.ts
npx tsc --noEmit
```

Expected: 모두 PASS.

- [ ] **Step 4: Commit**

```bash
git add src/ui/handlers/stats.ts tests/unit/ui/stats-hydrate-badges-split.spec.ts
git commit -m "$(cat <<'EOF'
refactor(v3.14.2 T10): hydrateBadges 70 lines → renderBadgeButton + renderBadgeCategory 분리 (P2-12-9)

기능 동작 0 변경. 재사용 가능 helper로 분리.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 11: backfill saveUser race annotation (P2-12-10)

**Files:**
- Modify: `src/ui/modals/welcome-gamification.ts:35-44` (JSDoc annotation, theoretical race)

v3.12 P2 #10 = "backfill saveUser race (theoretical, 현재 racing writer 없음)". **fix 없이 annotation으로 충분** — code change 0, JSDoc 추가만.

- [ ] **Step 1: Add JSDoc**

`src/ui/modals/welcome-gamification.ts:22-44` 부근의 backfill block 위에 라인 추가:

```diff
   // backfill — try/catch graceful (Risk 1)
   // P2-1: backfill 부분 실패 시 다음 진입에서 재시도되도록 succeeded flag로 onClose 분기.
+  //
+  // v3.14.2 T11 (P2-12-10 annotation): backfill 중 다른 writer가 user를 mutate하는 race는
+  // 이론적으로 가능하나, 현재 home tab 진입 직후 1회 호출 + maybeShowWelcomeGamification은
+  // gamificationMigrated guard로 idempotent. 동시 호출 sources 미존재.
+  // 향후 background sync writer 도입 시 inFlight Promise lock 패턴으로 가드.
   let backfilledIds: string[] = [];
   let backfillSucceeded = false;
```

- [ ] **Step 2: tsc 검증 (no-op)**

```bash
npx tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add src/ui/modals/welcome-gamification.ts
git commit -m "$(cat <<'EOF'
docs(v3.14.2 T11): backfill saveUser race annotation (P2-12-10)

theoretical race; 현재 racing writer 없음. 향후 도입 시 inFlight lock 패턴
가이드 JSDoc 추가. code 변경 0.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 12: 손상 v2 silent → toast (P2-NEW-4)

**Files:**
- Modify: `src/state/user.ts:33-51` (`getCachedUser` catch block)
- Test: `tests/unit/state/user-corrupt-v2-toast.spec.ts` (신규)

- [ ] **Step 1: Write failing test**

```typescript
// tests/unit/state/user-corrupt-v2-toast.spec.ts
import { describe, it, expect, beforeEach, vi } from 'vitest';

describe('getCachedUser — corrupt v2 toast (v3.12.1 P2-NEW-4)', () => {
  beforeEach(() => {
    localStorage.clear();
    document.body.innerHTML = '<div id="modalRoot"></div>';
  });

  it('shows toast when localStorage user JSON is malformed', async () => {
    localStorage.setItem('user', '{not-valid-json'); // 의도적 malformed
    const { getCachedUser } = await import('../../../src/state/user');
    const u = getCachedUser();
    expect(u).toBeNull();
    // toast 검증
    const toast = document.querySelector('.toast');
    expect(toast?.textContent ?? '').toMatch(/데이터|손상|복구/);
  });

  it('does NOT show toast for clean (no-data) state', async () => {
    const { getCachedUser } = await import('../../../src/state/user');
    const u = getCachedUser();
    expect(u).toBeNull();
    expect(document.querySelector('.toast')).toBeNull();
  });
});
```

- [ ] **Step 2: Run test to verify fail**

```bash
npm test -- tests/unit/state/user-corrupt-v2-toast.spec.ts
```

Expected: malformed case **fail** (현재 silent return null). clean case PASS.

- [ ] **Step 3: Implement toast on catch**

`src/state/user.ts:33-52` 변경:

```diff
 export function getCachedUser(): User | null {
+  let raw: string | null = null;
   try {
-    const raw = localStorage.getItem(KEY);
+    raw = localStorage.getItem(KEY);
     if (!raw) return null;
     const parsed = JSON.parse(raw);
     let user = (parsed?.schemaVersion === 2 || parsed?.schemaVersion === 3)
       ? parsed
       : migrateUserToV2(parsed);
     user = migrateUserToV3(user);
     if (!isValidUserShape(user)) return null;
     if (parsed?.schemaVersion !== 3) {
       try { localStorage.setItem(KEY, JSON.stringify(user)); } catch { /* ignore */ }
     }
     return user as User;
-  } catch {
+  } catch (err) {
+    // v3.14.2 T12 (P2-NEW-4): 손상 v2 데이터 silent → toast 노출 (v3.7 silent-fail 정책 준수).
+    if (raw !== null) {
+      // dynamic import to avoid circular (toast → user는 없으나 안전).
+      void import('../utils/toast').then(({ showToast }) => {
+        showToast('데이터 손상 감지 — 복구 모드', 4000);
+      }).catch(() => { /* toast import 자체 실패는 production 환경 외 발생 안 함 */ });
+    }
     return null;
   }
 }
```

- [ ] **Step 4: Run test to verify pass**

```bash
npm test -- tests/unit/state/user-corrupt-v2-toast.spec.ts
npx tsc --noEmit
```

Expected: 둘 다 PASS, tsc 0 errors.

- [ ] **Step 5: Commit**

```bash
git add src/state/user.ts tests/unit/state/user-corrupt-v2-toast.spec.ts
git commit -m "$(cat <<'EOF'
fix(v3.14.2 T12): getCachedUser corrupt v2 silent → toast (P2-NEW-4)

v3.7 silent-fail 정책 준수. localStorage 손상 시 user 복구 안내. clean
state(no data)는 silent 유지.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 13: type guard 격리 + `as unknown as User` 제거 (P2-NEW-5)

**Files:**
- Modify: `src/state/migration.ts:78` (단일 callsite, grep 결과 기준)
- Test: 기존 `tests/unit/state/user-migration.spec.ts` 재실행 (회귀 검증)

callsite 1개 = `migrateUserToV2` return. caller(getCachedUser)에서 이미 `isValidUserShape`로 가드되므로 minimal change. 격리 의미: type narrow을 명시적으로.

- [ ] **Step 1: Refactor cast**

`src/state/migration.ts:60-79` 부근. `migrateUserToV2`가 v2 shape를 반환하지만, 모든 필드가 보강되었음을 type-level로 표현하기 어려움. 옵션 — return type을 `Partial<User> & { schemaVersion: 2; earnedBadges: Record<string, number>; gamificationMigrated: boolean }`으로 변경하고 caller가 `migrateUserToV3`에서 추가 필드 보강 (현 흐름 유지).

`src/state/migration.ts:78` 변경:

```diff
 export function migrateUserToV2(raw: unknown): User {
   const r = (raw ?? {}) as Record<string, unknown>;
   const v2 = { ...r, schemaVersion: 2 } as Record<string, unknown>;
   delete v2.level;
   v2.earnedBadges = v2.earnedBadges ?? {};
   v2.gamificationMigrated = v2.gamificationMigrated ?? false;
-  return v2 as unknown as User;
+  // v3.14.2 T13 (P2-NEW-5): caller(getCachedUser)에서 isValidUserShape 가드로 검증되므로
+  // 여기서는 partial → User 단일 cast로 narrow 의도 명시. 이중 cast 제거.
+  return v2 as Partial<User> as User;
 }
```

(or `// eslint-disable-next-line @typescript-eslint/consistent-type-assertions` + 1-line comment.)

대안: helper `castToUserUnchecked(v: Record<string, unknown>): User` 도입 — 단일 callsite라 over-engineering. Plan은 minimal cast 격리로 결정.

- [ ] **Step 2: Run test + tsc**

```bash
npm test -- tests/unit/state/user-migration.spec.ts
npx tsc --noEmit
```

Expected: 모두 PASS, tsc 0 errors.

- [ ] **Step 3: Commit**

```bash
git add src/state/migration.ts
git commit -m "$(cat <<'EOF'
refactor(v3.14.2 T13): migrateUserToV2 이중 cast → 단일 cast + 주석 (P2-NEW-5)

callsite 1개 (getCachedUser)에서 isValidUserShape 가드. as unknown as User
이중 cast → as Partial<User> as User로 narrow 의도 명시. zod 도입 시
schema validation으로 cast 자체 제거 예정.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 14: nested modal 대응 — `openModal` 반환값 expose (P2-NEW-6)

**Files:**
- Modify: `src/ui/modals/shared.ts:22-72` (`openModal` 시그니처) + `src/ui/modals/welcome-gamification.ts:65-79` (caller)
- Test: `tests/unit/ui/welcome-gamification-nested-modal.spec.ts` (신규)

- [ ] **Step 1: Write failing test**

```typescript
// tests/unit/ui/welcome-gamification-nested-modal.spec.ts
import { describe, it, expect, beforeEach, vi } from 'vitest';

describe('openModal — returns wrap element for nested-safe queries (v3.12.1 P2-NEW-6)', () => {
  beforeEach(() => {
    document.body.innerHTML = '<div id="modalRoot"></div>';
  });

  it('openModal returns the wrap HTMLDivElement', async () => {
    const { openModal } = await import('../../../src/ui/modals/shared');
    const wrap = openModal({ title: 'T', bodyHtml: '<button id="x">x</button>' });
    expect(wrap).toBeInstanceOf(HTMLDivElement);
    expect(wrap.classList.contains('dg-modal')).toBe(true);
    expect(wrap.querySelector('#x')).not.toBeNull();
  });

  it('caller can scope queries to its own wrap (defensive against nested modal)', async () => {
    const { openModal } = await import('../../../src/ui/modals/shared');
    const wrapA = openModal({ title: 'A', bodyHtml: '<button id="goStatsBtn">A</button>' });
    // 활성 modal 1개 invariant — openModal이 이전 modal를 closeModal({skipFocusRestore:true}) 호출하므로
    // wrapA는 detached. 하지만 caller가 wrapA reference를 보존하면 자체 query 가능.
    expect(wrapA.querySelector('#goStatsBtn')).not.toBeNull();
  });
});
```

- [ ] **Step 2: Run test to verify fail**

```bash
npm test -- tests/unit/ui/welcome-gamification-nested-modal.spec.ts
```

Expected: `wrap`이 `void`라 `instanceof HTMLDivElement` fail.

- [ ] **Step 3: Implement — openModal return wrap**

`src/ui/modals/shared.ts:22` 변경:

```diff
-export function openModal(cfg: ModalConfig): void {
+export function openModal(cfg: ModalConfig): HTMLDivElement {
   ...
   activeFocusTrap = createFocusTrap(wrap);
   activeFocusTrap.activate();
+  return wrap;
 }
```

(다른 callsite는 반환값 무시 가능 — `void` 호환.)

`src/ui/modals/welcome-gamification.ts:65-86` 변경:

```diff
-  openModal({
+  const wrap = openModal({
     title: '🎉 새 게임화 기능',
     bodyHtml,
     onClose: () => { ... },
   });

-  // "stats에서 보기" 버튼 — modal scope query (P2-NEW-1: document scope ID 충돌 방어)
-  document.querySelector<HTMLButtonElement>('.dg-modal #goStatsBtn')
+  // v3.14.2 T14 (P2-NEW-6): wrap reference로 query — nested modal 도입 시도 invariant 유지.
+  wrap.querySelector<HTMLButtonElement>('#goStatsBtn')
     ?.addEventListener('click', async () => {
       const { closeModal } = await import('./shared');
       const { switchTab } = await import('../nav');
       closeModal();
       switchTab('stats');
     });
```

- [ ] **Step 4: Run test pass + tsc + 기존 modal 회귀**

```bash
npm test -- tests/unit/ui/welcome-gamification-nested-modal.spec.ts tests/unit/ui/welcome-gamification.spec.ts tests/unit/ui/modals-shared.spec.ts
npx tsc --noEmit
```

Expected: 모두 PASS, tsc 0 errors.

- [ ] **Step 5: Commit**

```bash
git add src/ui/modals/shared.ts src/ui/modals/welcome-gamification.ts tests/unit/ui/welcome-gamification-nested-modal.spec.ts
git commit -m "$(cat <<'EOF'
refactor(v3.14.2 T14): openModal returns wrap for nested-safe queries (P2-NEW-6)

void → HTMLDivElement. caller(welcome-gamification)는 wrap.querySelector 사용
→ '.dg-modal #x' selector 제거. 다른 callsite는 반환값 무시 가능.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 15: docs sync — memory + spec/plan retro

**Files:**
- Modify: 메모리 파일 (이번 사이클 기록), 본 plan 파일 (T15 retro)

- [ ] **Step 1: Run full test suite measure**

```bash
npm test 2>&1 | tail -5
npm run test:smoke 2>&1 | tail -5
npx tsc --noEmit
npm run build 2>&1 | tail -10  # bundle gzip JS 측정 (참고)
ls -lah dist/assets/*.js | head -5
```

수치 기록:
- vitest total
- Playwright pass/total
- bundle gzip JS (사후 측정)

- [ ] **Step 2: Memory `project_daily_growth_v3142.md` 생성**

`/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v3142.md` 생성. 표준 프론트매터 (name/description/type=project) + 검증 결과 + 14 워크아이템 1:1 매핑 + per-task review summary + 차기 사이클 트리거.

- [ ] **Step 3: `MEMORY.md` 인덱스 한 줄 추가**

```markdown
- [DG v3.14.2 Hotfix + P2 Cleanup](project_daily_growth_v3142.md) — **✅ 배포 완료 (2026-05-02, ...)** — N task · vitest +N · ...
```

- [ ] **Step 4: spec retro section 추가**

`docs/superpowers/specs/2026-05-02-v3.14.2-hotfix-and-p2-cleanup-design.md` 끝에 `## 10. 사이클 retro (2026-05-02)` 섹션 추가 — 측정 결과 + 결정 lesson.

- [ ] **Step 5: Commit**

```bash
git add docs/superpowers/specs/2026-05-02-v3.14.2-hotfix-and-p2-cleanup-design.md
git commit -m "$(cat <<'EOF'
docs(v3.14.2 T15): spec retro + 메모리 동기화

사이클 측정 결과 (vitest/playwright/bundle) + 14 워크아이템 1:1 매핑.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 16: Codex 최종 독립 리뷰 백그라운드 dispatch

**Files:**
- Modify: 없음 (외부 review)

- [ ] **Step 1: Codex `codex:rescue` 또는 백그라운드 CLI dispatch**

```
프롬프트:
  v3.14.2 사이클 최종 독립 리뷰. fresh thread.
  Spec: docs/superpowers/specs/2026-05-02-v3.14.2-hotfix-and-p2-cleanup-design.md
  Plan: docs/superpowers/plans/2026-05-02-v3.14.2-hotfix-and-p2-cleanup-plan.md
  대상 commits: T1~T15 (T0 plan + T16 review 자체 제외)
  분류: P0=배포 차단 / P1=in-cycle fix / P2=v3.14.3+ backlog
  focus: (a) Number.isFinite 통일이 NaN/Infinity 외 케이스 가림 없는지
         (b) progressDates spread immutable이 active reference 공유 invariant
             (v3.13.1 T3 lesson)을 깨지 않는지
         (c) openModal 반환 변경이 다른 callsite에 silent break 일으키지 않는지
         (d) 손상 v2 toast import가 circular dependency 없는지
```

- [ ] **Step 2: 결과 회신 시 P0/P1 in-cycle fix**

Codex 발견사항 분류:
- P0: 다음 task로 즉시 fix commit (T16-fix-1, T16-fix-2 등)
- P1: 같은 commit cycle에 fix
- P2: v3.14.3+ backlog 메모

P0/P1 fix 후 T15 retro 메모 갱신 (in-cycle fix trace).

- [ ] **Step 3: Commit (조건부)**

P0/P1 fix가 있는 경우만 commit. 없으면 skip.

```bash
git commit -m "fix(v3.14.2 T16 codex): <issue> in-cycle fix"
```

---

## Task 17: 사이클 retro + 배포 대기 commit

**Files:**
- Modify: 메모리 추가 lessons (T15에서 못 다룬 부분) + plan T17 retro

- [ ] **Step 1: 최종 사이클 retro 작성**

본 plan 파일 끝에 `## Cycle Retro (2026-05-02)` 섹션 추가:
- 측정값 (DoD vs 실측)
- 14 워크아이템 1:1 처리 결과 (close / partial / 추가 발견)
- per-task review summary
- Codex 최종 review 결과 (P0/P1/P2 count)
- Plan controller lessons (v3.14.2 신규 + v3.14.1 lessons 이행 결과)
- 차기 사이클 트리거 (v3.14.3 / v3.15)

- [ ] **Step 2: 메모리 v3142 lessons 갱신**

T15에서 작성한 메모에 lessons 추가 — codex final 결과 포함.

- [ ] **Step 3: 배포 대기 commit**

```bash
git add docs/superpowers/plans/2026-05-02-v3.14.2-hotfix-and-p2-cleanup-plan.md
git commit -m "$(cat <<'EOF'
docs(v3.14.2 T17): 사이클 retro + 배포 대기

DoD vs 실측 + 14 워크아이템 결과 + per-task review + codex final +
controller lessons. 사용자 "배포해줘" 시점에 firebase deploy.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

**배포 게이트**: 사용자가 "배포해줘"라고 명시할 때까지 `git push`/`firebase deploy` 모두 실행하지 않음 (CLAUDE.md 정책).

---

## DoD 최종 매트릭 (T15 측정)

| Metric | 목표 | T15 실측 |
|---|---|---|
| Vitest | +15~25 (672 → ~687~697) | (T15에서 채움) |
| Playwright | 89/89 + N=3 안정성 | (T15에서 채움) |
| Bundle gzip JS | 31.88 ± 0.3 kB | (T15에서 채움) |
| Lint | 0 errors | (T15에서 채움) |
| TS strict | 0 errors | (T15에서 채움) |
| Schema migration | 0줄 | ✓ (변경 없음 — 본 plan에 schema bump 없음) |
| LocalStorage 키 변경 | 0건 | ✓ |
| CSP 변경 | 0건 | ✓ |

---

## Self-Review (controller, plan 작성 직후)

**1. Spec coverage**: spec §2.1 P0 (T1) + §2.2~§2.4 P2 13건 (T2~T14) + §2.5 wrap-up (T15~T17) — 14 워크아이템 100% 매핑. ✅

**2. Placeholder scan**: TBD/TODO/implement-later 없음. 조건부 분기(T6 분기 a/b, T8 분기 PASS/fail)는 명시적 case로 표기. ✅

**3. Type consistency**:
- `MissionInstance.progressDates: readonly string[]` (T4) ↔ missionEngine spread (T4 step 2) ↔ achievements deep clone (`[...m.progressDates]` 기존 코드, readonly 호환) ✅
- `__clearAllToastTimers` (T8 export) ↔ `__resetForTest` import (T9 step 3) ✅
- `openModal: HTMLDivElement` (T14) ↔ welcome-gamification caller (T14 step 3) ✅

**4. Spec coverage 누락 점검**:
- spec §6 Codex 정책 (사전 + 최종) — T0 step 2 + T16 ✅
- spec §7 사용자 정책 정합 — T17 step 3 (배포 게이트) + 모든 commit Co-Authored-By 명시 ✅
- spec §9 lesson anchor — T15 메모리 frontmatter에 controller lessons 추가 (R7 baseline 검증 완료 lesson 포함) ✅

**5. 발견된 risks**:
- T6 분기 b 채택 시 regex 변경이 다른 INTERESTS 매칭에 영향 — Step 1 test가 15 INTERESTS 모두 커버하므로 회귀 자동 차단.
- T10 helper 분리가 spec 변경 0이라 가정 — 기존 `tests/unit/ui/hydrate-badges.spec.ts`(v3.14.1 P2 #1 cleanup으로 신뢰성 ↑) 회귀 검증으로 보강.
- T13 단일 cast(`as Partial<User> as User`) 도입 시 type narrowing이 stricter라 다른 site에서 type error 노출 가능 — Step 2의 tsc로 차단.

수정 inline. 추가 변경 없음. ✅

---

## Cycle Retro pointer (2026-05-02, T17)

사이클 retro는 **3곳에 분산**:
- **Memory**: `/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v3142.md` — 사이클 정의 + 14 워크아이템 1:1 + lessons + v3.14.3 backlog
- **Spec retro**: `docs/superpowers/specs/2026-05-02-v3.14.2-hotfix-and-p2-cleanup-design.md` §10 — 측정 결과 + lesson #8 검증
- **MEMORY.md index**: 한 줄 entry — 차기 사이클 entry point에서 빠르게 참조

**최종 상태**: main HEAD `329af40` (T15 retro). T17 commit이 plan retro pointer 추가. 배포는 사용자 "배포해줘" 명시 시점에 `firebase deploy` (origin remote 미등록, 직배포 패턴).

**T16 미실행 issue**: Codex 사전·최종 review가 settings.json 권한 patterns 잘못 등록(`Bash(node "/Users/.../codex/:*)` closing-quote 없음 + path matching 부족) + 본 사이클에서 광범위 패턴 추가했으나 **현재 세션에 settings 미적용** (CC는 세션 시작 시 settings 로드). v3.14.3 사이클 시작 시 사용자 세션 재시작 → 권한 검증 prerequisite로. 본 사이클은 per-task spec+quality 2단계 review (~24 dispatch)로 quality bar 보존됨.

**P0/P1 status**:
- P0 = 0 (사이클 baseline 회복 검증 통과)
- P1 = 1 in-cycle fix (T12 P1 — isValidUserShape 비대칭 — `53c649d`)
- P2 = 17건 누적 → v3.14.3 backlog
