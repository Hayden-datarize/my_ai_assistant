# v3.8 Answer Deletion UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** archive 답변을 타깃 / 벌크 / 전체 단위로 삭제 가능하게 만들고, 5초 in-memory Undo 안전망과 storage 에러 분기 토스트를 제공한다. XP/Streak/level/heatmap activity 마커는 보존(행동 변경 0).

**Architecture:** TDD per task. 의존성 순서로 6 task — `MSG` 카피 → `persistence` 신규 삭제 API → `showUndoToast` helper → archive 카드 ✕ → archive 선택 모드 → settings "데이터 삭제" 섹션. `window.confirm` 재사용, 위험 강조 클래스 신규 X(`btn-secondary` 재사용), 답변별 번역 캐시 동반 정리 N/A(브리핑 캐시만 존재 — Q5 무효).

**Tech Stack:** Vite + TypeScript SPA, vitest + Playwright, localStorage SSOT (`'dg.answers'` 키). v3.7 `getSaveErrorMessage` 헬퍼 재사용.

**Spec:** `docs/superpowers/specs/2026-04-27-v3.8-answer-deletion-design.md`

**Branch:** `feat/v3.8-answer-deletion` (이미 체크아웃됨, spec commit 2개 위에 작업)

**Commit convention:** `feat(v3.8 TN): ...` / `test(v3.8 TN): ...` / `fix(v3.8 TN): ...`. v3.7 패턴 따라 task별 단일 commit (test + impl 묶음) 권장.

---

## Task 1 — `MSG.*` 카피 추가 (의존성 root)

**Files:**
- Modify: `src/ui/messages.ts`
- Test: `tests/messages-delete.spec.ts` (신규)

**왜 먼저인가:** T2~T6의 confirm/토스트가 모두 이 키를 import. literal 사용 시 v3.7 P2-1 학습("카피 변경 시 spec fail 안전망") 위반.

- [ ] **Step 1: 신규 키 사용 테스트 작성**

`tests/messages-delete.spec.ts` 생성:

```ts
import { describe, it, expect } from 'vitest';
import { MSG } from '../src/ui/messages';

describe('MSG delete keys', () => {
  it('DELETE_CONFIRM_BULK formats with count', () => {
    expect(MSG.DELETE_CONFIRM_BULK(3)).toContain('3개');
    expect(MSG.DELETE_CONFIRM_BULK(3)).toContain('5초');
  });

  it('DELETE_CONFIRM_ALL formats with count', () => {
    expect(MSG.DELETE_CONFIRM_ALL(7)).toContain('7개');
    expect(MSG.DELETE_CONFIRM_ALL(7)).toContain('모두');
    expect(MSG.DELETE_CONFIRM_ALL(7)).toContain('5초');
  });

  it('exposes constant copy keys', () => {
    expect(MSG.DELETE_UNDO_TOAST).toBe('삭제됨');
    expect(MSG.DELETE_UNDO_ACTION).toBe('되돌리기');
    expect(MSG.DELETE_UNDO_RESTORED).toBe('복원됨');
    expect(MSG.DELETE_UNDO_FAILED).toBe('복원 실패');
  });
});
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

```bash
npx vitest run tests/messages-delete.spec.ts
```

Expected: FAIL — `Property 'DELETE_CONFIRM_BULK' does not exist on type ...` (TypeScript) 또는 undefined.

- [ ] **Step 3: `src/ui/messages.ts`에 키 추가**

기존 `MSG` 객체에 다음 항목 추가 (정확한 위치는 `partialTranslateFail` 인근 — 기존 함수형 카피와 그룹):

```ts
DELETE_CONFIRM_BULK: (n: number) =>
  `선택한 답변 ${n}개를 삭제할까요?\n5초 안에 되돌릴 수 있어요`,
DELETE_CONFIRM_ALL: (n: number) =>
  `archive 답변 ${n}개를 모두 삭제할까요?\n5초 안에 되돌릴 수 있어요`,
DELETE_UNDO_TOAST: '삭제됨',
DELETE_UNDO_ACTION: '되돌리기',
DELETE_UNDO_RESTORED: '복원됨',
DELETE_UNDO_FAILED: '복원 실패',
```

- [ ] **Step 4: 테스트 실행 → 통과 확인**

```bash
npx vitest run tests/messages-delete.spec.ts
```

Expected: PASS (3 tests).

- [ ] **Step 5: lint + typecheck**

```bash
npm run lint && npx tsc --noEmit
```

Expected: 0 error 0 warn.

- [ ] **Step 6: commit**

```bash
git add src/ui/messages.ts tests/messages-delete.spec.ts
git commit -m "feat(v3.8 T1): add MSG keys for delete confirm + undo toast"
```

---

## Task 2 — `persistence.ts` 답변 삭제 API 신규 export

**Files:**
- Modify: `src/state/persistence.ts`
- Test: `tests/persistence-delete.spec.ts` (신규)

**왜:** answers SSOT. T4~T6이 모두 의존. `loadAnswers`/`saveAnswers`/`appendAnswer` 옆에 `deleteAnswerById` / `deleteAnswersByIds` / `deleteAllAnswers` 추가.

- [ ] **Step 1: 단건 삭제 테스트 작성**

`tests/persistence-delete.spec.ts` 생성:

```ts
import { describe, it, expect, beforeEach, vi } from 'vitest';
import {
  saveAnswers,
  loadAnswers,
  deleteAnswerById,
  deleteAnswersByIds,
  deleteAllAnswers,
} from '../src/state/persistence';

const seed = () => {
  saveAnswers([
    { id: 'a', text: 'one' },
    { id: 'b', text: 'two' },
    { id: 'c', text: 'three' },
  ] as never);
};

beforeEach(() => {
  localStorage.clear();
});

describe('deleteAnswerById', () => {
  it('removes matching id and persists', () => {
    seed();
    deleteAnswerById('b');
    const after = loadAnswers();
    expect(after).toHaveLength(2);
    expect(after.find((a) => a.id === 'b')).toBeUndefined();
  });

  it('silent skip when id missing', () => {
    seed();
    deleteAnswerById('zzz');
    expect(loadAnswers()).toHaveLength(3);
  });

  it('propagates storage throw', () => {
    seed();
    const setItem = vi.spyOn(Storage.prototype, 'setItem').mockImplementationOnce(() => {
      throw new DOMException('quota', 'QuotaExceededError');
    });
    expect(() => deleteAnswerById('a')).toThrow(DOMException);
    setItem.mockRestore();
  });
});

describe('deleteAnswersByIds', () => {
  it('removes multiple matching ids atomically', () => {
    seed();
    deleteAnswersByIds(['a', 'c']);
    const after = loadAnswers();
    expect(after).toHaveLength(1);
    expect(after[0]?.id).toBe('b');
  });

  it('keeps others when partial match', () => {
    seed();
    deleteAnswersByIds(['a', 'unknown']);
    expect(loadAnswers().map((a) => a.id)).toEqual(['b', 'c']);
  });
});

describe('deleteAllAnswers', () => {
  it('clears array', () => {
    seed();
    deleteAllAnswers();
    expect(loadAnswers()).toEqual([]);
  });
});
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

```bash
npx vitest run tests/persistence-delete.spec.ts
```

Expected: FAIL — `deleteAnswerById is not a function` (또는 import error).

- [ ] **Step 3: `src/state/persistence.ts`에 3개 export 추가**

기존 `appendAnswer` 다음에 추가:

```ts
export function deleteAnswerById(id: string): void {
  const next = loadAnswers().filter((a) => a.id !== id);
  saveAnswers(next);
}

export function deleteAnswersByIds(ids: string[]): void {
  if (ids.length === 0) return;
  const set = new Set(ids);
  const next = loadAnswers().filter((a) => !set.has(a.id));
  saveAnswers(next);
}

export function deleteAllAnswers(): void {
  saveAnswers([]);
}
```

- [ ] **Step 4: 테스트 실행 → 통과 확인**

```bash
npx vitest run tests/persistence-delete.spec.ts
```

Expected: PASS (6 tests).

- [ ] **Step 5: 전체 vitest + lint + typecheck (회귀 점검)**

```bash
npm run lint && npx tsc --noEmit && npx vitest run
```

Expected: 0 error 0 warn, 359 → 365 pass (+6).

- [ ] **Step 6: commit**

```bash
git add src/state/persistence.ts tests/persistence-delete.spec.ts
git commit -m "feat(v3.8 T2): add deleteAnswerById/ByIds/All to persistence"
```

---

## Task 3 — `showUndoToast` helper

**Files:**
- Modify: `src/utils/toast.ts`
- Test: `tests/toast-undo.spec.ts` (신규)

**왜:** 기존 `showToast`는 단순 message + auto-dismiss. Undo 액션 버튼 없음. 별도 함수로 분리(시그니처 변경 0 → 회귀 차단).

먼저 기존 `src/utils/toast.ts`를 읽어 컨테이너/타이머 패턴을 파악해야 한다 — Step 0:

- [ ] **Step 0: 기존 toast.ts 구조 읽기**

```bash
cat src/utils/toast.ts
```

핵심 확인: 토스트 DOM 삽입 위치(예: `document.body` 또는 특정 컨테이너), auto-dismiss 타이머 변수명, CSS 클래스 컨벤션(예: `.toast`, `.toast--show`). 이 구조를 `showUndoToast`도 그대로 따른다.

- [ ] **Step 1: helper 테스트 작성**

`tests/toast-undo.spec.ts` 생성:

```ts
/**
 * @vitest-environment jsdom
 */
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';

beforeEach(() => {
  vi.useFakeTimers();
  document.body.replaceChildren();
});

afterEach(() => {
  vi.useRealTimers();
});

describe('showUndoToast', () => {
  it('auto-dismisses after duration without invoking onUndo', async () => {
    const { showUndoToast } = await import('../src/utils/toast');
    const onUndo = vi.fn();
    showUndoToast({
      message: '삭제됨',
      actionLabel: '되돌리기',
      onUndo,
      durationMs: 5000,
    });
    expect(document.querySelector('.toast')).not.toBeNull();
    vi.advanceTimersByTime(5001);
    expect(onUndo).not.toHaveBeenCalled();
    expect(document.querySelector('.toast')).toBeNull();
  });

  it('invokes onUndo and dismisses immediately on action click', async () => {
    const { showUndoToast } = await import('../src/utils/toast');
    const onUndo = vi.fn();
    showUndoToast({ message: '삭제됨', actionLabel: '되돌리기', onUndo });
    const btn = document.querySelector<HTMLButtonElement>('.toast button');
    expect(btn).not.toBeNull();
    btn!.click();
    expect(onUndo).toHaveBeenCalledTimes(1);
    expect(document.querySelector('.toast')).toBeNull();
  });

  it('dismisses previous toast and discards previous onUndo when new one shown', async () => {
    const { showUndoToast } = await import('../src/utils/toast');
    const first = vi.fn();
    const second = vi.fn();
    showUndoToast({ message: 'first', actionLabel: 'undo', onUndo: first });
    showUndoToast({ message: 'second', actionLabel: 'undo', onUndo: second });
    vi.advanceTimersByTime(5001);
    expect(first).not.toHaveBeenCalled();
    expect(second).not.toHaveBeenCalled();
  });
});
```

> **Note:** dynamic import + `vi.useFakeTimers` 패턴은 `tests/README.md` 가이드라인 준수. `afterEach`를 `it` 앞에 배치 (v3.7 P1 학습).

- [ ] **Step 2: 테스트 실행 → 실패 확인**

```bash
npx vitest run tests/toast-undo.spec.ts
```

Expected: FAIL — `showUndoToast is not exported`.

- [ ] **Step 3: `src/utils/toast.ts`에 helper 추가**

기존 `showToast` 다음 또는 같은 모듈 내 별도 함수로 추가. 핵심 구현(기존 컨테이너 패턴 재사용):

```ts
let activeUndoTimer: number | null = null;
let activeUndoEl: HTMLElement | null = null;

export function showUndoToast(opts: {
  message: string;
  actionLabel: string;
  onUndo: () => void;
  durationMs?: number;
}): void {
  const duration = opts.durationMs ?? 5000;

  // 이전 undo 토스트가 살아있으면 즉시 제거 + onUndo 폐기 (호출 X)
  if (activeUndoTimer !== null) {
    window.clearTimeout(activeUndoTimer);
    activeUndoTimer = null;
  }
  if (activeUndoEl) {
    activeUndoEl.remove();
    activeUndoEl = null;
  }

  const el = document.createElement('div');
  el.className = 'toast toast--undo';
  const msg = document.createElement('span');
  msg.textContent = opts.message;
  const btn = document.createElement('button');
  btn.type = 'button';
  btn.textContent = opts.actionLabel;
  btn.addEventListener('click', () => {
    if (activeUndoTimer !== null) {
      window.clearTimeout(activeUndoTimer);
      activeUndoTimer = null;
    }
    el.remove();
    activeUndoEl = null;
    opts.onUndo();
  });
  el.append(msg, btn);
  document.body.appendChild(el);
  activeUndoEl = el;

  activeUndoTimer = window.setTimeout(() => {
    el.remove();
    activeUndoEl = null;
    activeUndoTimer = null;
  }, duration);
}
```

> **Note:** Step 0에서 확인한 기존 `showToast`의 컨테이너 셀렉터/스타일과 일치하도록 `el.className` 조정 필요. 본 plan은 일반 `.toast` 가정 — Step 0 확인 후 정확한 클래스로 교체.

- [ ] **Step 4: 테스트 실행 → 통과 확인**

```bash
npx vitest run tests/toast-undo.spec.ts
```

Expected: PASS (3 tests).

- [ ] **Step 5: 전체 vitest + lint + typecheck**

```bash
npm run lint && npx tsc --noEmit && npx vitest run
```

Expected: 0 error 0 warn, 365 → 368 pass (+3).

- [ ] **Step 6: commit**

```bash
git add src/utils/toast.ts tests/toast-undo.spec.ts
git commit -m "feat(v3.8 T3): add showUndoToast helper with action button + 5s timer"
```

---

## Task 4 — archive 카드 ✕ 버튼 (타깃 삭제)

**Files:**
- Modify: `src/ui/tabs/archive.ts` (✕ 버튼 마크업)
- Modify: `src/ui/handlers/archive.ts` (이벤트 + 삭제 흐름)
- Test: `tests/archive-delete-target.spec.ts` (신규)

먼저 archive 마크업 구조를 정확히 파악 — Step 0:

- [ ] **Step 0: 기존 archive 카드 마크업 구조 확인**

```bash
sed -n '1,140p' src/ui/handlers/archive.ts
```

`rerenderList` 카드 루프(lines 98~117) 내 `archive-card` 마크업 정확히 확인. 카드 마크업이 string template 또는 `createElement` 인지, 클릭 위임이 있는지(예: `container.addEventListener('click', ...)`) 결정. 이 패턴을 ✕ 버튼에도 그대로 적용.

- [ ] **Step 1: 타깃 삭제 테스트 작성**

`tests/archive-delete-target.spec.ts` 생성:

```ts
/**
 * @vitest-environment jsdom
 */
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';
import { saveAnswers } from '../src/state/persistence';

beforeEach(() => {
  vi.useFakeTimers();
  localStorage.clear();
  document.body.replaceChildren();
});

afterEach(() => {
  vi.useRealTimers();
  vi.resetModules();
});

describe('archive target delete', () => {
  it('removes card from DOM and storage on ✕ click', async () => {
    saveAnswers([
      { id: 'a', text: 'one', date: '2026-04-27' },
      { id: 'b', text: 'two', date: '2026-04-27' },
    ] as never);

    const container = document.createElement('div');
    container.id = 'archiveTab';
    document.body.appendChild(container);

    const tab = await import('../src/ui/tabs/archive');
    const handlers = await import('../src/ui/handlers/archive');
    tab.renderArchive(container);
    handlers.mountArchiveHandlers();

    const card = document.querySelector<HTMLElement>(
      '.archive-card[data-answer-id="a"]'
    );
    expect(card).not.toBeNull();

    const btn = card!.querySelector<HTMLButtonElement>('.archive-card-delete');
    expect(btn).not.toBeNull();
    btn!.click();

    const { loadAnswers } = await import('../src/state/persistence');
    expect(loadAnswers()).toHaveLength(1);
    expect(loadAnswers()[0]?.id).toBe('b');
    expect(document.querySelector('.toast--undo')).not.toBeNull();
  });

  it('restores answer when undo clicked within 5s', async () => {
    saveAnswers([{ id: 'a', text: 'one', date: '2026-04-27' }] as never);

    const container = document.createElement('div');
    container.id = 'archiveTab';
    document.body.appendChild(container);
    const tab = await import('../src/ui/tabs/archive');
    const handlers = await import('../src/ui/handlers/archive');
    tab.renderArchive(container);
    handlers.mountArchiveHandlers();

    document
      .querySelector<HTMLButtonElement>('.archive-card-delete')!
      .click();
    document
      .querySelector<HTMLButtonElement>('.toast--undo button')!
      .click();

    const { loadAnswers } = await import('../src/state/persistence');
    expect(loadAnswers()).toHaveLength(1);
    expect(loadAnswers()[0]?.id).toBe('a');
  });

  it('shows error toast and keeps card on storage throw', async () => {
    saveAnswers([{ id: 'a', text: 'one', date: '2026-04-27' }] as never);
    const setItem = vi.spyOn(Storage.prototype, 'setItem').mockImplementation(() => {
      throw new DOMException('quota', 'QuotaExceededError');
    });

    const container = document.createElement('div');
    container.id = 'archiveTab';
    document.body.appendChild(container);
    const tab = await import('../src/ui/tabs/archive');
    const handlers = await import('../src/ui/handlers/archive');
    tab.renderArchive(container);
    handlers.mountArchiveHandlers();

    document.querySelector<HTMLButtonElement>('.archive-card-delete')!.click();

    expect(document.querySelector('.archive-card[data-answer-id="a"]')).not.toBeNull();
    expect(document.querySelector('.toast')).not.toBeNull(); // 에러 토스트
    setItem.mockRestore();
  });
});
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

```bash
npx vitest run tests/archive-delete-target.spec.ts
```

Expected: FAIL — `.archive-card-delete` 셀렉터 부재.

- [ ] **Step 3: `src/ui/tabs/archive.ts` 마크업에 ✕ 버튼 추가**

`rerenderList`의 카드 마크업에서 `data-answer-id="${id}"` attribute (이미 있을 수 있음 — Step 0 확인) 보장하고, 카드 내부에 ✕ 버튼 추가:

```html
<article class="archive-card" data-answer-id="${id}">
  <button class="archive-card-delete" type="button" aria-label="답변 삭제">×</button>
  <div class="archive-date">${date}</div>
  <div class="archive-text">${text}</div>
  <!-- 기존 archive-type 등 유지 -->
</article>
```

> **Note:** 기존 마크업이 createElement 패턴이면 `<button>` 노드 createElement로 동일하게. 기존이 string template이면 위 string 그대로 삽입. Step 0 확인 따라 결정.

- [ ] **Step 4: `src/ui/handlers/archive.ts`에 ✕ 클릭 핸들러 추가**

기존 `mountArchiveHandlers` 내 클릭 위임 패턴 확장 (또는 신규 위임). v3.7 `getSaveErrorMessage` import:

```ts
import {
  loadAnswers,
  appendAnswer,
  deleteAnswerById,
} from '../../state/persistence';
import { getSaveErrorMessage } from '../../state/user';
import { showToast, showUndoToast } from '../../utils/toast';
import { MSG } from '../messages';

// mountArchiveHandlers 내부 또는 별도 함수
function handleCardDeleteClick(e: Event): void {
  const btn = (e.target as HTMLElement).closest<HTMLButtonElement>(
    '.archive-card-delete'
  );
  if (!btn) return;
  e.stopPropagation(); // 카드 클릭(detail 모달 진입) 막기
  const card = btn.closest<HTMLElement>('.archive-card');
  const id = card?.dataset.answerId;
  if (!id) return;

  const snapshot = loadAnswers().find((a) => a.id === id);
  if (!snapshot) return;

  try {
    deleteAnswerById(id);
  } catch (err) {
    showToast(getSaveErrorMessage(err));
    return;
  }

  rerenderList();
  showUndoToast({
    message: MSG.DELETE_UNDO_TOAST,
    actionLabel: MSG.DELETE_UNDO_ACTION,
    onUndo: () => {
      try {
        appendAnswer(snapshot);
        rerenderList();
      } catch (err) {
        showToast(MSG.DELETE_UNDO_FAILED);
      }
    },
  });
}
```

`mountArchiveHandlers`에서 위임 등록 (예: `archiveContainer.addEventListener('click', handleCardDeleteClick)`).

- [ ] **Step 5: 테스트 실행 → 통과 확인**

```bash
npx vitest run tests/archive-delete-target.spec.ts
```

Expected: PASS (3 tests).

- [ ] **Step 6: 전체 vitest + lint + typecheck**

```bash
npm run lint && npx tsc --noEmit && npx vitest run
```

Expected: 0 error 0 warn, 368 → 371 pass (+3).

- [ ] **Step 7: Playwright 1 spec 추가**

`tests/e2e/archive-delete-target.spec.ts` 신규 (실제 Playwright 디렉토리는 plan 단계 확인 — `tests/e2e/` 또는 `e2e/`):

```ts
import { test, expect } from '@playwright/test';

test('archive 카드 ✕로 답변 삭제 후 undo로 복원', async ({ page }) => {
  await page.goto('/');
  // 답변 1건 미리 만들기 (storage seed 또는 home 탭에서 입력)
  await page.evaluate(() => {
    localStorage.setItem(
      'dg.answers',
      JSON.stringify([{ id: 'e2e-a', text: 'e2e test', date: '2026-04-27' }])
    );
  });
  await page.reload();
  await page.click('[data-tab="archive"]');
  await expect(page.locator('.archive-card[data-answer-id="e2e-a"]')).toBeVisible();
  await page.click('.archive-card[data-answer-id="e2e-a"] .archive-card-delete');
  await expect(page.locator('.archive-card[data-answer-id="e2e-a"]')).toHaveCount(0);
  await expect(page.locator('.toast--undo')).toBeVisible();
  await page.click('.toast--undo button');
  await expect(page.locator('.archive-card[data-answer-id="e2e-a"]')).toBeVisible();
});
```

- [ ] **Step 8: Playwright 실행**

```bash
npx playwright test tests/e2e/archive-delete-target.spec.ts
```

Expected: PASS (1 test). 실패 시 셀렉터/탭 진입 방식 정정.

- [ ] **Step 9: commit**

```bash
git add src/ui/tabs/archive.ts src/ui/handlers/archive.ts \
  tests/archive-delete-target.spec.ts tests/e2e/archive-delete-target.spec.ts
git commit -m "feat(v3.8 T4): add archive card ✕ button with undo toast + error fallback"
```

---

## Task 5 — archive 선택 모드 (벌크 삭제)

**Files:**
- Modify: `src/ui/tabs/archive.ts` (헤더 토글 버튼 + 벌크 삭제 버튼 마크업, 카드 체크박스 모드)
- Modify: `src/ui/handlers/archive.ts` (선택 상태 + 벌크 삭제 흐름)
- Modify: `src/styles/...` (`.archive--select-mode` 클래스 — 정확한 파일 plan 단계 확인, 가능성 높음: `src/styles/archive.css` 또는 main.css cascade layers)
- Test: `tests/archive-delete-bulk.spec.ts` (신규)

- [ ] **Step 0: archive 헤더 영역 + 스타일 진입점 확인**

```bash
grep -n "archive" src/styles/*.css 2>/dev/null | head -20
sed -n '1,50p' src/ui/tabs/archive.ts
```

archive 헤더 마크업이 어디에 있는지(예: `<h2>archive</h2>` 인근) + 스타일 파일 위치 확정.

- [ ] **Step 1: 벌크 삭제 테스트 작성**

`tests/archive-delete-bulk.spec.ts` 생성:

```ts
/**
 * @vitest-environment jsdom
 */
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';
import { saveAnswers } from '../src/state/persistence';

beforeEach(() => {
  vi.useFakeTimers();
  localStorage.clear();
  document.body.replaceChildren();
});

afterEach(() => {
  vi.useRealTimers();
  vi.resetModules();
});

const setupArchive = async () => {
  saveAnswers([
    { id: 'a', text: 'one', date: '2026-04-27' },
    { id: 'b', text: 'two', date: '2026-04-27' },
    { id: 'c', text: 'three', date: '2026-04-27' },
  ] as never);
  const container = document.createElement('div');
  container.id = 'archiveTab';
  document.body.appendChild(container);
  const tab = await import('../src/ui/tabs/archive');
  const handlers = await import('../src/ui/handlers/archive');
  tab.renderArchive(container);
  handlers.mountArchiveHandlers();
  return { tab, handlers, container };
};

describe('archive bulk delete', () => {
  it('toggles select mode and disables bulk button at 0 selected', async () => {
    await setupArchive();
    const toggle = document.querySelector<HTMLButtonElement>('#archiveSelectToggle');
    expect(toggle).not.toBeNull();
    toggle!.click();
    expect(toggle!.getAttribute('aria-pressed')).toBe('true');
    const bulk = document.querySelector<HTMLButtonElement>('#archiveBulkDelete');
    expect(bulk!.disabled).toBe(true);
    expect(bulk!.textContent).toContain('0');
  });

  it('selects cards and updates bulk count + enables button', async () => {
    await setupArchive();
    document.querySelector<HTMLButtonElement>('#archiveSelectToggle')!.click();
    const cardA = document.querySelector<HTMLElement>(
      '.archive-card[data-answer-id="a"]'
    )!;
    const cardB = document.querySelector<HTMLElement>(
      '.archive-card[data-answer-id="b"]'
    )!;
    cardA.click();
    cardB.click();
    const bulk = document.querySelector<HTMLButtonElement>('#archiveBulkDelete')!;
    expect(bulk.disabled).toBe(false);
    expect(bulk.textContent).toContain('2');
  });

  it('confirms then deletes selected and shows undo toast', async () => {
    await setupArchive();
    vi.spyOn(window, 'confirm').mockReturnValue(true);
    document.querySelector<HTMLButtonElement>('#archiveSelectToggle')!.click();
    document.querySelector<HTMLElement>('.archive-card[data-answer-id="a"]')!.click();
    document.querySelector<HTMLElement>('.archive-card[data-answer-id="c"]')!.click();
    document.querySelector<HTMLButtonElement>('#archiveBulkDelete')!.click();

    const { loadAnswers } = await import('../src/state/persistence');
    expect(loadAnswers().map((a) => a.id)).toEqual(['b']);
    expect(document.querySelector('.toast--undo')).not.toBeNull();
  });

  it('cancels confirm leaves answers untouched', async () => {
    await setupArchive();
    vi.spyOn(window, 'confirm').mockReturnValue(false);
    document.querySelector<HTMLButtonElement>('#archiveSelectToggle')!.click();
    document.querySelector<HTMLElement>('.archive-card[data-answer-id="a"]')!.click();
    document.querySelector<HTMLButtonElement>('#archiveBulkDelete')!.click();

    const { loadAnswers } = await import('../src/state/persistence');
    expect(loadAnswers()).toHaveLength(3);
    expect(document.querySelector('.toast--undo')).toBeNull();
  });

  it('undo restores all deleted answers', async () => {
    await setupArchive();
    vi.spyOn(window, 'confirm').mockReturnValue(true);
    document.querySelector<HTMLButtonElement>('#archiveSelectToggle')!.click();
    document.querySelector<HTMLElement>('.archive-card[data-answer-id="a"]')!.click();
    document.querySelector<HTMLElement>('.archive-card[data-answer-id="b"]')!.click();
    document.querySelector<HTMLButtonElement>('#archiveBulkDelete')!.click();
    document.querySelector<HTMLButtonElement>('.toast--undo button')!.click();

    const { loadAnswers } = await import('../src/state/persistence');
    expect(loadAnswers()).toHaveLength(3);
  });
});
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

```bash
npx vitest run tests/archive-delete-bulk.spec.ts
```

Expected: FAIL — `#archiveSelectToggle` / `#archiveBulkDelete` 부재.

- [ ] **Step 3: `src/ui/tabs/archive.ts` 헤더 마크업 추가**

archive 정적 렌더에서 헤더 영역에 다음 마크업 추가:

```html
<div class="archive-header">
  <h2>archive</h2>
  <button id="archiveSelectToggle" type="button" aria-pressed="false">선택</button>
  <button id="archiveBulkDelete" type="button" disabled>선택 항목 삭제 (0)</button>
</div>
```

기존 `<h2>archive</h2>` 등이 있다면 옆에 추가. 정확한 위치는 Step 0 확인 결과 따라.

- [ ] **Step 4: `src/styles/archive.css` (또는 동등)에 select-mode 클래스 추가**

```css
.archive-card-delete { /* T4에서 이미 추가했을 수도, 없으면 추가 */
  position: absolute;
  top: 8px;
  right: 8px;
}

.archive--select-mode .archive-card-delete {
  display: none;
}

.archive--select-mode .archive-card::before {
  content: '';
  /* 체크박스 표시 — 실제 디자인 토큰은 plan 단계 확인 */
  display: inline-block;
  width: 16px;
  height: 16px;
  border: 2px solid var(--color-border, currentColor);
  border-radius: 4px;
  margin-right: 8px;
}

.archive--select-mode .archive-card.selected::before {
  background: var(--color-accent, currentColor);
}
```

> **Note:** 정확한 CSS 토큰 변수명은 main.css cascade layers (v3.3.0 정비) 기반 — Step 0 확인.

- [ ] **Step 5: `src/ui/handlers/archive.ts`에 선택 상태 + 핸들러 추가**

기존 핸들러 위에 모듈 스코프 추가:

```ts
import { deleteAnswersByIds } from '../../state/persistence';

const selectedIds = new Set<string>();
let selectMode = false;

function updateBulkButton(): void {
  const bulk = document.getElementById('archiveBulkDelete') as HTMLButtonElement | null;
  if (!bulk) return;
  bulk.disabled = selectedIds.size === 0;
  bulk.textContent = `선택 항목 삭제 (${selectedIds.size})`;
}

function handleSelectToggle(): void {
  selectMode = !selectMode;
  const toggle = document.getElementById('archiveSelectToggle');
  toggle?.setAttribute('aria-pressed', String(selectMode));
  document.getElementById('archiveTab')?.classList.toggle('archive--select-mode', selectMode);
  if (!selectMode) {
    selectedIds.clear();
    document.querySelectorAll('.archive-card.selected').forEach((c) =>
      c.classList.remove('selected')
    );
  }
  updateBulkButton();
}

function handleCardClickInSelectMode(e: Event): void {
  if (!selectMode) return;
  const card = (e.target as HTMLElement).closest<HTMLElement>('.archive-card');
  if (!card) return;
  e.stopPropagation();
  const id = card.dataset.answerId;
  if (!id) return;
  if (selectedIds.has(id)) {
    selectedIds.delete(id);
    card.classList.remove('selected');
  } else {
    selectedIds.add(id);
    card.classList.add('selected');
  }
  updateBulkButton();
}

function handleBulkDeleteClick(): void {
  const n = selectedIds.size;
  if (n === 0) return;
  if (!window.confirm(MSG.DELETE_CONFIRM_BULK(n))) return;
  const ids = [...selectedIds];
  const snapshot = loadAnswers().filter((a) => selectedIds.has(a.id));
  try {
    deleteAnswersByIds(ids);
  } catch (err) {
    showToast(getSaveErrorMessage(err));
    return;
  }
  selectedIds.clear();
  selectMode = false;
  document.getElementById('archiveSelectToggle')?.setAttribute('aria-pressed', 'false');
  document.getElementById('archiveTab')?.classList.remove('archive--select-mode');
  rerenderList();
  updateBulkButton();
  showUndoToast({
    message: MSG.DELETE_UNDO_TOAST,
    actionLabel: MSG.DELETE_UNDO_ACTION,
    onUndo: () => {
      try {
        snapshot.forEach((a) => appendAnswer(a));
        rerenderList();
      } catch (err) {
        showToast(MSG.DELETE_UNDO_FAILED);
      }
    },
  });
}
```

`mountArchiveHandlers`에서 위 3 핸들러 모두 위임 등록.

- [ ] **Step 6: 테스트 실행 → 통과 확인**

```bash
npx vitest run tests/archive-delete-bulk.spec.ts
```

Expected: PASS (5 tests).

- [ ] **Step 7: 전체 vitest + lint + typecheck**

```bash
npm run lint && npx tsc --noEmit && npx vitest run
```

Expected: 0 error 0 warn, 371 → 376 pass (+5).

- [ ] **Step 8: Playwright 2 spec 추가**

`tests/e2e/archive-delete-bulk.spec.ts` 신규:

```ts
import { test, expect } from '@playwright/test';

test('선택 모드에서 다건 선택 + 벌크 삭제 + undo 복원', async ({ page }) => {
  page.on('dialog', (dialog) => dialog.accept());
  await page.goto('/');
  await page.evaluate(() => {
    localStorage.setItem(
      'dg.answers',
      JSON.stringify([
        { id: 'e1', text: 'one', date: '2026-04-27' },
        { id: 'e2', text: 'two', date: '2026-04-27' },
        { id: 'e3', text: 'three', date: '2026-04-27' },
      ])
    );
  });
  await page.reload();
  await page.click('[data-tab="archive"]');
  await page.click('#archiveSelectToggle');
  await page.click('.archive-card[data-answer-id="e1"]');
  await page.click('.archive-card[data-answer-id="e3"]');
  await expect(page.locator('#archiveBulkDelete')).toContainText('2');
  await page.click('#archiveBulkDelete');
  await expect(page.locator('.archive-card[data-answer-id="e1"]')).toHaveCount(0);
  await expect(page.locator('.archive-card[data-answer-id="e3"]')).toHaveCount(0);
  await expect(page.locator('.archive-card[data-answer-id="e2"]')).toBeVisible();
  await page.click('.toast--undo button');
  await expect(page.locator('.archive-card[data-answer-id="e1"]')).toBeVisible();
});

test('0건 선택 시 벌크 삭제 버튼 disabled', async ({ page }) => {
  await page.goto('/');
  await page.evaluate(() => {
    localStorage.setItem(
      'dg.answers',
      JSON.stringify([{ id: 'e1', text: 'one', date: '2026-04-27' }])
    );
  });
  await page.reload();
  await page.click('[data-tab="archive"]');
  await page.click('#archiveSelectToggle');
  await expect(page.locator('#archiveBulkDelete')).toBeDisabled();
});
```

- [ ] **Step 9: Playwright 실행**

```bash
npx playwright test tests/e2e/archive-delete-bulk.spec.ts
```

Expected: PASS (2 tests).

- [ ] **Step 10: commit**

```bash
git add src/ui/tabs/archive.ts src/ui/handlers/archive.ts \
  src/styles/archive.css \
  tests/archive-delete-bulk.spec.ts tests/e2e/archive-delete-bulk.spec.ts
git commit -m "feat(v3.8 T5): add archive select mode for bulk delete with confirm + undo"
```

---

## Task 6 — settings "데이터 삭제" 섹션 + 전체 초기화

**Files:**
- Modify: `src/ui/tabs/settings.ts` (마크업 + bindHandlers)
- Test: `tests/settings-delete-all.spec.ts` (신규)

- [ ] **Step 0: settings 마크업 끝 위치 확인**

```bash
sed -n '90,140p' src/ui/tabs/settings.ts
```

Slack Webhook 섹션 끝 위치 확인. 그 다음에 "데이터 삭제" 섹션 삽입.

- [ ] **Step 1: 전체 초기화 테스트 작성**

`tests/settings-delete-all.spec.ts` 생성:

```ts
/**
 * @vitest-environment jsdom
 */
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';
import { saveAnswers } from '../src/state/persistence';

beforeEach(() => {
  vi.useFakeTimers();
  localStorage.clear();
  document.body.replaceChildren();
});

afterEach(() => {
  vi.useRealTimers();
  vi.resetModules();
});

const mountSettings = async () => {
  const container = document.createElement('div');
  container.id = 'settingsTab';
  document.body.appendChild(container);
  const tab = await import('../src/ui/tabs/settings');
  tab.renderSettings(container);
  tab.bindHandlers();
};

describe('settings delete all answers', () => {
  it('shows confirm with answer count and deletes on accept', async () => {
    saveAnswers([
      { id: 'a', text: 'one' },
      { id: 'b', text: 'two' },
    ] as never);
    await mountSettings();
    const confirmSpy = vi.spyOn(window, 'confirm').mockReturnValue(true);

    document.querySelector<HTMLButtonElement>('#deleteAllAnswersBtn')!.click();

    expect(confirmSpy).toHaveBeenCalledOnce();
    expect(confirmSpy.mock.calls[0]?.[0]).toContain('2개');
    const { loadAnswers } = await import('../src/state/persistence');
    expect(loadAnswers()).toEqual([]);
    expect(document.querySelector('.toast--undo')).not.toBeNull();
  });

  it('cancel keeps answers untouched', async () => {
    saveAnswers([{ id: 'a', text: 'one' }] as never);
    await mountSettings();
    vi.spyOn(window, 'confirm').mockReturnValue(false);

    document.querySelector<HTMLButtonElement>('#deleteAllAnswersBtn')!.click();

    const { loadAnswers } = await import('../src/state/persistence');
    expect(loadAnswers()).toHaveLength(1);
    expect(document.querySelector('.toast--undo')).toBeNull();
  });

  it('disables button when no answers', async () => {
    await mountSettings();
    const btn = document.querySelector<HTMLButtonElement>('#deleteAllAnswersBtn');
    expect(btn!.disabled).toBe(true);
  });

  it('undo restores all', async () => {
    saveAnswers([
      { id: 'a', text: 'one' },
      { id: 'b', text: 'two' },
    ] as never);
    await mountSettings();
    vi.spyOn(window, 'confirm').mockReturnValue(true);

    document.querySelector<HTMLButtonElement>('#deleteAllAnswersBtn')!.click();
    document.querySelector<HTMLButtonElement>('.toast--undo button')!.click();

    const { loadAnswers } = await import('../src/state/persistence');
    expect(loadAnswers()).toHaveLength(2);
  });
});
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

```bash
npx vitest run tests/settings-delete-all.spec.ts
```

Expected: FAIL — `#deleteAllAnswersBtn` 부재.

- [ ] **Step 3: `src/ui/tabs/settings.ts`에 섹션 마크업 추가**

`renderSettings`의 string template 끝(또는 마지막 섹션 다음)에 추가:

```html
<section class="settings-section">
  <h3>데이터 삭제</h3>
  <p class="settings-help">archive 답변을 모두 삭제합니다. 삭제 후 5초 안에 되돌릴 수 있어요.</p>
  <button id="deleteAllAnswersBtn" type="button" class="btn btn-secondary" disabled>
    전체 답변 초기화
  </button>
</section>
```

- [ ] **Step 4: `bindHandlers`에 핸들러 추가**

```ts
import {
  loadAnswers,
  appendAnswer,
  deleteAllAnswers,
} from '../../state/persistence';
import { getSaveErrorMessage } from '../../state/user';
import { showToast, showUndoToast } from '../../utils/toast';
import { MSG } from '../messages';

// bindHandlers 내부:
const deleteAllBtn = document.getElementById('deleteAllAnswersBtn') as HTMLButtonElement | null;
if (deleteAllBtn) {
  // 진입 시 카운트 기반 disabled
  const refresh = () => {
    deleteAllBtn.disabled = loadAnswers().length === 0;
  };
  refresh();

  deleteAllBtn.addEventListener('click', () => {
    const n = loadAnswers().length;
    if (n === 0) return;
    if (!window.confirm(MSG.DELETE_CONFIRM_ALL(n))) return;
    const snapshot = loadAnswers();
    try {
      deleteAllAnswers();
    } catch (err) {
      showToast(getSaveErrorMessage(err));
      return;
    }
    refresh();
    showUndoToast({
      message: MSG.DELETE_UNDO_TOAST,
      actionLabel: MSG.DELETE_UNDO_ACTION,
      onUndo: () => {
        try {
          snapshot.forEach((a) => appendAnswer(a));
          refresh();
        } catch (err) {
          showToast(MSG.DELETE_UNDO_FAILED);
        }
      },
    });
  });
}
```

- [ ] **Step 5: 테스트 실행 → 통과 확인**

```bash
npx vitest run tests/settings-delete-all.spec.ts
```

Expected: PASS (4 tests).

- [ ] **Step 6: 전체 vitest + lint + typecheck**

```bash
npm run lint && npx tsc --noEmit && npx vitest run
```

Expected: 0 error 0 warn, 376 → 380 pass (+4).

- [ ] **Step 7: Playwright 1 spec 추가**

`tests/e2e/settings-delete-all.spec.ts`:

```ts
import { test, expect } from '@playwright/test';

test('settings 전체 답변 초기화 + undo 복원', async ({ page }) => {
  page.on('dialog', (dialog) => dialog.accept());
  await page.goto('/');
  await page.evaluate(() => {
    localStorage.setItem(
      'dg.answers',
      JSON.stringify([
        { id: 's1', text: 'one', date: '2026-04-27' },
        { id: 's2', text: 'two', date: '2026-04-27' },
      ])
    );
  });
  await page.reload();
  await page.click('[data-tab="settings"]');
  await expect(page.locator('#deleteAllAnswersBtn')).toBeEnabled();
  await page.click('#deleteAllAnswersBtn');
  await expect(page.locator('.toast--undo')).toBeVisible();
  await page.click('[data-tab="archive"]');
  await expect(page.locator('.archive-card')).toHaveCount(0);
  await page.click('.toast--undo button');
  await page.click('[data-tab="archive"]');
  await expect(page.locator('.archive-card')).toHaveCount(2);
});
```

- [ ] **Step 8: Playwright 실행**

```bash
npx playwright test tests/e2e/settings-delete-all.spec.ts
```

Expected: PASS (1 test).

- [ ] **Step 9: commit**

```bash
git add src/ui/tabs/settings.ts \
  tests/settings-delete-all.spec.ts tests/e2e/settings-delete-all.spec.ts
git commit -m "feat(v3.8 T6): add settings data delete section + full reset with undo"
```

---

## Task 7 — N=5 안정성 + 통합 회귀 점검

**Files:** 없음 (검증만)

- [ ] **Step 1: 신규 spec N=5 연속 pass 검증**

```bash
for i in 1 2 3 4 5; do
  echo "=== Run $i ==="
  npx vitest run \
    tests/messages-delete.spec.ts \
    tests/persistence-delete.spec.ts \
    tests/toast-undo.spec.ts \
    tests/archive-delete-target.spec.ts \
    tests/archive-delete-bulk.spec.ts \
    tests/settings-delete-all.spec.ts || break
done
```

Expected: 5/5 ALL PASS. 실패 spec 발견 시 `tests/README.md` 가이드라인(dynamic import / vi.useFakeTimers / afterEach 위치) 점검 → 수정 후 N=5 재실행.

- [ ] **Step 2: 전체 vitest 회귀 점검**

```bash
npx vitest run
```

Expected: 380 pass (v3.7 baseline 359 + 21 신규: T1 3 + T2 6 + T3 3 + T4 3 + T5 5 + T6 4 = 24, 일부 spec 안에서 setup 공유로 21~24).

- [ ] **Step 3: 전체 Playwright**

```bash
npx playwright test
```

Expected: 79 + 4 = 83 pass.

- [ ] **Step 4: 빌드 + 번들 사이즈 확인**

```bash
npm run build
ls -lh dist/assets/*.js dist/assets/*.css | head
```

Expected: JS gzip ~22.40 → ~23.0 kB 이내 (+0.5~1.0 kB 추정, ±2 kB DoD 충족).

- [ ] **Step 5: lint + tsc 최종**

```bash
npm run lint && npx tsc --noEmit
```

Expected: 0 error 0 warn.

- [ ] **Step 6: 검증 commit (변경 없음 — pass만 기록)**

빈 commit 생성 안 함. 이 step은 수동 검증 단계.

---

## Task 8 — 독립 심층 리뷰 + Manual smoke

**Files:** 없음 (외부 검증)

- [ ] **Step 1: `superpowers:code-reviewer` 심층 리뷰 호출**

CLAUDE.md "되돌리기 어려운 작업 (데이터 파괴)" 분류 → **심층 리뷰** 필수.

리뷰 dispatch 시 컨텍스트:
- spec: `docs/superpowers/specs/2026-04-27-v3.8-answer-deletion-design.md`
- plan: `docs/superpowers/plans/2026-04-27-v3.8-answer-deletion.md`
- diff: `git diff main...feat/v3.8-answer-deletion -- src/ tests/`
- 분류 기준: P0 (배포 차단) / P1 (in-cycle 수정) / P2 (백로그)

- [ ] **Step 2: P0 / P1 in-cycle 처리**

P0 발견 → 별 commit으로 즉시 수정 (`fix(v3.8 review): ...`). P1 → 같은 사이클 내 수정. P2 → 메모리 백로그 기록.

- [ ] **Step 3: Manual smoke (사용자 결정)**

CLAUDE.md "되돌리기 어려운 작업"이라 manual 권장. 사용자에게 옵션 제시:
- archive 카드 ✕ → undo 복원 1회
- 선택 모드 다건 → confirm → undo 1회
- settings 전체 초기화 → confirm → undo 1회
- storage 가득 시 (Quota) 에러 토스트 — 수동 재현 어려움, 단위테스트로 갈음 가능

사용자 OK → 다음 step. 생략 결정 시 vitest/Playwright/N=5/심층 리뷰로 갈음 명시.

- [ ] **Step 4: main merge + Firebase deploy**

```bash
git checkout main
git merge --no-ff feat/v3.8-answer-deletion -m "Merge feat/v3.8-answer-deletion (archive 답변 삭제 UI)"
npm run build
firebase deploy --only hosting
```

> **Note:** v3.7 패턴(직배포, origin remote 미등록) 그대로. push 단계 없음.

- [ ] **Step 5: 메모리 업데이트**

`/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/` 에 `project_daily_growth_v38_answer_deletion.md` 신규 + `MEMORY.md` 인덱스 1줄 추가.

기록 항목:
- merge SHA + Firebase URL + 배포 일자
- commits 수 / vitest / Playwright 카운트
- 독립 리뷰 결과 (P0/P1/P2 분류)
- 신규 backlog (휴지통/감사 로그/필터 UX 등)
- v3.9 차기 옵션 후보 (A/B/C 등)

---

## Self-Review (작성 후 점검)

작성된 plan을 spec과 대조한 결과:

**Spec 커버리지:**

| Spec section/요구 | Plan task |
|---|---|
| §2 Goal — 타깃/벌크/전체 3 단위 삭제 | T2 (API), T4 (타깃), T5 (벌크), T6 (전체) |
| §2 Goal — 5초 in-memory Undo | T3 (helper), T4/T5/T6 (사용처) |
| §2 Goal — 위험 등급별 confirm | T5 (벌크 confirm), T6 (전체 confirm). T4 타깃은 confirm 없음 (Q4 정책) |
| §2 Goal — XP/Streak/level/heatmap 보존 | 모든 task에서 user.ts 변경 0 (T2도 persistence만 건드림) |
| §2 Goal — storage 실패 시 v3.7 패턴 | T4/T5/T6 모두 `getSaveErrorMessage` 분기 |
| §2 Goal — 회귀 0 | T7 전체 vitest/Playwright + T8 심층 리뷰 |
| §3 Q5 N/A — 캐시 동반 정리 미수행 | T2 deleteCacheEntries 미포함 명시 |
| §4 모듈 변경 표 — 7 모듈 | T1(messages) / T2(persistence) / T3(toast) / T4+T5(archive tabs+handlers) / T6(settings) — 모두 매핑 |
| §5 T1 (deleteAnswerById/ByIds/All) | Plan T2 |
| §5 T2 (showUndoToast) | Plan T3 |
| §5 T3 (archive 카드 ✕) | Plan T4 |
| §5 T4 (선택 모드) | Plan T5 |
| §5 T5 (settings 전체 초기화) | Plan T6 |
| §5 T6 (MSG 카피) | Plan T1 (의존성 root로 우선 배치) |
| §8 테스트 전략 — vitest +15~20 / Playwright +6~8 / N=5 | T1~T6 + T7 |
| §10 DoD — 심층 리뷰 + 직배포 + 메모리 | T8 |

**Placeholder 스캔:** "TBD" / "TODO" / "implement later" 없음. 단 T3 Step 0/T4 Step 0/T5 Step 0/T6 Step 0의 "기존 구조 확인" 단계는 의도된 코드 탐색 (placeholder 아님).

**타입 일관성:**
- `deleteAnswerById(id: string)` / `deleteAnswersByIds(ids: string[])` / `deleteAllAnswers()` — T2 정의 ↔ T4/T5/T6 사용 일치
- `showUndoToast({ message, actionLabel, onUndo, durationMs? })` — T3 정의 ↔ T4/T5/T6 사용 일치
- `MSG.DELETE_*` — T1 정의 ↔ T4/T5/T6 사용 일치 (`DELETE_CONFIRM_BULK(n)` 함수형, 나머지 상수)

**Scope:** 8 task (실 구현 6 + 검증 1 + 리뷰/배포 1) — 단일 사이클 범위 적합. 13~17 commits 추정과 일치.
