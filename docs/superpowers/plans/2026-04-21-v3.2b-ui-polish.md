# v3.2b-ui Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v3.2b-ui Sections A~E 본구현 위에 10개 스타일 폴리시(P0/P1/P2 triage)를 한 번에 정돈한다.

**Architecture:** 거의 전부 `src/styles/main.css` 단일 파일 폴리시. 4개 신규 Playwright smoke + 1개 옵셔널 vitest drift 가드. JS/TS 마크업 변경 없음. CSS-only가 95%.

**Tech Stack:** TypeScript 5.4, Vite 5, Vitest 1.4 (jsdom), Playwright 1.42, ESLint + Prettier. 패키지 매니저: **npm**.

**Spec reference:** [docs/superpowers/specs/2026-04-21-v3.2b-ui-polish-design.md](../specs/2026-04-21-v3.2b-ui-polish-design.md)

---

## File Structure

### Edit (1 file)

| 파일 | 변경 요약 | Tasks |
|---|---|---|
| `src/styles/main.css` | P0~P2 모든 CSS 폴리시. `:root` 변수 1개 추가, 기존 selector ~12개 보강, 신규 selector ~7개 추가 | Task 2~12 |

### Create (1~2 files)

| 파일 | 역할 | Tasks |
|---|---|---|
| `tests/smoke/polish.spec.ts` | 4개 smoke: briefing hover · chip scale · focus-visible · disabled pointer-events | Task 5, 6, 9, 10 |
| `tests/unit/css-tokens.spec.ts` | (옵션) `--sidebar-width` drift 가드 — Task 12에서 P2 #3 적용 시 함께 추가 | Task 12 |

### Unchanged (explicit)

- `index.html`, `firebase.json` (CSP 무변경)
- `src/state/*`, `src/services/*`, `src/ui/handlers/*`, `src/ui/tabs/*`, `src/ui/onboarding.ts`, `src/ui/nav.ts`
- 기존 vitest 128 / Playwright 15 — 모두 그린 유지가 게이트

---

## Task 1: Plan Discovery — 실측 + grep + skip 결정

**Files:** none (verification only — 결정 결과를 다음 task들에 반영)

본 task는 spec §16 Plan Discovery Checklist 항목 중 결정 필요한 4가지를 한 번에 처리한다.

- [ ] **Step 1: dev server + 작은 viewport 실측 (#4)**

Run (background OK):

```bash
npm run dev
```

Chrome DevTools → Toggle device toolbar → custom viewport 차례로 280 / 320 / 360px 적용. 통계 탭(`#statsTab`) 진입. heatmap 7개 column이 균등 분할되고 셀이 잘리거나 겹치지 않는지 확인.

**결정 기록:**
- 안 깨짐 → Task 8 (#4)을 **skip**, follow-up backlog로 기록
- 280px 또는 320px에서 깨짐 → Task 8 (#4) **적용** (`@media (max-width: 360px) { .heatmap-cell { min-height: 22px; } }`)

- [ ] **Step 2: 768px 경계 transition 실측 (#2)**

같은 dev server에서 Chrome 창 가로 폭을 700 ↔ 800 천천히 drag. 현재(transition 없음) bottom-nav가 즉시 점프하는 것 vs 향후 width/padding transition 추가 시 부드러움 정도 체감 비교.

**결정 기록:**
- 가시 효과 의미 있음 → Task 11 (#2) **적용**
- 효과 거의 없음 → Task 11 (#2) **skip**, follow-up backlog로

- [ ] **Step 3: disabled 버튼에 title tooltip 패턴 grep (#10)**

Run:

```bash
git grep -nE "disabled[^>]*title=|title=[^>]*disabled" -- 'src/**/*.ts' 'src/**/*.html' 'index.html'
```

Expected: 매치 없거나 1~2건. 매치 결과를 검토:

**결정 기록:**
- 매치 없음 → Task 10 (#10) 영향 없음, 그대로 적용
- 매치 있음 → 해당 위치 검토 후 결정 (대부분 enabled-only로 reword 가능). 결정을 Task 10 노트에 기록

- [ ] **Step 4: `.briefing-card` focus 대상 재확인 (spec §16.5는 이미 (b) option 채택)**

Run:

```bash
git grep -n "createElement('article')" src/ui/handlers/home.ts
```

Expected: `card.className = 'briefing-card'` 직후 `<article>` 생성 라인 매치. 본 task에서는 변경 없이 확인만 — Task 9의 selector(`.briefing-card .briefing-title:focus-visible`)가 유효함을 확정.

- [x] **Step 5: 결정 사항 기록 (2026-04-21 실행 세션 기록)**

- **#4 (Task 8) — SKIP** — Hayden 기기 ≥ iPhone 12 (~390px), <360px 환경 없음. YAGNI. follow-up backlog 기록
- **#2 (Task 11) — SKIP** — `position: fixed` width transition 효과 미미, 추가 복잡도 < 가시 효과. follow-up backlog 기록
- **#10 (Task 10) — 안전 적용** — `disabled + title` grep 매치 없음. tooltip 영향 없음
- **#9 (Task 5) — focus 대상 확정** — `.briefing-card`는 `<article>`, focus는 내부 `.briefing-title` (`<a>`) — selector `.briefing-card .briefing-title:focus-visible` 유효

- [x] **Step 6: 시각 검증 흐름 변경 — Task 14 일괄 manual QA**

실행 에이전트(Claude)가 CSS 렌더링을 직접 볼 수 없어 매 task의 dev server 시각 확인을 Hayden에게 분산 요청하지 않고 **Task 14에 8 items로 몰아서** 진행. 각 task는 CSS 편집 + automated test 실행에 집중. dev server 백그라운드 기동도 생략 — 필요 시 Task 14 직전에 한 번만 띄움.

---

## Task 2: P0 #5 — Heatmap level-2 opacity 상향

**Files:**
- Modify: `src/styles/main.css` — `.heatmap-cell.level-2` rule (현재 `.heatmap-cell.level-1` 바로 다음 라인)

- [ ] **Step 1: 기준선 — 현재 통계 탭 시각 확인**

dev server에서 통계 탭 진입. heatmap이 보이는데 데이터가 적으면 임시로 `dg.answers`에 4단계 답변 데이터를 일시 주입(아래 evaluate 사용 권장 — 키는 [src/state/persistence.ts:5](src/state/persistence.ts#L5) `'dg.answers'`):

Chrome DevTools Console:

```js
const today = new Date();
const seed = [];
for (let i = 0; i < 28; i++) {
  const d = new Date(today); d.setDate(today.getDate() - i);
  const date = d.toISOString().slice(0, 10);
  const n = i % 4;  // 0/1/2/3 카운트로 4단계 시뮬
  for (let j = 0; j < n; j++) seed.push({ id: `seed_${i}_${j}`, date, content: 'x', createdAt: d.toISOString() });
}
localStorage.setItem('dg.answers', JSON.stringify(seed));
location.reload();
```

heatmap에서 level-1 / level-2 / level-3 셀이 시각적으로 얼마나 구분되는지 눈으로 확인 (level-2가 level-1과 거의 같으면 본 task가 의미 있음을 확정).

- [ ] **Step 2: opacity 변경**

`src/styles/main.css`에서 `.heatmap-cell.level-2` rule을 찾아 `opacity: 0.6;` → `opacity: 0.85;`로 변경:

```css
.heatmap-cell.level-2 { background: var(--primary-light); opacity: 0.85; }
```

- [ ] **Step 3: 시각 확인 (라이트 + 다크)**

dev server 새로고침. 통계 탭에서 level-1/2/3이 명확히 단계별 짙음으로 구분되는지 확인. 다크 모드 토글(설정 → 다크 모드 또는 `localStorage.dg_theme = 'dark'; location.reload()`) 후도 같은 확인.

**inversion 체크:** level-2 (0.85) > level-3 (0.8) 이지만 base color가 primary-light < primary 이므로 짙기 자연 정렬되어야 함. 만약 inversion 보이면 보고 후 stop (level-3을 0.95로 동시 보정 결정 필요).

- [ ] **Step 4: vitest + Playwright 회귀 가드 무영향 확인**

Run:

```bash
npm test -- tests/unit/heatmap.spec.ts
```

Expected: PASS (3 tests). class assertion만 검증하므로 opacity 변경에 무영향.

- [ ] **Step 5: Commit**

```bash
git add src/styles/main.css
git commit -m "style(heatmap): bump level-2 opacity 0.6 to 0.85 for clearer tier"
```

---

## Task 3: P0 #6 — 주말 셀 좌측 accent shadow

**Files:**
- Modify: `src/styles/main.css` — `.heatmap-cell.level-0.is-weekend` rule 인접

- [ ] **Step 1: 변경**

`src/styles/main.css`에서 `.heatmap-cell.level-0.is-weekend { background: var(--accent-light); }` 라인 바로 다음에 새 rule 추가:

```css
.heatmap-cell.level-0.is-weekend {
  background: var(--accent-light);
}
.heatmap-cell.is-weekend:not(.is-blank) {
  box-shadow: inset 2px 0 0 var(--accent);
}
```

(기존 level-0 rule 그대로 유지하고 새 rule을 그 아래에 추가. 두 rule이 함께 적용되어 level-0 주말은 background + inset shadow, level-1+ 주말은 활동 색 + inset shadow.)

- [ ] **Step 2: 시각 확인 (라이트 + 다크)**

dev server 새로고침. 통계 탭에서 모든 주말 column(토/일)에 좌측 amber 라인 보이는지 확인. blank 셀(미래 주말 일자)은 라인 없음 확인. 다크 모드도 확인 — `--accent: #FBBF24` (다크 토큰)는 충분히 밝음.

- [ ] **Step 3: 단위 테스트 가드**

Run:

```bash
npm test -- tests/unit/heatmap.spec.ts
```

Expected: PASS (3 tests). `marks exactly 8 weekend data cells`는 `.is-weekend:not(.is-blank)` selector로 카운트만 검증 — 색/shadow 무관.

- [ ] **Step 4: Commit**

```bash
git add src/styles/main.css
git commit -m "style(heatmap): inset accent shadow on all weekend data cells"
```

---

## Task 4: P0 #8 — 온보딩 chip selected 피드백 (CSS만, smoke는 Task 6에서)

**Files:**
- Modify: `src/styles/main.css` — `.onboarding-chip` 블록 (현재 `.onboarding-chip { ... }` + `:hover` + `.selected`)

- [ ] **Step 1: chip transition + hover + selected + dark mode + reduced-motion 일괄 변경**

`src/styles/main.css`에서 `.onboarding-chip` 블록을 찾아 다음으로 교체 (기존 `.onboarding-chip { ... }`, `.onboarding-chip:hover { ... }`, `.onboarding-chip.selected { ... }` 3개 rule 모두):

```css
.onboarding-chip {
  padding: 10px 12px;
  border-radius: var(--radius-md);
  border: 2px solid var(--border);
  background: var(--bg-card);
  color: var(--text-primary);
  font-size: 0.9rem;
  font-family: var(--font-body);
  cursor: pointer;
  transition: border-color 0.2s, background 0.2s, color 0.2s,
              transform 0.15s, box-shadow 0.2s;
  text-align: center;
}
.onboarding-chip:hover {
  border-color: var(--primary-light);
  transform: translateY(-1px);
  box-shadow: var(--shadow-sm);
}
.onboarding-chip.selected {
  background: var(--primary);
  border-color: var(--primary);
  color: #fff;
  transform: scale(1.03);
  box-shadow: 0 4px 12px rgba(79, 70, 229, 0.18);
}
.onboarding-chip.selected:hover {
  transform: scale(1.03);
}
[data-theme="dark"] .onboarding-chip.selected {
  box-shadow: 0 4px 12px rgba(129, 140, 248, 0.25);
}
@media (prefers-reduced-motion: reduce) {
  .onboarding-chip,
  .onboarding-chip.selected { transition: none; transform: none; }
}
```

- [ ] **Step 2: 온보딩 시각 확인**

dev server에서 `localStorage.removeItem('user'); location.reload()` 후 온보딩 step 1(관심사 선택) 진입. chip 6개 중 hover 시 살짝 떠오르는 효과 + 클릭(selected) 시 scale 1.03 + primary shadow 즉시 보이는지 확인. 다크 모드에서도 selected shadow가 보이는지 확인.

- [ ] **Step 3: vitest 무영향 확인**

Run:

```bash
npm test
```

Expected: PASS — onboarding 관련 unit/smoke (있다면) 그린.

- [ ] **Step 4: Commit**

```bash
git add src/styles/main.css
git commit -m "style(onboarding): selected chip scale + shadow + dark + reduced-motion"
```

---

## Task 5: polish.spec.ts 신규 + P0 #9 — 브리핑 카드 hover (TDD)

**Files:**
- Create: `tests/smoke/polish.spec.ts`
- Modify: `src/styles/main.css` — `.briefing-card` 블록 (line 362-370 부근)

- [ ] **Step 1: polish.spec.ts 신규 생성 + 첫 test 작성 (briefing card hover)**

Create `tests/smoke/polish.spec.ts`:

```ts
import { test, expect } from '@playwright/test';

test.use({ serviceWorkers: 'block' });

async function seedUser(page: import('@playwright/test').Page): Promise<void> {
  await page.addInitScript(() => {
    localStorage.setItem('user', JSON.stringify({
      name: 'Polish', interests: ['pm'],
      onboardedAt: '2026-04-01', streak: 0, lastActiveDate: '', xp: 0, level: 1,
    }));
  });
}

test('briefing card hover applies inset primary box-shadow (existence check)', async ({ page }) => {
  await seedUser(page);
  await page.route('**/api.rss2json.com/**', (route) =>
    route.fulfill({
      status: 200, contentType: 'application/json',
      body: JSON.stringify({
        feed: { title: 'src' },
        items: [{ title: 'T', link: 'https://example.com/1', description: 'd', pubDate: '' }],
      }),
    }),
  );
  await page.goto('/');
  await expect(page.locator('#homeTab')).toBeVisible();
  await page.locator('#refreshBriefing').click();
  const card = page.locator('.briefing-card').first();
  await expect(card).toBeVisible({ timeout: 10_000 });

  const beforeHover = await card.evaluate((el) => getComputedStyle(el).boxShadow);
  await card.hover();
  const afterHover = await card.evaluate((el) => getComputedStyle(el).boxShadow);

  expect(afterHover).not.toBe(beforeHover);
  expect(afterHover).not.toBe('none');
  expect(afterHover).toContain('inset');
});
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

Run:

```bash
npm run test:smoke -- tests/smoke/polish.spec.ts
```

Expected: FAIL — `afterHover`에 `inset` 키워드 없음 (현재 hover는 `box-shadow: var(--shadow-md)` 단일 outer shadow만).

- [ ] **Step 3: `.briefing-card` hover CSS 변경**

`src/styles/main.css`에서 `.briefing-card { ... }` 블록과 `.briefing-card:hover` 라인을 찾아 다음으로 변경:

기존:

```css
.briefing-card {
  min-width: 300px; max-width: 320px;
  background: var(--bg-card); border-radius: var(--radius-lg);
  padding: 16px; border: 1px solid var(--border);
  scroll-snap-align: start; flex-shrink: 0;
  transition: transform 0.2s, box-shadow 0.2s;
  cursor: pointer;
}
.briefing-card:hover { transform: translateY(-2px); box-shadow: var(--shadow-md); }
```

변경 후 (transition은 그대로, hover의 box-shadow에 inset 라인 추가):

```css
.briefing-card {
  min-width: 300px; max-width: 320px;
  background: var(--bg-card); border-radius: var(--radius-lg);
  padding: 16px; border: 1px solid var(--border);
  scroll-snap-align: start; flex-shrink: 0;
  transition: transform 0.2s, box-shadow 0.2s;
  cursor: pointer;
}
.briefing-card:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-md), inset 3px 0 0 var(--primary);
}
```

(border 그대로 유지. layout shift 없음.)

- [ ] **Step 4: 테스트 실행 — 통과 확인**

Run:

```bash
npm run test:smoke -- tests/smoke/polish.spec.ts
```

Expected: PASS (1 test).

- [ ] **Step 5: 시각 확인 (라이트 + 다크)**

dev server 홈 탭 진입. 브리핑 카드에 마우스 올리면 위로 떠오름 + 좌측 primary 강조선 + outer shadow 동시 보임. 읽음 카드(`.read`)도 hover 시 inset 라인 보임. 다크 모드도 확인.

- [ ] **Step 6: Commit**

```bash
git add src/styles/main.css tests/smoke/polish.spec.ts
git commit -m "feat(polish): briefing card hover gains inset primary shadow + smoke"
```

---

## Task 6: P0 #8 smoke 추가 — 온보딩 chip selected scale

**Files:**
- Modify: `tests/smoke/polish.spec.ts` (Task 5에서 생성됨)

- [ ] **Step 1: 새 test 추가 (chip scale)**

`tests/smoke/polish.spec.ts` 끝에 다음 test append:

```ts
test('onboarding chip selected has non-default transform (scale)', async ({ page }) => {
  await page.addInitScript(() => { localStorage.removeItem('user'); });
  await page.goto('/');
  const chip = page.locator('.onboarding-chip').first();
  await expect(chip).toBeVisible({ timeout: 5_000 });
  const before = await chip.evaluate((el) => getComputedStyle(el).transform);
  await chip.click();
  const after = await chip.evaluate((el) => getComputedStyle(el).transform);

  expect(after).not.toBe(before);
  expect(after).not.toBe('none');
});
```

- [ ] **Step 2: 테스트 실행**

Run:

```bash
npm run test:smoke -- tests/smoke/polish.spec.ts
```

Expected: PASS (2 tests). Task 4에서 이미 chip CSS가 `transform: scale(1.03)`으로 변경되었으므로 즉시 그린.

만약 FAIL이면: Task 4 `.onboarding-chip.selected`에 `transform: scale(1.03)`이 빠졌는지 또는 selector 매칭이 오답인지 확인.

- [ ] **Step 3: Commit**

```bash
git add tests/smoke/polish.spec.ts
git commit -m "test(polish): smoke for onboarding chip selected scale"
```

---

## Task 7: P1 #1 — 데스크탑 nav-item hover

**Files:**
- Modify: `src/styles/main.css` — `@media (min-width: 768px)` 블록 안 (현재 `.nav-item.active { ... }` 라인 인접)

- [ ] **Step 1: hover rule 추가**

`src/styles/main.css`에서 `@media (min-width: 768px) { ... }` 블록 안의 `.nav-item.active { ... }` rule **바로 다음**에 새 rule 삽입:

```css
@media (min-width: 768px) {
  /* …existing :root, .app-container, .bottom-nav, .nav-item, .nav-item.active 그대로… */
  .nav-item.active {
    border-left-color: var(--primary);
    background: var(--primary-ultra-light);
  }
  .nav-item:hover {
    background: var(--primary-ultra-light);
    color: var(--primary);
  }
  .nav-item .nav-icon { font-size: 1.15rem; }
  /* …rest unchanged… */
}
```

(active rule은 그대로 두고 그 아래에 hover rule 추가. 명시적 `.nav-item.active:hover` rule은 불필요 — active와 hover가 같은 bg+color로 자연 cascade.)

- [ ] **Step 2: 시각 확인 (라이트 + 다크)**

dev server에서 viewport ≥768px (예: 1280) 설정. sidebar의 nav-item 위에 마우스 올리면 ultra-light bg + primary 색으로 변하는지 확인. active item에 hover해도 동일 bg/color 유지(추가 변화 없음). 다크 모드에서도 적절히 구분되는지 확인.

- [ ] **Step 3: 모바일 영향 없음 확인**

viewport 375에서 nav 위에 마우스 올려도 변화 없음(모바일은 hover 의미 약함, 의도된 동작).

- [ ] **Step 4: Commit**

```bash
git add src/styles/main.css
git commit -m "style(nav): desktop nav-item hover state at 768+"
```

---

## Task 8: P1 #4 — Heatmap 작은 viewport (Discovery 결정에 따라 조건부)

**Files:**
- (조건부) Modify: `src/styles/main.css` — heatmap 섹션 끝

- [ ] **Step 1: Task 1 Step 1의 Discovery 결정 재확인**

Plan 파일 본문 Task 1 Step 5에서 기록된 결정 확인:

- "Discovery 결정: 안 깨짐, skip" → **Step 2~3 건너뛰고 Step 4로** (skip 처리)
- "Discovery 결정: 깨짐, 적용" → Step 2~3 진행

- [ ] **Step 2: (적용 시) CSS 추가**

`src/styles/main.css` heatmap 섹션 끝(예: `.heatmap-legend span { ... }` 다음)에 추가:

```css
@media (max-width: 360px) {
  .heatmap-cell { min-height: 22px; }
}
```

- [ ] **Step 3: (적용 시) 시각 + 단위 테스트 확인**

dev server에서 viewport 320px 통계 탭 — heatmap 7 column 균등 + 셀 잘림 없음. `npm test -- tests/unit/heatmap.spec.ts` PASS (cell count 검증만 하므로 무영향).

- [ ] **Step 4: Commit (skip 시 commit 없이 다음 task)**

skip 시: `git status` 후 변경 없음 확인, **commit 생략하고 Task 9로 진행**.

적용 시:

```bash
git add src/styles/main.css
git commit -m "style(heatmap): smaller cell min-height under 360px viewport"
```

---

## Task 9: P1 #7 — focus-visible 일관 적용 (TDD)

**Files:**
- Modify: `tests/smoke/polish.spec.ts`
- Modify: `src/styles/main.css` — 적절한 위치 (예: `.onboarding-chip` 다음, P2 free-floating selector 모음용 섹션)

- [ ] **Step 1: 실패 테스트 추가**

`tests/smoke/polish.spec.ts` 끝에 append:

```ts
test('focus-visible on first focusable shows visible outline (keyboard tab)', async ({ page }) => {
  await seedUser(page);
  await page.goto('/');
  await page.keyboard.press('Tab');
  const focused = await page.evaluate(() => {
    const el = document.activeElement as HTMLElement | null;
    if (!el || el === document.body) return null;
    const cs = getComputedStyle(el);
    return { tag: el.tagName, outlineStyle: cs.outlineStyle, outlineWidth: cs.outlineWidth };
  });
  expect(focused).not.toBeNull();
  expect(focused!.outlineStyle).not.toBe('none');
  expect(focused!.outlineWidth).not.toBe('0px');
});
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

Run:

```bash
npm run test:smoke -- tests/smoke/polish.spec.ts
```

Expected: FAIL — focus-visible rule 없어 outline은 브라우저 기본 (UA에 따라 보일 수도/없을 수도; Chromium은 일부 element는 'none'). 만약 그린이면 다음 step에서 selector 추가가 의미 없는 게 아니라 일관성 보강 → 진행.

- [ ] **Step 3: focus-visible CSS 추가**

`src/styles/main.css`에 새 selector list 추가 (위치는 `.onboarding-chip` block 끝 다음 또는 파일 끝의 utility 영역):

```css
/* v3.2b-polish: keyboard focus visible */
.btn:focus-visible,
.nav-item:focus-visible,
.briefing-card .briefing-title:focus-visible,
.heatmap-cell:focus-visible,
.onboarding-chip:focus-visible {
  outline: 2px solid var(--primary);
  outline-offset: 2px;
}
```

(`.interest-tag`는 dead selector라 제외 — spec §11.2)

- [ ] **Step 4: 테스트 실행 — 통과 확인**

Run:

```bash
npm run test:smoke -- tests/smoke/polish.spec.ts
```

Expected: PASS (3 tests).

- [ ] **Step 5: 키보드 시각 확인**

dev server에서 마우스 클릭 없이 Tab 키 연타 — header → nav-item → briefing-title(`<a>`) → onboarding-chip(온보딩 화면일 때) 순회 시 모든 element에 primary outline + 2px offset 보임. 마우스 클릭 시는 outline 안 보임 (focus-visible 표준).

- [ ] **Step 6: heatmap-cell outline 충돌 확인**

dev server 통계 탭에서 Tab으로 heatmap-cell 진입 시 인접 cell과 outline 겹침이 거슬리는지 시각 확인. 거슬리면 `.heatmap-cell:focus-visible { outline-offset: 1px; }` 별도 override 추가 (Step 3 selector list에서 분리).

- [ ] **Step 7: Commit**

```bash
git add src/styles/main.css tests/smoke/polish.spec.ts
git commit -m "feat(polish): consistent :focus-visible outline + smoke"
```

---

## Task 10: P1 #10 — btn disabled pointer-events (TDD)

**Files:**
- Modify: `tests/smoke/polish.spec.ts`
- Modify: `src/styles/main.css` — `.btn:disabled` rule (현재 line 284 부근)

- [ ] **Step 1: Task 1 Step 3의 grep 결과 재확인**

Plan 파일 본문 Task 1 Step 5에서 기록된 결정 확인:

- "매치 없음" 또는 "확인됨, 적용 가능" → 그대로 진행
- "매치 있음, 영향 있음" → 해당 위치 수정 방안 노트 확인 후 진행

- [ ] **Step 2: 실패 테스트 추가**

`tests/smoke/polish.spec.ts` 끝에 append:

```ts
test('disabled button has pointer-events: none', async ({ page }) => {
  await seedUser(page);
  await page.goto('/');
  const pe = await page.evaluate(() => {
    const btn = document.querySelector('.btn');
    if (!btn) return null;
    btn.setAttribute('disabled', '');
    return getComputedStyle(btn).pointerEvents;
  });
  expect(pe).toBe('none');
});
```

- [ ] **Step 3: 테스트 실행 — 실패 확인**

Run:

```bash
npm run test:smoke -- tests/smoke/polish.spec.ts
```

Expected: FAIL — 현재 `.btn:disabled { opacity: 0.5; cursor: not-allowed; }`에 pointer-events 없음 → 'auto' 반환.

- [ ] **Step 4: CSS 변경**

`src/styles/main.css`에서 `.btn:disabled { opacity: 0.5; cursor: not-allowed; }` 라인을 다음으로 변경:

```css
.btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
  pointer-events: none;
}
```

- [ ] **Step 5: 테스트 실행 — 통과 확인**

Run:

```bash
npm run test:smoke -- tests/smoke/polish.spec.ts
```

Expected: PASS (4 tests).

- [ ] **Step 6: 기존 settings/onboarding 동작 확인**

dev server에서 설정 탭 → Slack 섹션 → 빈 webhook 상태에서 저장 버튼이 disabled인지(모양상) + 클릭이 안 통하는지 확인. onboarding step 2 (Gemini key)에서도 동일.

- [ ] **Step 7: Commit**

```bash
git add src/styles/main.css tests/smoke/polish.spec.ts
git commit -m "feat(polish): btn:disabled pointer-events: none + smoke"
```

---

## Task 11: P2 #2 — Layout transition (Discovery 결정에 따라 조건부)

**Files:**
- (조건부) Modify: `src/styles/main.css` — `.bottom-nav` + `.nav-item` rules

- [ ] **Step 1: Task 1 Step 2의 Discovery 결정 재확인**

- "효과 거의 없음, skip" → 본 task 전체 **skip**, Step 5로
- "가시 효과 있음, 적용" → Step 2~4 진행

- [ ] **Step 2: (적용 시) `.bottom-nav` + `.nav-item`에 transition 추가**

`src/styles/main.css`에서 `.bottom-nav { ... }` (line 696 부근)에 마지막 `z-index: 100;` 다음에 추가:

```css
.bottom-nav {
  position: fixed; bottom: 0; left: 50%;
  transform: translateX(-50%);
  width: 100%; max-width: 480px;
  background: var(--bg-card);
  border-top: 1px solid var(--border);
  display: flex; padding: 6px 0;
  padding-bottom: calc(6px + var(--safe-bottom));
  z-index: 100;
  transition: width 0.2s ease, max-width 0.2s ease, padding 0.2s ease;
}
```

`.nav-item { ... }` (line 706 부근)에 마지막 `font-family` 다음에:

```css
.nav-item {
  flex: 1; display: flex; flex-direction: column;
  align-items: center; gap: 2px; padding: 6px 0;
  font-size: 0.65rem; color: var(--text-tertiary);
  cursor: pointer; transition: color 0.2s, background 0.2s, padding 0.2s, font-size 0.2s;
  background: none; border: none; font-family: var(--font-body);
}
```

(기존 `transition: color 0.2s;`를 위 4개 속성으로 확장.)

파일 끝 또는 적절한 위치에 reduced-motion override:

```css
@media (prefers-reduced-motion: reduce) {
  .bottom-nav, .nav-item { transition: none; }
}
```

- [ ] **Step 3: (적용 시) 시각 확인**

dev server에서 창 가로 폭 700 ↔ 800 천천히 drag → bottom-nav width/padding 부드럽게 전환되는지 확인.

- [ ] **Step 4: (적용 시) Commit**

```bash
git add src/styles/main.css
git commit -m "style(nav): transition between mobile bottom and desktop sidebar"
```

- [ ] **Step 5: (skip 시) 다음 task로 진행 (commit 없음)**

---

## Task 12: P2 #3 — sidebar-width CSS 변수화 + (옵션) drift 가드

**Files:**
- Modify: `src/styles/main.css` — `:root` + `@media 768+` `.app-container` + `.bottom-nav`
- Create: `tests/unit/css-tokens.spec.ts` (옵션)

- [ ] **Step 1: drift 가드 테스트 작성 (옵션이지만 권장)**

Create `tests/unit/css-tokens.spec.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';

const here = dirname(fileURLToPath(import.meta.url));
const cssPath = join(here, '..', '..', 'src', 'styles', 'main.css');
const css = readFileSync(cssPath, 'utf-8');

describe('css drift guards (v3.2b-polish)', () => {
  it('--sidebar-width defined in :root with 240px', () => {
    expect(css).toMatch(/:root\s*\{[\s\S]*?--sidebar-width:\s*240px/);
  });
  it('--sidebar-width used at least twice (max-width + padding-left + width)', () => {
    const matches = css.match(/var\(--sidebar-width\)/g) ?? [];
    expect(matches.length).toBeGreaterThanOrEqual(2);
  });
});
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

Run:

```bash
npm test -- tests/unit/css-tokens.spec.ts
```

Expected: FAIL — `--sidebar-width` 미정의.

- [ ] **Step 3: `:root`에 변수 추가**

`src/styles/main.css`의 `:root { ... }` 블록 마지막 `--content-bottom-pad:` 다음 라인에 추가:

```css
:root {
  /* …existing tokens… */
  --content-bottom-pad: calc(var(--nav-height) + var(--safe-bottom) + 24px);
  --sidebar-width: 240px;
}
```

- [ ] **Step 4: `@media (min-width: 768px)` 안 3곳 240px → var 치환**

같은 파일 `@media (min-width: 768px) { ... }` 블록에서:

```css
@media (min-width: 768px) {
  :root { --content-bottom-pad: 24px; }
  .app-container {
    max-width: 1200px;
    padding-left: var(--sidebar-width);  /* was 240px */
  }
  .bottom-nav {
    top: 0; bottom: 0; left: 0;
    transform: none;
    width: var(--sidebar-width);          /* was 240px */
    max-width: var(--sidebar-width);      /* was 240px */
    flex-direction: column;
    border-top: none;
    border-right: 1px solid var(--border);
    padding: 24px 0;
    padding-bottom: 24px;
    /* (Task 11에서 transition 추가했다면 그 라인 유지) */
  }
  /* …rest unchanged… */
}
```

- [ ] **Step 5: 테스트 실행 — 통과 확인**

Run:

```bash
npm test -- tests/unit/css-tokens.spec.ts
```

Expected: PASS (2 tests). `--sidebar-width: 240px` 정의 + `var(--sidebar-width)` 3회 사용 ≥2.

- [ ] **Step 6: 시각 회귀 확인 (변화 없어야 정답)**

dev server viewport 1280에서 sidebar 폭 240px 동일 + content max 1200px 동일 — 동작 변화 없음 확인.

- [ ] **Step 7: Commit**

```bash
git add src/styles/main.css tests/unit/css-tokens.spec.ts
git commit -m "refactor(css): sidebar-width as CSS var + drift guard"
```

---

## Task 13: 최종 게이트 — lint + tsc + vitest + Playwright + build

**Files:** none (verification only)

- [ ] **Step 1: ESLint**

Run:

```bash
npm run lint
```

Expected: PASS — no errors. CSS 변경뿐이라 eslint 영향 없어야.

- [ ] **Step 2: tsc + vite build**

Run:

```bash
npm run build
```

Expected: PASS — tsc clean, vite build dist/ 생성. gzip 사이즈 < 17KB (베이스라인 15.41 KB).

- [ ] **Step 3: 전체 vitest**

Run:

```bash
npm test
```

Expected: PASS — 기존 128 + 신규 (css-tokens 적용 시 +2) 모두 그린.

- [ ] **Step 4: 전체 Playwright smoke**

Run:

```bash
npm run test:smoke
```

Expected: PASS — 기존 15 + 신규 polish.spec.ts 4 테스트 모두 그린.

- [ ] **Step 5: 빌드 산출물 사이즈 확인**

Run:

```bash
ls -lh dist/assets/*.css
```

Expected: 메인 CSS 파일 사이즈가 베이스라인 대비 미미한 증가 (CSS 추가 ~1~2KB). 5KB 이상 증가 시 spec 외 변경 의심하고 보고.

- [ ] **Step 6: commit history 점검**

Run:

```bash
git log --oneline -n 15
```

Expected: 본 plan의 9~12개 commit + 이전 v3.2b-ui commit history 깔끔. fixup/WIP 노이즈 없음.

---

## Task 14: 사용자 수동 QA + 독립 코드 리뷰 안내

**Files:** none (사용자 액션 + 안내)

- [ ] **Step 1: dev server 라이트 모드 manual QA (5 items)**

dev server (Task 1 Step 6부터 살아있음)에서 spec §19.2 라이트 모드 5개 항목 순차 확인:

1. 데스크탑 1280px → nav hover 시 ultra-light bg + primary 색, sidebar 매끄러움
2. 온보딩 신규 진입 (시크릿 창 또는 `localStorage.removeItem('user'); reload`) → chip 클릭 시 scale + shadow 즉시 피드백
3. 통계 탭 → level-1/2/3 명확히 단계별 짙음, 주말 셀 좌측 amber 라인 (모든 level)
4. 브리핑 카드 hover → translateY + shadow + 좌측 primary 강조선 동시
5. 키보드 Tab 순회 → 첫 Tab부터 outline 보임 (마우스 클릭 시는 안 보임)

각 항목에 ✅/❌ 표시. 하나라도 ❌이면 stop, 보고.

- [ ] **Step 2: 다크 모드 manual QA (3 items)**

설정 탭에서 다크 모드 토글 후:

6. 온보딩 chip selected shadow → 다크 배경에서 보임 (rgba 별도 보정 작동)
7. heatmap level-2/3 → 다크 primary 색 base에서도 단계 구분
8. disabled 버튼 클릭 → 어떤 동작도 안 일어남 (Slack 저장 후 disabled 상태 등)

각 항목에 ✅/❌ 표시.

- [ ] **Step 3: dev server 종료**

```bash
# Task 1에서 background로 띄운 npm run dev 종료
```

- [ ] **Step 4: 독립 코드 리뷰 결정을 사용자에게 안내**

CLAUDE.md "배포 전 독립 코드 리뷰 (필수)" 적용. 본 plan의 commit 수(예상 9~12)는 **표준 리뷰** 범위:

> 표준 리뷰 (`superpowers:code-reviewer` 1회) 권장합니다. 진행할까요?

사용자가 "yes" 응답 시 → `superpowers:code-reviewer` agent dispatch (관점: spec 준수 + 명백한 버그 + CSS 회귀). P0/P1/P2 분류 결과 받아서 사용자에게 다시 보고.

P0 발견 시 → 수정 후 재검토. P0 없으면 → 사용자에게 배포 결정 위임.

- [ ] **Step 5: 배포는 사용자 명시적 요청 시만**

CLAUDE.md "git push는 사용자가 '배포해줘'라고 할 때만". push/firebase deploy는 본 plan **외부**에서 사용자 결정으로 실행.

---

## Self-Review (post-write)

**1. Spec coverage:**

- §5 P0 #5 → Task 2
- §6 P0 #6 → Task 3
- §7 P0 #8 → Task 4 (CSS) + Task 6 (smoke)
- §8 P0 #9 → Task 5 (polish.spec.ts 신규 + smoke + CSS)
- §9 P1 #1 → Task 7
- §10 P1 #4 → Task 8 (Discovery gated)
- §11 P1 #7 → Task 9 (smoke + CSS)
- §12 P1 #10 → Task 10 (smoke + CSS, Discovery gated for tooltip risk)
- §13 P2 #2 → Task 11 (Discovery gated)
- §14 P2 #3 → Task 12 (CSS + 옵션 drift test)
- §15 Test Strategy → Tasks 5/6/9/10/12에 분산 (4 smoke + 옵션 unit)
- §16 Discovery → Task 1 (4 항목 모두)
- §17 Affected Files → File Structure 섹션과 일치
- §18 Risk → 각 task의 시각 확인 step + Plan Discovery에서 처리
- §19 Manual QA → Task 14
- §20 Follow-ups → 본 plan 외, scope 명시

**2. Placeholder scan:** "TBD"/"implement later"/"similar to Task N" 검색 — 없음. 모든 task에 완전한 코드/명령어 포함.

**3. Type/selector 일관성:**

- `.briefing-card .briefing-title:focus-visible` (Task 9) vs spec §11.2 — 일치 ✓
- `.heatmap-cell.is-weekend:not(.is-blank)` (Task 3) vs 기존 unit test selector — 일치 ✓
- `.onboarding-chip.selected` transform `scale(1.03)` (Task 4) vs Task 6 smoke `not.toBe('none')` — 일치 ✓
- `box-shadow: var(--shadow-md), inset 3px 0 0 var(--primary)` (Task 5) vs Task 5 smoke `toContain('inset')` — 일치 ✓
- `--sidebar-width: 240px` (Task 12) vs Task 12 drift test regex — 일치 ✓
- `pointer-events: none` (Task 10) vs Task 10 smoke `toBe('none')` — 일치 ✓
- 4개 smoke 모두 `tests/smoke/polish.spec.ts` 단일 파일에 누적 추가 (Task 5에서 생성, Task 6/9/10에서 append) — 순서 의존성 명확 ✓

**4. Task 의존성:**

- Task 1 (Discovery) → Task 8/10/11 결정 입력
- Task 5 (polish.spec.ts 생성) → Task 6/9/10 (append) 의존
- Task 4 (chip CSS) → Task 6 (smoke)에서 통과 검증
- Task 2~12는 모두 main.css 단일 파일 — 동시 작업 conflict 가능성. 순차 실행이 안전 (이 plan의 task 순서대로)
- Task 13 final gate는 모든 앞 task 완료 후
- Task 14 manual QA는 Task 13 통과 후

**5. 커밋 granularity:**

- Task 2~5, 7, 9~12 각각 1 commit. Task 6은 chip smoke만 추가 commit. Task 8/11은 skip 시 commit 없음.
- 예상 commit 수: 8~10 (Discovery skip 정도에 따라). 9 commit이 modal 케이스.
- 모든 commit 메시지 conventional commit 형식 (`style:` `feat:` `refactor:` `test:`).
