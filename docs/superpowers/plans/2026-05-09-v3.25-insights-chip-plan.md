# DG v3.25 Insights Chip Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 인사이트 카드에 분야 chip을 추가한다. Gemini 분류는 기존 `PROMPTS.insight` 호출에 응답 형식만 확장하여 추가 cap 0. Insights 탭 chip 필터 row(임계값 ≥6) + 카드 안 chip + Detail 모달 수동 분류 dropdown으로 사용자가 누적 인사이트를 분야별로 탐색·정정한다.

**Architecture:** Schema v6 → v7 lazy migration (insights[].interestId 추가, default 'unknown'). `parseInsightResponse` helper로 Gemini 응답 `<text>|<interestId>` 형식을 견고하게 파싱 (markdown trim + 첫 비빈 줄 + lastIndexOf('|') + strict whitelist 폴백). UI는 archive `.filter-chip` / settings `.interest-chip` 두 기존 패턴 재활용.

**Tech Stack:** Vite + TypeScript (strict) + Vitest (jsdom) + Playwright. Firebase Hosting (origin remote 미등록 — main 직배포 패턴).

**Spec:** [docs/superpowers/specs/2026-05-09-v3.25-insights-chip-design.md](../specs/2026-05-09-v3.25-insights-chip-design.md)

---

## File Structure

| 파일 | 역할 | task |
|---|---|---|
| `src/state/user.ts` | `Insight.interestId` 필드 + `validateInterestId` helper + `isValidUserShape` shape 검증 강화 | T1 |
| `src/ui/events.ts` | `'dg:insights:updated': { id: string }` 이벤트 추가 + `EVENT_NAMES` 배열 sync | T1 |
| `src/ui/handlers/home.ts:1127` | `Insight` literal에 `interestId: 'unknown'` 임시 채움 (TS strict 0 의무) — T4에서 `parseInsightResponse` 결과로 교체 | T1 |
| `src/state/migration.ts` | `migrateUserToV7` 함수 추가 + chain 확장 + V3/V4/V5/V6 early-return에 v7 guard | T2 |
| `src/state/user.ts` `getCachedUser` | `migrateUserToV7(user)` chain 호출 + v7 lazy persist 분기 | T2 |
| `src/services/prompts.ts` | `PROMPTS.insight.build()` 응답 형식 `<text>\|<interestId>` 강제 + `maxOutputTokens` 80→110 | T3 |
| `src/utils/gemini-parse.ts` | `parseInsightResponse(raw)` 함수 추가 (첫 비빈 줄 / markdown trim / lastIndexOf('\|') / whitelist 폴백) | T3 |
| `src/ui/handlers/home.ts:1102-1147` | `parseInsightResponse` 적용 + `Insight.interestId` 채움 + 형식 깨짐 'unknown' 폴백 | T4 |
| `src/ui/tabs/insights.ts` | `renderInsights` 확장 — 카드 안 chip + 필터 row(≥6) + 활성 chip filter + 'updated' 이벤트 wiring | T5 |
| `src/ui/modals/insight-detail.ts` | `<select>` 분야 dropdown + change saveUser + rollback + dispatch | T6 |
| `src/styles/pages/insights.css` (또는 `archive.css` / `main.css`에 추가) | `.insight-chip` + `.insight-chip--muted` + `.insight-filter-row` + `.insight-detail-meta` + `.insight-interest-select` | T7 |
| `tests/unit/validate-interest-id.spec.ts` | whitelist match / mismatch / 'unknown' 입력 / shape 강화 spec | T1 |
| `tests/unit/migration-v7.spec.ts` | v6→v7 / v7 idempotent / corrupted entry 폴백 (id 누락 / text 누락 / interestId invalid) | T2 |
| `tests/unit/parse-insight-response.spec.ts` | 정상 / `\|` 없음 / id invalid / text에 `\|` 포함 / markdown wrap / 여러 줄 응답 | T3 |
| `tests/unit/ui/handlers/home-chat-wiring.spec.ts` (확장) 또는 `tests/unit/home-insight-save.spec.ts` (신규) | 정상 응답 → interestId 저장 / 형식 깨짐 → 'unknown' | T4 |
| `tests/unit/ui/tabs/insights.spec.ts` (확장) 또는 `tests/unit/insights-filter.spec.ts` (신규) | 필터 클릭 시 분야만 표시 / `dg:insights:added` 시 '전체' reset | T5 |
| `tests/unit/ui/modals/insight-detail.spec.ts` (확장) 또는 `tests/unit/insight-detail-dropdown.spec.ts` (신규) | dropdown change 시 saveUser + dispatch / save 실패 시 rollback (in-memory + select.value 양방향) | T6 |

**기존 spec ripple 의무 (v3.21 lesson L4)**:
- `tests/unit/state/...` 또는 어디 있든 `Insight` literal 생성하는 spec은 `interestId` 필드 추가 의무 (grep `Insight = {` / `insights:.*\[` 으로 사전 검출 필요)
- `tests/utils/userMock.ts` 등 fixture가 있다면 `schemaVersion: 7` 마이그레이션 의무

---

## Test Count Baseline (T0 산출물)

T0 baseline 실측 (2026-05-09): `TZ=Asia/Seoul npm test` → 179 file / 1098 pass. `TZ=America/New_York npm test` → 동일.

**Codex P0-A1 / P1-B1 / P1-F1 fix 반영 (plan v2)**:
- T1 +3 (validateInterestId만; shape guard 강화는 T2로 이동)
- T2 +8 (migration 4 + shape guard 강화 4 — Codex P0-A1 fix로 task 흡수)
- T3 +16 (기존 8 + JSON/quote/prefix/alias 8 — Codex P1-B1 강화)
- T4 +2, T5 +4, T6 +3 (변동 없음)

| TZ | Baseline | After T1 | After T2 | After T3 | After T4 | After T5 | After T6 | After T8 |
|---|---|---|---|---|---|---|---|---|
| Asia/Seoul | **1098** ✅ | 1101 (+3) | 1109 (+8) | 1125 (+16) | 1127 (+2) | 1131 (+4) | 1134 (+3) | **≥1134** |
| America/New_York | **1098** ✅ | 1101 (+3) | 1109 (+8) | 1125 (+16) | 1127 (+2) | 1131 (+4) | 1134 (+3) | **≥1134** |

**총 신규 spec ≥36 (lower bound, plan v2 기준)**. 각 task의 step expected가 plan v1 기준이면 plan v2 표를 우선.

## Bundle Baseline (T0 산출물)

T0 baseline 실측 (2026-05-09): `npm run build && gzip -c dist/assets/index-*.js | wc -c` → **39913 B** ✅ (v3.24 retro 기록과 일치).

| Step | Bundle | Δ | 누적 |
|---|---|---|---|
| Baseline | **39913** ✅ | — | — |
| T1 | ~+30 (interface field + helper + 임시 fix) | +30 | 39943 |
| T2 | ~+150 (migration func + chain) | +150 | 40093 |
| T3 | ~+200 (prompt 확장 + parser) | +200 | 40293 |
| T4 | ~+60 (parseInsightResponse 적용) | +60 | 40353 |
| T5 | ~+250 (chip 필터 row + 카드 chip + filter handler) | +250 | 40603 |
| T6 | ~+100 (dropdown HTML + handler + rollback) | +100 | 40703 |
| T7 | ~+10~+50 (CSS, 재활용 위주) | +30 | 40733 |
| **Target** | **40313 ≤ B ≤ 40713** | +400~+800 | — |

**T7 완료 후 측정 결과가 40713 초과 시 trim 검토** (HTML template indent 압축 / 미사용 CSS 제거). v3.24 lesson #6 (bundle trim 격하 패턴) 답습.

---

### Task 0: spec + plan v1 baseline + Codex 사전 review

**Files:**
- Modify: `docs/superpowers/plans/2026-05-09-v3.25-insights-chip-plan.md` (이 문서, baseline 표 채움)
- Read: `docs/superpowers/specs/2026-05-09-v3.25-insights-chip-design.md`

- [ ] **Step 1: vitest baseline 측정 (양쪽 TZ)**

Run:
```bash
TZ=Asia/Seoul npm test 2>&1 | grep -E "Tests|Test Files"
TZ=America/New_York npm test 2>&1 | grep -E "Tests|Test Files"
```
Expected: 양쪽 모두 동일 카운트 (~1098 pass). plan §"Test Count Baseline" Baseline 행 채움.

- [ ] **Step 2: bundle baseline 측정**

Run:
```bash
npm run build && gzip -c dist/assets/index-*.js | wc -c
```
Expected: 39913 ± 100 B. plan §"Bundle Baseline" Baseline 행 채움.

- [ ] **Step 3: Codex 사전 review dispatch (mandatory, T1 진입 전 완료 의무)**

Use `codex:rescue` skill (또는 `pr-review-toolkit:code-reviewer` agent fallback).

Scope:
- spec 전반 (특히 §3 schema migration, §4 parser, §5 UI threshold)
- plan task 매핑 + DoD threshold
- 기대 catch: parser edge case (Gemini 형식 무시 / id 사이 공백 / `unknown` 누락) / migration corrupted entry filter / chip 필터 ≥6 임계값 UX / dropdown rollback 양방향 동기화

분류: P0/P1/P2 + 권고. P0/P1는 plan v2 in-cycle 흡수 의무.

- [ ] **Step 4: 사전 review 결과 plan v2 반영 + commit**

P0/P1 catch가 있으면 plan v2로 in-cycle 흡수.

```bash
git add docs/superpowers/specs/2026-05-09-v3.25-insights-chip-design.md \
        docs/superpowers/plans/2026-05-09-v3.25-insights-chip-plan.md
git commit -m "docs(v3.25 T0): spec + plan v1 + baseline + 사전 Codex review patch"
```

---

### Task 1: 데이터 모델 — Insight.interestId + validateInterestId + isValidUserShape 강화

**Files:**
- Modify: `src/state/user.ts:22-26` (Insight interface), `:124-150` (isValidUserShape)
- Modify: `src/ui/events.ts:14-53` (EventMap), `:70-87` (EVENT_NAMES)
- Modify: `src/ui/handlers/home.ts:1127` (Insight literal 임시 fix)
- Create: `tests/unit/validate-interest-id.spec.ts`

- [ ] **Step 1: Write failing test for validateInterestId**

Create `tests/unit/validate-interest-id.spec.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { validateInterestId } from '../../src/state/user';

describe('validateInterestId (v3.25 T1)', () => {
  it('whitelist 멤버는 그대로 반환', () => {
    expect(validateInterestId('recruiting')).toBe('recruiting');
    expect(validateInterestId('ai_ml')).toBe('ai_ml');
    expect(validateInterestId('self_dev')).toBe('self_dev');
  });

  it('whitelist 외 string은 unknown 폴백', () => {
    expect(validateInterestId('hallucinated')).toBe('unknown');
    expect(validateInterestId('')).toBe('unknown');
    expect(validateInterestId('  recruiting  ')).toBe('unknown');  // trim은 caller 책임
  });

  it("'unknown' 입력은 그대로 unknown (whitelist 외이지만 sentinel)", () => {
    expect(validateInterestId('unknown')).toBe('unknown');
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/unit/validate-interest-id.spec.ts`
Expected: FAIL — `validateInterestId is not a function` (export 없음).

- [ ] **Step 3: Add validateInterestId + Insight.interestId field**

Modify `src/state/user.ts`:

```ts
// 기존 export interface Insight { ... } 확장
export interface Insight {
  id: string;
  text: string;
  createdAt: string;
  interestId: string;  // v3.25 NEW — INTERESTS[].id 또는 'unknown'
}

// validateInsightText 옆에 추가
import { INTERESTS } from '../utils/categories';

/**
 * v3.25 T1: Insight.interestId entry-level guard.
 * - INTERESTS 15개 whitelist 매칭 → 그대로 반환
 * - 매칭 실패 또는 'unknown' sentinel → 'unknown'
 * - trim은 caller 책임 (parseInsightResponse가 처리)
 */
export function validateInterestId(id: string): string {
  return INTERESTS.some(i => i.id === id) ? id : 'unknown';
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run tests/unit/validate-interest-id.spec.ts`
Expected: PASS (3 specs).

- [ ] **Step 5: isValidUserShape 강화 — MOVED to T2 (Codex P0-A1 fix)**

> **Codex 사전 review P0-A1 fix**: T1에서 isValidUserShape를 `interestId` required로 강화하면 T2 migration 전에 기존 v6 user (interestId 없음)가 corruption으로 처리되어 데이터 잃음. T1은 `Array.isArray(insights)`만 검증 (기존 v3.23 그대로) → T2 migration 끝나는 순간 모든 user가 v7으로 전환됨 → T2 끝에 shape guard 강화. **본 step은 T2 Step 9~11로 이동**.

- [ ] **Step 6: 기존 mock factory + fixture ripple grep + 갱신 (Codex P1-E1 fix)**

기존 `state/user`를 vi.mock하는 spec과 `Insight` literal 생성하는 fixture/spec을 모두 찾아 신규 export(`validateInterestId`) passthrough + `interestId: 'unknown'` 채움.

Run:
```bash
grep -rln "vi.mock.*state/user\|vi\.mock.*['\"].*state/user" tests/ | head -20
grep -rn "^\s*insights:\s*\[\|insights = \[\|Insight = {" tests/ src/ | head -20
grep -rn "interestId" tests/ | head -10
```

Expected 출력 (예상):
- `tests/unit/ui/handlers/home-chat-wiring.spec.ts` — `state/user` mock 정의
- `tests/unit/ui/modals/insight-detail.spec.ts` — `state/user` mock 정의
- `tests/utils/userMock.ts` 또는 `tests/helpers/userFixture.ts` — `Insight[]` literal

각 파일 갱신:
- mock factory의 `vi.mock('.../state/user', () => ({ ... }))`에 `validateInterestId: vi.fn(...)` 추가 (또는 `vi.importActual` 패턴으로 자동 passthrough)
- Insight literal에 `interestId: 'unknown'` 추가

- [ ] **Step 7: Add 'dg:insights:updated' event to events.ts**

Modify `src/ui/events.ts`:

```ts
// EventMap interface에 추가 (line 52 'dg:insights:removed' 다음)
'dg:insights:updated': { id: string };

// EVENT_NAMES 배열에 추가 (line 86 'dg:insights:removed' 다음)
'dg:insights:updated',
```

- [ ] **Step 8: home.ts:1127 Insight literal 임시 fix (TS strict 0 의무)**

Modify `src/ui/handlers/home.ts:1127-1131`:

```ts
const insight: Insight = {
  id: crypto.randomUUID(),
  text: insightText,
  interestId: 'unknown',  // v3.25 T1: 임시 — T4에서 parseInsightResponse 결과로 교체
  createdAt: new Date().toISOString(),
};
```

- [ ] **Step 9: 양쪽 TZ vitest + tsc 통과 검증**

Run:
```bash
npx tsc --noEmit
TZ=Asia/Seoul npm test 2>&1 | grep -E "Tests|Test Files"
TZ=America/New_York npm test 2>&1 | grep -E "Tests|Test Files"
```
Expected:
- TS: 0 errors
- Vitest: 1098 + 3 (validateInterestId) = 1101. 신규 +3 (shape 강화는 T2로 이동).
- 양쪽 TZ 동일
- 기존 spec 회귀 0 (mock factory ripple Step 6에서 처리됨)

DoD: 본 task의 신규 spec ≥3 + 기존 회귀 0.

- [ ] **Step 10: Commit**

```bash
git add src/state/user.ts src/ui/events.ts src/ui/handlers/home.ts \
        tests/unit/validate-interest-id.spec.ts
git commit -m "feat(v3.25 T1): Insight.interestId + validateInterestId + isValidUserShape 강화 + dg:insights:updated 이벤트"
```

---

### Task 2: Schema migration v6 → v7 (lazy)

**Files:**
- Modify: `src/state/migration.ts:306-328` (migrateUserToV6 다음에 migrateUserToV7 추가)
- Modify: `src/state/migration.ts` (V3/V4/V5/V6 early-return에 v7 guard 추가)
- Modify: `src/state/user.ts:84-110` (`getCachedUser` chain 확장 + v7 lazy persist 분기)
- Create: `tests/unit/migration-v7.spec.ts`

- [ ] **Step 1: Write failing tests for migrateUserToV7**

Create `tests/unit/migration-v7.spec.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { migrateUserToV7 } from '../../src/state/migration';

describe('migrateUserToV7 (v3.25 T2)', () => {
  function makeV6Base(): any {
    return {
      schemaVersion: 6,
      name: 'X', interests: [], onboardedAt: '2026-01-01',
      streak: 0, lastActiveDate: '2026-01-01',
      xp: 0, earnedBadges: {}, gamificationMigrated: true,
      missions: { active: [], cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 },
                  lastDailySeed: '2026-01-01', currentWeekIso: '2026-W01', currentMonthIso: '2026-01' },
      plantStateByInterest: {}, gardenIntroduced: true, gardenBackfilled: true,
      streakFreeze: { count: 2, lastEarnedAt: '2026-01-01' },
      insights: [],
    };
  }

  it('v6 → v7 — insights에 interestId=unknown 채움 + schemaVersion 7', () => {
    const v6 = makeV6Base();
    v6.insights = [
      { id: 'i1', text: 'ok', createdAt: '2026-01-01' },  // interestId 누락
      { id: 'i2', text: 'ok2', createdAt: '2026-01-02' },
    ];
    const v7 = migrateUserToV7(v6);
    expect(v7.schemaVersion).toBe(7);
    expect(v7.insights).toHaveLength(2);
    expect(v7.insights[0].interestId).toBe('unknown');
    expect(v7.insights[1].interestId).toBe('unknown');
  });

  it('v7 idempotent — 같은 reference 반환', () => {
    const v7 = makeV6Base();
    v7.schemaVersion = 7;
    v7.insights = [{ id: 'i1', text: 'ok', createdAt: '2026-01-01', interestId: 'recruiting' }];
    const result = migrateUserToV7(v7);
    expect(result.schemaVersion).toBe(7);
    expect(result.insights[0].interestId).toBe('recruiting');
  });

  it('corrupted entry 제거 — id 또는 text 누락', () => {
    const v6 = makeV6Base();
    v6.insights = [
      { id: 'i1', text: 'ok', createdAt: '2026-01-01', interestId: 'recruiting' },
      { id: 'i2', text: '', createdAt: '2026-01-02' },     // text empty → drop
      { id: '', text: 'orphan', createdAt: '2026-01-03' }, // id empty → drop
      { text: 'no-id', createdAt: '2026-01-04' },          // id 누락 → drop
    ];
    const v7 = migrateUserToV7(v6);
    expect(v7.insights).toHaveLength(1);
    expect(v7.insights[0].id).toBe('i1');
  });

  it('interestId invalid string — validateInterestId 폴백 (unknown)', () => {
    const v6 = makeV6Base();
    v6.insights = [
      { id: 'i1', text: 'ok', createdAt: '2026-01-01', interestId: 'hallucinated' },
    ];
    const v7 = migrateUserToV7(v6);
    expect(v7.insights[0].interestId).toBe('unknown');
  });
});
```

- [ ] **Step 2: Run tests — verify FAIL**

Run: `npx vitest run tests/unit/migration-v7.spec.ts`
Expected: FAIL — `migrateUserToV7 is not a function`.

- [ ] **Step 3: Add migrateUserToV7**

Modify `src/state/migration.ts` — append after `migrateUserToV6` (line 328):

```ts
/**
 * v3.25 T2: schema v6 → v7 — Insight.interestId 필드 추가.
 * - 기존 insights[]에 interestId='unknown' default 채움.
 * - id/text 누락 entry는 silent drop (corruption 폴백, v3.25 spec §3).
 * - validateInterestId로 invalid id → 'unknown' 폴백.
 * - idempotent: 이미 v7이면 그대로 반환 (early return).
 *
 * @internal Caller invariant — `getCachedUser` chain (V3→V4→V5→V6→V7) 후 호출.
 */
export function migrateUserToV7(u: unknown): User {
  const r = u as Record<string, unknown> & {
    schemaVersion?: number;
    insights?: unknown;
  };

  if (r.schemaVersion === 7) return r as unknown as User;

  // v6까지 lift (V6 early-return이 v7 guard됨)
  const v6 = migrateUserToV6(u);
  const v6r = v6 as unknown as Record<string, unknown>;

  // insights 마이그레이션 (id/text 누락 entry drop + interestId 폴백)
  const insightsIn = Array.isArray(v6r.insights) ? (v6r.insights as unknown[]) : [];
  const insights: Insight[] = insightsIn
    .map((i: any) => ({
      id: typeof i?.id === 'string' ? i.id : '',
      text: typeof i?.text === 'string' ? i.text : '',
      createdAt: typeof i?.createdAt === 'string' ? i.createdAt : new Date().toISOString(),
      interestId: typeof i?.interestId === 'string'
        ? validateInterestId(i.interestId)
        : 'unknown',
    }))
    .filter((i) => i.id.length > 0 && i.text.length > 0);  // 손상 entry drop

  return {
    ...v6,
    schemaVersion: 7 as const,
    insights,
  } as unknown as User;
}
```

Add imports at top of `src/state/migration.ts` (if not present):
```ts
import { validateInterestId } from './user';
```

- [ ] **Step 4: Add v7 guards to V3/V4/V5/V6 early-returns (chain superset)**

Modify `src/state/migration.ts` — find each `migrateUserToVX` 함수의 early-return 분기에 v7 guard 추가:

V3 early-return:
```ts
if (r.schemaVersion === 3 || r.schemaVersion === 4 || r.schemaVersion === 5 || r.schemaVersion === 6 || r.schemaVersion === 7) return r as unknown as User;
```
V4 / V5 / V6도 동일하게 v7 추가 (v3.23 lesson "chain superset" 패턴 답습).

- [ ] **Step 5: Run tests — verify PASS**

Run: `npx vitest run tests/unit/migration-v7.spec.ts`
Expected: 4 specs ALL PASS.

- [ ] **Step 6: Extend getCachedUser chain**

Modify `src/state/user.ts:84-110` (`getCachedUser`):

```ts
let user = (parsed?.schemaVersion === 2 || parsed?.schemaVersion === 3 || parsed?.schemaVersion === 4
            || parsed?.schemaVersion === 5 || parsed?.schemaVersion === 6 || parsed?.schemaVersion === 7)
  ? parsed
  : migrateUserToV2(parsed);
user = migrateUserToV3(user);
user = migrateUserToV4(user);
user = migrateUserToV5(user);
user = migrateUserToV6(user);
user = migrateUserToV7(user);  // v3.25 T2: v6→v7 lazy migration
```

`else if (parsed?.schemaVersion !== 6)` 분기를 `!== 7`로 변경:
```ts
} else if (parsed?.schemaVersion !== 7) {
  // lazy migrate v1/v2/v3/v4/v5/v6 → v7
  try { localStorage.setItem(KEY, JSON.stringify(user)); } catch { /* ignore */ }
}
```

Import 추가:
```ts
import { migrateUserToV2, migrateUserToV3, migrateUserToV4, migrateUserToV5, migrateUserToV6, migrateUserToV7 } from './migration';
```

- [ ] **Step 7: User.schemaVersion 타입 변경**

Modify `src/state/user.ts:52`:
```ts
schemaVersion: 7;
```

- [ ] **Step 8: migration spec PASS 검증**

Run:
```bash
npx tsc --noEmit
TZ=Asia/Seoul npm test 2>&1 | grep -E "Tests|Test Files"
TZ=America/New_York npm test 2>&1 | grep -E "Tests|Test Files"
```
Expected: 1101 + 4 = 1105 양쪽 TZ (T1 Step 9 expected 1101 + T2 신규 4). TS 0 errors. 기존 migration spec 회귀 0.

> **Codex P0-A1 안전점**: 본 step 시점에 모든 기존 user는 v7 마이그레이션 path 통과. v6 user → migrateUserToV7 → interestId='unknown' 채움. 다음 Step 9~11에서 strict shape guard 강화해도 데이터 손실 0.

- [ ] **Step 9: isValidUserShape 강화 spec 추가 (T1 Step 5에서 이동, Codex P0-A1 fix)**

Append to `tests/unit/validate-interest-id.spec.ts`:

```ts
import { getCachedUser } from '../../src/state/user';

describe('isValidUserShape — Insight entry shape (v3.25 T2)', () => {
  beforeEach(() => { localStorage.clear(); });

  function makeBaseUser(): any {
    return {
      schemaVersion: 7,
      name: 'X', interests: [], onboardedAt: '2026-01-01',
      streak: 0, lastActiveDate: '2026-01-01',
      xp: 0, earnedBadges: {}, gamificationMigrated: true,
      missions: { active: [], cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 },
                  lastDailySeed: '2026-01-01', currentWeekIso: '2026-W01', currentMonthIso: '2026-01' },
      plantStateByInterest: {}, gardenIntroduced: true, gardenBackfilled: true,
      streakFreeze: { count: 2, lastEarnedAt: '2026-01-01' },
      insights: [],
    };
  }

  it('정상 v7 insights — getCachedUser 통과', () => {
    const u = makeBaseUser();
    u.insights = [{ id: 'i1', text: 'ok', createdAt: '2026-01-01', interestId: 'recruiting' }];
    localStorage.setItem('user', JSON.stringify(u));
    expect(getCachedUser()).not.toBeNull();
  });

  it('v7 insights[].interestId 누락 — getCachedUser null', () => {
    const u = makeBaseUser();
    u.insights = [{ id: 'i1', text: 'ok', createdAt: '2026-01-01' }];
    localStorage.setItem('user', JSON.stringify(u));
    expect(getCachedUser()).toBeNull();
  });

  it('v7 insights[].interestId 비-string — getCachedUser null', () => {
    const u = makeBaseUser();
    u.insights = [{ id: 'i1', text: 'ok', createdAt: '2026-01-01', interestId: 123 }];
    localStorage.setItem('user', JSON.stringify(u));
    expect(getCachedUser()).toBeNull();
  });

  it('v6 insights — migrateUserToV7로 자동 변환 후 통과 (interestId=unknown 채움)', () => {
    const u = makeBaseUser();
    u.schemaVersion = 6;  // v6 user (interestId 없음)
    u.insights = [{ id: 'i1', text: 'ok', createdAt: '2026-01-01' }];  // interestId 없음
    localStorage.setItem('user', JSON.stringify(u));
    const result = getCachedUser();
    expect(result).not.toBeNull();
    expect(result!.schemaVersion).toBe(7);
    expect(result!.insights[0].interestId).toBe('unknown');
  });
});
```

- [ ] **Step 10: 신규 shape spec FAIL 검증**

Run: `npx vitest run tests/unit/validate-interest-id.spec.ts`
Expected: 정상 + v6 자동변환 spec PASS, 누락/비-string 2개 FAIL (현 isValidUserShape는 `Array.isArray(insights)`만 검증).

- [ ] **Step 11: isValidUserShape 강화 구현**

Modify `src/state/user.ts:124-150` (isValidUserShape) — `r.schemaVersion === 6 || r.schemaVersion === 7` 분기 안에 추가:

```ts
// v3.25 T2: Insight entry-level shape 강화 (P0-A1 fix — migration 후라 안전)
if (Array.isArray(r.insights)) {
  for (const i of r.insights as unknown[]) {
    if (!i || typeof i !== 'object') return false;
    const ie = i as Record<string, unknown>;
    if (typeof ie.id !== 'string' || ie.id.length === 0) return false;
    if (typeof ie.text !== 'string') return false;
    if (typeof ie.createdAt !== 'string') return false;
    if (typeof ie.interestId !== 'string') return false;
  }
}
```

- [ ] **Step 12: 신규 shape spec PASS 검증**

Run: `npx vitest run tests/unit/validate-interest-id.spec.ts`
Expected: 4 신규 spec ALL PASS.

- [ ] **Step 13: 양쪽 TZ vitest + tsc 최종 통과**

Run:
```bash
npx tsc --noEmit
TZ=Asia/Seoul npm test 2>&1 | grep -E "Tests|Test Files"
TZ=America/New_York npm test 2>&1 | grep -E "Tests|Test Files"
```
Expected: 1105 + 4 = 1109 양쪽 TZ. TS 0. 기존 spec 회귀 0 (Step 8에서 마이그레이션 path 통과 확인됨).

- [ ] **Step 14: Commit (migration + shape guard 강화 단일 commit)**

```bash
git add src/state/migration.ts src/state/user.ts \
        tests/unit/migration-v7.spec.ts tests/unit/validate-interest-id.spec.ts
git commit -m "feat(v3.25 T2): schema v6→v7 lazy migration + isValidUserShape Insight entry guard 강화 (Codex P0-A1)"
```

---

### Task 3: Gemini prompt 확장 + parseInsightResponse

**Files:**
- Modify: `src/services/prompts.ts:62-71` (PROMPTS.insight)
- Modify: `src/utils/gemini-parse.ts` (parseInsightResponse 추가)
- Create: `tests/unit/parse-insight-response.spec.ts`

- [ ] **Step 1: Write failing tests for parseInsightResponse**

Create `tests/unit/parse-insight-response.spec.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { parseInsightResponse } from '../../src/utils/gemini-parse';

describe('parseInsightResponse (v3.25 T3)', () => {
  it('정상 응답 — text|interestId split', () => {
    const r = parseInsightResponse('통찰 한 문장|recruiting');
    expect(r.text).toBe('통찰 한 문장');
    expect(r.interestId).toBe('recruiting');
  });

  it('| 없음 — 전체 text + unknown', () => {
    const r = parseInsightResponse('통찰만 있는 응답');
    expect(r.text).toBe('통찰만 있는 응답');
    expect(r.interestId).toBe('unknown');
  });

  it('interestId invalid — unknown 폴백', () => {
    const r = parseInsightResponse('통찰|hallucinated');
    expect(r.text).toBe('통찰');
    expect(r.interestId).toBe('unknown');
  });

  it('text에 | 포함 — lastIndexOf 기준 split', () => {
    const r = parseInsightResponse('a|b|c|recruiting');
    expect(r.text).toBe('a|b|c');
    expect(r.interestId).toBe('recruiting');
  });

  it('markdown wrap (backtick) 제거', () => {
    const r = parseInsightResponse('`통찰|recruiting`');
    expect(r.text).toBe('통찰');
    expect(r.interestId).toBe('recruiting');
  });

  it('markdown wrap (asterisk) 제거', () => {
    const r = parseInsightResponse('**통찰|ai_ml**');
    expect(r.text).toBe('통찰');
    expect(r.interestId).toBe('ai_ml');
  });

  it('여러 줄 응답 — 첫 비빈 줄만 사용', () => {
    const r = parseInsightResponse('  \n\n통찰 한 줄|recruiting\n설명 추가 줄|onboarding\n');
    expect(r.text).toBe('통찰 한 줄');
    expect(r.interestId).toBe('recruiting');
  });

  it("'unknown' literal 허용", () => {
    const r = parseInsightResponse('애매한 통찰|unknown');
    expect(r.text).toBe('애매한 통찰');
    expect(r.interestId).toBe('unknown');
  });

  // Codex P1-B1 강화: JSON wrap / quote / prefix / label alias

  it('JSON wrap (raw {...}) — text/interestId 추출', () => {
    const r = parseInsightResponse('{"text":"통찰","interestId":"recruiting"}');
    expect(r.text).toBe('통찰');
    expect(r.interestId).toBe('recruiting');
  });

  it('JSON fenced (```json) — text/interestId 추출', () => {
    const r = parseInsightResponse('```json\n{"text":"통찰","interestId":"ai_ml"}\n```');
    expect(r.text).toBe('통찰');
    expect(r.interestId).toBe('ai_ml');
  });

  it('JSON에 interestId 누락 — unknown 폴백 + text 추출', () => {
    const r = parseInsightResponse('{"text":"통찰"}');
    expect(r.text).toBe('통찰');
    expect(r.interestId).toBe('unknown');
  });

  it('id quote wrap ("recruiting") — quote 제거 후 매칭', () => {
    const r = parseInsightResponse('통찰|"recruiting"');
    expect(r.interestId).toBe('recruiting');
  });

  it('id prefix label (interestId: recruiting) — prefix 제거 후 매칭', () => {
    const r = parseInsightResponse('통찰|interestId: recruiting');
    expect(r.interestId).toBe('recruiting');
  });

  it("id 한국어 prefix (분야: recruiting) — 제거 후 매칭", () => {
    const r = parseInsightResponse('통찰|분야: recruiting');
    expect(r.interestId).toBe('recruiting');
  });

  it("id 한국어 label (채용) — alias map으로 매칭", () => {
    const r = parseInsightResponse('통찰|채용');
    expect(r.interestId).toBe('recruiting');
  });

  it("id emoji label (🎯 채용) — alias map으로 매칭", () => {
    const r = parseInsightResponse('통찰|🎯 채용');
    expect(r.interestId).toBe('recruiting');
  });
});
```

**총 신규 spec**: 8 (기존) + 8 (Codex P1-B1) = **16건**.

- [ ] **Step 2: Run tests — verify FAIL**

Run: `npx vitest run tests/unit/parse-insight-response.spec.ts`
Expected: FAIL — `parseInsightResponse is not a function`.

- [ ] **Step 3: Implement parseInsightResponse (P1-B1 강화 폴백 chain)**

Modify `src/utils/gemini-parse.ts`:

```ts
import { validateInsightText, validateInterestId } from '../state/user';
import { INTERESTS } from './categories';

/**
 * v3.25 T3: 한국어/emoji label → id alias map (Codex P1-B1 fix).
 * Gemini가 instruction 무시하고 label 직접 반환 시 폴백.
 */
const INTEREST_LABEL_ALIAS = new Map<string, string>(
  INTERESTS.flatMap(i => [
    [i.label, i.id] as [string, string],                                    // emoji prefix 포함 ('🎯 채용')
    [i.label.replace(/^[\p{Emoji}\s]+/u, ''), i.id] as [string, string],    // emoji prefix 제거 ('채용')
  ]),
);

/**
 * id 후처리: quote/prefix strip + alias map → whitelist 폴백.
 * @internal
 */
function normalizeInterestId(raw: string): string {
  const stripped = raw
    .trim()
    .replace(/^["'`]|["'`]$/g, '')                  // quote wrap
    .replace(/^(interestId|분야)\s*[:：]\s*/i, '')  // prefix label
    .trim();
  if (stripped.length === 0) return 'unknown';
  const aliased = INTEREST_LABEL_ALIAS.get(stripped);
  if (aliased) return aliased;
  return validateInterestId(stripped);
}

/**
 * v3.25 T3: Gemini insight 응답 `<text>|<interestId>` 견고 파싱.
 *
 * 폴백 chain (Codex P1-B1 강화):
 * 1. JSON wrap 감지 — fenced json 또는 raw {...} → text/interestId 추출 우선
 * 2. 첫 비빈 줄만 추출 (Gemini 여러 줄 응답 대비)
 * 3. markdown wrap 제거 (backtick / asterisk / dash)
 * 4. lastIndexOf('|') 기준 split (text에 '|' 포함 가능성 차단)
 * 5. id 후처리 — quote/prefix strip → alias map → validateInterestId 최종 폴백
 *
 * @throws Error text가 모든 폴백 후에도 빈 경우 (validateInsightText)
 */
export function parseInsightResponse(raw: string): { text: string; interestId: string } {
  // 1. JSON wrap 감지 (fenced json 또는 raw {...})
  const jsonMatch = raw.match(/\{[\s\S]*?"text"[\s\S]*?\}/);
  if (jsonMatch) {
    try {
      const obj = JSON.parse(jsonMatch[0]);
      if (typeof obj.text === 'string' && obj.text.trim().length > 0) {
        return {
          text: validateInsightText(obj.text),
          interestId: typeof obj.interestId === 'string'
            ? normalizeInterestId(obj.interestId)
            : 'unknown',
        };
      }
    } catch { /* fall through to line-based parse */ }
  }

  // 2. 첫 비빈 줄만 추출
  const firstLine = raw
    .split('\n')
    .map(l => l.trim())
    .find(l => l.length > 0) ?? '';

  // 3. markdown wrap 제거
  const stripped = firstLine
    .replace(/^[`*\-]+|[`*\-]+$/g, '')
    .trim();

  // 4. lastIndexOf('|') 기준 split
  const lastPipe = stripped.lastIndexOf('|');
  if (lastPipe === -1) {
    return {
      text: validateInsightText(stripped),
      interestId: 'unknown',
    };
  }

  const rawText = stripped.slice(0, lastPipe).trim();
  const rawId = stripped.slice(lastPipe + 1).trim();

  return {
    text: validateInsightText(rawText),
    interestId: normalizeInterestId(rawId),
  };
}
```

- [ ] **Step 4: Run tests — verify PASS**

Run: `npx vitest run tests/unit/parse-insight-response.spec.ts`
Expected: 8 specs ALL PASS.

- [ ] **Step 5: PROMPTS.insight 확장**

Modify `src/services/prompts.ts:62-71`:

```ts
insight: {
  maxOutputTokens: 110,
  build(input: ChatInput): string {
    return [
      '아래 대화에서 가장 중요한 통찰을 추출.',
      '반드시 다음 형식의 1줄로만 출력 (다른 설명/마크다운 금지):',
      '<통찰 한국어 1문장 (최대 80자)>|<interestId>',
      '',
      '<interestId>는 아래 16개 중 하나만 사용. 어떤 분야인지 매칭이 어려우면 unknown.',
      'recruiting, onboarding, culture, hr_system, labor_law, leadership,',
      'pm, ai_ml, data, startup, marketing, productivity,',
      'career, communication, self_dev, unknown',
      '---',
      turnsToText(input.chatTurns),
    ].join('\n');
  },
} satisfies PromptTemplate,
```

- [ ] **Step 6: 양쪽 TZ vitest 통과 검증**

Run:
```bash
TZ=Asia/Seoul npm test 2>&1 | grep -E "Tests|Test Files"
TZ=America/New_York npm test 2>&1 | grep -E "Tests|Test Files"
```
Expected: 1108 + 8 = 1116. TS 0. 기존 prompts spec 회귀 0.

- [ ] **Step 7: Commit**

```bash
git add src/services/prompts.ts src/utils/gemini-parse.ts \
        tests/unit/parse-insight-response.spec.ts
git commit -m "feat(v3.25 T3): PROMPTS.insight 응답 형식 확장 + parseInsightResponse (markdown trim + 첫 비빈 줄 + lastIndexOf split + whitelist 폴백)"
```

---

### Task 4: home.ts insight 저장 wiring (parseInsightResponse 적용)

**Files:**
- Modify: `src/ui/handlers/home.ts:1102-1147` (insight 저장 path)
- Modify: 기존 `tests/unit/ui/handlers/home-chat-wiring.spec.ts` 또는 신규 `tests/unit/home-insight-save.spec.ts`

- [ ] **Step 1: Write failing test for home.ts insight 저장 wiring**

Determine — 기존 `tests/unit/ui/handlers/home-chat-wiring.spec.ts`가 generate-insight 부분 cover 하는지 확인. 없으면 신규.

```bash
grep -n "generateInsight\|PROMPTS.insight\|interestId" tests/unit/ui/handlers/home-chat-wiring.spec.ts
```

신규일 경우 Create `tests/unit/home-insight-save.spec.ts`:

```ts
/**
 * @vitest-environment jsdom
 *
 * v3.25 T4: home insight 생성 wiring (parseInsightResponse 통합).
 */
import { describe, it, expect, beforeEach, vi } from 'vitest';

const mockGenerateText = vi.fn();
const mockGetApiKey = vi.fn().mockReturnValue('fake-key');
const mockCheckGemini = vi.fn().mockReturnValue(true);
const mockGetCachedUser = vi.fn();
const mockSaveUser = vi.fn();
const mockShowToast = vi.fn();
const mockDispatch = vi.fn();

vi.mock('../../src/services/gemini', () => ({
  generateText: (...a: unknown[]) => mockGenerateText(...a),
}));
vi.mock('../../src/utils/apiKey', () => ({
  getApiKey: (...a: unknown[]) => mockGetApiKey(...a),
}));
vi.mock('../../src/utils/geminiUsage', () => ({
  checkAndIncrementGemini: (...a: unknown[]) => mockCheckGemini(...a),
}));
vi.mock('../../src/state/user', async () => {
  const actual = await vi.importActual<any>('../../src/state/user');
  return {
    ...actual,  // validateInsightText / validateInterestId / Insight type passthrough
    getCachedUser: (...a: unknown[]) => mockGetCachedUser(...a),
    saveUser: (...a: unknown[]) => mockSaveUser(...a),
    getSaveErrorMessage: vi.fn().mockReturnValue('저장 실패'),
  };
});
vi.mock('../../src/utils/toast', () => ({
  showToast: (...a: unknown[]) => mockShowToast(...a),
}));
vi.mock('../../src/ui/events', () => ({
  dispatch: (...a: unknown[]) => mockDispatch(...a),
  on: vi.fn(),
}));

describe('generateInsightCard wiring (v3.25 T4)', () => {
  beforeEach(() => { vi.clearAllMocks(); });

  // helper — minimal 사용자 + history 준비
  function setup(geminiResponse: string) {
    mockGenerateText.mockResolvedValue(geminiResponse);
    mockGetCachedUser.mockReturnValue({
      schemaVersion: 7, insights: [], /* 기타 필드 생략 */
    } as any);
  }

  it('정상 응답 — Insight.interestId가 parseInsightResponse 결과', async () => {
    setup('통찰 한 문장|recruiting');
    // generateInsightCard 또는 export된 함수 호출
    // ... (기존 home.ts wiring path 호출 흐름 모방)
    // assert: saveUser 호출 시 user.insights[0].interestId === 'recruiting'
    expect(true).toBe(false);  // RED — 아직 wiring 안 됨
  });

  it('형식 깨짐 (| 없음) — interestId="unknown" 저장', async () => {
    setup('통찰만 있고 분야 없음');
    expect(true).toBe(false);  // RED
  });
});
```

**Note:** 신규 spec 작성 시 jsdom mock factory가 너무 무거우면, 기존 `tests/unit/ui/handlers/home-chat-wiring.spec.ts`의 mock 패턴을 답습. v3.24 lesson #8 (mock factory grep 의무) 준수.

- [ ] **Step 2: Run test — verify FAIL**

Run: `npx vitest run tests/unit/home-insight-save.spec.ts`
Expected: FAIL — interestId 채움 안 됨.

- [ ] **Step 3: Apply parseInsightResponse to home.ts**

Modify `src/ui/handlers/home.ts:1102-1147`:

```ts
try {
  const tmpl = PROMPTS.insight;
  const raw = await generateText({
    apiKey: getApiKey()!,
    prompt: tmpl.build({ chatTurns: history.map(m => ({ role: m.role, text: m.text })) }),
    maxOutputTokens: tmpl.maxOutputTokens,
  });
  // v3.25 T4: parseInsightResponse로 text + interestId 함께 추출
  let parsed: { text: string; interestId: string };
  try {
    parsed = parseInsightResponse(raw);
  } catch {
    showToast('AI 분석에 실패했어요');
    return;
  }
  const insightText = parsed.text;
  const insightInterestId = parsed.interestId;
  renderChatPreviewBubble({
    text: insightText,
    primaryLabel: '저장',
    secondaryLabel: '폐기',
    onPrimary: () => {
      const u = getCachedUser();
      if (!u) {
        showToast('사용자 정보를 불러올 수 없어요');
        return;
      }
      const insight: Insight = {
        id: crypto.randomUUID(),
        text: insightText,
        interestId: insightInterestId,  // v3.25 T4: T1 임시 'unknown' 교체
        createdAt: new Date().toISOString(),
      };
      u.insights.push(insight);
      try {
        saveUser(u);
        dispatch('dg:insights:added', { id: insight.id });
        showToast('인사이트 카드 1장 추가');
      } catch (e) {
        u.insights.pop();
        showToast(getSaveErrorMessage(e));
      }
    },
    onSecondary: () => {},
  });
} catch {
  showToast('AI 분석에 실패했어요');
}
```

Import 추가:
```ts
import { parseInsightResponse } from '../../utils/gemini-parse';
```

기존 `validateInsightText` import는 더 이상 직접 사용 안 함 (parseInsightResponse 내부에서 호출). 단, ts unused import 경고는 grep 검증 후 제거.

- [ ] **Step 4: Run test — verify PASS**

Run: `npx vitest run tests/unit/home-insight-save.spec.ts`
Expected: PASS (기존 RED 단언을 실제 wiring 검증으로 교체).

- [ ] **Step 5: 양쪽 TZ vitest + tsc 통과**

Run:
```bash
npx tsc --noEmit
TZ=Asia/Seoul npm test 2>&1 | grep -E "Tests|Test Files"
TZ=America/New_York npm test 2>&1 | grep -E "Tests|Test Files"
```
Expected: 1116 + 2 = 1118. TS 0.

- [ ] **Step 6: Commit**

```bash
git add src/ui/handlers/home.ts \
        tests/unit/home-insight-save.spec.ts
git commit -m "feat(v3.25 T4): home insight 저장 wiring (parseInsightResponse 적용 — interestId 자동 분류)"
```

---

### Task 5: Insights 탭 chip 필터 + 카드 chip

**Files:**
- Modify: `src/ui/tabs/insights.ts:14-37` (renderInsights)
- Modify: `src/ui/tabs/insights.ts:43-52` (mountInsightsHandlers — 'updated' 이벤트 + 필터 reset)
- Modify: `tests/unit/ui/tabs/insights.spec.ts` 확장 또는 신규 `tests/unit/insights-filter.spec.ts`

- [ ] **Step 1: Write failing tests for chip filter**

Add to `tests/unit/ui/tabs/insights.spec.ts` (또는 신규 `tests/unit/insights-filter.spec.ts`):

```ts
describe('insights chip filter (v3.25 T5)', () => {
  beforeEach(() => { /* setup user with mixed interests */ });

  it('insights.length < 6 — filter row 미노출 (카드만)', () => {
    // 5개 insights, distinct interest 4개
    // renderInsights call
    // expect(container.querySelector('.insight-filter-row')).toBeNull();
    // expect cards have .insight-chip
  });

  it('insights.length >= 6 — filter row 노출 + distinct interest chip만', () => {
    // 6개 insights, distinct interest 3개 ['recruiting', 'ai_ml', 'unknown']
    // renderInsights call
    // expect filter row exists
    // expect chips: ['전체', '🎯 채용', '🤖 AI/ML', '📰 미분류']  (전체 + 3 distinct in catalog order)
  });

  it('chip 클릭 시 해당 분야 카드만 grid 표시', () => {
    // renderInsights with 6 insights
    // click chip data-interest-id='recruiting'
    // expect grid: only recruiting cards visible
  });

  it("dg:insights:added 이벤트 시 '전체'로 reset", () => {
    // mountInsightsHandlers
    // setup tab DOM with active filter='recruiting'
    // dispatch 'dg:insights:added'
    // expect: active chip is '전체'
  });
});
```

- [ ] **Step 2: Run tests — verify FAIL**

Run: `npx vitest run tests/unit/ui/tabs/insights.spec.ts`
Expected: 신규 spec FAIL.

- [ ] **Step 3: Extend renderInsights**

Modify `src/ui/tabs/insights.ts:14-37`:

```ts
import { INTERESTS, getCategoryLabel } from '../../utils/categories';

export function renderInsights(container: HTMLElement, activeFilter: string = ''): void {
  const user = getCachedUser();
  const insights = (user?.insights ?? []).slice().sort((a, b) =>
    b.createdAt.localeCompare(a.createdAt),
  );

  if (insights.length === 0) {
    // 기존 empty state 유지 (변경 없음)
    container.innerHTML = `<div class="insights-section" id="insightsTab"><h2 style="margin-bottom:16px;">💡 인사이트</h2><div id="insightContent" style="color:var(--text-secondary);font-size:0.9rem;line-height:1.6;">아직 인사이트가 없어요. 대화를 나누고 인사이트 카드를 만들어보세요.</div></div>`;
    return;
  }

  // 필터링 (activeFilter='' → 전체)
  const filtered = activeFilter
    ? insights.filter(i => i.interestId === activeFilter)
    : insights;

  // 필터 row HTML — insights.length >= 6 일 때만
  const filterRowHtml = insights.length >= 6
    ? buildFilterRow(insights, activeFilter)
    : '';

  // 카드 HTML
  const cardsHtml = filtered.map(i => {
    const isUnknown = i.interestId === 'unknown';
    const chipLabel = isUnknown ? '📰 미분류' : escapeHtml(getCategoryLabel(i.interestId));
    const chipClass = isUnknown ? 'insight-chip insight-chip--muted' : 'insight-chip';
    return `<button type="button" class="insight-card" data-insight-id="${escapeHtml(i.id)}"><span class="${chipClass}">${chipLabel}</span><span class="insight-text">${escapeHtml(i.text)}</span><span class="insight-date">${escapeHtml(formatRelative(i.createdAt))}</span></button>`;
  }).join('');

  // eslint-disable-next-line no-restricted-syntax -- escapeHtml applied
  container.innerHTML = `<div class="insights-section" id="insightsTab"><h2 style="margin-bottom:16px;">💡 인사이트</h2>${filterRowHtml}<div class="insights-grid">${cardsHtml}</div></div>`;

  // 카드 클릭 wiring (기존)
  container.querySelectorAll<HTMLButtonElement>('.insight-card').forEach(btn => {
    btn.addEventListener('click', () => {
      const id = btn.dataset.insightId!;
      openInsightDetailModal(id);
    });
  });

  // 필터 chip 클릭 wiring
  container.querySelectorAll<HTMLButtonElement>('.insight-filter-row .filter-chip').forEach(chip => {
    chip.addEventListener('click', () => {
      const newFilter = chip.dataset.interestId ?? '';
      renderInsights(container, newFilter);  // re-render with new filter
    });
  });
}

function buildFilterRow(insights: Insight[], activeFilter: string): string {
  // distinct interestId set
  const distinctIds = new Set(insights.map(i => i.interestId));

  // 정렬 순서: 전체 → INTERESTS 카탈로그 순서 (존재하는 분야만) → '미분류'
  const sortedIds: string[] = [
    '',  // 전체
    ...INTERESTS.filter(int => distinctIds.has(int.id)).map(int => int.id),
    ...(distinctIds.has('unknown') ? ['unknown'] : []),
  ];

  const chips = sortedIds.map(id => {
    const label = id === '' ? '전체' : (id === 'unknown' ? '📰 미분류' : escapeHtml(getCategoryLabel(id)));
    const activeClass = id === activeFilter ? ' active' : '';
    return `<button type="button" class="filter-chip${activeClass}" data-interest-id="${escapeHtml(id)}">${label}</button>`;
  }).join('');

  return `<div class="insight-filter-row">${chips}</div>`;
}
```

- [ ] **Step 4: Extend mountInsightsHandlers — added/removed/updated 모두 '전체' reset + refresh**

Modify `src/ui/tabs/insights.ts:43-52`:

```ts
export function mountInsightsHandlers(): void {
  const refresh = (): void => {
    const tabEl = document.getElementById('insightsTab')?.parentElement;
    if (tabEl) renderInsights(tabEl as HTMLElement, '');  // '전체'로 reset
  };
  on('dg:insights:added', refresh);
  on('dg:insights:removed', refresh);
  on('dg:insights:updated', refresh);  // v3.25 T5: dropdown 변경 시 refresh
}
```

- [ ] **Step 5: Run tests — verify PASS**

Run: `npx vitest run tests/unit/ui/tabs/insights.spec.ts`
Expected: 모든 신규 spec PASS.

- [ ] **Step 6: 양쪽 TZ vitest 통과**

Run:
```bash
TZ=Asia/Seoul npm test 2>&1 | grep -E "Tests|Test Files"
TZ=America/New_York npm test 2>&1 | grep -E "Tests|Test Files"
```
Expected: 1118 + 4 = 1122 (T5 +4 spec, 본 plan §"Test Count Baseline"의 +2 보다 많을 수 있음 — 4건이 자연 도달 시 fine).

- [ ] **Step 7: Commit**

```bash
git add src/ui/tabs/insights.ts \
        tests/unit/ui/tabs/insights.spec.ts
git commit -m "feat(v3.25 T5): insights chip filter row(≥6) + 카드 chip + dg:insights:updated wiring"
```

---

### Task 6: Detail 모달 분야 dropdown

**Files:**
- Modify: `src/ui/modals/insight-detail.ts:16-42`
- Modify/Extend: `tests/unit/ui/modals/insight-detail.spec.ts`

- [ ] **Step 1: Write failing test for dropdown change + rollback**

Append to `tests/unit/ui/modals/insight-detail.spec.ts`:

```ts
describe('dropdown 분야 변경 (v3.25 T6)', () => {
  beforeEach(() => { vi.clearAllMocks(); });

  it('change 시 saveUser 호출 + dg:insights:updated dispatch', () => {
    // mock user with insight {id: 'i1', interestId: 'unknown'}
    // openInsightDetailModal('i1')
    // simulate select change to 'recruiting'
    // expect mockSaveUser called once with user where insights[0].interestId === 'recruiting'
    // expect mockDispatch called with 'dg:insights:updated', { id: 'i1' }
  });

  it('saveUser throw 시 in-memory + select.value 양방향 rollback', () => {
    // mock user with insight {id: 'i1', interestId: 'unknown'}
    // mockSaveUser.mockImplementationOnce(() => { throw new Error('Quota'); })
    // openInsightDetailModal
    // simulate select change to 'recruiting'
    // expect user.insights[0].interestId === 'unknown' (in-memory rollback)
    // expect select.value === 'unknown' (UI rollback)
    // expect mockShowToast with getSaveErrorMessage 결과
    // expect mockDispatch NOT called for 'dg:insights:updated'
  });

  it("invalid value (whitelist 외) — validateInterestId로 'unknown' 폴백", () => {
    // simulate select.value = 'recruiting' but somehow 'hallucinated' input
    // expect interestId === 'unknown' after change
  });
});
```

- [ ] **Step 2: Run test — verify FAIL**

Run: `npx vitest run tests/unit/ui/modals/insight-detail.spec.ts`
Expected: 신규 spec FAIL.

- [ ] **Step 3: Implement dropdown in openInsightDetailModal**

Modify `src/ui/modals/insight-detail.ts`:

```ts
import { openModal, closeModal } from './shared';
import { getCachedUser, saveUser, getSaveErrorMessage, validateInterestId } from '../../state/user';
import { INTERESTS, getCategoryLabel } from '../../utils/categories';
import { escapeHtml } from '../../utils/escapeHtml';
import { showToast } from '../../utils/toast';
import { dispatch } from '../events';

export function openInsightDetailModal(id: string): void {
  const user = getCachedUser();
  const insight = user?.insights.find(i => i.id === id);
  if (!user || !insight) return;

  // dropdown options: '미분류' 첫번째 + INTERESTS 15개 (catalog 순서)
  const optionsHtml = [
    `<option value="unknown"${insight.interestId === 'unknown' ? ' selected' : ''}>📰 미분류</option>`,
    ...INTERESTS.map(i => {
      const sel = insight.interestId === i.id ? ' selected' : '';
      return `<option value="${escapeHtml(i.id)}"${sel}>${escapeHtml(getCategoryLabel(i.id))}</option>`;
    }),
  ].join('');

  // eslint-disable-next-line no-restricted-syntax -- escapeHtml applied
  const bodyHtml = `<div class="insight-detail"><p class="insight-detail-text">${escapeHtml(insight.text)}</p><div class="insight-detail-meta"><label class="insight-detail-interest"><span>분야:</span><select class="insight-interest-select">${optionsHtml}</select></label><p class="insight-detail-date">${escapeHtml(insight.createdAt.slice(0, 10))}</p></div><button type="button" class="btn btn-danger insight-delete-btn">삭제</button></div>`;
  const modal = openModal({ title: '인사이트', bodyHtml });

  // dropdown change wiring (v3.25 T6)
  const select = modal.querySelector<HTMLSelectElement>('.insight-interest-select')!;
  select.addEventListener('change', () => {
    const newId = validateInterestId(select.value);
    const u = getCachedUser();
    const idx = u?.insights.findIndex(i => i.id === id) ?? -1;
    if (!u || idx === -1) return;
    const previous = u.insights[idx].interestId;
    u.insights[idx].interestId = newId;
    try {
      saveUser(u);
      dispatch('dg:insights:updated', { id });
      showToast('분야를 변경했어요');
    } catch (e) {
      u.insights[idx].interestId = previous;
      select.value = previous;
      showToast(getSaveErrorMessage(e));
    }
  });

  // delete wiring (기존 v3.23 T9 그대로)
  modal.querySelector('.insight-delete-btn')?.addEventListener('click', () => {
    if (!window.confirm('이 인사이트를 삭제할까요?')) return;
    const removed = user.insights.find(i => i.id === id);
    if (!removed) return;
    user.insights = user.insights.filter(i => i.id !== id);
    try {
      saveUser(user);
      dispatch('dg:insights:removed', { id });
      closeModal();
      showToast('인사이트를 삭제했어요');
    } catch (e) {
      user.insights.push(removed);
      showToast(getSaveErrorMessage(e));
    }
  });
}
```

- [ ] **Step 4: Run test — verify PASS**

Run: `npx vitest run tests/unit/ui/modals/insight-detail.spec.ts`
Expected: 신규 + 기존 spec ALL PASS.

- [ ] **Step 5: 양쪽 TZ vitest + tsc 통과**

Run:
```bash
npx tsc --noEmit
TZ=Asia/Seoul npm test 2>&1 | grep -E "Tests|Test Files"
TZ=America/New_York npm test 2>&1 | grep -E "Tests|Test Files"
```
Expected: 1122 + 3 = 1125. TS 0.

- [ ] **Step 6: Commit**

```bash
git add src/ui/modals/insight-detail.ts \
        tests/unit/ui/modals/insight-detail.spec.ts
git commit -m "feat(v3.25 T6): insight detail 모달 분야 dropdown (change 즉시 saveUser + 양방향 rollback)"
```

---

### Task 7: CSS polish — chip + filter row + dropdown styling

**Files:**
- Modify: `src/styles/pages/archive.css` 또는 `src/styles/main.css` 또는 신규 `src/styles/pages/insights.css`
  (기존 컨벤션 따라 결정 — `ls src/styles/pages/`로 확인)

- [ ] **Step 1: 기존 CSS layout 컨벤션 확인**

Run:
```bash
ls src/styles/pages/
grep -l "insightsTab\|insight-card\|insight-grid" src/styles/
```

Expected: 기존 insight 관련 CSS 위치 파악. 없으면 `src/styles/pages/insights.css` 신규 생성 (Vite 자동 import는 main.css에 `@import` 추가 필요).

- [ ] **Step 2: Add chip / filter / dropdown CSS**

Modify (또는 신규 생성) — append:

```css
/* v3.25 T7: insight chip + filter row + dropdown */

.insight-card {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 6px;
}

.insight-card .insight-chip {
  padding: 4px 10px;
  border-radius: 12px;
  background: var(--primary-ultra-light);
  color: var(--primary);
  font-size: 0.75rem;
  font-weight: 600;
  align-self: flex-start;
}
.insight-card .insight-chip--muted {
  background: var(--bg-input);
  color: var(--text-secondary);
}

.insight-filter-row {
  display: flex;
  gap: 8px;
  margin-bottom: 16px;
  flex-wrap: wrap;
}
/* .filter-chip은 archive.css에서 재활용 (이미 있음) */

.insight-detail-meta {
  display: flex;
  gap: 12px;
  align-items: baseline;
  flex-wrap: wrap;
  margin: 8px 0;
}
.insight-detail-interest {
  display: flex;
  gap: 6px;
  align-items: baseline;
  font-size: 0.85rem;
  color: var(--text-secondary);
}
.insight-interest-select {
  padding: 4px 8px;
  border-radius: 6px;
  border: 1px solid var(--border);
  background: var(--bg-input);
  color: var(--text-primary);
  font-size: 0.85rem;
  font-family: inherit;
}
```

`@media (hover: hover)` 가드는 `.filter-chip`에 이미 적용되어 있으므로 (archive.css 재활용) 추가 처리 X.

- [ ] **Step 3: Bundle 측정 (DoD bundle range 검증)**

Run:
```bash
npm run build && gzip -c dist/assets/index-*.js | wc -c
```
Expected: 40313 ≤ B ≤ 40713 (39913 + 400~+800).

초과 시 trim 검토: HTML template indent / 미사용 CSS rule / dead import.

- [ ] **Step 4: 다크 테마 검증 (수동)**

Run dev server:
```bash
npm run dev
```

브라우저에서:
1. 라이트 테마 — `.insight-chip` (primary-ultra-light), `.insight-chip--muted` (bg-input) 시각 확인
2. 다크 테마 토글 — 두 chip 모두 텍스트/배경 대비 충분한지 확인
3. 인사이트 6개+ 시 필터 row 노출 확인
4. detail 모달 dropdown 시각 확인 (라이트/다크 양쪽)

- [ ] **Step 5: 양쪽 TZ vitest 회귀 0 확인**

Run:
```bash
TZ=Asia/Seoul npm test 2>&1 | grep -E "Tests|Test Files"
TZ=America/New_York npm test 2>&1 | grep -E "Tests|Test Files"
```
Expected: 1125 (변동 0 — CSS task는 vitest 영향 없음).

- [ ] **Step 6: Commit**

```bash
git add src/styles/...
git commit -m "feat(v3.25 T7): insight chip / filter row / dropdown CSS (다크 테마 검증 통과)"
```

---

### Task 8: 사이클 끝 검증 + Codex 최종 review + retro

**Files:**
- Create: `~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v325.md` (home-scope memory, **repo 외부 — git tracked 아님**)
- Modify: `~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md` (index 추가, 동일 home-scope)
- Modify: `docs/superpowers/specs/2026-05-09-v3.25-insights-chip-design.md` §10/§11 retro append (사이클 metric / commit 목록 / lessons)

- [ ] **Step 1: TS strict + Lint 통과**

Run:
```bash
npx tsc --noEmit
npm run lint
```
Expected: 모두 0 errors.

- [ ] **Step 2: 양쪽 TZ vitest 통과**

Run:
```bash
TZ=Asia/Seoul npm test 2>&1 | grep -E "Tests|Test Files"
TZ=America/New_York npm test 2>&1 | grep -E "Tests|Test Files"
```
Expected: 양쪽 ≥1134 pass (baseline 1098 + ≥36, plan v2 기준 — Codex P0-A1/P1-B1 흡수 후). Test Count 표 채움.

- [ ] **Step 3: Smoke cold-fork fresh fork 통과 (v3.14.4 graduated)**

Run:
```bash
rm -rf node_modules/.vite-temp dist
npm run build
npm run test:smoke
```
Expected: 96/96 pass (변동 0). 시간 60s 이하.

- [ ] **Step 4: Bundle CLI gzip 최종 측정**

Run:
```bash
gzip -c dist/assets/index-*.js | wc -c
```
Expected: 40313 ≤ B ≤ 40713. Bundle 표 최종 행 채움.

40713 초과 시: HTML indent compress / 미사용 CSS / dead code 제거. v3.24 lesson #6 참조.

- [ ] **Step 5: Codex 최종 review dispatch**

Use `codex:rescue` skill (또는 controller fallback `pr-review-toolkit:code-reviewer`).

Scope: 전체 사이클 commit (T0~T7) full diff + DoD 검증 결과 + 사용자 검증 영역.

분류: `DEPLOY_APPROVED` / `APPROVED_WITH_NOTES` / `REJECT`.

P0 catch 시 in-cycle fix 후 재dispatch.

- [ ] **Step 6: Memory 갱신**

Create `~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v325.md`:

```markdown
---
name: DG v3.25 배포 완료 (또는 사이클 완료)
description: insights chip (Gemini 분류) — schema v6→v7 + write-time 자동 분류 + 필터 row + dropdown + ...
type: project
---

(retro 본문 — commit 목록 / 핵심 변경 / 신규 lessons / DoD 결과 / v3.26+ carry-forward)
```

Append to `~/.claude/projects/.../memory/MEMORY.md`:
```
- [DG v3.25 ...](project_daily_growth_v325.md) — 한 줄 요약 (~150 자)
```

- [ ] **Step 7: Spec retro append**

Modify spec `docs/superpowers/specs/2026-05-09-v3.25-insights-chip-design.md` — `## §X Retro` 섹션 추가 (v3.24 spec 패턴 답습):
- 사이클 요약 표 (commit / 길이 / TS / Lint / Vitest 양쪽 TZ / Smoke / Bundle / Codex 사전·최종)
- Commit 목록 표 (T0~T7)
- 핵심 변경 (T1~T7 각 요약)
- 신규 lessons (graduate 후보)
- v3.25 신규 P2 (Codex 최종 review에서 식별된 carry-forward)
- v3.26+ 잔여 carry seed
  - **Codex P2-C1 carry**: chip 필터 reset 정책 — `dg:insights:added`만 '전체' reset, `removed`/`updated`는 현재 chip 필터 preserve (탐색 상태 보존). 본 사이클은 단순화 위해 모두 reset 했지만 v3.26에서 event별 정책 분리 검토.
  - **Codex P2-G1 graduated**: T8 memory 경로 (`~/.claude/projects/.../memory/`, repo 외부 git untracked) — 본 plan v2에서 명문화 완료, 추가 carry 0.

- [ ] **Step 8: Final commit (Codex P2-G1 fix — repo-tracked 파일만)**

memory 파일은 home-scope (`~/.claude/...`)이라 git 추적 X. spec retro append만 commit.

```bash
git add docs/superpowers/specs/2026-05-09-v3.25-insights-chip-design.md
git commit -m "docs(v3.25 T8 retro): 사이클 끝 검증 + Codex 최종 review + retro append"
```

- [ ] **Step 9: Deploy 옵션 사용자 confirm 후 (별도 step — 사용자 명시 승인 필요)**

```bash
# 사용자 "배포해줘" 명시 시점에만:
npm run build
firebase deploy --only hosting
```

URL: https://my-ai-assistant-904f3.web.app

---

## DoD 최종 체크 (T8 종료 시점)

- [ ] TS strict 0 errors
- [ ] Lint 0 errors
- [ ] Vitest TZ=Asia/Seoul ≥1134 pass (plan v2 기준)
- [ ] Vitest TZ=America/New_York ≥1134 pass (plan v2 기준)
- [ ] Smoke 96/96 pass cold-fork
- [ ] Bundle CLI gzip ∈ [40313, 40713] B
- [ ] Codex 사전 review (T0) 결과 P0/P1 plan v2 in-cycle 흡수 완료
- [ ] Codex 최종 review (T8) `DEPLOY_APPROVED*`
- [ ] Memory `project_daily_growth_v325.md` + MEMORY.md index 추가
- [ ] Spec retro append (§Retro 섹션 작성)

---

## 사이클 metrics 목표 (v3.24 metrics 답습)

- Per-task review dispatch: T1/T3/T5/T6 reviewer agent (4회) + T2/T4/T7 controller-scope (3회) → 7 dispatch
- Codex review dispatch: 2회 (사전 T0 + 최종 T8)
- In-cycle review fix commit: ≤ 4건
- Commit 카운트: 9~13 (T0 spec/plan + T1~T8 main + 0~3 review fix)
- 사이클 길이: 1~2일

---

**plan status**: draft v1 (2026-05-09)
**다음 단계**: T0 baseline 측정 → Codex 사전 review dispatch → P0/P1 plan v2 in-cycle 흡수 → T1 진입
