# v3.26 Cleanup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v3.22~v3.25 4사이클 누적 ~20건 carry를 theme-grouped 9 task로 일괄 청산하고 bundle GREEN 복귀(40980 → ≤ 40713 B).

**Architecture:** cleanup 사이클 — schema/CSP/migration 0줄, 사용자 visible 변경 0. 변경 영역(테마)별 task 묶음(v3.16/v3.20/v3.24 graduated 패턴). T7(bundle GREEN)은 T1~T6 누적 영향 흡수 후 진입. Codex 2-pass mandatory(v3.24 graduated) + per-task review 비대칭 dispatch(v3.24 graduated).

**Tech Stack:** Vite + TypeScript SPA, Vitest, Playwright, ESLint(`.eslintrc.cjs` CommonJS), Firebase Hosting. baseline: `9c32b48`.

**Spec ref:** [`docs/superpowers/specs/2026-05-10-v3.26-cleanup-design.md`](../specs/2026-05-10-v3.26-cleanup-design.md)

---

## File Structure (Mapping)

### Touched files (T1~T7 합산)

| 카테고리 | 파일 | 책임 | 변경 task |
|---|---|---|---|
| **utils/intl** | `src/utils/intl.ts` (신설) | Intl.DateTimeFormat KST singleton 중앙화 | T1 |
| **utils/dates** | `src/utils/dates.ts` | `KST_FMT` → `intl.ts`로 이전 | T1 |
| **utils/stats** | `src/utils/statsAggregate.ts` | `KST_FMT` → `intl.ts` import | T1 |
| **state/missionEngine** | `src/state/missionEngine.ts` | `KST_FMT` → `intl.ts` import | T1 |
| **lint config** | `.eslintrc.cjs` | `no-restricted-imports` patterns 강화 (Date.parse / new Date() ISO 등) | T1 |
| **state/user** | `src/state/user.ts` | `isValidUserShape` v7 Insight entry guard에 `interestId` normalize→`'unknown'` + `console.warn` 1줄 | T2 |
| **state/migration** | `src/state/migration.ts` | (기존 v6→v7 path 유지, 변경 없을 가능성 — T2 entry guard 한 곳) | T2 |
| **ui/modal-system** | `src/ui/modals/shared.ts` | `ModalConfig`를 discriminated union `{bodyHtml: string} \| {bodyNode: Node}` (xor) | T3 |
| **ui/stats-range-modal** | `src/ui/modals/stats-range-modal.ts` | `bodyNode` path 채택, innerHTML 직렬화 단계 제거 | T3 |
| **styles** | `src/styles/components.css` (또는 적절 layer) | `.bar-label` rule + focus-visible 추가 | T4 |
| **ui/garden-grid** | `src/ui/garden-grid.ts` 또는 `src/ui/components/garden-grid.ts` | aria-label에 escapeHtml 적용 | T4 |
| **data/prompts** | `src/data/prompts.ts` | "이미 압축" 주석 검증 (변경 없을 가능성) | T4 |
| **tests/migration** | `tests/unit/state/migration-v6.spec.ts` | `as any` → 명시 타입 헬퍼로 정리 | T5 |
| **tests** | tests/* (apiKey 주석, byInterest null spec, mock cleanup, N1·N3 등) | quality 정리 | T5 |
| **메타** | `.gitignore` (옵션 a 채택 시) 또는 `.claude/COMMON_MISTAKES.md` 위치 (옵션 b) + `CLAUDE.md` cross-link | tracked 결정 | T6 |
| **trim 대상** | T7에서 measurement 후 결정 (chip HTML, parser dead path, migration silent drop, stats-stat-card config, chat-bubble legacy wrapper) | bundle 압축 | T7 |
| **docs** | `docs/superpowers/specs/2026-05-10-v3.26-cleanup-design.md` (retro 추가) + memory `project_daily_growth_v326.md` + `MEMORY.md` index | retro | T8 |

### 신설 spec 파일 (예상)
- `tests/unit/utils/intl-singleton.spec.ts` (T1, +1~2 tests)
- `tests/unit/state/insight-interest-id-hardening.spec.ts` (T2, +3~5 tests)
- `tests/unit/ui/modal-shared-contract.spec.ts` (T3, +2~3 tests)
- 기타 batch tests (T4/T5, +5~8 tests)

신규 vitest 합계 +10~+18 (DoD §1.5).

---

## DoD 체크포인트 (각 task 완료 시)

```bash
# task 완료 후 표준 3종
npx tsc --noEmit                    # 0 errors
npm run lint                        # 0 errors / ≤ 37 warnings
npm test -- --run <영향 범위>       # 신규 spec PASS / N=5 5/5 안정
```

사이클 끝(T8) 추가:
```bash
TZ=Asia/Seoul npm test -- --run     # 1182~1190 PASS
TZ=America/New_York npm test -- --run # 동일 PASS
rm -rf node_modules/.vite-temp dist && npm run build
gzip -c dist/assets/index-*.js | wc -c # ≤ 40713 B (hard fail)
npm run test:smoke                  # 96/96 cold-fork
```

---

# Task 0: spec + plan v1 + baseline + 사전 Codex review patch

**Files:**
- Already created: `docs/superpowers/specs/2026-05-10-v3.26-cleanup-design.md`
- Created here: `docs/superpowers/plans/2026-05-10-v3.26-cleanup-plan.md` (this file)

- [ ] **Step 1: baseline 확정 (이미 측정 완료)**

이미 brainstorming에서 측정:
- TS strict: 0 errors
- Lint: 0 errors / 37 warnings
- Vitest TZ=Asia/Seoul: 1172/1172 pass
- Bundle CLI gzip: 40980 B
- Smoke: (T0 진입 시점 미측정 — T8에서 검증)

baseline 정보를 spec §1.5 + plan 헤더에 기록 (이미 반영).

- [ ] **Step 2: 사전 Codex review dispatch**

```bash
# 옵션 1: codex CLI (graduated 권장)
codex review docs/superpowers/specs/2026-05-10-v3.26-cleanup-design.md docs/superpowers/plans/2026-05-10-v3.26-cleanup-plan.md

# 옵션 2: controller fallback (권한/한도 실패 시, v3.14.5/v3.20/v3.24/v3.25 선례)
# Agent(subagent_type='pr-review-toolkit:code-reviewer', prompt='v3.26 spec + plan v1 P0/P1 분류')
```

review 결과:
- **P0**: in-cycle plan v2 흡수 (필수)
- **P1**: in-cycle plan v2 흡수 (권장)
- **P2**: v3.27+ carry seed에 추가

review fix 적용 시 spec / plan 둘 다 update 가능 (T0 단일 commit).

- [ ] **Step 3: T0 commit**

```bash
git add docs/superpowers/specs/2026-05-10-v3.26-cleanup-design.md docs/superpowers/plans/2026-05-10-v3.26-cleanup-plan.md
git commit -m "$(cat <<'EOF'
docs(v3.26 T0): spec + plan v2 + baseline + 사전 Codex review patch

- spec: v3.22~v3.25 누적 ~20건 cleanup, theme-grouped 10 slot, bundle GREEN 복귀 hard fail
- plan v2 (Codex 사전 review 흡수, P0=0/P1=4/P2=3 → 모두 in-cycle):
  * T1 → T1a/T1b split + 둘 다 reviewer-agent (P1-1)
  * T1b 시작 시 TZ 인벤토리 테이블 작성 (smoke today 3 spot + createdAt read 5 경로) (P1-2)
  * T2 normalizeInsightInterestIds 분리 + isValidUserShape pure invariant 유지 (P1-3)
  * T7 trim 후보 LOW(chip HTML)/MED(stats-stat-card·chat-bubble)/HIGH(parser·migration) 위험도 gate (P1-4)
  * T3 negative type test (both/neither) 추가 (P2-1)
  * T6 audit 결과 명시 기록 (P2-2)
- baseline: TS 0 / lint 0err·37warn / vitest 1172 / bundle 40980 (ceiling 40713 over by +267)
- 사전 Codex review: P0=0 / P1=4 (T1 split / TZ 인벤토리 / validator pure / trim gate) / P2=3 — 모두 plan v2 in-cycle 흡수

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

# Task 1a: Intl singleton + ESLint dates path 검증

> **plan v2 (Codex 사전 review P1-1 흡수)**: T1 4 sub-task fat → T1a (Intl/ESLint, infra 정리) + T1b (smoke/createdAt, runtime/data path)로 split. T1a/T1b 둘 다 reviewer-agent dispatch (large).

**Files:**
- Create: `src/utils/intl.ts`
- Modify: `src/utils/dates.ts` (KST_FMT 이전)
- Modify: `src/utils/statsAggregate.ts` (KST_FMT 이전)
- Modify: `src/state/missionEngine.ts` (KST_FMT 이전)
- Modify: `.eslintrc.cjs` (no-restricted-imports 검증, 격하 옵션 d 채택 가능)
- Test: `tests/unit/utils/intl-singleton.spec.ts` (신설)

## Sub-task 1.1: Intl.DateTimeFormat KST singleton 중앙화

- [ ] **Step 1: 신설 `src/utils/intl.ts` 작성**

```typescript
// src/utils/intl.ts
/**
 * Intl.DateTimeFormat KST singleton 중앙화 (v3.26 T1).
 * 사용 의무: KST anchor 가 필요한 모든 module은 본 export를 import.
 */

export const KST_FMT_DATE = new Intl.DateTimeFormat('en-CA', {
  timeZone: 'Asia/Seoul',
  year: 'numeric',
  month: '2-digit',
  day: '2-digit',
});
```

- [ ] **Step 2: 신규 spec `tests/unit/utils/intl-singleton.spec.ts`**

```typescript
import { describe, it, expect } from 'vitest';
import { KST_FMT_DATE } from '../../../src/utils/intl';

describe('KST_FMT_DATE singleton', () => {
  it('uses Asia/Seoul timeZone', () => {
    expect(KST_FMT_DATE.resolvedOptions().timeZone).toBe('Asia/Seoul');
  });

  it('formats UTC noon as KST date (en-CA: YYYY-MM-DD)', () => {
    // 2026-05-10T03:00:00Z = 2026-05-10 12:00 KST
    expect(KST_FMT_DATE.format(new Date('2026-05-10T03:00:00Z'))).toBe('2026-05-10');
  });

  it('formats UTC midnight as previous KST date for negative-offset days', () => {
    // 2026-05-10T15:00:00Z = 2026-05-11 00:00 KST (KST 자정)
    expect(KST_FMT_DATE.format(new Date('2026-05-10T15:00:00Z'))).toBe('2026-05-11');
  });
});
```

- [ ] **Step 3: 신규 spec 실행 (실패 확인 — `intl.ts` 미존재 시 test failure)**

```bash
npm test -- --run tests/unit/utils/intl-singleton.spec.ts
```

Expected: 신규 file 작성 후 PASS. 미작성 시 module resolution error.

- [ ] **Step 4: 기존 `KST_FMT` import 으로 swap**

`src/utils/dates.ts`:
```typescript
// Before:
const KST_FMT = new Intl.DateTimeFormat('en-CA', { timeZone: 'Asia/Seoul', year: 'numeric', month: '2-digit', day: '2-digit' });

// After:
import { KST_FMT_DATE as KST_FMT } from './intl';
```

`src/utils/statsAggregate.ts`:
```typescript
// Before:
const KST_FMT = new Intl.DateTimeFormat('en-CA', { ... });

// After:
import { KST_FMT_DATE as KST_FMT } from './intl';
```

`src/state/missionEngine.ts`:
```typescript
// Before:
const KST_FMT = new Intl.DateTimeFormat('en-CA', { ... });

// After:
import { KST_FMT_DATE as KST_FMT } from '../utils/intl';
```

- [ ] **Step 5: 기존 spec 회귀 검증**

```bash
TZ=Asia/Seoul npm test -- --run tests/unit/dates.spec.ts tests/unit/utils/intl-singleton.spec.ts
TZ=America/New_York npm test -- --run tests/unit/dates.spec.ts tests/unit/utils/intl-singleton.spec.ts
```

Expected: 양쪽 TZ 동일 PASS.

## Sub-task 1.2: ESLint `no-restricted-imports` 강화

> **결정 필요 (T1 진입 시 controller가 user에 confirm)**:
> 현재 rule은 `getDateStr` from `**/utils/dates`만 차단. 추가 강화 후보:
> - (a) `Date.parse` 직접 호출 차단 (TZ ambiguity) — 너무 광범위, 격하 권고
> - (b) `new Date('YYYY-MM-DD')` 패턴 차단 — JS엔진별 TZ 다름 — 권장
> - (c) (a)+(b) 모두 — 광범위, ROI 낮음
> - (d) **rule 추가 0줄** + `getDateStr` deprecation 정착 검증만 (현행 충분 판단) — 가장 안전, 격하 권고
>
> **Plan v1 권장**: 옵션 (d) — 현행 rule이 v3.22 이후 새 위반 0건이라면 graduate 자체로 충분. T1.2는 "검증 only" task로 격하.

- [ ] **Step 1: 현재 dates.ts 의존도 grep 확인**

```bash
grep -rn "from.*utils/dates" src/ tests/ | grep -v "utils/dates'" | head -20
# 또는
grep -rn "import.*getDateStr" src/ tests/ | head -10
```

Expected: 0건 위반 (이미 sweep 완료 v3.14.5/v3.22).

- [ ] **Step 2: ESLint 실행 + 위반 0건 확인**

```bash
npm run lint -- --rule 'no-restricted-imports: error'
```

Expected: 0 errors / 37 warnings.

- [ ] **Step 3: 결정 — rule 추가 미실시 (옵션 d 채택)**

위반 0건이면 graduate 충분. 추가 rule 0줄. spec/plan에 결정 명시.

> **격하 결정 시**: T1.2는 0줄 변경. T1 commit 메시지에 "ESLint rule 강화는 옵션 d 채택 (현행 충분, 위반 0건 검증)" 명시.

## T1a 검증 + commit

- [ ] **T1a 표준 3종 + 양쪽 TZ**

```bash
npx tsc --noEmit
npm run lint
TZ=Asia/Seoul npm test -- --run
TZ=America/New_York npm test -- --run
```

Expected: TS 0 / lint 0err·≤37warn / vitest 양쪽 PASS (1172 + 신규 1~3 = 1173~1175).

- [ ] **T1a per-task review (reviewer-agent dispatch — Codex 사전 P1-1 흡수, large 승격)**

```text
Agent(subagent_type='pr-review-toolkit:code-reviewer', prompt='
Review v3.26 T1a (Intl singleton 중앙화 + ESLint dates path 검증):
- src/utils/intl.ts (신설): KST_FMT_DATE singleton
- src/utils/dates.ts / statsAggregate.ts / missionEngine.ts: KST_FMT swap to import
- .eslintrc.cjs: no-restricted-imports 위반 0건 graduate 검증 (변경 0줄)
- tests/unit/utils/intl-singleton.spec.ts (신설)

Focus:
1. KST anchor invariant 보존 — singleton swap이 caller invariant 깨지 않는가?
2. tree-shaking 영향 — 단일 export가 caller bundle 사이즈에 영향?
3. 사용자 visible 변경 0 검증

Return P0/P1/P2.
')
```

- [ ] **T1a commit**

```bash
git add src/utils/intl.ts src/utils/dates.ts src/utils/statsAggregate.ts src/state/missionEngine.ts tests/unit/utils/intl-singleton.spec.ts
git commit -m "$(cat <<'EOF'
refactor(v3.26 T1a): Intl.DateTimeFormat KST singleton 중앙화 + ESLint graduate 검증

- src/utils/intl.ts (신설): KST_FMT_DATE singleton (3 callers swap)
- ESLint no-restricted-imports: 위반 0건 graduate 검증 (옵션 d 채택, 변경 0줄)
- vitest +3 (intl-singleton)
- 사용자 visible 변경 0

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

# Task 1b: Smoke 자정 race fix + createdAt UTC closing

> **plan v2 (Codex 사전 P1-2 흡수)**: TZ 인벤토리 테이블을 T1b 시작 시 작성. smoke today 위치(3 spot 실측: `tests/smoke/v3.13-missions.spec.ts:24-27`, `tests/smoke/v3.23-stub-cleanup.spec.ts:35-39`, `tests/smoke/missions-tab.spec.ts:24-29`) + createdAt read 경로(`src/ui/modals/insight-detail.ts:34-37`, `src/ui/handlers/archive.ts:358-360,490-495`) + write-side 경로 항목별 fix/carry 결정.

**Files:**
- Modify: `tests/smoke/v3.13-missions.spec.ts` (line 24-27)
- Modify: `tests/smoke/v3.23-stub-cleanup.spec.ts` (line 35-39)
- Modify: `tests/smoke/missions-tab.spec.ts` (line 24-29)
- Modify: `src/ui/modals/insight-detail.ts` (line 34-37 createdAt read)
- Modify: `src/ui/handlers/archive.ts` (line 358-360, 490-495 createdAt read)
- Modify: createdAt write-side spot (T1b 시작 시 grep으로 확정)
- Test: 신규 회귀 spec 1~2건 (createdAt UTC ISO contract)

## Sub-task 1b.1: TZ 인벤토리 테이블 작성 (P1-2 흡수)

- [ ] **Step 1: smoke today 소스 전체 grep + 테이블 작성**

```bash
grep -rn "getKstDateStr\|getDateStr\|toLocaleDateString\|toISOString.*split\|new Date()" tests/smoke/ | head -30
```

산출물 (T1b 진입 시점에 plan 옆에 작성):

| 위치 (file:line) | 현재 패턴 | 결정 (fix / carry) | 비고 |
|---|---|---|---|
| `tests/smoke/v3.13-missions.spec.ts:24-27` | `getKstDateStr(new Date())` (브라우저 시간) | fix → `'2026-05-10'` 고정 | smoke fixture |
| `tests/smoke/v3.23-stub-cleanup.spec.ts:35-39` | 동일 | fix | |
| `tests/smoke/missions-tab.spec.ts:24-29` | 동일 | fix | |
| `<추가 위치>` | ... | ... | T1b 진입 시점 grep으로 채움 |

- [ ] **Step 2: createdAt read/write 경로 grep + 테이블 작성**

```bash
# write side
grep -rn "createdAt:" src/ | head -30

# read side (display)
grep -rn "createdAt" src/ui/ | head -30
```

산출물:

| 종류 | 위치 (file:line) | 현재 패턴 | 결정 |
|---|---|---|---|
| read | `src/ui/modals/insight-detail.ts:34-37` | createdAt 표기 | 정책 점검 |
| read | `src/ui/handlers/archive.ts:358-360` | createdAt 표기 | 정책 점검 |
| read | `src/ui/handlers/archive.ts:490-495` | createdAt 표기 | 정책 점검 |
| write | `<grep으로 확정>` | UTC ISO vs KST anchor | UTC ISO 통일 |

## Sub-task 1b.2: smoke today 3 spot fix

- [ ] **Step 1: 각 spot fix (deterministic fixture)**

```typescript
// tests/smoke/v3.13-missions.spec.ts (예시 — 실제 line 24-27 위치)
// Before:
const today = await page.evaluate(() => {
  return new Date().toLocaleDateString('en-CA', { timeZone: 'Asia/Seoul' });
});

// After:
const today = '2026-05-10'; // fixed fixture for smoke determinism
// 또는 page.addInitScript로 Date 모킹 (smoke 환경 따라 결정)
```

- [ ] **Step 2: 자정 race 회귀 검증 N=3**

```bash
for i in 1 2 3; do
  rm -rf node_modules/.vite-temp
  npm run test:smoke -- --grep "missions" 2>&1 | tail -3
done
```

Expected: N=3 3/3 PASS, cold-fork 96/96.

## Sub-task 1b.3: createdAt UTC ISO 정책 closing

- [ ] **Step 1: 정책 명문화**

closure: `createdAt`은 항상 UTC ISO (`new Date().toISOString()`). KST 표기는 display 시점에 `KST_FMT_DATE.format(new Date(createdAt))` (T1a 도입 singleton 활용).

- [ ] **Step 2: write-side 위반 fix (인벤토리 결과)**

```typescript
// Before (KST anchor write — 위반):
createdAt: getKstDateStr(new Date()),

// After (UTC ISO write):
createdAt: new Date().toISOString(),
```

- [ ] **Step 3: read-side display 검증 (insight-detail / archive)**

`KST_FMT_DATE.format(new Date(createdAt))` 패턴 일관 적용 확인. 위반 시 fix.

- [ ] **Step 4: 회귀 spec 신규**

```typescript
// tests/unit/data/createdAt-utc.spec.ts (신설)
import { describe, it, expect } from 'vitest';
import { KST_FMT_DATE } from '../../../src/utils/intl';

describe('createdAt UTC ISO contract (v3.26 T1b)', () => {
  it('write-side stores UTC ISO with Z suffix', () => {
    const insight = { createdAt: new Date('2026-05-10T03:00:00Z').toISOString() };
    expect(insight.createdAt).toMatch(/Z$/);
  });

  it('read-side displays as KST date', () => {
    const utc = '2026-05-10T03:00:00Z'; // KST 12:00
    expect(KST_FMT_DATE.format(new Date(utc))).toBe('2026-05-10');
  });

  it('UTC midnight before KST midnight → next KST day', () => {
    const utc = '2026-05-10T15:00:00Z'; // KST 2026-05-11 00:00
    expect(KST_FMT_DATE.format(new Date(utc))).toBe('2026-05-11');
  });
});
```

## T1b 검증 + commit

- [ ] **T1b 표준 3종 + 양쪽 TZ + smoke**

```bash
npx tsc --noEmit
npm run lint
TZ=Asia/Seoul npm test -- --run
TZ=America/New_York npm test -- --run
npm run test:smoke 2>&1 | tail -5
```

Expected: TS 0 / lint 0err / vitest 양쪽 PASS (1175 + 신규 ~3 = 1178) / smoke 96/96.

- [ ] **T1b per-task review (reviewer-agent dispatch — Codex 사전 P1-1+P1-2 흡수)**

```text
Agent(subagent_type='pr-review-toolkit:code-reviewer', prompt='
Review v3.26 T1b (Smoke 자정 race fix + createdAt UTC closing):
- TZ 인벤토리 테이블 (smoke today 3 spot + createdAt read 5 경로)
- tests/smoke/v3.13-missions.spec.ts / v3.23-stub-cleanup.spec.ts / missions-tab.spec.ts: today 고정
- src/ui/modals/insight-detail.ts / src/ui/handlers/archive.ts: createdAt read 경로 검증
- write-side spot fix (있을 시)
- tests/unit/data/createdAt-utc.spec.ts (신설, 3 specs)

Focus:
1. 인벤토리 누락 — grep miss spot 있는가?
2. smoke fixture 고정이 다른 spec(date-dependent)에 회귀 유발?
3. createdAt UTC ISO contract 일관 — 위반 spot 0건?
4. KST_FMT_DATE.format(new Date(iso)) 패턴 정합성

Return P0/P1/P2.
')
```

- [ ] **T1b commit**

```bash
git add tests/smoke/*.spec.ts src/ui/modals/insight-detail.ts src/ui/handlers/archive.ts tests/unit/data/createdAt-utc.spec.ts <기타 write-side files>
git commit -m "$(cat <<'EOF'
fix(v3.26 T1b): Smoke 자정 race fix + createdAt UTC ISO closing (v3.22 자정 race + v3.23 T3/T8)

- TZ 인벤토리 테이블 (smoke today 3 spot + createdAt read 5 경로) — Codex 사전 P1-2
- smoke today 고정: v3.13-missions / v3.23-stub-cleanup / missions-tab (deterministic fixture '2026-05-10')
- createdAt UTC ISO contract: write side new Date().toISOString() / read side KST_FMT_DATE.format
- vitest +3 (createdAt-utc-contract)
- smoke 96/96 cold-fork PASS (N=3 3/3)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

# Task 2: interestId hardening

> **plan v2 (Codex 사전 P1-3 흡수)**: `isValidUserShape`는 pure validator 유지(`src/state/user.ts:143-202` 기존 invariant 보존). mutation은 별도 `normalizeInsightInterestIds` 함수로 분리하고 migration/load path에서 호출.

**Files:**
- Modify: `src/state/user.ts` (신규 export `normalizeInsightInterestIds` — pure validator는 변경 0)
- Modify: `src/state/migration.ts` (v6→v7 migrate path 마지막에 `normalizeInsightInterestIds` 호출 wiring)
- Modify: `src/state/user.ts` 또는 caller (load path — `getCachedUser` 직후 normalize 1회)
- Test: `tests/unit/state/insight-interest-id-hardening.spec.ts` (신설)

## Steps

- [ ] **Step 1: 현재 `isValidUserShape` 확인 (pure validator 유지 의무)**

```bash
grep -n "isValidUserShape\|interestId\|export function" src/state/user.ts | head -30
```

기존 `isValidUserShape` (line 143-202) — pure validator. **mutation 추가 ❌** (Codex P1-3).

- [ ] **Step 2: 신규 spec `tests/unit/state/insight-interest-id-hardening.spec.ts`**

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { normalizeInsightInterestIds, isValidUserShape } from '../../../src/state/user';

describe('normalizeInsightInterestIds (v3.26 T2 — pure normalizer, P1-3 흡수)', () => {
  beforeEach(() => {
    vi.spyOn(console, 'warn').mockImplementation(() => {});
  });

  it('preserves known interestId (recruiting) untouched', () => {
    const insights = [{ id: 'i1', text: 'foo', createdAt: 'x', interestId: 'recruiting' }];
    normalizeInsightInterestIds(insights);
    expect(insights[0].interestId).toBe('recruiting');
    expect(console.warn).not.toHaveBeenCalled();
  });

  it('normalizes invalid interestId to "unknown" + warns once', () => {
    const insights = [{ id: 'i1', text: 'foo', createdAt: 'x', interestId: '<script>' }];
    normalizeInsightInterestIds(insights);
    expect(insights[0].interestId).toBe('unknown');
    expect(console.warn).toHaveBeenCalledTimes(1);
    expect(console.warn).toHaveBeenCalledWith(expect.stringContaining('interestId'));
  });

  it('handles empty string interestId → "unknown"', () => {
    const insights = [{ id: 'i1', text: 'foo', createdAt: 'x', interestId: '' }];
    normalizeInsightInterestIds(insights);
    expect(insights[0].interestId).toBe('unknown');
  });

  it('handles null/non-string interestId → "unknown"', () => {
    const insights = [{ id: 'i1', text: 'foo', createdAt: 'x', interestId: null as any }];
    normalizeInsightInterestIds(insights);
    expect(insights[0].interestId).toBe('unknown');
  });

  it('isValidUserShape remains pure (no mutation) — P1-3 invariant', () => {
    // v7 shape with invalid interestId: validator should NOT touch it
    const user = makeV7User([{ id: 'i1', text: 'foo', createdAt: 'x', interestId: '<bad>' }]);
    const before = user.insights[0].interestId;
    isValidUserShape(user);
    expect(user.insights[0].interestId).toBe(before); // 변화 없음 (pure validator)
  });
});

function makeV7User(insights: any[]): any {
  return {
    schemaVersion: 7,
    name: 'Test',
    insights,
    // 다른 v7 required fields...
  };
}
```

- [ ] **Step 3: 신규 spec 실행 (실패 확인)**

```bash
npm test -- --run tests/unit/state/insight-interest-id-hardening.spec.ts
```

Expected: 5 fail (`normalizeInsightInterestIds` 미존재).

- [ ] **Step 4: `normalizeInsightInterestIds` 신규 export (`src/state/user.ts`)**

```typescript
// src/state/user.ts (신규 export, isValidUserShape 변경 ❌)

/**
 * v3.26 T2 (P1-3): Insight[] interestId in-place normalize.
 * - 비-string / non-whitelist id → 'unknown' (silent corruption guard, v3.25 lesson #6)
 * - 가시화: console.warn 1줄 / 항목당
 *
 * `isValidUserShape`는 pure validator 유지 — mutation 분리 (Codex 사전 P1-3).
 * caller: migration v6→v7 마지막 step + load path (getCachedUser 직후 1회).
 */
export function normalizeInsightInterestIds(insights: Array<{ id: string; interestId: unknown }>): void {
  for (const i of insights) {
    const id = typeof i.interestId === 'string' ? i.interestId : '';
    if (!isWhitelistedInterestId(id)) {
      console.warn(
        `[v3.26 T2] invalid interestId "${i.interestId}" → normalized to "unknown" (insight=${i.id})`
      );
      i.interestId = 'unknown';
    }
  }
}

function isWhitelistedInterestId(id: string): boolean {
  // 기존 validateInterestId 로직 활용: validateInterestId(id) === id 일 때만 whitelist
  return validateInterestId(id) === id;
}
```

> **Note**: `isValidUserShape` 본문은 **0줄 변경**. mutation 분리가 P1-3 핵심.

- [ ] **Step 4b: caller wiring — migration v6→v7 + load path**

```typescript
// src/state/migration.ts (v6→v7 migrate 마지막)
import { normalizeInsightInterestIds } from './user';

// migrateToV7 함수 안 마지막 (insights 채워진 직후):
if (Array.isArray(out.insights)) {
  normalizeInsightInterestIds(out.insights);
}
```

```typescript
// src/state/user.ts (load path — getCachedUser 또는 readPersistedUser 직후 1회)
// 위치: getCachedUser 함수 안, isValidUserShape 통과 직후
if (user && Array.isArray(user.insights)) {
  normalizeInsightInterestIds(user.insights);
}
```

- [ ] **Step 5: 신규 spec 실행 (PASS 확인)**

```bash
npm test -- --run tests/unit/state/insight-interest-id-hardening.spec.ts
```

Expected: 5/5 PASS.

- [ ] **Step 6: 회귀 검증 (기존 migration-v6 spec 영향 0)**

```bash
TZ=Asia/Seoul npm test -- --run tests/unit/state/migration-v6.spec.ts tests/unit/migration-v7.spec.ts
TZ=America/New_York npm test -- --run tests/unit/state/migration-v6.spec.ts tests/unit/migration-v7.spec.ts
```

Expected: 양쪽 TZ 기존 PASS 유지.

- [ ] **Step 7: 표준 3종**

```bash
npx tsc --noEmit
npm run lint
TZ=Asia/Seoul npm test -- --run
```

Expected: TS 0 / lint 0err / vitest 1175~1180.

- [ ] **Step 8: T2 per-task review (reviewer agent dispatch — large change in data shape guard)**

```text
Agent(subagent_type='pr-review-toolkit:code-reviewer', prompt='
Review v3.26 T2 changes (Codex 사전 P1-3 흡수):
- src/state/user.ts: normalizeInsightInterestIds 신규 export (pure normalizer)
- src/state/user.ts: isValidUserShape **본문 0줄 변경** (pure validator invariant 유지)
- src/state/migration.ts: v6→v7 migrate 마지막에 normalizeInsightInterestIds 호출 wiring
- src/state/user.ts: load path (getCachedUser 직후) wiring
- tests/unit/state/insight-interest-id-hardening.spec.ts (신설, 5 specs 포함 validator pure invariant spec)

Focus:
1. **isValidUserShape pure invariant** — 본문 0줄 변경 검증 (Codex P1-3 핵심)
2. silent corruption guard 패턴 (v3.25 lesson #6 graduated)
3. console.warn dedup — 동일 user 다회 호출 시 spam 가능성
4. caller wiring — migration + load path 둘 다 호출되어 double-normalize 안전?
5. 기존 caller 회귀 — getCachedUser path 정합

Return P0/P1/P2 분류.
')
```

- [ ] **Step 9: T2 commit**

```bash
git add src/state/user.ts src/state/migration.ts tests/unit/state/insight-interest-id-hardening.spec.ts
git commit -m "$(cat <<'EOF'
fix(v3.26 T2): interestId hardening — normalizeInsightInterestIds 분리 (Codex 사전 P1-3, v3.25 P2-1)

- src/state/user.ts: normalizeInsightInterestIds 신규 export (pure normalizer)
- src/state/user.ts: isValidUserShape 본문 **0줄 변경** (pure validator invariant 유지)
- src/state/migration.ts: v6→v7 migrate 마지막에 호출 wiring
- src/state/user.ts: load path (getCachedUser 직후) wiring
- silent corruption guard (v3.25 lesson #6): console.warn 1줄 가시화
- 신규 spec 5건: known/invalid/empty/null + isValidUserShape pure invariant

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

# Task 3: openModal contract 확장 (`bodyNode` discriminated union)

**Files:**
- Modify: `src/ui/modals/shared.ts` (`ModalConfig` xor union + `openModal` body 처리)
- Modify: `src/ui/modals/stats-range-modal.ts` (`bodyNode` 채택, innerHTML 직렬화 단계 제거)
- Test: `tests/unit/ui/modal-shared-contract.spec.ts` (신설)

## Steps

- [ ] **Step 1: 신규 spec `tests/unit/ui/modal-shared-contract.spec.ts`**

```typescript
import { describe, it, expect, beforeEach } from 'vitest';
import { openModal, closeModal } from '../../../src/ui/modals/shared';

describe('openModal discriminated union (v3.26 T3)', () => {
  beforeEach(() => {
    document.body.innerHTML = '<div id="modalRoot"></div>';
  });

  it('accepts bodyHtml (legacy path, escaped HTML string)', () => {
    const modal = openModal({ title: 'T', bodyHtml: '<p>safe</p>' });
    expect(modal.querySelector('.dg-modal-body')?.innerHTML).toBe('<p>safe</p>');
    closeModal();
  });

  it('accepts bodyNode (DOM Node, no innerHTML serialization)', () => {
    const node = document.createElement('div');
    node.textContent = 'nodey';
    const modal = openModal({ title: 'T', bodyNode: node });
    expect(modal.querySelector('.dg-modal-body')?.firstChild).toBe(node);
    expect(modal.querySelector('.dg-modal-body')?.textContent).toBe('nodey');
    closeModal();
  });

  it('escapes bodyHtml interpolations per caller contract (caller responsibility)', () => {
    // bodyHtml은 caller-escape 의무 — 본 spec은 contract 보존만 검증
    const modal = openModal({ title: 'T', bodyHtml: '<p>&lt;script&gt;</p>' });
    expect(modal.querySelector('.dg-modal-body')?.textContent).toContain('<script>');
    closeModal();
  });

  // Codex 사전 P2-1 흡수: TypeScript negative type assertions
  it('TS rejects "neither bodyHtml nor bodyNode" config (negative type test)', () => {
    // @ts-expect-error — discriminated union: 둘 중 하나 필수
    const cfgNeither: ModalConfig = { title: 'T' };
    expect(cfgNeither).toBeDefined(); // runtime 무관, ts type check만
  });

  it('TS rejects "both bodyHtml and bodyNode" config (negative type test)', () => {
    // @ts-expect-error — discriminated xor: 둘 다 지정 시 type error (bodyNode?: never 또는 bodyHtml?: never 위반)
    const cfgBoth: ModalConfig = { title: 'T', bodyHtml: '<p>x</p>', bodyNode: document.createElement('div') };
    expect(cfgBoth).toBeDefined();
  });
});
```

- [ ] **Step 2: 신규 spec 실행 (실패 확인)**

```bash
npm test -- --run tests/unit/ui/modal-shared-contract.spec.ts
```

Expected: bodyNode test fail (contract 미존재 → TS error 또는 런타임 에러).

- [ ] **Step 3: `ModalConfig` discriminated union으로 확장**

```typescript
// src/ui/modals/shared.ts
// Before:
export interface ModalConfig {
  title: string;
  bodyHtml: string;
  onClose?: () => void;
}

// After (v3.26 T3): xor discriminated union
type ModalBody =
  | { bodyHtml: string; bodyNode?: never }
  | { bodyNode: Node; bodyHtml?: never };

export type ModalConfig = {
  title: string;
  onClose?: () => void;
} & ModalBody;
```

- [ ] **Step 4: `openModal` body 처리 분기**

```typescript
// src/ui/modals/shared.ts (openModal 안)
const bodyEl = wrap.querySelector<HTMLDivElement>('.dg-modal-body');
if (bodyEl) {
  if ('bodyNode' in cfg && cfg.bodyNode) {
    // v3.26 T3: DOM Node path — innerHTML 직렬화 없음
    bodyEl.append(cfg.bodyNode);
  } else if ('bodyHtml' in cfg && cfg.bodyHtml !== undefined) {
    // eslint-disable-next-line no-restricted-syntax -- caller-supplied bodyHtml per ModalConfig contract; caller MUST escape interpolations
    bodyEl.innerHTML = cfg.bodyHtml;
  }
}
```

- [ ] **Step 5: 신규 spec 실행 (PASS 확인)**

```bash
npm test -- --run tests/unit/ui/modal-shared-contract.spec.ts
```

Expected: 3/3 PASS.

- [ ] **Step 6: 기존 caller 회귀 검증 (bodyHtml path 0 변경)**

```bash
grep -rn "openModal({" src/ | head -10
```

caller list 확인. 모두 `bodyHtml` 사용 중이면 0 변경 (하위 호환).

```bash
TZ=Asia/Seoul npm test -- --run tests/unit/ui/
```

Expected: 기존 modal-* spec 전체 PASS.

- [ ] **Step 7: stats-range-modal `bodyNode` 채택**

```bash
grep -n "innerHTML\|bodyHtml" src/ui/modals/stats-range-modal.ts
```

기존 패턴 (string concat → escapeHtml → innerHTML)을 DOM API로 마이그레이션:

```typescript
// Before:
const html = `<div class="stats-range">${escapeHtml(label)}<select>...</select></div>`;
openModal({ title: '범위 선택', bodyHtml: html });

// After (v3.26 T3):
const root = document.createElement('div');
root.className = 'stats-range';
const labelEl = document.createElement('span');
labelEl.textContent = label; // escapeHtml 불필요 (textContent 자동 escape)
root.append(labelEl);
const select = document.createElement('select');
// ... options append
root.append(select);
openModal({ title: '범위 선택', bodyNode: root });
```

- [ ] **Step 8: 양쪽 TZ + smoke 회귀**

```bash
TZ=Asia/Seoul npm test -- --run
TZ=America/New_York npm test -- --run
npm run test:smoke -- --grep "stats|range"
```

Expected: vitest 1180~1186 / smoke 영향 0 PASS.

- [ ] **Step 9: T3 per-task review (reviewer agent — contract 확장 large change)**

```text
Agent(subagent_type='pr-review-toolkit:code-reviewer', prompt='
Review v3.26 T3 changes:
- src/ui/modals/shared.ts: ModalConfig discriminated union {bodyHtml: string} | {bodyNode: Node} (xor)
- src/ui/modals/stats-range-modal.ts: bodyNode path 채택, innerHTML 직렬화 단계 제거
- tests/unit/ui/modal-shared-contract.spec.ts (신설, 3 specs)

Focus:
1. xor union이 TypeScript에서 정확히 enforce되는가? (둘 다 미지정 / 둘 다 지정 케이스)
2. 기존 caller (bodyHtml only) 0 변경 보장
3. bodyNode path에서 caller가 escapeHtml 호출 의무 사라지는가? (XSS round-trip 안전성)
4. focus trap 동작이 bodyNode path에서도 정상인가?

Return P0/P1/P2 분류.
')
```

- [ ] **Step 10: T3 commit**

```bash
git add src/ui/modals/shared.ts src/ui/modals/stats-range-modal.ts tests/unit/ui/modal-shared-contract.spec.ts
git commit -m "$(cat <<'EOF'
refactor(v3.26 T3): openModal contract 확장 — bodyHtml | bodyNode discriminated union (v3.24 P2-3)

- ModalConfig: {bodyHtml: string} | {bodyNode: Node} xor union (TypeScript enforce)
- openModal body 분기: bodyNode → DOM append (innerHTML 직렬화 없음) / bodyHtml → 기존 path 유지
- stats-range-modal: bodyNode 채택, escapeHtml 직렬화 단계 제거
- 신규 spec 3건 + 기존 caller 0 변경 보장 (bodyHtml-only)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

# Task 4: per-task UI/CSS polish batch (4 sub-item)

**Files:**
- Modify: `src/styles/components.css` (또는 적절 layer) — `.bar-label` rule + focus-visible 추가
- Modify: `src/ui/garden-grid.ts` (또는 위치) — aria-label에 `escapeHtml`
- Modify: `src/data/prompts.ts` — "이미 압축" 주석 verification (변경 0 가능)
- Test: 기존 spec 강화 + regex assertion 신규 1~2

## Sub-task 4.1: `.bar-label` CSS rule 추가 (v3.23 T5 minor)

- [ ] **Step 1: CSS regex assertion 신규 spec 또는 기존 spec 확장**

```typescript
// tests/unit/styles/bar-label.spec.ts (신설 또는 settings spec 확장)
import { readFileSync } from 'node:fs';
import { describe, it, expect } from 'vitest';

describe('.bar-label CSS rule (v3.23 T5 carry → v3.26 T4)', () => {
  it('defines .bar-label rule', () => {
    const css = readFileSync('src/styles/components.css', 'utf-8');
    expect(css).toMatch(/\.bar-label\s*\{[^}]*font-size/);
  });
});
```

- [ ] **Step 2: `.bar-label` CSS rule 추가**

```css
/* src/styles/components.css */
@layer components {
  .bar-label {
    font-size: var(--font-size-xs);
    color: var(--color-text-muted);
  }
}
```

- [ ] **Step 3: spec PASS 확인**

```bash
npm test -- --run tests/unit/styles/bar-label.spec.ts
```

## Sub-task 4.2: focus-visible 누락 보완 (v3.23 T9)

- [ ] **Step 1: 현재 focus-visible 사용처 grep**

```bash
grep -rn "focus-visible\|:focus\b" src/styles/ | head -20
```

- [ ] **Step 2: 누락 spot 발견 시 rule 추가** (v3.23 T9 carry 항목 — 구체 spot은 grep 결과 따라)

```css
/* 예시 — 실제 누락 spot 위치 따라 */
.some-button:focus-visible {
  outline: 2px solid var(--color-accent);
  outline-offset: 2px;
}
```

- [ ] **Step 3: 회귀 검증** — visible 변경 거의 없음 (focus-visible은 키보드 사용 시만 trigger)

## Sub-task 4.3: garden-grid aria-label `escapeHtml` (v3.24 T3 P2-5)

- [ ] **Step 1: garden-grid 현황 확인**

```bash
grep -n "aria-label" src/ui/components/garden-grid.ts src/ui/garden-grid.ts 2>/dev/null | head -10
```

- [ ] **Step 2: aria-label에 escapeHtml 적용**

```typescript
// Before:
button.setAttribute('aria-label', `${label} 단계 ${stage}`);

// After (v3.26 T4):
import { escapeHtml } from '../utils/escapeHtml';
button.setAttribute('aria-label', `${escapeHtml(label)} 단계 ${stage}`);
```

> **Note**: `setAttribute('aria-label', ...)`는 textContent와 동일하게 자동 escape되므로 escapeHtml 불필요할 수 있음. v3.24 T3 P2-5는 "pre-existing" minor라 격하 가능. 검증 후 결정.

- [ ] **Step 3: 회귀 spec 1건 (필요 시)**

```typescript
it('garden-grid aria-label escapes user-controlled label', () => {
  const grid = renderGarden({ '<script>': { stage: 1 } });
  const btn = grid.querySelector('button');
  expect(btn?.getAttribute('aria-label')).not.toContain('<script>');
});
```

## Sub-task 4.4: prompts.ts "이미 압축" verification (v3.24 T3 P2-4)

- [ ] **Step 1: 현재 prompts.ts 압축 상태 확인**

```bash
wc -l src/data/prompts.ts
grep -c "^\s*$" src/data/prompts.ts
```

- [ ] **Step 2: indent 최소화 verification — 변경 0 가능**

판단: prompts.ts 압축 자체 검증. 미달 시 추가 trim, 충분하면 0줄.

## T4 검증 + commit

- [ ] **T4 표준 3종**

```bash
npx tsc --noEmit
npm run lint
TZ=Asia/Seoul npm test -- --run
```

Expected: TS 0 / lint 0err / vitest 1182~1186.

- [ ] **T4 per-task review (controller-scope)**

자체 review:
- (a) visible 변경 0인가? (.bar-label은 미세 폴리시, focus-visible은 키보드만 영향)
- (b) garden-grid escapeHtml이 redundant인가? (setAttribute 자동 escape 검증)

- [ ] **T4 commit**

```bash
git add <changed files>
git commit -m "$(cat <<'EOF'
fix(v3.26 T4): UI/CSS polish batch — .bar-label rule + focus-visible + garden-grid aria escape (v3.23/v3.24 minor carry)

Sub-items:
- .bar-label CSS rule (v3.23 T5 carry)
- focus-visible 누락 보완 (v3.23 T9)
- garden-grid aria-label escapeHtml 적용 (v3.24 T3 P2-5)
- prompts.ts 압축 verification (v3.24 T3 P2-4, 0줄 변경 결과)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

# Task 5: per-task type/spec quality batch (≤6 sub-item)

**Files:**
- Modify: `tests/unit/state/migration-v6.spec.ts` (`as any` 정리)
- Modify: 코멘트 정리 (apiKey 주석 / cachedAt 진단)
- Test: byInterest null spec / mock cleanup / N1·N3

## Sub-task 5.1: `migration-v6.spec.ts` `as any` 정리

- [ ] **Step 1: 명시 타입 헬퍼 작성**

```typescript
// tests/unit/state/migration-v6.spec.ts 위쪽
type V6User = ReturnType<typeof migrateUserToV6>;
type V7User = ReturnType<typeof migrateUserToV7>;

function getV7Field<K extends keyof V7User>(u: unknown, key: K): V7User[K] {
  return (u as V7User)[key];
}
```

- [ ] **Step 2: `(out as any).<field>` 패턴 swap**

```typescript
// Before:
expect((out as any).plantStateByInterest?.recruiting?.stage).toBe(2);

// After:
expect(getV7Field(out, 'plantStateByInterest')?.recruiting?.stage).toBe(2);
// 또는 type-assert:
const v7 = out as V7User;
expect(v7.plantStateByInterest?.recruiting?.stage).toBe(2);
```

8 occurrences swap (line 62, 120, 122, 124, 130, 131 + 추가).

- [ ] **Step 3: spec PASS 확인**

```bash
npm test -- --run tests/unit/state/migration-v6.spec.ts
```

Expected: 기존 PASS 유지.

## Sub-task 5.2: apiKey 주석 정리 (v3.23 T2 carry)

- [ ] **Step 1: apiKey 관련 주석 grep**

```bash
grep -rn "apiKey" src/utils/apiKey.ts src/state/ | grep -E "^\s*//|^\s*\*" | head -10
```

- [ ] **Step 2: stale 주석 정리** — 맥락 따라 결정 (1~3줄 변경)

## Sub-task 5.3: cachedAt 진단 필드 명확화 (v3.23 T6 mid-pass P2-3)

- [ ] **Step 1: cachedAt 관련 주석 / debug 메시지 확인**

```bash
grep -rn "cachedAt" src/ | head -20
```

- [ ] **Step 2: 진단 의도 명확화 주석 추가 또는 debug 분기 정리**

## Sub-task 5.4: byInterest null explicit spec (v3.24 T2 P2)

- [ ] **Step 1: 신규 spec 추가**

```typescript
// tests/unit/data/byInterest-null.spec.ts (신설 또는 기존 spec 확장)
it('buildInterestBars returns null when byInterest is empty', () => {
  expect(buildInterestBars({})).toBeNull();
});

it('buildInterestBars returns null when byInterest only has unknown', () => {
  expect(buildInterestBars({ unknown: 5 })).toBeNull();
});
```

## Sub-task 5.5: mock cleanup (`daily: undefined` redundant)

- [ ] **Step 1: redundant mock 위치 grep**

```bash
grep -rn "daily: undefined" tests/ | head -10
```

- [ ] **Step 2: redundant 제거**

```typescript
// Before:
mockUser({ ...defaults, daily: undefined, ... });

// After:
mockUser({ ...defaults, ... });
```

## Sub-task 5.6: v3.23 T10 N1·N3 (smoke fixture helper / Slack rel 통일 등)

- [ ] **Step 1: v3.23 T10 spec 확인 후 N1·N3 항목 fix**

> **격하 가능성**: N1·N3 일부는 v3.24/v3.25에서 흡수됐을 수 있음. 잔존 분만 처리.

## T5 검증 + commit

- [ ] **T5 표준 3종**

```bash
npx tsc --noEmit
npm run lint
TZ=Asia/Seoul npm test -- --run
```

Expected: TS 0 / lint 0err·≤37warn (`as any` 정리로 warn 1~5건 감소 가능) / vitest 1184~1190.

- [ ] **T5 per-task review (controller-scope)**

자체 review:
- (a) `as any` 정리가 type 정확성 향상시키는가?
- (b) byInterest null spec이 정책 문서화 역할 하는가?
- (c) mock cleanup이 회귀 위험 없는가?

- [ ] **T5 commit**

```bash
git add <changed files>
git commit -m "$(cat <<'EOF'
test(v3.26 T5): type/spec quality batch — as any 정리 + 주석 정리 + null spec + mock cleanup (v3.23/v3.24 minor carry)

Sub-items:
- migration-v6.spec.ts as any → V7User type-assert (v3.23 T1)
- apiKey 주석 정리 (v3.23 T2)
- cachedAt 진단 필드 명확화 (v3.23 T6 mid P2-3)
- byInterest null explicit spec (v3.24 T2 P2)
- daily: undefined mock cleanup (v3.24 T2 P2)
- v3.23 T10 N1·N3 잔존분 (필요 시)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

# Task 6: COMMON_MISTAKES.md tracked 결정

**Files:**
- Modify: `.gitignore` (옵션 a 채택 시) 또는 file move (옵션 b)
- Modify: `CLAUDE.md` cross-link

## Steps

- [ ] **Step 1: 사용자 confirm 1회**

```text
COMMON_MISTAKES.md tracked 결정 옵션:
(a) .gitignore 예외 추가 — !.claude/COMMON_MISTAKES.md 한 줄 / controller agent auto-load 호환 + git audit 동시 만족 (Recommended)
(b) docs/.claude/COMMON_MISTAKES.md로 이전 — controller load path 변경 필요
(c) 현행 유지 + decision rationale 명문화

→ 사용자 선택?
```

- [ ] **Step 2: content audit (옵션 a/b 채택 시) — Codex 사전 P2-2 흡수**

```bash
# secrets / 명단 / API key / 이메일 / Slack URL 0줄 확인
grep -nE "API_KEY|password|secret|token|@datarize.ai|sukjoon|hooks.slack.com|xoxb-|xoxp-" .claude/COMMON_MISTAKES.md | head -10
```

Expected: 0 hits.

**Codex 사전 review 사전 audit (P2-2)**: `.claude/COMMON_MISTAKES.md:1-193` 전체 read 결과 API key/token/password/이메일/개인명/Slack credentials 0건 확인됨. "Slack webhook" 언급은 generic 표현 (URL/credentials 미포함). T6 commit 시 audit 결과 commit body에 명시.

- [ ] **Step 3: `.gitignore` 예외 추가 (옵션 a)**

```diff
# .gitignore
 .claude/
+!.claude/COMMON_MISTAKES.md
```

- [ ] **Step 4: CLAUDE.md cross-link 강화**

```markdown
## Core References
- **Common Mistakes**: `.claude/COMMON_MISTAKES.md` ⚠️ **MANDATORY** (git tracked v3.26 T6)
```

- [ ] **Step 5: T6 표준 3종**

```bash
npx tsc --noEmit  # 영향 0
npm run lint      # 영향 0
git status        # COMMON_MISTAKES.md 추적 확인
```

- [ ] **Step 6: T6 per-task review (controller-scope, meta change)**

자체 review:
- (a) content audit 통과? (secrets 0)
- (b) cross-machine consistency 보장?
- (c) CLAUDE.md cross-link 정확?

- [ ] **Step 7: T6 commit**

```bash
git add .gitignore .claude/COMMON_MISTAKES.md CLAUDE.md
git commit -m "$(cat <<'EOF'
docs(v3.26 T6): COMMON_MISTAKES.md tracked decision — .gitignore exception (옵션 a) (v3.24 P2-2)

- .gitignore 예외 !.claude/COMMON_MISTAKES.md 추가
- CLAUDE.md cross-link 강화 (git tracked 표시)
- content audit (Codex 사전 P2-2 흡수, 명시 기록):
  * API_KEY/password/secret/token/이메일/개인명/Slack credentials 0건
  * "Slack webhook" 언급은 generic (URL/credentials 미포함)
  * 검증 grep 패턴: API_KEY|password|secret|token|@datarize.ai|sukjoon|hooks.slack.com|xoxb-|xoxp-
- 효과: controller auto-load 호환 + git audit + cross-machine consistency 동시 만족

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

# Task 7: Bundle GREEN 복귀 (final trim)

> **plan v2 (Codex 사전 P1-4 흡수)**: trim 후보별 위험도 gate 도입. LOW 우선 처리 → MED는 caller 회귀 검증 → HIGH는 per-candidate 회귀 spec + reviewer signoff 의무.
>
> **위험도 분류 (Codex 실측)**:
> - **LOW** — chip HTML indent 압축 (`src/ui/tabs/insights.ts:42-51`, dynamic string escape 확인)
> - **MED** — `stats-stat-card` JSON config (`src/ui/modals/stats-range-modal.ts:31-54`, 파생 UI), `chat-bubble` legacy wrapper (`src/ui/chat-bubble.ts:68-84`, caller `src/ui/handlers/home.ts:1059-1072,1121-1136` 사용 중)
> - **HIGH** — parser dead path (`src/utils/gemini-parse.ts:51-66`, JSON parse 실패 시 의도적 fallthrough), migration silent drop (`src/state/migration.ts:357-371`, entry 삭제 가능성)

**Files:**
- LOW 우선: `src/ui/tabs/insights.ts` (chip HTML indent 압축)
- MED: `src/ui/modals/stats-range-modal.ts` / `src/ui/chat-bubble.ts` + callers
- HIGH (조건부): `src/utils/gemini-parse.ts` / `src/state/migration.ts`

> **gate**: T1a~T6 누적 영향 측정 후 진입. LOW만으로 ≤40713 도달 시 MED/HIGH 미실행 (YAGNI). LOW + MED로도 미달 시에만 HIGH 진입(per-candidate 회귀 spec + reviewer signoff 의무).

## Steps

- [ ] **Step 1: T6 시점 bundle 측정**

```bash
rm -rf dist
npm run build 2>&1 | tail -5
gzip -c dist/assets/index-*.js | wc -c
```

baseline 40980 → T6 시점 X B. 필요 trim = X - 40713.

- [ ] **Step 2: trim 후보 grep**

```bash
# (a) chip HTML indent 압축
grep -n "insight-chip\|filter-chip" src/ui/tabs/insights.ts src/ui/components/*.ts | head -10

# (b) parser dead path
grep -n "// dead\|// TODO|// XXX" src/utils/gemini-parse.ts | head -5

# (c) migration silent drop 단순화
grep -n "if (!.*) return\|silent drop" src/state/migration.ts | head -5

# (d) stats-stat-card JSON config table
ls src/ui/components/stats-stat-card.ts 2>/dev/null

# (e) chat-bubble legacy wrapper
grep -n "legacy\|wrapper" src/ui/chat-bubble.ts | head -5
```

- [ ] **Step 3: LOW 후보 우선 적용 (P1-4 gate)**

LOW (chip HTML indent) 먼저 시도 → bundle 측정 → 미달 시 MED → 미달 시 HIGH:

```typescript
// (a) LOW: chip HTML indent — 다중 줄 string template → single-line
// Before:
const html = `<div class="insight-chip">
  <span class="insight-chip__label">${escapeHtml(label)}</span>
</div>`;

// After:
const html = `<div class="insight-chip"><span class="insight-chip__label">${escapeHtml(label)}</span></div>`;
```

- [ ] **Step 3a: LOW 적용 후 bundle 측정**

```bash
rm -rf dist && npm run build 2>&1 | tail -3
gzip -c dist/assets/index-*.js | wc -c
```

≤40713 도달이면 **MED/HIGH 미실행 (YAGNI)** → Step 5로 직행.

- [ ] **Step 3b (조건부): MED 후보 — bundle 미달 시만**

```typescript
// (c) MED: stats-stat-card JSON config table화 — 파생 UI, caller 회귀 검증 의무
// 측정 후 net trim ROI 산출 → 시도 결정.

// (d) MED: chat-bubble legacy wrapper 제거 — caller 사용 중 (home.ts:1059-1072,1121-1136)
// caller migrate 후 wrapper 제거. caller 회귀 검증 의무.
```

MED 적용 시 caller 회귀 검증 + 표준 3종 양쪽 TZ.

- [ ] **Step 3c (조건부): HIGH 후보 — LOW+MED로도 미달 시만, per-candidate 회귀 spec + reviewer signoff 의무**

```typescript
// (b) HIGH: parser dead path — JSON parse 실패 시 의도적 fallthrough (gemini-parse.ts:51-66)
// 진짜 unreachable 검증 + 회귀 spec 1~2건 작성 후만 적용. reviewer agent signoff 필수.

// (e) HIGH: migration silent drop — entry 삭제 가능성 (migration.ts:357-371)
// silent corruption 위험 → 진짜 dead 검증 + 회귀 spec + reviewer signoff 필수.
```

HIGH 진입 시 **별도 micro-task로 분리** + 각 후보 per-candidate 회귀 spec 1~2건 + reviewer agent signoff 의무. signoff 없이 commit 금지.

> **R1+P1-4 정책**: HIGH가 silent corruption 유발 시 in-cycle 차단. v3.27+ carry로 격하 후 다른 trim 방안 모색.

- [ ] **Step 4: 매 trim 후 bundle 측정**

```bash
rm -rf dist && npm run build 2>&1 | tail -3
gzip -c dist/assets/index-*.js | wc -c
```

목표 ≤ 40713 도달까지 반복.

- [ ] **Step 5: 회귀 검증 (양쪽 TZ + smoke)**

```bash
TZ=Asia/Seoul npm test -- --run
TZ=America/New_York npm test -- --run
npm run test:smoke 2>&1 | tail -5
```

Expected: 양쪽 TZ vitest 1184~1190 PASS / smoke 96/96.

- [ ] **Step 6: T7 per-task review (reviewer agent — 다중 영역 변경)**

```text
Agent(subagent_type='pr-review-toolkit:code-reviewer', prompt='
Review v3.26 T7 changes (Bundle GREEN 복귀, baseline 40980 → ≤ 40713).

Diff 영역:
- <trim 적용 영역 list>

Focus:
1. silent corruption: dead path 제거 시 진짜 unreachable인가?
2. HTML indent 압축으로 escapeHtml round-trip 안전성 손상 없는가?
3. migration silent drop 단순화 시 user data 손실 위험은?
4. JSON config table화 시 type 정확성 보존?
5. legacy wrapper 제거 시 caller 회귀 0건 보장?

Return P0/P1/P2.
')
```

- [ ] **Step 7: 미달 시 P0 차단 + 추가 trim**

bundle ≤ 40713 미달 시 R1 위반 → 추가 trim micro-task 의무 (in-cycle, P0급).

- [ ] **Step 8: T7 commit**

```bash
git add <changed files>
git commit -m "$(cat <<'EOF'
perf(v3.26 T7): Bundle GREEN 복귀 — <baseline> → <final> B (Δ -<delta> B) (v3.24 P2-1 + v3.25 P2-2)

Trim 적용:
- <후보 1>
- <후보 2>
- ...

Measurement (CLI gzip canonical, v3.14.5 graduated):
- Before T1~T6 누적: <X> B
- After T7 trim: <Y> B (≤ 40713 GREEN)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

# Task 8: 사이클 끝 검증 + Codex 최종 review + retro

**Files:**
- Modify: `docs/superpowers/specs/2026-05-10-v3.26-cleanup-design.md` (§6 retro section)
- Modify: `/Users/hayden/.claude/projects/.../memory/project_daily_growth_v326.md` (신설)
- Modify: `/Users/hayden/.claude/projects/.../memory/MEMORY.md` (index entry 추가)

## Steps

- [ ] **Step 1: 3종 검증 양쪽 TZ + 빌드 + smoke**

```bash
npx tsc --noEmit
npm run lint
TZ=Asia/Seoul npm test -- --run 2>&1 | tail -5
TZ=America/New_York npm test -- --run 2>&1 | tail -5
rm -rf node_modules/.vite-temp dist
npm run build 2>&1 | tail -5
gzip -c dist/assets/index-*.js | wc -c  # ≤ 40713
npm run test:smoke 2>&1 | tail -5
```

Expected:
- TS 0 errors
- Lint 0 errors / ≤ 37 warnings
- Vitest 1182~1190 양쪽 TZ PASS
- Bundle ≤ 40713
- Smoke 96/96 (cold-fork)

- [ ] **Step 2: Codex 최종 review dispatch (deploy gate)**

```bash
codex review <T1~T7 commits diff or branch>
# fallback: Agent(subagent_type='pr-review-toolkit:code-reviewer', ...)
```

분류:
- `DEPLOY_APPROVED` / `APPROVED_WITH_NOTES` / `REJECT`
- P0=0 / P1=0 → deploy 가능
- P0/P1 발견 시 in-cycle hotfix 또는 carry 결정

- [ ] **Step 3: spec §6 retro 작성**

```markdown
## §6. Retro (T8, 2026-05-XX)

### 사이클 요약
| 항목 | 결과 |
|---|---|
| 종료 시점 main HEAD | <commit> |
| Commit 카운트 | <N> (T0~T7 + retro + per-task fix) |
| 사이클 길이 | <X>일 |
| TS strict | 0 errors |
| Lint | 0 errors / <N> warnings |
| Vitest TZ=Asia/Seoul | <N> / <N> pass |
| Vitest TZ=America/New_York | <N> / <N> pass |
| Smoke cold-fork (N=1) | 96 / 96 pass |
| Bundle CLI gzip | <N> B (baseline 40980 → -<delta>, GREEN) |
| Codex 사전 review | <분류> |
| Codex 최종 review | <분류> |
| Per-task review dispatch | reviewer N + controller M (v3.24 graduated 비대칭) |

### Commit 목록 / 핵심 변경 / 신규 lessons / v3.27+ carry
... (실측 후 채움)
```

- [ ] **Step 4: memory `project_daily_growth_v326.md` 작성**

```markdown
---
name: DG v3.26 Cleanup
description: v3.22~v3.25 누적 ~20건 carry 일괄 청산 + bundle GREEN 복귀
type: project
---

**✅ 배포 완료** (2026-05-XX, main HEAD `<sha>`, Firebase live: ...) — N task / M commits · vitest <N> 양쪽 TZ + 96 smoke · bundle <X> → <Y> (Δ -<delta>, GREEN). Codex 사전 <분류> / 최종 <분류>. ...
```

- [ ] **Step 5: MEMORY.md index entry 추가 (한 줄, ≤ 200자)**

```markdown
- [DG v3.26 Cleanup](project_daily_growth_v326.md) — **✅ 배포 완료 (2026-05-XX, ...)** — v3.22~v3.25 누적 ~20건 carry 일괄 청산. bundle <X>→<Y> (Δ -<delta>, GREEN). Codex 2-pass <분류>.
```

- [ ] **Step 6: T8 retro commit**

```bash
git add docs/superpowers/specs/2026-05-10-v3.26-cleanup-design.md
# memory file은 ~/.claude/projects/.../memory/ 별도 경로 (repo 외)
git commit -m "$(cat <<'EOF'
docs(v3.26 T8 retro): 사이클 끝 검증 + Codex 최종 <분류> + retro append

- 3종 양쪽 TZ + smoke fresh fork PASS
- Bundle <X> → <Y> (Δ -<delta>, GREEN 복귀)
- Codex 사전 <분류> / 최종 <분류>
- v3.22~v3.25 누적 ~20건 carry 일괄 청산
- v3.27+ carry seed: <신규 P2 / per-task carry>

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 7: 사용자 "배포해줘" 명시 대기**

본 사이클은 직배포 패턴 (origin remote 미등록). T8 retro commit 후 사용자 "배포해줘" 명시 시점에:

```bash
npm run build && firebase deploy --only hosting
```

URL: https://my-ai-assistant-904f3.web.app

---

# Self-Review (Plan)

> brainstorming-skill에서 spec self-review 1회 + writing-plans skill에서 plan self-review 1회. 본 section은 plan v1 자체 검증.

## 1. Spec coverage (plan v2 — Codex 사전 review 흡수 후)

| Spec section | Plan task | 상태 |
|---|---|---|
| §1.2 A v3.25 P2 (2) — interestId hardening | **T2 (P1-3 normalizer 분리)** | ✓ |
| §1.2 A v3.25 P2 (2) — bundle GREEN | **T7 (P1-4 위험도 gate)** | ✓ |
| §1.2 B v3.24 P2 (3) — bundle GREEN P2-1 | T7 | ✓ |
| §1.2 B v3.24 P2 (3) — COMMON_MISTAKES tracked | **T6 (P2-2 audit 명시)** | ✓ |
| §1.2 B v3.24 P2 (3) — openModal `bodyNode` | **T3 (P2-1 negative type test)** | ✓ |
| §1.2 C v3.24 per-task carry (5) — byInterest null spec | T5 | ✓ |
| §1.2 C — mock cleanup | T5 | ✓ |
| §1.2 C — HTML indent | T7 (LOW 후보) | ✓ |
| §1.2 C — prompts 압축 검증 | T4 | ✓ |
| §1.2 C — garden-grid aria escapeHtml | T4 | ✓ |
| §1.2 D v3.23 per-task minor (8) — as any | T5 | ✓ |
| §1.2 D — apiKey 주석 | T5 | ✓ |
| §1.2 D — 자정 race | **T1b (P1-1 split + P1-2 인벤토리)** | ✓ |
| §1.2 D — `.bar-label` CSS | T4 | ✓ |
| §1.2 D — cachedAt | T5 | ✓ |
| §1.2 D — createdAt UTC | **T1b (P1-2 인벤토리)** | ✓ |
| §1.2 D — focus-visible | T4 | ✓ |
| §1.2 D — N1·N3 | T5 | ✓ |
| §1.2 E v3.22 P2 TZ (3) — ESLint dates path | **T1a (격하 옵션 d)** | ✓ |
| §1.2 E — Smoke KST 자정 race | T1b (D와 통합) | ✓ |
| §1.2 E — Intl singleton 중앙화 | **T1a** | ✓ |
| §1.5 DoD | T8 | ✓ |
| §2.1 per-task review 비대칭 (T1a/T1b 둘 다 large 승격) | T1a~T7 each | ✓ |
| §2.2 Codex 2-pass | T0 + T8 | ✓ |
| §3 R1~R7 risk 완화 (R1 trim gate, R2 인벤토리, R3 normalizer 분리) | 각 task 분산 | ✓ |
| §4 v3.27+ carry seed | T8 | ✓ |

**전 항목 cover. 갭 0건. Codex 사전 review P1×4 + P2×3 모두 in-cycle 흡수.**

## 2. Placeholder scan

- T0 Step 2 commit message에 `<P0/P1/P2 카운트 또는 controller fallback>` placeholder — Codex 사전 review 결과 따라 채움 (운영 시점 결정).
- T7 Step 8 commit message `<baseline>` `<final>` placeholder — measurement 시점 채움.
- T8 retro section `<sha>` `<N>` `<delta>` placeholder — 검증 시점 채움.
- T5 Sub-task 5.6 "잔존분 (필요 시)" — 실제 격하 가능, 현재 검증 후 결정.

⇒ 모두 운영 시점 채워지는 동적 값. plan 자체에는 placeholder 없음 (정상).

## 3. Type consistency

- `KST_FMT_DATE` (T1): 단일 export, 3 caller import 일관 ✓
- `ModalConfig` (T3): xor union 정의, 모든 caller가 `bodyHtml` xor `bodyNode` 사용 ✓
- `validateInterestId` (T2): 기존 export 활용, 신규 `isWhitelistedInterestId` helper는 spec 안에서만 정의 (혹은 inline)

⇒ 일관성 OK.

---

# Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-05-10-v3.26-cleanup-plan.md`.**

**Two execution options:**

1. **Subagent-Driven (recommended)** — fresh subagent per task + two-stage review. v3.17 이후 graduated 패턴.
2. **Inline Execution** — 동일 세션에서 batch execution + checkpoint review.

**다음 단계**: T0 사전 Codex review dispatch → P0/P1 in-cycle 흡수 → T0 commit (spec + plan v1 + Codex review patch) → T1 진입.
