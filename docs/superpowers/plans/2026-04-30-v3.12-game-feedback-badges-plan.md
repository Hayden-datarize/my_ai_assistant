# DG v3.12 — Game Feedback + Badges Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v3.10에 깔린 XP/streak/level 데이터를 시각·UX에서 활성화 — 즉시 보상 4종 (XP float / level-up confetti / streak fire pulse / badge toast) + 18 뱃지 catalog + Tier 시스템 단일화.

**Architecture:** 단일 이벤트 진입점 = `recordDailyAnswer` / `toggleScrap` / `saveMemo` 3곳에 `prev=takeSnapshot → mutate → saveUser → curr=takeSnapshot → runSweep` 패턴. `runSweep` → `detectEvents(prev,curr)` → 영구 unlock 저장 + 4종 CustomEvent dispatch. UI layer (`rewards.ts`)가 큐잉/모션/모달 담당.

**Tech Stack:** TypeScript (strict) · Vite · Vitest jsdom · Playwright smoke · Vanilla DOM · CSS-only confetti (canvas-confetti **미도입**).

**Spec:** `docs/superpowers/specs/2026-04-29-v3.12-game-feedback-badges-design.md` (Status: Approved)

---

## §0 Existing Landscape (touched files)

> 본 사이클이 건드리는 기존 코드 지도. Plan 작성 전 grep 검증 완료. (Lesson: COMMON_MISTAKES §2)

### State layer (`src/state/`)

- **`schema.ts`** — `Answer` / `UserSettings` / `ArchiveEntry` 스키마. **`User` 인터페이스는 여기 없음** (역사적 이유로 `user.ts`에 직접 정의).
- **`user.ts:4-12`** — `interface User { name, interests, onboardedAt, streak, lastActiveDate, xp, level }`. v1 shape.
- **`user.ts:50-67`** — `recordDailyAnswer(xpDelta)` — 현재 `u.level = 1 + Math.floor(u.xp / 100)` 줄을 **본 plan T2에서 제거** (level 필드 drop).
- **`user.ts:36-38`** — `saveUser(u)` — `localStorage.setItem(KEY, ...)`. throws on QuotaExceededError.
- **`user.ts:89-94`** — `getSaveErrorMessage(err)` — v3.7 에러 UX 헬퍼 (Quota 분기).
- **`migration.ts`** — `migrateAnswer` / `migrateUserSettings` / `migrateUnknown`만 존재. **User 마이그레이션 함수는 없음** → T2에서 신규 추가.
- **`briefings.ts:49-59`** — `mutate(index, fn)` 패턴 + `toggleScrap` / `saveMemo`. T4에서 sweep wiring.
- **localStorage 키**: `'user'` (User), `'briefings'` (Briefing[]). **본 사이클 키 변경 0건**.

### UI layer (`src/ui/`)

- **`events.ts:14-37`** — `EventMap` typed CustomEvent. T6에서 4개 reward event 추가.
- **`events.ts:41-52`** — `dispatch<K>(name, detail)` / `on<K>(name, handler)` typed 헬퍼. T3 `emitEvents`가 사용.
- **`modals/shared.ts:22-72`** — `openModal({title, bodyHtml, onClose?})`. T10/T13 모달 재사용.
- **`handlers/stats.ts:74-81`** — `levels[]` 하드코딩 배열. T1 후 `import { TIERS } from '../../state/leveling'`로 교체 (T11에서 처리).
- **`handlers/stats.ts:243-269`** — `hydrateBadges()` 하드코딩 6 뱃지 (`if user.level >= 3` 등). T11에서 catalog 기반으로 교체.
- **`handlers/stats.ts:310`** — `growthSummary`가 `user.level`을 직접 읽음. T2/T11에서 `getCurrentTier(user.xp).id`로 교체.
- **`handlers/home.ts:73`** — `recordDailyAnswer(10)` 호출 site. T4 sweep은 함수 안에서 처리하므로 caller 변경 0줄.
- **`handlers/home.ts:445`** / **`handlers/archive.ts:250,264`** — `toggleScrap(idx)` 호출 site. T4 sweep은 함수 안에서 처리하므로 caller 변경 0줄.
- **`modals/memo.ts:44`** — `saveMemo(index, value)` 호출 site. T4 sweep은 함수 안에서 처리하므로 caller 변경 0줄.
- **`utils/toast.ts`** — `showToast(message, durationMs?)` + `showUndoToast({...})`. T6의 reward 토스트는 별도 클래스 modifier (`.toast--levelup` 등) — 기존 toast 인프라 **재사용 안 함**, rewards.ts에서 자체 컨테이너 + 큐잉.

### CSS

- **`styles/main.css`** — 단일 CSS. 게임화 관련 신규 클래스: `.toast--levelup` / `.toast--streak` / `.toast--badge` / `.xp-float` / `.fire-pulse` / `.badge-pulse` / `.confetti-particle` / `.badge--earned` / `.badge--locked` / `.badge-lock` (T7~T14).

---

## Phase 0 — Foundation

### Task 1: leveling.ts — Tier 단일화

**Files:**
- Create: `src/state/leveling.ts`
- Test: `tests/unit/state/leveling.spec.ts`

- [ ] **Step 1: Write the failing test**

```ts
// tests/unit/state/leveling.spec.ts
import { describe, it, expect } from 'vitest';
import { TIERS, getCurrentTier, getNextTier, didLevelUp } from '../../../src/state/leveling';

describe('leveling', () => {
  it('TIERS는 6개 tier (id 1..6)', () => {
    expect(TIERS).toHaveLength(6);
    expect(TIERS.map(t => t.id)).toEqual([1, 2, 3, 4, 5, 6]);
  });

  it('getCurrentTier(0) → 새싹 (id 1)', () => {
    expect(getCurrentTier(0).id).toBe(1);
    expect(getCurrentTier(0).name).toBe('새싹');
  });

  it('getCurrentTier(99) → 새싹 (boundary 미만)', () => {
    expect(getCurrentTier(99).id).toBe(1);
  });

  it('getCurrentTier(100) → 새잎 (boundary 정확)', () => {
    expect(getCurrentTier(100).id).toBe(2);
  });

  it('getCurrentTier(2500) → 하늘 (MAX 초과 시 MAX 유지)', () => {
    expect(getCurrentTier(2500).id).toBe(6);
  });

  it('getNextTier(0) → 새잎 (다음 tier)', () => {
    expect(getNextTier(0)?.id).toBe(2);
  });

  it('getNextTier(2500) → null (MAX는 다음 없음)', () => {
    expect(getNextTier(2500)).toBeNull();
  });

  it('didLevelUp(99, 100) → 새잎 (boundary 통과)', () => {
    expect(didLevelUp(99, 100)?.id).toBe(2);
  });

  it('didLevelUp(100, 200) → null (같은 tier 내 이동)', () => {
    expect(didLevelUp(100, 200)).toBeNull();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
npm test -- tests/unit/state/leveling.spec.ts
```

Expected: FAIL (`Cannot find module '../../../src/state/leveling'`)

- [ ] **Step 3: Implement leveling.ts**

```ts
// src/state/leveling.ts
export interface Tier {
  id: number;        // 1..6
  icon: string;
  name: string;
  thresh: number;    // unlock xp
}

export const TIERS: readonly Tier[] = [
  { id: 1, icon: '🌱', name: '새싹', thresh: 0 },
  { id: 2, icon: '🌿', name: '새잎', thresh: 100 },
  { id: 3, icon: '🌳', name: '나무', thresh: 300 },
  { id: 4, icon: '🌲', name: '숲',   thresh: 600 },
  { id: 5, icon: '🏔️', name: '산',   thresh: 1000 },
  { id: 6, icon: '🌌', name: '하늘', thresh: 2000 },
] as const;

export function getCurrentTier(xp: number): Tier {
  // TIERS는 thresh asc 정렬. xp >= thresh 만족하는 가장 높은 tier 반환.
  let result: Tier = TIERS[0]!;
  for (const t of TIERS) {
    if (xp >= t.thresh) result = t;
    else break;
  }
  return result;
}

export function getNextTier(xp: number): Tier | null {
  return TIERS.find(t => t.thresh > xp) ?? null;
}

export function didLevelUp(prevXp: number, currXp: number): Tier | null {
  const prev = getCurrentTier(prevXp);
  const curr = getCurrentTier(currXp);
  return curr.id !== prev.id ? curr : null;
}
```

- [ ] **Step 4: Run test to verify all pass**

```bash
npm test -- tests/unit/state/leveling.spec.ts
```

Expected: PASS (9 tests)

- [ ] **Step 5: Commit**

```bash
git add src/state/leveling.ts tests/unit/state/leveling.spec.ts
git commit -m "$(cat <<'EOF'
feat(v3.12 T1): add leveling.ts with TIERS + getCurrentTier/getNextTier/didLevelUp

Single source of truth for the 6-tier ladder (새싹/새잎/나무/숲/산/하늘).
Replaces hardcoded levels[] in handlers/stats.ts (T11 will swap call site).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: User schema v2 + lazy migration

**Files:**
- Modify: `src/state/user.ts:4-12,50-67` (User 인터페이스 + `recordDailyAnswer` level 줄 제거)
- Modify: `src/state/migration.ts` (append `migrateUserToV2`)
- Modify: `src/state/user.ts:28-30` (`loadUserData` lazy migrate)
- Test: `tests/unit/state/user-migration.spec.ts`

> **중요**: backfill (이미 자격 있는 뱃지 unlock)은 **T13**에서 처리. 본 task는 schema bump + 빈 객체만.

- [ ] **Step 1: Write the failing test**

```ts
// tests/unit/state/user-migration.spec.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { loadUserData, saveUser, type User } from '../../../src/state/user';
import { migrateUserToV2 } from '../../../src/state/migration';

beforeEach(() => localStorage.clear());

describe('user schema v2 migration', () => {
  it('migrateUserToV2: v1 → v2 (level 제거, 신규 필드 채움)', () => {
    const v1 = { name: '하든', interests: ['AI'], onboardedAt: '2026-01-01', streak: 5, lastActiveDate: '2026-04-29', xp: 250, level: 3 };
    const v2 = migrateUserToV2(v1);
    expect(v2.schemaVersion).toBe(2);
    expect((v2 as any).level).toBeUndefined();
    expect(v2.earnedBadges).toEqual({});
    expect(v2.gamificationMigrated).toBe(false);
    expect(v2.streak).toBe(5);
    expect(v2.xp).toBe(250);
    expect(v2.interests).toEqual(['AI']);
  });

  it('loadUserData: schemaVersion 없으면 lazy migrate + saveUser 1회', () => {
    localStorage.setItem('user', JSON.stringify({ name: '하든', interests: [], onboardedAt: '2026-01-01', streak: 0, lastActiveDate: '', xp: 0, level: 1 }));
    const u = loadUserData()!;
    expect(u.schemaVersion).toBe(2);
    expect((u as any).level).toBeUndefined();
    expect(u.earnedBadges).toEqual({});
    // localStorage에 저장됐는지 확인
    const raw = JSON.parse(localStorage.getItem('user')!);
    expect(raw.schemaVersion).toBe(2);
  });

  it('loadUserData: schemaVersion 2 idempotent (재호출해도 동일)', () => {
    saveUser({ name: '하든', interests: [], onboardedAt: '2026-01-01', streak: 0, lastActiveDate: '', xp: 0, earnedBadges: {}, gamificationMigrated: false, schemaVersion: 2 });
    const a = loadUserData()!;
    const b = loadUserData()!;
    expect(a).toEqual(b);
  });

  it('recordDailyAnswer: u.level 필드 갱신 안 함 (제거됨)', async () => {
    saveUser({ name: '하든', interests: [], onboardedAt: '2026-01-01', streak: 0, lastActiveDate: '', xp: 99, earnedBadges: {}, gamificationMigrated: false, schemaVersion: 2 });
    const { recordDailyAnswer } = await import('../../../src/state/user');
    recordDailyAnswer(10);
    const raw = JSON.parse(localStorage.getItem('user')!);
    expect(raw.xp).toBe(109);
    expect(raw.level).toBeUndefined();
  });

  it('migrateUserToV2: earnedBadges 이미 있으면 보존 (idempotent guard)', () => {
    const partial = { name: 'x', interests: [], onboardedAt: '', streak: 0, lastActiveDate: '', xp: 0, earnedBadges: { 'streak-3': 1700000000000 } };
    const v2 = migrateUserToV2(partial);
    expect(v2.earnedBadges).toEqual({ 'streak-3': 1700000000000 });
  });

  it('migrateUserToV2: gamificationMigrated 이미 true면 보존', () => {
    const partial = { name: 'x', interests: [], onboardedAt: '', streak: 0, lastActiveDate: '', xp: 0, gamificationMigrated: true };
    const v2 = migrateUserToV2(partial);
    expect(v2.gamificationMigrated).toBe(true);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
npm test -- tests/unit/state/user-migration.spec.ts
```

Expected: FAIL (`migrateUserToV2 not exported`)

- [ ] **Step 3: Update User interface + add migrateUserToV2**

Update `src/state/user.ts:4-12`:

```ts
// src/state/user.ts (top)
export interface User {
  name: string;
  interests: string[];
  onboardedAt: string;
  streak: number;
  lastActiveDate: string;
  xp: number;
  // ❌ removed: level (computed via getCurrentTier(xp).id)
  earnedBadges: Record<string, number>;     // badgeId → unlockedAt epoch ms
  gamificationMigrated: boolean;            // 환영 모달 1회 보장 flag
  schemaVersion: 2;
}
```

Append to `src/state/migration.ts`:

```ts
// src/state/migration.ts (append)
import type { User } from './user';

/**
 * v3.12: User v1 → v2 마이그레이션.
 * - drop `level` (computed via getCurrentTier(xp).id)
 * - add earnedBadges / gamificationMigrated / schemaVersion: 2
 *
 * backfill (이미 자격 있는 뱃지 unlock)은 T13 환영 모달에서 처리.
 */
export function migrateUserToV2(raw: any): User {
  const v2: any = { ...raw, schemaVersion: 2 };
  delete v2.level;
  v2.earnedBadges = v2.earnedBadges ?? {};
  v2.gamificationMigrated = v2.gamificationMigrated ?? false;
  return v2 as User;
}
```

Update `src/state/user.ts:28-30,50-67`:

```ts
// src/state/user.ts (replace getCachedUser + loadUserData + recordDailyAnswer)
import { migrateUserToV2 } from './migration';

export function getCachedUser(): User | null {
  try {
    const raw = localStorage.getItem(KEY);
    if (!raw) return null;
    const parsed = JSON.parse(raw);
    if (parsed && parsed.schemaVersion === 2) return parsed as User;
    // lazy migrate v1 → v2 (or partial shape)
    const migrated = migrateUserToV2(parsed);
    localStorage.setItem(KEY, JSON.stringify(migrated));
    return migrated;
  } catch {
    return null;
  }
}

export function loadUserData(): User | null {
  return getCachedUser();
}

// ... saveUser unchanged ...

export function recordDailyAnswer(xpDelta: number): void {
  const u = loadUserData();
  if (!u) return;
  const today = getDateStr();

  if (u.lastActiveDate !== today) {
    const y = new Date(today);
    y.setDate(y.getDate() - 1);
    const yesterday = getDateStr(y);
    u.streak = u.lastActiveDate === yesterday ? u.streak + 1 : 1;
  }

  u.xp += xpDelta;
  // ❌ removed: u.level = 1 + Math.floor(u.xp / 100);
  u.lastActiveDate = today;

  saveUser(u);
  // sweep call은 T4에서 추가
}
```

- [ ] **Step 4: Run all unit tests to catch regression**

```bash
npm test -- tests/unit/state/user-migration.spec.ts
npm test -- tests/unit/state/user.spec.ts  # 기존 user 테스트 (있을 경우)
```

Expected: 신규 6 PASS. 기존 spec에서 `user.level` 직접 검증하던 부분이 있다면 확인 (없으면 OK).

- [ ] **Step 5: Run typecheck — `user.level` 사용처 노출**

```bash
npx tsc --noEmit
```

Expected: 2건 type error 노출 — `src/ui/handlers/stats.ts:254-255,310` (`user.level` 참조). 이는 **T11에서 수정**. 본 step에서는 cast로 임시 회피하거나 build 차단을 피하려면 stats.ts에 임시 패치만 하고 T11에서 정식 정리.

**임시 패치** (stats.ts 254/255/310 — 다음 줄로 교체, T11에서 다시 덮어씀):

```ts
// stats.ts:254 임시 — `level` 대신 inline tier 계산
if (user && user.xp >= 300) earned.push({ icon: '🌳', label: '나무' });
// stats.ts:255 임시
if (user && user.xp >= 1000) earned.push({ icon: '🏔️', label: '산' });
// stats.ts:310 임시 — `level` 대신 tier name
import { getCurrentTier } from '../../state/leveling';
const tierName = getCurrentTier(user.xp).name;
el.textContent = `최근 ${recent}개의 기록으로 ${tierName} 단계까지 도달했어요. 오늘도 한 걸음 더 나아가 볼까요?`;
```

```bash
npx tsc --noEmit && npm test -- tests/unit/
```

Expected: tsc PASS, vitest PASS.

- [ ] **Step 6: Commit**

```bash
git add src/state/user.ts src/state/migration.ts src/ui/handlers/stats.ts tests/unit/state/user-migration.spec.ts
git commit -m "$(cat <<'EOF'
feat(v3.12 T2): User schema v2 (drop level, add earnedBadges/schemaVersion)

Lazy migration in loadUserData. recordDailyAnswer no longer maintains
the redundant level field — getCurrentTier(xp) becomes the single source.

Backfill of already-eligible badges deferred to T13 (welcome modal).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: gameTypes.ts + achievements.ts (xp/level/streak arm)

**Files:**
- Create: `src/state/gameTypes.ts`
- Create: `src/state/achievements.ts`
- Modify: `src/ui/events.ts:14-37` (append 4 reward events to EventMap)
- Test: `tests/unit/state/achievements.spec.ts`

> 본 task에서는 xp-gained / level-up / streak-milestone arm만. **badge arm은 T5에서 활성화**. saveUser 실패 시 sweep 차단 검증 포함 (Critical Review C1).

- [ ] **Step 1: Add 4 reward events to EventMap**

Update `src/ui/events.ts:14-37`:

```ts
export interface EventMap {
  // ... existing entries unchanged ...
  // v3.12 reward events
  'dg:reward:xp-float': { amount: number; at: number };
  'dg:reward:level-up': { tierId: number; at: number };
  'dg:reward:streak-milestone': { days: number; at: number };
  'dg:reward:badge-unlock': { badgeId: string; at: number };
}
```

Append to `EVENT_NAMES` array (line ~54):

```ts
export const EVENT_NAMES: readonly EventName[] = [
  // ... existing ...
  'dg:reward:xp-float',
  'dg:reward:level-up',
  'dg:reward:streak-milestone',
  'dg:reward:badge-unlock',
] as const;
```

- [ ] **Step 2: Write the failing test**

```ts
// tests/unit/state/achievements.spec.ts
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { takeSnapshot, detectEvents, runSweep, persistUnlocks } from '../../../src/state/achievements';
import type { Snapshot, GameEvent } from '../../../src/state/gameTypes';
import { saveUser, loadUserData } from '../../../src/state/user';

beforeEach(() => localStorage.clear());

function emptySnap(over: Partial<Snapshot> = {}): Snapshot {
  return {
    xp: 0, streak: 0, answersCount: 0, scrapsCount: 0, memosCount: 0,
    uniqueAnsweredTypes: new Set(), selectedInterests: new Set(),
    engagedInterests: new Set(), uniqueScrapCategories: 0,
    earnedBadgeIds: new Set(),
    ...over,
  };
}

describe('achievements — detectEvents', () => {
  it('xp-gained: prev.xp < curr.xp → event with delta amount', () => {
    const events = detectEvents(emptySnap({ xp: 90 }), emptySnap({ xp: 100 }));
    const xp = events.find(e => e.kind === 'xp-gained');
    expect(xp).toBeDefined();
    expect((xp as any).amount).toBe(10);
  });

  it('xp-gained: same xp → no event', () => {
    const events = detectEvents(emptySnap({ xp: 100 }), emptySnap({ xp: 100 }));
    expect(events.find(e => e.kind === 'xp-gained')).toBeUndefined();
  });

  it('level-up: 99 → 100 (boundary 정확) → 새잎 tier event', () => {
    const events = detectEvents(emptySnap({ xp: 99 }), emptySnap({ xp: 100 }));
    const lv = events.find(e => e.kind === 'level-up');
    expect(lv).toBeDefined();
    expect((lv as any).tierId).toBe(2);
  });

  it('level-up: 100 → 200 (same tier) → no event', () => {
    const events = detectEvents(emptySnap({ xp: 100 }), emptySnap({ xp: 200 }));
    expect(events.find(e => e.kind === 'level-up')).toBeUndefined();
  });

  it('streak-milestone: prev<3 && curr>=3 → 3 days event', () => {
    const events = detectEvents(emptySnap({ streak: 2 }), emptySnap({ streak: 3 }));
    const sm = events.find(e => e.kind === 'streak-milestone');
    expect(sm).toBeDefined();
    expect((sm as any).days).toBe(3);
  });

  it('streak-milestone: 7→8 → no event (already past 7)', () => {
    const events = detectEvents(emptySnap({ streak: 7 }), emptySnap({ streak: 8 }));
    expect(events.find(e => e.kind === 'streak-milestone')).toBeUndefined();
  });

  it('streak-milestone: 0→100 → 통과한 모든 milestone emit (3/7/30/100)', () => {
    const events = detectEvents(emptySnap({ streak: 0 }), emptySnap({ streak: 100 }));
    // UI 큐 max 3 cap이므로 4번째부터 silent persist (rewards.ts에서 처리).
    const days = events.filter(e => e.kind === 'streak-milestone').map(e => (e as any).days);
    expect(days).toContain(100);
    expect(days).toContain(3);
  });

  it('takeSnapshot: empty user → zeros', () => {
    saveUser({ name: 'x', interests: [], onboardedAt: '', streak: 0, lastActiveDate: '', xp: 0, earnedBadges: {}, gamificationMigrated: false, schemaVersion: 2 });
    const snap = takeSnapshot();
    expect(snap.xp).toBe(0);
    expect(snap.streak).toBe(0);
    expect(snap.earnedBadgeIds.size).toBe(0);
  });

  it('persistUnlocks: badge events → user.earnedBadges 갱신', () => {
    saveUser({ name: 'x', interests: [], onboardedAt: '', streak: 0, lastActiveDate: '', xp: 0, earnedBadges: {}, gamificationMigrated: false, schemaVersion: 2 });
    const events: GameEvent[] = [{ kind: 'badge', badgeId: 'streak-3', at: 1700000000000 }];
    persistUnlocks(events);
    const u = loadUserData()!;
    expect(u.earnedBadges['streak-3']).toBe(1700000000000);
  });

  it('runSweep: events 없으면 dispatch도 persist도 안 함', () => {
    saveUser({ name: 'x', interests: [], onboardedAt: '', streak: 0, lastActiveDate: '', xp: 0, earnedBadges: {}, gamificationMigrated: false, schemaVersion: 2 });
    const spy = vi.spyOn(document, 'dispatchEvent');
    const same = emptySnap({ xp: 100 });
    runSweep(same, same);
    expect(spy).not.toHaveBeenCalledWith(expect.objectContaining({ type: expect.stringMatching(/^dg:reward:/) }));
  });
});
```

- [ ] **Step 3: Run test to verify it fails**

```bash
npm test -- tests/unit/state/achievements.spec.ts
```

Expected: FAIL (`Cannot find module '../../../src/state/achievements'`)

- [ ] **Step 4: Implement gameTypes.ts**

```ts
// src/state/gameTypes.ts
/**
 * 게임화 시리즈 공통 타입 (순환 회피 위해 별도 모듈로 분리).
 * achievements.ts와 badgeCatalog.ts 모두 여기서 import (단방향).
 */
export interface Snapshot {
  xp: number;
  streak: number;
  answersCount: number;
  scrapsCount: number;
  memosCount: number;
  uniqueAnsweredTypes: ReadonlySet<string>;
  selectedInterests: ReadonlySet<string>;
  engagedInterests: ReadonlySet<string>;
  uniqueScrapCategories: number;
  earnedBadgeIds: ReadonlySet<string>;
}

export type GameEvent =
  | { kind: 'xp-gained'; amount: number; at: number }
  | { kind: 'level-up'; tierId: number; at: number }
  | { kind: 'streak-milestone'; days: number; at: number }
  | { kind: 'badge'; badgeId: string; at: number };

export const STREAK_MILESTONES = [3, 7, 30, 100, 365] as const;
```

- [ ] **Step 5: Implement achievements.ts (badge arm은 T5에서)**

```ts
// src/state/achievements.ts
import { dispatch } from '../ui/events';
import { didLevelUp } from './leveling';
import type { Snapshot, GameEvent } from './gameTypes';
import { STREAK_MILESTONES } from './gameTypes';
import { loadUserData, saveUser } from './user';
import { loadBriefings } from './briefings';
import { loadAnswers } from './persistence';

/**
 * Read-only — 현재 user/answers/briefings 상태로부터 Snapshot 생성.
 * 본 sweep 전후로 두 번 찍어 detectEvents에 넘긴다.
 */
export function takeSnapshot(): Snapshot {
  const u = loadUserData();
  const answers = loadAnswers();
  const briefings = loadBriefings();
  const scraps = briefings.filter(b => b.scrapped);
  const memos = briefings.filter(b => b.memo && b.memo.trim().length > 0);
  // T5에서 uniqueAnsweredTypes / engagedInterests / uniqueScrapCategories 채움.
  // 본 task 기준으로 빈 set / 0 default OK (badge arm 비활성).
  return {
    xp: u?.xp ?? 0,
    streak: u?.streak ?? 0,
    answersCount: answers.length,
    scrapsCount: scraps.length,
    memosCount: memos.length,
    uniqueAnsweredTypes: new Set(),
    selectedInterests: new Set(u?.interests ?? []),
    engagedInterests: new Set(),
    uniqueScrapCategories: 0,
    earnedBadgeIds: new Set(Object.keys(u?.earnedBadges ?? {})),
  };
}

export function detectEvents(prev: Snapshot, curr: Snapshot): GameEvent[] {
  const out: GameEvent[] = [];
  const at = Date.now();

  if (curr.xp > prev.xp) {
    out.push({ kind: 'xp-gained', amount: curr.xp - prev.xp, at });
  }

  const tier = didLevelUp(prev.xp, curr.xp);
  if (tier) out.push({ kind: 'level-up', tierId: tier.id, at });

  for (const m of STREAK_MILESTONES) {
    if (prev.streak < m && curr.streak >= m) {
      out.push({ kind: 'streak-milestone', days: m, at });
    }
  }

  // badge arm은 T5에서 활성화
  return out;
}

/**
 * badge unlock 이벤트를 user.earnedBadges에 영구 저장.
 * 다른 event 종류는 transient (저장 안 함).
 */
export function persistUnlocks(events: GameEvent[]): void {
  const u = loadUserData();
  if (!u) return;
  let dirty = false;
  for (const e of events) {
    if (e.kind === 'badge' && !u.earnedBadges[e.badgeId]) {
      u.earnedBadges[e.badgeId] = e.at;
      dirty = true;
    }
  }
  if (dirty) {
    try {
      saveUser(u);
    } catch {
      // Quota 등 — 영구 저장 실패. 토스트는 이미 dispatch됐을 수 있으나
      // 다음 진입 시 detectEvents가 prev=earnedBadges에 빠진 채 재실행 → 재시도됨.
      // 본 사이클 단순화: silent ignore (사용자 UX 영향 적음).
    }
  }
}

const EVENT_NAME_MAP = {
  'xp-gained': 'dg:reward:xp-float',
  'level-up': 'dg:reward:level-up',
  'streak-milestone': 'dg:reward:streak-milestone',
  'badge': 'dg:reward:badge-unlock',
} as const;

export function emitEvents(events: GameEvent[]): void {
  for (const e of events) {
    switch (e.kind) {
      case 'xp-gained':
        dispatch('dg:reward:xp-float', { amount: e.amount, at: e.at });
        break;
      case 'level-up':
        dispatch('dg:reward:level-up', { tierId: e.tierId, at: e.at });
        break;
      case 'streak-milestone':
        dispatch('dg:reward:streak-milestone', { days: e.days, at: e.at });
        break;
      case 'badge':
        dispatch('dg:reward:badge-unlock', { badgeId: e.badgeId, at: e.at });
        break;
    }
  }
}

/**
 * 단일 진입점. prev/curr Snapshot으로 events 도출 → 영구 저장 → emit.
 * 호출자(recordDailyAnswer / toggleScrap / saveMemo)는 saveUser 성공 후
 * curr=takeSnapshot()을 다시 찍어 본 함수에 넘겨야 함.
 */
export function runSweep(prev: Snapshot, curr: Snapshot): void {
  const events = detectEvents(prev, curr);
  if (events.length === 0) return;
  persistUnlocks(events);
  emitEvents(events);
}
```

- [ ] **Step 6: Run test to verify all pass**

```bash
npm test -- tests/unit/state/achievements.spec.ts
```

Expected: PASS (10 tests)

- [ ] **Step 7: Commit**

```bash
git add src/state/gameTypes.ts src/state/achievements.ts src/ui/events.ts tests/unit/state/achievements.spec.ts
git commit -m "$(cat <<'EOF'
feat(v3.12 T3): gameTypes.ts + achievements.ts (xp/level/streak arm)

- gameTypes.ts: Snapshot + GameEvent pure type module (avoids cycle
  between achievements.ts and badgeCatalog.ts in T5).
- achievements.ts: takeSnapshot / detectEvents (xp-gained / level-up /
  streak-milestone) / persistUnlocks / emitEvents / runSweep.
- events.ts: 4 reward CustomEvent names added to EventMap.
- Badge arm intentionally deferred to T5 (badgeCatalog).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Phase 1 — Wiring

### Task 4: sweep 호출 wiring (3곳)

**Files:**
- Modify: `src/state/user.ts:50-67` (recordDailyAnswer)
- Modify: `src/state/briefings.ts:57,59` (toggleScrap, saveMemo)
- Test: `tests/unit/state/sweep-wiring.spec.ts`

> Critical Review C1 적용: saveUser throw 시 sweep 차단 검증 필수.

- [ ] **Step 1: Write the failing test**

```ts
// tests/unit/state/sweep-wiring.spec.ts
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { saveUser, recordDailyAnswer } from '../../../src/state/user';
import { toggleScrap, saveMemo, saveBriefings, type Briefing } from '../../../src/state/briefings';

beforeEach(() => {
  localStorage.clear();
  saveUser({ name: 'x', interests: [], onboardedAt: '', streak: 0, lastActiveDate: '', xp: 95, earnedBadges: {}, gamificationMigrated: false, schemaVersion: 2 });
});

function captureRewardEvents(): string[] {
  const captured: string[] = [];
  const handler = (e: Event) => captured.push(e.type);
  ['dg:reward:xp-float', 'dg:reward:level-up', 'dg:reward:streak-milestone', 'dg:reward:badge-unlock'].forEach(n =>
    document.addEventListener(n, handler)
  );
  return captured;
}

describe('sweep wiring', () => {
  it('recordDailyAnswer(10) → xp-float event dispatch', () => {
    const captured = captureRewardEvents();
    recordDailyAnswer(10);
    expect(captured).toContain('dg:reward:xp-float');
  });

  it('recordDailyAnswer(10): xp 95→105 → level-up event dispatch', () => {
    const captured = captureRewardEvents();
    recordDailyAnswer(10);
    expect(captured).toContain('dg:reward:level-up');
  });

  it('recordDailyAnswer: saveUser throw 시 sweep 안 함', () => {
    const captured = captureRewardEvents();
    const spy = vi.spyOn(Storage.prototype, 'setItem').mockImplementation(() => {
      throw new DOMException('quota', 'QuotaExceededError');
    });
    expect(() => recordDailyAnswer(10)).toThrow();
    expect(captured).toHaveLength(0);
    spy.mockRestore();
  });

  it('toggleScrap: scraps 0→1 → sweep 호출 (xp-float은 X — xp 변화 없음)', () => {
    saveBriefings([{ id: '1', date: '2026-04-30', url: 'https://x.com', title: 't', summary: 's', scrapped: false, read: false, memo: '' }]);
    const captured = captureRewardEvents();
    toggleScrap(0);
    // xp 변화 없으므로 reward event 0개 (T5 badge arm 활성화 후엔 'scrap-1' badge unlock 발생)
    expect(captured).toHaveLength(0);
  });

  it('saveMemo: memo 작성 → sweep 호출 (T5 후 memo 5건째에 unlock)', () => {
    saveBriefings([{ id: '1', date: '2026-04-30', url: 'https://x.com', title: 't', summary: 's', scrapped: false, read: false, memo: '' }]);
    const captured = captureRewardEvents();
    saveMemo(0, '내 메모');
    expect(captured).toHaveLength(0);  // T5 후 badge arm 활성화 시 갱신
  });

  it('toggleScrap: saveBriefings throw 시 sweep 안 함', () => {
    saveBriefings([{ id: '1', date: '2026-04-30', url: 'https://x.com', title: 't', summary: 's', scrapped: false, read: false, memo: '' }]);
    const captured = captureRewardEvents();
    const spy = vi.spyOn(Storage.prototype, 'setItem').mockImplementation((k) => {
      if (k === 'briefings') throw new DOMException('quota', 'QuotaExceededError');
    });
    expect(() => toggleScrap(0)).toThrow();
    expect(captured).toHaveLength(0);
    spy.mockRestore();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
npm test -- tests/unit/state/sweep-wiring.spec.ts
```

Expected: FAIL (sweep 호출 안 함)

- [ ] **Step 3: Wire recordDailyAnswer**

Update `src/state/user.ts:50-67`:

```ts
import { takeSnapshot, runSweep } from './achievements';

export function recordDailyAnswer(xpDelta: number): void {
  const u = loadUserData();
  if (!u) return;
  const today = getDateStr();
  const prev = takeSnapshot();

  if (u.lastActiveDate !== today) {
    const y = new Date(today);
    y.setDate(y.getDate() - 1);
    const yesterday = getDateStr(y);
    u.streak = u.lastActiveDate === yesterday ? u.streak + 1 : 1;
  }

  u.xp += xpDelta;
  u.lastActiveDate = today;

  saveUser(u);  // throws on Quota — sweep 안 함

  const curr = takeSnapshot();
  runSweep(prev, curr);
}
```

- [ ] **Step 4: Wire toggleScrap + saveMemo**

Update `src/state/briefings.ts:57-59`:

```ts
import { takeSnapshot, runSweep } from './achievements';

function mutateWithSweep(index: number, fn: (b: Briefing) => void): void {
  const list = loadBriefings();
  const target = list[index];
  if (!target) return;
  const prev = takeSnapshot();
  fn(target);
  saveBriefings(list);  // throws on Quota — sweep 안 함
  const curr = takeSnapshot();
  runSweep(prev, curr);
}

export function toggleScrap(index: number): void { mutateWithSweep(index, b => { b.scrapped = !b.scrapped; }); }
export function setRead(index: number): void { mutate(index, b => { b.read = true; }); }  // sweep 불필요 (read flag만)
export function saveMemo(index: number, memo: string): void { mutateWithSweep(index, b => { b.memo = memo; }); }
```

> 주의: `setRead`는 sweep 불필요 (read flag는 어떤 뱃지 predicate도 트리거 안 함). 기존 `mutate` 유지.

- [ ] **Step 5: Run all tests**

```bash
npm test -- tests/unit/state/
```

Expected: 신규 6 PASS. 기존 user / briefings spec 모두 PASS.

- [ ] **Step 6: Commit**

```bash
git add src/state/user.ts src/state/briefings.ts tests/unit/state/sweep-wiring.spec.ts
git commit -m "$(cat <<'EOF'
feat(v3.12 T4): wire sweep into recordDailyAnswer / toggleScrap / saveMemo

prev=takeSnapshot → mutate → save → curr=takeSnapshot → runSweep.
saveUser/saveBriefings throw aborts sweep (false-fire 방지, Critical Review C1).

setRead 는 어떤 badge predicate 도 트리거하지 않으므로 mutate (sweep 미포함) 유지.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Phase 2 — Badge Catalog

### Task 5: badgeCatalog.ts (18) + Snapshot 확장 + badge arm 활성화

**Files:**
- Create: `src/state/badgeCatalog.ts`
- Modify: `src/state/gameTypes.ts` (이미 Snapshot 정의 있음 — 변경 0줄)
- Modify: `src/state/achievements.ts` (`takeSnapshot` 확장 + `detectEvents` badge arm 활성화)
- Test: `tests/unit/state/badge-catalog.spec.ts`

- [ ] **Step 1: Write the failing test (각 18 predicate 1개씩)**

```ts
// tests/unit/state/badge-catalog.spec.ts
import { describe, it, expect } from 'vitest';
import { BADGE_CATALOG, findBadge } from '../../../src/state/badgeCatalog';
import type { Snapshot } from '../../../src/state/gameTypes';

function snap(over: Partial<Snapshot> = {}): Snapshot {
  return {
    xp: 0, streak: 0, answersCount: 0, scrapsCount: 0, memosCount: 0,
    uniqueAnsweredTypes: new Set(), selectedInterests: new Set(),
    engagedInterests: new Set(), uniqueScrapCategories: 0,
    earnedBadgeIds: new Set(),
    ...over,
  };
}

describe('BADGE_CATALOG', () => {
  it('총 18개', () => expect(BADGE_CATALOG).toHaveLength(18));
  it('id 모두 unique', () => {
    const ids = BADGE_CATALOG.map(b => b.id);
    expect(new Set(ids).size).toBe(18);
  });
  it('5 카테고리 분포 — Streak 5 / Volume 4 / Tier 3 / Diversity 3 / Engagement 3', () => {
    const byCat = BADGE_CATALOG.reduce((m, b) => ((m[b.category] = (m[b.category] ?? 0) + 1), m), {} as Record<string, number>);
    expect(byCat).toEqual({ streak: 5, volume: 4, tier: 3, diversity: 3, engagement: 3 });
  });
});

describe('predicates — Streak (5)', () => {
  it('streak-3 unlock at streak===3', () => expect(findBadge('streak-3')!.predicate(snap({ streak: 3 }))).toBe(true));
  it('streak-3 locked at streak===2', () => expect(findBadge('streak-3')!.predicate(snap({ streak: 2 }))).toBe(false));
  it('streak-7 at 7', () => expect(findBadge('streak-7')!.predicate(snap({ streak: 7 }))).toBe(true));
  it('streak-30 at 30', () => expect(findBadge('streak-30')!.predicate(snap({ streak: 30 }))).toBe(true));
  it('streak-100 at 100', () => expect(findBadge('streak-100')!.predicate(snap({ streak: 100 }))).toBe(true));
  it('streak-365 at 365', () => expect(findBadge('streak-365')!.predicate(snap({ streak: 365 }))).toBe(true));
});

describe('predicates — Volume (4)', () => {
  it('answers-1 at 1', () => expect(findBadge('answers-1')!.predicate(snap({ answersCount: 1 }))).toBe(true));
  it('answers-10 at 10', () => expect(findBadge('answers-10')!.predicate(snap({ answersCount: 10 }))).toBe(true));
  it('answers-30 at 30', () => expect(findBadge('answers-30')!.predicate(snap({ answersCount: 30 }))).toBe(true));
  it('answers-100 at 100', () => expect(findBadge('answers-100')!.predicate(snap({ answersCount: 100 }))).toBe(true));
});

describe('predicates — Tier (3)', () => {
  it('tier-3-tree at xp===300', () => expect(findBadge('tier-3-tree')!.predicate(snap({ xp: 300 }))).toBe(true));
  it('tier-3-tree locked at xp===299', () => expect(findBadge('tier-3-tree')!.predicate(snap({ xp: 299 }))).toBe(false));
  it('tier-5-mountain at xp===1000', () => expect(findBadge('tier-5-mountain')!.predicate(snap({ xp: 1000 }))).toBe(true));
  it('tier-6-sky at xp===2000', () => expect(findBadge('tier-6-sky')!.predicate(snap({ xp: 2000 }))).toBe(true));
});

describe('predicates — Diversity (3)', () => {
  it('diversity-types: 5종 중 4종 답변 → unlock', () => {
    const s = snap({ uniqueAnsweredTypes: new Set(['분석', '전환', '실무', '성장']) });
    expect(findBadge('diversity-types')!.predicate(s)).toBe(true);
  });
  it('diversity-types: 5종 모두 답변 → unlock', () => {
    const s = snap({ uniqueAnsweredTypes: new Set(['분석', '전환', '실무', '성장', '트렌드']) });
    expect(findBadge('diversity-types')!.predicate(s)).toBe(true);
  });
  it('diversity-types: 3종만 답변 → locked', () => {
    const s = snap({ uniqueAnsweredTypes: new Set(['분석', '전환', '실무']) });
    expect(findBadge('diversity-types')!.predicate(s)).toBe(false);
  });
  it('diversity-interests-all: 선택한 모든 관심분야에 스크랩 1+', () => {
    const s = snap({ selectedInterests: new Set(['AI', 'HR']), engagedInterests: new Set(['AI', 'HR']) });
    expect(findBadge('diversity-interests-all')!.predicate(s)).toBe(true);
  });
  it('diversity-interests-all: 관심분야 0개 → false (최소 1개 선택 필요)', () => {
    const s = snap({ selectedInterests: new Set(), engagedInterests: new Set() });
    expect(findBadge('diversity-interests-all')!.predicate(s)).toBe(false);
  });
  it('diversity-scrap-cats at 8', () => expect(findBadge('diversity-scrap-cats')!.predicate(snap({ uniqueScrapCategories: 8 }))).toBe(true));
});

describe('predicates — Engagement (3)', () => {
  it('scrap-1 at scrapsCount 1', () => expect(findBadge('scrap-1')!.predicate(snap({ scrapsCount: 1 }))).toBe(true));
  it('scrap-50 at scrapsCount 50', () => expect(findBadge('scrap-50')!.predicate(snap({ scrapsCount: 50 }))).toBe(true));
  it('memo-5 at memosCount 5', () => expect(findBadge('memo-5')!.predicate(snap({ memosCount: 5 }))).toBe(true));
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
npm test -- tests/unit/state/badge-catalog.spec.ts
```

Expected: FAIL (`Cannot find module '../../../src/state/badgeCatalog'`)

- [ ] **Step 3: Implement badgeCatalog.ts**

```ts
// src/state/badgeCatalog.ts
import type { Snapshot } from './gameTypes';

export interface BadgeDef {
  id: string;
  icon: string;
  name: string;
  description: string;
  category: 'streak' | 'volume' | 'tier' | 'diversity' | 'engagement';
  predicate: (snap: Snapshot) => boolean;
}

export const BADGE_CATALOG: readonly BadgeDef[] = [
  // Streak (5)
  { id: 'streak-3',   icon: '🔥', name: '첫 불씨',     description: '연속 3일 답변',   category: 'streak', predicate: s => s.streak >= 3 },
  { id: 'streak-7',   icon: '🔥', name: '주간 스트릭',  description: '연속 7일 답변',   category: 'streak', predicate: s => s.streak >= 7 },
  { id: 'streak-30',  icon: '🌟', name: '한 달 스트릭', description: '연속 30일 답변',  category: 'streak', predicate: s => s.streak >= 30 },
  { id: 'streak-100', icon: '💯', name: '백일 스트릭',  description: '연속 100일 답변', category: 'streak', predicate: s => s.streak >= 100 },
  { id: 'streak-365', icon: '👑', name: '일년 스트릭',  description: '연속 365일 답변', category: 'streak', predicate: s => s.streak >= 365 },

  // Volume (4)
  { id: 'answers-1',   icon: '🌱', name: '첫 답변',    description: '누적 답변 1개',   category: 'volume', predicate: s => s.answersCount >= 1 },
  { id: 'answers-10',  icon: '📚', name: '열 걸음',    description: '누적 답변 10개',  category: 'volume', predicate: s => s.answersCount >= 10 },
  { id: 'answers-30',  icon: '🎯', name: '한 달 완성', description: '누적 답변 30개',  category: 'volume', predicate: s => s.answersCount >= 30 },
  { id: 'answers-100', icon: '💎', name: '백문백답',   description: '누적 답변 100개', category: 'volume', predicate: s => s.answersCount >= 100 },

  // Tier (3) — xp boundary 와 1:1
  { id: 'tier-3-tree',     icon: '🌳', name: '나무', description: '레벨 3 도달 (XP 300)',  category: 'tier', predicate: s => s.xp >= 300 },
  { id: 'tier-5-mountain', icon: '🏔️', name: '산',   description: '레벨 5 도달 (XP 1000)', category: 'tier', predicate: s => s.xp >= 1000 },
  { id: 'tier-6-sky',      icon: '🌌', name: '하늘', description: '레벨 6 MAX 도달 (XP 2000)', category: 'tier', predicate: s => s.xp >= 2000 },

  // Diversity (3)
  { id: 'diversity-types',         icon: '🎨', name: '다재다능',   description: '5가지 질문 유형 중 4종 이상 답변 (분석/전환/실무/성장/트렌드)',
    category: 'diversity',
    predicate: s => {
      // P0 fix: 실제 home.ts:657-661은 한국어 라벨 5종을 사용. 영문 키는 dead badge였음.
      const KO_TYPES = ['분석', '전환', '실무', '성장', '트렌드'];
      return KO_TYPES.filter(t => s.uniqueAnsweredTypes.has(t)).length >= 4;
    } },
  { id: 'diversity-interests-all', icon: '🌍', name: '만물박사',   description: '선택한 모든 관심분야에 스크랩 1건 이상',
    category: 'diversity',
    predicate: s => s.selectedInterests.size > 0 &&
      Array.from(s.selectedInterests).every(i => s.engagedInterests.has(i)) },
  { id: 'diversity-scrap-cats',    icon: '📖', name: '호기심',     description: '스크랩한 브리핑의 unique 카테고리 8개 이상',
    category: 'diversity',
    predicate: s => s.uniqueScrapCategories >= 8 },

  // Engagement (3)
  { id: 'scrap-1',  icon: '📌', name: '첫 스크랩', description: '누적 스크랩 1개',  category: 'engagement', predicate: s => s.scrapsCount >= 1 },
  { id: 'scrap-50', icon: '📰', name: '정보왕',    description: '누적 스크랩 50개', category: 'engagement', predicate: s => s.scrapsCount >= 50 },
  { id: 'memo-5',   icon: '✏️', name: '메모왕',    description: '누적 메모 5개',    category: 'engagement', predicate: s => s.memosCount >= 5 },
] as const;

export function findBadge(id: string): BadgeDef | undefined {
  return BADGE_CATALOG.find(b => b.id === id);
}
```

- [ ] **Step 4: Update achievements.ts — Snapshot 확장 fields + badge arm 활성화**

Replace `takeSnapshot` and `detectEvents` (badge arm) in `src/state/achievements.ts`:

```ts
// achievements.ts (replace takeSnapshot)
import { BADGE_CATALOG } from './badgeCatalog';
import type { Briefing } from './briefings';

// 답변에 카테고리 매핑은 없음 — 스크랩 카테고리만 사용 (spec §7.1).
// briefing.id 또는 sourceTitle/url 기반 카테고리 추출은 본 사이클 단순화 — interests 매칭만:
function categorizeScrapsByInterest(briefings: Briefing[], interests: string[]): {
  engaged: Set<string>;
  uniqueCount: number;
} {
  const engaged = new Set<string>();
  const cats = new Set<string>();
  for (const b of briefings) {
    if (!b.scrapped) continue;
    // P1-2 fix: invalid url일 때 new URL throw → takeSnapshot 전체 실패 → sweep 차단 위험.
    // sourceTitle 우선, 없으면 hostname try/catch graceful (최종 fallback은 빈 문자열 → cats에 추가 안 함).
    let cat = (b.sourceTitle ?? '').trim();
    if (!cat) {
      try { cat = new URL(b.url).hostname; } catch { cat = ''; }
    }
    if (cat) cats.add(cat);
    for (const i of interests) {
      // 관심분야 텍스트가 sourceTitle/title/summary에 포함되는지 (case-insensitive)
      const hay = `${b.sourceTitle ?? ''} ${b.title} ${b.summary}`.toLowerCase();
      if (hay.includes(i.toLowerCase())) engaged.add(i);
    }
  }
  return { engaged, uniqueCount: cats.size };
}

export function takeSnapshot(): Snapshot {
  const u = loadUserData();
  const answers = loadAnswers();
  const briefings = loadBriefings();
  const scraps = briefings.filter(b => b.scrapped);
  const memos = briefings.filter(b => b.memo && b.memo.trim().length > 0);
  const interests = u?.interests ?? [];
  const { engaged, uniqueCount } = categorizeScrapsByInterest(briefings, interests);

  // P0 fix: 실제 코드는 한국어 라벨 5종 (home.ts:657-661 + schema.ts:17 주석).
  const KNOWN_TYPES = new Set(['분석', '전환', '실무', '성장', '트렌드']);
  const answeredTypes = new Set<string>();
  for (const a of answers) {
    if (a.type && KNOWN_TYPES.has(a.type)) answeredTypes.add(a.type);
  }

  return {
    xp: u?.xp ?? 0,
    streak: u?.streak ?? 0,
    answersCount: answers.length,
    scrapsCount: scraps.length,
    memosCount: memos.length,
    uniqueAnsweredTypes: answeredTypes,
    selectedInterests: new Set(interests),
    engagedInterests: engaged,
    uniqueScrapCategories: uniqueCount,
    earnedBadgeIds: new Set(Object.keys(u?.earnedBadges ?? {})),
  };
}
```

Update `detectEvents` to **add badge arm** (place at end of function before `return out`):

```ts
// achievements.ts — detectEvents (badge arm 추가)
export function detectEvents(prev: Snapshot, curr: Snapshot): GameEvent[] {
  const out: GameEvent[] = [];
  const at = Date.now();

  if (curr.xp > prev.xp) out.push({ kind: 'xp-gained', amount: curr.xp - prev.xp, at });
  const tier = didLevelUp(prev.xp, curr.xp);
  if (tier) out.push({ kind: 'level-up', tierId: tier.id, at });
  for (const m of STREAK_MILESTONES) {
    if (prev.streak < m && curr.streak >= m) {
      out.push({ kind: 'streak-milestone', days: m, at });
    }
  }

  // ✅ T5 badge arm — !prev.earned && !curr.earned && predicate(curr)
  for (const b of BADGE_CATALOG) {
    if (prev.earnedBadgeIds.has(b.id)) continue;       // 이미 영구 unlock
    if (curr.earnedBadgeIds.has(b.id)) continue;       // 이중 안전망
    if (b.predicate(curr)) {
      out.push({ kind: 'badge', badgeId: b.id, at });
    }
  }

  return out;
}
```

- [ ] **Step 5: Add wiring assertions to T4 spec (badge unlock 검증)**

Append to `tests/unit/state/sweep-wiring.spec.ts`:

```ts
describe('sweep wiring — badge arm (T5 활성화 후)', () => {
  it('toggleScrap: scraps 0→1 → scrap-1 badge unlock', () => {
    saveBriefings([{ id: '1', date: '2026-04-30', url: 'https://x.com', title: 't', summary: 's', scrapped: false, read: false, memo: '' }]);
    const captured = captureRewardEvents();
    toggleScrap(0);
    expect(captured).toContain('dg:reward:badge-unlock');
  });

  it('saveMemo: 5번째 memo → memo-5 badge unlock', () => {
    const briefings = Array.from({ length: 6 }, (_, i) => ({
      id: `${i}`, date: '2026-04-30', url: `https://x${i}.com`, title: 't', summary: 's',
      scrapped: false, read: false, memo: i < 4 ? `m${i}` : '',
    }));
    saveBriefings(briefings);
    const captured = captureRewardEvents();
    saveMemo(4, '다섯번째');  // 4 → 5 memos
    expect(captured).toContain('dg:reward:badge-unlock');
  });
});
```

- [ ] **Step 6: Run all relevant tests**

```bash
npm test -- tests/unit/state/badge-catalog.spec.ts tests/unit/state/sweep-wiring.spec.ts tests/unit/state/achievements.spec.ts
```

Expected: badge-catalog 21 PASS · sweep-wiring 8 PASS · achievements 10 PASS.

- [ ] **Step 7: Commit**

```bash
git add src/state/badgeCatalog.ts src/state/achievements.ts tests/unit/state/badge-catalog.spec.ts tests/unit/state/sweep-wiring.spec.ts
git commit -m "$(cat <<'EOF'
feat(v3.12 T5): badgeCatalog.ts (18) + Snapshot diversity fields + badge arm

- 5 categories: Streak 5 / Volume 4 / Tier 3 / Diversity 3 / Engagement 3.
- takeSnapshot now derives uniqueAnsweredTypes / engagedInterests /
  uniqueScrapCategories from answers + briefings + user.interests.
- detectEvents badge arm: emit when !prev.earned && predicate(curr).
- Each predicate has a unit test (boundary + non-boundary).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Phase 3 — UI Reward Layer

### Task 6: rewards.ts (큐 + listener + dismiss)

**Files:**
- Create: `src/ui/rewards.ts`
- Modify: `src/main.ts` (mount rewards listener at app boot)
- Test: `tests/unit/ui/rewards.spec.ts`

> 본 task는 **공통 인프라**만 — 실제 모션/모달은 T7~T10에서 plug-in. `renderToast(kind, payload)`는 본 task에서 placeholder, T8/T9/T10에서 클래스 modifier 채움.

- [ ] **Step 1: Write the failing test**

```ts
// tests/unit/ui/rewards.spec.ts
import { describe, it, expect, beforeEach, vi, afterEach } from 'vitest';
import { mountRewards, __resetForTest } from '../../../src/ui/rewards';
import { dispatch } from '../../../src/ui/events';

beforeEach(() => {
  document.body.innerHTML = '<div id="modalRoot"></div>';
  __resetForTest();
  mountRewards();
});

afterEach(() => vi.useRealTimers());

describe('rewards — toast 큐', () => {
  it('badge-unlock event → 토스트 1개 DOM 추가', () => {
    dispatch('dg:reward:badge-unlock', { badgeId: 'streak-3', at: Date.now() });
    expect(document.querySelectorAll('.toast--badge')).toHaveLength(1);
  });

  it('동시 5개 unlock → 최대 3개만 visible (큐 cap)', () => {
    for (const id of ['streak-3', 'answers-1', 'tier-3-tree', 'memo-5', 'scrap-1']) {
      dispatch('dg:reward:badge-unlock', { badgeId: id, at: Date.now() });
    }
    expect(document.querySelectorAll('.toast--badge').length).toBeLessThanOrEqual(3);
  });

  it('dismiss 버튼 → 토스트 즉시 제거', () => {
    dispatch('dg:reward:badge-unlock', { badgeId: 'streak-3', at: Date.now() });
    const closeBtn = document.querySelector<HTMLButtonElement>('.toast--badge .toast-close');
    closeBtn?.click();
    expect(document.querySelectorAll('.toast--badge')).toHaveLength(0);
  });

  it('알 수 없는 badgeId → 토스트 안 띄움 (graceful)', () => {
    dispatch('dg:reward:badge-unlock', { badgeId: 'unknown-99', at: Date.now() });
    expect(document.querySelectorAll('.toast--badge')).toHaveLength(0);
  });

  it('badge 토스트 3초 후 자동 제거', () => {
    vi.useFakeTimers();
    dispatch('dg:reward:badge-unlock', { badgeId: 'streak-3', at: Date.now() });
    expect(document.querySelectorAll('.toast--badge')).toHaveLength(1);
    vi.advanceTimersByTime(3500);
    expect(document.querySelectorAll('.toast--badge')).toHaveLength(0);
  });

  it('큐 4번째: silent persist만 (이미 persistUnlocks가 처리한 상태이므로 토스트만 drop)', () => {
    for (const id of ['streak-3', 'answers-1', 'tier-3-tree', 'memo-5']) {
      dispatch('dg:reward:badge-unlock', { badgeId: id, at: Date.now() });
    }
    expect(document.querySelectorAll('.toast--badge').length).toBeLessThanOrEqual(3);
  });

  it('mountRewards idempotent — 두 번 호출해도 listener 1번만', () => {
    mountRewards();  // beforeEach가 이미 호출함, 본 줄은 두 번째
    dispatch('dg:reward:badge-unlock', { badgeId: 'streak-3', at: Date.now() });
    expect(document.querySelectorAll('.toast--badge')).toHaveLength(1);  // 2개 아님
  });

  it('badge 토스트 click → modal-system openModal 호출 (T10에서 wiring)', () => {
    dispatch('dg:reward:badge-unlock', { badgeId: 'streak-3', at: Date.now() });
    const detail = document.querySelector<HTMLButtonElement>('.toast--badge .toast-detail');
    expect(detail).not.toBeNull();
    // 실제 modal 본체는 T10에서 lazy-load + 검증
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
npm test -- tests/unit/ui/rewards.spec.ts
```

Expected: FAIL (`Cannot find module`)

- [ ] **Step 3: Implement rewards.ts (placeholder confetti/xp-float — T7/T8에서 채움)**

```ts
// src/ui/rewards.ts
import { on } from './events';
import { findBadge } from '../state/badgeCatalog';
import { TIERS } from '../state/leveling';

const MAX_VISIBLE = 3;
const TOAST_DURATIONS = { badge: 3000, levelup: 4000, streak: 3000 } as const;

let mounted = false;
let activeCount = 0;
// P1-1 fix: spec 간 document listener 누적 → flake 방지. mountRewards가 등록한
// listener의 dispose 함수를 모아두고 __resetForTest에서 모두 호출.
const disposers: Array<() => void> = [];

function ensureContainer(): HTMLElement {
  let el = document.getElementById('rewardToastContainer');
  if (el) return el;
  el = document.createElement('div');
  el.id = 'rewardToastContainer';
  el.className = 'toast-container toast-container--reward';
  (document.getElementById('modalRoot') ?? document.body).append(el);
  return el;
}

function spawnToast(html: string, modifier: 'badge' | 'levelup' | 'streak', durationMs: number): HTMLElement | null {
  if (activeCount >= MAX_VISIBLE) return null;
  const container = ensureContainer();
  const el = document.createElement('div');
  el.className = `toast toast--${modifier}`;
  el.setAttribute('role', 'status');
  // eslint-disable-next-line no-restricted-syntax -- caller-controlled, no user interpolation in html (only escaped names from BADGE_CATALOG / TIERS)
  el.innerHTML = html;
  container.append(el);
  activeCount++;
  el.querySelector('.toast-close')?.addEventListener('click', () => removeToast(el));
  setTimeout(() => removeToast(el), durationMs);
  return el;
}

function removeToast(el: HTMLElement): void {
  if (!el.parentElement) return;
  el.classList.add('toast-leaving');
  setTimeout(() => {
    el.remove();
    activeCount = Math.max(0, activeCount - 1);
  }, 250);
}

function escapeText(s: string): string {
  // 카탈로그 데이터만 들어옴 — 그래도 방어적으로 escape
  return s.replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]!));
}

export function mountRewards(): void {
  if (mounted) return;
  mounted = true;

  disposers.push(on('dg:reward:badge-unlock', ({ badgeId }) => {
    const def = findBadge(badgeId);
    if (!def) return;
    const html = `
      <span class="toast-icon">${escapeText(def.icon)}</span>
      <span class="toast-text">🎯 <strong>${escapeText(def.name)}</strong> 뱃지 획득!</span>
      <button type="button" class="toast-detail" data-badge-id="${escapeText(def.id)}">자세히</button>
      <button type="button" class="toast-close" aria-label="닫기">×</button>
    `;
    const el = spawnToast(html, 'badge', TOAST_DURATIONS.badge);
    // T10에서 .toast-detail 핸들러 wiring (lazy import modal)
    if (el) {
      el.querySelector('.toast-detail')?.addEventListener('click', async () => {
        const { openBadgeDetail } = await import('./modals/badge-detail');
        openBadgeDetail(def.id);
      });
    }
  }));

  disposers.push(on('dg:reward:level-up', ({ tierId }) => {
    const tier = TIERS.find(t => t.id === tierId);
    if (!tier) return;
    const html = `
      <span class="toast-icon">${escapeText(tier.icon)}</span>
      <span class="toast-text"><strong>${escapeText(tier.name)}</strong> 레벨 달성!</span>
      <button type="button" class="toast-close" aria-label="닫기">×</button>
    `;
    spawnToast(html, 'levelup', TOAST_DURATIONS.levelup);
    // T8에서 playConfetti() 호출 추가
  }));

  disposers.push(on('dg:reward:streak-milestone', ({ days }) => {
    const html = `
      <span class="toast-icon">🔥</span>
      <span class="toast-text"><strong>${days}일</strong> 스트릭!</span>
      <button type="button" class="toast-close" aria-label="닫기">×</button>
    `;
    spawnToast(html, 'streak', TOAST_DURATIONS.streak);
    sessionStorage.setItem('dg:streakPulsePending', String(days));
  }));

  // xp-gained는 T7에서 (floating motion DOM 별도 — 토스트 컨테이너 아님).
  // T7 step 3에서 disposers.push(on('dg:reward:xp-float', ...)) 형태로 추가.
}

/** @internal — test only. P1-1: dispose 후 mount flag 초기화 (spec 간 listener 누수 방지). */
export function __resetForTest(): void {
  while (disposers.length) disposers.pop()!();
  mounted = false;
  activeCount = 0;
  document.getElementById('rewardToastContainer')?.remove();
}
```

- [ ] **Step 4: Mount rewards in main.ts**

Find the boot sequence in `src/main.ts` (look for `mountStatsHandlers` or similar) and add:

```ts
// src/main.ts (near other mount* calls)
import { mountRewards } from './ui/rewards';
// ...
mountRewards();
```

- [ ] **Step 5: Run test**

```bash
npm test -- tests/unit/ui/rewards.spec.ts
```

Expected: 8 PASS.

- [ ] **Step 6: Commit**

```bash
git add src/ui/rewards.ts src/main.ts tests/unit/ui/rewards.spec.ts
git commit -m "$(cat <<'EOF'
feat(v3.12 T6): rewards.ts — toast queue + 4 reward listeners (max 3 cap)

Single UI entry point for the 4 dg:reward:* CustomEvents. Toast container
under #modalRoot; 3-cap with silent overflow drop. T7~T10 add motion +
confetti + modal wiring on top.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: +XP floating motion + xpBadge ping

**Files:**
- Modify: `src/ui/rewards.ts` (xp-float listener + DOM 헬퍼 + xpBadge fallback)
- Modify: `src/styles/main.css` (`.xp-float` keyframes + `.badge-pulse`)
- Test: `tests/unit/ui/xp-float.spec.ts`

- [ ] **Step 1: Write the failing test**

```ts
// tests/unit/ui/xp-float.spec.ts
import { describe, it, expect, beforeEach, vi, afterEach } from 'vitest';
import { mountRewards, __resetForTest } from '../../../src/ui/rewards';
import { dispatch } from '../../../src/ui/events';

beforeEach(() => {
  document.body.innerHTML = `
    <div id="modalRoot"></div>
    <div class="chat-bubble" id="ut"></div>
    <span id="xpBadge">XP 0</span>
  `;
  __resetForTest();
  mountRewards();
});

afterEach(() => vi.useRealTimers());

describe('xp-float', () => {
  it('xp-float event → .xp-float DOM 1개 추가', () => {
    dispatch('dg:reward:xp-float', { amount: 10, at: Date.now() });
    expect(document.querySelectorAll('.xp-float')).toHaveLength(1);
    expect(document.querySelector('.xp-float')!.textContent).toBe('+10 XP');
  });

  it('xp-float 1.2초 후 DOM 제거', () => {
    vi.useFakeTimers();
    dispatch('dg:reward:xp-float', { amount: 10, at: Date.now() });
    vi.advanceTimersByTime(1500);
    expect(document.querySelectorAll('.xp-float')).toHaveLength(0);
  });

  it('xpBadge ping — .badge-pulse 클래스 add 후 300ms remove', () => {
    vi.useFakeTimers();
    dispatch('dg:reward:xp-float', { amount: 10, at: Date.now() });
    vi.advanceTimersByTime(1000);
    expect(document.getElementById('xpBadge')!.classList.contains('badge-pulse')).toBe(true);
    vi.advanceTimersByTime(400);
    expect(document.getElementById('xpBadge')!.classList.contains('badge-pulse')).toBe(false);
  });

  it('xpBadge 미존재 시 → fallback 좌표 (우상단 absolute) 사용, 에러 안 남', () => {
    document.getElementById('xpBadge')?.remove();
    expect(() => dispatch('dg:reward:xp-float', { amount: 10, at: Date.now() })).not.toThrow();
    expect(document.querySelectorAll('.xp-float')).toHaveLength(1);
  });

  it('chat-bubble 미존재 시 → 화면 중앙 fallback start point', () => {
    document.querySelector('.chat-bubble')?.remove();
    expect(() => dispatch('dg:reward:xp-float', { amount: 10, at: Date.now() })).not.toThrow();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
npm test -- tests/unit/ui/xp-float.spec.ts
```

Expected: 5 FAIL.

- [ ] **Step 3: Append xp-float listener to rewards.ts**

```ts
// rewards.ts — mountRewards body 마지막에 추가 (T6 disposers 패턴 일관 유지)
disposers.push(on('dg:reward:xp-float', ({ amount }) => {
  spawnXpFloat(amount);
}));

function spawnXpFloat(amount: number): void {
  const startEl = document.querySelector<HTMLElement>('.chat-bubble:last-of-type');
  const xpBadge = document.getElementById('xpBadge');

  // 시작점: chat bubble 우측 가장자리. 없으면 화면 중앙.
  const startRect = startEl?.getBoundingClientRect();
  const startX = startRect ? startRect.right : window.innerWidth / 2;
  const startY = startRect ? startRect.top + startRect.height / 2 : window.innerHeight / 2;

  // 끝점: xpBadge 중앙. 미존재 / 비가시(jsdom checkVisibility 없음 → width===0 체크) 시 우상단.
  const endRect = (xpBadge && (xpBadge as HTMLElement).getBoundingClientRect());
  const isVisible = endRect && endRect.width > 0 && endRect.height > 0;
  const endX = isVisible ? endRect.left + endRect.width / 2 : window.innerWidth - 32;
  const endY = isVisible ? endRect.top + endRect.height / 2 : 32;

  const el = document.createElement('span');
  el.className = 'xp-float';
  el.textContent = `+${amount} XP`;
  el.style.setProperty('--start-x', `${startX}px`);
  el.style.setProperty('--start-y', `${startY}px`);
  el.style.setProperty('--end-x', `${endX}px`);
  el.style.setProperty('--end-y', `${endY}px`);
  document.body.append(el);
  setTimeout(() => el.remove(), 1300);

  // 도착 시 xpBadge ping
  setTimeout(() => {
    if (!xpBadge) return;
    xpBadge.classList.add('badge-pulse');
    setTimeout(() => xpBadge.classList.remove('badge-pulse'), 300);
  }, 1000);
}
```

- [ ] **Step 4: CSS — `.xp-float` keyframes + `.badge-pulse`**

Append to `src/styles/main.css`:

```css
/* v3.12 T7 — XP floating + badge pulse */
.xp-float {
  position: fixed;
  left: var(--start-x);
  top: var(--start-y);
  transform: translate(-50%, -50%);
  font-weight: 700;
  color: var(--color-success, #16a34a);
  pointer-events: none;
  z-index: 1000;
  animation: xpFloat 1.2s ease-out forwards;
}
@keyframes xpFloat {
  0%   { left: var(--start-x); top: var(--start-y); opacity: 1; }
  83%  { left: var(--end-x);   top: var(--end-y);   opacity: 1; }
  100% { left: var(--end-x);   top: var(--end-y);   opacity: 0; }
}
.badge-pulse {
  animation: badgePulse 300ms ease-out;
}
@keyframes badgePulse {
  0%   { transform: scale(1); }
  50%  { transform: scale(1.15); }
  100% { transform: scale(1); }
}
```

- [ ] **Step 5: Run test + visual sanity**

```bash
npm test -- tests/unit/ui/xp-float.spec.ts
npm run dev   # 브라우저에서 답변 제출 → +10 XP가 chat bubble → xpBadge 방향 1.2초 motion 확인 (사용자 확인용)
```

Expected: 5 PASS.

- [ ] **Step 6: Commit**

```bash
git add src/ui/rewards.ts src/styles/main.css tests/unit/ui/xp-float.spec.ts
git commit -m "$(cat <<'EOF'
feat(v3.12 T7): +XP floating motion (chat bubble → xpBadge) + badge pulse

CSS variables (--start-x/y, --end-x/y) drive the keyframe so the JS layer
just measures bounding rects + injects a span. xpBadge missing/invisible
falls back to top-right absolute coords. xpBadge ping fires at the
arrival mark (300ms scale pulse).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 8: Level-up confetti (CSS-only)

**Files:**
- Modify: `src/ui/rewards.ts` (level-up listener에 `playConfetti` 호출)
- Modify: `src/styles/main.css` (`.toast--levelup` + `.confetti-particle` keyframes)
- Test: `tests/unit/ui/level-up.spec.ts`

> 본 task는 spec compliance review만 (CSS-only, closure semantics 위험 X — Phase 정책).

- [ ] **Step 1: Write the failing test**

```ts
// tests/unit/ui/level-up.spec.ts
import { describe, it, expect, beforeEach, vi, afterEach } from 'vitest';
import { mountRewards, __resetForTest } from '../../../src/ui/rewards';
import { dispatch } from '../../../src/ui/events';

beforeEach(() => {
  document.body.innerHTML = '<div id="modalRoot"></div>';
  __resetForTest();
  mountRewards();
});

afterEach(() => vi.useRealTimers());

describe('level-up', () => {
  it('level-up event → .toast--levelup 1개 + .confetti-particle 30개', () => {
    dispatch('dg:reward:level-up', { tierId: 3, at: Date.now() });
    expect(document.querySelectorAll('.toast--levelup')).toHaveLength(1);
    expect(document.querySelectorAll('.confetti-particle')).toHaveLength(30);
  });

  it('confetti 1초 후 모두 제거', () => {
    vi.useFakeTimers();
    dispatch('dg:reward:level-up', { tierId: 3, at: Date.now() });
    vi.advanceTimersByTime(1500);
    expect(document.querySelectorAll('.confetti-particle')).toHaveLength(0);
  });

  it('알 수 없는 tierId → 토스트 안 띄움 (graceful)', () => {
    dispatch('dg:reward:level-up', { tierId: 99, at: Date.now() });
    expect(document.querySelectorAll('.toast--levelup')).toHaveLength(0);
  });

  it('confetti는 level-up 1회만 — badge unlock 동시 발생해도 confetti 1회', () => {
    dispatch('dg:reward:level-up', { tierId: 3, at: Date.now() });
    dispatch('dg:reward:badge-unlock', { badgeId: 'tier-3-tree', at: Date.now() });
    expect(document.querySelectorAll('.confetti-particle')).toHaveLength(30);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
npm test -- tests/unit/ui/level-up.spec.ts
```

Expected: FAIL.

- [ ] **Step 3: Add playConfetti to rewards.ts**

In `src/ui/rewards.ts`, add helper + invoke from level-up listener:

```ts
// rewards.ts — top of file 또는 하단 helpers
const CONFETTI_COLORS = ['#16a34a', '#3b82f6', '#f59e0b', '#ef4444', '#a855f7', '#ec4899'];
const CONFETTI_COUNT = 30;

function playConfetti(): void {
  const root = document.getElementById('modalRoot') ?? document.body;
  const wrap = document.createElement('div');
  wrap.className = 'confetti-burst';
  for (let i = 0; i < CONFETTI_COUNT; i++) {
    const p = document.createElement('span');
    p.className = 'confetti-particle';
    p.style.setProperty('--angle', `${(360 / CONFETTI_COUNT) * i}deg`);
    p.style.setProperty('--color', CONFETTI_COLORS[i % CONFETTI_COLORS.length]!);
    p.style.setProperty('--delay', `${(i % 5) * 30}ms`);
    wrap.append(p);
  }
  root.append(wrap);
  setTimeout(() => wrap.remove(), 1100);
}
```

Update level-up listener (add `playConfetti()` after `spawnToast`):

```ts
on('dg:reward:level-up', ({ tierId }) => {
  const tier = TIERS.find(t => t.id === tierId);
  if (!tier) return;
  const html = `...`;  // unchanged
  spawnToast(html, 'levelup', TOAST_DURATIONS.levelup);
  playConfetti();  // ✅ T8 추가
});
```

- [ ] **Step 4: CSS — `.toast--levelup` + `.confetti-particle`**

Append to `src/styles/main.css`:

```css
/* v3.12 T8 — Level-up toast + CSS-only confetti */
.toast--levelup {
  background: linear-gradient(135deg, #facc15 0%, #f97316 100%);
  color: #fff;
  font-size: 1.05em;
  padding: 14px 18px;
}
.confetti-burst {
  position: fixed;
  inset: 0;
  pointer-events: none;
  z-index: 999;
  overflow: hidden;
}
.confetti-particle {
  position: absolute;
  top: 50%;
  left: 50%;
  width: 10px;
  height: 10px;
  background: var(--color);
  border-radius: 2px;
  transform-origin: center;
  animation: confettiFly 1s ease-out forwards;
  animation-delay: var(--delay);
}
@keyframes confettiFly {
  0%   { transform: translate(-50%, -50%) rotate(0deg) translateX(0); opacity: 1; }
  100% { transform: translate(-50%, -50%) rotate(calc(var(--angle) + 720deg)) translateX(calc(180px + 50px * sin(var(--angle)))); opacity: 0; }
}
```

> ⚠️ 일부 브라우저는 `sin()` CSS function 미지원 — fallback으로 단순 translate:

```css
/* fallback: angle-driven translateX만 */
@keyframes confettiFly {
  0%   { transform: translate(-50%, -50%) rotate(var(--angle)) translateX(0); opacity: 1; }
  100% { transform: translate(-50%, -50%) rotate(calc(var(--angle) + 720deg)) translateX(220px); opacity: 0; }
}
```

→ 후자 사용 (호환성).

- [ ] **Step 5: Run test + visual sanity**

```bash
npm test -- tests/unit/ui/level-up.spec.ts
npm run dev   # 브라우저에서 xp boundary 통과 시 confetti 1초 burst 확인
```

Expected: 4 PASS.

- [ ] **Step 6: Commit**

```bash
git add src/ui/rewards.ts src/styles/main.css tests/unit/ui/level-up.spec.ts
git commit -m "$(cat <<'EOF'
feat(v3.12 T8): level-up toast (.toast--levelup) + CSS-only confetti

30 particles fanned out via --angle CSS variable (no canvas-confetti
library — saves ~1.5kB gzip). 1s burst, auto-cleanup. Confetti fires on
level-up only; badge unlocks do not trigger confetti (avoid visual
inflation, spec §5.8).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 9: Streak milestone + fire-pulse on stats

**Files:**
- Modify: `src/ui/rewards.ts` (이미 T6에서 listener + sessionStorage flag 작성됨 — 본 task는 스타일만 검증)
- Modify: `src/ui/handlers/stats.ts:96-110` (`hydrateStatGrid`에 `streakPulsePending` consume 추가)
- Modify: `src/styles/main.css` (`.toast--streak` + `.fire-pulse`)
- Test: `tests/unit/ui/streak-pulse.spec.ts`

- [ ] **Step 1: Write the failing test**

```ts
// tests/unit/ui/streak-pulse.spec.ts
import { describe, it, expect, beforeEach, vi, afterEach } from 'vitest';
import { mountRewards, __resetForTest } from '../../../src/ui/rewards';
import { hydrateStats } from '../../../src/ui/handlers/stats';
import { dispatch } from '../../../src/ui/events';
import { saveUser } from '../../../src/state/user';

beforeEach(() => {
  document.body.innerHTML = `
    <div id="modalRoot"></div>
    <span id="statStreak"></span>
    <span id="statAnswers"></span>
    <span id="statArticles"></span>
    <span id="statXp"></span>
    <span id="levelIcon"></span>
    <span id="levelName"></span>
    <span id="levelXpText"></span>
    <span id="xpProgressFill"></span>
    <div id="heatmapGrid"></div>
    <div id="badgesGrid"></div>
    <div id="categoryBreakdown"></div>
    <div id="growthSummary"></div>
  `;
  sessionStorage.clear();
  localStorage.clear();
  saveUser({ name: 'x', interests: [], onboardedAt: '', streak: 7, lastActiveDate: '', xp: 100, earnedBadges: {}, gamificationMigrated: true, schemaVersion: 2 });
  __resetForTest();
  mountRewards();
});

afterEach(() => vi.useRealTimers());

describe('streak-milestone', () => {
  it('streak-milestone event → sessionStorage flag set + 토스트', () => {
    dispatch('dg:reward:streak-milestone', { days: 7, at: Date.now() });
    expect(sessionStorage.getItem('dg:streakPulsePending')).toBe('7');
    expect(document.querySelectorAll('.toast--streak')).toHaveLength(1);
  });

  it('hydrateStats: pending flag 있으면 #statStreak.fire-pulse 1.5초 후 제거', () => {
    sessionStorage.setItem('dg:streakPulsePending', '7');
    vi.useFakeTimers();
    hydrateStats();
    expect(document.getElementById('statStreak')!.classList.contains('fire-pulse')).toBe(true);
    expect(sessionStorage.getItem('dg:streakPulsePending')).toBeNull();
    vi.advanceTimersByTime(1700);
    expect(document.getElementById('statStreak')!.classList.contains('fire-pulse')).toBe(false);
  });

  it('hydrateStats: pending 없으면 fire-pulse 추가 안 함', () => {
    hydrateStats();
    expect(document.getElementById('statStreak')!.classList.contains('fire-pulse')).toBe(false);
  });

  it('streak-milestone 토스트 3초 후 자동 제거', () => {
    vi.useFakeTimers();
    dispatch('dg:reward:streak-milestone', { days: 7, at: Date.now() });
    expect(document.querySelectorAll('.toast--streak')).toHaveLength(1);
    vi.advanceTimersByTime(3500);
    expect(document.querySelectorAll('.toast--streak')).toHaveLength(0);
  });

  it('hydrateStats fire-pulse 1회만 (재진입 시 추가 안 함)', () => {
    sessionStorage.setItem('dg:streakPulsePending', '7');
    hydrateStats();
    hydrateStats();  // pending 이미 consumed
    // 두 번째 호출 시 fire-pulse 추가 안 함 (이미 sessionStorage에 없음)
    // 첫 호출에서 add 됐으므로 1회 OK — 두 번째에서 또 add 안 됨 검증
    expect(sessionStorage.getItem('dg:streakPulsePending')).toBeNull();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
npm test -- tests/unit/ui/streak-pulse.spec.ts
```

Expected: 일부 FAIL (hydrateStats가 pending consume 안 함).

- [ ] **Step 3: Update hydrateStats — pending consume**

In `src/ui/handlers/stats.ts`, add to top of `hydrateStats` or `hydrateStatGrid`:

```ts
// stats.ts — hydrateStatGrid 함수 끝 (setText('statXp', ...) 다음)
function hydrateStatGrid(): void {
  // ... existing setText calls ...
  setText('statStreak', user?.streak ?? 0);
  setText('statAnswers', answers.length);
  setText('statArticles', scrapCount);
  setText('statXp', user?.xp ?? 0);

  // v3.12 T9 — streak milestone fire pulse (sessionStorage flag consume)
  const pending = sessionStorage.getItem('dg:streakPulsePending');
  if (pending) {
    const stat = document.getElementById('statStreak');
    if (stat) {
      stat.classList.add('fire-pulse');
      sessionStorage.removeItem('dg:streakPulsePending');
      setTimeout(() => stat.classList.remove('fire-pulse'), 1500);
    }
  }
}
```

- [ ] **Step 4: CSS — `.toast--streak` + `.fire-pulse`**

Append to `src/styles/main.css`:

```css
/* v3.12 T9 — Streak milestone toast + fire pulse on #statStreak */
.toast--streak {
  background: linear-gradient(135deg, #f97316 0%, #dc2626 100%);
  color: #fff;
  font-size: 1.05em;
}
.fire-pulse {
  position: relative;
}
.fire-pulse::after {
  content: '🔥';
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  pointer-events: none;
  animation: firePulse 0.5s ease-out 3;
}
@keyframes firePulse {
  0%   { transform: scale(0.5); opacity: 0; }
  50%  { transform: scale(1.5); opacity: 1; }
  100% { transform: scale(0.5); opacity: 0; }
}
```

- [ ] **Step 5: Run test**

```bash
npm test -- tests/unit/ui/streak-pulse.spec.ts
```

Expected: 5 PASS.

- [ ] **Step 6: Commit**

```bash
git add src/ui/handlers/stats.ts src/styles/main.css tests/unit/ui/streak-pulse.spec.ts
git commit -m "$(cat <<'EOF'
feat(v3.12 T9): streak milestone toast + fire-pulse on next stats entry

sessionStorage flag (dg:streakPulsePending) bridges the milestone toast
(home tab) to the next stats hydrate (3-pulse 🔥 ::after on #statStreak).
Flag consumed once — refresh loses it (toast was the primary signal).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 10: Badge unlock toast + detail modal

**Files:**
- Create: `src/ui/modals/badge-detail.ts`
- Modify: `src/ui/rewards.ts` (이미 T6에서 lazy import wired — 본 task는 modal 본체)
- Modify: `src/styles/main.css` (`.toast--badge` + `.badge-modal-*`)
- Test: `tests/unit/ui/badge-detail.spec.ts`

- [ ] **Step 1: Write the failing test**

```ts
// tests/unit/ui/badge-detail.spec.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { openBadgeDetail } from '../../../src/ui/modals/badge-detail';
import { saveUser } from '../../../src/state/user';

beforeEach(() => {
  localStorage.clear();
  document.body.innerHTML = '<div id="modalRoot"></div>';
});

describe('openBadgeDetail', () => {
  it('알 수 없는 badgeId → 모달 안 띄움 (silent)', () => {
    openBadgeDetail('unknown-99');
    expect(document.querySelectorAll('.dg-modal')).toHaveLength(0);
  });

  it('streak-3 → 모달 + icon/name/description 표시', () => {
    openBadgeDetail('streak-3');
    expect(document.querySelectorAll('.dg-modal')).toHaveLength(1);
    expect(document.querySelector('.badge-modal-icon')!.textContent).toBe('🔥');
    expect(document.querySelector('.badge-modal-name')!.textContent).toBe('첫 불씨');
    expect(document.querySelector('.badge-modal-desc')!.textContent).toBe('연속 3일 답변');
  });

  it('earned 뱃지 → unlock date 표시', () => {
    saveUser({ name: 'x', interests: [], onboardedAt: '', streak: 0, lastActiveDate: '', xp: 0, earnedBadges: { 'streak-3': new Date('2026-04-29').getTime() }, gamificationMigrated: true, schemaVersion: 2 });
    openBadgeDetail('streak-3');
    const date = document.querySelector('.badge-modal-date');
    expect(date?.textContent).toMatch(/2026-04-29/);
  });

  it('locked 뱃지 → unlock date 자리에 "달성 조건"', () => {
    openBadgeDetail('streak-365');  // 일반적으로 unearned
    const date = document.querySelector('.badge-modal-date');
    expect(date?.textContent ?? '').not.toMatch(/획득/);
  });

  it('XSS — badge name 이상 문자 escape 처리 (catalog 통제 데이터지만 방어적)', () => {
    // 데이터는 catalog 고정이라 XSS 직접 발생 불가능. 본 테스트는 escapeHtml 호출이
    // .badge-modal-name 안에 들어가는 것만 검증.
    openBadgeDetail('streak-3');
    const html = document.querySelector('.badge-modal-name')!.innerHTML;
    expect(html).not.toContain('<script>');
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
npm test -- tests/unit/ui/badge-detail.spec.ts
```

Expected: FAIL (modal not implemented).

- [ ] **Step 3: Implement badge-detail.ts**

```ts
// src/ui/modals/badge-detail.ts
import { openModal } from './shared';
import { findBadge } from '../../state/badgeCatalog';
import { loadUserData } from '../../state/user';
import { escapeHtml } from '../../utils/escapeHtml';

export function openBadgeDetail(badgeId: string): void {
  const def = findBadge(badgeId);
  if (!def) return;
  const u = loadUserData();
  const unlockedAt = u?.earnedBadges?.[badgeId];
  const dateText = unlockedAt
    ? `${new Date(unlockedAt).toISOString().slice(0, 10)} 획득`
    : `달성 조건: ${escapeHtml(def.description)}`;

  const bodyHtml = `
    <div class="badge-modal">
      <div class="badge-modal-icon">${escapeHtml(def.icon)}</div>
      <div class="badge-modal-name">${escapeHtml(def.name)}</div>
      <div class="badge-modal-desc">${escapeHtml(def.description)}</div>
      <div class="badge-modal-date">${dateText}</div>
    </div>
  `;
  openModal({ title: '뱃지', bodyHtml });
}
```

- [ ] **Step 4: CSS — `.toast--badge` + `.badge-modal-*`**

Append to `src/styles/main.css`:

```css
/* v3.12 T10 — Badge unlock toast + detail modal */
.toast--badge {
  background: linear-gradient(135deg, #a855f7 0%, #6366f1 100%);
  color: #fff;
}
.toast-detail {
  background: rgba(255, 255, 255, 0.2);
  border: 1px solid rgba(255, 255, 255, 0.4);
  color: #fff;
  padding: 4px 10px;
  border-radius: 6px;
  cursor: pointer;
  font-size: 0.9em;
  margin-left: 8px;
}
.toast-detail:hover { background: rgba(255, 255, 255, 0.3); }
.badge-modal {
  text-align: center;
  padding: 20px;
}
.badge-modal-icon {
  font-size: 4rem;
  margin-bottom: 12px;
}
.badge-modal-name {
  font-size: 1.4rem;
  font-weight: 700;
  margin-bottom: 8px;
}
.badge-modal-desc {
  color: var(--color-text-muted, #6b7280);
  margin-bottom: 16px;
}
.badge-modal-date {
  font-size: 0.9em;
  color: var(--color-text-muted, #6b7280);
}
```

- [ ] **Step 5: Run test**

```bash
npm test -- tests/unit/ui/badge-detail.spec.ts
```

Expected: 5 PASS.

- [ ] **Step 6: Commit**

```bash
git add src/ui/modals/badge-detail.ts src/styles/main.css tests/unit/ui/badge-detail.spec.ts
git commit -m "$(cat <<'EOF'
feat(v3.12 T10): badge unlock toast detail modal (lazy chunk)

Lazy-imported on toast .toast-detail click (rewards.ts already wired in
T6). Modal-system reuse (openModal({title, bodyHtml})). Earned badges
show unlock date (ISO YYYY-MM-DD); locked show "달성 조건: ...".

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Phase 4 — Silhouette Grid

### Task 11: hydrateBadges 갱신 — 5 카테고리 silhouette grid

**Files:**
- Modify: `src/ui/handlers/stats.ts:243-269` (`hydrateBadges` rewrite)
- Modify: `src/ui/handlers/stats.ts:74-81,310` (`levels[]` → `TIERS` + `getCurrentTier`)
- Modify: `src/styles/main.css` (`.badges-section` / `.badge--earned` / `.badge--locked` / `.badge-lock`)
- Test: `tests/unit/ui/hydrate-badges.spec.ts`

- [ ] **Step 1: Write the failing test**

```ts
// tests/unit/ui/hydrate-badges.spec.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { hydrateStats } from '../../../src/ui/handlers/stats';
import { saveUser } from '../../../src/state/user';
import { saveBriefings } from '../../../src/state/briefings';

const STATS_DOM = `
  <span id="statStreak"></span><span id="statAnswers"></span>
  <span id="statArticles"></span><span id="statXp"></span>
  <span id="levelIcon"></span><span id="levelName"></span>
  <span id="levelXpText"></span><span id="xpProgressFill"></span>
  <div id="heatmapGrid"></div>
  <div id="badgesGrid"></div>
  <div id="categoryBreakdown"></div>
  <div id="growthSummary"></div>
`;

beforeEach(() => {
  localStorage.clear();
  document.body.innerHTML = STATS_DOM;
});

describe('hydrateBadges (catalog 기반)', () => {
  it('빈 user (xp=0, no badges) → 18 grid + 0 earned', () => {
    saveUser({ name: 'x', interests: [], onboardedAt: '', streak: 0, lastActiveDate: '', xp: 0, earnedBadges: {}, gamificationMigrated: true, schemaVersion: 2 });
    hydrateStats();
    const grid = document.getElementById('badgesGrid')!;
    expect(grid.querySelectorAll('.badge')).toHaveLength(18);
    expect(grid.querySelectorAll('.badge--earned')).toHaveLength(0);
    expect(grid.querySelectorAll('.badge--locked')).toHaveLength(18);
  });

  it('count 헤더 — earned/총 18', () => {
    saveUser({ name: 'x', interests: [], onboardedAt: '', streak: 0, lastActiveDate: '', xp: 0, earnedBadges: { 'streak-3': 1700000000000, 'answers-1': 1700000000000 }, gamificationMigrated: true, schemaVersion: 2 });
    hydrateStats();
    const heading = document.querySelector('.badges-section h3');
    expect(heading?.textContent).toMatch(/2\s*\/\s*18/);
  });

  it('5 카테고리 grouping (Streak → Volume → Tier → Diversity → Engagement)', () => {
    saveUser({ name: 'x', interests: [], onboardedAt: '', streak: 0, lastActiveDate: '', xp: 0, earnedBadges: {}, gamificationMigrated: true, schemaVersion: 2 });
    hydrateStats();
    const headings = Array.from(document.querySelectorAll('.badges-category h4')).map(h => h.textContent);
    expect(headings).toEqual([
      expect.stringContaining('Streak'),
      expect.stringContaining('Volume'),
      expect.stringContaining('Tier'),
      expect.stringContaining('Diversity'),
      expect.stringContaining('Engagement'),
    ]);
  });

  it('locked 뱃지 — 🔒 overlay + data-tooltip 존재', () => {
    saveUser({ name: 'x', interests: [], onboardedAt: '', streak: 0, lastActiveDate: '', xp: 0, earnedBadges: {}, gamificationMigrated: true, schemaVersion: 2 });
    hydrateStats();
    const locked = document.querySelector<HTMLElement>('.badge--locked');
    expect(locked?.querySelector('.badge-lock')?.textContent).toBe('🔒');
    expect(locked?.dataset['tooltip']).toMatch(/달성 조건/);
  });

  it('earned 뱃지 — locked overlay 없음 + aria-label에 이름 포함', () => {
    saveUser({ name: 'x', interests: [], onboardedAt: '', streak: 5, lastActiveDate: '', xp: 0, earnedBadges: { 'streak-3': 1700000000000 }, gamificationMigrated: true, schemaVersion: 2 });
    hydrateStats();
    const earned = document.querySelector<HTMLElement>('.badge--earned');
    expect(earned).not.toBeNull();
    expect(earned?.querySelector('.badge-lock')).toBeNull();
    expect(earned?.getAttribute('aria-label')).toMatch(/첫 불씨/);
  });

  it('hydrateLevelCard: TIERS import — user.xp=200 → 새잎', () => {
    saveUser({ name: 'x', interests: [], onboardedAt: '', streak: 0, lastActiveDate: '', xp: 200, earnedBadges: {}, gamificationMigrated: true, schemaVersion: 2 });
    hydrateStats();
    expect(document.getElementById('levelName')!.textContent).toBe('새잎');
  });

  it('hydrateGrowthSummary: tierName 포함 (level 숫자 X)', () => {
    saveUser({ name: 'x', interests: [], onboardedAt: '', streak: 0, lastActiveDate: '', xp: 350, earnedBadges: {}, gamificationMigrated: true, schemaVersion: 2 });
    saveBriefings([]);
    hydrateStats();
    const summary = document.getElementById('growthSummary')!.textContent ?? '';
    expect(summary).toMatch(/나무|성장 요약/);
  });

  it('earned 뱃지 click → openModal (badge-detail)', async () => {
    saveUser({ name: 'x', interests: [], onboardedAt: '', streak: 5, lastActiveDate: '', xp: 0, earnedBadges: { 'streak-3': 1700000000000 }, gamificationMigrated: true, schemaVersion: 2 });
    document.body.insertAdjacentHTML('beforeend', '<div id="modalRoot"></div>');
    hydrateStats();
    const earned = document.querySelector<HTMLButtonElement>('.badge--earned');
    earned?.click();
    await new Promise(r => setTimeout(r, 50));  // lazy import
    expect(document.querySelector('.badge-modal')).not.toBeNull();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
npm test -- tests/unit/ui/hydrate-badges.spec.ts
```

Expected: 8 FAIL (현재 hydrateBadges는 6개 하드코딩).

- [ ] **Step 3: Replace levels[] with TIERS + replace hydrateBadges**

Update `src/ui/handlers/stats.ts`:

```ts
// 상단 import 추가
import { TIERS, getCurrentTier } from '../../state/leveling';
import { BADGE_CATALOG } from '../../state/badgeCatalog';
import { escapeHtml } from '../../utils/escapeHtml';

// hydrateLevelCard:74-81 — `const levels = [...]` 줄 제거하고 TIERS 사용
function hydrateLevelCard(): void {
  const user = getCachedUser();
  const icon = document.getElementById('levelIcon');
  const name = document.getElementById('levelName');
  const xpText = document.getElementById('levelXpText');
  const fill = document.getElementById('xpProgressFill');
  if (!user) return;

  const current = getCurrentTier(user.xp);
  const next = TIERS.find(l => l.thresh > user.xp);
  if (icon) icon.textContent = current.icon;
  if (name) name.textContent = current.name;
  if (next) {
    const pct = ((user.xp - current.thresh) / (next.thresh - current.thresh)) * 100;
    if (xpText) xpText.textContent = `${user.xp} / ${next.thresh} XP`;
    if (fill) fill.style.width = `${Math.min(100, Math.max(0, pct))}%`;
  } else {
    if (xpText) xpText.textContent = `${user.xp} XP · MAX`;
    if (fill) fill.style.width = '100%';
  }
}

// hydrateGrowthSummary:300-311 — user.level 제거, getCurrentTier 사용
function hydrateGrowthSummary(): void {
  const el = document.getElementById('growthSummary');
  const user = getCachedUser();
  const answers = loadAnswers();
  if (!el) return;
  if (!user || answers.length === 0) {
    el.textContent = '기록을 쌓아가면 이곳에 성장 요약이 표시됩니다.';
    return;
  }
  const recent = answers.slice(0, 7).length;
  const tierName = getCurrentTier(user.xp).name;
  el.textContent = `최근 ${recent}개의 기록으로 ${tierName} 단계까지 도달했어요. 오늘도 한 걸음 더 나아가 볼까요?`;
}

// hydrateBadges:243-269 — catalog 기반 rewrite
const CATEGORY_LABELS: Record<string, string> = {
  streak:      '🔥 Streak',
  volume:      '📚 Volume',
  tier:        '🌳 Tier',
  diversity:   '🎨 Diversity',
  engagement:  '✏️ Engagement',
};
const CATEGORY_ORDER = ['streak', 'volume', 'tier', 'diversity', 'engagement'] as const;

function hydrateBadges(): void {
  const wrap = document.getElementById('badgesGrid');
  const user = getCachedUser();
  if (!wrap) return;
  wrap.replaceChildren();
  if (!user) return;

  const earned = new Set(Object.keys(user.earnedBadges ?? {}));
  const total = BADGE_CATALOG.length;

  // section heading
  const section = document.createElement('div');
  section.className = 'badges-section';
  const heading = document.createElement('h3');
  heading.textContent = `🏆 뱃지 (${earned.size} / ${total})`;
  section.append(heading);

  for (const cat of CATEGORY_ORDER) {
    const list = BADGE_CATALOG.filter(b => b.category === cat);
    if (list.length === 0) continue;
    const catWrap = document.createElement('div');
    catWrap.className = 'badges-category';
    const h4 = document.createElement('h4');
    h4.textContent = CATEGORY_LABELS[cat] ?? cat;
    const grid = document.createElement('div');
    grid.className = 'badges-grid';
    for (const def of list) {
      const isEarned = earned.has(def.id);
      const btn = document.createElement('button');
      btn.type = 'button';
      btn.className = `badge ${isEarned ? 'badge--earned' : 'badge--locked'}`;
      btn.setAttribute('aria-label', isEarned ? `${def.name} — ${def.description}` : '잠긴 뱃지');
      if (!isEarned) btn.dataset['tooltip'] = `달성 조건: ${def.description}`;
      btn.dataset['badgeId'] = def.id;
      const iconEl = document.createElement('span');
      iconEl.className = 'badge-icon';
      iconEl.textContent = def.icon;
      const nameEl = document.createElement('span');
      nameEl.className = 'badge-name';
      nameEl.textContent = def.name;
      btn.append(iconEl, nameEl);
      if (!isEarned) {
        const lock = document.createElement('span');
        lock.className = 'badge-lock';
        lock.textContent = '🔒';
        btn.append(lock);
      }
      btn.addEventListener('click', async () => {
        const { openBadgeDetail } = await import('../modals/badge-detail');
        openBadgeDetail(def.id);
      });
      grid.append(btn);
    }
    catWrap.append(h4, grid);
    section.append(catWrap);
  }
  wrap.append(section);
}
```

- [ ] **Step 4: CSS — `.badges-section` / `.badge--earned` / `.badge--locked` / `.badge-lock`**

Append to `src/styles/main.css`:

```css
/* v3.12 T11 — Silhouette grid */
.badges-section h3 { margin: 12px 0; font-size: 1.1rem; }
.badges-category h4 { margin: 8px 0 6px; font-size: 0.95rem; color: var(--color-text-muted, #6b7280); }
.badges-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(72px, 1fr));
  gap: 8px;
  margin-bottom: 10px;
}
.badge {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 10px 4px;
  background: var(--color-bg-card, #fff);
  border: 1px solid var(--color-border, #e5e7eb);
  border-radius: 8px;
  cursor: pointer;
  font-size: 0.75rem;
  text-align: center;
  transition: transform 0.15s ease;
}
.badge:hover { transform: scale(1.04); }
.badge-icon { font-size: 1.5rem; line-height: 1; margin-bottom: 4px; }
.badge-name { font-size: 0.7rem; color: var(--color-text, #111); }
.badge--locked .badge-icon { filter: grayscale(1); opacity: 0.4; }
.badge--locked .badge-name { color: var(--color-text-muted, #6b7280); }
.badge-lock {
  position: absolute;
  top: 4px;
  right: 4px;
  font-size: 0.7rem;
  opacity: 0.7;
}
```

- [ ] **Step 5: Run test**

```bash
npm test -- tests/unit/ui/hydrate-badges.spec.ts
npx tsc --noEmit  # T2 임시 패치 줄들도 정식 정리됐는지 확인
```

Expected: 8 PASS, tsc 0 error.

- [ ] **Step 6: Commit**

```bash
git add src/ui/handlers/stats.ts src/styles/main.css tests/unit/ui/hydrate-badges.spec.ts
git commit -m "$(cat <<'EOF'
feat(v3.12 T11): silhouette grid — 18 badges in 5 category groups

hydrateBadges replaced (was 6 hardcoded). Locked = grayscale + 🔒
overlay + data-tooltip; earned = full color + click → badge-detail modal
(lazy import). Category order: Streak → Volume → Tier → Diversity →
Engagement (rarity ascending). hydrateLevelCard now imports TIERS
(removes levels[] hardcode). hydrateGrowthSummary uses getCurrentTier
(removes user.level reference).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 12: Tooltip + 키보드

**Files:**
- Modify: `src/ui/handlers/stats.ts` (locked badge tap → tooltip toggle)
- Modify: `src/styles/main.css` (`.badge-tooltip` + `.badge--locked.show-tooltip::after`)
- Test: `tests/unit/ui/badge-tooltip.spec.ts`

> Critical Review C4: dismiss = 3초 auto-hide (단순한 쪽 선택 — outside-click도 동일하지만 timeout이 코드 더 적음).

- [ ] **Step 1: Write the failing test**

```ts
// tests/unit/ui/badge-tooltip.spec.ts
import { describe, it, expect, beforeEach, vi, afterEach } from 'vitest';
import { hydrateStats } from '../../../src/ui/handlers/stats';
import { saveUser } from '../../../src/state/user';

const STATS_DOM = `
  <span id="statStreak"></span><span id="statAnswers"></span>
  <span id="statArticles"></span><span id="statXp"></span>
  <span id="levelIcon"></span><span id="levelName"></span>
  <span id="levelXpText"></span><span id="xpProgressFill"></span>
  <div id="heatmapGrid"></div><div id="badgesGrid"></div>
  <div id="categoryBreakdown"></div><div id="growthSummary"></div>
  <div id="modalRoot"></div>
`;

beforeEach(() => {
  localStorage.clear();
  document.body.innerHTML = STATS_DOM;
  saveUser({ name: 'x', interests: [], onboardedAt: '', streak: 0, lastActiveDate: '', xp: 0, earnedBadges: {}, gamificationMigrated: true, schemaVersion: 2 });
});

afterEach(() => vi.useRealTimers());

describe('badge tooltip', () => {
  it('locked 뱃지 click → .show-tooltip 클래스 add', () => {
    hydrateStats();
    const locked = document.querySelector<HTMLButtonElement>('.badge--locked')!;
    locked.click();
    expect(locked.classList.contains('show-tooltip')).toBe(true);
  });

  it('locked 뱃지 → 3초 후 .show-tooltip 자동 제거', () => {
    vi.useFakeTimers();
    hydrateStats();
    const locked = document.querySelector<HTMLButtonElement>('.badge--locked')!;
    locked.click();
    expect(locked.classList.contains('show-tooltip')).toBe(true);
    vi.advanceTimersByTime(3500);
    expect(locked.classList.contains('show-tooltip')).toBe(false);
  });

  it('Enter 키 → tooltip toggle', () => {
    hydrateStats();
    const locked = document.querySelector<HTMLButtonElement>('.badge--locked')!;
    locked.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter' }));
    expect(locked.classList.contains('show-tooltip')).toBe(true);
  });

  it('Space 키 → tooltip toggle', () => {
    hydrateStats();
    const locked = document.querySelector<HTMLButtonElement>('.badge--locked')!;
    locked.dispatchEvent(new KeyboardEvent('keydown', { key: ' ' }));
    expect(locked.classList.contains('show-tooltip')).toBe(true);
  });

  it('aria-describedby — locked 뱃지 tooltip id 연결 (button 자체에 data-tooltip)', () => {
    hydrateStats();
    const locked = document.querySelector<HTMLButtonElement>('.badge--locked')!;
    expect(locked.dataset['tooltip']).toMatch(/달성 조건/);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
npm test -- tests/unit/ui/badge-tooltip.spec.ts
```

Expected: FAIL.

- [ ] **Step 3: Update hydrateBadges — locked tap toggle**

In `src/ui/handlers/stats.ts` `hydrateBadges`, replace the click handler block with:

```ts
// hydrateBadges 안 — locked vs earned 분기
btn.addEventListener('click', async () => {
  if (isEarned) {
    const { openBadgeDetail } = await import('../modals/badge-detail');
    openBadgeDetail(def.id);
  } else {
    showTooltip(btn);
  }
});
btn.addEventListener('keydown', (e) => {
  if (e.key === 'Enter' || e.key === ' ') {
    e.preventDefault();
    btn.click();
  }
});
```

Add helper at module scope:

```ts
function showTooltip(btn: HTMLButtonElement): void {
  // close previously open tooltip(s)
  document.querySelectorAll('.badge.show-tooltip').forEach(el => el.classList.remove('show-tooltip'));
  btn.classList.add('show-tooltip');
  setTimeout(() => btn.classList.remove('show-tooltip'), 3000);
}
```

- [ ] **Step 4: CSS — `.badge.show-tooltip::after`**

Append to `src/styles/main.css`:

```css
/* v3.12 T12 — Locked badge tooltip */
.badge.show-tooltip::after {
  content: attr(data-tooltip);
  position: absolute;
  bottom: calc(100% + 6px);
  left: 50%;
  transform: translateX(-50%);
  padding: 6px 10px;
  background: var(--color-tooltip-bg, #1f2937);
  color: #fff;
  font-size: 0.75rem;
  border-radius: 4px;
  white-space: nowrap;
  z-index: 10;
  pointer-events: none;
  animation: tooltipIn 0.15s ease-out;
}
@keyframes tooltipIn {
  from { opacity: 0; transform: translateX(-50%) translateY(4px); }
  to   { opacity: 1; transform: translateX(-50%) translateY(0); }
}
```

- [ ] **Step 5: Run test**

```bash
npm test -- tests/unit/ui/badge-tooltip.spec.ts
```

Expected: 5 PASS.

- [ ] **Step 6: Commit**

```bash
git add src/ui/handlers/stats.ts src/styles/main.css tests/unit/ui/badge-tooltip.spec.ts
git commit -m "$(cat <<'EOF'
feat(v3.12 T12): locked badge tooltip — tap toggle + 3s auto-hide + keyboard

3-second auto-dismiss (Critical Review C4 — simpler than outside-click).
Enter/Space toggle for keyboard accessibility. Existing earned-badge
click flow (modal) preserved via isEarned branch.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Phase 5 — Migration UX

### Task 13: 환영 모달 + backfill (idempotent)

**Files:**
- Create: `src/ui/modals/welcome-gamification.ts`
- Modify: `src/ui/handlers/home.ts` (또는 `src/ui/nav.ts:56` `dg:nav:tab-changed` listener) — home tab 활성화 hook
- Test: `tests/unit/ui/welcome-gamification.spec.ts`

- [ ] **Step 1: Write the failing test**

```ts
// tests/unit/ui/welcome-gamification.spec.ts
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { maybeShowWelcomeGamification } from '../../../src/ui/modals/welcome-gamification';
import { saveUser, loadUserData } from '../../../src/state/user';
import { saveAnswers } from '../../../src/state/persistence';

beforeEach(() => {
  localStorage.clear();
  document.body.innerHTML = '<div id="modalRoot"></div>';
});

describe('maybeShowWelcomeGamification', () => {
  it('answers=0 → 모달 안 띄움', async () => {
    saveUser({ name: 'x', interests: [], onboardedAt: '', streak: 0, lastActiveDate: '', xp: 0, earnedBadges: {}, gamificationMigrated: false, schemaVersion: 2 });
    saveAnswers([]);
    await maybeShowWelcomeGamification();
    expect(document.querySelector('.dg-modal')).toBeNull();
  });

  it('gamificationMigrated=true → 모달 안 띄움 (idempotent)', async () => {
    saveUser({ name: 'x', interests: ['AI'], onboardedAt: '', streak: 5, lastActiveDate: '', xp: 100, earnedBadges: {}, gamificationMigrated: true, schemaVersion: 2 });
    saveAnswers([{ id: '1', questionId: 'q1', text: '...', authorId: 'self', createdAt: '2026-04-01', schemaVersion: 1 }]);
    await maybeShowWelcomeGamification();
    expect(document.querySelector('.dg-modal')).toBeNull();
  });

  it('첫 진입 (answers>=1, !gamificationMigrated) → backfill + 모달 표시', async () => {
    saveUser({ name: 'x', interests: ['AI'], onboardedAt: '', streak: 5, lastActiveDate: '', xp: 350, earnedBadges: {}, gamificationMigrated: false, schemaVersion: 2 });
    // answers 10개 → answers-10 + answers-1 + tier-3-tree (xp 350) + streak-3 unlock 후보
    const answers = Array.from({ length: 10 }, (_, i) => ({
      id: `${i}`, questionId: 'q', text: 'a', authorId: 'self', createdAt: '2026-04-01', schemaVersion: 1 as const,
    }));
    saveAnswers(answers);
    await maybeShowWelcomeGamification();
    expect(document.querySelector('.dg-modal')).not.toBeNull();
    const u = loadUserData()!;
    expect(Object.keys(u.earnedBadges).length).toBeGreaterThan(0);
    expect(u.earnedBadges['streak-3']).toBeDefined();
    expect(u.earnedBadges['answers-10']).toBeDefined();
    expect(u.earnedBadges['tier-3-tree']).toBeDefined();
  });

  it('모달 닫기 → gamificationMigrated=true saveUser', async () => {
    saveUser({ name: 'x', interests: ['AI'], onboardedAt: '', streak: 5, lastActiveDate: '', xp: 100, earnedBadges: {}, gamificationMigrated: false, schemaVersion: 2 });
    saveAnswers([{ id: '1', questionId: 'q', text: 'a', authorId: 'self', createdAt: '2026-04-01', schemaVersion: 1 }]);
    await maybeShowWelcomeGamification();
    document.querySelector<HTMLButtonElement>('.dg-modal-close')?.click();
    await new Promise(r => setTimeout(r, 50));
    expect(loadUserData()!.gamificationMigrated).toBe(true);
  });

  it('두 번째 호출 (gamificationMigrated=true) → no-op', async () => {
    saveUser({ name: 'x', interests: ['AI'], onboardedAt: '', streak: 5, lastActiveDate: '', xp: 100, earnedBadges: {}, gamificationMigrated: false, schemaVersion: 2 });
    saveAnswers([{ id: '1', questionId: 'q', text: 'a', authorId: 'self', createdAt: '2026-04-01', schemaVersion: 1 }]);
    await maybeShowWelcomeGamification();
    document.querySelector<HTMLButtonElement>('.dg-modal-close')?.click();
    await new Promise(r => setTimeout(r, 50));
    document.body.innerHTML = '<div id="modalRoot"></div>';
    await maybeShowWelcomeGamification();  // 두 번째 호출
    expect(document.querySelector('.dg-modal')).toBeNull();
  });

  it('backfill 중 throw 시 → schema 보존 + 모달은 그래도 표시 (graceful)', async () => {
    saveUser({ name: 'x', interests: ['AI'], onboardedAt: '', streak: 5, lastActiveDate: '', xp: 100, earnedBadges: {}, gamificationMigrated: false, schemaVersion: 2 });
    saveAnswers([{ id: '1', questionId: 'q', text: 'a', authorId: 'self', createdAt: '2026-04-01', schemaVersion: 1 }]);
    const spy = vi.spyOn(Storage.prototype, 'setItem').mockImplementationOnce(() => { throw new Error('quota'); });
    await maybeShowWelcomeGamification();
    expect(document.querySelector('.dg-modal')).not.toBeNull();
    spy.mockRestore();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

```bash
npm test -- tests/unit/ui/welcome-gamification.spec.ts
```

Expected: FAIL.

- [ ] **Step 3: Implement welcome-gamification.ts**

```ts
// src/ui/modals/welcome-gamification.ts
import { openModal } from './shared';
import { loadUserData, saveUser } from '../../state/user';
import { takeSnapshot } from '../../state/achievements';
import { BADGE_CATALOG, findBadge } from '../../state/badgeCatalog';
import { escapeHtml } from '../../utils/escapeHtml';
import { loadAnswers } from '../../state/persistence';

/**
 * Home tab 활성화 시 호출. answers >= 1 && !gamificationMigrated → backfill + 환영 모달.
 * Idempotent — 모달 닫을 때 gamificationMigrated=true saveUser. 다시 호출되도 no-op.
 */
export async function maybeShowWelcomeGamification(): Promise<void> {
  const u = loadUserData();
  if (!u) return;
  if (u.gamificationMigrated) return;
  const answers = loadAnswers();
  if (answers.length === 0) return;

  // backfill — try/catch graceful (Risk 1)
  // P2-1: backfill 부분 실패 시 다음 진입에서 재시도되도록 succeeded flag로 onClose 분기.
  let backfilledIds: string[] = [];
  let backfillSucceeded = false;
  try {
    const snap = takeSnapshot();
    const now = Date.now();
    for (const def of BADGE_CATALOG) {
      if (def.predicate(snap) && !u.earnedBadges[def.id]) {
        u.earnedBadges[def.id] = now;
        backfilledIds.push(def.id);
      }
    }
    if (backfilledIds.length > 0) {
      saveUser(u);  // throw 시 catch로 — backfillSucceeded false 유지
    }
    backfillSucceeded = true;
  } catch {
    // backfill 실패 — schema 보존, 빈 컬렉션으로 시작 (영구 데이터 손실 없음).
    // gamificationMigrated는 false 유지 → 다음 home 진입 시 재시도.
    backfilledIds = [];
    backfillSucceeded = false;
  }

  const N = backfilledIds.length;
  const miniGrid = backfilledIds
    .map(id => findBadge(id))
    .filter((b): b is NonNullable<ReturnType<typeof findBadge>> => !!b)
    .map(def => `
      <button type="button" class="badge badge--earned" aria-label="${escapeHtml(def.name)}">
        <span class="badge-icon">${escapeHtml(def.icon)}</span>
        <span class="badge-name">${escapeHtml(def.name)}</span>
      </button>
    `).join('');

  const bodyHtml = `
    <div class="welcome-game">
      <p>이미 달성한 <strong>${N}개</strong> 뱃지가 컬렉션에 추가되었어요!</p>
      <div class="badges-grid">${miniGrid}</div>
      <button type="button" class="btn btn-primary" id="goStatsBtn">stats 탭에서 보기</button>
    </div>
  `;

  openModal({
    title: '🎉 새 게임화 기능',
    bodyHtml,
    onClose: () => {
      const u2 = loadUserData();
      if (!u2 || u2.gamificationMigrated) return;
      // P2-1: backfill 부분 실패 시 flag set 안 함 → 다음 home 진입에서 재시도.
      if (!backfillSucceeded) return;
      u2.gamificationMigrated = true;
      try { saveUser(u2); } catch { /* graceful */ }
    },
  });

  // "stats에서 보기" 버튼 — 동적 wiring
  document.getElementById('goStatsBtn')?.addEventListener('click', async () => {
    const { switchTab } = await import('../nav');
    switchTab('stats');
  });
}
```

- [ ] **Step 4: Wire home tab activation hook**

In `src/ui/handlers/home.ts` (또는 `mountStatsHandlers` 패턴 따라 별도 mount 함수):

Find the existing `mountHomeHandlers` (또는 `dg:nav:tab-changed` listener — `mountStatsHandlers`와 같은 위치) and add:

```ts
// home.ts handler 안에 dg:nav:tab-changed listener 추가
import { on } from '../events';

// mountHomeHandlers / mountHomeListeners 안에 추가
on('dg:nav:tab-changed', async ({ tab }) => {
  if (tab === 'home') {
    const { maybeShowWelcomeGamification } = await import('../modals/welcome-gamification');
    void maybeShowWelcomeGamification();
  }
});
```

> **주의**: home.ts에 이미 `mountHomeHandlers` 등 mount 함수가 있을 가능성 높음. 없으면 신규 함수 신설 후 `src/main.ts`에서 호출. **plan 실행 시점에 home.ts 구조 확인 후 적절한 위치에 추가**.

- [ ] **Step 5: Run test**

```bash
npm test -- tests/unit/ui/welcome-gamification.spec.ts
```

Expected: 6 PASS.

- [ ] **Step 6: Commit**

```bash
git add src/ui/modals/welcome-gamification.ts src/ui/handlers/home.ts tests/unit/ui/welcome-gamification.spec.ts
git commit -m "$(cat <<'EOF'
feat(v3.12 T13): welcome modal + idempotent backfill on home tab activation

Triggered by dg:nav:tab-changed { tab: 'home' }. Conditions:
!gamificationMigrated && answers.length >= 1. Backfill in try/catch —
schema preserved on quota fail (Risk 1). Modal close sets
gamificationMigrated=true (single-write).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Phase 6 — Polish & Verification

### Task 14: prefers-reduced-motion

**Files:**
- Modify: `src/styles/main.css` (append `@media (prefers-reduced-motion: reduce)` block)
- Test: `tests/unit/ui/reduced-motion.spec.ts`

> 본 task는 spec compliance review만 (CSS-only, closure 없음).

- [ ] **Step 1: Write the failing test (matchMedia mock)**

```ts
// tests/unit/ui/reduced-motion.spec.ts
import { describe, it, expect, beforeEach } from 'vitest';

beforeEach(() => {
  // jsdom doesn't support prefers-reduced-motion CSS; we test the rule exists
  document.body.innerHTML = '<style id="appcss"></style>';
});

describe('prefers-reduced-motion CSS rule (link to main.css existence)', () => {
  it('main.css 안에 @media (prefers-reduced-motion: reduce) 블록 존재', async () => {
    const css = await import('../../../src/styles/main.css?inline').catch(() => null);
    if (css === null) {
      // Vite raw 미지원 환경: skip with explicit pass
      expect(true).toBe(true);
      return;
    }
    const text = (css.default ?? '') as string;
    expect(text).toMatch(/@media\s*\(\s*prefers-reduced-motion\s*:\s*reduce\s*\)/);
    expect(text).toMatch(/\.confetti-particle\s*\{[^}]*display:\s*none/);
    expect(text).toMatch(/\.xp-float[^{}]*\{[^}]*animation:\s*none/);
  });
});
```

> **참고**: Vitest jsdom은 matchMedia + 실 CSS 평가를 안 함. 본 spec은 main.css에 rule이 있는지만 확인. visual 검증은 Step 5 dev server에서 OS 토글로 수동.

- [ ] **Step 2: Run test to verify it fails**

```bash
npm test -- tests/unit/ui/reduced-motion.spec.ts
```

Expected: FAIL (rule 미존재).

- [ ] **Step 3: Append reduced-motion media query**

Append to `src/styles/main.css`:

```css
/* v3.12 T14 — prefers-reduced-motion */
@media (prefers-reduced-motion: reduce) {
  .xp-float,
  .fire-pulse::after,
  .badge-pulse,
  .toast,
  .toast--levelup,
  .toast--streak,
  .toast--badge {
    animation: none !important;
    transition: none !important;
    opacity: 1 !important;
  }
  .confetti-particle {
    display: none !important;
  }
  .badge.show-tooltip::after {
    animation: none !important;
  }
}
```

- [ ] **Step 4: Run test + manual visual check**

```bash
npm test -- tests/unit/ui/reduced-motion.spec.ts
npm run dev   # macOS: 시스템 환경설정 → 손쉬운 사용 → 디스플레이 → "동작 줄이기" 활성화 후 답변 제출 → 토스트 instant + confetti 안 보임 검증
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/styles/main.css tests/unit/ui/reduced-motion.spec.ts
git commit -m "$(cat <<'EOF'
feat(v3.12 T14): prefers-reduced-motion — disable animations, hide confetti

Toast text remains (information delivery preserved); only motion is
suppressed. Confetti display:none (no residual particles).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 15: Playwright smoke +2 + spec/plan retro sync

**Files:**
- Create: `tests/smoke/v3.12-game-feedback.spec.ts`
- Modify: `docs/superpowers/specs/2026-04-29-v3.12-game-feedback-badges-design.md` (retro sync if 본 plan 실행 중 spec과 다른 결정이 발생했다면)
- Modify: `docs/superpowers/plans/2026-04-30-v3.12-game-feedback-badges-plan.md` (자기 자신 — retro sync)

- [ ] **Step 1: Write the smoke spec**

```ts
// tests/smoke/v3.12-game-feedback.spec.ts
import { test, expect } from '@playwright/test';

test.describe('v3.12 Game Feedback + Badges', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/');
    // 온보딩 1회 끝낸 user state seed (기존 smoke 패턴 재사용)
    await page.evaluate(() => {
      localStorage.setItem('user', JSON.stringify({
        name: 'Smoke', interests: ['AI'], onboardedAt: new Date().toISOString(),
        streak: 0, lastActiveDate: '', xp: 0,
        earnedBadges: {}, gamificationMigrated: true, schemaVersion: 2,
      }));
    });
    await page.reload();
  });

  test('답변 제출 → toast--badge (첫 답변) + xp-float visible', async ({ page }) => {
    // 홈 탭 답변 입력 (셀렉터는 v3.11 smoke 패턴 따라)
    await page.locator('#answerInput').fill('스모크 테스트 답변입니다.');
    await page.locator('button[type="submit"], #submitAnswerBtn').first().click();

    // +XP floating
    await expect(page.locator('.xp-float')).toBeVisible({ timeout: 2000 });
    // answers-1 badge unlock 토스트
    await expect(page.locator('.toast--badge')).toBeVisible({ timeout: 2000 });
    // 토스트 안에 "첫 답변" 또는 그에 준하는 텍스트
    await expect(page.locator('.toast--badge')).toContainText(/첫 답변|뱃지 획득/);
  });

  test('stats 탭 → silhouette grid 5 카테고리 + locked 회색', async ({ page }) => {
    await page.locator('[data-tab="stats"], button:has-text("stats")').first().click();
    // 5 카테고리 heading
    await expect(page.locator('.badges-category h4')).toHaveCount(5);
    // 18 뱃지 모두 grid에
    await expect(page.locator('#badgesGrid .badge')).toHaveCount(18);
    // locked 18 (빈 user)
    await expect(page.locator('#badgesGrid .badge--locked')).toHaveCount(18);
    // 첫 locked 뱃지에 🔒 overlay
    await expect(page.locator('.badge--locked .badge-lock').first()).toContainText('🔒');
  });
});
```

- [ ] **Step 2: Run smoke**

```bash
npm run build
npx playwright test tests/smoke/v3.12-game-feedback.spec.ts --reporter=list
```

Expected: 2 PASS.

- [ ] **Step 3: Run full unit + smoke suite**

```bash
npm test -- tests/unit/
npx playwright test --reporter=list
```

Expected: 453 (existing) + 95 (new) = ~548 vitest PASS · 87 + 2 = 89 Playwright PASS.

- [ ] **Step 4: Bundle size check**

```bash
npm run build
ls -la dist/assets/*.js | awk '{print $5, $9}'  # gzip은 빌드 산출물 옆 .gz 파일 또는 별도 스크립트
```

Expected: main JS gzip +1.5 kB ±0.2 kB vs v3.11 baseline.

- [ ] **Step 5: spec/plan retro sync (drift가 있다면)**

본 plan 실행 중 spec과 다른 결정이 발생했다면, spec과 plan 양쪽에 retroactive 동기화 줄을 추가:

- spec 마지막에 `## §9 Retroactive Sync (T15에서 추가됨)` 섹션 신설 후 변경 항목 bullet 작성
- plan에 동일한 섹션 추가 (이 파일 마지막)

drift 없으면 step 5 skip.

- [ ] **Step 6: Commit**

```bash
git add tests/smoke/v3.12-game-feedback.spec.ts docs/superpowers/specs/2026-04-29-v3.12-game-feedback-badges-design.md docs/superpowers/plans/2026-04-30-v3.12-game-feedback-badges-plan.md
git commit -m "$(cat <<'EOF'
test(v3.12 T15): playwright smoke +2 (badge toast + silhouette grid) + retro sync

Smoke covers the two end-to-end paths the unit tests can't reach:
1. Answer submit → xp-float + toast--badge (first-answer-1 unlock)
2. Stats tab → 5-category silhouette grid + 18 locked badges

Retroactive spec/plan sync if any decision diverged during execution.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## §7 Final Verification (배포 직전)

- [ ] **Step 1: 전체 테스트 통과 (N=5 stability)**

```bash
for i in 1 2 3 4 5; do npm test -- tests/unit/ || break; done
npx playwright test --reporter=list
```

Expected: 신규 추가된 모든 spec 5/5 PASS.

- [ ] **Step 2: 독립 표준 리뷰 (CLAUDE.md 절대 규칙)**

`superpowers:code-reviewer` (sonnet) 디스패치 — 본 사이클 13~17 commits 대상.

- 분류: P0 = 0 / P1 = in-cycle / P2 = v3.13 backlog
- review fix는 `chore(v3.12 review): ...` 또는 `fix(v3.12 review): ...` commit으로 추가

- [ ] **Step 3: Bundle size 측정 (DoD ±0.2 kB)**

```bash
npm run build
# v3.11 baseline gzip JS 와 비교 (이전 사이클 commit 대비 diff)
```

- [ ] **Step 4: main merge + Firebase deploy**

사용자 승인 후:

```bash
git checkout main
git merge --no-ff feat/v3.12-game-feedback
firebase deploy
```

- [ ] **Step 5: Memory 갱신**

`/Users/hayden/.claude/projects/.../memory/project_daily_growth_v312.md` 신규 작성:
- 배포 일자, main merge SHA, Firebase live 표시
- task별 commit count, vitest/Playwright 카운트
- 독립 리뷰 결과 (P0/P1/P2)
- v3.13 forward reference (mission/challenge 구조)

`MEMORY.md` 인덱스에 항목 추가.

---

## §8 Self-Review (plan 작성자 점검)

본 plan을 spec과 대조해 누락이나 placeholder가 없는지 직접 점검:

| Spec 섹션 | Plan task | OK |
|---|---|---|
| §1.2A 즉시 보상 4종 | T7 (xp) / T8 (level) / T9 (streak) / T10 (badge) | ✅ |
| §1.2B 18 뱃지 catalog | T5 + T11 silhouette grid | ✅ |
| §1.2C Tier 단일화 | T1 (TIERS) + T11 (levels[] 제거) + T2 (level 필드 drop) | ✅ |
| §2.4 진입점 3곳 | T4 wiring (recordDailyAnswer / toggleScrap / saveMemo) | ✅ |
| §2.4 saveUser throw 차단 | T4 step 1 spec + sweep-wiring spec 검증 | ✅ |
| §3 badge predicates 18개 | T5 step 1에 각 predicate 1개씩 spec | ✅ |
| §4.1 schema v2 | T2 (drop level + earnedBadges/gamificationMigrated/schemaVersion) | ✅ |
| §4.2 lazy migrate | T2 step 3 (loadUserData inline 검사) | ✅ |
| §4.3 환영 모달 + backfill | T13 (idempotent + try/catch graceful) | ✅ |
| §5.1 4 이벤트 매핑 | T3 step 1 (events.ts EventMap 추가) + T6~T10 listener | ✅ |
| §5.2 xp-float fallback | T7 step 3 (`isVisible` 체크 + 우상단 fallback) | ✅ |
| §5.3 CSS confetti | T8 step 4 (canvas-confetti 미도입, 30 particles) | ✅ |
| §5.4 streak fire pulse | T9 (sessionStorage flag + hydrateStats consume) | ✅ |
| §5.5 badge modal | T10 (badge-detail.ts lazy import) | ✅ |
| §5.6 환영 모달 unlocked mini-grid | T13 (backfilledIds → mini-grid) | ✅ |
| §5.7 silhouette grid | T11 (5 카테고리 + 🔒 overlay + tooltip) | ✅ |
| §5.8 큐 max 3 | T6 (`MAX_VISIBLE = 3`) | ✅ |
| §5.9 prefers-reduced-motion | T14 | ✅ |
| §6.2 per-task 2단계 review | 각 task DoD에 명시 — T8/T14는 spec compliance만 | ✅ |
| §6.3 DoD 모든 항목 | T15 verification + §7 Final Verification | ✅ |

**Type/이름 consistency 점검**:
- `Snapshot` / `GameEvent` / `BadgeDef` 타입 모두 동일 시그니처 (T3 → T5)
- `runSweep` / `takeSnapshot` / `detectEvents` / `persistUnlocks` / `emitEvents` 모두 T3에서 정의 + T4/T5/T13에서 호출 일관
- `findBadge` / `BADGE_CATALOG` (T5) → T6/T10/T11/T12/T13 모두 동일 import
- `TIERS` / `getCurrentTier` / `getNextTier` / `didLevelUp` (T1) → T3 (didLevelUp) + T11 (getCurrentTier) 일관
- 4 CustomEvent name (`dg:reward:xp-float` / `dg:reward:level-up` / `dg:reward:streak-milestone` / `dg:reward:badge-unlock`) — T3 정의, T6~T10 listen 동일
- `User` v2 필드 (`earnedBadges` / `gamificationMigrated` / `schemaVersion: 2`) — T2 정의, T3/T4/T11/T13 사용 동일
- `localStorage` 키 변경 0건 — `'user'` / `'briefings'` 그대로

**Placeholder scan**: TBD/TODO/"implement later"/"add appropriate ..." 없음. 모든 step에 실제 코드 또는 명시적 명령어 포함.

**카운트 보정 (독립 검토 결과)**: 실제 it-block 합산 ~100±3 (453 → ~553). spec §1.4/§6.3 DoD를 "+100 ±3"로 보정. T별 분포는 §6.1 표 그대로 둠.

**검토 적용 이력 (2026-04-30 plan 검토 1차)**:

- **P0**: `diversity-types` predicate를 한국어 5종 (4종 이상)으로 변경 (T5 step 1/3/4 + spec §3.2 동기화). 영문 키는 dead badge였음 — 실제 home.ts:657-661은 분석/전환/실무/성장/트렌드 라벨 사용.
- **P1-1**: rewards.ts `disposers[]` 패턴으로 listener cleanup (T6 step 3 + T7 step 3). spec 간 document listener 누적으로 인한 flake 방지.
- **P1-2**: `takeSnapshot`의 `new URL(b.url).hostname`을 try/catch로 감쌈 (T5 step 4). invalid url일 때 sweep 전체 실패하던 것 graceful 처리.
- **P1-3**: spec §1.4/§6.3 DoD vitest count "+95" → "+100 ±3" 보정.
- **P2-1**: T13 `backfillSucceeded` flag 추가 → 부분 실패 시 onClose에서 `gamificationMigrated=true` set 안 함 (영구 unlock 손실 방지, 다음 home 진입 시 재시도).
- **P2-3**: T3 streak-milestone test 코멘트 정정 ("가장 큰 통과만" → "통과한 모든 milestone emit").
- **P2-2 skip 사유**: T2 step 5의 stats.ts 임시 패치는 적용해야 T2~T10 commit이 buildable. T11에서 hydrateBadges rewrite 시 line 254-255 패치는 자연 사라지고, line 310 패치는 영구 (`getCurrentTier(user.xp).name` 사용). 죽은 코드 commit 발생 안 함.
- **P2-4**: P1-1로 자연 해소 (disposers 정리되면 listener 누수 자체가 발생 안 함).

---

**End of Plan**
