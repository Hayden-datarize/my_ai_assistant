# v3.5 Polish / URL 통일 / i18n Lite Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v3.4 이후 누적된 P2 backlog 12건 + 신규 폴리시 4건(총 16건)을 단일 사이클에 정리한다. 핵심은 read/write URL validation 통합(B1) + 영문 RSS 번역 사이클의 silent error fixup(A2) + heatmap reduce-motion CSS 버그 수정(D1) + 중복 메시지 dedup(i18n Lite).

**Architecture:** 기존 Vite + TypeScript SPA 구조 유지. 새 모듈 3개 추가 (`src/utils/url.ts`, `src/state/user.ts`, `src/ui/messages.ts`). 단일 브랜치 `feat/v3.5-polish-url-i18n`에 ~10 commit. 각 task = 1 commit. 마지막 task가 i18n Lite로 모든 dedup 메시지를 흡수한다.

**Tech Stack:** Vite 5 / TypeScript strict / Vitest + Playwright / Firebase Hosting

**Spec:** `docs/superpowers/specs/2026-04-26-v3.5-polish-design.md`

---

## File Structure

### 신규 파일

| Path | 책임 |
|---|---|
| `src/utils/url.ts` | `isHttpsUrl(value: unknown): value is string` — `new URL()` 기반 https-only validator |
| `src/state/user.ts` | `LegacyUser` 타입 + `getCachedUser()` — home.ts/stats.ts 양쪽이 사용하는 user localStorage 어댑터 |
| `src/ui/messages.ts` | i18n Lite — 4~5개 highest-frequency 메시지 상수 + 어조 규칙 주석 |
| `tests/unit/url.spec.ts` | B1 helper unit 테스트 |
| `tests/unit/messages.spec.ts` | i18n Lite 메시지 identity + 어조 규칙 단언 |

### 수정 파일

| Path | 변경 |
|---|---|
| `src/state/briefings.ts` | `isValidImageUrl` 폐기 → `isHttpsUrl` 직접 호출 + 캐스트 의도 주석 (B2) |
| `src/ui/handlers/home.ts` | `swapTitleInDOM` scoping (A3) + `loadUser` 제거 + `getCachedUser` import (F2) + i18n Lite 메시지 교체 |
| `src/ui/handlers/stats.ts` | `loadUser` 제거 + `getCachedUser` import (F2) + `isoKey` 폐기 + `getDateStr` 직접 호출 (F3) + future cell aria-label format (C2/C3) + dataCells 주석 (C1) |
| `src/ui/translateQueue.ts` | partial-failure summary toast (A2) |
| `src/ui/translateToast.ts` | `showTranslateError` 메시지를 `messages.ts`로 교체 |
| `src/ui/tabs/settings.ts` | "저장됨" 류 메시지 통일 |
| `src/ui/modals/memo.ts` | "저장되었어요" 메시지 통일 |
| `src/styles/components/heatmap.css` | reduce-motion 셀렉터 추가 (D1) |
| `tests/smoke/layout.spec.ts` | mobile bottom nav 격리 강화 (D2) |
| `tests/smoke/polish.spec.ts` | `waitForTimeout(300)` → event-driven (F4) |
| `tests/smoke/slack.spec.ts` | `waitForTimeout(4000)` → `waitForResponse` 또는 명시 mock event (F4) |
| `tests/smoke/tier-downgrade.spec.ts` | route 등록 순서 주석 (B5) |
| `tests/unit/state-briefings.spec.ts` | URL 시드 통일 (B4) |
| `tests/unit/briefings-translation.spec.ts` | URL 시드 통일 (B4) |
| `tests/unit/briefings-dedup.spec.ts` | URL 시드 통일 (B4) |

---

## Setup

- [ ] **Setup-1: 브랜치 생성**

```bash
git checkout main
git pull --ff-only origin main 2>/dev/null || true
git checkout -b feat/v3.5-polish-url-i18n
```

- [ ] **Setup-2: baseline 검증 (모든 테스트 green인지 확인)**

```bash
npm run lint
npx tsc --noEmit
npm test
npm run test:smoke
npm run build
```

Expected:
- lint / typecheck: 0 error 0 warn
- vitest: 330 pass
- Playwright: 79 pass (heatmap reduce-motion D1, layout flake D2 가 baseline에서 flaky일 수 있음 — 현 상태 기록만)
- build: CSS ~33.5 kB / JS ~63.5 kB (gzip)

- [ ] **Setup-3: spec/plan 커밋**

```bash
git add docs/superpowers/specs/2026-04-26-v3.5-polish-design.md docs/superpowers/plans/2026-04-26-v3.5-polish.md
git commit -m "docs(v3.5): add spec + implementation plan"
```

---

## Task 1: B1 — `isHttpsUrl` 공유 helper 신설

**Files:**
- Create: `src/utils/url.ts`
- Test: `tests/unit/url.spec.ts`

- [ ] **Step 1: 실패 테스트 작성**

`tests/unit/url.spec.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { isHttpsUrl } from '../../src/utils/url';

describe('isHttpsUrl', () => {
  it('accepts https:// URLs', () => {
    expect(isHttpsUrl('https://example.com')).toBe(true);
    expect(isHttpsUrl('https://cdn.example.com/img.jpg')).toBe(true);
    expect(isHttpsUrl('HTTPS://example.com')).toBe(true); // protocol case-insensitive
  });

  it('rejects http://', () => {
    expect(isHttpsUrl('http://example.com')).toBe(false);
  });

  it('rejects javascript: / data: schemes', () => {
    expect(isHttpsUrl('javascript:alert(1)')).toBe(false);
    expect(isHttpsUrl('data:text/html,<script>')).toBe(false);
  });

  it('rejects malformed input', () => {
    expect(isHttpsUrl('not a url')).toBe(false);
    expect(isHttpsUrl('')).toBe(false);
    expect(isHttpsUrl('  https://example.com  ')).toBe(false); // whitespace not auto-trimmed
  });

  it('rejects non-string types', () => {
    expect(isHttpsUrl(null)).toBe(false);
    expect(isHttpsUrl(undefined)).toBe(false);
    expect(isHttpsUrl(123)).toBe(false);
    expect(isHttpsUrl({})).toBe(false);
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
npx vitest run tests/unit/url.spec.ts
```

Expected: FAIL (Cannot find module '../../src/utils/url')

- [ ] **Step 3: 최소 구현**

`src/utils/url.ts`:

```typescript
/**
 * Validates that a value is an https:// URL parseable by `new URL()`.
 * - Rejects all non-string types (null/undefined/number/object).
 * - Rejects http://, javascript:, data:, and other schemes.
 * - Protocol comparison is case-insensitive (URL parser auto-lowercases).
 * - Whitespace is NOT auto-trimmed; callers responsible for normalization.
 */
export function isHttpsUrl(value: unknown): value is string {
  if (typeof value !== 'string') return false;
  try {
    return new URL(value).protocol === 'https:';
  } catch {
    return false;
  }
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
npx vitest run tests/unit/url.spec.ts
```

Expected: PASS (5 tests)

- [ ] **Step 5: 커밋**

```bash
git add src/utils/url.ts tests/unit/url.spec.ts
git commit -m "chore(util): add isHttpsUrl shared helper + tests"
```

---

## Task 2: B1 wiring + A3 swapTitleInDOM scoping

**Files:**
- Modify: `src/state/briefings.ts:18-41` (replace `isValidImageUrl` with `isHttpsUrl`)
- Modify: `src/ui/handlers/home.ts:130-133` (add `root` param)
- Test: 기존 `tests/unit/state-briefings.spec.ts` 가 cover (URL 검증 동작 동일 인풋 집합)
- Test: `tests/unit/swap-title-scoping.spec.ts` (신규)

- [ ] **Step 1: 기존 briefings 테스트 회귀 확인 (현 상태 baseline)**

```bash
npx vitest run tests/unit/state-briefings.spec.ts
```

Expected: PASS — 현재 회귀 없는 baseline 확보.

- [ ] **Step 2: `briefings.ts` 수정**

`src/state/briefings.ts:18-41` 영역을 다음으로 교체:

```typescript
const KEY = 'briefings';

// `b as Briefing` is an unchecked assertion at the localStorage boundary;
// downstream we validate fields we care about (currently imageUrl). Other
// fields are not normalized — by-design, since callers tolerate string drift.
export function loadBriefings(): Briefing[] {
  try {
    const raw = JSON.parse(localStorage.getItem(KEY) ?? '[]') as unknown;
    if (!Array.isArray(raw)) return [];
    return raw.map((b) => {
      const briefing = b as Briefing;
      // Drop imageUrl only if present-but-invalid; leave undefined alone.
      if (briefing.imageUrl !== undefined && !isHttpsUrl(briefing.imageUrl)) {
        const normalized: Briefing = { ...briefing };
        delete normalized.imageUrl;
        return normalized;
      }
      return briefing;
    });
  } catch {
    return [];
  }
}
```

상단 import 추가 (line 1 근처):

```typescript
import { isHttpsUrl } from '../utils/url';
```

기존 `function isValidImageUrl(...)` 함수 정의 (line 20-22) 완전 삭제.

- [ ] **Step 3: 기존 briefings 테스트 재실행**

```bash
npx vitest run tests/unit/state-briefings.spec.ts
```

Expected: PASS — 동일 인풋 집합이므로 모두 통과.

- [ ] **Step 4: A3 scoping 실패 테스트 작성**

`tests/unit/swap-title-scoping.spec.ts`:

```typescript
import { describe, it, expect } from 'vitest';
// 주: swapTitleInDOM은 home.ts의 internal이라 test export가 필요하다.
// Step 5 구현에서 home.ts에 `export` 추가 후 import한다.

import { swapTitleInDOM } from '../../src/ui/handlers/home';

describe('swapTitleInDOM scoping', () => {
  it('scopes query to provided root, not document', () => {
    document.body.innerHTML = `
      <div id="rootA">
        <div data-briefing-id="dup"><span class="card-title">A</span></div>
      </div>
      <div id="rootB">
        <div data-briefing-id="dup"><span class="card-title">B</span></div>
      </div>
    `;
    const rootA = document.getElementById('rootA') as HTMLElement;
    swapTitleInDOM('dup', '교체됨', rootA);
    expect(rootA.querySelector('.card-title')?.textContent).toBe('교체됨');
    // rootB는 영향 없어야 함
    const rootB = document.getElementById('rootB') as HTMLElement;
    expect(rootB.querySelector('.card-title')?.textContent).toBe('B');
  });

  it('defaults to document when root omitted (back-compat)', () => {
    document.body.innerHTML = `
      <div data-briefing-id="solo"><span class="card-title">old</span></div>
    `;
    swapTitleInDOM('solo', 'new');
    expect(document.querySelector('[data-briefing-id="solo"] .card-title')?.textContent).toBe('new');
  });
});
```

- [ ] **Step 5: 테스트 실패 확인**

```bash
npx vitest run tests/unit/swap-title-scoping.spec.ts
```

Expected: FAIL (swapTitleInDOM not exported, root param signature mismatch)

- [ ] **Step 6: A3 구현**

`src/ui/handlers/home.ts:130-133` 영역을 다음으로 교체:

```typescript
export function swapTitleInDOM(id: string, titleKo: string, root: ParentNode = document): void {
  const titleEl = root.querySelector(`[data-briefing-id="${id}"] .card-title`);
  if (titleEl) titleEl.textContent = titleKo;
}
```

- [ ] **Step 7: 테스트 통과 확인 + 전체 unit 회귀**

```bash
npx vitest run tests/unit/swap-title-scoping.spec.ts tests/unit/state-briefings.spec.ts
npx vitest run
```

Expected: 모든 vitest pass (~336 pass, +6 from Task 1+2).

- [ ] **Step 8: 커밋**

```bash
git add src/state/briefings.ts src/ui/handlers/home.ts tests/unit/swap-title-scoping.spec.ts
git commit -m "refactor(state): use isHttpsUrl in loadBriefings + swapTitleInDOM scoping"
```

---

## Task 3: A2 — TranslateQueue partial-failure summary toast

**Files:**
- Modify: `src/ui/translateQueue.ts`
- Test: 기존 `tests/unit/translateQueue.spec.ts` 확장

- [ ] **Step 1: 실패 테스트 작성 (기존 spec에 추가)**

`tests/unit/translateQueue.spec.ts` 끝에 다음 테스트 추가:

```typescript
import { TranslateQueue } from '../../src/ui/translateQueue';

describe('TranslateQueue partial-failure summary', () => {
  it('invokes onDrain with failedCount > 0 when any work threw', async () => {
    const onDrain = vi.fn();
    const queue = new TranslateQueue(
      async (id: string) => { if (id === 'fail') throw new Error('boom'); return id; },
      { concurrency: 2, delayMs: 0, onDrain },
    );
    queue.enqueue('ok1');
    queue.enqueue('fail');
    queue.enqueue('ok2');
    await new Promise((r) => setTimeout(r, 50)); // allow drain
    expect(onDrain).toHaveBeenCalledTimes(1);
    expect(onDrain).toHaveBeenCalledWith({ failedCount: 1 });
  });

  it('does not invoke onDrain when no failures (silent success)', async () => {
    const onDrain = vi.fn();
    const queue = new TranslateQueue(
      async (id: string) => id,
      { concurrency: 2, delayMs: 0, onDrain },
    );
    queue.enqueue('a');
    queue.enqueue('b');
    await new Promise((r) => setTimeout(r, 50));
    // onDrain은 항상 호출되지만 failedCount=0이면 호출자가 spam 안 함.
    // 정책: onDrain은 실패 1건+에서만 호출 (caller 단순화)
    expect(onDrain).not.toHaveBeenCalled();
  });
});
```

기존 spec 파일 상단 import에 `vi` 미포함 시 추가:

```typescript
import { describe, it, expect, vi } from 'vitest';
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
npx vitest run tests/unit/translateQueue.spec.ts
```

Expected: FAIL (`onDrain` option not supported, signature mismatch)

- [ ] **Step 3: 구현**

`src/ui/translateQueue.ts` 전체를 다음으로 교체:

```typescript
/**
 * TranslateQueue — concurrency-limited background queue for title translations.
 *
 * - 동시에 최대 `concurrency`개의 작업만 실행
 * - 각 dispatch 사이에 `delayMs` 만큼의 최소 간격 유지
 * - 동일 id가 in-flight 중이거나 pending 중이면 중복 enqueue 무시
 * - 실패는 카운터에 누적; drain 완료 시 failedCount > 0이면 `onDrain` 호출
 *   (정책: 실패 1건+에서만 호출 — caller가 spam 방지 신경 쓸 필요 없음)
 */

type Worker = (id: string) => Promise<string>;

interface Options {
  concurrency: number;
  delayMs: number;
  onDrain?: (info: { failedCount: number }) => void;
}

export class TranslateQueue {
  private pending: string[] = [];
  private inFlight = new Set<string>();
  private active = 0;
  private lastDispatchAt = 0;
  private failedCount = 0;

  constructor(private work: Worker, private opts: Options) {}

  enqueue(id: string): void {
    if (this.inFlight.has(id) || this.pending.includes(id)) return;
    this.pending.push(id);
    this.process();
  }

  private process(): void {
    while (this.active < this.opts.concurrency && this.pending.length > 0) {
      const id = this.pending.shift();
      if (id === undefined) break;
      this.inFlight.add(id);
      this.active++;
      const now = Date.now();
      const wait = Math.max(0, this.opts.delayMs - (now - this.lastDispatchAt));
      this.lastDispatchAt = now + wait;
      setTimeout(() => { void this.run(id); }, wait);
    }
  }

  private async run(id: string): Promise<void> {
    try { await this.work(id); }
    catch { this.failedCount++; }
    finally {
      this.active--;
      this.inFlight.delete(id);
      this.process();
      // Drain detection: nothing in-flight, nothing pending.
      if (this.active === 0 && this.pending.length === 0) {
        const failed = this.failedCount;
        this.failedCount = 0; // reset for next batch
        if (failed > 0) this.opts.onDrain?.({ failedCount: failed });
      }
    }
  }
}
```

- [ ] **Step 4: home.ts 호출처에서 onDrain 연결**

`src/ui/handlers/home.ts:111-128` 의 `titleQueue` 생성을 다음으로 교체:

```typescript
const titleQueue = new TranslateQueue(
  async (id: string): Promise<string> => {
    const list = loadBriefings();
    const b = list.find((x) => x.id === id);
    if (!b) return '';
    const apiKey = getApiKey();
    if (!apiKey) return '';
    if (isSessionBlocked()) return '';
    if (!checkAndIncrement()) throw new Error('cap reached');
    const ko = await translateTitle(b.title, apiKey);
    setTranslation(id, { titleKo: ko });
    swapTitleInDOM(id, ko);
    return ko;
  },
  {
    concurrency: 3,
    delayMs: 200,
    onDrain: ({ failedCount }) => {
      // i18n Lite (Task 10)에서 messages.ts로 흡수 예정 — 현재는 인라인.
      const msg = `일부 카드 번역에 실패했어요 (${failedCount}건). 잠시 후 다시 시도해 주세요.`;
      showToast(msg);
    },
  },
);
```

`showToast` import 확인 (이미 import 되어있을 가능성 큼; 없으면 상단에 추가):

```typescript
import { showToast } from '../../utils/toast';
```

- [ ] **Step 5: 테스트 통과 확인 + 전체 vitest 회귀**

```bash
npx vitest run tests/unit/translateQueue.spec.ts
npx vitest run
```

Expected: 모든 vitest pass.

- [ ] **Step 6: 커밋**

```bash
git add src/ui/translateQueue.ts src/ui/handlers/home.ts tests/unit/translateQueue.spec.ts
git commit -m "feat(ui): translateQueue summary toast on partial failure"
```

---

## Task 4: F1 — Test escape hatch dead-code 검증 + 가드

**Files (조사 결과에 따라 변동):**
- Verify: `dist/assets/*.js` (build 산출물)
- Possibly modify: `src/services/translate.ts:69-71`, `src/state/usage.ts:58-61`, `src/ui/translateToast.ts:47-49`

- [ ] **Step 1: production build 후 escape hatch 노출 검사**

```bash
npm run build
grep -E "__resetSessionBlockForTest|__resetToastDedupForTest|resetForTest" dist/assets/*.js || echo "NOT_FOUND"
```

Expected: 둘 중 하나
- `NOT_FOUND` → Vite tree-shake가 잘 작동, 코드 변경 불필요. **Step 4로 직접 이동**.
- 함수명 출력 → Vite가 dead-code 제거 못함, **Step 2 진행**.

- [ ] **Step 2: 노출된 경우만 — `import.meta.env.MODE` 가드 추가**

(Step 1에서 NOT_FOUND이면 이 단계 스킵)

`src/services/translate.ts:69-71`:

```typescript
// Test-only escape hatch — guarded by build mode so production tree-shakes it out
export function __resetSessionBlockForTest(): void {
  if (import.meta.env.MODE !== 'test') return;
  sessionBlocked = false;
}
```

`src/state/usage.ts:58-61`:

```typescript
export function resetForTest(): void {
  if (import.meta.env.MODE !== 'test') return;
  localStorage.removeItem(USAGE_KEY);
  localStorage.removeItem(CAP_KEY);
}
```

`src/ui/translateToast.ts:47-49`:

```typescript
export function __resetToastDedupForTest(): void {
  if (import.meta.env.MODE !== 'test') return;
  lastShownError = null;
}
```

- [ ] **Step 3: vitest에서 escape hatch가 여전히 동작하는지 확인**

```bash
npx vitest run
```

Expected: 모든 vitest pass — vitest는 `MODE === 'test'` 평가하므로 가드 통과해서 reset 동작.

- [ ] **Step 4: 검증 결과 commit (코드 변경 0이면 spec/메모리에 결과 기록 1줄)**

코드 변경 있음 (Step 2 실행한 경우):

```bash
git add src/services/translate.ts src/state/usage.ts src/ui/translateToast.ts
git commit -m "chore(prod-bundle): guard test escape hatches behind import.meta.env.MODE"
```

코드 변경 없음 (Step 1에서 NOT_FOUND):

```bash
git commit --allow-empty -m "chore(prod-bundle): verify test escape hatches are tree-shaken (no code changes)

Confirmed via 'grep -E ... dist/assets/*.js' that __resetSessionBlockForTest,
__resetToastDedupForTest, resetForTest are not present in production bundle.
Vite's tree-shaker strips them since they're never imported from production code.
No guard needed."
```

---

## Task 5: D1 — heatmap reduce-motion CSS 셀렉터 추가

**Files:**
- Modify: `src/styles/components/heatmap.css:163-166`
- Test: `tests/smoke/heatmap.spec.ts:124` (기존 스펙이 stabilized 되어야 함)

- [ ] **Step 1: 현 baseline 확인 — D1 테스트가 실패하는지 재현**

```bash
npx playwright test tests/smoke/heatmap.spec.ts -g "reduce-motion"
```

Expected: FAIL (or flaky) — `.heatmap-cell.is-future` 가 `heatmap-fade-in-future` keyframe 으로 entrance animation 잔류.

- [ ] **Step 2: CSS 수정**

`src/styles/components/heatmap.css:163-166` 영역을 다음으로 교체:

```css
/* Reduce motion (local rule — base.css has no global rule in v3.3.2 baseline)
 * v3.5: explicitly disable both standard and future-cell entrance animations.
 * .heatmap-cell.is-future has higher specificity than .heatmap-cell, so we
 * must list it explicitly (not relying on base selector cascade).
 */
@media (prefers-reduced-motion: reduce) {
  .heatmap-cell { transition: none; }
  .heatmap-grid.is-entering .heatmap-cell,
  .heatmap-grid.is-entering .heatmap-cell.is-future { animation: none; }
}
```

- [ ] **Step 3: D1 테스트 재실행 — green 확인**

```bash
npx playwright test tests/smoke/heatmap.spec.ts -g "reduce-motion"
```

Expected: PASS.

- [ ] **Step 4: heatmap 전체 spec 회귀 확인**

```bash
npx playwright test tests/smoke/heatmap.spec.ts
```

Expected: 전체 PASS.

- [ ] **Step 5: 커밋**

```bash
git add src/styles/components/heatmap.css
git commit -m "fix(heatmap): include future-fade keyframe in reduce-motion guard"
```

---

## Task 6: D2 — mobile bottom nav suite-flake 격리

**Files:**
- Modify: `tests/smoke/layout.spec.ts:38` 영역

작업량 cap: **1시간**. 1시간 안에 원인 미확인 시 Step 5 fallback (test.fixme + backlog).

- [ ] **Step 1: flake 재현 — full suite 1회 실행해서 어떤 spec 직후에 fail 하는지 기록**

```bash
npx playwright test tests/smoke/ --workers=1 --reporter=list 2>&1 | tee /tmp/v3.5-suite-run.log
grep -A2 -B5 "layout.spec.ts.*38\|mobile bottom nav" /tmp/v3.5-suite-run.log
```

Expected: pass 또는 fail 로그 + 직전 spec 파일명 기록.

- [ ] **Step 2: 의심 잔류 상태 (viewport / localStorage / serviceWorker) 확인**

`tests/smoke/layout.spec.ts:1-50` 영역의 `beforeEach` 또는 viewport 설정 확인. 직전 spec 이 변경한 viewport/storage 가 leak 되는지 추론.

- [ ] **Step 3: `beforeEach`에 viewport + localStorage 명시 reset 추가**

`tests/smoke/layout.spec.ts` 의 mobile bottom nav 테스트가 속한 `describe`/`test.use` 블록 직전에 다음을 추가 (정확한 line은 step 1 조사 결과로 결정):

```typescript
test.beforeEach(async ({ page, context }) => {
  // v3.5: explicit reset to defeat suite-flake — earlier specs may leak
  // viewport size or localStorage across pages within the same context.
  await context.clearCookies();
  await page.setViewportSize({ width: 375, height: 667 }); // mobile
  await page.addInitScript(() => {
    localStorage.clear();
    sessionStorage.clear();
  });
});
```

- [ ] **Step 4: Suite 재실행 — flake 해소 확인**

```bash
npx playwright test tests/smoke/ --workers=1
```

Expected: PASS — D2 spec이 더 이상 flake 안 함.

- [ ] **Step 5 (fallback): 1시간 안에 원인 미확인 시**

(Step 4가 여전히 flake한 경우만)

`tests/smoke/layout.spec.ts:38` 의 해당 `test('...mobile bottom nav...', ...)` 를 `test.fixme('...mobile bottom nav...', ...)` 로 변경하고 다음 주석 추가:

```typescript
// v3.5: suite-flake under investigation — single-run passes 3/3, suite-run fails
// intermittently. Likely viewport/storage leak from prior spec. Tracked as
// v3.6 backlog (see project_daily_growth_v35_*.md). Re-enable when fixed.
test.fixme('mobile bottom nav: ...', async ({ page }) => { /* original body */ });
```

- [ ] **Step 6: 커밋**

```bash
git add tests/smoke/layout.spec.ts
git commit -m "test(layout): stabilize mobile bottom nav suite isolation"
```

(Step 5 fallback 적용된 경우 commit 메시지를 다음으로 변경:)

```bash
git commit -m "test(layout): mark mobile bottom nav as fixme pending v3.6 investigation"
```

---

## Task 7: F2 + F3 — `loadUser` 추출 + `isoKey` 폐기

**Files:**
- Create: `src/state/user.ts`
- Modify: `src/ui/handlers/home.ts:62-77` (interface + loadUser 제거)
- Modify: `src/ui/handlers/stats.ts:17-41` (interface + loadUser + isoKey 제거)

- [ ] **Step 1: home.ts와 stats.ts의 `loadUser` diff 비교 (사전 확인)**

```bash
diff <(sed -n '72,77p' src/ui/handlers/home.ts) <(sed -n '26,31p' src/ui/handlers/stats.ts)
```

Expected: 동일 함수 본문 (둘 다 `localStorage.getItem('user')` JSON.parse, catch returns null).

`LegacyUser` 인터페이스 비교:

```bash
sed -n '62,70p' src/ui/handlers/home.ts
echo "---"
sed -n '19,24p' src/ui/handlers/stats.ts
```

Expected: stats.ts의 `LegacyUser`는 home.ts의 strict subset (name, streak, xp, level만). 추출 시 home.ts shape를 사용 (full superset).

- [ ] **Step 2: 신규 모듈 작성**

`src/state/user.ts`:

```typescript
export interface LegacyUser {
  name: string;
  interests: string[];
  onboardedAt: string;
  streak: number;
  lastActiveDate: string;
  xp: number;
  level: number;
}

const USER_STORAGE = 'user';

export function getCachedUser(): LegacyUser | null {
  try {
    const raw = localStorage.getItem(USER_STORAGE);
    return raw ? (JSON.parse(raw) as LegacyUser) : null;
  } catch {
    return null;
  }
}
```

- [ ] **Step 3: home.ts 수정**

`src/ui/handlers/home.ts:62-77` 영역 (interface `LegacyUser` + `function loadUser`)을 **삭제**하고, 상단 import 블록에 추가:

```typescript
import { getCachedUser, type LegacyUser } from '../../state/user';
```

`USER_STORAGE` 상수가 home.ts에 정의되어 있다면 (다른 곳에서 사용 안 한다면) 함께 삭제. 다른 곳에서 사용 중이면 유지 + saveUser는 그대로 둠 (saveUser는 별도 작업 — 본 task scope 아님).

이 파일 내 `loadUser()` 호출 모두를 `getCachedUser()` 로 grep+replace:

```bash
grep -n "loadUser()" src/ui/handlers/home.ts
```

각 호출처에서 `loadUser()` → `getCachedUser()` 교체.

- [ ] **Step 4: stats.ts 수정**

`src/ui/handlers/stats.ts:17-41` 영역 (USER_STORAGE 상수 + `interface LegacyUser` + `function loadUser` + `function isoKey`)을 **모두 삭제**. import 블록에 추가:

```typescript
import { getCachedUser, type LegacyUser } from '../../state/user';
```

`isoKey(d)` 호출 (line 50) 을 `getDateStr(d)` 로 교체:

```typescript
const n = counts.get(getDateStr(d)) ?? 0;
```

이 파일 내 `loadUser()` 호출 모두를 `getCachedUser()` 로 교체:

```bash
grep -n "loadUser()" src/ui/handlers/stats.ts
```

stats.ts의 `LegacyUser`는 strict subset이었지만 home.ts shape가 superset이므로 stats.ts 사용 부위는 모두 호환 (extra fields 무시).

- [ ] **Step 5: 전체 unit + smoke 회귀**

```bash
npx tsc --noEmit
npx vitest run
npx playwright test tests/smoke/
```

Expected: 모두 PASS — 동작 동일, refactor only.

- [ ] **Step 6: 커밋**

```bash
git add src/state/user.ts src/ui/handlers/home.ts src/ui/handlers/stats.ts
git commit -m "refactor(state): extract shared loadUser into state/user + dedup isoKey"
```

---

## Task 8: F4 — `waitForTimeout` 2건 제거

**Files:**
- Modify: `tests/smoke/polish.spec.ts:38`
- Modify: `tests/smoke/slack.spec.ts:105`

- [ ] **Step 1: polish.spec.ts:38 — transition 대기 → `expect.poll`**

`tests/smoke/polish.spec.ts:35-46` 영역을 다음으로 교체:

```typescript
  const beforeHover = await card.evaluate((el) => getComputedStyle(el).transform);
  await card.hover();
  // v3.5: replace waitForTimeout(300) — poll computed transform until it stabilizes
  // at the post-hover translateY(-2px) value. Avoids tying to a specific transition
  // duration and prevents test flake when the duration changes.
  await expect.poll(
    async () => card.evaluate((el) => getComputedStyle(el).transform),
    { timeout: 2000, intervals: [50, 100, 200] },
  ).not.toBe(beforeHover);
  const afterHover = await card.evaluate((el) => getComputedStyle(el).transform);

  expect(afterHover).not.toBe(beforeHover);
  expect(afterHover).not.toBe('none');
  expect(afterHover).toMatch(/matrix.*-2\s*\)|translate.*-2px/);
```

- [ ] **Step 2: polish.spec.ts 회귀**

```bash
npx playwright test tests/smoke/polish.spec.ts -g "translateY transform"
```

Expected: PASS.

- [ ] **Step 3: slack.spec.ts:105 — async mock 대기 → 명시 wait**

`tests/smoke/slack.spec.ts:97-107` 영역을 다음으로 교체:

```typescript
  await page.locator('#answerArea').fill('오늘 집중이 좋았다. 문서 정리에 두 시간을 썼다. 분산이 없었다.');

  // v3.5: replace waitForTimeout(4000) — wait for the Gemini evaluator response
  // (which is what gates whether slack would have been called). Once that returns,
  // slackCalls is final.
  const evalResponse = page.waitForResponse(/generativelanguage\.googleapis\.com/);
  await page.evaluate(() => {
    document.dispatchEvent(new CustomEvent('dg:home:submit-answer'));
  });
  await evalResponse;
  // Allow microtask tick after response so any post-evaluation slack call
  // would have been dispatched.
  await page.waitForFunction(() => true);
  expect(slackCalls).toBe(0);
});
```

- [ ] **Step 4: slack.spec.ts 회귀**

```bash
npx playwright test tests/smoke/slack.spec.ts
```

Expected: PASS.

- [ ] **Step 5: 전체 smoke 회귀**

```bash
npx playwright test tests/smoke/
```

Expected: 모두 PASS.

- [ ] **Step 6: 커밋**

```bash
git add tests/smoke/polish.spec.ts tests/smoke/slack.spec.ts
git commit -m "test(smoke): replace waitForTimeout with event-driven waits"
```

---

## Task 9: B2 + B4 + B5 + C1 — invariant comments 4 sites

**Files:**
- Modify: `src/state/briefings.ts:28-30` (B2 — 캐스트 의도 주석은 Task 2에서 이미 처리됨, 본 task에서는 추가 정밀화)
- Modify: `tests/unit/state-briefings.spec.ts`, `tests/unit/briefings-translation.spec.ts`, `tests/unit/briefings-dedup.spec.ts` (B4 — URL 시드 통일)
- Modify: `tests/smoke/tier-downgrade.spec.ts` (B5)
- Modify: `src/ui/handlers/stats.ts:215-217` (C1)

- [ ] **Step 1: B4 — 테스트 시드 URL을 `https://example.com/...` 로 통일**

3개 파일에서 `x.com` / `a.com` / `e.com` 도메인을 `example.com` 로 일괄 치환. `cdn.example.com` 과 `http://insecure.example.com` 은 의도적 케이스라 보존.

```bash
# 1. state-briefings.spec.ts: 'https://x.com' → 'https://example.com'
sed -i '' "s|'https://x\.com'|'https://example.com'|g" tests/unit/state-briefings.spec.ts
sed -i '' "s|'https://x\.com/|'https://example.com/|g" tests/unit/state-briefings.spec.ts

# 2. briefings-dedup.spec.ts: 'https://x.com/N' → 'https://example.com/N', `https://a.com/${i}` → `https://example.com/${i}`
sed -i '' "s|'https://x\.com/|'https://example.com/|g" tests/unit/briefings-dedup.spec.ts
sed -i '' "s|https://a\.com/|https://example.com/|g" tests/unit/briefings-dedup.spec.ts

# 3. briefings-translation.spec.ts: 'https://e.com/X' → 'https://example.com/X'
sed -i '' "s|'https://e\.com/|'https://example.com/|g" tests/unit/briefings-translation.spec.ts

# 4. 보존 확인 (변경되지 않아야 함)
grep -E "cdn\.example\.com|insecure\.example\.com" tests/unit/state-briefings.spec.ts
```

Expected (last grep):
```
tests/unit/state-briefings.spec.ts:120:      imageUrl: 'https://cdn.example.com/img.jpg',
tests/unit/state-briefings.spec.ts:100:      imageUrl: 'http://insecure.example.com/img.jpg',  // http, not https
```

이 두 줄은 보존됨 (의도적 케이스).

- [ ] **Step 2: B5 — tier-downgrade route 등록 순서 주석**

`tests/smoke/tier-downgrade.spec.ts` 의 `page.route` 호출들 위에 다음 주석 1줄 추가 (정확 line은 spec 본문 보고 결정):

```typescript
// Route registration order matters: more-specific patterns first, catch-all last.
// Playwright matches in registration order, so reversing the registration causes
// the catch-all to swallow specific patterns and break the test.
```

- [ ] **Step 3: C1 — `dataCells[0]` past invariant 주석**

`src/ui/handlers/stats.ts:215-217` 영역의 주석을 다음으로 교체:

```typescript
  // v3.3.4.2: post-refactor every child is a data cell (no more .is-blank padding).
  // v3.5 (C1): dataCells[0] is always a *past* cell after v3.3.4.3's future-guard
  // — the heatmap window starts at firstDay (Mon, 4 weeks ago), which is always
  // before today. So `tabindex="0"` on dataCells[0] is safe; no first-non-future
  // search needed.
  const dataCells = Array.from(grid.querySelectorAll<HTMLButtonElement>('.heatmap-cell'));
  dataCells.forEach((c, i) => c.setAttribute('tabindex', i === 0 ? '0' : '-1'));
```

- [ ] **Step 4: B2 — Briefing 캐스트 주석 (Task 2에서 추가했지만 본문 정밀화 확인)**

Task 2 Step 2에서 추가된 주석:

```typescript
// `b as Briefing` is an unchecked assertion at the localStorage boundary;
// downstream we validate fields we care about (currently imageUrl). Other
// fields are not normalized — by-design, since callers tolerate string drift.
```

이 주석이 정확한지 확인 — 정확하면 Step 4 스킵, 부족하면 보강.

- [ ] **Step 5: 전체 회귀**

```bash
npm run lint
npx vitest run
npx playwright test tests/smoke/
```

Expected: 모두 PASS.

- [ ] **Step 6: 커밋**

```bash
git add src/ui/handlers/stats.ts tests/unit/state-briefings.spec.ts tests/unit/briefings-translation.spec.ts tests/unit/briefings-dedup.spec.ts tests/smoke/tier-downgrade.spec.ts
git commit -m "chore(comments): add invariant comments + unify briefings test seed URLs"
```

---

## Task 10: C2 + C3 + i18n Lite — `messages.ts` + heatmap aria-label format + 호출처 dedup

**Files:**
- Create: `src/ui/messages.ts`
- Test: `tests/unit/messages.spec.ts`
- Modify: `src/ui/handlers/stats.ts:190` (aria-label format)
- Modify: `src/ui/translateToast.ts` (showTranslateError 메시지 교체)
- Modify: `src/ui/handlers/home.ts` (3~4곳 메시지 교체)
- Modify: `src/ui/translateQueue.ts` 또는 home.ts onDrain (Task 3에서 인라인으로 작성된 메시지를 MSG.PARTIAL_TRANSLATE_FAIL로 교체)
- Modify: `src/ui/tabs/settings.ts:250` (저장 메시지 통일)
- Modify: `src/ui/modals/memo.ts:44` (저장 메시지 통일)

- [ ] **Step 1: messages.ts 작성**

`src/ui/messages.ts`:

```typescript
/**
 * i18n Lite — highest-frequency 한국어 UX 메시지의 단일 진입점.
 *
 * 어조 규칙 (위반 시 lint 또는 PR review에서 catch):
 * - 캐주얼 `~했어요` 톤 (정중체 `~했습니다` 금지, 명사형 `~됨` 금지)
 * - 마침표는 모든 메시지(toast 포함) 끝에 사용
 * - 이모지는 success(✅) / info(ℹ️) / error(❌) 3종만, 그 외 emoji 금지
 *
 * 본 모듈은 Lite — 305개 분산 한국어 문자열 중 빈도 ≥2의 핵심 5개만 통합.
 * Full i18n (`t('key')` 시스템 + 다국어 카탈로그)은 v3.6 별도 사이클.
 */
export const MSG = {
  TRY_AGAIN: '잠시 후 다시 시도해 주세요.',
  SAVE_SUCCESS: '저장되었어요.',
  AI_RESPONSE_FAIL: '지금은 AI 응답을 받지 못했어요. 잠시 후 다시 시도해 주세요.',
  DEMO_API_KEY_PROMPT: 'AI 응답을 받으려면 API 키를 등록해 주세요.',
  /**
   * Title-translation queue partial failure summary toast.
   * Caller fills `{count}` via template — kept as function for parameterization.
   */
  partialTranslateFail: (count: number): string =>
    `일부 카드 번역에 실패했어요 (${count}건). 잠시 후 다시 시도해 주세요.`,
} as const;
```

- [ ] **Step 2: messages 테스트 작성**

`tests/unit/messages.spec.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { MSG } from '../../src/ui/messages';

describe('MSG (i18n Lite)', () => {
  it('all string members end with a period', () => {
    const stringEntries = Object.entries(MSG).filter(([, v]) => typeof v === 'string');
    for (const [key, val] of stringEntries) {
      expect(val, `MSG.${key}`).toMatch(/\.$/);
    }
  });

  it('uses casual ~해요 tone (no formal ~합니다 / nominal ~됨)', () => {
    const stringEntries = Object.entries(MSG).filter(([, v]) => typeof v === 'string');
    for (const [key, val] of stringEntries) {
      expect(val, `MSG.${key}`).not.toMatch(/합니다\.?$|됩니다\.?$|됨\.?$/);
    }
  });

  it('partialTranslateFail interpolates count', () => {
    expect(MSG.partialTranslateFail(3)).toContain('(3건)');
    expect(MSG.partialTranslateFail(3)).toMatch(/\.$/);
  });

  it('exposes expected keys', () => {
    expect(MSG.TRY_AGAIN).toBeTruthy();
    expect(MSG.SAVE_SUCCESS).toBeTruthy();
    expect(MSG.AI_RESPONSE_FAIL).toBeTruthy();
    expect(MSG.DEMO_API_KEY_PROMPT).toBeTruthy();
  });
});
```

- [ ] **Step 3: 테스트 실패 → 통과 확인**

```bash
npx vitest run tests/unit/messages.spec.ts
```

Expected: PASS (4 tests).

- [ ] **Step 4: C2/C3 — heatmap aria-label format 변경 + 테스트**

`src/ui/handlers/stats.ts:190` 영역을 다음으로 교체:

```typescript
    if (isFuture) {
      cell.dataset['future'] = 'true';
      cell.setAttribute('aria-disabled', 'true');
      // v3.5 (C2/C3): aria-label uses human-friendly date format,
      // matching mouseenter info text style.
      cell.setAttribute('aria-label', `${formatCellLabel(key)}, 미래 날짜`);
    } else {
      const ariaLabel = n > 0
        ? `${formatCellLabel(key)}, ${n}개 달성`
        : `${formatCellLabel(key)}, 기록 없음`;
      cell.setAttribute('aria-label', ariaLabel);
    }
```

(non-future case도 일관성 위해 같이 변경 — `${key}` 만 raw 였던 것을 `formatCellLabel(key)`로 통일.)

`tests/smoke/heatmap.spec.ts` (기존 spec)에 다음 테스트 2건을 추가. 위치는 기존 reduce-motion 테스트(`:124`) 직후가 적절:

```typescript
test('future cell aria-label uses 한글 날짜 format (v3.5 C2/C3)', async ({ page }) => {
  // 시드: 답변 0건 + 통계 탭 진입 → heatmap 렌더 → 마지막 셀이 future일 가능성 큰 viewport
  await page.addInitScript(() => {
    localStorage.setItem('user', JSON.stringify({
      name: 'T', interests: ['pm'], onboardedAt: '2026-04-01',
      streak: 0, lastActiveDate: '', xp: 0, level: 1,
    }));
    localStorage.setItem('answers', '[]');
    sessionStorage.setItem('dg.briefings.auto-refresh-tried', '1');
  });
  await page.goto('/');
  await page.locator('[data-tab="stats"]').click();
  await expect(page.locator('.heatmap-grid')).toBeVisible({ timeout: 5_000 });

  const futureCells = page.locator('.heatmap-cell.is-future');
  const count = await futureCells.count();
  if (count === 0) {
    test.skip(); // 오늘이 일요일이면 future cell이 없을 수 있음 — 그 날만 skip
    return;
  }
  const ariaLabel = await futureCells.first().getAttribute('aria-label');
  expect(ariaLabel).toMatch(/^\d+월 \d+일 \([월화수목금토일]\), 미래 날짜$/);
});

test('past cell aria-label uses 한글 날짜 format (v3.5 C2/C3)', async ({ page }) => {
  await page.addInitScript(() => {
    localStorage.setItem('user', JSON.stringify({
      name: 'T', interests: ['pm'], onboardedAt: '2026-04-01',
      streak: 0, lastActiveDate: '', xp: 0, level: 1,
    }));
    localStorage.setItem('answers', '[]');
    sessionStorage.setItem('dg.briefings.auto-refresh-tried', '1');
  });
  await page.goto('/');
  await page.locator('[data-tab="stats"]').click();
  await expect(page.locator('.heatmap-grid')).toBeVisible({ timeout: 5_000 });

  // 첫 번째 non-future cell — 항상 과거 (firstDay = 4주 전 월요일)
  const pastCell = page.locator('.heatmap-cell:not(.is-future)').first();
  const ariaLabel = await pastCell.getAttribute('aria-label');
  expect(ariaLabel).toMatch(/^\d+월 \d+일 \([월화수목금토일]\), (\d+개 달성|기록 없음)$/);
});
```

> **Rationale**: 통계 탭 + heatmap rendering은 `mountStatsHandlers` 외에도 DOM 컨테이너, 라이프사이클 이벤트, viewport 매개가 얽혀 있어 vitest unit setup이 복잡하다. Playwright smoke는 실제 hydration 경로를 그대로 사용하므로 본 검증에 적합.

- [ ] **Step 5: 메시지 dedup — 호출처 교체**

다음 위치들을 grep으로 확인 후 `MSG.X` 로 교체:

`src/ui/translateToast.ts:32`:

```typescript
import { MSG } from './messages';
// ...
const msg = isAuth
  ? 'API 키가 유효하지 않아요. 설정에서 다시 입력해주세요.' // 단일 호출 — i18n Lite 미적용 (빈도 1)
  : `번역에 실패했어요. ${MSG.TRY_AGAIN}`;
```

`src/ui/handlers/home.ts:500/503`:

```typescript
empty.textContent = `오늘 표시할 브리핑을 가져오지 못했어요. ${MSG.TRY_AGAIN}`;
// ...
showToast(`브리핑을 가져오지 못했어요. ${MSG.TRY_AGAIN}`);
```

`src/ui/handlers/home.ts:739`:

```typescript
const msg = MSG.AI_RESPONSE_FAIL;
```

`src/ui/handlers/home.ts:783`:

```typescript
addBubble('ai', MSG.AI_RESPONSE_FAIL);
```

`src/ui/handlers/home.ts:721/769`:

```typescript
addBubble('ai', MSG.DEMO_API_KEY_PROMPT, { ... });
```

`src/ui/handlers/home.ts:716`:

```typescript
showToast(`답변이 ${MSG.SAVE_SUCCESS}`);
// 또는 단순:
showToast('답변이 저장되었어요.');  // v3.5 톤 일관성 — MSG.SAVE_SUCCESS reuse
```

권장: `showToast('답변이 저장되었어요.')` — context prefix를 다르게 합성하는 것이 caller 책임. MSG.SAVE_SUCCESS는 generic save용으로 둠.

`src/ui/tabs/settings.ts:250`:

```typescript
showToast(`✅ Slack Webhook ${MSG.SAVE_SUCCESS}`);
```

`src/ui/modals/memo.ts:44`:

```typescript
showToast(`메모가 ${MSG.SAVE_SUCCESS}`);
```

`src/ui/handlers/home.ts` 의 `titleQueue` onDrain 콜백 (Task 3에서 인라인으로 작성):

```typescript
onDrain: ({ failedCount }) => {
  showToast(MSG.partialTranslateFail(failedCount));
},
```

각 파일 상단 import 추가:

```typescript
import { MSG } from '../messages';   // 또는 '../ui/messages' depending on relative path
```

- [ ] **Step 6: 전체 회귀**

```bash
npm run lint
npx tsc --noEmit
npx vitest run
npx playwright test tests/smoke/
npm run build
```

Expected:
- 모든 테스트 PASS
- 번들 사이즈 v3.4 baseline 대비 ±2 kB 이내

- [ ] **Step 7: 커밋**

```bash
git add src/ui/messages.ts tests/unit/messages.spec.ts src/ui/handlers/stats.ts src/ui/translateToast.ts src/ui/handlers/home.ts src/ui/tabs/settings.ts src/ui/modals/memo.ts tests/smoke/heatmap.spec.ts
git commit -m "feat(i18n-lite): centralize repeated UX strings + heatmap aria-label format"
```

---

## Final Verification

- [ ] **Verify-1: 전체 lint / typecheck / vitest / smoke / build pass**

```bash
npm run lint
npx tsc --noEmit
npx vitest run
npx playwright test tests/smoke/
npm run build
```

Expected: 모든 단계 0 error.
- vitest: 330 → ~341 (+11: B1 5, A2 2, A3 2, messages 4 — heatmap aria 테스트는 Playwright로 이동)
- Playwright: 79 → ~81 stabilized (D1 green + D2 green/fixme + C2/C3 aria 2건 신규)
- 번들: ±2 kB 이내 v3.4 baseline (CSS 33.49 / JS 63.46 gzip)

- [ ] **Verify-2: commit log 정리 확인**

```bash
git log --oneline main..HEAD
```

Expected: ~10 commits in v3.5 spec 순서대로.

- [ ] **Verify-3: 독립 심층 리뷰 디스패치**

`superpowers:code-reviewer` 또는 `pr-review-toolkit:code-reviewer` agent 1회 실행. 리뷰 입력: `git diff main..HEAD` + spec 파일.

Expected: P0=0, P1 ≤ 2 (in-cycle 반영), P2 v3.6 backlog.

- [ ] **Verify-4: in-cycle review fix commit (필요 시)**

리뷰 P0/P1 반영 commit 1~2개 추가. 메시지: `chore(v3.5): apply in-cycle review fixes (P1, P2-N)`.

- [ ] **Verify-5: 메모리 업데이트 + main merge 준비**

`/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v35_polish.md` 신규 작성 + `MEMORY.md` 인덱스 추가.

main merge는 사용자 명시 요청 시에만:

```bash
git checkout main
git merge --no-ff feat/v3.5-polish-url-i18n -m "Merge feat/v3.5-polish-url-i18n (v3.5 Polish / URL / i18n Lite)"
```

Firebase 배포는 별도 사용자 요청 후 (`firebase deploy --only hosting`).

---

## Rollback Plan

각 commit이 독립적이라 단일 commit revert로 격리 가능:

| Commit | Revert 시 영향 |
|---|---|
| Task 2 (B1 wiring) | 이미지 URL validation이 regex로 복귀 — 동일 인풋 집합이므로 사실상 영향 없음 |
| Task 3 (A2) | partial-failure toast 사라짐 — silent 복귀 |
| Task 5 (D1) | reduce-motion 복원 — flake 재발 |
| Task 7 (F2/F3) | refactor 되돌림 — 동작 동일 |
| Task 10 (i18n Lite) | 메시지 인라인 복귀 |

위험 commit: **Task 2 (B1 wiring)** — `isHttpsUrl()` 이 기존 통과 imageUrl을 거부하면 briefings 일부 누락. **Task 2 직후 Step 5 회귀 즉시 실행** 으로 catch.
