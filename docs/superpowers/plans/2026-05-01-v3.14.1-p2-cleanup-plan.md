# v3.14.1 P2 Cleanup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v3.14 backlog의 P2 4건(#1 hydrate-badges flake, #2 v3.12 first-run flake, #3 lint 16건, #4 short-token false-positive 확장)을 한 사이클에 정리.

**Architecture:** v3.14 T13에서 도입한 `matchKeyword` 헬퍼를 `src/utils/`로 격상해 `src/state/achievements.ts`에서도 재사용(state→ui 역방향 의존 회피). lint는 기계적 `eslint-disable-next-line` + 1건 src 영역만 escapeHtml 안전성 재확인 후 동일 패턴. 테스트 flake는 fixed `setTimeout` → `vi.waitFor`/timeout 상향 + 명시적 precondition wait로 race 제거.

**Tech Stack:** Vite 5 + TypeScript 5 + vitest 1.6 (jsdom) + Playwright 1.42 + ESLint 8 + Firebase Hosting (직배포 패턴, origin remote 미등록).

**Cycle metadata:**
- Baseline: v3.14 HEAD `7c6ead7`
- Test baseline: 668 vitest + 93 Playwright smoke
- Lint baseline: 16 errors / 8 warnings (8 warnings는 explicit-any → out of scope)
- Bundle baseline: 31.81 kB gzip JS
- DoD: vitest +5±3, Playwright ±0, lint errors 16→0, bundle Δ ±0.2 kB
- Review depth: **표준 리뷰** (4~10 commits 범주, codex 2-pass 권장 by v3.14 lesson #4)

---

## File Structure

**Created:**
- (none)

**Modified:**
- `src/utils/interestKeywords.ts` — `matchKeyword` 헬퍼를 같은 파일에 추가 (cohesive: 둘 다 interest 매칭)
- `src/ui/handlers/home.ts` — 자체 `matchKeyword` 정의 제거 + utils에서 import (line 392-412 삭제, line 504 import 추가)
- `src/state/achievements.ts` — `categorizeScrapsByInterest`에서 `hay.includes(k)` → `matchKeyword(hay, k)` 교체, import 추가
- `src/ui/missions-section.ts:53` — `root.innerHTML = html` → 안전성 코멘트 + `eslint-disable-next-line` (interpolation 모두 static catalog/literal 검증)
- `tests/unit/home-cross-interest-trigger.spec.ts` — `matchKeyword` import 경로 변경 (`src/ui/handlers/home` → `src/utils/interestKeywords`)
- `tests/unit/state-user.spec.ts:4` — 미사용 `DEFAULT_MISSIONS` import 제거
- `tests/unit/ui/badge-detail.spec.ts` (line 8, 42), `badge-tooltip.spec.ts` (18), `hydrate-badges.spec.ts` (20, 98), `level-up.spec.ts` (6), `missions-section.spec.ts` (5), `rewards-mission.spec.ts` (6), `rewards.spec.ts` (6), `streak-pulse.spec.ts` (9), `welcome-gamification.spec.ts` (9, 58), `xp-float.spec.ts` (6, 52) — 각 line 위에 `// eslint-disable-next-line no-restricted-syntax` 추가 (15 lines)
- `tests/unit/ui/hydrate-badges.spec.ts:102` — `await new Promise(r => setTimeout(r, 50))` → `await vi.waitFor(...)` 패턴
- `tests/smoke/v3.12-game-feedback.spec.ts:48-63` — submit precondition wait + visibility timeout 2000→5000ms

**Created (test):**
- `tests/unit/state/achievements-cross-interest.spec.ts` — `categorizeScrapsByInterest` regression: `ai_ml` interest가 'daily training' substring에 false-positive 매칭하지 않는지

---

### Task 1: matchKeyword 헬퍼를 src/utils/로 격상

**Why:** `src/state/achievements.ts`가 `src/ui/handlers/home.ts`를 import하면 state→ui 역방향 의존 발생. utils로 빼면 양쪽 클린.

**Files:**
- Modify: `src/utils/interestKeywords.ts` (현재 12 lines)
- Modify: `src/ui/handlers/home.ts:392-412` (matchKeyword 정의 제거) + line 10 import 영역
- Modify: `tests/unit/home-cross-interest-trigger.spec.ts:178, 185, 192, 198` (import 경로 4건)

- [ ] **Step 1: `src/utils/interestKeywords.ts`에 matchKeyword 추가**

기존 `interestKeywords` 함수 아래에 추가:

```typescript
/**
 * v3.14 T13 (codex P1): cross-interest 매칭 헬퍼 — 짧은 ASCII 토큰(`ai`, `ml` 등)이
 * 무관 영단어 부분 문자열(`daily`, `retail` 등)에 매칭되는 false-positive를 차단하기 위한
 * word-boundary 매칭. 한국어/긴 토큰은 기존 substring includes 유지.
 *
 * 호출 측은 hay를 `.toLowerCase()`로 정규화한 상태로 넘긴다 (interestKeywords도 lowercase 출력).
 *
 * 예: `matchKeyword('daily training session', 'ai')` → false (false-positive 차단)
 *     `matchKeyword('ai 동향 정리', 'ai')` → true (word-boundary 양쪽 모두 만족)
 *     `matchKeyword('ai_ml etf 시장', 'ai_ml')` → true (length>3 → includes)
 *     `matchKeyword('인사제도 개편', '인사제도')` → true (non-ASCII → includes)
 *
 * v3.14.1 T1: home.ts에서 utils로 격상 — achievements.ts(state)에서도 재사용.
 */
export function matchKeyword(hay: string, keyword: string): boolean {
  if (keyword.length <= 3 && /^[a-z0-9_]+$/.test(keyword)) {
    return new RegExp(`\\b${keyword}\\b`).test(hay);
  }
  return hay.includes(keyword);
}
```

- [ ] **Step 2: `src/ui/handlers/home.ts`에서 정의 삭제 + import 교체**

line 392-412 (jsdoc 포함 matchKeyword 함수 전체) 삭제.

line 10 (`import { interestKeywords } from '../utils/interestKeywords';`)을 다음으로 변경:

```typescript
import { interestKeywords, matchKeyword } from '../utils/interestKeywords';
```

line 504(`const matched = interests.some((i) => interestKeywords(i).some((k) => matchKeyword(hay, k)));`)는 그대로 유지.

- [ ] **Step 3: 단위 테스트 import 경로 변경**

`tests/unit/home-cross-interest-trigger.spec.ts`의 4개 dynamic import 라인 (178, 185, 192, 198)을 일괄 `replace_all`로 교체:

- 기존: `await import('../../src/ui/handlers/home')`
- 신규: `await import('../../src/utils/interestKeywords')`

- [ ] **Step 4: vitest 실행으로 회귀 없음 확인**

```bash
npm test -- tests/unit/home-cross-interest-trigger.spec.ts tests/unit/utils
```

Expected: PASS (matchKeyword 4 tests + interestKeywords 기존 3 tests = 7+ pass)

- [ ] **Step 5: 전체 단위 테스트로 import 경로 회귀 0건 확인**

```bash
npm test
```

Expected: 668 pass (변동 없음)

- [ ] **Step 6: Commit**

```bash
git add src/utils/interestKeywords.ts src/ui/handlers/home.ts tests/unit/home-cross-interest-trigger.spec.ts
git commit -m "refactor(v3.14.1 T1): matchKeyword을 utils로 격상 — state도 재사용 가능"
```

---

### Task 2: achievements.ts에 matchKeyword 적용 + regression 테스트

**Why:** `categorizeScrapsByInterest`가 `hay.includes(k)`로 매칭 — `ai_ml` interest의 keyword `ai`가 'daily training' 같은 무관 텍스트에 매칭되어 `engagedInterests` Set이 잘못 채워짐 → 다양성 뱃지(`diversity-3`/`diversity-5`) 조기 unlock 가능. v3.14 T13 helper로 차단.

**Files:**
- Modify: `src/state/achievements.ts:1-41` (import 추가 + line 37 교체)
- Create: `tests/unit/state/achievements-cross-interest.spec.ts`

- [ ] **Step 1: 회귀 테스트 작성 (실패 기대)**

`tests/unit/state/achievements-cross-interest.spec.ts`:

```typescript
import { describe, it, expect, beforeEach } from 'vitest';
import { takeSnapshot } from '../../../src/state/achievements';
import { saveBriefings, type Briefing } from '../../../src/state/briefings';
import { saveUser } from '../../../src/state/user';
import { mkUser } from './userFixture';

function mkBriefing(overrides: Partial<Briefing>): Briefing {
  return {
    id: 'b1',
    title: '',
    summary: '',
    url: 'https://example.com/x',
    sourceTitle: 'example',
    publishedAt: '2026-05-01T00:00:00Z',
    fetchedAt: '2026-05-01T00:00:00Z',
    interest: 'ai_ml',
    read: false,
    scrapped: true,
    ...overrides,
  };
}

describe('categorizeScrapsByInterest — short-token false-positive 차단 (v3.14.1 T2)', () => {
  beforeEach(() => localStorage.clear());

  it('ai_ml interest의 keyword "ai"가 "daily training" 텍스트에 매칭되지 않는다', () => {
    saveUser(mkUser({ interests: ['ai_ml'], gamificationMigrated: true }));
    saveBriefings([
      mkBriefing({ id: 'b1', title: 'daily training session', summary: 'retail digest' }),
    ]);
    const snap = takeSnapshot();
    expect(snap.engagedInterests.has('ai_ml')).toBe(false);
  });

  it('ai_ml interest는 "AI 동향 정리" 텍스트에 매칭된다 (true-positive 유지)', () => {
    saveUser(mkUser({ interests: ['ai_ml'], gamificationMigrated: true }));
    saveBriefings([
      mkBriefing({ id: 'b2', title: 'AI 동향 정리', summary: 'ml engineering 사례' }),
    ]);
    const snap = takeSnapshot();
    expect(snap.engagedInterests.has('ai_ml')).toBe(true);
  });

  it('한국어 interest(인사_총무)는 길이>3로 includes 유지 — "인사제도 개편" 매칭', () => {
    saveUser(mkUser({ interests: ['hr_ga'], gamificationMigrated: true }));
    saveBriefings([
      mkBriefing({ id: 'b3', title: '인사제도 개편 동향', summary: '' }),
    ]);
    const snap = takeSnapshot();
    // hr_ga의 keyword 중 "인사" 또는 "총무"가 텍스트에 들어있어야 매칭
    // (interestKeywords('hr_ga') 결과에 따라; 실제 매칭 여부는 catalog label에 의존)
    // → 본 테스트는 "한국어 토큰은 includes 유지" 동작 검증이 목적이라 결과는
    //   true/false 둘 중 하나로 결정적이면 OK. 실제 INTERESTS catalog의 hr_ga label을
    //   참조해 keyword를 도출 후 명시.
    expect(typeof snap.engagedInterests.has('hr_ga')).toBe('boolean');
  });
});
```

> **주의 (implementer):** Step 1 작성 전 `src/state/categories.ts`의 `INTERESTS` 배열에서 `'hr_ga'`(또는 동등) interest의 label을 grep해 keyword를 확인할 것. label이 한국어 한 토큰이면 위 3번째 테스트의 expectation을 `true`로 명시적 단언으로 강화. label이 없거나 다른 ID면 다른 한국어 interest로 교체.

- [ ] **Step 2: 테스트 실행으로 첫 번째 케이스 fail 확인**

```bash
npm test -- tests/unit/state/achievements-cross-interest.spec.ts
```

Expected: 첫 케이스 FAIL — `engagedInterests.has('ai_ml')` should be `false` but is `true` (substring 'ai' matches 'd**ai**ly').

- [ ] **Step 3: achievements.ts에 matchKeyword 적용**

`src/state/achievements.ts:10` 아래 import 추가:

```typescript
import { interestKeywords, matchKeyword } from '../utils/interestKeywords';
```

(기존 line 10 `import { interestKeywords } ...`을 `import { interestKeywords, matchKeyword } ...`로 변경.)

`src/state/achievements.ts:37` 변경:

- 기존: `if (interestKeywords(i).some(k => hay.includes(k))) engaged.add(i);`
- 신규: `if (interestKeywords(i).some(k => matchKeyword(hay, k))) engaged.add(i);`

- [ ] **Step 4: 테스트 PASS 확인**

```bash
npm test -- tests/unit/state/achievements-cross-interest.spec.ts
```

Expected: 3 tests PASS.

- [ ] **Step 5: 기존 achievements 테스트 회귀 0건 확인**

```bash
npm test -- tests/unit/state/achievements.spec.ts tests/unit/state/cumulative-counters.spec.ts
```

Expected: 모두 PASS (변경 없음).

- [ ] **Step 6: Commit**

```bash
git add src/state/achievements.ts tests/unit/state/achievements-cross-interest.spec.ts
git commit -m "fix(v3.14.1 T2): categorizeScrapsByInterest short-token false-positive 차단"
```

---

### Task 3: src/ui/missions-section.ts:53 lint 정리 (XSS 자가진단 포함)

**Why:** `root.innerHTML = html`이 eslint `no-restricted-syntax`에 걸림. 단 interpolation은 모두 (a) PERIODS 리터럴 union, (b) GROUP_TITLE static record, (c) 숫자(progress count), (d) `escapeHtml(def.text)` 처리된 catalog 텍스트 → XSS 노출 0. `eslint-disable-next-line`로 의도적 허용 + 보안 코멘트 명시.

**XSS 자가 진단:**
- `${listId}`: `'missionGroupDaily'` 등 PERIODS 정적 변환
- `${period}`: `'daily'|'weekly'|'monthly'` 리터럴 union
- `${GROUP_TITLE[period]}`: 정적 const Record
- `${completedCount}`, `${missions.length}`: `number` 산출
- `${missions.map(renderCard).join('')}`: renderCard 내부에서 `escapeHtml(ariaLabel)`, `escapeHtml(def.text)` 적용 (line 25-26 확인됨), 나머지는 `m.progress`/`def.target`/`def.rewardXp` 모두 number, `cls`는 정적 클래스 union
- 결론: **XSS 노출 없음**. 단 catalog가 향후 외부 입력 도입 시 escapeHtml 누락 위험 — 코멘트로 invariant 고지.

**Files:**
- Modify: `src/ui/missions-section.ts:49-53`

- [ ] **Step 1: line 49-53에 disable 코멘트 + 안전성 메모 추가**

기존:

```typescript
export function renderMissionsSection(root: HTMLElement, active: MissionInstance[]): void {
  const html = `<section id="missionsSection" class="missions-section" aria-label="미션">
    ${PERIODS.map(p => renderGroup(p, active.filter(m => m.period === p))).join('')}
  </section>`;
  root.innerHTML = html;
```

신규:

```typescript
export function renderMissionsSection(root: HTMLElement, active: MissionInstance[]): void {
  const html = `<section id="missionsSection" class="missions-section" aria-label="미션">
    ${PERIODS.map(p => renderGroup(p, active.filter(m => m.period === p))).join('')}
  </section>`;
  // v3.14.1 T3: 모든 interpolation은 정적 catalog/리터럴 union 또는 escapeHtml 처리됨 (renderCard:25-26).
  // catalog에 외부 입력이 도입되면 escapeHtml 추가 필수 — 그 전까지 의도적 허용.
  // eslint-disable-next-line no-restricted-syntax
  root.innerHTML = html;
```

- [ ] **Step 2: lint 확인 — error 16→15**

```bash
npm run lint 2>&1 | tail -5
```

Expected: `✖ 23 problems (15 errors, 8 warnings)` (1건 감소).

- [ ] **Step 3: missions-section 단위 테스트 회귀 0건 확인**

```bash
npm test -- tests/unit/ui/missions-section.spec.ts
```

Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add src/ui/missions-section.ts
git commit -m "chore(v3.14.1 T3): missions-section.ts:53 — XSS-safe 안전성 명시 + eslint-disable"
```

---

### Task 4: 테스트 파일 lint 일괄 정리 (15 errors)

**Why:** 테스트가 jsdom에서 DOM seed를 위해 `innerHTML` 사용 — XSS 표면 없음(테스트 코드는 production 번들에 포함 안 됨). 기계적 disable로 일괄 처리.

**Files (15 lines + 1 unused import):**
- `tests/unit/state-user.spec.ts:4` — `DEFAULT_MISSIONS` import 제거
- `tests/unit/ui/badge-detail.spec.ts:8, 42` — disable 2건
- `tests/unit/ui/badge-tooltip.spec.ts:18` — disable 1건
- `tests/unit/ui/hydrate-badges.spec.ts:20, 98` — disable 2건
- `tests/unit/ui/level-up.spec.ts:6` — disable 1건
- `tests/unit/ui/missions-section.spec.ts:5` — disable 1건
- `tests/unit/ui/rewards-mission.spec.ts:6` — disable 1건
- `tests/unit/ui/rewards.spec.ts:6` — disable 1건
- `tests/unit/ui/streak-pulse.spec.ts:9` — disable 1건
- `tests/unit/ui/welcome-gamification.spec.ts:9, 58` — disable 2건
- `tests/unit/ui/xp-float.spec.ts:6, 52` — disable 2건

- [ ] **Step 1: state-user.spec.ts:4 unused import 제거**

`tests/unit/state-user.spec.ts:4`:

- 기존: `import { mkUser, DEFAULT_MISSIONS } from './state/userFixture';`
- 신규: `import { mkUser } from './state/userFixture';`

- [ ] **Step 2: 13개 spec 파일에 disable 코멘트 일괄 삽입**

각 파일의 해당 line 바로 앞에 다음 코멘트를 추가:

```typescript
// eslint-disable-next-line no-restricted-syntax
```

> **주의:** insertAdjacentHTML도 같은 룰 ID(`no-restricted-syntax`) — 동일 disable 코멘트로 처리. (`hydrate-badges.spec.ts:98`)

`Edit` 또는 `Read`+`Write`로 파일별 처리. 같은 line 번호 기준 grep해서 의도하지 않은 위치에 들어가지 않게 주의.

예시 (badge-detail.spec.ts:8):

```typescript
// 기존
beforeEach(() => {
  document.body.innerHTML = `...`;
});

// 신규
beforeEach(() => {
  // eslint-disable-next-line no-restricted-syntax
  document.body.innerHTML = `...`;
});
```

- [ ] **Step 3: lint 확인 — error 15→0**

```bash
npm run lint 2>&1 | tail -5
```

Expected: `✖ 8 problems (0 errors, 8 warnings)`. **0 errors 필수.**

- [ ] **Step 4: 테스트 회귀 0건 확인**

```bash
npm test
```

Expected: 671 pass (668 baseline + 3 신규 from T2). 변동 0건이면 disable 코멘트가 동작에 영향 없음을 증명.

- [ ] **Step 5: Commit**

```bash
git add tests/unit/state-user.spec.ts tests/unit/ui/*.spec.ts
git commit -m "chore(v3.14.1 T4): 테스트 lint 일괄 정리 — 16 errors → 0 (innerHTML disable + unused import)"
```

---

### Task 5: hydrate-badges flake fix (50ms timeout → vi.waitFor)

**Why:** `tests/unit/ui/hydrate-badges.spec.ts:102`의 `await new Promise(r => setTimeout(r, 50))`는 dynamic import 완료를 가정하는 fixed wait. cold-start 환경에서 50ms 부족 시 `.badge-modal` 단언 실패. vitest의 `vi.waitFor`로 polling.

**Files:**
- Modify: `tests/unit/ui/hydrate-badges.spec.ts:96-104`

- [ ] **Step 1: vi import 확인 및 추가**

`tests/unit/ui/hydrate-badges.spec.ts:1`:

- 기존: `import { describe, it, expect, beforeEach } from 'vitest';`
- 신규: `import { describe, it, expect, beforeEach, vi } from 'vitest';`

- [ ] **Step 2: line 102의 fixed wait을 vi.waitFor로 교체**

기존 (96-104):

```typescript
it('earned 뱃지 click → openModal (badge-detail)', async () => {
  saveUser(mkUser({ streak: 5, earnedBadges: { 'streak-3': 1700000000000 }, gamificationMigrated: true }));
  // eslint-disable-next-line no-restricted-syntax
  document.body.insertAdjacentHTML('beforeend', '<div id="modalRoot"></div>');
  hydrateStats();
  const earned = document.querySelector<HTMLButtonElement>('.badge--earned');
  earned?.click();
  await new Promise(r => setTimeout(r, 50));  // lazy import
  expect(document.querySelector('.badge-modal')).not.toBeNull();
});
```

신규:

```typescript
it('earned 뱃지 click → openModal (badge-detail)', async () => {
  saveUser(mkUser({ streak: 5, earnedBadges: { 'streak-3': 1700000000000 }, gamificationMigrated: true }));
  // eslint-disable-next-line no-restricted-syntax
  document.body.insertAdjacentHTML('beforeend', '<div id="modalRoot"></div>');
  hydrateStats();
  const earned = document.querySelector<HTMLButtonElement>('.badge--earned');
  earned?.click();
  // v3.14.1 T5: dynamic import 완료를 polling — fixed 50ms cold-start flake 차단
  await vi.waitFor(() => {
    expect(document.querySelector('.badge-modal')).not.toBeNull();
  }, { timeout: 2000, interval: 20 });
});
```

> **주의:** T4에서 line 98의 disable 코멘트가 추가된 상태라면 그대로 유지. T4 미완 상태로 T5 먼저 작업 시 line 98에 disable 추가 필요.

- [ ] **Step 3: 테스트 실행 — N=5 안정 확인**

```bash
for i in 1 2 3 4 5; do npm test -- tests/unit/ui/hydrate-badges.spec.ts 2>&1 | grep -E "(passed|failed)" | tail -2; done
```

Expected: 5번 모두 9 passed.

- [ ] **Step 4: Commit**

```bash
git add tests/unit/ui/hydrate-badges.spec.ts
git commit -m "test(v3.14.1 T5): hydrate-badges flake — vi.waitFor로 lazy import polling"
```

---

### Task 6: v3.12 game-feedback Playwright flake fix

**Why:** `tests/smoke/v3.12-game-feedback.spec.ts:48-63`의 "답변 제출 → toast--badge + xp-float" 케이스가 full-suite first-run 시 flake. 원인 후보: (a) page hydration 미완료 상태에서 textarea fill, (b) submit 후 reward 이벤트 dispatch 타이밍, (c) 2000ms timeout이 cold-start에 빡빡함.

**전략:**
1. submit 전에 `submitBtn`이 enabled 됐는지 명시 wait
2. visibility timeout 2000 → 5000ms (cold-start margin)
3. xp-float 단언 후 toast 단언 사이의 race를 제거하기 위해 두 selector를 동시 polling

**Files:**
- Modify: `tests/smoke/v3.12-game-feedback.spec.ts:48-63`

- [ ] **Step 1: 현재 spec 라인 48-63 교체**

기존:

```typescript
test('답변 제출 → toast--badge (첫 답변) + xp-float visible', async ({ page }) => {
  const textarea = page.locator('#answerArea');
  await expect(textarea).toBeVisible({ timeout: 10_000 });
  await textarea.fill('오늘은 TypeScript 타입 가드를 공부했고, 생각보다 유용했다.');

  await page.locator('#submitBtn').click();

  // +XP floating animation
  await expect(page.locator('.xp-float')).toBeVisible({ timeout: 2000 });

  // answers-1 badge unlock toast (v3.13 미션 입문 뱃지와 공존 — 첫 답변 toast로 명시 필터)
  const toast = page.locator('.toast--badge').filter({ hasText: '첫 답변' });
  await expect(toast).toBeVisible({ timeout: 2000 });
  // 토스트 안에 "첫 답변" (badge name) 또는 "뱃지 획득" (suffix)
  await expect(toast).toContainText(/첫 답변|뱃지 획득/);
});
```

신규:

```typescript
test('답변 제출 → toast--badge (첫 답변) + xp-float visible', async ({ page }) => {
  const textarea = page.locator('#answerArea');
  await expect(textarea).toBeVisible({ timeout: 10_000 });
  await expect(textarea).toBeEnabled({ timeout: 10_000 });

  await textarea.fill('오늘은 TypeScript 타입 가드를 공부했고, 생각보다 유용했다.');

  // v3.14.1 T6: submit 버튼이 활성화될 때까지 명시 대기 (hydration race 제거)
  const submitBtn = page.locator('#submitBtn');
  await expect(submitBtn).toBeEnabled({ timeout: 5000 });
  await submitBtn.click();

  // +XP floating animation (cold-start 마진 5000ms)
  await expect(page.locator('.xp-float')).toBeVisible({ timeout: 5000 });

  // answers-1 badge unlock toast (v3.13 미션 입문 뱃지와 공존 — 첫 답변 toast로 명시 필터)
  const toast = page.locator('.toast--badge').filter({ hasText: '첫 답변' });
  await expect(toast).toBeVisible({ timeout: 5000 });
  await expect(toast).toContainText(/첫 답변|뱃지 획득/);
});
```

- [ ] **Step 2: N=5 first-run 안정 확인**

```bash
for i in 1 2 3 4 5; do
  npx playwright test tests/smoke/v3.12-game-feedback.spec.ts --reporter=line 2>&1 | tail -3;
done
```

Expected: 5번 모두 2 passed.

- [ ] **Step 3: 전체 Playwright smoke 회귀 0건 확인**

```bash
npm run test:smoke 2>&1 | tail -10
```

Expected: 93 passed (변동 없음).

- [ ] **Step 4: Commit**

```bash
git add tests/smoke/v3.12-game-feedback.spec.ts
git commit -m "test(v3.14.1 T6): v3.12 game-feedback flake — submit precondition + 5s margin"
```

---

### Task 7: 최종 검증 + 메트릭 + 메모리 doc

**Why:** DoD(빌드/lint/test/번들 delta) 충족 확인 + project memory 갱신.

**Files:**
- Read-only: `npm run build`, `npm run lint`, `npm test`, `npm run test:smoke`
- Update: `<claude-memory>/memory/MEMORY.md` 인덱스
- Create: `<claude-memory>/memory/project_daily_growth_v3141.md`

- [ ] **Step 1: 빌드 + lint + test 일괄 실행**

```bash
npm run build 2>&1 | tail -10
```

Expected: vite build 성공. `dist/assets/*.js` gzip 사이즈 기록 (baseline 31.81 kB).

```bash
npm run lint 2>&1 | tail -3
```

Expected: `✖ 8 problems (0 errors, 8 warnings)` — 0 errors.

```bash
npm test 2>&1 | tail -5
```

Expected: 671 vitest pass.

```bash
npm run test:smoke 2>&1 | tail -3
```

Expected: 93 Playwright pass.

- [ ] **Step 2: 번들 delta 측정**

```bash
ls -la dist/assets/*.js | awk '{print $5, $9}'
gzip -kc dist/assets/index-*.js | wc -c
```

기록: baseline 31.81 → 현재 X.XX kB, Δ = +Y.YY (DoD ±0.2 kB 안 안에 들어가는지 확인).

- [ ] **Step 3: 메모리 doc 작성**

`memory/project_daily_growth_v3141.md` 작성 (프론트매터 + body):

- 핵심: v3.14 P2 4건 cleanup, vitest +3 (T2 신규), lint 16→0, bundle Δ ±0.2 kB
- T1 lesson: state→ui 역방향 의존 회피를 위해 utils 격상은 사이클 시작 시점에 결정해야
- T6 lesson: Playwright cold-start 안정화는 timeout 상향 + 명시적 enabled wait이 표준

`MEMORY.md`에 한 줄 인덱스 추가:

```markdown
- [DG v3.14.1 P2 Cleanup](project_daily_growth_v3141.md) — **✅ 배포 완료 (2026-05-01, main HEAD `<HASH>`)** — N commits · 671 vitest (+3) + 93 Playwright · lint 16→0 · matchKeyword utils 격상 + achievements short-token 차단 + 2 flake 안정화
```

- [ ] **Step 4: 사용자 검토 요청 (codex 2-pass)**

v3.14 lesson #4: "default for substantial cycles: codex 2-pass가 표준 깊이 review의 비용 대비 가치 가장 높음".

- 사용자에게 "codex 2-pass review 돌릴까요?" 확인 후 dispatch.
- P0/P1 in-cycle fix → P2는 v3.14.2/v3.15 backlog.

- [ ] **Step 5: 사용자 승인 시 main 직배포**

> **절대 규칙:** "배포해줘" 명시 전에는 `git push`/`firebase deploy` 금지. 본 사이클은 origin remote 미등록 → main fast-forward + Firebase Hosting CLI deploy 패턴 (v3.14와 동일).

---

## Self-Review Checklist (plan 작성자)

**1. Spec coverage:**
- ✅ #1 (hydrate-badges flake) → T5
- ✅ #2 (v3.12 first-run flake) → T6
- ✅ #3 (16 lint errors) → T3 (src 1건) + T4 (test 15건 + unused import 1건)
- ✅ #4 (categorizeScrapsByInterest matchKeyword) → T2 (+ T1 prerequisite)

**2. Placeholder scan:** 모든 step에 실제 코드/명령/expected output 포함. 단 T2 Step 1의 `'hr_ga'` interest는 implementer가 catalog에서 정확한 ID/label을 grep해서 채우라고 명시.

**3. Type consistency:**
- `matchKeyword(hay: string, keyword: string): boolean` — T1/T2 동일
- `interestKeywords(id: string): string[]` — 기존 시그니처 변동 없음
- `categorizeScrapsByInterest`의 반환 타입 `{ engaged: Set<string>; uniqueCount: number }` 변동 없음

**4. v3.14 lessons 적용 확인:**
- ✅ "Plan은 codebase 실제 키·시그니처 사전 grep" — T1/T2 모두 실제 line 번호로 매핑됨, INTERESTS catalog 확인 지시 포함
- ✅ "함수명 컨벤션 사전 확인" — `matchKeyword` 시그니처 v3.14와 동일 유지
- ✅ "Codex 2-pass" — T7 Step 4에 명시
