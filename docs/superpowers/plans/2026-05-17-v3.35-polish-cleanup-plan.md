# v3.35 Polish + Cleanup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v3.34 carry 4건 청산 — questionText `<mark>` highlight 확장(C2) + archive chunk bundle metric 신규(C1) + ranking NFC micro-opt(C5) + cross-product spec(C6).

**Architecture:** archive.ts:renderAnswerCard에 currentTokens 분기 추가(highlightHtml 재사용 — body와 동일 pattern). ranking.ts 단일 normalize 변수 추출. CLAUDE.md §Bundle 측정 표준에 archive chunk dual record 라인 추가.

**Tech Stack:** TypeScript / Vite SPA / Vitest / Playwright / ESLint.

**Spec**: `docs/superpowers/specs/2026-05-17-v3.35-polish-cleanup-design.md`.

**Baseline (T0)**: HEAD `18cc113` / bundle 19079 B (index) + **7173 B (archive 2 chunk 합산)** / vitest 1332 / smoke 100 / lint 0.

---

### Task 0: Spec + Plan + Codex 사전 review

**Files:**
- Spec: `docs/superpowers/specs/2026-05-17-v3.35-polish-cleanup-design.md` (이미 작성됨)
- Plan: `docs/superpowers/plans/2026-05-17-v3.35-polish-cleanup-plan.md` (본 파일)

- [ ] **Step 1: archive chunk T0 baseline 측정** ✅ 실측 완료

```bash
gzip -c dist/assets/archive-*.js | wc -c
```
Actual: **7173 B** (archive handler 4.97 kB + archive-detail 2.18 kB 두 chunk gzip 합산).

- [ ] **Step 2: Codex 사전 review CLI direct (mandatory, v3.24 graduated)**

```bash
codex exec --skip-git-repo-check -o /tmp/v3.35-codex-pre.txt "v3.35 spec docs/superpowers/specs/2026-05-17-v3.35-polish-cleanup-design.md + plan docs/superpowers/plans/2026-05-17-v3.35-polish-cleanup-plan.md를 사전 review. P0/P1/P2 분류 + in-cycle 반영 가능 여부 판단. 본 사이클은 polish + cleanup으로 v3.34 carry 4건(C1 archive chunk metric / C2 questionText highlight / C5 NFC micro-opt / C6 cross-product spec) 청산. XSS 안전성 + bundle threshold + spec coverage 중심. 5분 이내 분석."
```

Expected: P0/P1 명시. 모두 in-cycle 흡수 후 plan v2.

- [ ] **Step 3: P0/P1 흡수 → plan v2**

(Codex 결과 기반 — 단순 spec edit + plan amend. T2 진입 전 완료 의무.)

- [ ] **Step 4: commit T0**

```bash
git add docs/superpowers/specs/2026-05-17-v3.35-polish-cleanup-design.md docs/superpowers/plans/2026-05-17-v3.35-polish-cleanup-plan.md
git commit -m "$(cat <<'EOF'
docs(v3.35 T0): spec + plan v2 + Codex 사전 review CLI direct (P0=0/P1=3/P2=4 모두 in-cycle 흡수)

baseline: HEAD 18cc113 / bundle 19079 (index) + 7173 (archive 2 chunk 합산) / vitest 1332 / smoke 100 / lint 0.

Codex 사전 review 흡수:
- P1-1 archive baseline 7173 실측 정정 (plan TBD/4900~5100 추정값 삭제)
- P1-2 ranking spec 경로 tests/utils/ → tests/unit/ (3 spot)
- P1-3 T2 red gate spec — search() helper + handleArchiveSearch 직접 호출 패턴 (debounce 200ms 우회), off-state 명시 invariant
- P2-1 최종 review commit range bf27100 → 18cc113 정정
- P2-2 Bundle threshold 3-tier source-of-truth 분리 (cycle gate/CLAUDE 표준/archive soft)
- P2-3 T2 spec에 <img onerror> XSS guard fixture 1건 추가
- P2-4 초성-only 토큰 mark 없음 (highlight.ts:10 graduated 정책) spec §4.1 명시
EOF
)"
```

---

### Task 1: C5 — ranking.ts NFC normalize 1회

**Files:**
- Modify: `src/utils/ranking.ts:17-22`
- Test: `tests/unit/ranking.spec.ts` (기존 9 case 그대로 통과 검증)

- [ ] **Step 1: 기존 9 case PASS baseline**

```bash
npm test -- tests/unit/ranking.spec.ts --run 2>&1 | tail -10
```
Expected: 9 passed.

- [ ] **Step 2: ranking.ts:17-22 변경**

```ts
// 변경 전 (line 17-22)
const normalized = fields.map((f) => ({
  weight: f.weight,
  body: f.text.normalize('NFC').toLowerCase(),
  initials: getInitialConsonants(f.text.normalize('NFC').toLowerCase()),
}));

// 변경 후
const normalized = fields.map((f) => {
  const text = f.text.normalize('NFC').toLowerCase();
  return {
    weight: f.weight,
    body: text,
    initials: getInitialConsonants(text),
  };
});
```

- [ ] **Step 3: 9 case PASS 재확인**

```bash
npm test -- tests/unit/ranking.spec.ts --run 2>&1 | tail -10
```
Expected: 9 passed (변동 없음).

- [ ] **Step 4: tsc + lint**

```bash
npx tsc --noEmit && npm run lint
```
Expected: 0 errors / 0 problems.

- [ ] **Step 5: commit T1**

```bash
git add src/utils/ranking.ts
git commit -m "$(cat <<'EOF'
refactor(v3.35 T1): ranking.ts NFC normalize 2회→1회 — single variable extraction

f.text.normalize('NFC').toLowerCase() 호출 1회로 줄임. body / initials 둘 다 동일 normalized 값 재사용. 기존 9 case 그대로 PASS.

v3.34 T1 reviewer M1 carry 청산.
EOF
)"
```

---

### Task 2: C2 — archive.ts renderAnswerCard questionText highlight

**Files:**
- Modify: `src/ui/handlers/archive.ts:570-579` (renderAnswerCard questionText 분기)
- Test: `tests/ui/archive-ranking.spec.ts` (DOM-level spec 2건 추가)

- [ ] **Step 1: 실패 spec 작성 — 기존 helper(`mkAnswer`/`setup`/`search`) 재사용 + XSS fixture 1건**

```ts
// tests/ui/archive-ranking.spec.ts 안 describe('v3.34 T3: archive ranking integration (DOM-level)') 내부에 추가

it('v3.35 C2: questionText hit 시 archive-card-question에 <mark> 적용', () => {
  saveAnswers([
    mkAnswer({
      id: 'q-hit-only',
      questionText: 'TypeScript best practices',
      text: '답변 본문은 매칭 안 됨',
      createdAt: '2026-05-15T00:00:00.000Z',
    }),
  ]);
  setup();
  clickEntity('answer');
  search('typescript');  // handleArchiveSearch() 직접 호출 — debounce 우회

  const card = document.querySelector('[data-answer-id="q-hit-only"]');
  expect(card).toBeTruthy();
  const question = card!.querySelector('.archive-card-question');
  // ✅ <mark> 삽입 + ❓ prefix 유지
  expect(question?.innerHTML).toContain('<mark>');
  expect(question?.textContent).toContain('❓');
});

it('v3.35 C2: currentTokens 0일 때 archive-card-question은 textContent 유지 (<mark> 없음)', () => {
  saveAnswers([
    mkAnswer({
      id: 'no-tokens',
      questionText: '질문 본문',
      text: '본문 답변',
      createdAt: '2026-05-15T00:00:00.000Z',
    }),
  ]);
  setup();
  clickEntity('answer');
  // search() 호출 X — currentTokens 0 상태

  const card = document.querySelector('[data-answer-id="no-tokens"]');
  const question = card!.querySelector('.archive-card-question');
  expect(question?.innerHTML).not.toContain('<mark>');
  expect(question?.textContent).toBe('❓ 질문 본문');
});

it('v3.35 C2 XSS guard: questionText에 <img onerror>가 있어도 mark 분기에서 escape 후 inject', () => {
  saveAnswers([
    mkAnswer({
      id: 'xss-q',
      questionText: '<img src=x onerror=alert(1)> typescript test',
      text: '답변',
      createdAt: '2026-05-15T00:00:00.000Z',
    }),
  ]);
  setup();
  clickEntity('answer');
  search('typescript');

  const card = document.querySelector('[data-answer-id="xss-q"]');
  const question = card!.querySelector('.archive-card-question')!;
  // ✅ raw <img>는 절대 inject되지 않음 (escape됨)
  expect(question.querySelector('img')).toBeNull();
  // ✅ <mark>는 정상 inject
  expect(question.innerHTML).toContain('<mark>');
  // ✅ &lt; entity로 escape됐는지 확인
  expect(question.innerHTML).toContain('&lt;img');
});
```

**Note (Codex 사전 P2-4)**: 초성-only 토큰은 `<mark>` 없음 (highlight.ts:10 graduated 정책). 본 spec은 substring token 케이스만 검증.

- [ ] **Step 2: 실패 확인**

```bash
npm test -- tests/ui/archive-ranking.spec.ts --run 2>&1 | tail -15
```
Expected: **3 fail** — (1) questionText `<mark>` inject 없음 (현재 textContent), (2) off-state는 textContent로 이미 PASS 가능하지만 명시적 invariant로 spec화, (3) XSS guard도 현재 textContent라 mark inject 없음으로 fail. (Codex P1-3 흡수: off-state는 정확히는 PASS 가능 → 명시 spec으로 codify, 다른 2건 빨간 게이트.)

- [ ] **Step 3: archive.ts:570-579 변경**

```ts
// 변경 전
const q = document.createElement('p');
if (a.questionText) {
  q.className = 'archive-card-question';
  q.textContent = `❓ ${a.questionText}`;
} else {
  q.className = 'archive-card-question archive-card-question--missing';
  q.textContent = '질문 정보 없음 (이전 버전 답변)';
}
card.append(q);

// 변경 후
const q = document.createElement('p');
if (a.questionText) {
  q.className = 'archive-card-question';
  if (currentTokens.length > 0) {
    // eslint-disable-next-line no-restricted-syntax -- highlightHtml escapeHtml + escapeRegex 적용, <mark> only inject
    q.innerHTML = `❓ ${highlightHtml(a.questionText, currentTokens)}`;
  } else {
    q.textContent = `❓ ${a.questionText}`;
  }
} else {
  q.className = 'archive-card-question archive-card-question--missing';
  q.textContent = '질문 정보 없음 (이전 버전 답변)';
}
card.append(q);
```

- [ ] **Step 4: 3 spec PASS 확인**

```bash
npm test -- tests/ui/archive-ranking.spec.ts --run 2>&1 | tail -15
```
Expected: **8 passed** (기존 5 + 신규 3 = questionText hit + off-state invariant + XSS guard).

- [ ] **Step 5: tsc + lint**

```bash
npx tsc --noEmit && npm run lint
```
Expected: 0 errors / 0 problems.

- [ ] **Step 6: commit T2**

```bash
git add src/ui/handlers/archive.ts tests/ui/archive-ranking.spec.ts
git commit -m "$(cat <<'EOF'
feat(v3.35 T2): archive renderAnswerCard questionText `<mark>` highlight 확장

v3.34 carry C2 청산 — Answer 검색이 questionText를 매칭 대상으로 확장됐으나 hit 결과의 <mark> 시각화는 a.text만 적용 중이었음. questionText preview에도 currentTokens 활성 시 highlightHtml 적용. ❓ emoji prefix는 코드 리터럴이라 escape 불필요, innerHTML 결합으로 외부 prefix.

DOM-level spec 2건 추가 (highlight on / off).
EOF
)"
```

---

### Task 3: C6 — ranking.spec.ts cross-product case 1건

**Files:**
- Test: `tests/unit/ranking.spec.ts`

- [ ] **Step 1: cross-product case 추가**

```ts
// tests/unit/ranking.spec.ts 안에 추가

it('다중 field × 다중 token cross-product max', () => {
  const fields = [
    { text: 'TypeScript best practices', weight: 2 },
    { text: 'React hooks rules', weight: 1 },
  ];
  const tokens = ['typescript', 'react'];
  // typescript: field 1 hit (weight 2) — max 2
  // react: field 2 hit (weight 1) — max 1
  // total: 2 + 1 = 3
  expect(scoreEntry(fields, tokens)).toBe(3);
});
```

- [ ] **Step 2: PASS 확인**

```bash
npm test -- tests/unit/ranking.spec.ts --run 2>&1 | tail -10
```
Expected: 10 passed (기존 9 + 신규 1).

- [ ] **Step 3: tsc**

```bash
npx tsc --noEmit
```
Expected: 0 errors.

- [ ] **Step 4: commit T3**

```bash
git add tests/unit/ranking.spec.ts
git commit -m "$(cat <<'EOF'
test(v3.35 T3): ranking cross-product case 1건 (다중 field × 다중 token max)

v3.34 T1 reviewer M5 carry 청산. scoreEntry의 토큰별 max(field weight) invariant가 cross-product 케이스에서도 유지되는지 명시. v3.34 T3 DOM-level spec이 간접 cover했으나 unit-level 명시 추가.
EOF
)"
```

---

### Task 4: C1 — CLAUDE.md §Bundle 측정 표준 갱신 (archive chunk dual record)

**Files:**
- Modify: `CLAUDE.md` (§Cycle Workflow Bundle 측정 표준)

- [ ] **Step 1: CLAUDE.md 갱신**

기존 라인:

```markdown
**Bundle 측정 표준 (v3.14.5 T5 graduation)**: 사이클 retro에 bundle 절대값 기록 시 `gzip -c dist/assets/index-*.js | wc -c` 단일 stream을 canonical로 기록한다. vite reporter chunked estimation은 dual-record 폐기 (v3.14.4가 마지막 사례).
```

변경 후:

```markdown
**Bundle 측정 표준 (v3.14.5 T5 graduation, v3.35 T4 extension)**:

1. **Canonical (deploy gate)** — main entry size:
   `gzip -c dist/assets/index-*.js | wc -c`

2. **Archive chunk (dual record, v3.35+)** — archive 영역 변경 시 누락 catch:
   `gzip -c dist/assets/archive-*.js | wc -c`

사이클 retro §Bundle 측정에 두 값 모두 기록. Threshold:
- index: GREEN ≤ +300 B / AMBER ≤ +500 B (v3.14.5 그대로)
- archive chunk: GREEN ≤ +300 B / AMBER ≤ +600 B (신규, archive 영역 변경 시만 적용)

vite reporter chunked estimation은 dual-record 폐기 (v3.14.4가 마지막 사례).
```

- [ ] **Step 2: commit T4**

```bash
git add CLAUDE.md
git commit -m "$(cat <<'EOF'
docs(v3.35 T4): CLAUDE.md §Bundle 측정 표준 extension — archive chunk dual record

v3.34 carry C1 청산 — v3.34에서 ranking 코드가 archive dynamic chunk로 emitted됐으나 canonical metric(index entry만)은 size impact를 underestimate (+1.25 kB가 +3 B로만 reflected). archive 영역 변경 시 archive-*.js gzip 별도 측정 의무.

Threshold: archive chunk GREEN ≤ +300 B / AMBER ≤ +600 B (lazy load이라 first paint 영향 X, index 보다 관대).
EOF
)"
```

---

### Task 5: 사이클-끝 검증 + Codex 최종 review

- [ ] **Step 1: 3종 검증**

```bash
npx tsc --noEmit && npm run lint && npm test -- --run
```
Expected: 0 errors / 0 problems / **1336 passed** (baseline 1332 + T2 +3 + T3 +1).

- [ ] **Step 2: 양쪽 TZ vitest**

```bash
TZ=Asia/Seoul npm test -- --run 2>&1 | tail -3
TZ=America/New_York npm test -- --run 2>&1 | tail -3
```
Expected: 양쪽 1336 (drift 0).

- [ ] **Step 3: smoke**

```bash
npm run test:smoke 2>&1 | tail -5
```
Expected: 100 passed (변동 없음).

- [ ] **Step 4: build + bundle dual 측정**

```bash
npm run build && echo "=== index ===" && gzip -c dist/assets/index-*.js | wc -c && echo "=== archive ===" && gzip -c dist/assets/archive-*.js | wc -c
```
Expected: index ≤ 19279, archive baseline ± 300.

- [ ] **Step 5: 신규 spec N=5**

```bash
for i in 1 2 3 4 5; do npm test -- tests/ui/archive-ranking.spec.ts tests/unit/ranking.spec.ts --run 2>&1 | grep -E "Tests|passed"; done
```
Expected: 5/5 0 flake.

- [ ] **Step 6: Codex 최종 review CLI direct**

```bash
codex exec --skip-git-repo-check -o /tmp/v3.35-codex-final.txt "v3.35 사이클 최종 review. cycle range 18cc113..HEAD (T0~T4). 변경: archive.ts renderAnswerCard questionText highlight 확장 / ranking.ts NFC 1회 / cross-product spec / CLAUDE.md bundle metric extension. Classification: DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT. P0/P1/P2 명시. tsc 0 / lint 0 / vitest 1336 / smoke 100 / bundle index ___ / archive ___."
```

Expected: APPROVED_WITH_NOTES 이상.

---

### Task 6: Retro

- [ ] **Step 1: spec §6 retro 채움 (final HEAD, task table, Codex 결과, lessons, carry)**

- [ ] **Step 2: MEMORY.md `project_daily_growth_v335.md` entry 추가 + 신규 memory file 작성**

- [ ] **Step 3: Obsidian vault sync (02-Cycles spec / 03-Lessons memory / 00-Index timeline)**

- [ ] **Step 4: commit T6**

```bash
git add docs/superpowers/specs/2026-05-17-v3.35-polish-cleanup-design.md
git commit -m "$(cat <<'EOF'
docs(v3.35 T6 retro): 사이클 완료 — polish + cleanup C1+C2+C5+C6 청산

bundle ___ (index) + ___ (archive) / vitest 1336 / Codex direct CLI 양쪽 시도 (___) / lessons ___ / carry ___.
EOF
)"
```

---

## Self-Review

**Spec coverage**:
- C1 archive chunk metric → T4 ✓
- C2 questionText highlight → T2 ✓
- C5 NFC 1회 → T1 ✓
- C6 cross-product → T3 ✓
- C7 briefings normalize → deferred (spec §4.2) ✓
- 사이클-끝 검증 + Codex 최종 → T5 ✓
- retro + sync → T6 ✓

**Placeholder scan**: T0 step 1/2의 baseline 측정값은 실측 의무 (실행 시점에 산출). T6의 ___는 retro 단계에서 채움. 그 외 placeholder 0.

**Type consistency**: `currentTokens: string[]` / `highlightHtml(text, tokens)` / `scoreEntry(fields, tokens)` 모두 v3.34 기존 signature 정합. 신규 type 0개.
