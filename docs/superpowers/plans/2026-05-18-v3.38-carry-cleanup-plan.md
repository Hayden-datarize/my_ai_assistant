# v3.38 Carry Cleanup + Polish — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v3.37 carry 7건 (C1+C3+C4+C5+C6+C7+C9)을 3 Track (hygiene → data/search → navigation feature)으로 묶어 일괄 청산. Schema/CSP/Migration 0줄.

**Architecture:** ESLint custom rule(`no-nfd-korean-literal`) + 기존 utils 확장(`fuzzy.ts` Levenshtein 추가) + archive search summary UI + stats 탭 progressive nav entry. v3.37 graduated `resetArchiveFilters()` + `navigateToInterestArchive()` 단일 진입점 재사용.

**Tech Stack:** TypeScript strict, Vitest jsdom, Playwright, ESLint custom rule (CommonJS), Vite build, Firebase Hosting.

**Spec:** `docs/superpowers/specs/2026-05-18-v3.38-carry-cleanup-design.md` (commit `9914c2a`)

---

## File Structure

### 신규 파일

| 경로 | 책임 | 생성 task |
|---|---|---|
| `.eslint-rules/no-nfd-korean-literal.js` | 한국어 string literal NFC 강제 ESLint rule | T1 |
| `tests/utils/fuzzy-distance.spec.ts` | Levenshtein 함수 단위 spec | T5a |
| `src/ui/components/archive-search-summary.ts` | entity별 hit count summary chip render | T7a |
| `tests/ui/components/archive-search-summary.spec.ts` | summary chip 단위 spec | T7a |

### 수정 파일

| 경로 | 변경 task | 책임 변경 |
|---|---|---|
| `.eslintrc.cjs` | T1 | local rule plugin 등록 + activation |
| `src/utils/fuzzy.ts` | T5a | Levenshtein distance 함수 추가 (export) |
| `src/ui/handlers/archive.ts` | T5b, T7b | fuzzy match 통합 + entity scope + search summary listener |
| `src/state/briefings.ts` | T4 | `?? ''` 정리 + schema strict 적용 |
| `src/ui/handlers/stats.ts` | T6 | progressive nav chip 추가 |
| `tests/smoke/plant-detail-modal.spec.ts` | T2 | test 2, 3 visibility wait 추가 |
| `tests/ui/modals/plant-detail.spec.ts` | T3 | state-block vacuous spec → observable spy |

---

## Validation Standards (전 task 공통)

각 task commit 직전 3종 검증 통과 의무 (CLAUDE.md graduated):

1. **TS strict**: `npx tsc --noEmit` → 0 error
2. **Lint**: `npm run lint` → 0 error (C9 rule 포함 후 T1+)
3. **Tests**: 영향 범위 spec 전체 PASS. 신규 spec은 N=5 5/5 안정성 (TDD 의미)

양쪽 TZ 검증은 T4/T5/T6/T7처럼 NFC/date 연관 task에 의무:

```bash
TZ=Asia/Seoul npm test -- <spec path>
TZ=America/New_York npm test -- <spec path>
```

Bundle quadruple record는 T1, T4, T5, T6, T7 각 commit 직후 (cycle gate):

```bash
npm run build
echo "index canonical:  $(gzip -c dist/assets/index-*.js | wc -c) B"
echo "archive handler:  $(gzip -c dist/assets/archive-handler-*.js 2>/dev/null | wc -c) B"
echo "archive tab:      $(gzip -c dist/assets/archive-*.js | grep -v handler | head -1 | wc -c) B"
echo "stats:            $(gzip -c dist/assets/stats-*.js | wc -c) B"
```

---

## Task 0: Baseline Capture + Codex 사전 Review

**Files:**
- Read-only (no commit yet)
- Output: `/tmp/v3.38-baseline.txt`, `/tmp/v3.38-codex-pre.txt`

- [ ] **Step 1: 현재 main HEAD 확인**

```bash
git log -1 --oneline
# expected: 9914c2a docs(v3.38): spec v1 — carry cleanup + polish 7건 일괄 청산
```

- [ ] **Step 2: vitest baseline 측정**

```bash
TZ=Asia/Seoul npm test 2>&1 | tail -10 | tee /tmp/v3.38-baseline-vitest-seoul.txt
TZ=America/New_York npm test 2>&1 | tail -10 | tee /tmp/v3.38-baseline-vitest-ny.txt
```

Expected: 1351 passed (양쪽 TZ 동일). drift 발생 시 STOP — v3.37 deploy 직후이므로 0 drift여야 함.

- [ ] **Step 3: smoke baseline 측정 (N=1 cold)**

```bash
npm run build && npm run test:smoke 2>&1 | tail -10 | tee /tmp/v3.38-baseline-smoke.txt
```

Expected: 102 passed.

- [ ] **Step 4: Bundle baseline 측정 (quadruple)**

```bash
npm run build
echo "=== v3.38 baseline ===" | tee /tmp/v3.38-baseline-bundle.txt
echo "index canonical: $(gzip -c dist/assets/index-*.js | wc -c) B" | tee -a /tmp/v3.38-baseline-bundle.txt
ls dist/assets/ | grep -E "^(archive|stats)" | while read f; do
  echo "$f: $(gzip -c dist/assets/$f | wc -c) B" | tee -a /tmp/v3.38-baseline-bundle.txt
done
```

Expected: v3.37 retro 기록 — index 19077 / archive handler 5031 / archive tab 2179 / stats 3582.
실측이 retro 값과 다르면 retro 값을 baseline으로 채택, 실측을 v3.38 baseline으로 기록 (실측이 신뢰).

- [ ] **Step 5: Codex 사전 review 호출 (direct CLI graduated)**

```bash
codex exec --skip-git-repo-check -o /tmp/v3.38-codex-pre.txt "$(cat <<'EOF'
You are reviewing a draft cycle plan for the Daily Growth web app v3.38 carry cleanup + polish cycle.

Spec: docs/superpowers/specs/2026-05-18-v3.38-carry-cleanup-design.md (commit 9914c2a)
Plan: docs/superpowers/plans/2026-05-18-v3.38-carry-cleanup-plan.md

Tasks in plan:
- T1 (C9): NFC normalize ESLint rule + 기존 위반 fix
- T2 (C7): smoke plant-detail-modal test 2/3 garden-card visibility wait 비대칭 fix
- T3 (C6): T2 spec state-block vacuous → observable spy 변경
- T4 (C3): briefings.ts ?? '' 정리 + schema strict
- T5a (C4): src/utils/fuzzy.ts Levenshtein distance 함수 추가
- T5b (C4): archive.ts fuzzy match 통합 + entity scope
- T6 (C5): stats 탭 progressive nav chip 추가 (Insight detail card)
- T7a (C1): archive-search-summary 컴포넌트 신규
- T7b (C1): archive.ts에 summary chip listener wire

Codebase pattern reminders:
- TypeScript strict, no any (warn 모드)
- Vitest jsdom + Playwright smoke (serviceWorkers: 'block' default)
- ESLint `no-restricted-syntax` (innerHTML/outerHTML/insertAdjacentHTML 금지)
- ESLint `no-restricted-imports` (`getDateStr` deprecated, `getKstDateStr` only)
- NFC normalize + Intl.DateTimeFormat KST anchor
- Bundle quadruple record gate (index ≤ +300 B GREEN deploy gate)

Review focus:
1. P0: deep semantic correctness bug, cross-file invariant, security (XSS), data corruption risk
2. P1: spec/plan ambiguity, schema strict ripple miss, missing test case
3. P2: 가독성, polish

Output format:
- VERDICT: PROCEED | REQUIRES_FIX | REJECT
- P0/P1/P2 항목별 issue list with task ID
- 권장 fix
EOF
)"
cat /tmp/v3.38-codex-pre.txt
```

- [ ] **Step 6: Codex 사전 review 결과 분류 + plan v2 fold**

P0 또는 P1 발견 시:
- 본 plan에 in-cycle 흡수. plan v2 commit.
- P2는 v3.39+ carry로 §8 또는 spec §8에 기록.

CLAUDE.md 규칙: P0/P1 plan 반영 전 T1 진입 X.

- [ ] **Step 7: plan v2 commit (Codex 사전 결과 흡수했을 경우)**

```bash
git add docs/superpowers/plans/2026-05-18-v3.38-carry-cleanup-plan.md
git add docs/superpowers/specs/2026-05-18-v3.38-carry-cleanup-design.md  # 필요시
git commit -m "docs(v3.38 T0): Codex 사전 review 흡수 plan v2

Codex 사전 P0=X / P1=Y in-cycle 흡수, P2=Z v3.39+ carry."
```

P0/P1=0이면 본 step skip. T1 진입.

---

## Task 1: C9 — NFC normalize ESLint Rule (Track 3)

**Files:**
- Create: `.eslint-rules/no-nfd-korean-literal.js`
- Modify: `.eslintrc.cjs` (plugin registration + rule activation)
- 후속: 기존 위반 fix (별도 sub-step)

- [ ] **Step 1: ESLint rule 파일 생성**

```bash
mkdir -p .eslint-rules
```

Write `.eslint-rules/no-nfd-korean-literal.js`:

```js
/**
 * v3.38 T1 (C9): 한국어 string literal NFC normalize 강제.
 *
 * NFD (Unicode decomposed) 형태로 한국어가 작성되면 NFC 정규화된 production
 * 데이터와 silent 매칭 fail. 본 rule은 한국어 음절 블록(가-힯) 포함 string literal /
 * template literal이 NFC가 아니면 error.
 *
 * 본 rule은 ESLint local plugin이라 .eslintrc.cjs의 `plugins` 와 `rules` 에서
 * 'local/no-nfd-korean-literal' 로 참조.
 */

/** @type {import('eslint').Rule.RuleModule} */
module.exports = {
  meta: {
    type: 'problem',
    docs: { description: '한국어 string literal은 NFC normalize 필수' },
    schema: [],
    messages: {
      nfd: '한국어 string literal이 NFC normalize 아님 — production 매칭 silent fail risk. .normalize("NFC") 적용 또는 literal 재타이핑.',
    },
  },
  create(context) {
    return {
      Literal(node) {
        if (typeof node.value !== 'string') return;
        if (!/[가-힯]/.test(node.value)) return;
        if (node.value !== node.value.normalize('NFC')) {
          context.report({ node, messageId: 'nfd' });
        }
      },
      TemplateElement(node) {
        const raw = node.value.cooked;
        if (typeof raw !== 'string') return;
        if (!/[가-힯]/.test(raw)) return;
        if (raw !== raw.normalize('NFC')) {
          context.report({ node, messageId: 'nfd' });
        }
      },
    };
  },
};
```

- [ ] **Step 2: ESLint plugin 등록**

Modify `.eslintrc.cjs`:

```js
// Top-level 변경:
// 1) `plugins: ['@typescript-eslint']` → `plugins: ['@typescript-eslint', 'local']`
// 2) rules 블록에 'local/no-nfd-korean-literal': 'error' 추가
```

ESLint local plugin은 `.eslintrc.cjs` 옆 `eslint-plugin-local` 형태로 npm 등록하거나, 인라인 plugin 등록 방식 (간단):

Replace `.eslintrc.cjs` content with:

```js
const localPlugin = {
  rules: {
    'no-nfd-korean-literal': require('./.eslint-rules/no-nfd-korean-literal.js'),
  },
};

module.exports = {
  root: true,
  parser: '@typescript-eslint/parser',
  plugins: ['@typescript-eslint'],
  extends: ['eslint:recommended', 'plugin:@typescript-eslint/recommended'],
  parserOptions: { ecmaVersion: 2020, sourceType: 'module' },
  env: { browser: true, es2020: true, node: true },
  rules: {
    'no-restricted-syntax': [
      // ... (기존 5개 selector 유지)
    ],
    '@typescript-eslint/no-explicit-any': 'warn',
    'no-restricted-imports': ['error', {
      // ... (기존 getDateStr deprecated 유지)
    }],
    'local/no-nfd-korean-literal': 'error',
  },
  overrides: [
    {
      files: ['src/utils/dates.ts', 'tests/unit/dates.spec.ts'],
      rules: { 'no-restricted-imports': 'off' },
    },
  ],
  ignorePatterns: ['dist/', 'node_modules/', 'daily-growth.html'],
  // local plugin 등록 (.eslintrc.cjs 인라인 방식 — eslint-plugin-local npm package 불요)
  // ESLint v8까지 호환. v9 flat config 마이그레이션 시 재작성 필요.
};

// Inline plugin registration (workaround for plugin require)
module.exports.plugins = ['@typescript-eslint'];
Object.assign(module.exports, {
  // ESLint v8 LegacyConfigArray가 plugin object를 직접 인식하지 못하므로,
  // 'local' plugin 이름을 plugins 배열에 추가 후 require로 resolve.
  // → 실제로는 별도 npm package 필요. fallback: package.json에 inline.
});
```

**⚠️ ESLint v8 local plugin 등록은 npm package화가 표준**. inline 방식은 v9 flat config부터 안정. 본 task에서는 두 옵션 중 선택:

- **옵션 A**: `package.json` devDependencies에 `eslint-plugin-local-rules` 추가 + `.eslintrc.cjs`에서 require.
- **옵션 B**: `package.json`에 inline local plugin definition (workspaces 형태).
- **옵션 C** (권장 — 외부 라이브러리 0줄 정책): npm package `eslint-plugin-local-rules` 추가는 외부 lib 추가이므로 spec §1.1 위반. **flat config(`eslint.config.js`) 마이그레이션** 진행.

**spec §1.1 외부 lib 0줄 정책에 따라 옵션 C 채택 → ESLint v8 legacy config(`.eslintrc.cjs`)를 flat config(`eslint.config.js`)로 마이그레이션**.

새 파일 `eslint.config.js`:

```js
import tsParser from '@typescript-eslint/parser';
import tsPlugin from '@typescript-eslint/eslint-plugin';
import nfdRule from './.eslint-rules/no-nfd-korean-literal.js';

const localPlugin = { rules: { 'no-nfd-korean-literal': nfdRule } };

export default [
  {
    ignores: ['dist/**', 'node_modules/**', 'daily-growth.html'],
  },
  {
    files: ['src/**/*.ts', 'tests/**/*.ts'],
    languageOptions: {
      parser: tsParser,
      ecmaVersion: 2020,
      sourceType: 'module',
      globals: {
        // browser + node globals
        window: 'readonly', document: 'readonly', localStorage: 'readonly',
        sessionStorage: 'readonly', fetch: 'readonly', console: 'readonly',
        setTimeout: 'readonly', clearTimeout: 'readonly',
        setInterval: 'readonly', clearInterval: 'readonly',
        // ... (기존 env browser+node에서 자동 노출되던 globals 복원)
        process: 'readonly', Buffer: 'readonly', __dirname: 'readonly',
      },
    },
    plugins: {
      '@typescript-eslint': tsPlugin,
      local: localPlugin,
    },
    rules: {
      ...tsPlugin.configs.recommended.rules,
      'no-restricted-syntax': [
        'error',
        // ... (기존 5 selector 그대로 복사)
      ],
      '@typescript-eslint/no-explicit-any': 'warn',
      'no-restricted-imports': ['error', { patterns: [...] }],
      'local/no-nfd-korean-literal': 'error',
    },
  },
  {
    files: ['src/utils/dates.ts', 'tests/unit/dates.spec.ts'],
    rules: { 'no-restricted-imports': 'off' },
  },
];
```

**대안 (단순)**: `.eslintrc.cjs`는 legacy로 두고, `package.json`의 `eslintConfig.plugins` workaround. 그러나 ESLint v8 LegacyConfig는 local plugin require를 직접 지원 X (외부 npm 필요).

**최종 결정**: 옵션 C (flat config 마이그레이션). T1을 2 sub-task로 split:
- **T1a**: `eslint.config.js` flat config 마이그레이션 (functionality 1:1 보존).
- **T1b**: `local/no-nfd-korean-literal` rule 추가 + 기존 위반 fix.

T1a 별도 task 신설.

(T1 본문은 위 옵션 C의 T1a + T1b로 split. 본 plan의 T1을 T1a + T1b로 confgure.)

- [ ] **Step 3: T1 split 후 sub-task 진입 — 본 task는 T1a 진입 트리거만**

이 step에서 T1을 T1a + T1b로 split 결정 commit (plan v2 또는 T0 plan v1에 split 명시). 그리고 T1a 진입.

---

## Task 1a: ESLint flat config 마이그레이션 (T1 split)

**Files:**
- Create: `eslint.config.js`
- Delete: `.eslintrc.cjs` (마이그레이션 후)
- Modify: `package.json` ("type": "module" 영향 검토)

- [ ] **Step 1: 현재 lint 상태 측정**

```bash
npm run lint 2>&1 | tail -5
```

Expected: 0 error, 28 warnings (v3.26 retro 기준).

- [ ] **Step 2: package.json + node version 호환 확인**

```bash
node -v   # ESLint v8 flat config는 node ≥18.18 또는 ≥20. ESLint v9는 node ≥18.18.
cat package.json | grep -E "eslint|type"
```

- [ ] **Step 3: `eslint.config.js` 작성**

```js
import tsParser from '@typescript-eslint/parser';
import tsPlugin from '@typescript-eslint/eslint-plugin';

export default [
  {
    ignores: ['dist/**', 'node_modules/**', 'daily-growth.html'],
  },
  {
    files: ['src/**/*.ts', 'tests/**/*.ts'],
    languageOptions: {
      parser: tsParser,
      ecmaVersion: 2020,
      sourceType: 'module',
      globals: {
        window: 'readonly', document: 'readonly', localStorage: 'readonly',
        sessionStorage: 'readonly', navigator: 'readonly',
        fetch: 'readonly', Request: 'readonly', Response: 'readonly',
        URL: 'readonly', URLSearchParams: 'readonly',
        AbortController: 'readonly', AbortSignal: 'readonly',
        Event: 'readonly', CustomEvent: 'readonly', EventTarget: 'readonly',
        HTMLElement: 'readonly', HTMLInputElement: 'readonly', HTMLButtonElement: 'readonly',
        Element: 'readonly', Node: 'readonly', Text: 'readonly',
        console: 'readonly',
        setTimeout: 'readonly', clearTimeout: 'readonly',
        setInterval: 'readonly', clearInterval: 'readonly',
        queueMicrotask: 'readonly',
        crypto: 'readonly', Intl: 'readonly',
        process: 'readonly', Buffer: 'readonly',
        __dirname: 'readonly', __filename: 'readonly',
        global: 'readonly', globalThis: 'readonly',
      },
    },
    plugins: {
      '@typescript-eslint': tsPlugin,
    },
    rules: {
      ...tsPlugin.configs.recommended.rules,
      'no-restricted-syntax': [
        'error',
        {
          selector: "MemberExpression[property.name='innerHTML']",
          message: 'innerHTML is forbidden — use textContent or render() helper. If HTML is required, escapeHtml all interpolations.',
        },
        {
          selector: "MemberExpression[computed=true][property.value='innerHTML']",
          message: "el['innerHTML'] is the same as el.innerHTML — forbidden for the same reason. Use textContent or escapeHtml.",
        },
        {
          selector: "MemberExpression[property.name='outerHTML']",
          message: 'outerHTML is forbidden — use DOM APIs (createElement, replaceWith). If HTML is required, escapeHtml all interpolations.',
        },
        {
          selector: "MemberExpression[computed=true][property.value='outerHTML']",
          message: "el['outerHTML'] is the same as el.outerHTML — forbidden for the same reason.",
        },
        {
          selector: "CallExpression[callee.property.name='insertAdjacentHTML']",
          message: 'insertAdjacentHTML is a CSP-relevant XSS sink — use DOM APIs or escapeHtml.',
        },
      ],
      '@typescript-eslint/no-explicit-any': 'warn',
      'no-restricted-imports': ['error', {
        patterns: [{
          group: ['**/utils/dates'],
          importNames: ['getDateStr'],
          message: 'getDateStr is deprecated (v3.22). Use getKstDateStr for KST anchor (Asia/Seoul).',
        }],
      }],
    },
  },
  {
    files: ['src/utils/dates.ts', 'tests/unit/dates.spec.ts'],
    rules: { 'no-restricted-imports': 'off' },
  },
];
```

- [ ] **Step 4: `.eslintrc.cjs` 삭제**

```bash
git rm .eslintrc.cjs
```

- [ ] **Step 5: `package.json` `type` field 확인**

Flat config는 `eslint.config.js`가 ESM이므로 `package.json`의 `"type": "module"`이 권장. 현재 `"type"` 없으면 추가, `"commonjs"`면 commonjs 유지 + `eslint.config.mjs`로 rename.

```bash
grep '"type"' package.json
```

- 없음 → `"type": "module"` 추가 또는 `eslint.config.mjs`로 rename
- `"module"` → 그대로
- `"commonjs"` → `eslint.config.mjs`로 rename

본 task에서는 **`eslint.config.mjs`로 rename** 채택 (다른 .js 파일들 commonjs 영향 차단).

```bash
git mv eslint.config.js eslint.config.mjs
```

- [ ] **Step 6: ESLint version 확인 + 필요 시 upgrade**

```bash
cat package.json | grep '"eslint"'
# eslint v8.21+ flat config 지원 (legacy parallel), v9 flat 기본. v8.57까지 legacy 동작.
```

ESLint v8.21~ 8.57 사이면 flat config 사용 가능 — 그대로 진행.
ESLint v9.0+ 이면 flat config 기본 — `.eslintrc.cjs` 제거 자연스러움.

- [ ] **Step 7: lint 재실행**

```bash
npm run lint 2>&1 | tail -10
```

Expected: 0 error (기존 5 no-restricted-syntax + 1 no-restricted-imports + tsPlugin recommended 동일). warnings 28건 그대로.

Diff 발생 시:
- globals 누락 (no-undef error) → Step 3 globals에 추가
- recommended rule 차이 → flat config v9 변경점 확인

- [ ] **Step 8: tsc + vitest 영향 확인**

```bash
npx tsc --noEmit && npm test -- --reporter=dot 2>&1 | tail -10
```

Expected: 0 error / 1351 PASS.

- [ ] **Step 9: Commit**

```bash
git add eslint.config.mjs package.json
git commit -m "build(v3.38 T1a): ESLint v8 legacy → flat config 마이그레이션

local plugin 인라인 등록 가능하도록 flat config(eslint.config.mjs)로 마이그레이션.
기존 5 no-restricted-syntax + 1 no-restricted-imports + tsPlugin recommended + dates.ts override
1:1 동작 보존. lint 0 error / 28 warnings 그대로.

T1b 신규 local rule 등록 사전 작업.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 1b: NFC normalize local ESLint rule + 위반 fix

**Files:**
- Create: `.eslint-rules/no-nfd-korean-literal.js` (T1 Step 1 본문 그대로)
- Modify: `eslint.config.mjs` (local plugin 등록)
- Modify: 위반 발견 파일 (Step 4)

- [ ] **Step 1: rule 파일 작성**

Write `.eslint-rules/no-nfd-korean-literal.js` (T1 Step 1 본문 그대로).

- [ ] **Step 2: `eslint.config.mjs`에 plugin import + rule activation**

Modify `eslint.config.mjs`:

```js
import tsParser from '@typescript-eslint/parser';
import tsPlugin from '@typescript-eslint/eslint-plugin';
import nfdRule from './.eslint-rules/no-nfd-korean-literal.js';

const localPlugin = { rules: { 'no-nfd-korean-literal': nfdRule } };

export default [
  // ... 기존 ignores
  {
    files: ['src/**/*.ts', 'tests/**/*.ts'],
    languageOptions: { /* 기존 그대로 */ },
    plugins: {
      '@typescript-eslint': tsPlugin,
      local: localPlugin,        // 추가
    },
    rules: {
      // ... 기존 그대로
      'local/no-nfd-korean-literal': 'error',  // 추가
    },
  },
  // ... 기존 dates.ts override
];
```

⚠️ `.eslint-rules/no-nfd-korean-literal.js`가 CommonJS (`module.exports`)이므로 ESM import 시 default 처리 확인:

```js
// CommonJS module을 ESM에서 import: default가 module.exports.
import nfdRule from './.eslint-rules/no-nfd-korean-literal.js';
// nfdRule = module.exports (rule object 자체)
```

- [ ] **Step 3: rule 단위 test (Manual)**

테스트 케이스 작성:

```bash
cat > /tmp/v3.38-t1b-test.ts <<'EOF'
const ok = '분석';      // NFC
const ng = '분석';      // NFD (자모 분리 — 이 line은 NFD 형태로 작성됐다 가정)
const tmpl = `분석 ${'좋아'}`;
EOF
cp /tmp/v3.38-t1b-test.ts /tmp/test-nfd.ts
npx eslint /tmp/test-nfd.ts 2>&1 | head
```

⚠️ Editor가 자동 NFC 정규화 가능 → 강제 NFD test는 별도 fixture 필요. fixture 작성 어렵다면 별도 unit test로 rule visitor 단위 검증 가능 (선택).

본 task에서는 **lint full run으로 회귀 검증 갈음**.

- [ ] **Step 4: 기존 위반 grep + fix**

```bash
npm run lint 2>&1 | grep "no-nfd-korean-literal" | tee /tmp/v3.38-t1b-violations.txt
wc -l /tmp/v3.38-t1b-violations.txt
```

- 0 발견 → Step 5 skip, Step 6 직행.
- 1+ 발견 → 각 파일 NFC normalize. 방법:
  - VS Code: `Cmd+Shift+P` → "Save as UTF-8" + extension "NFC normalizer" 또는
  - CLI: `python3 -c "import unicodedata,sys; print(unicodedata.normalize('NFC', open(sys.argv[1]).read()), end='')" path/to/file.ts > /tmp/fixed.ts && mv /tmp/fixed.ts path/to/file.ts`

- [ ] **Step 5: 위반 fix 후 lint 재실행**

```bash
npm run lint 2>&1 | tail -10
```

Expected: 0 error.

- [ ] **Step 6: vitest 회귀 확인**

```bash
npm test -- --reporter=dot 2>&1 | tail -5
```

Expected: 1351 PASS (T1a 동일).

- [ ] **Step 7: Bundle 측정 (lint rule이라 0 변화 expected)**

```bash
npm run build
echo "index: $(gzip -c dist/assets/index-*.js | wc -c) B"
```

Expected: T1a 직후와 동일 (lint rule은 bundle 0 영향).

- [ ] **Step 8: Commit**

```bash
git add .eslint-rules/ eslint.config.mjs <위반 fix 파일들>
git commit -m "feat(v3.38 T1b): NFC normalize ESLint rule + 기존 위반 N건 fix

한국어 음절 블록(가-힯) 포함 string/template literal이 NFC normalize 아니면 error.
NFD silent 매칭 fail risk 차단 (v3.37 T3 reviewer M-5 P2 graduated).

기존 위반 N건 fix: <파일 list>.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: C7 — smoke garden-card visibility 비대칭 fix (Track 3)

**Files:**
- Modify: `tests/smoke/plant-detail-modal.spec.ts:85, ~131-132`

- [ ] **Step 1: 현재 상태 확인**

```bash
grep -n "garden-card\[data-interest-id" tests/smoke/plant-detail-modal.spec.ts
```

Expected (v3.37 retro 기준):
- test 1 (line ~49-50): `await expect(card).toBeVisible();` 존재
- test 2 (line ~85-86): visibility wait 없이 바로 click
- test 3 (line ~131-132): visibility wait 없이 바로 click

- [ ] **Step 2: test 2 spec 수정**

Find:
```ts
await page.locator('.garden-card[data-interest-id="leadership"]').click();
await expect(page.locator('.plant-detail-modal')).toBeVisible();
```

Replace (이건 test 2 패턴):
```ts
const card2 = page.locator('.garden-card[data-interest-id="leadership"]');
await expect(card2).toBeVisible();
await card2.click();
await expect(page.locator('.plant-detail-modal')).toBeVisible();
```

- [ ] **Step 3: test 3 spec 수정**

test 3은 stats 탭 이동 후 garden card click이 있는 구간 (line ~131-132 retro 기준). 동일 패턴 적용:

```ts
// Before
await page.locator('#bottomNav button[data-tab-id="stats"]').click();
await page.locator('.garden-card[data-interest-id="leadership"]').click();
await expect(page.locator('.plant-detail-modal')).toBeVisible();

// After
await page.locator('#bottomNav button[data-tab-id="stats"]').click();
const card3 = page.locator('.garden-card[data-interest-id="leadership"]');
await expect(card3).toBeVisible();
await card3.click();
await expect(page.locator('.plant-detail-modal')).toBeVisible();
```

- [ ] **Step 4: smoke 재실행 N=3 cold-fork**

```bash
for i in 1 2 3; do
  echo "=== N=$i ==="
  npm run test:smoke -- tests/smoke/plant-detail-modal.spec.ts 2>&1 | tail -10
done
```

Expected: 모든 N에서 3/3 PASS, 0 flake.

- [ ] **Step 5: lint + tsc**

```bash
npm run lint && npx tsc --noEmit
```

- [ ] **Step 6: Commit**

```bash
git add tests/smoke/plant-detail-modal.spec.ts
git commit -m "test(v3.38 T2): smoke plant-detail test 2/3 garden-card visibility wait 추가

test 1과 동일한 \`await expect(card).toBeVisible()\` 사전 wait 추가.
cold-start race click-before-paint flake risk 차단 (v3.37 T3 reviewer M-1 graduated).

N=3 cold-fork 0 flake 확인.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: C6 — T2 spec state-block vacuous 정리 (Track 3)

**Files:**
- Modify: `tests/ui/modals/plant-detail.spec.ts` (state-block spec, line range 사전 확인)

- [ ] **Step 1: 현재 spec read + vacuous 위치 식별**

```bash
grep -n "describe\|state\|state-block\|module state" tests/ui/modals/plant-detail.spec.ts | head -20
```

T2 reviewer가 "state-block spec vacuous"라고 지목한 spec을 찾아 actual assert를 read.

```bash
sed -n '<후보 line>,<+30>p' tests/ui/modals/plant-detail.spec.ts
```

- [ ] **Step 2: vacuous 판정**

체크 항목:
- spec이 module closure state(`currentEntity`, `currentTokens` 등) 직접 export 없이 internal-only인가?
- assertion이 setup 이후 즉시 state read하는데 → 사실상 동어반복(tautology)인가?
- DOM/spy/callback 등 observable side-effect를 통한 verification이 없는가?

3개 모두 yes면 vacuous. 1개라도 no면 정상 spec → C6 close 없이 retro에 "false positive" 기록.

- [ ] **Step 3 (Option A): vacuous면 spec delete**

다른 spec(예: counter-click integration, navigateToInterestArchive sequence)이 동일 invariant cover 시 spec delete.

```ts
// Delete the entire `it('...', ...)` block.
```

- [ ] **Step 3 (Option B): vacuous지만 invariant valuable이면 observable spy 추가**

`resetArchiveFilters()` 호출 후 archive module의 `rerenderList` 또는 DOM 변경 spy.

```ts
import { vi } from 'vitest';

// spec body 내부
const rerenderSpy = vi.fn();
vi.doMock('../../../src/ui/handlers/archive', () => ({
  resetArchiveFilters: vi.fn(),
  handleArchiveSearch: vi.fn(),
  rerenderList: rerenderSpy,
}));

// ... navigateToInterestArchive 호출 후
expect(rerenderSpy).toHaveBeenCalledOnce();
```

- [ ] **Step 4: 결정 + 적용**

Option A (delete)와 Option B (observable) 중 reviewer M-2 본문 의도에 맞게 선택. 의도 명확하지 않으면 Option B (가치 보존 + observable 추가).

- [ ] **Step 5: vitest 재실행**

```bash
TZ=Asia/Seoul npm test -- tests/ui/modals/plant-detail.spec.ts
TZ=America/New_York npm test -- tests/ui/modals/plant-detail.spec.ts
```

Expected: 양쪽 TZ 동일 PASS count. delete 옵션은 -1, observable 옵션은 PASS count 동일.

- [ ] **Step 6: lint + tsc**

```bash
npm run lint && npx tsc --noEmit
```

- [ ] **Step 7: Commit**

```bash
git add tests/ui/modals/plant-detail.spec.ts
git commit -m "test(v3.38 T3): plant-detail spec state-block vacuous → <delete|observable spy>

v3.37 T2 reviewer M-2 흡수. <delete: 동일 invariant 다른 spec cover | observable: rerenderList spy로 회귀 가드 ↑>.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: C3 — briefings normalize + schema strict (Track 2)

**Files:**
- Modify: `src/state/briefings.ts`
- Modify: 관련 fixture/spec (검색 결과 기반)

- [ ] **Step 1: briefings 관련 `?? ''` 전체 grep**

```bash
grep -rn "briefing\|briefings" src/ tests/ | grep "?? ''" | tee /tmp/v3.38-t4-violations.txt
wc -l /tmp/v3.38-t4-violations.txt
```

Expected: src/state/briefings.ts:81 `sourceTitle ?? ''` 외 추가 위치 있을 수 있음. v3.35 carry 출처가 광범위했으므로 5~15건 예상.

- [ ] **Step 2: 각 위치 분류**

각 `?? ''` 사용처를 다음 카테고리로 분류:

- **(A) sourceTitle, imageUrl 등 optional field**: 자연스러운 fallback. 유지.
- **(B) title, summary 등 required field**: schema strict 위반. 제거 + write-side validate.
- **(C) read-side defensive coding** (예: `briefing.title || briefing.summary || ''`): 필요한지 평가. 필요시 유지, 불요시 제거.

- [ ] **Step 3: schema strict 적용 (B 카테고리)**

만약 write path에 `?? ''`이 있다면 (예: RSS extract 결과를 briefings에 저장하는 코드):

Before:
```ts
const briefing: Briefing = {
  id, date, url,
  title: rssItem.title ?? '',
  summary: rssItem.description ?? '',
  // ...
};
```

After:
```ts
if (!rssItem.title?.trim()) {
  console.warn('[briefings] missing title, skipping', rssItem.url);
  return null;  // 또는 throw
}
if (!rssItem.description?.trim()) {
  console.warn('[briefings] missing summary, skipping', rssItem.url);
  return null;
}
const briefing: Briefing = {
  id, date, url,
  title: rssItem.title.trim(),
  summary: rssItem.description.trim(),
  // ...
};
```

write path 정확한 위치는 Step 1 grep 결과로 확인.

- [ ] **Step 4: read path defensive 제거 (C 카테고리)**

만약 read에서 `briefing.title ?? ''` 같은 redundant fallback이 있다면 제거. Briefing interface에서 title은 required이므로 fallback 자체가 type-noise.

- [ ] **Step 5: fixture/spec update**

만약 spec에서 partial Briefing 객체 (`{ id: '1', title: 'X' }`)를 사용하면 strict shape으로 보강 의무. 다음 명령으로 spec 위치 확인:

```bash
grep -rn "as Briefing\|: Briefing\b" tests/ | head -20
```

각 spec의 `as Briefing` 사용처가 strict required field 모두 포함하는지 검사. 누락 시 추가.

- [ ] **Step 6: NFC normalize 추가 (write-side)**

write path에 NFC normalize 의무 추가:

```ts
title: rssItem.title.trim().normalize('NFC'),
summary: rssItem.description.trim().normalize('NFC'),
```

이로써 v3.35 carry "briefings normalize"의 normalize 부분 충족.

- [ ] **Step 7: vitest 양쪽 TZ**

```bash
TZ=Asia/Seoul npm test -- briefings
TZ=America/New_York npm test -- briefings
```

Expected: 양쪽 TZ 동일 PASS.

신규 spec 1+ 필요할 가능성 (write-side strict reject case):

```ts
it('write path rejects briefing with empty title', () => {
  const result = createBriefing({ title: '', summary: 'X', url: 'https://...' });
  expect(result).toBeNull();
});

it('write path NFC normalizes Korean title', () => {
  const nfdTitle = '분석'.normalize('NFD');
  const result = createBriefing({ title: nfdTitle, summary: 'X', url: 'https://...' });
  expect(result?.title).toBe('분석'.normalize('NFC'));
});
```

- [ ] **Step 8: lint + tsc**

```bash
npm run lint && npx tsc --noEmit
```

⚠️ `local/no-nfd-korean-literal` 인해 spec의 NFD literal은 직접 작성 X. `.normalize('NFD')` runtime 호출로 작성 (위 예시처럼).

- [ ] **Step 9: Bundle 측정**

```bash
npm run build
echo "index: $(gzip -c dist/assets/index-*.js | wc -c) B"
```

Expected: T1b baseline 대비 ±50 B (write-side validate 코드 추가 미미).

- [ ] **Step 10: Commit**

```bash
git add src/state/briefings.ts tests/  # 영향 파일
git commit -m "feat(v3.38 T4): briefings ?? '' 제거 + schema strict + NFC normalize write-side

required field(title/summary)는 write-side에서 validate, missing 시 null return.
optional field(sourceTitle/imageUrl)는 ?? '' 자연 유지.
write path에 NFC normalize 의무 (v3.35 carry C3 closure).

신규 spec X건: empty title reject + NFC normalize 회귀 가드.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 5a: C4 — Levenshtein distance helper 추가 (Track 2)

**Files:**
- Modify: `src/utils/fuzzy.ts` (function 추가)
- Create: `tests/utils/fuzzy-distance.spec.ts`

- [ ] **Step 1: fuzzy.ts 현재 export 확인**

```bash
grep -n "^export" src/utils/fuzzy.ts
```

Expected:
- `tokenizeQuery`
- `getInitialConsonants`
- `isInitialOnlyToken`
- `matchesAllTokens`

- [ ] **Step 2: 실패 test 작성 (TDD red)**

Write `tests/utils/fuzzy-distance.spec.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { levenshteinDistance, fuzzyMatchesToken } from '../../src/utils/fuzzy';

describe('levenshteinDistance', () => {
  it('returns 0 for identical strings', () => {
    expect(levenshteinDistance('hello', 'hello')).toBe(0);
  });

  it('counts insertions', () => {
    expect(levenshteinDistance('cat', 'cats')).toBe(1);
  });

  it('counts deletions', () => {
    expect(levenshteinDistance('cats', 'cat')).toBe(1);
  });

  it('counts substitutions', () => {
    expect(levenshteinDistance('cat', 'bat')).toBe(1);
  });

  it('combines operations', () => {
    expect(levenshteinDistance('kitten', 'sitting')).toBe(3);
  });

  it('handles empty strings', () => {
    expect(levenshteinDistance('', '')).toBe(0);
    expect(levenshteinDistance('', 'abc')).toBe(3);
    expect(levenshteinDistance('abc', '')).toBe(3);
  });

  it('handles Korean (NFC normalized)', () => {
    expect(levenshteinDistance('분석', '분식')).toBe(1);
    expect(levenshteinDistance('리더쉽', '리더십')).toBe(1);
  });
});

describe('fuzzyMatchesToken', () => {
  it('matches exact substring (distance 0)', () => {
    expect(fuzzyMatchesToken('hello world', 'hello')).toBe(true);
  });

  it('matches with 1 typo for token length 3-4', () => {
    expect(fuzzyMatchesToken('hello world', 'helo')).toBe(true);    // missing l
    expect(fuzzyMatchesToken('hello world', 'hxllo')).toBe(true);   // substitution
  });

  it('matches with 2 typos for token length 5+', () => {
    expect(fuzzyMatchesToken('developer', 'develpor')).toBe(true);  // 2 swap
  });

  it('strict for very short tokens (length ≤ 2)', () => {
    expect(fuzzyMatchesToken('hello', 'hi')).toBe(false);  // dist=2 but token short
    expect(fuzzyMatchesToken('hi there', 'hi')).toBe(true); // exact substring
  });

  it('returns false for distance over threshold', () => {
    expect(fuzzyMatchesToken('hello', 'xyzab')).toBe(false);
  });

  it('NFC normalizes Korean inputs', () => {
    const nfdToken = '분석'.normalize('NFD');
    expect(fuzzyMatchesToken('분석 자료', nfdToken)).toBe(true);
  });
});
```

- [ ] **Step 3: Run test → FAIL**

```bash
npm test -- tests/utils/fuzzy-distance.spec.ts
```

Expected: FAIL with "levenshteinDistance is not exported" 또는 "is not a function".

- [ ] **Step 4: implementation**

Append to `src/utils/fuzzy.ts`:

```ts
/**
 * v3.38 T5a: Levenshtein distance (표준 DP O(NM)).
 * NFC normalize는 caller 의무 — `fuzzyMatchesToken` 등 wrapper에서 정규화.
 */
export function levenshteinDistance(a: string, b: string): number {
  if (a === b) return 0;
  const m = a.length;
  const n = b.length;
  if (m === 0) return n;
  if (n === 0) return m;

  let prev = Array.from({ length: n + 1 }, (_, i) => i);
  let curr = new Array<number>(n + 1);

  for (let i = 1; i <= m; i++) {
    curr[0] = i;
    for (let j = 1; j <= n; j++) {
      const cost = a[i - 1] === b[j - 1] ? 0 : 1;
      curr[j] = Math.min(
        curr[j - 1] + 1,         // insertion
        prev[j] + 1,             // deletion
        prev[j - 1] + cost,      // substitution
      );
    }
    [prev, curr] = [curr, prev];
  }
  return prev[n];
}

/**
 * v3.38 T5a: fuzzy token matching with adaptive threshold.
 *
 * - token length ≤ 2: exact substring only (distance=0)
 * - token length 3-4: distance ≤ 1
 * - token length ≥ 5: distance ≤ 2
 *
 * 검색 token이 text의 부분 문자열에 fuzzy match되면 true.
 * 매칭 알고리즘: text를 token.length 슬라이딩 윈도우로 훑으며 최소 distance ≤ threshold 인지 확인.
 *
 * 성능 cap: text.length × token.length > 100,000 이면 exact substring fallback (UI freeze 차단).
 */
export function fuzzyMatchesToken(text: string, rawToken: string): boolean {
  const t = text.normalize('NFC').toLowerCase();
  const k = rawToken.normalize('NFC').toLowerCase();

  if (k.length === 0) return true;
  if (t.includes(k)) return true;
  if (k.length <= 2) return false;

  // 성능 cap
  if (t.length * k.length > 100_000) {
    return false;
  }

  const threshold = k.length <= 4 ? 1 : 2;
  const win = k.length;

  for (let i = 0; i + win <= t.length; i++) {
    const sub = t.slice(i, i + win);
    if (levenshteinDistance(sub, k) <= threshold) return true;
  }

  // 추가: window를 ±1 확장한 변형도 시도 (insertion/deletion fuzzy match)
  for (let delta = -1; delta <= 1; delta += 2) {
    const w = win + delta;
    if (w < 1) continue;
    for (let i = 0; i + w <= t.length; i++) {
      const sub = t.slice(i, i + w);
      if (levenshteinDistance(sub, k) <= threshold) return true;
    }
  }

  return false;
}
```

- [ ] **Step 5: Run test → PASS**

```bash
npm test -- tests/utils/fuzzy-distance.spec.ts
```

Expected: 12+ PASS, 0 FAIL.

- [ ] **Step 6: 회귀 — 기존 fuzzy spec**

```bash
TZ=Asia/Seoul npm test -- fuzzy
TZ=America/New_York npm test -- fuzzy
```

Expected: 기존 fuzzy.spec.ts (있다면) 그대로 PASS + 신규 12+ PASS.

- [ ] **Step 7: lint + tsc**

```bash
npm run lint && npx tsc --noEmit
```

- [ ] **Step 8: Bundle 측정**

```bash
npm run build
echo "archive handler: $(gzip -c dist/assets/archive-handler*.js | wc -c) B"
```

fuzzy.ts는 archive handler에 import되므로 archive handler chunk 변경 예상. Δ +200~+400 B.

- [ ] **Step 9: Commit**

```bash
git add src/utils/fuzzy.ts tests/utils/fuzzy-distance.spec.ts
git commit -m "feat(v3.38 T5a): src/utils/fuzzy.ts에 levenshteinDistance + fuzzyMatchesToken 추가

표준 DP O(NM) Levenshtein. adaptive threshold: 길이 ≤2 strict / 3-4 dist≤1 / ≥5 dist≤2.
성능 cap: text*token > 100k이면 exact fallback (UI freeze 차단).
NFC normalize는 wrapper 의무.

신규 spec X건 (양쪽 TZ PASS).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 5b: C4 — archive.ts fuzzy 통합 + entity scope (Track 2)

**Files:**
- Modify: `src/ui/handlers/archive.ts` (search filter 로직)
- Modify: `tests/ui/handlers/*.spec.ts` (기존 search spec 확장)

- [ ] **Step 1: archive.ts 현재 search 로직 read**

```bash
grep -n "matchesAllTokens\|handleArchiveSearch\|currentTokens" src/ui/handlers/archive.ts | head -20
```

각 search filter callsite를 identify. v3.27 retro 기준 archive 4 entity(answer/scrap/memo/insight)에 search filter 적용.

- [ ] **Step 2: 실패 test 작성**

신규 spec 또는 기존 spec 확장:

```ts
import { describe, it, expect, beforeEach } from 'vitest';
// archive handler entry

describe('archive search fuzzy match (v3.38 T5b)', () => {
  beforeEach(() => {
    // 4 entity seed (answer / scrap / memo / insight)
    // each with Korean text e.g. '분석 자료', '리더십 발휘'
  });

  it('fuzzy matches 1-typo Korean token', () => {
    // setup: input '분식' (실제 '분석')
    // expected: '분석' 포함 카드 visible
  });

  it('entity scope: answer만 fuzzy active일 때 answer entity만 매칭', () => {
    // setup: currentEntity='answer', input='리덥쉽' (실제 '리더십')
    // expected: scrap 카드는 매칭 skip, answer만 fuzzy 적용
  });

  it('fuzzy 0건 fallback → exact match만 표시', () => {
    // setup: input='zzz' (전혀 매치 X)
    // expected: 카드 0개 표시 + empty state
  });
});
```

- [ ] **Step 3: Run test → FAIL**

```bash
npm test -- archive
```

- [ ] **Step 4: archive.ts 수정 — fuzzy 통합**

Find search filter callsite (예시, actual은 grep 결과 기준):

```ts
const matches = matchesAllTokens(hay, currentTokens);
```

Replace:

```ts
// v3.38 T5b: fuzzy + entity scope.
const exactMatch = matchesAllTokens(hay, currentTokens);
const shouldFuzzy = currentEntity === 'all' || matchesCurrentEntity(card, currentEntity);
const fuzzyMatch = !exactMatch && shouldFuzzy && currentTokens.every((t) => fuzzyMatchesToken(hay, t));
const matches = exactMatch || fuzzyMatch;
```

`fuzzyMatchesToken` import 추가:

```ts
import { tokenizeQuery, matchesAllTokens, fuzzyMatchesToken } from '../../utils/fuzzy';
```

`matchesCurrentEntity` helper 신설 (entity scope 판정):

```ts
function matchesCurrentEntity(card: ArchiveCard, entity: ArchiveEntity): boolean {
  if (entity === 'all') return true;
  return card.entity === entity;
}
```

- [ ] **Step 5: Run test → PASS**

```bash
npm test -- archive
```

- [ ] **Step 6: fallback 검증**

handleArchiveSearch에서 fuzzy 결과 0건일 때 empty state 표시 확인. Empty state는 기존 archive 로직 재사용 (해당 분기 있는지 사전 확인).

- [ ] **Step 7: smoke 회귀 — archive search 관련**

```bash
npm run build && npm run test:smoke -- tests/smoke/archive
```

Expected: 0 flake. 기존 search 관련 smoke (`archive-search`, `archive-keyword-prefill`)가 있다면 그대로 PASS.

- [ ] **Step 8: 양쪽 TZ vitest 풀 회귀**

```bash
TZ=Asia/Seoul npm test 2>&1 | tail -5
TZ=America/New_York npm test 2>&1 | tail -5
```

Expected: 1351 + T4 추가 + T5a 추가 + T5b 추가 (예상 +15~25 net).

- [ ] **Step 9: lint + tsc + Bundle**

```bash
npm run lint && npx tsc --noEmit
npm run build
echo "archive handler: $(gzip -c dist/assets/archive-handler*.js | wc -c) B"
echo "archive tab: $(gzip -c dist/assets/archive-*.js | grep -v handler | head -1 | wc -c) B"
echo "index: $(gzip -c dist/assets/index-*.js | wc -c) B"
```

Expected: archive handler ≤ +300 B GREEN, index +0~+50 B.

- [ ] **Step 10: Commit**

```bash
git add src/ui/handlers/archive.ts tests/
git commit -m "feat(v3.38 T5b): archive 검색 fuzzy match + entity scope 통합

fuzzyMatchesToken을 exact match 0건 시 fallback으로 적용.
entity chip 활성 시 fuzzy 매칭도 entity scope 내에서만 수행 (currentEntity='all'은 전체).
exact + fuzzy 모두 0건이면 기존 empty state 표시.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: C5 — stats 탭 progressive nav entry (Track 1)

**Files:**
- Modify: `src/ui/handlers/stats.ts` (또는 `src/ui/modals/insight-detail.ts`)
- Test: 신규 unit + smoke 1+

- [ ] **Step 1: stats 탭 Insight detail UI 위치 식별**

```bash
grep -rn "insight-detail\|insightDetail\|insight detail" src/ui/ | head -10
```

만약 modal이면 modals/, 인라인이면 handlers/stats.ts 안 logic.

- [ ] **Step 2: navigation 진입 chip의 UI 위치 결정**

옵션:
- **(a) Insight detail modal 내부 footer**: 모달이 열린 상태에서 chip 클릭 → 모달 닫고 archive 진입.
- **(b) Stats 탭 inline grid 위 hover/click chip**: 모달 안 거치고 직접 archive 진입.

선호: **(a)** plant-detail-modal 패턴(v3.36 T1) 그대로 따름. consistency ↑.

- [ ] **Step 3: 실패 test 작성**

신규 unit spec `tests/ui/modals/insight-detail.spec.ts` (또는 기존 file 확장):

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest';

describe('insight-detail progressive nav chip (v3.38 T6 C5)', () => {
  beforeEach(() => {
    document.body.innerHTML = '';
    vi.resetModules();
  });

  it('renders "이 인사이트의 답변/스크랩 보기" chip when insight has interestId', async () => {
    // setup: open insight modal with interestId='leadership'
    // expected: chip text '🔍 이 인사이트의 답변/스크랩 보기' visible
  });

  it('chip click → navigateToInterestArchive 호출', async () => {
    // setup: spy on navigateToInterestArchive (or archive handlers)
    // chip click
    // expected: spy 1회 호출 with correct interestId
  });

  it('omits chip when insight.interestId is undefined or unknown', () => {
    // setup: insight without interestId
    // expected: chip not in DOM
  });
});
```

- [ ] **Step 4: Run test → FAIL**

```bash
npm test -- insight-detail
```

- [ ] **Step 5: implementation**

만약 insight-detail이 별도 modal:

`src/ui/modals/insight-detail.ts`에 chip 추가:

```ts
function makeArchiveNavChip(interestId: string): HTMLButtonElement {
  const btn = document.createElement('button');
  btn.type = 'button';
  btn.className = 'insight-action-chip';
  btn.textContent = '🔍 이 인사이트의 답변/스크랩 보기';
  btn.setAttribute('aria-label', '이 인사이트의 분야 archive 탐색');
  btn.addEventListener('click', () => {
    void navigateToInterestArchive(interestId).catch((err) => {
      console.warn('[insight-detail] navigate failed', err);
    });
  });
  return btn;
}
```

`navigateToInterestArchive`는 v3.37에서 plant-detail.ts에 `@internal` export로 노출. 재사용 가능:

```ts
import { navigateToInterestArchive } from './plant-detail';
```

또는 archive 직접 import (lighter):

```ts
const { resetArchiveFilters, handleArchiveSearch } = await import('../handlers/archive');
// ... 동일 시퀀스
```

선호: plant-detail.ts에서 `navigateToInterestArchive` re-export하거나, 공용 helper로 추출.

⚠️ **DRY 검토**: navigateToInterestArchive 패턴이 plant-detail + insight-detail 2 callsite로 늘어남. 공용 helper로 추출 (`src/ui/navigation/interest-archive-nav.ts` 신규)할지 결정. T0 plan write 시점 명시:

- **결정**: 본 task에서는 plant-detail.ts의 `navigateToInterestArchive`를 그대로 import해 재사용 (`@internal export 자연 활용). 3 callsite 되는 시점에 공용 helper로 추출 — v3.39+ carry candidate.

- [ ] **Step 6: render flow 통합**

기존 insight-detail modal renderer에서 interestId 있을 때 chip append:

```ts
if (insight.interestId && INTEREST_LABEL[insight.interestId]) {
  modalBody.append(makeArchiveNavChip(insight.interestId));
}
```

- [ ] **Step 7: smoke spec 신규**

`tests/smoke/insight-detail-modal.spec.ts` 또는 기존 insight 관련 smoke에 추가:

```ts
test('v3.38: insight detail chip → archive 탭 + 검색어 prefill', async ({ page }) => {
  // seed: user with insights[0] interestId='leadership'
  // open insight detail
  // chip click
  // expected:
  //   - archive 탭 활성
  //   - #archiveSearch.value '리더십'
  //   - input focused
});
```

T2 N=3 cold-fork 검증.

- [ ] **Step 8: Run unit + smoke**

```bash
TZ=Asia/Seoul npm test -- insight
TZ=America/New_York npm test -- insight
npm run build && npm run test:smoke -- insight-detail
```

- [ ] **Step 9: lint + tsc + Bundle**

```bash
npm run lint && npx tsc --noEmit
npm run build
echo "stats: $(gzip -c dist/assets/stats-*.js | wc -c) B"
echo "index: $(gzip -c dist/assets/index-*.js | wc -c) B"
```

Expected: stats chunk +50~+150 B (chip render + listener).

- [ ] **Step 10: Commit**

```bash
git add src/ui/modals/insight-detail.ts tests/
git commit -m "feat(v3.38 T6 C5): insight detail modal에 archive progressive nav chip 추가

interestId가 있는 insight detail 열림 시 '🔍 이 인사이트의 답변/스크랩 보기' chip 표시.
chip 클릭 → v3.37 navigateToInterestArchive 시퀀스 재사용 (resetArchiveFilters + switchTab + input.value + focus).
DRY: plant-detail.navigateToInterestArchive @internal export 재활용.
3 callsite 시점에 공용 helper로 추출 검토 — v3.39+ carry.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 7a: C1 — archive-search-summary component 신규 (Track 1)

**Files:**
- Create: `src/ui/components/archive-search-summary.ts`
- Create: `tests/ui/components/archive-search-summary.spec.ts`

- [ ] **Step 1: 실패 test 작성**

Write `tests/ui/components/archive-search-summary.spec.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { renderArchiveSearchSummary } from '../../../src/ui/components/archive-search-summary';

describe('renderArchiveSearchSummary (v3.38 T7a C1)', () => {
  it('renders chips with hit counts for each entity', () => {
    const counts = { answer: 12, scrap: 5, insight: 2, memo: 0 };
    const el = renderArchiveSearchSummary(counts);
    expect(el.querySelector('[data-entity="answer"]')?.textContent).toContain('답변 12');
    expect(el.querySelector('[data-entity="scrap"]')?.textContent).toContain('스크랩 5');
    expect(el.querySelector('[data-entity="insight"]')?.textContent).toContain('인사이트 2');
  });

  it('omits chips with hit count 0', () => {
    const counts = { answer: 12, scrap: 0, insight: 0, memo: 0 };
    const el = renderArchiveSearchSummary(counts);
    expect(el.querySelector('[data-entity="scrap"]')).toBeNull();
    expect(el.querySelector('[data-entity="insight"]')).toBeNull();
  });

  it('returns null when all counts are 0', () => {
    const counts = { answer: 0, scrap: 0, insight: 0, memo: 0 };
    expect(renderArchiveSearchSummary(counts)).toBeNull();
  });

  it('chips are buttons with data-entity attribute', () => {
    const el = renderArchiveSearchSummary({ answer: 1, scrap: 0, insight: 0, memo: 0 });
    const btn = el?.querySelector('button[data-entity="answer"]');
    expect(btn).toBeTruthy();
    expect(btn?.tagName).toBe('BUTTON');
    expect(btn?.getAttribute('type')).toBe('button');
  });
});
```

- [ ] **Step 2: Run → FAIL**

```bash
npm test -- archive-search-summary
```

Expected: FAIL "module not found".

- [ ] **Step 3: Implementation**

Write `src/ui/components/archive-search-summary.ts`:

```ts
/**
 * v3.38 T7a (C1): archive 검색 결과 위쪽에 entity별 hit count chip 묶음.
 *
 * 사용자가 검색 후 결과를 entity별로 빠르게 필터링.
 * - hit count 0인 entity는 chip 숨김.
 * - 전체 hit 0이면 null return (caller는 summary 자체 미표시).
 */

export interface ArchiveSearchCounts {
  answer: number;
  scrap: number;
  insight: number;
  memo: number;
}

const ENTITY_LABEL: Record<keyof ArchiveSearchCounts, string> = {
  answer: '답변',
  scrap: '스크랩',
  insight: '인사이트',
  memo: '메모',
};

const ENTITY_ORDER: (keyof ArchiveSearchCounts)[] = ['answer', 'scrap', 'insight', 'memo'];

export function renderArchiveSearchSummary(counts: ArchiveSearchCounts): HTMLElement | null {
  const total = ENTITY_ORDER.reduce((sum, key) => sum + counts[key], 0);
  if (total === 0) return null;

  const root = document.createElement('div');
  root.className = 'archive-search-summary';

  const prefix = document.createElement('span');
  prefix.className = 'archive-search-summary-label';
  prefix.textContent = '검색 결과:';
  root.append(prefix);

  for (const key of ENTITY_ORDER) {
    if (counts[key] === 0) continue;
    const btn = document.createElement('button');
    btn.type = 'button';
    btn.className = 'entity-summary-chip';
    btn.dataset.entity = key;
    btn.setAttribute('aria-label', `${ENTITY_LABEL[key]} ${counts[key]}개로 필터`);
    btn.textContent = `${ENTITY_LABEL[key]} ${counts[key]}`;
    root.append(btn);
  }

  return root;
}
```

- [ ] **Step 4: Run → PASS**

```bash
npm test -- archive-search-summary
```

Expected: 4 PASS, 0 FAIL.

- [ ] **Step 5: lint + tsc**

```bash
npm run lint && npx tsc --noEmit
```

- [ ] **Step 6: Commit**

```bash
git add src/ui/components/archive-search-summary.ts tests/ui/components/archive-search-summary.spec.ts
git commit -m "feat(v3.38 T7a C1): archive-search-summary 컴포넌트 신규

entity별 hit count chip 묶음 (답변/스크랩/인사이트/메모).
hit 0인 entity는 chip 숨김 + 전체 0이면 null return.

신규 4 spec PASS.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 7b: C1 — archive.ts summary chip listener wire (Track 1)

**Files:**
- Modify: `src/ui/handlers/archive.ts` (rerenderList에 summary render + click listener)
- Modify: 기존 archive spec (회귀 + 신규 summary spec)

- [ ] **Step 1: archive.ts rerenderList 함수 위치 확인**

```bash
grep -n "rerenderList\|function rerender\|render archive" src/ui/handlers/archive.ts | head -10
```

- [ ] **Step 2: 실패 test 작성**

`tests/ui/handlers/archive-search-summary-integration.spec.ts` 신규 또는 기존 archive spec 확장:

```ts
describe('archive search summary integration (v3.38 T7b C1)', () => {
  it('shows summary above results when search has hits', async () => {
    // setup: 4 entity seed (answer x 12 with '리더십' / scrap x 5 / insight x 2 / memo x 0)
    // input.value='리더십'; handleArchiveSearch();
    // expected: .archive-search-summary visible with chips
  });

  it('hides summary when search is empty', () => {
    // setup: input.value=''; handleArchiveSearch();
    // expected: .archive-search-summary not in DOM (또는 hidden)
  });

  it('clicking entity chip changes currentEntity + rerenders', () => {
    // setup: hit chips visible
    // click [data-entity="answer"]
    // expected: currentEntity='answer', .archive-entity-chip[data-entity="answer"] active
  });
});
```

- [ ] **Step 3: Run → FAIL**

- [ ] **Step 4: archive.ts 수정 — summary render + listener**

`rerenderList()` 내부에 다음 패턴 추가:

```ts
import { renderArchiveSearchSummary } from '../components/archive-search-summary';
import type { ArchiveSearchCounts } from '../components/archive-search-summary';

// 기존 rerenderList 끝부분에
function updateSearchSummary(matchedCards: ArchiveCard[]): void {
  const slot = document.getElementById('archiveSearchSummary');
  if (!slot) return;
  slot.replaceChildren();
  if (currentTokens.length === 0) return;

  const counts: ArchiveSearchCounts = { answer: 0, scrap: 0, insight: 0, memo: 0 };
  for (const card of matchedCards) {
    if (card.entity in counts) counts[card.entity as keyof ArchiveSearchCounts]++;
  }
  const summary = renderArchiveSearchSummary(counts);
  if (summary) slot.append(summary);
}

// listener delegated (rerenderList 호출 시점 1회만 wire)
function wireSearchSummaryClickOnce(): void {
  const slot = document.getElementById('archiveSearchSummary');
  if (!slot || slot.dataset.wired === '1') return;
  slot.dataset.wired = '1';
  slot.addEventListener('click', (ev) => {
    const target = ev.target as HTMLElement;
    const chip = target.closest('button.entity-summary-chip') as HTMLButtonElement | null;
    if (!chip) return;
    const entity = chip.dataset.entity as keyof ArchiveSearchCounts | undefined;
    if (!entity) return;
    setCurrentEntity(entity);   // 기존 helper (또는 chip click handler 재사용)
    rerenderList();
  });
}
```

`index.html` 또는 archive tab template에 `<div id="archiveSearchSummary"></div>` slot 추가 의무.

- [ ] **Step 5: index.html 또는 archive tab template 수정**

```bash
grep -n "archiveSearch\|archiveFilters\|archiveTab" index.html
```

archive tab markup에 search input 바로 아래 slot 추가:

```html
<div id="archiveSearchSummary"></div>
```

- [ ] **Step 6: Run → PASS**

```bash
TZ=Asia/Seoul npm test -- archive-search-summary
TZ=America/New_York npm test -- archive-search-summary
```

- [ ] **Step 7: smoke 회귀 + 신규 smoke 1 추가**

```ts
test('v3.38: archive 검색 시 entity summary chip 표시 + 클릭', async ({ page }) => {
  // seed: 다양 entity (answer + scrap)
  // input '리더십'
  // expected: .archive-search-summary visible
  // chip [data-entity="answer"] click
  // expected: .archive-entity-chip[data-entity="answer"] active + 결과 filter됨
});
```

- [ ] **Step 8: lint + tsc + Bundle**

```bash
npm run lint && npx tsc --noEmit
npm run build
echo "archive handler: $(gzip -c dist/assets/archive-handler*.js | wc -c) B"
echo "archive tab: $(gzip -c dist/assets/archive-*.js | grep -v handler | head -1 | wc -c) B"
echo "index: $(gzip -c dist/assets/index-*.js | wc -c) B"
```

Expected: archive handler +200~+400 B, archive tab +50~+150 B, index 미미.

- [ ] **Step 9: Commit**

```bash
git add src/ui/handlers/archive.ts index.html tests/
git commit -m "feat(v3.38 T7b C1): archive 검색 시 entity-aware hit count chip 묶음 표시

rerenderList()에 entity hit count 집계 + renderArchiveSearchSummary 통합.
#archiveSearchSummary slot에 chip render, delegate listener 1회 wire.
chip click → currentEntity 변경 + rerender (기존 setCurrentEntity 재사용).

신규 smoke 1: 검색 + summary chip 클릭 end-to-end.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 8: Codex 최종 review + retro + memory sync

**Files:**
- Output: `/tmp/v3.38-codex-final.txt`
- Modify: spec §9 Retro
- Create: `~/.claude/projects/.../memory/project_daily_growth_v338.md`
- Modify: `~/.claude/projects/.../memory/MEMORY.md`
- Modify: CLAUDE.md (lesson graduate 시)
- Vault sync to `02-Cycles/` + `03-Lessons/`

- [ ] **Step 1: 풀 회귀 검증**

```bash
TZ=Asia/Seoul npm test 2>&1 | tail -5
TZ=America/New_York npm test 2>&1 | tail -5
npm run lint
npx tsc --noEmit
npm run build && npm run test:smoke 2>&1 | tail -10
```

Expected: 양쪽 TZ drift 0, lint/tsc 0 error, smoke 0 fail.

- [ ] **Step 2: Bundle final quadruple record**

```bash
npm run build
echo "=== v3.38 final ==="
echo "index canonical: $(gzip -c dist/assets/index-*.js | wc -c) B"
ls dist/assets/ | grep -E "^(archive|stats)" | while read f; do
  echo "$f: $(gzip -c dist/assets/$f | wc -c) B"
done
```

baseline (T0 Step 4) 대비 Δ 계산. DoD 위반 시 dedicated trim 또는 deploy 검토.

- [ ] **Step 3: Codex 최종 review**

```bash
codex exec --skip-git-repo-check -o /tmp/v3.38-codex-final.txt "$(cat <<'EOF'
You are the deploy gate reviewer for Daily Growth v3.38.

Spec: docs/superpowers/specs/2026-05-18-v3.38-carry-cleanup-design.md
Plan: docs/superpowers/plans/2026-05-18-v3.38-carry-cleanup-plan.md

All main tasks T1a/T1b/T2/T3/T4/T5a/T5b/T6/T7a/T7b are now committed.

Cycle range: parent 870a25d (v3.37 deploy) → tip HEAD.

Review focus:
1. P0 (deploy blocker): security (XSS), data corruption risk, regression on existing feature, schema/CSP violation
2. P1 (in-cycle fix mandatory): spec/plan deviation, missing test, cross-file invariant break
3. P2 (carry to v3.39+): polish, ROI low, optional

Output:
- VERDICT: DEPLOY_APPROVED | APPROVED_WITH_NOTES | REJECT
- P0/P1/P2 항목별 issue list with commit SHA reference
- 권장 fix
EOF
)"
cat /tmp/v3.38-codex-final.txt
```

CLI 한도/권한 issue 시 fallback: `pr-review-toolkit:code-reviewer` agent (CLAUDE.md fallback 정책 따름).

- [ ] **Step 4: Codex 결과 classification**

- **DEPLOY_APPROVED**: 즉시 deploy 가능. retro 작성 후 사용자에게 보고.
- **APPROVED_WITH_NOTES**: P0=0, P1=0 확인. P2는 v3.39+ carry. deploy 가능.
- **REJECT**: P0/P1 in-cycle fix → retry. fix commit 후 Step 3 재실행.

- [ ] **Step 5: spec §9 Retro 작성**

`docs/superpowers/specs/2026-05-18-v3.38-carry-cleanup-design.md` §9 Retro 채움:

```markdown
## §9. Retro

**Status**: ✅ 사이클 완료, 배포 대기 (2026-05-XX, tip <SHA>)

### 실제 task 수

X main work + T0 + T8 = Y commit.

### vitest / smoke / TS / lint

| 항목 | baseline | final | Δ |
|---|---|---|---|
| vitest (Asia/Seoul) | 1351 | X | +X |
| vitest (America/New_York) | 1351 | X | +X (drift 0) |
| smoke | 102 | X | +X |
| TS strict | 0 | 0 | 0 |
| lint | 0 / 28 warn | 0 / X warn | Δ |

### Bundle quadruple

| chunk | baseline | final | Δ | gate |
|---|---|---|---|---|
| index | 19077 | X | +X | GREEN/AMBER/RED |
| archive handler | 5031 | X | +X | (실제 chunk 이름과 정확히 매칭) |
| archive tab | 2179 | X | +X | |
| stats | 3582 | X | +X | |

### Codex 2-pass 결과

- 사전 (T0): VERDICT — P0/P1/P2 catch list + in-cycle 흡수 X건
- 최종 (T8): VERDICT — P0/P1/P2 catch list + carry X건

### per-task review dispatch

T1~T7 main task별 reviewer dispatch 결과. in-cycle fix N건.

### 신규 lessons

(작성)

### Carry-forward v3.39+

- C2 Answer/Scrap interestId schema migration (v3.36 brainstorming P3)
- C8 plant-detail seed fixture 추출 (4th smoke trigger)
- F3 NFD 자모 분리 검출 rule 확장 (v3.38 §2.1.1 carry)
- F4 fuzzy threshold A/B UI (v3.38 §2.2.2 carry)
- F5 entity summary chip 색상 토큰 (v3.38 §2.3.2 carry)
- 본 사이클 Codex 최종 P2: ...
```

- [ ] **Step 6: memory file 작성 + MEMORY.md index 갱신**

Create `~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v338.md`:

```markdown
---
name: project_daily_growth_v338
description: "DG v3.38 carry cleanup 7건 일괄 청산 — NFC ESLint rule + briefings strict + fuzzy Levenshtein + stats progressive nav + entity summary chip + smoke visibility 비대칭"
metadata:
  node_type: memory
  type: project
  originSessionId: <session-id>
---

# DG v3.38 Carry Cleanup + Polish

**Status**: ✅ 사이클 완료, 배포 대기 (2026-05-XX, tip `<SHA>`)
**Cycle range**: parent `870a25d` (v3.37 deploy) → tip `<SHA>`

## 핵심 변경

(Track 1/2/3 요약 — spec §2 본문 압축)

## 검증

(vitest/smoke/TS/lint/bundle 표)

## Commits (Y main + 1 retro)

(commit table)

## Carry-forward (v3.39+)

(spec §9 carry 그대로)

## Lessons

(신규 + graduate 후보)
```

`~/.claude/projects/.../memory/MEMORY.md`에 한 줄 인덱스 추가 (v3.37 entry 위에 v3.38 entry).

- [ ] **Step 7: Vault sync (CLAUDE.md global Obsidian 정책)**

```bash
VAULT="/Users/hayden/Hayden/20_Projects/Dev_Project/SJ_Daily Growth(Ai assistant)"
cp docs/superpowers/specs/2026-05-18-v3.38-carry-cleanup-design.md "$VAULT/02-Cycles/"
cp ~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v338.md "$VAULT/03-Lessons/"
cp ~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md "$VAULT/03-Lessons/"
```

Vault 00-Index.md 사이클 타임라인에 v3.38 한 줄 추가 (사용자 vault에서 직접 편집 가능 — 본 step에서는 skip 가능, 명시적 sync 요청 시 추가).

- [ ] **Step 8: retro commit + 배포 가능 보고**

```bash
git add docs/superpowers/specs/2026-05-18-v3.38-carry-cleanup-design.md
git commit -m "docs(v3.38 T8): retro + carry-forward 6건 정리

vitest +X / smoke +X / Bundle Δ X B. Codex 최종 <VERDICT>. carry v3.39+: C2 schema migration + C8 fixture trigger + F3/F4/F5 + ...

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

사용자에게 결과 보고 + "배포해줘" 명시 요청. **사용자 명시 X 상태에서 자동 deploy 절대 X** (CLAUDE.md 절대 규칙).

---

## Bundle Gate 최종 판정

| chunk | baseline | final | Δ | gate |
|---|---|---|---|---|
| index canonical | 19077 | TBD | TBD | ≤+300 B GREEN deploy |
| archive handler | 5031 | TBD | TBD | ≤+300 B GREEN soft |
| archive tab | 2179 | TBD | TBD | ≤+300 B GREEN soft |
| stats | 3582 | TBD | TBD | ≤+300 B GREEN soft |

deploy gate(index)가 RED이면 dedicated trim task 추가 (carry to v3.39 또는 in-cycle).

---

## Self-Review (writing-plans skill Step)

### Spec coverage

| spec section | task |
|---|---|
| §2.1.1 C9 NFC ESLint rule | T1a + T1b |
| §2.1.2 C7 smoke visibility | T2 |
| §2.1.3 C6 T2 spec vacuous | T3 |
| §2.2.1 C3 briefings normalize | T4 |
| §2.2.2 C4 fuzzy Levenshtein | T5a + T5b |
| §2.3.1 C5 stats progressive nav | T6 |
| §2.3.2 C1 entity summary chip | T7a + T7b |
| §4 DoD | T8 풀 검증 |
| §5 risk mitigation | 각 task Step 1 baseline + Step N validation |
| §6 task order | 본 plan 순서 (T1a~T7b) |
| §7 bundle/schema/CSP | T1b/T4/T5b/T6/T7b 각 Bundle 측정 + Schema 0 변경 확인 |

전 spec 항목 task 매핑 ✓.

### Placeholder scan

- T3 Option A vs B: T0 plan 작성 시점 actual code read 후 확정 — placeholder 아닌 의도적 deferred (skill 허용).
- T8 spec §9 Retro template은 retro 작성 시 fill — placeholder 아닌 templated section.
- Bundle Gate 최종 판정 표의 `TBD`: T8 Step 2에서 fill — 실측 baseline이 변동 가능하므로 plan에 hardcode X 정당.
- T6 Step 5 implementation: insight-detail 구조 미확인 영역 — actual code read 후 적용 명시. 의도적 deferred (modal vs inline 분기 결정 plan에 명시).

placeholder scan 통과.

### Type consistency

- `ArchiveSearchCounts` (T7a) ↔ T7b `counts` 변수 일치 ✓
- `levenshteinDistance` (T5a) ↔ `fuzzyMatchesToken` (T5a) 내부 호출 일치 ✓
- `navigateToInterestArchive` (T6) ↔ v3.37 plant-detail.ts export 일치 ✓
- `resetArchiveFilters` (T4/T5b/T6) ↔ v3.37 archive.ts export 일치 ✓

type 일관성 통과.

### 후속 정정 (spec sync)

T1을 T1a + T1b로 split했으므로 spec §6 Task order 표에서 T1을 split 표기해야 함 → T0 Step 6에서 spec sync 진행.
또한 spec §2.2.2 C4는 "신규 파일" 표기였으나 실제 `src/utils/fuzzy.ts` 기존 파일 확장 — spec 정정 의무. T0 Step 6에서 함께 진행.

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-05-18-v3.38-carry-cleanup-plan.md`. Two execution options:

**1. Subagent-Driven (Recommended)** — Fresh subagent per task, two-stage review (spec compliance + code quality) between tasks, fast iteration. DG v3.32~v3.37 6사이클 graduated 패턴.

**2. Inline Execution** — Execute tasks in this session using executing-plans, batch execution with checkpoints.

CLAUDE.md SDD graduated 정책 + 본 사이클 task 수(10~12) 고려 → 본 plan은 **Subagent-Driven**으로 진행 권장.
