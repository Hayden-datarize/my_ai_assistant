# v3.33 lint cleanup + meta graduate Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** lint baseline 30 problems (2 errors + 28 warnings) → 0 problems + Codex 최종 review default soft 명문화

**Architecture:** test 파일 11개의 `as any` 패턴 3종 (plant fixture / 배열 fixture / union narrow + legacy field 접근) + innerHTML 2건을 type-safe 변경으로 일괄 정리. production code 0 touch. CLAUDE.md `§Codex 2-pass Review` soft 명문화로 메타 정책 갱신.

**Tech Stack:** TypeScript strict, Vitest, ESLint (`@typescript-eslint/no-explicit-any`, `no-restricted-syntax`)

---

## §Test Count Baseline

| 단계 | 보고값 | 실측값 | Δ | 원인 |
|---|---|---|---|---|
| (i) v3.32 retro → 본 사이클 T0 시작 | 1312 | 1312 (T0 확인) | 0 | 직전 사이클 정합 |
| (ii) 본 사이클 T0 → T6 끝 | 1312 | TBD | TBD | 신규 spec 없음 — 기존 spec 타입만 정리. expected Δ = 0 |

Lint baseline:
| 단계 | errors | warnings | total |
|---|---|---|---|
| T0 | 2 | 28 | 30 |
| T6 (DoD) | 0 | 0 | 0 |

Bundle CLI gzip baseline:
- v3.32 retro: 19076 B
- v3.33 T0 expected: 19076 B (production code 0 touch)
- v3.33 T6 expected: 19076 B (Δ = 0 B)

---

### Task 0: spec + plan v1 + Codex 사전 review + plan v2 in-cycle 흡수

**Files:**
- Create: `docs/superpowers/specs/2026-05-13-v3.33-lint-cleanup-meta-graduate-design.md`
- Create: `docs/superpowers/plans/2026-05-13-v3.33-lint-cleanup-meta-graduate-plan.md`
- Modify (potentially, after 사전 review): both files above as plan v2

- [ ] **Step 1: spec/plan v1 commit**

```bash
git add docs/superpowers/specs/2026-05-13-v3.33-*.md docs/superpowers/plans/2026-05-13-v3.33-*.md
git commit -m "docs(v3.33 T0): spec + plan v1 — lint cleanup 30→0 + Codex 최종 review soft 명문화"
```

- [ ] **Step 2: baseline 측정**

```bash
npm run lint 2>&1 | tail -5
# Expected: ✖ 30 problems (2 errors, 28 warnings)

npm test -- --run 2>&1 | tail -3
# Expected: Tests 1312 passed (1312)

TZ=America/New_York npm test -- --run 2>&1 | tail -3
# Expected: Tests 1312 passed (1312)

npm run build && gzip -c dist/assets/index-*.js | wc -c
# Expected: 19076 (v3.32 retro 일치)
```

- [ ] **Step 3: Codex 사전 review dispatch (v3.31 L1 graduated)**

```bash
codex exec --skip-git-repo-check -o /tmp/v3.33-codex-pre.txt "
v3.33 lint cleanup + meta graduate spec/plan 사전 review 요청.

Spec: docs/superpowers/specs/2026-05-13-v3.33-lint-cleanup-meta-graduate-design.md
Plan: docs/superpowers/plans/2026-05-13-v3.33-lint-cleanup-meta-graduate-plan.md

본 사이클은:
- tests/ 11개 파일의 \`as any\` 28건 + innerHTML='' 2건을 type-safe 변경으로 일괄 정리 (lint baseline 30→0)
- CLAUDE.md \`§Codex 2-pass Review §최종 review\` soft 명문화 (direct CLI 1차 의무 + fallback + 알림)
- production code 0 touch, 신규 spec 0개, bundle Δ=0 expected

3가지 패턴 fix 전략:
- 패턴 A (10건): \`{ stage, cumulativeActivity } as any\` → \`as Partial<PlantState>\`
- 패턴 B (8건): \`[...] as any\` (saveAnswers/saveBriefings) → \`satisfies Answer[] | Briefing[]\`
- 패턴 C (10건): \`(union as any).field\` → type predicate \`Extract<GameEvent, { kind: K }>\`; legacy \`(v as any).level\` → \`Record<string, unknown>\`

P0/P1/P2 분류로 review 부탁드립니다. 특히:
1. 패턴 A의 \`Partial<PlantState>\`가 type-safety 손실 위험? \`Pick<...>\` 대체가 더 strict?
2. 패턴 C-1의 type predicate \`Extract<T, { kind: K }>\`가 GameEvent variant 전체에 type-safe?
3. T5 soft 명문화 정책 — direct CLI 실패 시 사용자 알림 의무가 사이클-끝 워크플로 어디에 들어가야 자연스러운가?
4. 누락된 패턴 / 케이스가 있는가?
"
```

Expected: P0/P1 흡수 → plan v2 작성, P2는 retro에 backlog.

- [ ] **Step 4: spec/plan v2 commit (if P0/P1 흡수 필요)**

```bash
git add docs/superpowers/specs/2026-05-13-v3.33-*.md docs/superpowers/plans/2026-05-13-v3.33-*.md
git commit -m "docs(v3.33 T0): spec/plan v2 — Codex 사전 review P*N 흡수"
```

(P0/P1 = 0이면 skip)

---

### Task 1: 패턴 A — plant fixture `as any` 10건 정리

**Files:**
- Modify: `tests/ui/modals/plant-detail.spec.ts:16,26,44,57,72,83,96,109`
- Modify: `tests/unit/state/migration-v4.spec.ts:50,86`

- [ ] **Step 1: PlantState import 추가**

`tests/ui/modals/plant-detail.spec.ts` 파일 상단 (line 5 mkUser import 직후):

```ts
import { mkUser } from '../../unit/state/userFixture';
import type { PlantState } from '../../../src/state/plantTypes';
```

- [ ] **Step 2: 8 occurrence 일괄 치환 — v3.33 T0 사전 review P1-1 (collection-level satisfies)**

각 `plantStateByInterest: { ... } as any`의 inner cast 제거하고 **collection 통째로** `satisfies Record<string, PlantState>` 적용:

```ts
// Before
plantStateByInterest: { leadership: { stage: 2, cumulativeActivity: 10 } as any },

// After
plantStateByInterest: {
  leadership: { stage: 2, cumulativeActivity: 10 },
} satisfies Record<string, PlantState>,
```

8 sites: line 16, 26, 44, 57 (multi-line — `unlockedAt` 포함), 72, 83, 96 (multi-line — `lastEngagedAt` 포함), 109.

multi-line 케이스 (line 57, 96):

```ts
// Before (57)
plantStateByInterest: {
  leadership: { stage: 5, cumulativeActivity: 200, unlockedAt: '2026-04-15T00:00:00.000Z' } as any,
},

// After
plantStateByInterest: {
  leadership: { stage: 5, cumulativeActivity: 200, unlockedAt: '2026-04-15T00:00:00.000Z' },
} satisfies Record<string, PlantState>,
```

이유: `Record<string, PlantState>`는 (a) 미래 PlantState required field 추가 시 fixture가 컴파일 fail (regression catch), (b) `satisfies`는 narrow 보존, (c) `Partial<PlantState>` 대비 strict.

- [ ] **Step 3: migration-v4.spec.ts 2 sites — `(out as any).plantStateByInterest` 접근**

이 케이스는 `out`이 `User`이고 plant 필드가 schema에 포함되어 있으나 migration 결과의 nested access가 strict TS에서 통과 안 됨. Fix — `as unknown as Record<string, unknown>`보다 직접 plantStateByInterest 타입 단언:

```ts
// Before (line 50)
expect((out as any).plantStateByInterest.recruiting.stage).toBe(3);

// After
expect(out.plantStateByInterest['recruiting']?.stage).toBe(3);
```

이유: `out`은 `User` 타입이고 `plantStateByInterest`는 schema 필드. 직접 접근 가능. `as any` 불필요했던 케이스 — TS strict noUncheckedIndexedAccess 옵션 때문에 `?.`가 필요.

line 86 동일 패턴.

- [ ] **Step 4: 영향 범위 vitest 실행**

```bash
npm test -- --run tests/ui/modals/plant-detail.spec.ts tests/unit/state/migration-v4.spec.ts 2>&1 | tail -5
# Expected: PASS (개수 변동 없음)
```

- [ ] **Step 5: TS strict + lint 확인**

```bash
npx tsc --noEmit 2>&1 | tail -5
# Expected: 0 errors

npm run lint 2>&1 | grep -E "plant-detail|migration-v4" | head -20
# Expected: 0 warnings on these files
```

- [ ] **Step 6: per-task reviewer dispatch (subagent-driven-development)**

`pr-review-toolkit:code-reviewer` 1회 — focus: 패턴 일관성 / Partial<PlantState> 적정성 / `?.` chain 회귀 없음.

- [ ] **Step 7: Commit**

```bash
git add tests/ui/modals/plant-detail.spec.ts tests/unit/state/migration-v4.spec.ts
git commit -m "test(v3.33 T1): 패턴 A plant fixture as-any → Partial<PlantState> 10건"
```

---

### Task 2: 패턴 B — 배열 fixture `as any` 8건 정리

**Files:**
- Modify: `tests/utils/interestCounts.spec.ts:13,22,35,43,52,59`
- Modify: `tests/state/migration-v5.spec.ts:51` (legacy access — 실제로는 패턴 C 케이스, T3로 이동)
- Modify: `tests/unit/migration-v7.spec.ts:13` (`makeV6Base(): any`)

- [ ] **Step 1: interestCounts.spec.ts — Answer / Briefing import + pinned 필드 추가 + satisfies 치환 (P1-2 흡수)**

파일 상단 import 갱신:

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { getAnswerCountByInterest, getScrapCountByInterest } from '../../src/utils/interestCounts';
import { saveAnswers } from '../../src/state/persistence';
import { saveBriefings } from '../../src/state/briefings';
import type { Answer } from '../../src/state/schema';
import type { Briefing } from '../../src/state/briefings';
```

**v3.33 T0 사전 review P1-2**: `Answer`와 `Briefing` 모두 `pinned: boolean` **required** (v3.28 T2 graduated). 단순 `satisfies` fail → fixture에 `pinned: false` 추가하고 `satisfies` 유지. `as Answer[]` fallback 미사용 (type-safe cleanup 메시지 보존).

6 sites 치환 (Answer fixture × 3 + Briefing fixture × 3):

```ts
// Before (line 10-13)
saveAnswers([
  { id: '1', text: '리더십에 대한 답변', schemaVersion: 1, questionId: 'q1', authorId: 'self', createdAt: '2026-05-08T00:00:00Z' },
  { id: '2', text: 'AI/ML 관련', schemaVersion: 1, questionId: 'q2', authorId: 'self', createdAt: '2026-05-08T00:00:00Z' },
] as any);

// After
saveAnswers([
  { id: '1', text: '리더십에 대한 답변', schemaVersion: 1, questionId: 'q1', authorId: 'self', createdAt: '2026-05-08T00:00:00Z', pinned: false },
  { id: '2', text: 'AI/ML 관련', schemaVersion: 1, questionId: 'q2', authorId: 'self', createdAt: '2026-05-08T00:00:00Z', pinned: false },
] satisfies Answer[]);
```

Briefing 동일 패턴 — 각 element에 `pinned: false` 추가 + `satisfies Briefing[]`:

```ts
// Before (line 19-22)
saveBriefings([
  { id: 'b1', date: '2026-05-08', url: 'https://x.com/1', title: '리더십 trend', summary: 'leadership tips', scrapped: true, read: false, memo: '' },
  { id: 'b2', date: '2026-05-08', url: 'https://x.com/2', title: 'AI news', summary: 'machine learning', scrapped: false, read: false, memo: '' },
] as any);

// After
saveBriefings([
  { id: 'b1', date: '2026-05-08', url: 'https://x.com/1', title: '리더십 trend', summary: 'leadership tips', scrapped: true, read: false, memo: '', pinned: false },
  { id: 'b2', date: '2026-05-08', url: 'https://x.com/2', title: 'AI news', summary: 'machine learning', scrapped: false, read: false, memo: '', pinned: false },
] satisfies Briefing[]);
```

6 sites: line 10-13, 19-22, 33-35, 41-43, 50-52, 57-59. Answer 3건 + Briefing 3건.

- [ ] **Step 2: migration-v7.spec.ts — `makeV6Base(): any` → intersection 타입 (P2-3 흡수)**

```ts
// Before (line 13)
function makeV6Base(): any {

// After
function makeV6Base(): Record<string, unknown> & { schemaVersion: 6 | 7; insights: unknown[] } {
```

이유: `Record<string, unknown>` 단독은 broad index — schemaVersion / insights mutation 의도를 타입에 명시. 후속 `v6.insights = [...]` mutation type-safe.

- [ ] **Step 3: 영향 범위 vitest 실행**

```bash
npm test -- --run tests/utils/interestCounts.spec.ts tests/unit/migration-v7.spec.ts 2>&1 | tail -5
# Expected: PASS (개수 변동 없음)
```

- [ ] **Step 4: TS strict + lint 확인**

```bash
npx tsc --noEmit 2>&1 | tail -5
# Expected: 0 errors

npm run lint 2>&1 | grep -E "interestCounts|migration-v7" | head -10
# Expected: 0 warnings on these files
```

- [ ] **Step 5: per-task reviewer dispatch**

focus: `satisfies` vs `as` 일관성 / Answer/Briefing import path 정합 / 인터미턴트 missing field 없음.

- [ ] **Step 6: Commit**

```bash
git add tests/utils/interestCounts.spec.ts tests/unit/migration-v7.spec.ts
git commit -m "test(v3.33 T2): 패턴 B 배열 fixture as-any → satisfies + Record 7건"
```

---

### Task 3: 패턴 C — union narrow / legacy field 접근 10건 정리

**Files:**
- Modify: `tests/unit/state/achievements.spec.ts:27,39,51,62` (union narrow × 4)
- Modify: `tests/unit/state/events-plant.spec.ts:77` (union narrow × 1)
- Modify: `tests/unit/state/user-migration.spec.ts:13,25,160` (legacy field × 3)
- Modify: `tests/unit/state-user.spec.ts:26` (legacy field × 1)
- Modify: `tests/unit/validate-interest-id.spec.ts:43` (legacy field × 1)
- Modify: `tests/state/migration-v5.spec.ts:51` (legacy access × 1)

**Note**: `tests/state/migration-v5.spec.ts:51`의 `(out as any).insights` — `out`이 v5 User로 cast되지만 insights는 v6+ 필드. Pre-v6 변형 검증 케이스이므로 legacy field 패턴.

- [ ] **Step 1: achievements.spec.ts — discriminated union narrow × 4 (P2-1 흡수)**

파일 상단 import 확인 (line 3에 `Snapshot, GameEvent` 이미 import — 추가 불필요).

**v3.33 T0 사전 review P2-1**: `xp!.amount` non-null assertion 대신 `xp?.amount` optional chain — 단일 expression.

4 sites 치환:

```ts
// Before (line 25-27)
const xp = events.find(e => e.kind === 'xp-gained');
expect(xp).toBeDefined();
expect((xp as any).amount).toBe(10);

// After
const xp = events.find((e): e is Extract<GameEvent, { kind: 'xp-gained' }> => e.kind === 'xp-gained');
expect(xp?.amount).toBe(10);
```

line 37-39 (`'level-up'`, `.tierId`):

```ts
// After
const lv = events.find((e): e is Extract<GameEvent, { kind: 'level-up' }> => e.kind === 'level-up');
expect(lv?.tierId).toBe(2);
```

line 49-51 (`'streak-milestone'`, `.days`):

```ts
// After
const sm = events.find((e): e is Extract<GameEvent, { kind: 'streak-milestone' }> => e.kind === 'streak-milestone');
expect(sm?.days).toBe(3);
```

line 62 (filter + map):

```ts
// Before
const days = events.filter(e => e.kind === 'streak-milestone').map(e => (e as any).days);

// After
const days = events.filter((e): e is Extract<GameEvent, { kind: 'streak-milestone' }> => e.kind === 'streak-milestone').map(e => e.days);
```

- [ ] **Step 2: events-plant.spec.ts:77 — predicate 불필요 (P2-2 흡수)**

**v3.33 T0 사전 review P2-2**: `at: number`는 6 GameEvent variant 공통 필드 (`src/state/gameTypes.ts:27-32` 전수 확인). predicate 불필요 — 단순 optional chain:

```ts
// Before (line 76-77)
const e = events.find(ev => ev.kind === 'plant-stage-up');
expect(typeof (e as any)?.at).toBe('number');

// After
const e = events.find(ev => ev.kind === 'plant-stage-up');
expect(typeof e?.at).toBe('number');
```

- [ ] **Step 3: user-migration.spec.ts × 3 + state-user.spec.ts × 1 + validate-interest-id.spec.ts × 1 — legacy field**

```ts
// Before (line 13)
expect((v2 as any).level).toBeUndefined();

// After
expect((v2 as unknown as Record<string, unknown>).level).toBeUndefined();
```

`migrateUserToV2` 반환은 `User` 타입이고 `level`은 User에 없는 deprecated 필드. `User` → `Record<string, unknown>` 직접 cast가 안 되므로 `unknown` 2-step.

line 25, 160 / state-user.spec.ts:26 / validate-interest-id.spec.ts:43 동일.

- [ ] **Step 4: migration-v5.spec.ts:51 — `(out as any).insights`**

```ts
// Before
expect((out as any).insights).toEqual(v6.insights);

// After
expect((out as unknown as Record<string, unknown>).insights).toEqual(v6.insights);
```

- [ ] **Step 5: 영향 범위 vitest 실행**

```bash
npm test -- --run tests/unit/state/achievements.spec.ts tests/unit/state/events-plant.spec.ts tests/unit/state/user-migration.spec.ts tests/unit/state-user.spec.ts tests/unit/validate-interest-id.spec.ts tests/state/migration-v5.spec.ts 2>&1 | tail -5
# Expected: PASS (개수 변동 없음)
```

- [ ] **Step 6: TS strict + lint 확인**

```bash
npx tsc --noEmit 2>&1 | tail -5
# Expected: 0 errors

npm run lint 2>&1 | tail -5
# Expected: T1+T2+T3 합쳐서 warnings 28 → 0 (errors 2건은 T4에서 처리)
```

- [ ] **Step 7: per-task reviewer dispatch**

focus: type predicate 정확성 (`Extract<T, { kind: K }>` GameEvent variant 전수 확인) / `Record<string, unknown>` cast가 의도 정확히 표현하는지 / `xp!.amount` non-null assertion 안전성 (직전 `expect(xp).toBeDefined()` 가드).

- [ ] **Step 8: Commit**

```bash
git add tests/unit/state/achievements.spec.ts tests/unit/state/events-plant.spec.ts tests/unit/state/user-migration.spec.ts tests/unit/state-user.spec.ts tests/unit/validate-interest-id.spec.ts tests/state/migration-v5.spec.ts
git commit -m "test(v3.33 T3): 패턴 C union narrow + legacy field as-any 10건"
```

---

### Task 4: errors 2건 fix — innerHTML → replaceChildren

**Files:**
- Modify: `tests/ui/archive-chip-filter.spec.ts:11`
- Modify: `tests/ui/archive-relocated-onboarding.spec.ts:12`

- [ ] **Step 1: archive-chip-filter.spec.ts:11 치환**

```ts
// Before (line 11)
beforeEach(async () => {
  localStorage.clear();
  sessionStorage.clear();
  document.body.innerHTML = '';
});

// After
beforeEach(async () => {
  localStorage.clear();
  sessionStorage.clear();
  document.body.replaceChildren();
});
```

- [ ] **Step 2: archive-relocated-onboarding.spec.ts:12 동일 치환**

- [ ] **Step 3: 영향 범위 vitest 실행**

```bash
npm test -- --run tests/ui/archive-chip-filter.spec.ts tests/ui/archive-relocated-onboarding.spec.ts 2>&1 | tail -5
# Expected: PASS (개수 변동 없음, replaceChildren 동등 의미)
```

- [ ] **Step 4: lint 일괄 확인 (T1~T4 누적)**

```bash
npm run lint 2>&1 | tail -3
# Expected: 0 problems (errors 0 + warnings 0) — v3.33 DoD 달성
```

- [ ] **Step 5: Commit**

```bash
git add tests/ui/archive-chip-filter.spec.ts tests/ui/archive-relocated-onboarding.spec.ts
git commit -m "test(v3.33 T4): innerHTML → replaceChildren 2건 (lint 0 problems 달성)"
```

---

### Task 5: meta graduate — CLAUDE.md `§Codex 2-pass Review §최종 review` soft 명문화

**Files:**
- Modify: `CLAUDE.md` (`## Codex 2-pass Review` 섹션 — `2. 최종 review` 항목)

- [ ] **Step 1: 현재 § 위치 확인**

```bash
grep -n "최종 review\|Codex 2-pass\|deploy gate" CLAUDE.md | head -10
```

- [ ] **Step 2: `2. 최종 review` 항목을 단계화 정책으로 갱신**

기존 (대략):

```markdown
2. **최종 review (deploy gate)** — 모든 main work commit 후, 배포 직전. `DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT` 분류.
   - 사용자 "배포해줘" 명시 시점에 게이트로 작동.
```

치환 (v3.33 soft 명문화 — fallback 책임 명시 + 사용자 알림 의무 + P2-4 흡수):

```markdown
2. **최종 review (deploy gate)** — 모든 main work commit 후, 배포 직전. `DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT` 분류.
   - 사용자 "배포해줘" 명시 시점에 게이트로 작동.
   - **default dispatch** (v3.33 graduated):
     ```bash
     codex exec --skip-git-repo-check -o /tmp/<cycle>-codex-final.txt "<prompt>"
     ```
   - **fallback 단계 정책** (CLI 한도/권한 issue 시):
     1. direct CLI 1차 시도 **의무** — 실패 명시 캡처
     2. controller `pr-review-toolkit:code-reviewer`로 대체
     3. **사용자에게 즉시 알림** — fallback 진입 시점 (classification 전, 검증 결과 보고와 함께)
     4. fallback review는 **deploy gate 충족**으로 인정 (배포 차단 X)
     5. retro §verification에 시도 결과 명시 (성공/fallback/실패 사유)
     6. retroactive direct CLI 재시도는 **권고/carry** (필수 X — 외부 권한 회복 시 차기 사이클로)
   - **ROI 근거** (v3.32 L2): direct CLI 성공 시 P0/P1/P2 종합 catch. controller fallback만으로는 P2 catch 부족 입증.
```

- [ ] **Step 3: 변경 검증 — markdown 형식 확인**

```bash
grep -A 30 "최종 review (deploy gate)" CLAUDE.md
# Expected: 위 갱신 내용 정확 표시
```

- [ ] **Step 4: Commit**

```bash
git add CLAUDE.md
git commit -m "docs(v3.33 T5): CLAUDE.md Codex 최종 review soft 명문화 — direct CLI 1차 의무 + fallback + 사용자 알림"
```

---

### Task 6: 사이클-끝 3종 검증 + Codex 최종 review

**Files:** (검증만, 코드 touch 없음)

- [ ] **Step 1: TS strict**

```bash
npx tsc --noEmit
# Expected: 0 errors
```

- [ ] **Step 2: Lint (DoD core)**

```bash
npm run lint 2>&1 | tail -3
# Expected: 0 problems (errors 0 + warnings 0)
```

- [ ] **Step 3: vitest KST**

```bash
npm test -- --run 2>&1 | tail -3
# Expected: Tests 1312 passed (1312)
```

- [ ] **Step 4: vitest NY**

```bash
TZ=America/New_York npm test -- --run 2>&1 | tail -3
# Expected: Tests 1312 passed (1312) — KST와 동일
```

- [ ] **Step 5: Smoke (cold-start fresh fork)**

```bash
rm -rf node_modules/.vite-temp dist
npm run build
npm run test:smoke 2>&1 | tail -5
# Expected: 100 passed
```

- [ ] **Step 6: Bundle 측정 (CLI gzip canonical)**

```bash
gzip -c dist/assets/index-*.js | wc -c
# Expected: 19076 (Δ = 0 B, production code 0 touch)
```

- [ ] **Step 7: Codex 최종 review dispatch (T5 soft graduate 자기-적용)**

```bash
codex exec --skip-git-repo-check -o /tmp/v3.33-codex-final.txt "
v3.33 lint cleanup + meta graduate 최종 review 요청 (deploy gate).

git diff range: c48d562..HEAD
주요 변경:
- T1: tests/ui/modals/plant-detail.spec.ts (8) + tests/unit/state/migration-v4.spec.ts (2)
  — Partial<PlantState> + ?. chain
- T2: tests/utils/interestCounts.spec.ts (6) + tests/unit/migration-v7.spec.ts (1)
  — satisfies Answer[] | Briefing[] + Record<string, unknown>
- T3: tests/unit/state/achievements.spec.ts (4) + tests/unit/state/events-plant.spec.ts (1)
  + tests/unit/state/user-migration.spec.ts (3) + tests/unit/state-user.spec.ts (1)
  + tests/unit/validate-interest-id.spec.ts (1) + tests/state/migration-v5.spec.ts (1)
  — Extract<GameEvent, { kind: K }> type predicate + Record<string, unknown>
- T4: tests/ui/archive-{chip-filter,relocated-onboarding}.spec.ts
  — innerHTML='' → replaceChildren()
- T5: CLAUDE.md §Codex 2-pass Review §최종 review soft 명문화

검증 결과:
- TS strict: 0 errors
- Lint: 0 problems (v3.32 30 → 0, 완전 청산)
- vitest KST + NY: 1312 PASS 양쪽 동일 (drift 0)
- smoke: 100 PASS cold-start fresh fork
- bundle CLI gzip: 19076 B (Δ = 0 B, production 0 touch)

production code 0 touch 의도된 cleanup 사이클.

DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT 분류로 review 부탁드립니다.
특히:
1. \`Extract<GameEvent, { kind: K }>\` type predicate가 GameEvent variant 전체에 type-safe?
2. \`as unknown as Record<string, unknown>\` cast가 lint rule 회피 의도가 아닌 deprecated field 부재 검증 의도를 명확히 표현?
3. T5 soft 명문화 — fallback 정책의 실효성, 책임 명시 충분?
"
```

**Fallback** (T5 정책 자기-적용): direct CLI 실패 시 controller `pr-review-toolkit:code-reviewer`로 대체 + **사용자에게 즉시 알림** + retro §verification에 시도 결과 명시.

- [ ] **Step 8: Codex review 결과 분류**

- **DEPLOY_APPROVED**: T7 retro 진행, deploy 대기.
- **APPROVED_WITH_NOTES**: P0/P1는 in-cycle 흡수 (이 Task에서 fix commit), P2는 retro carry.
- **REJECT**: P0 즉시 fix → 재dispatch.

- [ ] **Step 9: (P0/P1 흡수 commit, 필요 시)**

```bash
git add <fix files>
git commit -m "<refactor|test>(v3.33 T6 fix): Codex 최종 review P*N in-cycle 흡수"
```

---

### Task 7: retro + MEMORY.md + Obsidian vault sync

**Files:**
- Modify: `docs/superpowers/specs/2026-05-13-v3.33-lint-cleanup-meta-graduate-design.md` (§7 retro)
- Modify: `/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md`
- Create: `/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v333.md`
- Copy: Obsidian vault `02-Cycles/` + `03-Lessons/` sync (전역 CLAUDE.md 정책)

- [ ] **Step 1: retro §7 작성**

`docs/superpowers/specs/2026-05-13-v3.33-*.md` §7에 다음 sub-section 채움:
- 7.1 결과 요약 (HEAD / commits / 변경 파일 수 / vitest / lint / bundle)
- 7.2 Task별 결과 표
- 7.3 Lint baseline 변화 (30 → 0 = 100% cleanup)
- 7.4 Bundle 측정 (CLI gzip Δ)
- 7.5 Codex 2-pass 결과 (사전 / 최종)
- 7.6 Lessons (graduate 후보)
- 7.7 Carry-forward (v3.34)
- 7.8 Next Recommended Step

- [ ] **Step 2: MEMORY.md 본 사이클 entry 1줄 추가**

```markdown
- [DG v3.33 Lint Cleanup + Meta Graduate](project_daily_growth_v333.md) — <상세>
```

- [ ] **Step 3: 신규 memory 파일 생성**

`memory/project_daily_growth_v333.md`에 표준 frontmatter + 사이클 요약:

```markdown
---
name: project-daily-growth-v333
description: v3.33 lint baseline 30→0 완전 청산 + Codex 최종 review soft 명문화
metadata:
  type: project
---

(retro §7 요약 — HEAD / commits / 검증 / Codex / lessons / carry)
```

- [ ] **Step 4: Obsidian vault sync (전역 CLAUDE.md 정책)**

```bash
VAULT="/Users/hayden/Hayden/20_Projects/Dev_Project/SJ_Daily Growth(Ai assistant)"
cp docs/superpowers/specs/2026-05-13-v3.33-*.md "$VAULT/02-Cycles/"
cp /Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v333.md "$VAULT/03-Lessons/"
cp /Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md "$VAULT/03-Lessons/"
# 00-Index.md 사이클 타임라인 표에 v3.33 한 줄 추가 (manual edit)
```

- [ ] **Step 5: retro commit**

```bash
git add docs/superpowers/specs/2026-05-13-v3.33-*.md
git commit -m "docs(v3.33 T7 retro): 사이클-끝 + lint 30→0 + Codex 최종 review soft 명문화 graduated + v3.34 carry"
```

---

## §Self-Review Checklist

1. **Spec coverage**: 모든 §1~§6 항목이 task로 매핑됨 ✅
   - §1 motivation → T0 spec
   - §2 architecture (패턴 A/B/C/D) → T1/T2/T3/T4
   - §2.2 메타 graduate → T5
   - §4 DoD/Bundle → T6
   - §7 retro → T7
2. **Placeholder scan**: TBD/TODO 사용 위치 — `§Test Count Baseline` 표 `(ii) T6 끝`만 TBD (실측 후 작성). 그 외 placeholder 없음.
3. **Type consistency**:
   - `Partial<PlantState>` (T1) — `src/state/plantTypes.ts:3` 정합.
   - `satisfies Answer[]` (T2) — `src/state/schema.ts:9` 정합.
   - `satisfies Briefing[]` (T2) — `src/state/briefings.ts:11` 정합.
   - `Extract<GameEvent, { kind: K }>` (T3) — `src/state/gameTypes.ts:26` GameEvent 정합.
   - `Record<string, unknown>` (T2/T3 legacy) — built-in TS utility, import 불필요.
4. **명령 정확성**: `npx tsc --noEmit`, `npm run lint`, `npm test -- --run`, `TZ=America/New_York npm test -- --run`, `gzip -c dist/assets/index-*.js | wc -c` — 모두 v3.32 사이클에서 검증된 명령. 추가 검증 불필요.
