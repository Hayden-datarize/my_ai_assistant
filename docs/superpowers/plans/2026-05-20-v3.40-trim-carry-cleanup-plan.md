# v3.40 Trim + Carry Cleanup + Lesson Graduate — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v3.39 §9.6 carry-forward 13건 일괄 청산 + bundle canonical (entry JS + CSS) -1200 B 합산 trim + lesson 5건 graduate. 신규 기능 0, schema migration 0.

**Architecture:** XL cleanup cycle. Spec v2 (Codex 사전 review P0×1+P1×6+P2×2 흡수). canonical 재정의 = `entry-*.js + index-*.css` gzip 합산 (baseline 30204 B → target 29004 B). 15 main task + 1 wrap-up (T15 retro).

**Tech Stack:** Vite + TypeScript + Vitest + Playwright + Firebase Hosting. ESLint flat config + local NFC rule. localStorage + IndexedDB (없음, localStorage 전용).

**Spec**: [docs/superpowers/specs/2026-05-20-v3.40-trim-carry-cleanup-design.md](../specs/2026-05-20-v3.40-trim-carry-cleanup-design.md)

---

## File Structure

### 신규 파일
- `src/ui/handlers/archive-nav.ts` — T8: `navigateToInterestArchive(interestId)` 공용 helper (resetArchiveFilters 재사용 + 내부 dynamic import)
- `src/ui/handlers/byInterestNotice.ts` — T7: `maybeShowByInterestNotice()` one-time toast 공유 helper
- `tests/smoke/_helpers/plant-detail-seed.ts` — T9: smoke fixture `seedPlantDetail(page, opts)`

### 수정 대상 파일
- `src/main.ts` — T2 (welcome-garden + onboarding dynamic import)
- `src/utils/gemini-parse.ts` — T5 (line 107 validateInterestId 적용)
- `src/ui/handlers/home.ts` — T5 (line 974 submitAnswer dataset double-close)
- `src/ui/tabs/stats.ts` — T7 (renderStats 진입점 maybeShowByInterestNotice 호출)
- `src/ui/handlers/archive-listeners.ts` — T7 (archive 진입 trigger)
- `src/styles/tokens.css` — T12 (`--chip-answer/briefing/insight-color` 추가)
- `src/styles/pages/archive.css` — T12 (chip 색상 var() 교체)
- `src/styles/base.css` 또는 `src/styles/layout.css` — T6 (scroll-margin selector 보강, 진단 결과 의존)
- `tests/smoke/archive-interest-filter.spec.ts` — T6/T10/T11
- `tests/smoke/archive-insight-detail-nav.spec.ts` — T6/T9
- `tests/smoke/archive-plant-detail-nav.spec.ts` — T6/T9/T11
- `tests/smoke/plant-detail-modal.spec.ts` — T9
- `tests/smoke/archive-unified.spec.ts` — T11
- `tests/smoke/v3.13-missions.spec.ts` — T11
- `src/ui/modals/plant-detail.ts` — T8 (`navigateToInterestArchive` 정의 → archive-nav.ts 이동)
- `src/ui/modals/insight-detail.ts` — T8 (import path 갱신)
- `src/ui/handlers/archive.ts` — T8 (L689 reset 시맨틱 공용 helper 호출)
- `package.json` — T3 (knip devDep 추가 시도)
- `eslint.config.mjs` — 필요 시 (T3 knip ignore)
- `.claude/COMMON_MISTAKES.md` — T13 (L1 §12 신규 섹션)
- `CLAUDE.md` — T13 (L2/L3/L5/L6 + Codex default checklist inline)
- `docs/superpowers/specs/2026-05-18-v3.38-carry-cleanup-design.md` — T10 (§9.6 C3 정정 주석)

### 제거 대상 (T3 dead code 후보 — T1 P0 gate에서 확정)
- 후보 리스트는 T3 실행 시점에 knip 또는 수동 grep 결과로 확정.

---

## Task 1: Baseline 측정 + Lane Budget 확정 (P0 gate)

**Files:**
- Modify: `docs/superpowers/specs/2026-05-20-v3.40-trim-carry-cleanup-design.md` (lane budget amend 필요 시)
- Output: T1 측정 결과 retro §9.4 임시 기록

- [ ] **Step 1: 깨끗한 build 실행**

Run:
```bash
rm -rf dist node_modules/.vite-temp && npm run build
```
Expected: Build success, `dist/assets/` chunk emit.

- [ ] **Step 2: Canonical 합산 측정**

Run:
```bash
JS=$(gzip -c dist/assets/index-*.js | wc -c | tr -d ' ')
CSS=$(gzip -c dist/assets/index-*.css | wc -c | tr -d ' ')
TOTAL=$((JS + CSS))
echo "Entry JS: $JS B, CSS: $CSS B, Canonical 합산: $TOTAL B"
```
Expected: `Entry JS: 19706 B, CSS: 10498 B, Canonical 합산: 30204 B` (또는 ±50 B 변동).

- [ ] **Step 3: Archive chunk 측정**

Run:
```bash
gzip -c dist/assets/archive-*.js | wc -c
```
Expected: 6096 B (또는 ±50 B).

- [ ] **Step 4: Entry JS 내부 크기 분포 manual 분석**

Run:
```bash
# Vite source map 분석 (큰 module 파악)
ls -la dist/assets/index-*.js.map
# 또는 단순 크기 비율 추정
wc -l src/utils/*.ts src/state/*.ts | sort -n -r | head -10
```
Expected: migration.ts (509 lines), user.ts (463 lines), achievements.ts (210 lines) 등 큰 module 식별.

- [ ] **Step 5: CSS 내부 분석 — unused token 후보**

Run:
```bash
# 정의된 token 모두 추출
grep -E "^\s*--[a-z-]+:" src/styles/tokens.css | sed -E 's/^\s*(--[a-z-]+):.*/\1/' > /tmp/tokens.txt
# 각 token 사용처 카운트
while read token; do
  count=$(rg -c "var\($token\b|$token\b" src --type css --type ts 2>/dev/null | awk -F: '{sum+=$2} END {print sum}')
  echo "$count $token"
done < /tmp/tokens.txt | sort -n | head -20
```
Expected: 사용처 0~2회 token 리스트 (T4 trim 후보).

- [ ] **Step 6: Lane budget 확정**

Plan §1 G1과 비교:
- T2 lane (dynamic import): welcome-garden + onboarding 예상 -250~450 B
- T3 lane (dead code): -100~300 B
- T4 lane (CSS unused): -500~1000 B
- **합산 예상**: -850~1750 B (-1200 B 의무 도달 가능)

미달 가능성 (예: 합산 < -1000 B):
- 사용자에게 옵션 제시 (target 완화 vs 새 lane 발굴 vs spec amend)
- spec v3 작성 후 progress

- [ ] **Step 7: T1 측정 결과 임시 기록 (retro §9.4 placeholder 갱신)**

`docs/superpowers/specs/2026-05-20-v3.40-trim-carry-cleanup-design.md` §9.4 임시 갱신:
```markdown
### 9.4 Bundle 측정 (T1 baseline)
| 항목 | 측정값 |
|---|---|
| Entry JS (gzip) | <측정값> B |
| CSS (gzip) | <측정값> B |
| Canonical 합산 | <측정값> B |
| Archive chunk | <측정값> B |
| Lane budget 확정 | T2 <예상> / T3 <예상> / T4 <예상> = 합산 <예상> |
```

- [ ] **Step 8: Commit**

```bash
git add docs/superpowers/specs/2026-05-20-v3.40-trim-carry-cleanup-design.md
git commit -m "docs(v3.40 T1): baseline 측정 + lane budget 확정

canonical 합산: <측정값> B (entry JS <X> + CSS <Y>)
Lane budget: T2 <X> / T3 <Y> / T4 <Z> = 합산 <SUM>

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: Dynamic Import — welcome-garden + onboarding

**Files:**
- Modify: `src/main.ts`

- [ ] **Step 1: main.ts 현재 구조 Read**

기존 static import 확인:
```ts
import { renderOnboarding } from './ui/onboarding';
import { maybeShowWelcomeGarden } from './ui/modals/welcome-garden';
```
+ 호출 위치 grep:
```bash
rg -n "renderOnboarding|maybeShowWelcomeGarden" src/main.ts
```

- [ ] **Step 2: dynamic import 패턴 변경 (welcome-garden)**

`src/main.ts:6` static import 제거. 호출 위치에 dynamic import:
```ts
// 변경 전 (line 6):
// import { maybeShowWelcomeGarden } from './ui/modals/welcome-garden';

// 변경 후 (호출 위치):
void import('./ui/modals/welcome-garden').then(({ maybeShowWelcomeGarden }) => {
  maybeShowWelcomeGarden();
});
```

- [ ] **Step 3: dynamic import 패턴 변경 (onboarding)**

`src/main.ts:5` 동일 패턴:
```ts
// 변경 전:
// import { renderOnboarding } from './ui/onboarding';

// 변경 후 (호출 위치, 조건부):
if (/* 미온보딩 조건 */) {
  void import('./ui/onboarding').then(({ renderOnboarding }) => {
    renderOnboarding();
  });
}
```

(실제 조건은 main.ts:5 호출 직전 조건 확인 후 동일하게 유지)

- [ ] **Step 4: Build + chunk 측정**

Run:
```bash
rm -rf dist && npm run build
JS=$(gzip -c dist/assets/index-*.js | wc -c | tr -d ' ')
CSS=$(gzip -c dist/assets/index-*.css | wc -c | tr -d ' ')
echo "After T2: Entry JS $JS / CSS $CSS / Canonical $((JS+CSS))"
ls -la dist/assets/welcome-garden-*.js dist/assets/onboarding-*.js 2>/dev/null
```
Expected: welcome-garden + onboarding 신규 chunk emit. canonical 합산 -250~450 B.

- [ ] **Step 5: 단위 + smoke 검증**

Run:
```bash
npx tsc --noEmit
npm run lint
npm test -- main.spec
```
Expected: 0 errors. main 영역 vitest PASS.

- [ ] **Step 6: First-launch smoke 회귀 확인**

Run:
```bash
npm run test:smoke -- --grep "welcome|onboarding"
```
Expected: PASS. welcome-garden 모달이 first-launch 시 정상 표시 (dynamic load <50ms).

- [ ] **Step 7: Commit**

```bash
git add src/main.ts
git commit -m "perf(v3.40 T2): welcome-garden + onboarding dynamic import (-XXX B canonical)

main.ts static import 2건 → dynamic import.
- welcome-garden: first-launch 1회만 로드
- onboarding: 미온보딩 사용자만 로드

Bundle canonical: <baseline> → <after> (-XXX B)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: Dead Code Sweep (knip + 수동 grep fallback)

**Files:**
- Modify: `package.json` (knip devDep 시도)
- Modify: dead code 발견 source 파일 (T1 측정 기준 후보)

- [ ] **Step 1: knip 설치 시도**

Run:
```bash
npm install --save-dev knip 2>&1 | head -10
```
Expected: 설치 success. 실패 시 fallback (Step 2 건너뛰고 Step 3).

- [ ] **Step 2: knip 실행 (성공 시)**

Run:
```bash
npx knip --include exports,types,files --reporter compact > /tmp/v3.40-knip.txt 2>&1
head -50 /tmp/v3.40-knip.txt
```
Expected: unused exports / files 리스트.

- [ ] **Step 3: 수동 grep fallback (knip 실패 시 또는 보완)**

Run:
```bash
# @internal 마크 export 후보 추출
rg -n "@internal" src --type ts | head -20
# 각 후보 production caller 검증
# 예: getCachedUserShape이라는 후보면
rg -n "getCachedUserShape" src tests --type ts
```
Expected: caller 0건인 export 식별.

- [ ] **Step 4: 후보 리스트를 사용자에게 confirm 요청**

`AskUserQuestion` multiSelect 사용:
```
question: "T3 dead code sweep — 어떤 export를 제거할까요? (검증 후 caller 0건)"
options: 후보 1, 후보 2, ..., 후보 N (multiSelect=true)
```
Expected: 사용자 선택 항목 즉시 제거.

- [ ] **Step 5: 선택 항목 제거**

각 선택 항목 별로 `export` 키워드 + 함수/const 본문 제거. dependency 함수도 caller 0이 되면 cascade 제거.

- [ ] **Step 6: 검증**

Run:
```bash
npx tsc --noEmit
npm run lint
npm test
```
Expected: 0 errors. 전체 vitest PASS.

- [ ] **Step 7: Build + bundle 측정**

Run:
```bash
rm -rf dist && npm run build
JS=$(gzip -c dist/assets/index-*.js | wc -c | tr -d ' ')
CSS=$(gzip -c dist/assets/index-*.css | wc -c | tr -d ' ')
echo "After T3: Entry JS $JS / CSS $CSS / Canonical $((JS+CSS))"
```
Expected: canonical -100~300 B.

- [ ] **Step 8: 미선택 항목 carry table 기록**

`docs/superpowers/specs/2026-05-20-v3.40-trim-carry-cleanup-design.md` §8 v3.41+ 후보에 미선택 항목 1줄씩 추가.

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "perf(v3.40 T3): dead code sweep — N exports 제거 (-XXX B canonical)

knip + 수동 grep 후보 N건 중 M건 사용자 confirm 후 제거.
미선택 항목 K건은 v3.41 carry table.

Bundle canonical: <after T2> → <after T3> (-XXX B)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: CSS Unused Sweep (Mid-cycle Bundle Measurement)

**Files:**
- Modify: `src/styles/tokens.css`
- Modify: `src/styles/main.css` 또는 components/* (duplicate rule 발견 위치)

- [ ] **Step 1: T1 Step 5 결과 (unused token 후보) 재사용 또는 재실행**

Run:
```bash
grep -E "^\s*--[a-z-]+:" src/styles/tokens.css | sed -E 's/^\s*(--[a-z-]+):.*/\1/' > /tmp/tokens.txt
while read token; do
  count=$(rg -c "$token" src --type css --type ts 2>/dev/null | awk -F: '{sum+=$2} END {print sum}')
  echo "$count $token"
done < /tmp/tokens.txt | sort -n | head -20
```
Expected: 사용처 0~2회 token 리스트.

- [ ] **Step 2: Unused token 제거**

각 count=0 token을 `src/styles/tokens.css`에서 제거. count=1~2인 경우 사용처 확인 후 인라인 결정 (제거 보류 가능).

- [ ] **Step 3: Duplicate rule 식별**

Run:
```bash
# selector 중복 탐지 (대략적)
rg -n "^\.[a-z][a-z0-9_-]+\s*{" src/styles/main.css src/styles/components/*.css | awk -F'[: ]' '{print $NF}' | sort | uniq -d | head -10
```
Expected: 중복 selector 리스트.

- [ ] **Step 4: Duplicate rule 통합**

발견된 중복을 한 곳으로 통합. 단, modifier (`:hover`, `:focus`)는 다른 selector로 간주.

- [ ] **Step 5: `@media (hover: hover)` 가드 일관 확인**

Run:
```bash
rg -n "@media\s*\(hover" src/styles/
```
Expected: 모든 `:hover` 스타일이 `@media (hover: hover)` 안에 포함. 누락 시 가드 추가.

- [ ] **Step 6: Build + canonical 측정 (mid-cycle gate)**

Run:
```bash
rm -rf dist && npm run build
JS=$(gzip -c dist/assets/index-*.js | wc -c | tr -d ' ')
CSS=$(gzip -c dist/assets/index-*.css | wc -c | tr -d ' ')
TOTAL=$((JS+CSS))
DELTA=$((TOTAL - 30204))
echo "After T4: Entry JS $JS / CSS $CSS / Canonical $TOTAL (Δ $DELTA)"
```
Expected: Canonical Δ ≤ -1200 B (T2~T4 합산 의무).

- [ ] **Step 7: Mid-cycle gate — Δ < -1200 B 미달 시 처리**

미달 시:
- T8 helper trim 추가 효과 활용 가능 → T5~T7 진행하고 T8 종료 시 재측정.
- 또는 사용자에게 옵션 제시 (target 완화 / 새 lane 발굴 / spec v3).

도달 시: 다음 task 진행.

- [ ] **Step 8: 단위 + smoke 검증**

Run:
```bash
npx tsc --noEmit
npm run lint
npm test
npm run test:smoke -- --grep "archive|home"
```
Expected: 모두 PASS.

- [ ] **Step 9: Commit**

```bash
git add src/styles/
git commit -m "perf(v3.40 T4): CSS unused token + duplicate rule sweep (-XXX B canonical)

- Unused token N건 제거 (tokens.css)
- Duplicate rule M건 통합 (main.css + components/*)
- @media (hover: hover) 가드 K건 보완

Bundle canonical (mid-cycle): <after T3> → <after T4> (-XXX B 합산 T2~T4 = -YYY B)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: C2 — gemini-parse Validation + submitAnswer Double-close

**Files:**
- Modify: `src/utils/gemini-parse.ts:107`
- Modify: `src/ui/handlers/home.ts:974`
- Test: `tests/unit/gemini-parse.spec.ts` (신규 또는 기존)
- Test: `tests/unit/home.spec.ts` (신규 또는 기존)

- [ ] **Step 1: 기존 spec 위치 확인**

Run:
```bash
ls tests/unit/gemini-parse.spec.ts tests/unit/home.spec.ts 2>/dev/null
rg -n "validateInterestId" src/state/user.ts
```
Expected: spec 존재 여부 확인 + validateInterestId 시그니처 확인.

- [ ] **Step 2: 실패 spec 작성 (gemini-parse)**

`tests/unit/gemini-parse.spec.ts` (또는 기존 확장):

```ts
import { describe, it, expect } from 'vitest';
import { parseQuestionResponse } from '../../src/utils/gemini-parse';

describe('parseQuestionResponse — interestId validation (v3.40 C2)', () => {
  it('userInterests에 invalid id 섞여 있으면 first valid 반환', () => {
    const result = parseQuestionResponse('Q?', ['__invalid__', 'ai_ml', 'hr_system']);
    expect(result.targetInterestId).toBe('ai_ml');
  });

  it('userInterests 모두 invalid면 unknown', () => {
    const result = parseQuestionResponse('Q?', ['__invalid__', '__bad__']);
    expect(result.targetInterestId).toBe('unknown');
  });

  it('userInterests empty면 unknown', () => {
    const result = parseQuestionResponse('Q?', []);
    expect(result.targetInterestId).toBe('unknown');
  });

  it('first userInterests가 이미 valid면 그대로', () => {
    const result = parseQuestionResponse('Q?', ['ai_ml', 'hr_system']);
    expect(result.targetInterestId).toBe('ai_ml');
  });
});
```

- [ ] **Step 3: 실패 확인**

Run:
```bash
npm test -- gemini-parse.spec
```
Expected: FAIL (현재 `userInterests[0]` 반환이라 invalid 들어가면 fail).

- [ ] **Step 4: 구현 (gemini-parse.ts:107)**

```ts
// 변경 전 (line 107 근처):
// targetInterestId: userInterests[0] ?? 'unknown',

// 변경 후:
import { validateInterestId } from '../state/user';

// ...

targetInterestId: userInterests.find(id => validateInterestId(id) !== 'unknown') ?? 'unknown',
```

- [ ] **Step 5: 통과 확인**

Run:
```bash
npm test -- gemini-parse.spec
```
Expected: PASS (4/4).

- [ ] **Step 6: 실패 spec 작성 (submitAnswer dataset)**

`tests/unit/home.spec.ts` (또는 기존 확장):

```ts
import { describe, it, expect, vi } from 'vitest';
// import submitAnswer ...

describe('submitAnswer — dataset interestId double-close (v3.40 C2)', () => {
  it('dataset corrupt interestId면 unknown으로 저장', async () => {
    // setup: question DOM에 data-interest-id="__invalid__"
    // expectation: 저장된 answer.interestId === 'unknown'
  });

  it('dataset valid interestId면 그대로', async () => {
    // dataset = 'ai_ml' → answer.interestId === 'ai_ml'
  });

  it('cached question path에서도 validation 적용', async () => {
    // cached question이 invalid dataset 가져도 unknown
  });

  it('fallback path에서도 validation 적용', async () => {
    // fallback question도 동일
  });
});
```

- [ ] **Step 7: 실패 확인**

Run:
```bash
npm test -- home.spec
```
Expected: FAIL (현재 dataset 그대로 사용).

- [ ] **Step 8: 구현 (home.ts:974 submitAnswer)**

```ts
// 변경 전 (line 974 근처):
// const rawInterestId = questionEl?.dataset.interestId ?? 'unknown';
// makeAnswer({ ..., interestId: rawInterestId })

// 변경 후:
import { validateInterestId } from '../../state/user';

const rawInterestId = questionEl?.dataset.interestId ?? 'unknown';
const safeInterestId = validateInterestId(rawInterestId);
// ... makeAnswer({ ..., interestId: safeInterestId })
```

- [ ] **Step 9: 통과 확인**

Run:
```bash
npm test -- home.spec
```
Expected: PASS (4/4).

- [ ] **Step 10: 사이클 3종 검증**

Run:
```bash
npx tsc --noEmit
npm run lint
npm test
```
Expected: 0 errors. 전체 vitest PASS (+6~8 신규).

- [ ] **Step 11: Bundle 추가 task budget check (T5 단독)**

Run:
```bash
rm -rf dist && npm run build
JS=$(gzip -c dist/assets/index-*.js | wc -c | tr -d ' ')
CSS=$(gzip -c dist/assets/index-*.css | wc -c | tr -d ' ')
echo "After T5: Entry JS $JS / CSS $CSS / Canonical $((JS+CSS))"
```
Expected: T4 baseline 대비 +30~60 B.

- [ ] **Step 12: Commit**

```bash
git add src/utils/gemini-parse.ts src/ui/handlers/home.ts tests/unit/
git commit -m "feat(v3.40 T5 C2): gemini-parse + submitAnswer interestId validation 보강

- gemini-parse.ts: userInterests[0] → find(validateInterestId) ?? 'unknown'
- home.ts submitAnswer: dataset interestId double-close validateInterestId

v3.39 Codex P2-1 in-cycle 흡수. +6~8 신규 vitest PASS.
Bundle: +XX B canonical (T5 단독).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: C3 + L4 — bottom-nav 진단 + dispatchEvent 변환

**Files:**
- Modify: `src/styles/base.css` 또는 `src/styles/layout.css` (진단 결과 의존)
- Modify: `tests/smoke/archive-interest-filter.spec.ts` (dispatchEvent 9건)
- Modify: `tests/smoke/archive-insight-detail-nav.spec.ts` (dispatchEvent 5건 예상)
- Modify: `tests/smoke/archive-plant-detail-nav.spec.ts` (dispatchEvent 5건 예상)

- [ ] **Step 1: dispatchEvent 카운트 재실측**

Run:
```bash
for f in tests/smoke/archive-interest-filter.spec.ts tests/smoke/archive-insight-detail-nav.spec.ts tests/smoke/archive-plant-detail-nav.spec.ts; do
  count=$(rg -c "dispatchEvent\(['\"]click['\"]" "$f")
  echo "$count $f"
done
```
Expected: spec v2 명시 14건 또는 다른 카운트. 카운트 확정 후 진행.

- [ ] **Step 2: 진단 spec 신규 작성 (Playwright forensics)**

`tests/smoke/_diagnostic/bottom-nav-overlap.spec.ts` 신규 (1회용 진단):

```ts
import { test, expect } from '@playwright/test';

test.use({ serviceWorkers: 'block' });

test('bottom-nav fixed overlay 진단: card click 실패 root cause', async ({ page }) => {
  await page.goto('http://localhost:4173');

  // archive 탭 진입, 카드 1개 식별
  await page.locator('#bottomNav button[data-tab="archive"]').click();
  const card = page.locator('.archive-insight-card').first();
  await card.scrollIntoViewIfNeeded();

  // 1. boundingBox 측정
  const cardBox = await card.boundingBox();
  const navBox = await page.locator('.bottom-nav').boundingBox();
  console.log('Card box:', cardBox);
  console.log('Nav box:', navBox);
  console.log('Overlap (px):', (cardBox && navBox) ? Math.max(0, cardBox.y + cardBox.height - navBox.y) : 0);

  // 2. elementFromPoint 검사
  const centerEl = await page.evaluate((sel) => {
    const card = document.querySelector(sel);
    if (!card) return null;
    const rect = card.getBoundingClientRect();
    const el = document.elementFromPoint(rect.left + rect.width / 2, rect.top + rect.height / 2);
    return el?.tagName + '.' + (el?.className || '');
  }, '.archive-insight-card');
  console.log('Element at card center:', centerEl);

  // 3. click() 시도
  try {
    await card.click({ timeout: 2000 });
    console.log('click() PASS');
  } catch (err) {
    console.log('click() FAIL:', (err as Error).message);
  }

  // 4. computed style 확인
  const navStyle = await page.locator('.bottom-nav').evaluate((el) => {
    const s = getComputedStyle(el);
    return { zIndex: s.zIndex, pointerEvents: s.pointerEvents, position: s.position };
  });
  console.log('Nav style:', navStyle);

  // 5. card scroll-margin 확인
  const cardScrollMargin = await card.evaluate((el) => getComputedStyle(el).scrollMarginBottom);
  console.log('Card scroll-margin-bottom:', cardScrollMargin);

  expect(true).toBe(true); // 진단 spec — 정보 수집만
});
```

- [ ] **Step 3: 진단 실행 + 결과 분석**

Run:
```bash
npm run preview &
PREVIEW_PID=$!
sleep 3
npx playwright test tests/smoke/_diagnostic/bottom-nav-overlap.spec.ts --reporter=list
kill $PREVIEW_PID
```
Expected: console log로 root cause 추정 가능 (Case A/B/C/D 중 하나).

- [ ] **Step 4: Case별 fix 적용**

**Case A** (scroll-margin selector mismatch):
`src/styles/base.css:38~39` 수정:
```css
.archive-card, .archive-insight-card, .mission-card, .plant-card {
  scroll-margin-bottom: var(--content-bottom-pad);
}
```

**Case B** (z-index 충돌):
`src/styles/layout.css:43` 영역:
```css
.bottom-nav { z-index: 100; /* 명시 */ }
.archive-card { z-index: 1; /* 명시 */ }
```

**Case C** (pointer-events overlay):
`src/styles/layout.css`:
```css
.bottom-nav { pointer-events: auto; }
.bottom-nav-overlay { pointer-events: none; }
```

**Case D** (padding-bottom 누락):
`src/styles/layout.css:73` 확인 — body padding-bottom이 실제 적용되는지. 미적용 시 명시.

진단 결과에 따라 1개 또는 복합 적용.

- [ ] **Step 5: 진단 spec 제거 + dispatchEvent 변환**

진단 spec (`tests/smoke/_diagnostic/`) 삭제. 14건(또는 재실측 카운트) `.dispatchEvent('click')` → `.click()` 일반화. workaround 주석 제거.

예시:
```ts
// 변경 전:
// await insightCard.dispatchEvent('click');

// 변경 후:
await insightCard.click();
```

- [ ] **Step 6: 회귀 spec 추가 (production click 보장)**

`tests/smoke/archive-interest-filter.spec.ts` 또는 신규 spec:

```ts
test('bottom-nav button 실제 click이 tab 전환 (dispatchEvent 우회 없이)', async ({ page }) => {
  // ... seed
  const archiveBtn = page.locator('#bottomNav button[data-tab="archive"]');
  await archiveBtn.click();  // .dispatchEvent 아닌 .click()
  await expect(page.locator('#archiveTab.active')).toBeVisible();
});
```

- [ ] **Step 7: Smoke N=3 cold-start fresh fork**

Run:
```bash
for i in 1 2 3; do
  rm -rf node_modules/.vite-temp
  npm run test:smoke -- --grep "archive|bottom-nav" --reporter=line
done
```
Expected: 3/3 PASS. 1건 flake 시 진단 단계 추가.

- [ ] **Step 8: Build + bundle 측정**

Run:
```bash
rm -rf dist && npm run build
JS=$(gzip -c dist/assets/index-*.js | wc -c | tr -d ' ')
CSS=$(gzip -c dist/assets/index-*.css | wc -c | tr -d ' ')
echo "After T6: Entry JS $JS / CSS $CSS / Canonical $((JS+CSS))"
```
Expected: T5 baseline +20~40 B (CSS 추가 시).

- [ ] **Step 9: 사이클 3종 검증**

Run:
```bash
npx tsc --noEmit
npm run lint
npm test
```
Expected: 0 errors. 전체 PASS.

- [ ] **Step 10: Commit**

```bash
git add src/styles/ tests/smoke/
git commit -m "fix(v3.40 T6 C3+L4): bottom-nav 진단 + dispatchEvent N건 → click() 변환

진단 결과: Case <X> — <root cause 한 줄>
Fix: <CSS 변경 요약>

- dispatchEvent('click') N건 → .click() 일반화
- workaround 주석 제거
- 회귀 spec 1건 추가 (production click 경로 보장)

v3.39 Codex P2-2 in-cycle 흡수 + Codex 사전 P1-1 진단 절차 보강.
Smoke N=3 cold-start fresh fork PASS.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 7: C5 — One-time `maybeShowByInterestNotice()` 공유 Helper

**Files:**
- Create: `src/ui/handlers/byInterestNotice.ts`
- Modify: `src/ui/tabs/stats.ts` (renderStats 진입점)
- Modify: `src/ui/handlers/archive-listeners.ts` (archive 진입 trigger)
- Test: `tests/unit/byInterestNotice.spec.ts` 신규

- [ ] **Step 1: showToast API 시그니처 확인**

Run:
```bash
head -30 src/utils/toast.ts
rg -n "export.*function|export.*const" src/utils/toast.ts
```
Expected: `showToast(message: string, options?: { duration?: number; dismissible?: boolean })` 또는 유사 시그니처.

- [ ] **Step 2: 실패 spec 작성**

`tests/unit/byInterestNotice.spec.ts` 신규:

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { maybeShowByInterestNotice } from '../../src/ui/handlers/byInterestNotice';

vi.mock('../../src/utils/toast', () => ({
  showToast: vi.fn(),
}));

import { showToast } from '../../src/utils/toast';

describe('maybeShowByInterestNotice — v3.40 C5', () => {
  beforeEach(() => {
    localStorage.clear();
    vi.clearAllMocks();
  });

  it('flag 미존재 시 toast show + flag set', () => {
    maybeShowByInterestNotice();
    expect(showToast).toHaveBeenCalledOnce();
    expect(localStorage.getItem('dg.toast.byInterestNotice')).toBe('1');
  });

  it('flag === "1" 존재 시 toast skip', () => {
    localStorage.setItem('dg.toast.byInterestNotice', '1');
    maybeShowByInterestNotice();
    expect(showToast).not.toHaveBeenCalled();
  });

  it('flag 비정상 값 ("0", "false") 시 strict check로 show', () => {
    localStorage.setItem('dg.toast.byInterestNotice', '0');
    maybeShowByInterestNotice();
    expect(showToast).toHaveBeenCalledOnce();
  });

  it('show 후 두 번째 호출은 skip (idempotent)', () => {
    maybeShowByInterestNotice();
    maybeShowByInterestNotice();
    expect(showToast).toHaveBeenCalledOnce();
  });
});
```

- [ ] **Step 3: 실패 확인**

Run:
```bash
npm test -- byInterestNotice.spec
```
Expected: FAIL (module not found).

- [ ] **Step 4: 구현**

`src/ui/handlers/byInterestNotice.ts` 신규:

```ts
import { showToast } from '../../utils/toast';

const STORAGE_KEY = 'dg.toast.byInterestNotice';

export function maybeShowByInterestNotice(): void {
  if (localStorage.getItem(STORAGE_KEY) === '1') return;

  showToast('📊 분야별 통계가 정확해졌어요\n이제 관심분야 단위로 집계됩니다', {
    duration: 4000,
    dismissible: true,
  });

  localStorage.setItem(STORAGE_KEY, '1');
}
```

(showToast 실제 시그니처에 맞춰 options 조정)

- [ ] **Step 5: 통과 확인**

Run:
```bash
npm test -- byInterestNotice.spec
```
Expected: PASS (4/4).

- [ ] **Step 6: stats 진입 wiring**

`src/ui/tabs/stats.ts` `renderStats` 함수 진입점:

```ts
import { maybeShowByInterestNotice } from '../handlers/byInterestNotice';

export async function renderStats(): Promise<void> {
  maybeShowByInterestNotice();
  // ... 기존 로직
}
```

- [ ] **Step 7: archive 진입 wiring**

`src/ui/handlers/archive-listeners.ts` 또는 archive tab switch trigger:

```ts
import { maybeShowByInterestNotice } from './byInterestNotice';

// archive tab 전환 listener 내부:
() => {
  maybeShowByInterestNotice();
  // ... 기존 로직
}
```

- [ ] **Step 8: Integration smoke (archive + stats 진입 시 toast)**

`tests/smoke/v3.40-byinterest-notice.spec.ts` 신규:

```ts
import { test, expect } from '@playwright/test';

test.use({ serviceWorkers: 'block' });

test('stats 첫 진입 시 byInterest toast 표시', async ({ page }) => {
  await page.goto('http://localhost:4173');
  // ... onboarding seed skip ...
  await page.locator('#bottomNav button[data-tab="stats"]').click();
  await expect(page.locator('.toast').filter({ hasText: '분야별 통계' })).toBeVisible();
});

test('archive 진입 시도 toast 표시 (다른 시점 첫 진입)', async ({ page }) => {
  // localStorage reset 후 archive 먼저 진입
  await page.goto('http://localhost:4173');
  await page.evaluate(() => localStorage.removeItem('dg.toast.byInterestNotice'));
  await page.reload();
  await page.locator('#bottomNav button[data-tab="archive"]').click();
  await expect(page.locator('.toast').filter({ hasText: '분야별 통계' })).toBeVisible();
});

test('두 번째 진입은 toast 미표시', async ({ page }) => {
  // localStorage flag set 후 진입
  await page.goto('http://localhost:4173');
  await page.evaluate(() => localStorage.setItem('dg.toast.byInterestNotice', '1'));
  await page.reload();
  await page.locator('#bottomNav button[data-tab="stats"]').click();
  await expect(page.locator('.toast').filter({ hasText: '분야별 통계' })).not.toBeVisible();
});
```

- [ ] **Step 9: Smoke 실행**

Run:
```bash
npm run test:smoke -- --grep "byinterest|stats|archive" --reporter=line
```
Expected: PASS.

- [ ] **Step 10: Build + bundle +200 B 한도 검증 (T5~T7 합산)**

Run:
```bash
rm -rf dist && npm run build
JS=$(gzip -c dist/assets/index-*.js | wc -c | tr -d ' ')
CSS=$(gzip -c dist/assets/index-*.css | wc -c | tr -d ' ')
TOTAL=$((JS+CSS))
DELTA=$((TOTAL - <T4_BASELINE>))  # T4 종료 값으로 치환
echo "T5+T6+T7 합산 Δ (T4 대비): $DELTA B"
```
Expected: Δ ≤ +200 B. 초과 시 T8에서 helper trim 흡수 또는 C5 copy 단축.

- [ ] **Step 11: 사이클 3종 검증**

Run:
```bash
npx tsc --noEmit
npm run lint
npm test
```
Expected: 0 errors. 전체 PASS (+4~6 신규 vitest).

- [ ] **Step 12: Commit**

```bash
git add src/ui/handlers/byInterestNotice.ts src/ui/tabs/stats.ts src/ui/handlers/archive-listeners.ts tests/
git commit -m "feat(v3.40 T7 C5): one-time byInterestNotice toast 공유 helper

신규 maybeShowByInterestNotice() 공유 helper:
- stats 진입 + archive 진입 둘 다 trigger
- localStorage 'dg.toast.byInterestNotice' === '1' strict check
- copy: '분야별 통계가 정확해졌어요...'

v3.39 byInterest 의미 정정 안내 (Codex 사전 P1-2 흡수, archive trigger 확장).
+4~6 신규 vitest + 3 smoke.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 8: C4 — archive-nav-chip 공용 Helper (Dynamic Import 유지)

**Files:**
- Create: `src/ui/handlers/archive-nav.ts`
- Modify: `src/ui/modals/plant-detail.ts` (L115 정의 이동, L83 import path)
- Modify: `src/ui/modals/insight-detail.ts` (L12 import path)
- Modify: `src/ui/handlers/archive.ts` (L689 reset 시맨틱 helper 호출)

- [ ] **Step 1: 기존 navigateToInterestArchive 시그니처 + caller 확인**

Run:
```bash
rg -n "navigateToInterestArchive" src --type ts
sed -n '110,140p' src/ui/modals/plant-detail.ts
```
Expected: 정의 1곳 + caller 2~3곳.

- [ ] **Step 2: 신규 archive-nav.ts 작성 (dynamic import 유지)**

`src/ui/handlers/archive-nav.ts` 신규:

```ts
/**
 * v3.40 T8 (C4): archive-nav-chip 공용 helper.
 *
 * navigateToInterestArchive: plant-detail / insight-detail / archive.ts 3 callsite 공유.
 * 내부 dynamic import 유지 — archive.ts를 static 끌어오면 plant-detail/insight-detail
 * chunk가 archive와 merge되어 chunk 분리 깨짐 (v3.40 Codex 사전 P1-3).
 *
 * @internal — caller는 위 3 위치로만 노출.
 */
export async function navigateToInterestArchive(interestId: string): Promise<void> {
  const { resetArchiveFilters, switchToArchive, setInterestFilter } = await import('./archive');
  resetArchiveFilters();
  await switchToArchive();
  setInterestFilter(interestId);
}
```

(archive.ts exports 시그니처에 맞춰 import 멤버 조정. 실제 멤버는 T8 시작 시 grep으로 확정.)

- [ ] **Step 3: plant-detail.ts에서 정의 제거 + import 추가**

`src/ui/modals/plant-detail.ts`:

```ts
// 변경 전 (L115~):
// export async function navigateToInterestArchive(interestId: string): Promise<void> { ... }

// 변경 후:
import { navigateToInterestArchive } from '../handlers/archive-nav';

// L83 caller는 그대로 navigateToInterestArchive(...) 호출
```

- [ ] **Step 4: insight-detail.ts import path 갱신**

`src/ui/modals/insight-detail.ts:12`:

```ts
// 변경 전:
// import { navigateToInterestArchive } from './plant-detail';

// 변경 후:
import { navigateToInterestArchive } from '../handlers/archive-nav';
```

- [ ] **Step 5: archive.ts L689 reset 시맨틱 통합 (선택적)**

L689 영역이 동일 reset 시맨틱이면 동일 함수 호출로 단일화. 시맨틱 다르면 기존 유지.

`src/ui/handlers/archive.ts:689` 영역 확인 후 결정.

- [ ] **Step 6: 검증**

Run:
```bash
npx tsc --noEmit
npm run lint
npm test
```
Expected: 0 errors. 전체 vitest PASS (refactor only).

- [ ] **Step 7: Build + chunk 분리 보존 확인**

Run:
```bash
rm -rf dist && npm run build
ls -la dist/assets/plant-detail-*.js dist/assets/insight-detail-*.js dist/assets/archive-nav-*.js 2>/dev/null
JS=$(gzip -c dist/assets/index-*.js | wc -c | tr -d ' ')
CSS=$(gzip -c dist/assets/index-*.css | wc -c | tr -d ' ')
echo "After T8: Entry JS $JS / CSS $CSS / Canonical $((JS+CSS))"
```
Expected: plant-detail / insight-detail chunk 그대로 존재. archive-nav 별도 chunk 또는 archive chunk 일부. canonical -50~-100 B.

- [ ] **Step 8: Smoke 회귀 확인**

Run:
```bash
npm run test:smoke -- --grep "plant-detail|insight-detail|archive-nav" --reporter=line
```
Expected: PASS.

- [ ] **Step 9: Commit**

```bash
git add src/ui/
git commit -m "refactor(v3.40 T8 C4): archive-nav 공용 helper 추출 (dynamic import 유지)

신규 src/ui/handlers/archive-nav.ts:
- navigateToInterestArchive: plant-detail / insight-detail / archive.ts 3 callsite 공유
- 내부 dynamic import 유지 (chunk 분리 보존, Codex P1-3 흡수)

v3.39 §9.6 C4 + Codex 사전 P1-3 in-cycle 흡수.
Bundle: -XX B canonical (helper 중복 제거).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 9: C8 — plant-detail Seed Fixture (4 Smoke Spec 공유)

**Files:**
- Create: `tests/smoke/_helpers/plant-detail-seed.ts`
- Modify: `tests/smoke/archive-insight-detail-nav.spec.ts`
- Modify: `tests/smoke/archive-plant-detail-nav.spec.ts`
- Modify: `tests/smoke/plant-detail-modal.spec.ts`
- Modify: `tests/smoke/archive-interest-filter.spec.ts`

- [ ] **Step 1: 4 spec 현재 seed boilerplate diff 분석**

Run:
```bash
for f in archive-insight-detail-nav.spec.ts archive-plant-detail-nav.spec.ts plant-detail-modal.spec.ts archive-interest-filter.spec.ts; do
  echo "=== tests/smoke/$f ==="
  grep -A 20 "addInitScript\|localStorage.setItem.*dg\." "tests/smoke/$f" | head -30
  echo ""
done
```
Expected: 4 spec의 seed 코드 공통점/차이 식별.

- [ ] **Step 2: 공통 seed 추출 + opts 시그니처 결정**

`tests/smoke/_helpers/plant-detail-seed.ts` 신규:

```ts
import { Page } from '@playwright/test';

export interface PlantDetailSeedOpts {
  plantIds?: string[];          // default: ['plant_ai_basic']
  interestId?: string;          // default: 'ai_ml'
  stage?: number;                // default: 3
  schemaVersion?: number;        // default: 9
}

export async function seedPlantDetail(page: Page, opts: PlantDetailSeedOpts = {}): Promise<void> {
  const plantIds = opts.plantIds ?? ['plant_ai_basic'];
  const interestId = opts.interestId ?? 'ai_ml';
  const stage = opts.stage ?? 3;
  const schemaVersion = opts.schemaVersion ?? 9;

  await page.addInitScript(({ plantIds, interestId, stage, schemaVersion }) => {
    const user = {
      schemaVersion,
      interests: [interestId],
      garden: plantIds.map((id) => ({
        plantId: id,
        interestId,
        stage,
        plantedAt: '2026-05-01T00:00:00.000Z',
      })),
      // ... 기타 필수 필드 (4 spec common)
    };
    localStorage.setItem('dg.user', JSON.stringify(user));
  }, { plantIds, interestId, stage, schemaVersion });
}
```

(실제 user shape은 4 spec 분석 결과 반영)

- [ ] **Step 3: 4 spec에서 boilerplate → helper 호출 1줄로 교체**

각 spec 상단:

```ts
// 변경 전 (boilerplate 10~15줄):
// await page.addInitScript(() => { ... localStorage seed ... });

// 변경 후:
import { seedPlantDetail } from './_helpers/plant-detail-seed';
// ...
await seedPlantDetail(page);  // 또는 { plantIds: [...], ... } 명시
```

각 spec 별 opts 차이는 인자로 명시 (opts 시그니처가 4 spec diff 커버).

- [ ] **Step 4: Smoke 4 spec 통과 확인**

Run:
```bash
npm run test:smoke -- --grep "archive-insight-detail|archive-plant-detail|plant-detail-modal|archive-interest-filter" --reporter=line
```
Expected: 4 spec 모두 PASS (refactor only — 동일 결과).

- [ ] **Step 5: N=3 cold-start fresh fork**

Run:
```bash
for i in 1 2 3; do
  rm -rf node_modules/.vite-temp
  npm run test:smoke -- --grep "plant-detail|archive-insight-detail" --reporter=line
done
```
Expected: 3/3 PASS.

- [ ] **Step 6: 사이클 3종 검증**

Run:
```bash
npx tsc --noEmit
npm run lint
```
Expected: 0 errors.

- [ ] **Step 7: Commit**

```bash
git add tests/smoke/
git commit -m "refactor(v3.40 T9 C8): plant-detail seed fixture 추출 (4 spec 공유)

신규 tests/smoke/_helpers/plant-detail-seed.ts:
- seedPlantDetail(page, opts) — 4 smoke spec 공유 fixture
- opts: { plantIds, interestId, stage, schemaVersion }

4 spec boilerplate 10~15줄 → helper 호출 1줄로 교체.
v3.39 §9.6 C8 in-cycle 흡수.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 10: C11 + C13 + C6 Carry 정정 (Smoke Spec Doc/Name + v3.38 §9.6 정정)

**Files:**
- Modify: `tests/smoke/archive-interest-filter.spec.ts`
- Modify: `docs/superpowers/specs/2026-05-18-v3.38-carry-cleanup-design.md` (§9.6 C3 정정)

- [ ] **Step 1: archive-interest-filter.spec.ts inline algorithm 주석 식별**

Run:
```bash
rg -n "interest-filter-algorithm|exact filter algorithm|inline" tests/smoke/archive-interest-filter.spec.ts
```
Expected: 알고리즘 설명 주석 위치 확인.

- [ ] **Step 2: 주석 제거 + @see 링크로 교체 (C11)**

```ts
// 변경 전 (대형 주석 5~10줄):
// /**
//  * archive interest filter algorithm:
//  * 1. exact match by interestId
//  * 2. legacy 'unknown' fallback ...
//  */

// 변경 후 (1줄):
// @see docs/superpowers/specs/2026-05-19-v3.39-answer-briefing-interestid-design.md §3 archive read-side exact filter
```

- [ ] **Step 3: spec name / assertion mismatch 정정 (C13)**

```ts
// 변경 전 (예시):
// test('exact filter', async ({ page }) => {
//   // ... assertion uses 'token filter' wording
//   await expect(...).toContain('token filter results');
// });

// 변경 후:
// test('exact + legacy fallback filter', async ({ page }) => {
//   await expect(...).toContain('exact filter results');
// });
```

- [ ] **Step 4: @internal JSDoc 추가 (C13)**

```ts
/**
 * @internal v3.39 T5 — archive entity-interest exact filter spec.
 * @see docs/superpowers/specs/2026-05-19-v3.39-...md §3
 */
test('exact + legacy fallback filter', ...);
```

- [ ] **Step 5: v3.38 §9.6 C3 정정 (C6 carry 정정)**

`docs/superpowers/specs/2026-05-18-v3.38-carry-cleanup-design.md` §9.6 C3 항목:

```markdown
| C3 | NFD 자모 분리 검출 rule | ... | Low (P3) |
```

뒤에 한 줄 추가:

```markdown
> **v3.40 T10 정정**: 본 항목은 v3.38 T1b에서 이미 `.eslint-rules/no-nfd-korean-literal.js`로 graduate 완료 (음절 블록 + 자모 영역 U+1100~U+11FF + NFC normalize 검증). carry 잘못 기록.
```

- [ ] **Step 6: 검증**

Run:
```bash
npm run lint
npm run test:smoke -- --grep "archive-interest-filter" --reporter=line
```
Expected: 0 errors. spec 통과 (변경 0, 텍스트만).

- [ ] **Step 7: Commit**

```bash
git add tests/smoke/archive-interest-filter.spec.ts docs/superpowers/specs/2026-05-18-v3.38-carry-cleanup-design.md
git commit -m "docs(v3.40 T10): smoke spec doc/name 정리 + v3.38 §9.6 C3 carry 정정

- C11: archive-interest-filter.spec.ts inline algorithm 주석 → spec doc @see 링크
- C13: spec name/assertion mismatch 정정 + @internal JSDoc 추가
- C6 carry 정정: v3.38 §9.6 C3 \"NFD rule\"이 이미 v3.38 T1b에서 graduate 완료 명시

v3.39 §9.6 C11/C13 + v3.38 §9.6 C3 in-cycle.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 11: C12 — `waitForTimeout` → `expect.poll` (Debounce 1s / Missions 3s)

**Files:**
- Modify: `tests/smoke/archive-interest-filter.spec.ts:197`
- Modify: `tests/smoke/archive-plant-detail-nav.spec.ts:61, 113`
- Modify: `tests/smoke/archive-unified.spec.ts:62`
- Modify: `tests/smoke/v3.13-missions.spec.ts:66, 125`
- Modify: 가능 시 production `src/ui/missions-section.ts` (aria-live 추가)

- [ ] **Step 1: 6건 변환 — debounce 200ms (4건)**

archive-interest-filter / archive-plant-detail-nav (2) / archive-unified:

```ts
// 변경 전:
// await page.waitForTimeout(300); // debounce 200ms + margin

// 변경 후:
await expect.poll(async () => await page.locator('.archive-card').count(), {
  timeout: 1_000,
  intervals: [100, 200, 200],
}).toBe(expectedCount); // expectedCount는 spec context에 맞게
```

(실제 변환은 각 spec context에 맞춰 — count 외 textContent / visibility 등 사용)

- [ ] **Step 2: 6건 변환 — missions submit sweep (2건)**

v3.13-missions:66, 125.

먼저 production code에 `aria-live` 추가:

`src/ui/missions-section.ts` 또는 mission state element:

```ts
// 변경 전:
// const cls = m.completed ? 'mission-card mission-card--done' : 'mission-card';
// return `<li class="${cls}" ...>`;

// 변경 후 (aria-live region 추가):
return `<li class="${cls}" aria-live="polite" ...>`;
```

(이미 aria-label은 있으니 aria-live만 추가, +5~10 B)

spec 변환:

```ts
// 변경 전:
// await page.waitForTimeout(300); // submit 후 sweep + state save 대기

// 변경 후:
await expect.poll(
  async () => await page.locator('.mission-card[aria-live="polite"]').textContent(),
  { timeout: 3_000, intervals: [200, 300, 500] }
).toContain('완수');  // 또는 spec context의 expected text
```

- [ ] **Step 3: Smoke 변환된 6건 통과 확인**

Run:
```bash
npm run test:smoke -- --grep "archive-interest-filter|archive-plant-detail|archive-unified|v3.13-missions" --reporter=line
```
Expected: 6 spec PASS.

- [ ] **Step 4: N=3 cold-start fresh fork**

Run:
```bash
for i in 1 2 3; do
  rm -rf node_modules/.vite-temp
  npm run test:smoke -- --grep "archive-interest-filter|archive-plant-detail|archive-unified|v3.13-missions" --reporter=line
done
```
Expected: 3/3 PASS — flake 0.

- [ ] **Step 5: Bundle 측정 (aria-live 추가 영향)**

Run:
```bash
rm -rf dist && npm run build
JS=$(gzip -c dist/assets/index-*.js | wc -c | tr -d ' ')
CSS=$(gzip -c dist/assets/index-*.css | wc -c | tr -d ' ')
echo "After T11: Entry JS $JS / CSS $CSS / Canonical $((JS+CSS))"
```
Expected: +5~10 B (aria-live attribute production code 변경).

- [ ] **Step 6: 사이클 3종 검증**

Run:
```bash
npx tsc --noEmit
npm run lint
npm test
```
Expected: 0 errors.

- [ ] **Step 7: Commit**

```bash
git add tests/smoke/ src/ui/missions-section.ts
git commit -m "test(v3.40 T11 C12): waitForTimeout 6건 → expect.poll (debounce 1s / missions 3s)

- 4건 (archive-interest-filter / archive-plant-detail-nav x2 / archive-unified): waitForTimeout(300) → expect.poll 1_000ms debounce 대기
- 2건 (v3.13-missions:66, 125): aria-live='polite' + expect.poll 3_000ms (cold-start + tab chunk load)

v3.39 §9.6 C12 + Codex 사전 P1-4 흡수 (missions 1_000 → 3_000ms 정정).
N=3 cold-start fresh fork PASS.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 12: C9 — Entity-summary-chip 색상 토큰화

**Files:**
- Modify: `src/styles/tokens.css`
- Modify: `src/styles/pages/archive.css` (entity-summary-chip 색상)

- [ ] **Step 1: 현재 chip 색상 hard-coded 위치 확인**

Run:
```bash
sed -n '285,320p' src/styles/pages/archive.css
rg -n "entity-summary-chip" src/ui src/styles --type ts --type css
```
Expected: 정확한 hard-coded 색상 값 + selector 패턴 (`[data-entity="answer"]` 등) 확인.

- [ ] **Step 2: tokens.css 추가**

`src/styles/tokens.css` `:root` 블록:

```css
:root {
  /* ... 기존 토큰 ... */
  /* v3.40 T12: entity-summary-chip 색상 토큰 */
  --chip-answer-color: var(--accent-blue, #3b82f6);
  --chip-briefing-color: var(--accent-green, #10b981);
  --chip-insight-color: var(--accent-purple, #8b5cf6);
}
```

- [ ] **Step 3: archive.css 색상 var() 교체**

`src/styles/pages/archive.css`:

```css
.entity-summary-chip[data-entity="answer"] {
  color: var(--chip-answer-color);
}
.entity-summary-chip[data-entity="briefing"] {
  color: var(--chip-briefing-color);
}
.entity-summary-chip[data-entity="insight"] {
  color: var(--chip-insight-color);
}
```

(기존 hard-coded 값이 위와 다르면 토큰 default 값 조정 또는 색상 값 매칭)

- [ ] **Step 4: Build + bundle 측정**

Run:
```bash
rm -rf dist && npm run build
JS=$(gzip -c dist/assets/index-*.js | wc -c | tr -d ' ')
CSS=$(gzip -c dist/assets/index-*.css | wc -c | tr -d ' ')
echo "After T12: Entry JS $JS / CSS $CSS / Canonical $((JS+CSS))"
```
Expected: -10~+10 B (token 재사용 / 신규 token).

- [ ] **Step 5: 사용자 manual 시각 확인 요청**

Run:
```bash
npm run preview &
PREVIEW_PID=$!
sleep 3
```

사용자에게 메시지:
> v3.40 T12: archive 영역 entity chip 3종 색상 시각 검증 부탁드립니다.
> Preview: http://localhost:4173
> 확인 사항: archive 탭 진입 → entity chip (Answer/Briefing/Insight) 색상이 v3.39 production과 일치하는지.

사용자 확인 후 (`kill $PREVIEW_PID`).

- [ ] **Step 6: 사이클 3종 검증**

Run:
```bash
npx tsc --noEmit
npm run lint
npm test
```
Expected: 0 errors.

- [ ] **Step 7: Commit**

```bash
git add src/styles/
git commit -m "style(v3.40 T12 C9): entity-summary-chip 색상 토큰화

신규 tokens.css:
- --chip-answer-color
- --chip-briefing-color
- --chip-insight-color

archive.css entity-summary-chip[data-entity] 색상 hard-coded → var() 교체.
v3.39 §9.6 C9 in-cycle 흡수.

사용자 manual 시각 확인 통과.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 13: Lesson Graduate L1/L2/L3/L5/L6 (Doc-only)

**Files:**
- Modify: `.claude/COMMON_MISTAKES.md` (§12 신규)
- Modify: `CLAUDE.md` (4 섹션 갱신)

- [ ] **Step 1: COMMON_MISTAKES.md §12 추가**

`.claude/COMMON_MISTAKES.md` 끝에 §12 섹션 추가:

```markdown
## 12. interest id ≠ keyword 토큰 (matchesInterest helper, 2026-05-20, v3.40 graduate)

**Symptom**: `matchKeyword(text, interestId)` 사용 시 본문이 `'AI/ML'`/`'AI'`/`'OpenAI'`인데 `'ai_ml'` literal 없어 결과 hide. v3.39 T8 P1-1 (interest nav 토큰 검색 과도 제한) + 13사이클 누적 Codex 사전 catch.

**Root cause**: interest id (`'ai_ml'`, `'hr_system'`)는 `INTERESTS.id` slug. keyword token 아님.

**Fix/Prevention**:
- **`matchesInterest(text: string, id: string): boolean`** (`src/utils/interestKeywords.ts:75`) — interest id를 keyword 집합으로 변환 후 매칭.
- **`entityMatchesInterest(e: ArchiveEntity, id: string | null): boolean`** (`src/ui/handlers/archive.ts:198`) — entity-level wrapper. `interestId === id` exact + legacy 'unknown' fallback keyword 일관 적용.
- archive/insight/stats entity-interest 매칭은 위 2개 helper만 사용.

**관련 파일**: `src/utils/interestKeywords.ts`, `src/ui/handlers/archive.ts`, `src/state/user.ts`

---

**Last Updated**: 2026-05-20
```

(맨 끝 "Last Updated" 갱신)

- [ ] **Step 2: CLAUDE.md "Cycle Workflow" 섹션 보강 (L2 + L3 + L3 inline checklist)**

`CLAUDE.md` "Cycle Workflow" 섹션 적절한 위치에 추가:

```markdown
### Spec reviewer checklist (v3.40 graduate, v3.39 T6 사례)

새 predicate/helper/event 정의 시:
- **predicate-defined vs wired**: caller 전수 grep 후 spec §wiring sites에 명시 (v3.39 T6 wiring 미적용 사례)
- **mutation paths 전수 grep** (v3.30 lesson #9 confirm)
- **test mock path stale 여부** (v3.30 lesson #11 confirm)

### Schema bump 시 chain superset 필수 spot (v3.40 graduate, 13사이클 ROI)

- migration chain (V2→V3→...→VN) — 모든 prior version에 N으로 가는 경로
- `src/state/user.ts` load (3 spot: hydrate / persist / shape guard) — hard-coded version path 포함
- 신규 entity 추가 시 entity-별 storage migration도 동일

### Codex 사전 review default checklist (v3.40 graduate, prompt 신규 파일 미작성, CLAUDE.md inline)

1. Schema chain superset coverage (위 3 spot)
2. interest id ≠ keyword 토큰 (matchesInterest helper 사용 여부)
3. predicate-defined vs wired (caller grep + wiring sites)
4. mutation paths 전수 grep
5. test mock path stale 여부

위 5건은 Codex 사전 review prompt에 명시 불요 — default checklist로 자동 검토.
```

- [ ] **Step 3: CLAUDE.md "subagent-driven-development" 섹션 (L5)**

`CLAUDE.md` 적절한 위치에 추가:

```markdown
### Subagent controller fallback 정책 (v3.40 graduate, v3.39 T2/T6 사례)

subagent dispatch 후 controller 의무 검증 3단계:
1. **commit 정합**: `git log --oneline -5`로 expected commit이 main에 반영됐는지 확인
2. **영향 범위 grep 재확인**: subagent가 보고한 영향 범위 외 누락 여부 (예: fixture sweep 일부 미완)
3. **차분 vitest/smoke**: 영향 spec 단위 실행 → PASS 확인

미완 발견 시: controller가 직접 마무리 + retro §lessons에 사례 명시.
```

- [ ] **Step 4: CLAUDE.md "code-reviewer" 섹션 (L6)**

`CLAUDE.md` 적절한 위치에 추가:

```markdown
### code-reviewer M-rating nit 격상 검토 의무 (v3.40 graduate, v3.39 T8 사례)

code-reviewer agent의 M-rating nit이 다음에 해당하면 P1 격상 검토:
- **사용자 가시 동작 변경** (UX nit)
- **data invariant 영향** (silent corruption 가능)
- **smoke spec workaround 패턴** (production 미해결, 예: dispatchEvent 우회)

격하 사유는 retro §lessons에 명시. 격상 후에는 in-cycle fix 의무.
```

- [ ] **Step 5: doc 정합 grep 검증**

Run:
```bash
rg -n "matchesInterest|chain superset|subagent fallback|M-rating nit|predicate-defined" .claude/ CLAUDE.md
```
Expected: 5 lesson 모두 graduate 위치에 명시.

- [ ] **Step 6: 충돌 검증 (기존 lesson과 모순 없는지)**

Run:
```bash
grep -n "interest id\|interest matching\|matchesInterest" .claude/COMMON_MISTAKES.md | head -10
```
Expected: §12 신규 외 중복 없음.

- [ ] **Step 7: Commit**

```bash
git add .claude/COMMON_MISTAKES.md CLAUDE.md
git commit -m "docs(v3.40 T13): lesson graduate 5건 (L1 COMMON_MISTAKES §12 + L2/L3/L5/L6 CLAUDE.md)

- L1 (12사이클 ROI): COMMON_MISTAKES.md §12 \"interest id ≠ keyword 토큰\" 신규
- L2 (v3.39 T6): CLAUDE.md spec reviewer checklist (predicate-defined vs wired)
- L3 (13사이클 ROI): CLAUDE.md Cycle Workflow schema chain superset 3 spot + Codex default checklist inline
- L5 (v3.39 T2/T6): CLAUDE.md subagent controller fallback 의무 검증 3단계
- L6 (v3.39 T8): CLAUDE.md code-reviewer M-rating nit 격상 검토 의무

Codex 사전 review prompt 신규 파일 미작성 — CLAUDE.md inline default checklist로 graduate (Codex P1-5 흡수).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 14: 사이클 끝 3종 검증 + Final Bundle Measurement (G1 Net Check)

**Files:**
- Modify: `docs/superpowers/specs/2026-05-20-v3.40-trim-carry-cleanup-design.md` (§9.4 final)

- [ ] **Step 1: 3종 검증 일괄 실행**

Run:
```bash
npx tsc --noEmit
npm run lint
npm test
```
Expected: 0 errors. 전체 vitest PASS (양쪽 TZ — vitest config에서 KST 강제).

- [ ] **Step 2: Smoke serialized + cold-start fresh fork**

Run:
```bash
rm -rf node_modules/.vite-temp dist
npm run test:smoke
```
Expected: ≥ 109 spec PASS.

- [ ] **Step 3: N=3 cold-start fresh fork**

Run:
```bash
for i in 1 2 3; do
  rm -rf node_modules/.vite-temp
  npm run test:smoke -- --reporter=line | tail -5
done
```
Expected: 3/3 PASS — flake 0.

- [ ] **Step 4: TZ=NY 양쪽 머신 vitest 확인**

Run:
```bash
TZ=America/New_York npm test 2>&1 | tail -3
```
Expected: PASS (양쪽 TZ).

- [ ] **Step 5: Final bundle measurement (G1 net check)**

Run:
```bash
rm -rf dist && npm run build
JS=$(gzip -c dist/assets/index-*.js | wc -c | tr -d ' ')
CSS=$(gzip -c dist/assets/index-*.css | wc -c | tr -d ' ')
ARCH=$(gzip -c dist/assets/archive-*.js | wc -c | tr -d ' ')
TOTAL=$((JS+CSS))
DELTA=$((TOTAL - 30204))
ARCH_DELTA=$((ARCH - 6096))
echo "Final: Entry JS $JS / CSS $CSS / Canonical $TOTAL (Δ $DELTA)"
echo "Archive chunk $ARCH (Δ $ARCH_DELTA)"
```
Expected:
- Canonical ≤ 29004 B (Δ ≤ -1200 B).
- Archive ≤ 5896 B (Δ ≤ -200 B).

Hard fail 시: G1 P0 stop → 사용자에게 옵션 제시 (target 완화 / 추가 trim).

- [ ] **Step 6: §9.4 최종 측정 기록**

`docs/superpowers/specs/2026-05-20-v3.40-trim-carry-cleanup-design.md` §9.4 갱신:

```markdown
### 9.4 Bundle 측정 (v3.40 final)

| 항목 | baseline | T1 | T4 (mid) | T7 (+ task) | T14 (final) | Δ (baseline → final) |
|---|---|---|---|---|---|---|
| Entry JS (gzip) | 19706 | <T1> | <T4> | <T7> | <T14> | <Δ> |
| CSS (gzip) | 10498 | <T1> | <T4> | <T7> | <T14> | <Δ> |
| **Canonical 합산** | **30204** | <T1> | <T4> | <T7> | <T14> | <Δ> |
| Archive chunk | 6096 | <T1> | <T4> | <T7> | <T14> | <Δ> |

DoD 충족도:
- Canonical Δ ≤ -1200 B: ✅/⚠/❌
- Archive Δ ≤ -200 B: ✅/⚠/❌
- Trim absolute (T2~T4): -<X> B (의무 -1200 B): ✅/⚠
- 추가 task 한도 (T5~T7): +<Y> B (한도 +200 B): ✅/⚠
```

- [ ] **Step 7: Commit**

```bash
git add docs/superpowers/specs/2026-05-20-v3.40-trim-carry-cleanup-design.md
git commit -m "test(v3.40 T14): 사이클 끝 3종 검증 + final bundle measurement

- npx tsc --noEmit: 0 errors
- npm run lint: 0 errors
- npm test: <count> PASS 양쪽 TZ
- npm run test:smoke: <count> PASS cold-start fresh fork
- N=3 cold-start: 3/3 PASS

Final bundle:
- Canonical: 30204 → <X> (Δ <Y> / 의무 -1200 B): ✅/⚠/❌
- Archive : 6096 → <X> (Δ <Y> / 의무 -200 B): ✅/⚠/❌

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 15: Retro + Codex 최종 review + 배포 (Wrap-up)

**Files:**
- Modify: `docs/superpowers/specs/2026-05-20-v3.40-trim-carry-cleanup-design.md` (§9 retro)

- [ ] **Step 1: Retro §9.1 작업 요약 작성**

§9.1 작업 요약: T0~T14 각 task별 1줄 요약 + commit hash.

- [ ] **Step 2: Retro §9.2~§9.8 작성**

- §9.2 검증 결과: vitest count / smoke count / N=3 결과
- §9.3 Codex 2-pass 결과: 사전 REQUIRES_FIX → v2 흡수, 최종 (TBD)
- §9.4: T14 final 측정 표 (이미 갱신)
- §9.5 신규 lessons / graduate 후보 (v3.41+)
- §9.6 Carry-forward (v3.41+) — T3 미선택 후보 + T6 진단 추가 case + 기타
- §9.7 DoD 충족도 — G1~G5 표
- §9.8 사이클 평가

- [ ] **Step 3: Codex 최종 review prompt 작성**

`/tmp/v3.40-codex-final-prompt.txt`:

```
v3.40 사이클 최종 review. spec + 사이클 diff 검토.

Spec: /Users/hayden/Hayden_AX_Project/my_ai_assistance/docs/superpowers/specs/2026-05-20-v3.40-trim-carry-cleanup-design.md
사이클 diff: git log --oneline e5d4a0c..HEAD

검토 포커스:
1. spec v2 P0/P1 in-cycle 흡수 정합 (실제 commit과 spec 약속 일치 여부)
2. G1 DoD 충족: canonical net -1200 B 달성 여부
3. T2~T4 trim absolute -1200 B 도달 여부
4. T5~T7 추가 +200 B 한도 준수
5. T6 진단 결과 root cause 명확성 + dispatchEvent 변환 회귀 없음
6. T8 helper dynamic import 유지 (chunk merge 없음)
7. T11 missions 3_000ms timeout 적절성
8. Lesson graduate doc 정합 (5건)

분류: DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT.
P0/P1/P2 분류 + file:line 참조 + 종합 의견.
```

- [ ] **Step 4: Codex 최종 review dispatch**

Run:
```bash
codex exec --skip-git-repo-check -o /tmp/v3.40-codex-final.txt "$(cat /tmp/v3.40-codex-final-prompt.txt)"
```
Expected: review 결과 `/tmp/v3.40-codex-final.txt`.

- [ ] **Step 5: 결과 분류 + 흡수**

`cat /tmp/v3.40-codex-final.txt` 결과:
- **DEPLOY_APPROVED**: 그대로 배포 진행.
- **APPROVED_WITH_NOTES**: P1 in-cycle 흡수 또는 v3.41 carry 결정 (사용자 confirm).
- **REJECT**: P0 in-cycle 수정 → 재실행 또는 사용자 결정 후 진행.

흡수 후 §9.3 갱신.

- [ ] **Step 6: Retro § 작성 완료 commit**

```bash
git add docs/superpowers/specs/2026-05-20-v3.40-trim-carry-cleanup-design.md
git commit -m "docs(v3.40 T15): retro §1~§9 작성 + Codex 최종 review <분류>

- §9.1~§9.8 모두 채움
- Codex 최종: <DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT> P0=<N> P1=<N> P2=<N>
- <분류 결과별 흡수 또는 carry 명시>

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

- [ ] **Step 7: 사용자 "배포해줘" 시점 배포**

사용자 명시 승인 후:

Run:
```bash
npm run build
firebase deploy --only hosting
```
Expected: Firebase Hosting live (my-ai-assistant-904f3.web.app).

- [ ] **Step 8: Obsidian Vault sync** (Hayden 글로벌 CLAUDE.md sync 정책)

CLAUDE.md "Obsidian Vault Sync 정책" 절차대로:
- 신규 사이클 spec (v3.40 + plan) → `<vault>/02-Cycles/`
- 신규 memory file (`project_daily_growth_v340.md` 또는 v3.39 확장) → `<vault>/03-Lessons/`
- `00-Index.md` 사이클 타임라인에 v3.40 1줄 추가

```bash
VAULT="/Users/hayden/Hayden/20_Projects/Dev_Project/SJ_Daily Growth(Ai assistant)"
cp docs/superpowers/specs/2026-05-20-v3.40-trim-carry-cleanup-design.md "$VAULT/02-Cycles/"
cp docs/superpowers/plans/2026-05-20-v3.40-trim-carry-cleanup-plan.md "$VAULT/02-Cycles/"
# memory file 갱신 후 sync
```

---

## Self-Review

**1. Spec coverage:**
- §1 G1 Bundle trim: T1 (baseline) + T2 (dynamic import) + T3 (dead code) + T4 (CSS sweep) + T14 (final) ✅
- §1 G2 Medium carry: T5 (C2) + T6 (C3+L4) + T7 (C5) ✅
- §1 G3 P3 polish: T8 (C4) + T9 (C8) + T10 (C11+C13+C6 carry) + T11 (C12) + T12 (C9) ✅
- §1 G4 Lesson graduate: T13 (L1/L2/L3/L5/L6) ✅
- §1 G5 Baseline 측정 보강: T1 + T14 ✅
- §7 Risk R1~R14: 모든 task에 mitigation 명시 ✅
- §0.4 Non-goals: schema migration 0 / 신규 기능 0 ✅

**2. Placeholder scan:**
- `<측정값>`, `<count>`, `<X>` 등 placeholder는 실행 시점에 채워야 하는 변수 — 의도적.
- TBD 없음 (§9 retro는 T15에서 채움).
- "Similar to Task N" 패턴 없음 — 각 task 독립.

**3. Type consistency:**
- `navigateToInterestArchive(interestId: string): Promise<void>` — T8 / T2~T6 caller 동일.
- `maybeShowByInterestNotice(): void` — T7 정의 + T7 caller (stats + archive) 동일.
- `seedPlantDetail(page, opts): Promise<void>` — T9 정의 + 4 spec caller 동일.
- `validateInterestId(id: string): string` — T5 (existing) 재사용 + 시그니처 일관.

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-05-20-v3.40-trim-carry-cleanup-plan.md`. Two execution options:

**1. Subagent-Driven (recommended)** — controller가 fresh subagent를 task별 dispatch, 두 단계 review (per-task quality + spec compliance), 빠른 iteration. v3.39 패턴 동일.

**2. Inline Execution** — 현재 세션에서 task 일괄 실행, checkpoint마다 사용자 review.

Which approach?
