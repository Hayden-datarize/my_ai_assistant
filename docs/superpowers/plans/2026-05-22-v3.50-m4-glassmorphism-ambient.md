# v3.50 — M4 Glassmorphism welcome + Max stage ambient + cleanup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Welcome 모달 2개 glassmorphism surface + 정원 max stage (stage 5) ambient halo + v3.49 carry 3건(C2 cleanup symmetry, C3 dead path 제거, C5 CSS bundle baseline graduation) 정리. UI-only, schema 0.

**Architecture:** CSS-only glass(`.modal-glass` + `--border-glass` token)를 wrapper class에 부착(전역 openModal 미접근). garden-grid 내부에서 `bloomedCount` derive → CSS custom property + class toggle, ambient는 `::before` radial-gradient halo. 4-layer 호환 가드(`@supports color-mix` / `@supports backdrop-filter` / `prefers-reduced-transparency` / `prefers-reduced-motion`).

**Tech Stack:** Vite + TypeScript SPA, vitest unit, Playwright smoke. CSS custom properties, color-mix(in oklab), backdrop-filter.

---

## File Structure

**Create:**
- `tests/unit/welcome-glass.spec.ts` — `.modal-glass` class 부착 검증 (welcome-gamification + welcome-garden)
- `tests/unit/garden-grid-bloom.spec.ts` — bloomedCount derive + class/CSS var 검증
- `tests/unit/missions-sparkle-cleanup.spec.ts` — cleanup symmetry 검증
- `tests/smoke/v3.50-welcome-bloom.spec.ts` — smoke 통합 (welcome glass + bloom ambient)
- `docs/superpowers/specs/2026-05-22-v3.50-codex-pre.txt` — Codex 사전 review 결과 (T0)
- `docs/superpowers/specs/2026-05-22-v3.50-codex-final.txt` — Codex 최종 review 결과 (T6)

**Modify:**
- `src/styles/tokens.css` — `--border-glass` 토큰 추가 (light/dark)
- `src/styles/main.css` — `.modal-glass` + `.garden-grid--has-bloomed` 정의 + 호환 가드
- `src/ui/modals/welcome-gamification.ts` (line 58) — wrapper class에 `modal-glass` 추가
- `src/ui/modals/welcome-garden.ts` (line 50) — wrapper class에 `modal-glass` 추가
- `src/ui/components/garden-grid.ts` (line 35~54) — `bloomedCount` derive + class/CSS var
- `src/ui/missions-sparkle.ts` (line 17~23) — `mountMissionsSparkleListener()` cleanup 반환
- `src/ui/handlers/missions-listeners.ts` — sparkle cleanup을 off 배열에 포함
- `CLAUDE.md` — "Bundle 측정 표준" 섹션에 CSS triple-record 추가 + small cycle exception 명문화

**Delete:**
- `src/ui/handlers/missions.ts` — v3.14 lazy register graduate 이후 dead path
- `tests/unit/handlers-missions.spec.ts` — 위 파일의 유일한 caller

---

### Task 0: T0 Setup — bundle baseline + Codex 사전 review

**Files:**
- Read: `docs/superpowers/specs/2026-05-22-v3.50-m4-glassmorphism-ambient-design.md`
- Create: `docs/superpowers/specs/2026-05-22-v3.50-codex-pre.txt`

- [ ] **Step 1: 작업 디렉토리 동기화**

Run: `git status && git log --oneline -3`
Expected: working tree clean, HEAD = v3.49 retro amend `5e0f29e` 또는 그 이후.

- [ ] **Step 2: Production build + bundle baseline triple capture**

Run:
```bash
npm run build
echo "--- baseline ---"
echo "index: $(gzip -c dist/assets/index-*.js | wc -c) B"
echo "archive: $(gzip -c dist/assets/archive-*.js | wc -c) B"
echo "css: $(gzip -c dist/assets/*.css | wc -c) B"
echo "missions chunk: $(gzip -c dist/assets/missions-*.js 2>/dev/null | wc -c) B"
```

Expected: 3개 measurement 모두 출력. v3.49 retro 기준:
- index ≈ 9514 B
- archive 2 chunks 합산
- CSS ≈ 10791 B (v3.49 retro 기록 — baseline first-record)

Plan memo에 baseline 기록 (T6 비교용).

- [ ] **Step 3: Codex 사전 review CLI direct dispatch**

prompt 텍스트 준비 (spec §8.1 체크리스트 9건 + plan task 분해 요약).

Run:
```bash
codex exec --skip-git-repo-check \
  -o docs/superpowers/specs/2026-05-22-v3.50-codex-pre.txt \
  "$(cat <<'EOF'
Repo: Daily Growth (Vite + TypeScript SPA). 사이클 v3.50 spec+plan 사전 review.

Spec: docs/superpowers/specs/2026-05-22-v3.50-m4-glassmorphism-ambient-design.md
Plan: docs/superpowers/plans/2026-05-22-v3.50-m4-glassmorphism-ambient.md

체크리스트:
1. .modal-glass의 @supports 가드 정합 (color-mix + backdrop-filter 두 개 모두) + prefers-reduced-transparency 가드 일관
2. ambient: bloomedCount 정수 invariant + escape safety + isolation:isolate + pointer-events:none + clamp 강도 cap (0.3/0.7)
3. C3 file 삭제 후 잔존 import 0 검증 방법 (tsc + grep + lint 3-layer)
4. C2 cleanup symmetry: mountMissionsSparkleListener cleanup return + idempotent + double-cleanup safe
5. C5 CLAUDE.md graduation wording 정합
6. 토큰 부재 시 fallback (--surface-1 / --color-primary-vivid 실측)
7. welcome openModal global surface 미접근 invariant
8. derive 위치: user.plantStateByInterest 실측 정합 (user.garden.plants 아님)
9. smoke 1 spec 통합 (welcome glass + bloom ambient) — A/B 분리 assertion 정합

P0/P1/P2 분류. JSON 아닌 마크다운으로.
EOF
)" < /dev/null
```

Expected: 결과 파일 생성 + P0/P1/P2 분류. CLI 한도 시 controller fallback (`pr-review-toolkit:code-reviewer`).

- [ ] **Step 4: 사전 review 결과 → plan v2 in-cycle 흡수**

review 결과 읽고 P0/P1는 spec 또는 plan에 in-cycle 반영. P2는 비목표 후보. 변경 commit:

```bash
git add docs/superpowers/specs/2026-05-22-v3.50-*.md docs/superpowers/plans/2026-05-22-v3.50-*.md docs/superpowers/specs/2026-05-22-v3.50-codex-pre.txt
git commit -m "docs(v3.50 T0): spec+plan v1 + Codex 사전 review 흡수"
```

---

### Task 1: C3 — `handlers/missions.ts` dead path 제거 (rewire + 삭제 통합)

> **Codex 사전 review P0×3 흡수**: T1 단독으로 file 삭제 시 production code(`missions-listeners.ts:15`)에 dangling import 발생. rewire 먼저 → 삭제 순서 적용. grep 패턴은 dynamic import + vi.mock 모두 catch하도록 확장.

**Files:**
- Modify: `src/ui/handlers/missions-listeners.ts:14~16` — `import('./missions').then(...handleMissionsTabChanged)` → `import('./home').then(...hydrateMissions)`
- Modify: `tests/unit/handlers-register.spec.ts:20,25,101` — `vi.mock('../../src/ui/handlers/missions')` → `vi.mock('../../src/ui/handlers/home')` + spy 이름 정합
- Delete: `src/ui/handlers/missions.ts`
- Delete: `tests/unit/handlers-missions.spec.ts`

- [ ] **Step 1: 잔존 import 전수 grep (확장 패턴)**

Run:
```bash
grep -rnE "handleMissionsTabChanged|mountMissionsHandlers|import\\(['\"][^'\"]*handlers/missions['\"]|vi\\.(do)?[Mm]ock\\(['\"][^'\"]*handlers/missions['\"]" src/ tests/ --include="*.ts" \
  | grep -v "missions-triggers\\|missions-listeners\\|missions-section\\|missions-sparkle"
```

Expected: 결과 4 묶음:
- `src/ui/handlers/missions.ts:16,20,24` — 제거 대상 본체
- `src/ui/handlers/missions-listeners.ts:14~16` — rewire 대상 (production)
- `tests/unit/handlers-missions.spec.ts:33,51` — 폐기 대상
- `tests/unit/handlers-register.spec.ts:20,25,101` — mock target 변경 대상

(다른 결과 발견 시 stop + 사용자 보고.)

- [ ] **Step 2: missions-listeners.ts rewire**

Edit `src/ui/handlers/missions-listeners.ts:11~20`:

```typescript
  const off = [
    on('dg:nav:tab-changed', ({ tab }) => {
      if (tab !== 'missions') return;
      // v3.50 T1 C3: handlers/missions.ts dead path 제거에 따른 rewire.
      // hydrateMissions는 home.ts:361에 정의 (v3.14 graduate 이후 home handler 소유).
      void import('./home')
        .then((m) => m.hydrateMissions())
        .catch((err) => {
          console.warn('[missions-listeners] tab-changed failed', err);
        });
    }),
  ];
```

- [ ] **Step 3: handlers-register.spec.ts mock target 변경**

Read `tests/unit/handlers-register.spec.ts` lines 15~110 후, `missions` spy 부분(:20)과 `vi.mock`(:25) + `expect(...).toHaveBeenCalledTimes(1)` assertion(:101) 정합 수정:

```typescript
// 기존:
// const missionsSpies = { handleMissionsTabChanged: vi.fn() };
// vi.mock('../../src/ui/handlers/missions', () => missionsSpies);

// 변경:
const missionsSpies = { hydrateMissions: vi.fn() };
vi.mock('../../src/ui/handlers/home', () => ({
  ...await vi.importActual('../../src/ui/handlers/home'),
  hydrateMissions: missionsSpies.hydrateMissions,
}));
// assertion:
await vi.waitFor(() => expect(missionsSpies.hydrateMissions).toHaveBeenCalledTimes(1));
```

> **주의**: `home.ts`는 다른 export도 많아 `await vi.importActual`로 spread 후 `hydrateMissions`만 spy로 override. 만약 home.ts mock surface가 spec 다른 곳에 영향이 있다면 spy 방식을 `vi.spyOn(home, 'hydrateMissions')`로 좁힘 검토. file 통째 mock 대신 named export만 stub.

대안 패턴(implementer 판단):
```typescript
import * as homeModule from '../../src/ui/handlers/home';
const hydrateMissionsSpy = vi.spyOn(homeModule, 'hydrateMissions').mockImplementation(() => {});
// assertion:
await vi.waitFor(() => expect(hydrateMissionsSpy).toHaveBeenCalledTimes(1));
```

`vi.spyOn` 패턴이 부수효과 적음 — 권장.

- [ ] **Step 4: 두 dead file 삭제**

Run:
```bash
git rm src/ui/handlers/missions.ts tests/unit/handlers-missions.spec.ts
```

- [ ] **Step 5: tsc + lint 통과 확인**

Run: `npx tsc --noEmit && npm run lint`
Expected: 0 errors / 0 warnings. dangling import 없음 (Step 2 rewire 덕분).

- [ ] **Step 6: vitest 회귀 확인 (handlers-register + missions 영역)**

Run: `npm test -- --run handlers-register missions-listeners`
Expected: PASS. Step 3 mock 변경이 정합 시 통과.

- [ ] **Step 7: 전체 vitest**

Run: `npm test -- --run`
Expected: baseline 1611 → 1611 - (handlers-missions.spec.ts 폐기 test 수) ≈ 1607~1610 PASS. drift는 retro에 명시.

- [ ] **Step 8: Commit**

```bash
git add src/ui/handlers/missions-listeners.ts tests/unit/handlers-register.spec.ts
git rm src/ui/handlers/missions.ts tests/unit/handlers-missions.spec.ts
git commit -m "refactor(v3.50 T1 C3): handlers/missions.ts dead path 제거 + missions-listeners rewire to home.hydrateMissions"
```

---

### Task 2: C2 — sparkle listener cleanup symmetry

**Files:**
- Modify: `src/ui/missions-sparkle.ts:17~23` (cleanup return 추가)
- Modify: `src/ui/handlers/missions-listeners.ts:9` (cleanup 정합)
- Create: `tests/unit/missions-sparkle-cleanup.spec.ts`

> **Codex 사전 review P1-3 / P1-4 흡수**: events.ts는 `emit` 아닌 **`dispatch`** export. cleanup 구현은 module-level `unsubscribe` 참조하면 old cleanup이 re-mount 이후 호출 시 새 listener 떼버림 → local disposer capture + `cleaned` guard로 격리.

- [ ] **Step 1: 신규 spec 먼저 (TDD red)**

Create `tests/unit/missions-sparkle-cleanup.spec.ts`:

```typescript
import { describe, it, expect, beforeEach } from 'vitest';
import {
  mountMissionsSparkleListener,
  consumeSparkleQueue,
  __resetSparkleState,
} from '../../src/ui/missions-sparkle';
import { dispatch } from '../../src/ui/events';

describe('mountMissionsSparkleListener cleanup symmetry', () => {
  beforeEach(() => {
    __resetSparkleState();
  });

  it('returns a cleanup function that detaches the listener', () => {
    const cleanup = mountMissionsSparkleListener();
    expect(typeof cleanup).toBe('function');

    dispatch('dg:reward:mission-complete', {
      defId: 'before-cleanup',
      period: 'daily',
      rewardXp: 10,
      at: Date.now(),
    });
    expect(consumeSparkleQueue().has('before-cleanup')).toBe(true);

    cleanup();

    dispatch('dg:reward:mission-complete', {
      defId: 'after-cleanup',
      period: 'daily',
      rewardXp: 10,
      at: Date.now(),
    });
    expect(consumeSparkleQueue().has('after-cleanup')).toBe(false);
  });

  it('idempotent: second mount returns no-op cleanup (only first cleanup detaches)', () => {
    const first = mountMissionsSparkleListener();
    const second = mountMissionsSparkleListener();

    expect(typeof first).toBe('function');
    expect(typeof second).toBe('function');
    second();   // no-op (첫 mount의 listener는 그대로)

    dispatch('dg:reward:mission-complete', {
      defId: 'after-no-op',
      period: 'daily',
      rewardXp: 10,
      at: Date.now(),
    });
    expect(consumeSparkleQueue().has('after-no-op')).toBe(true);

    first();    // 실제 detach
    dispatch('dg:reward:mission-complete', {
      defId: 'after-real-cleanup',
      period: 'daily',
      rewardXp: 10,
      at: Date.now(),
    });
    expect(consumeSparkleQueue().has('after-real-cleanup')).toBe(false);
  });

  it('stale cleanup after remount does not detach the new listener', () => {
    // Codex 사전 review P1-4: closure가 module-level unsubscribe 참조 시 발생할 회귀.
    // local disposer capture + cleaned guard로 차단되는지 검증.
    const firstCleanup = mountMissionsSparkleListener();
    firstCleanup();  // 첫 listener detach

    const secondCleanup = mountMissionsSparkleListener();  // 새 listener wire
    firstCleanup();  // stale cleanup 재호출 (idempotent 보장)

    dispatch('dg:reward:mission-complete', {
      defId: 'after-stale-cleanup',
      period: 'daily',
      rewardXp: 10,
      at: Date.now(),
    });
    // 새 listener가 살아 있어야 한다 — stale cleanup이 떼지 못함.
    expect(consumeSparkleQueue().has('after-stale-cleanup')).toBe(true);

    secondCleanup();  // 정상 detach
  });

  it('double cleanup() is safe (no throw)', () => {
    const cleanup = mountMissionsSparkleListener();
    expect(() => {
      cleanup();
      cleanup();
    }).not.toThrow();
  });
});
```

- [ ] **Step 2: Run test to verify it fails (red)**

Run: `npm test -- missions-sparkle-cleanup`
Expected: FAIL. `mountMissionsSparkleListener` 현재 `void` 반환.

- [ ] **Step 3: missions-sparkle.ts에 cleanup return 추가 (local disposer + cleaned guard)**

Edit `src/ui/missions-sparkle.ts:17~23` 영역 (module-level `unsubscribe` 변수 제거 가능 — local capture로 전환):

```typescript
export function mountMissionsSparkleListener(): () => void {
  if (listenerWired) {
    // 두 번째 mount는 no-op cleanup 반환 — 첫 mount의 listener는 그대로.
    return () => { /* no-op */ };
  }

  // local disposer capture — module-level 참조 회피.
  const disposer = on('dg:reward:mission-complete', ({ defId }) => {
    sparkleQueue.add(defId);
  });
  listenerWired = true;
  // module-level `unsubscribe`는 __resetSparkleState()용으로 유지.
  unsubscribe = disposer;

  let cleaned = false;
  return () => {
    if (cleaned) return;       // double cleanup safety
    cleaned = true;
    // 본 closure가 capture한 disposer만 호출 — 다른 mount의 listener는 안 건드림.
    disposer();
    // 본 cleanup이 production 첫 mount의 것이면 module flag도 reset.
    if (unsubscribe === disposer) {
      unsubscribe = null;
      listenerWired = false;
    }
  };
}
```

- [ ] **Step 4: Run test to verify it passes (green)**

Run: `npm test -- missions-sparkle-cleanup`
Expected: PASS 4/4.

- [ ] **Step 5: missions-listeners.ts에 sparkle cleanup 정합**

> **주의**: T1에서 missions-listeners.ts rewire를 이미 완료(`import('./home').then(m => m.hydrateMissions())`). 본 step은 `mountMissionsSparkleListener()` 반환값을 cleanup array에 합치는 것만.

Edit `src/ui/handlers/missions-listeners.ts`:

```typescript
import { on } from '../events';
import { mountMissionsSparkleListener } from '../missions-sparkle';

export function registerMissionsListeners(): () => void {
  // v3.49 T5 Codex 최종 P1: sparkle queue listener는 boot 시점부터 활성이어야 한다.
  // v3.50 T2 C2: cleanup symmetry — sparkle detach도 off에 포함.
  const cleanupSparkle = mountMissionsSparkleListener();

  const off = [
    on('dg:nav:tab-changed', ({ tab }) => {
      if (tab !== 'missions') return;
      // v3.50 T1 C3 rewire: handlers/missions.ts dead path 제거 → home.hydrateMissions().
      void import('./home')
        .then((m) => m.hydrateMissions())
        .catch((err) => {
          console.warn('[missions-listeners] tab-changed failed', err);
        });
    }),
    cleanupSparkle,
  ];

  return () => {
    for (let i = off.length - 1; i >= 0; i--) off[i]?.();
  };
}
```

- [ ] **Step 6: Full vitest로 cleanup 정합 회귀 검증**

Run: `npm test -- --run missions`
Expected: 모든 missions-* spec PASS. cleanup 추가에 따른 회귀 0.

- [ ] **Step 7: tsc + lint**

Run: `npx tsc --noEmit && npm run lint`
Expected: 0 errors.

- [ ] **Step 8: Commit**

```bash
git add tests/unit/missions-sparkle-cleanup.spec.ts src/ui/missions-sparkle.ts src/ui/handlers/missions-listeners.ts
git commit -m "refactor(v3.50 T2 C2): mountMissionsSparkleListener cleanup return — local disposer + cleaned guard"
```

---

### Task 3: M4 — Glassmorphism welcome modals

**Files:**
- Modify: `src/styles/tokens.css` — `--border-glass` 토큰 light/dark
- Modify: `src/styles/main.css` — `.modal-glass` 정의 + 호환 가드
- Modify: `src/ui/modals/welcome-gamification.ts:58`
- Modify: `src/ui/modals/welcome-garden.ts:50`
- Create: `tests/unit/welcome-glass.spec.ts`

> **Codex 사전 review P1-2 / P2-1 흡수**: 실제 토큰은 `--bg-card` / `--primary` (`--surface-1` / `--color-primary-vivid` 부재). `.dg-modal-card`의 opaque 배경이 위에 덮으므로 `:has()` 부모 override 추가.

- [ ] **Step 1: 토큰 실측 재확인**

Run:
```bash
grep -nE "^\s+--(bg-card|primary)\b" src/styles/tokens.css
```

Expected:
- light `--primary: #4F46E5;` (line 6) / `--bg-card: #FFFFFF;` (line 15)
- dark `--primary: #818CF8;` (line 39) / `--bg-card: #2A2D3A;` (line 47)

- [ ] **Step 2: 신규 spec 먼저 (TDD red)**

Create `tests/unit/welcome-glass.spec.ts`:

```typescript
/** @vitest-environment jsdom */
import { describe, it, expect, beforeEach, vi } from 'vitest';

describe('M4 glassmorphism — welcome wrappers carry .modal-glass', () => {
  beforeEach(() => {
    document.body.innerHTML = '<div id="modal-root"></div>';
    vi.resetModules();
  });

  it('welcome-garden modal wrapper has .modal-glass class', async () => {
    vi.doMock('../../src/state/user', () => ({
      loadUserData: () => null,
      saveUser: () => {},
    }));
    const { showWelcomeGardenModal } = await import('../../src/ui/modals/welcome-garden');
    showWelcomeGardenModal();
    const wrap = document.querySelector('.welcome-garden-modal');
    expect(wrap).not.toBeNull();
    expect(wrap?.classList.contains('modal-glass')).toBe(true);
  });

  it('welcome-gamification modal wrapper has .modal-glass class', async () => {
    vi.doMock('../../src/state/user', () => ({
      loadUserData: () => ({
        gamificationMigrated: false,
        badges: { earned: [], seen: [] },
        interests: [],
        plantStateByInterest: {},
      }),
      saveUser: () => {},
    }));
    const { maybeShowWelcomeGamificationModal } = await import(
      '../../src/ui/modals/welcome-gamification'
    );
    // 위 spec 컨텍스트와 똑같이 backfill flow 통과 → wrapper render. 호출 시그니처는
    // welcome-gamification.ts 실제 export 기준으로 implementer가 미세 조정 가능.
    await maybeShowWelcomeGamificationModal?.();
    const wrap = document.querySelector('.welcome-game');
    expect(wrap).not.toBeNull();
    expect(wrap?.classList.contains('modal-glass')).toBe(true);
  });
});
```

> **주의**: `showWelcomeGardenModal` / `maybeShowWelcomeGamificationModal` export 이름은 추정. T3 Step 2 진입 시 두 파일의 actual export 이름으로 정합. backfill flow 통과 조건이 까다로우면 wrapper만 render하는 helper(`renderWelcomeGameModalBody` 등)로 spec 좁힘.

- [ ] **Step 3: Run test to verify it fails (red)**

Run: `npm test -- welcome-glass`
Expected: FAIL. 현재 wrapper에 `modal-glass` class 없음.

- [ ] **Step 4: 토큰 추가 — `src/styles/tokens.css`**

Edit `:root` 블록에 추가:
```css
  --border-glass: rgba(0, 0, 0, 0.08);
```

Edit `[data-theme="dark"]` 블록에 추가:
```css
  --border-glass: rgba(255, 255, 255, 0.12);
```

- [ ] **Step 5: `.modal-glass` 정의 + `.dg-modal-card` 부모 override — `src/styles/main.css`**

적절한 위치(modal 관련 섹션 또는 새 섹션)에 추가:

```css
/* v3.50 T3 M4 — Glassmorphism surface (welcome 모달 wrapper 부착).
 * Codex 사전 review P1-1: color-mix + backdrop-filter 양쪽 가드 묶음 — 부분 지원 시
 * "blur 없는 translucent" 어색 surface 회피.
 * Codex P1-2: 토큰 --bg-card / --border-glass 실측 정합.
 * Codex P2-1: 부모 .dg-modal-card opaque 배경을 :has()로 override. */
.modal-glass {
  background: var(--bg-card);
  border: 1px solid var(--border-glass);
}

@supports
  ((background: color-mix(in oklab, white, black))) and
  ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
  .modal-glass {
    background: color-mix(in oklab, var(--bg-card) 75%, transparent);
    backdrop-filter: blur(20px) saturate(180%);
    -webkit-backdrop-filter: blur(20px) saturate(180%);
  }
}

@media (prefers-reduced-transparency: reduce) {
  .modal-glass {
    background: var(--bg-card);
    backdrop-filter: none;
    -webkit-backdrop-filter: none;
  }
}

/* welcome 모달이 .modal-glass child를 가질 때만 부모 .dg-modal-card transparent화.
 * 다른 모달(.dg-modal-card 단독)은 영향 X. :has() 미지원 시 glass 효과 약함(회귀 0). */
.dg-modal-card:has(.modal-glass) {
  background: transparent;
  border: none;
  box-shadow: none;
}
```

- [ ] **Step 6: welcome-gamification wrapper class 부착**

Edit `src/ui/modals/welcome-gamification.ts:58`:

```typescript
  const bodyHtml = `<div class="welcome-game modal-glass"><p>이미 달성한 <strong>${N}개</strong> 뱃지가 컬렉션에 추가되었어요!</p><div class="badges-grid">${miniGrid}</div><button type="button" class="btn btn-primary" id="goStatsBtn">stats 탭에서 보기</button></div>`;
```

- [ ] **Step 7: welcome-garden wrapper class 부착**

Edit `src/ui/modals/welcome-garden.ts:50`:

```typescript
  const bodyHtml = `<div class="welcome-garden-modal modal-glass"><p>${escapeHtml(MSG.GARDEN_INTRODUCE_BODY).replace(/\n/g, '<br>')}</p>${highlightHtml}<div class="modal-actions"><button type="button" class="btn-primary" id="welcomeGardenViewBtn">${escapeHtml(MSG.GARDEN_CTA_VIEW)}</button><button type="button" class="btn-secondary" id="welcomeGardenCloseBtn">${escapeHtml(MSG.GARDEN_CTA_CLOSE)}</button></div></div>`;
```

- [ ] **Step 8: Run test to verify it passes (green)**

Run: `npm test -- welcome-glass`
Expected: PASS 2/2.

- [ ] **Step 9: full vitest 회귀 확인**

Run: `npm test -- --run welcome`
Expected: 기존 welcome-* spec 모두 PASS. 신규 class만 추가됐고 기존 class 보존이라 회귀 0.

- [ ] **Step 10: tsc + lint**

Run: `npx tsc --noEmit && npm run lint`
Expected: 0 errors.

- [ ] **Step 11: Commit**

```bash
git add src/styles/tokens.css src/styles/main.css src/ui/modals/welcome-gamification.ts src/ui/modals/welcome-garden.ts tests/unit/welcome-glass.spec.ts
git commit -m "feat(v3.50 T3 M4): glassmorphism welcome modals — .modal-glass + --border-glass token"
```

---

### Task 4: M4 — Max stage ambient (garden bloom halo)

**Files:**
- Modify: `src/ui/components/garden-grid.ts:27~55` (`renderGardenGrid` derive + class/CSS var)
- Modify: `src/styles/main.css` — `.garden-grid--has-bloomed` 정의
- Create: `tests/unit/garden-grid-bloom.spec.ts`

- [ ] **Step 1: 신규 spec 먼저 (TDD red)**

Create `tests/unit/garden-grid-bloom.spec.ts`:

```typescript
/** @vitest-environment jsdom */
import { describe, it, expect, beforeEach } from 'vitest';
import { renderGardenGrid } from '../../src/ui/components/garden-grid';
import type { User } from '../../src/state/user';

function makeUser(plantStages: Record<string, number>): User {
  const interests = Object.keys(plantStages);
  const plantStateByInterest: User['plantStateByInterest'] = {};
  for (const id of interests) {
    plantStateByInterest[id] = {
      stage: plantStages[id] as 1 | 2 | 3 | 4 | 5,
      cumulativeActivity: 1,
      lastWaterDate: '2026-05-22',
    } as User['plantStateByInterest'][string];
  }
  // 최소 shape — 그 외 필드는 renderGardenGrid가 사용하지 않으므로 unknown cast.
  return {
    interests,
    plantStateByInterest,
  } as unknown as User;
}

describe('renderGardenGrid — max stage ambient (v3.50 M4)', () => {
  let root: HTMLElement;
  beforeEach(() => {
    document.body.innerHTML = '<div id="grid-root"></div>';
    root = document.getElementById('grid-root')!;
  });

  it('no bloomed plant: only .garden-grid (no --has-bloomed)', () => {
    const user = makeUser({ tech: 3, design: 2 });
    renderGardenGrid(root, user);
    const grid = root.querySelector('.garden-grid');
    expect(grid).not.toBeNull();
    expect(grid?.classList.contains('garden-grid--has-bloomed')).toBe(false);
    expect((grid as HTMLElement).style.getPropertyValue('--bloomed-count')).toBe('0');
  });

  it('one bloomed plant: .garden-grid--has-bloomed + --bloomed-count=1', () => {
    const user = makeUser({ tech: 5, design: 2 });
    renderGardenGrid(root, user);
    const grid = root.querySelector('.garden-grid');
    expect(grid?.classList.contains('garden-grid--has-bloomed')).toBe(true);
    expect((grid as HTMLElement).style.getPropertyValue('--bloomed-count')).toBe('1');
  });

  it('five bloomed plants: --bloomed-count=5 (raw, no JS cap)', () => {
    const user = makeUser({ a: 5, b: 5, c: 5, d: 5, e: 5 });
    renderGardenGrid(root, user);
    const grid = root.querySelector('.garden-grid');
    expect(grid?.classList.contains('garden-grid--has-bloomed')).toBe(true);
    expect((grid as HTMLElement).style.getPropertyValue('--bloomed-count')).toBe('5');
  });

  it('empty interests: garden-empty fallback, no bloom class', () => {
    const user = makeUser({});
    renderGardenGrid(root, user);
    expect(root.querySelector('.garden-empty')).not.toBeNull();
    expect(root.querySelector('.garden-grid--has-bloomed')).toBeNull();
  });
});
```

- [ ] **Step 2: Run test to verify it fails (red)**

Run: `npm test -- garden-grid-bloom`
Expected: FAIL. 현재 garden-grid에 `--has-bloomed` class도, `--bloomed-count` CSS var도 없음.

- [ ] **Step 3: `renderGardenGrid` derive + 출력 수정**

Edit `src/ui/components/garden-grid.ts:35~54`:

이전:
```typescript
  const cards = visible.map(id => {
    ...
  }).join('');
  // eslint-disable-next-line no-restricted-syntax -- 위에서 escapeHtml 전처리 완료
  root.innerHTML = `<div class="garden-grid">${cards}</div>`;
```

수정 후:
```typescript
  const cards = visible.map(id => {
    ...
  }).join('');
  // v3.50 T4 M4 — max stage(5) 식물 N개에 따른 ambient halo intensity.
  // bloomedCount는 정수 → escape 불요. CSS clamp(0.3, n*0.12, 0.7)로 강도 cap.
  const bloomedCount = visible.reduce(
    (n, id) => (user.plantStateByInterest[id]?.stage === 5 ? n + 1 : n),
    0,
  );
  const bloomClass = bloomedCount > 0 ? ' garden-grid--has-bloomed' : '';
  // eslint-disable-next-line no-restricted-syntax -- 위에서 escapeHtml 전처리 완료
  root.innerHTML = `<div class="garden-grid${bloomClass}" style="--bloomed-count: ${bloomedCount};">${cards}</div>`;
```

- [ ] **Step 4: `.garden-grid--has-bloomed` CSS 정의 — `src/styles/main.css`**

> **Codex 사전 review P1-2 정정**: `--primary` (`--color-primary-vivid` 부재).

`.garden-grid` 정의 인근(또는 stats 섹션)에 추가:

```css
/* v3.50 T4 M4 — Max stage ambient halo (stage 5 식물 N개에 비례). */
.garden-grid {
  --bloomed-count: 0;
}

.garden-grid--has-bloomed {
  position: relative;
  isolation: isolate;
}

.garden-grid--has-bloomed::before {
  content: "";
  position: absolute;
  inset: -10%;
  background: radial-gradient(
    circle at center,
    var(--primary),
    transparent 60%
  );
  opacity: 0.4;
  filter: blur(40px);
  z-index: -1;
  pointer-events: none;
  animation: garden-bloom-pulse 6s ease-in-out infinite;
}

@supports (background: color-mix(in oklab, white, black)) {
  .garden-grid--has-bloomed::before {
    background: radial-gradient(
      circle at center,
      color-mix(
        in oklab,
        var(--primary)
          calc(clamp(0.3, var(--bloomed-count) * 0.12, 0.7) * 100%),
        transparent
      ),
      transparent 60%
    );
    opacity: 1;
  }
}

@keyframes garden-bloom-pulse {
  0%, 100% { opacity: 0.5; }
  50%      { opacity: 1; }
}

@media (prefers-reduced-motion: reduce) {
  .garden-grid--has-bloomed::before {
    animation: none;
    opacity: 0.6;
  }
}
```

> **주의**: `pointer-events: none` 명시 — 카드 클릭 침범 차단. `isolation: isolate`로 z-index -1이 부모 stacking context 침범 X.

- [ ] **Step 5: Run test to verify it passes (green)**

Run: `npm test -- garden-grid-bloom`
Expected: PASS 4/4.

- [ ] **Step 6: 기존 garden-grid spec 회귀 확인**

Run: `npm test -- --run garden-grid`
Expected: 기존 garden-grid 관련 spec 모두 PASS. 신규 attribute(style/class) 추가만이라 기존 assertion 회귀 0.

- [ ] **Step 7: tsc + lint**

Run: `npx tsc --noEmit && npm run lint`
Expected: 0 errors.

- [ ] **Step 8: Commit**

```bash
git add src/ui/components/garden-grid.ts src/styles/main.css tests/unit/garden-grid-bloom.spec.ts
git commit -m "feat(v3.50 T4 M4): garden max stage ambient halo (bloomedCount + .garden-grid--has-bloomed)"
```

---

### Task 5: Smoke 통합 spec (welcome glass + bloom ambient)

**Files:**
- Create: `tests/smoke/v3.50-welcome-bloom.spec.ts`

- [ ] **Step 1: 기존 smoke seed pattern 실측**

Run:
```bash
grep -nE "primeOnboardedUser|primeOnboarded|test\.beforeEach|page\.evaluate.*localStorage" tests/smoke/v3.49-missions-grid.spec.ts | head -10
```

Expected: v3.49가 사용한 seed helper / localStorage 패턴 확인. v3.50 spec도 같은 pattern 따른다.

- [ ] **Step 2: smoke spec 작성 — v3.49 패턴(`primeOnboardedUser + addInitScript read-merge`) 따름**

> **Codex 사전 review P2-2 흡수**: `PrimeUserOpts`는 schemaVersion 2|3|4만 받고 plantStateByInterest 필드 없음. 따라서 `primeOnboardedUser(schemaVersion: 3)` 호출 후 `page.addInitScript`로 read-merge 패턴 (v3.49 missions-grid smoke와 동일). `wantWelcomeGarden` 옵션은 helper에 없음 — `gardenIntroduced: false`로 미진입 상태 seed.

Create `tests/smoke/v3.50-welcome-bloom.spec.ts`:

```typescript
import { test, expect } from '@playwright/test';
import { primeOnboardedUser } from '../helpers/seed';

test.describe('v3.50 M4 — welcome glass + bloom ambient', () => {
  test('welcome-garden modal wrapper carries .modal-glass class', async ({ page }) => {
    // gardenIntroduced=false → 첫 진입 시 welcome-garden 자동 노출.
    // schemaVersion: 3 → migration chain이 v10까지 끌어올림.
    await primeOnboardedUser(page, {
      schemaVersion: 3,
      gardenIntroduced: false,
      interests: ['tech'],
    });
    await page.goto('/');

    const wrap = page.locator('.welcome-garden-modal');
    await expect(wrap).toBeVisible({ timeout: 5000 });
    await expect(wrap).toHaveClass(/modal-glass/);
  });

  test('garden tab with stage-5 plant has .garden-grid--has-bloomed', async ({ page }) => {
    // 1단계: schemaVersion 3 seed (migration chain이 v10까지 끌어올림).
    await primeOnboardedUser(page, {
      schemaVersion: 3,
      gardenIntroduced: true,  // welcome-garden modal 미진입
      interests: ['tech'],
    });

    // 2단계: addInitScript read-merge로 plantStateByInterest 주입 (v3.49 패턴).
    await page.addInitScript(() => {
      const raw = localStorage.getItem('user');
      if (!raw) return;
      const u = JSON.parse(raw) as Record<string, unknown>;
      u.plantStateByInterest = {
        tech: {
          stage: 5,
          cumulativeActivity: 50,
          lastWaterDate: '2026-05-22',
          unlockedAt: '2026-05-22',
        },
      };
      localStorage.setItem('user', JSON.stringify(u));
    });

    await page.goto('/');

    // stats(정원) 탭 진입 — selector는 nav.ts data-tab="stats" 확인 필요.
    await page.locator('button[data-tab="stats"]').click();

    const grid = page.locator('.garden-grid');
    await expect(grid).toBeVisible({ timeout: 5000 });
    await expect(grid).toHaveClass(/garden-grid--has-bloomed/);

    const bloomedCount = await grid.evaluate(
      (el) => (el as HTMLElement).style.getPropertyValue('--bloomed-count'),
    );
    expect(bloomedCount).toBe('1');
  });
});
```

> **주의**: `data-tab="stats"` selector는 실측 (v3.49 missions-grid smoke의 selector 패턴 따름). `welcome-garden` 자동 노출 trigger는 `gardenIntroduced: false` + production code의 첫 진입 분기 확인. helper signature 변경은 본 사이클 비목표 — helper 확장 필요 시 v3.51+ carry.

- [ ] **Step 3: smoke 1차 실행 (warm)**

Run: `npm run test:smoke -- v3.50-welcome-bloom`
Expected: 2/2 PASS. 실패 시 helper signature 정합으로 재시도.

- [ ] **Step 4: smoke N=3 cold-fork 검증 (flake 검출)**

Run:
```bash
for i in 1 2 3; do
  echo "--- run $i ---"
  npm run test:smoke -- v3.50-welcome-bloom 2>&1 | tail -5
done
```

Expected: 3/3 runs PASS (총 6/6 pass, 0 flake).

- [ ] **Step 5: 전체 smoke 회귀 확인**

Run: `npm run test:smoke`
Expected: baseline 116 → 118 (+2 본 사이클 신규). 기존 flake 0.

- [ ] **Step 6: Commit**

```bash
git add tests/smoke/v3.50-welcome-bloom.spec.ts
git commit -m "test(v3.50 T5): smoke — welcome glass + bloom ambient (N=3 0 flake)"
```

---

### Task 6: C5 graduation + 사이클 마무리 (3종 게이트 + 양쪽 TZ + Codex 최종 + 배포 + retro + 일시 종료)

**Files:**
- Modify: `CLAUDE.md` — Bundle 측정 표준 섹션
- Create: `docs/superpowers/specs/2026-05-22-v3.50-codex-final.txt`
- Modify: `docs/superpowers/specs/2026-05-22-v3.50-m4-glassmorphism-ambient-design.md` — §10 Retro 섹션 추가
- Memory: `~/.claude/projects/.../memory/project_daily_growth_v350.md` (신규)

- [ ] **Step 1: C5 — CLAUDE.md "Bundle 측정 표준" 섹션 graduation**

Edit `CLAUDE.md`의 Bundle 측정 표준 섹션을 다음으로 교체(또는 보강):

```markdown
**Bundle 측정 표준 (v3.14.5 T5 graduation, v3.35 T4 extension, v3.50 T6 CSS triple-record)**:

1. **Canonical (deploy gate)** — main entry size:
   `gzip -c dist/assets/index-*.js | wc -c`

2. **Archive chunk (dual record, v3.35+, archive 영역 변경 시 누락 catch)**:
   `gzip -c dist/assets/archive-*.js | wc -c`

3. **CSS asset (triple record, v3.50+, UI-only 사이클 누락 catch)**:
   `gzip -c dist/assets/*.css | wc -c`

사이클 retro §Bundle 측정에 세 값 모두 기록. Threshold:

- index (canonical): GREEN ≤ +300 B / AMBER ≤ +500 B
- archive chunk (archive 영역 변경 시만 적용): GREEN ≤ +300 B / AMBER ≤ +600 B soft (lazy load이라 first paint 영향 X, index 보다 관대)
- CSS asset (v3.50+): GREEN ≤ +500 B / AMBER ≤ +800 B (UI-only 사이클은 CSS Δ가 main signal, 토큰/component 변경에 민감)

**Graduation 경위 (v3.50 T6)**: v3.49 T0 CSS baseline 캡처 누락 → T5에서 Δ 추적 불가, "추정"만 가능. UI-only 사이클에서 CSS가 main bundle signal이라 누락 risk 크다. v3.50부터 T0에 triple capture 의무.
```

- [ ] **Step 2: 3종 게이트 + 양쪽 TZ**

Run:
```bash
npx tsc --noEmit
npm run lint
npm test -- --run
TZ=America/New_York npm test -- --run
```

Expected: 4단계 모두 PASS. test 수: 1611 baseline에서 ±2~6 (T1 -1~3 / T2 +3 / T3 +2 / T4 +4) = 약 1615 PASS.

- [ ] **Step 3: smoke N=3 최종 검증**

Run:
```bash
for i in 1 2 3; do
  echo "--- run $i ---"
  npm run test:smoke 2>&1 | tail -3
done
```

Expected: 3/3 runs all PASS (116 baseline → 118 신규).

- [ ] **Step 4: Bundle 측정 (T0 baseline vs T6 deploy)**

Run:
```bash
npm run build
echo "--- T6 final ---"
echo "index: $(gzip -c dist/assets/index-*.js | wc -c) B"
echo "archive: $(gzip -c dist/assets/archive-*.js | wc -c) B"
echo "css: $(gzip -c dist/assets/*.css | wc -c) B"
```

Expected:
- index Δ ≤ +300 B (M4는 CSS 주력 → index Δ 거의 0)
- archive Δ ≈ 0 B (archive 영역 미접근)
- CSS Δ ≤ +500 B (glass + ambient ≈ +200~400 B 예상)

- [ ] **Step 5: Codex 최종 review CLI direct dispatch**

Run:
```bash
codex exec --skip-git-repo-check \
  -o docs/superpowers/specs/2026-05-22-v3.50-codex-final.txt \
  "$(cat <<'EOF'
Repo: Daily Growth (Vite + TypeScript SPA). 사이클 v3.50 최종 deploy gate review.

Branch: main
Recent commits: v3.50 T0~T5 (spec/plan + C3 dead path 제거 + C2 cleanup symmetry + M4 glass + M4 ambient + smoke).

체크리스트:
1. C3 file 삭제 후 잔존 import 없는지 (production + test)
2. C2 cleanup symmetry idempotent + double-cleanup safety
3. M4 glass — @supports 가드 정합 + prefers-reduced-transparency 가드 + welcome 외 다른 모달 surface 미접근
4. M4 ambient — pointer-events:none + isolation:isolate + escape safety + reduced-motion 가드 + color-mix fallback
5. 토큰 정합 (--surface-1 / --color-primary-vivid / --border-glass 실제 정의 존재)
6. bundle triple-record threshold 준수
7. v3.49 lesson 적용 (smoke production 코드 실측 / production wiring 검증)
8. 일시 종료 retro carry 항목 누락 없음

분류: DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT. P0/P1/P2.
EOF
)" < /dev/null
```

Expected: 결과 파일 생성. CLI 한도 시 controller fallback (small cycle exception OK, v3.43 graduate).

- [ ] **Step 6: Codex 최종 review 결과 반영**

- **DEPLOY_APPROVED**: 그대로 deploy로.
- **APPROVED_WITH_NOTES**: P1 in-cycle fix 후 재검증.
- **REJECT**: P0 in-cycle fix → 재 review → 통과 후 deploy.

In-cycle fix 시 commit message: `fix(v3.50 T6 Codex 최종 P0/P1): <항목>`.

- [ ] **Step 7: 사용자에게 배포 confirm 요청**

> "v3.50 모든 게이트 통과 (vitest 1615 / smoke 118 / bundle triple GREEN / Codex 최종 <분류>). 배포할까요?"

사용자 "배포해줘" 명시 후에만 Step 8 진행. **CLAUDE.md 절대 규칙 — 자동 deploy 금지.**

- [ ] **Step 8: Firebase 배포**

Run:
```bash
npm run build
firebase deploy --only hosting
```

Expected: deploy 성공 + live URL 출력 (`my-ai-assistant-904f3.web.app`).

- [ ] **Step 9: Retro 작성 (spec §10 추가)**

Edit `docs/superpowers/specs/2026-05-22-v3.50-m4-glassmorphism-ambient-design.md` 끝에 추가:

```markdown
## §11. Retro (2026-05-22)

### 11.1 결과 요약

| 항목 | 값 |
|---|---|
| Commits | <T0~T6 + 신규 in-cycle fix 합산> |
| vitest (TZ=local) | <T6 measurement>/PASS |
| vitest (TZ=America/New_York) | 동일 |
| smoke (cold-fork) | <T6 measurement>/PASS |
| smoke N=3 cold-fork | v3.50-welcome-bloom 6/6 |
| 3종 게이트 | tsc 0 / lint 0 / vitest PASS |
| Bundle index canonical (gzip) | <T0> → <T6> (Δ <delta> GREEN/AMBER) |
| Bundle archive chunk (gzip) | <T0> → <T6> (Δ <delta>) |
| Bundle CSS asset (gzip) | <T0 = 10791> → <T6> (Δ <delta> GREEN/AMBER) — first-record |
| schema | v10 그대로 (변경 0) |

### 11.2 Codex 2-pass

- 사전 review: <분류> P0/P1/P2 / 결과
- 최종 review: <분류> P0/P1/P2 / 결과

### 11.3 신규 lessons

1. CSS bundle triple-record graduation — v3.50 T6 명문화.
2. C3 dead path catch ROI — v3.49 retro에서 catch한 잔재를 다음 사이클에 정리하는 패턴.
3. <기타 사이클 lessons>

### 11.4 일시 종료 선언

본 사이클 retro 시점부터 **roadmap_v346_to_v350.md A안 활성**:

- 자동 cycle 시작 X.
- 재개 트리거: 사용자 실 사용 후 명시 요청 / production 보안 hotfix.

### 11.5 미진입 carry

(spec §9 그대로 + 본 사이클 신규 carry)

---

**End of cycle.**
```

- [ ] **Step 10: Memory 파일 작성 + MEMORY.md 인덱스 갱신**

Create `~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v350.md`:

```markdown
---
name: project-daily-growth-v350
description: "DG v3.50 — M4 glassmorphism welcome 모달 + max stage ambient + cleanup 3건 (C2/C3/C5) + 사이클 일시 종료 선언"
metadata:
  type: project
---

# DG v3.50 — M4 + lightweight cleanup + 일시 종료 (배포 완료)

**배포**: 2026-05-22 (main HEAD <hash>, Firebase live: my-ai-assistant-904f3.web.app)

(retro 요약 — spec §11 그대로 압축)

## Carry (재개 시점)

(spec §9 그대로)

## 재개 정책

roadmap_v346_to_v350.md A안 활성 — 자동 cycle 시작 X.
```

Edit `~/.claude/projects/.../memory/MEMORY.md` 마지막에 추가:

```markdown
- [DG v3.50 M4 + cleanup + 일시 종료](project_daily_growth_v350.md) — **✅ 배포 완료 + 일시 종료 선언 (2026-05-22, main HEAD <hash>, Firebase live)** — <commit수> commit · vitest <count> 양쪽TZ · smoke 118 N=3 · bundle index Δ <delta>/archive Δ<delta>/CSS Δ<delta> triple-record graduate · Codex 2-pass <result>. M4 glass(.modal-glass)+ambient(.garden-grid--has-bloomed)+C2/C3/C5 cleanup. 사이클 일시 종료, 사용자 actual use 기간 진입.
```

- [ ] **Step 11: 최종 commit (retro + memory)**

```bash
git add docs/superpowers/specs/2026-05-22-v3.50-*.md docs/superpowers/specs/2026-05-22-v3.50-codex-final.txt CLAUDE.md
git commit -m "docs(v3.50 T6): retro + C5 CSS triple-record graduation + 일시 종료 선언"
```

Memory 파일은 사용자 home 디렉토리이므로 별도 (git tracked X).

- [ ] **Step 12: Obsidian vault sync (배포 직후 자동, CLAUDE.md sync 정책)**

vault 경로: `/Users/hayden/Hayden/20_Projects/Dev_Project/SJ_Daily Growth(Ai assistant)/`

```bash
VAULT="/Users/hayden/Hayden/20_Projects/Dev_Project/SJ_Daily Growth(Ai assistant)"
cp docs/superpowers/specs/2026-05-22-v3.50-m4-glassmorphism-ambient-design.md "$VAULT/02-Cycles/"
cp ~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v350.md "$VAULT/03-Lessons/"
cp ~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md "$VAULT/03-Lessons/"
```

00-Index.md 사이클 타임라인 한 줄 추가 (v3.49 retro L7 lesson: detail row 사전 grep 의무):

```bash
grep -nE "^\| 20.*v3\.50 " "$VAULT/00-Index.md"
```

Expected: 결과 없음 (신규 row). 있으면 중복 추가 회피.

수동 add (Edit 도구로 strict 시간 역순 정렬 유지).

---

## Self-Review

### Spec coverage check

| Spec 섹션 | Plan task |
|---|---|
| §3 Glass surface 정의 | T3 Steps 4~7 |
| §4 Ambient derive + CSS | T4 Steps 3~4 |
| §5.1 C3 dead path | T1 Steps 1~3 |
| §5.2 C2 cleanup symmetry | T2 Steps 1~5 |
| §5.3 C5 CSS graduation | T6 Step 1 |
| §6 Task 분해 | T0~T6 직접 1:1 |
| §7.1 Unit DoD (welcome-glass / garden-grid-bloom / missions-sparkle-cleanup) | T2/T3/T4 신규 spec |
| §7.2 Smoke 통합 spec | T5 |
| §7.3 3종 게이트 + 양쪽 TZ | T6 Step 2 |
| §7.4 Bundle triple record | T0 Step 2 + T6 Step 4 |
| §8 Codex 2-pass | T0 Step 3 + T6 Step 5 |
| §9 Carry | T6 Step 9 retro §11.5 |
| §10 Risk | T1/T3/T4 가드 + smoke N=3 |

Gap: 없음.

### Placeholder scan

- "TBD"/"TODO" — 0건.
- "implement later"/"fill in details" — 0건.
- "Similar to Task N" — 0건. 모든 코드 블록 explicit.
- "Add appropriate error handling" — 0건. graceful catch / `pointer-events: none` / `isolation: isolate` 명시.
- helper signature 정합(`primeOnboardedUser`) — Step 1에 실측 의무 명시(추정 회피).

### Type consistency

- `mountMissionsSparkleListener: () => () => void` — T2 Step 3 정의 + T2 Step 1 spec assertion 일치.
- `bloomedCount: number` (raw integer) — T4 Step 3 derive + T4 Step 1 spec assertion(`'1'` / `'5'` 문자열, `getPropertyValue` 반환 string) 정합.
- `renderGardenGrid(root, user)` 시그니처 변경 0 — T4 Step 3.
- `.modal-glass` class 부착 — T3 Step 4~7 + welcome-glass.spec 모두 정합.
- `.garden-grid--has-bloomed` class — T4 Step 3~4 + garden-grid-bloom.spec 정합.

이상 없음.

---

**End of plan.**
