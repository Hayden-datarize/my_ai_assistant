# v3.28 Bundle Trim Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v3.27까지 누적된 bundle drift 회수 — CLI gzip **42731 B → ≤ 41500 B** (Δ ≥ **−1231 B**).

**Architecture:** Dead/orphan 제거 + write-side normalize → read-side fallback 9곳 제거 + migration chain simplify + LOW utility inline. **신규 기능 0 / schema 변경 0 / CSP 0 / API 변경 0**. main 직배포 패턴 — 사이클 끝 firebase deploy 1회.

**Tech Stack:** Vite + TypeScript / Vitest (양쪽 TZ KST+NY) / Playwright smoke 97/97 / Firebase Hosting.

**Baseline (2026-05-11)**: v3.27 deploy tip 직후 build — CLI gzip 42731 B / vitest 1217 / smoke 97/97 / TS strict 0 / lint baseline.

**Spec**: [docs/superpowers/specs/2026-05-11-v3.28-trim-design.md](../specs/2026-05-11-v3.28-trim-design.md)

---

## File Structure

| 파일 | 역할 | 예상 Δ |
|---|---|---|
| `src/styles/layout.css` | `#insightsTab` orphan 3 selector 제거 | −60 B |
| `src/styles/pages/stats.css` / `archive.css` | comment ref 정리 | ±0 (cosmetic) |
| `src/state/schema.ts` | `makeAnswer`/`makeBriefing`(생성 helper) — `pinned: false` default 박기 | +30 B |
| `src/state/migration.ts` | v8 backfill — `Answer.pinned` / `Briefing.pinned` `?? false` 정규화 추가 | +40 B |
| `src/ui/handlers/archive.ts` | read-side `?? false` 9곳 제거 (write-side 정규화 후 dead) | −200 B |
| `src/state/user.ts` | migration chain re-order (v8 우선 prepend) + dead drop simplify | −80 B |
| `src/state/migration.ts` 외 | dead import sweep (`closeModal` precedent 확산) | −50 B |
| LOW utility / innerHTML indent (T5에서 grep으로 확정) | inline화 / template literal trim | −300 B (목표) |

**예상 누적 Δ: −620 B** (DoD −1231 B 대비 −611 B 미달 → T5에서 LOW surface 더 발굴 필요. T3·T4 LOW surface grep을 plan T0 단계에서 추가 측정.)

---

## Conventions (every task)

- **TS strict**: `npx tsc --noEmit` → 0 errors (모든 task 후)
- **Lint**: `npm run lint` → 0 errors (v3.14.4 graduated)
- **Vitest 영향 범위**: 변경된 spec만 우선 → cycle 끝(T6)에서 양쪽 TZ 전체 재확인
- **Bundle 측정**: `npm run build && gzip -c dist/assets/index-*.js | wc -c` (v3.14.5 canonical, vite reporter 사용 금지)
- **Commit**: `feat:`/`refactor:`/`fix:`/`docs:` conventional + `<scope>` 표기 (`v3.28 T<N>`)
- **per-task review**: large refactor (T2 / T5) 만 `pr-review-toolkit:code-reviewer` agent dispatch (v3.24 graduated: small task는 controller 자체 검토로 격하)

---

## Task 0: Spec/plan write + 사전 Codex review

**Status**: 이 plan 작성 시점에 진행 중.

- [ ] **Step 1: Plan v1 write**

이 파일 (`docs/superpowers/plans/2026-05-11-v3.28-trim-plan.md`) commit.

```bash
git add docs/superpowers/plans/2026-05-11-v3.28-trim-plan.md
git commit -m "docs(v3.28 T0): plan v1 — 8 task / trim 표면 grep / DoD ≤ 41500 B"
```

- [ ] **Step 2: 사전 Codex review dispatch**

`codex:codex-rescue` 또는 `Agent(subagent_type=codex:codex-rescue)` 로 spec+plan diff 전달, P0/P1/P2 분류 요청.

근거: CLAUDE.md "Codex 2-pass Review — graduate 명문화" (사이클 T0 mandatory dispatch).

CLI 한도 시 fallback: `pr-review-toolkit:code-reviewer` agent controller self-review (v3.14.5 / v3.20 / v3.24 precedent), retro에 명시.

- [ ] **Step 3: P0/P1 plan v2 in-cycle 흡수**

P0가 있으면 plan v2로 amend commit. T1 진입 전 완료 의무.

```bash
git add docs/superpowers/plans/2026-05-11-v3.28-trim-plan.md
git commit -m "docs(v3.28 T0): plan v2 — Codex 사전 review P<N> in-cycle 흡수"
```

- [ ] **Step 4: T3/T4/T5 LOW surface 추가 grep** (사용자 요청 시 또는 trim 목표 미달 우려 시)

```bash
# Migration silent drop (legacy v1 분기 dead 확인)
grep -n "schemaVersion === 1\|schemaVersion === 2" src/state/

# Dead import sweep (closeModal precedent)
grep -rn "^import" src/ | grep -oE "from '[^']+'" | sort | uniq -c | sort -rn | head -20

# Single-use const (LOW utility)
grep -rEn "^(const|export const) [A-Z_]+ = " src/state/ src/ui/ | head -30

# innerHTML template literal indent
grep -rEn "innerHTML\s*=\s*\`" src/ui/ | head -20
```

결과를 plan T3/T4/T5 task에 반영 (commit "plan v3" 또는 in-task amend).

---

## Task 1: CSS orphan sweep — `#insightsTab` (P2-1)

**Files:**
- Modify: `src/styles/layout.css:71` `:90` `:104` — `#insightsTab` selector 제거
- Modify: `src/styles/pages/stats.css:9` `archive.css:9` — comment ref 정리 (cosmetic)

**Verification**: smoke 97/97 (archive entity chip + tab nav 동작 확인).

- [ ] **Step 1: layout.css selector 제거**

`src/styles/layout.css:71` — comma-separated selector 그룹에서 `#insightsTab` token 제거.

```css
/* Before */
#homeTab,
#archiveTab,
#insightsTab,
#statsTab,
#settingsTab { ... }

/* After */
#homeTab,
#archiveTab,
#statsTab,
#settingsTab { ... }
```

`src/styles/layout.css:90` — `#insightsTab { ... }` 단독 rule 전체 제거.

`src/styles/layout.css:104` — 같은 패턴 (다른 selector 그룹 안의 `#insightsTab` token 제거).

실제 indent / 다른 selector는 `Read` 후 정확히 확인 — token만 제거.

- [ ] **Step 2: comment ref 정리 (stats.css:9, archive.css:9)**

`src/styles/pages/stats.css:9`:
```css
/* Before */
/* Total 80px = aligned with #homeTab / #archiveTab / #insightsTab / #settingsTab. */

/* After */
/* Total 80px = aligned with #homeTab / #archiveTab / #settingsTab. */
```

`src/styles/pages/archive.css:9` — 동일 패턴.

- [ ] **Step 3: TS strict + lint + smoke 영향 spec**

```bash
npx tsc --noEmit
npm run lint
npm test -- tests/smoke
```

기대: TS 0, lint 0, smoke spec 영향 없음 (PASS 유지).

- [ ] **Step 4: bundle 측정**

```bash
npm run build && gzip -c dist/assets/index-*.js | wc -c
```

기대: 42731 → ~42671 (−60 B). 기록.

- [ ] **Step 5: commit**

```bash
git add src/styles/layout.css src/styles/pages/stats.css src/styles/pages/archive.css
git commit -m "$(cat <<'EOF'
refactor(v3.28 T1): #insightsTab CSS orphan sweep (P2-1)

- src/styles/layout.css: dead selector 3곳 제거 (:71/:90/:104 — v3.27 T2a tab 폐기 잔재)
- src/styles/pages/stats.css:9 / archive.css:9: comment ref 동기화
- Bundle CLI gzip: 42731 → <measured> B
EOF
)"
```

---

## Task 2: `pinned` write-side normalize (P2-2)

**Files:**
- Modify: `src/state/schema.ts` — `makeAnswer` / `makeBriefing` helper에 `pinned: false` default (Insight는 v3.27 T1에 이미 normalize됨 — `migration.ts:417`)
- Modify: `src/state/migration.ts` — v8 backfill에 `Answer.pinned` / `Briefing.pinned` `?? false` 정규화 추가
- Modify: `src/ui/handlers/archive.ts` — read-side `?? false` 9곳 제거 (`:510`, `:556`, `:580~:582`, `:626`, `:648`, `:662`, `:674~:676`, `:716`)
- Test: `tests/state/migration.test.ts` 또는 `tests/state/schema.test.ts` — `pinned` default false 회귀 spec

**Verification**: vitest 영향 범위 (state/migration/archive) + 양쪽 TZ.

- [ ] **Step 1: 회귀 test 먼저 작성 (TDD red)**

`tests/state/schema.test.ts` 또는 신규 spec — `makeAnswer({...}).pinned === false` / `makeBriefing({...}).pinned === false`. migration v7→v8에서 `.pinned`가 undefined일 때 false로 정규화되는 spec.

```ts
import { describe, it, expect } from 'vitest';
import { makeAnswer, makeBriefing } from '../../src/state/schema';
import { migrateUser } from '../../src/state/migration';

describe('v3.28 T2: pinned write-side normalize', () => {
  it('makeAnswer should default pinned to false', () => {
    const a = makeAnswer({ id: 'a1', userId: 'u1', text: 'q', date: '2026-05-11', q: 'q' /* 실제 schema에 맞게 */ });
    expect(a.pinned).toBe(false);
  });

  it('makeBriefing should default pinned to false', () => {
    const b = makeBriefing({ id: 'b1', userId: 'u1', date: '2026-05-11', /* ... */ });
    expect(b.pinned).toBe(false);
  });

  it('migrate v7 user with .pinned undefined should normalize to false', () => {
    const v7 = { schemaVersion: 7, /* ... answers: [{id, pinned: undefined}] ... */ };
    const v8 = migrateUser(v7);
    expect(v8.answers[0].pinned).toBe(false);
  });
});
```

**Actual schema field names** — Step 1 실행 전에 `src/state/schema.ts` Read해서 `Answer` / `Briefing` 정확한 shape 확인 후 spec 작성.

- [ ] **Step 2: red 확인**

```bash
npm test -- tests/state/schema
```

기대: FAIL (makeAnswer/makeBriefing이 pinned default 없음).

- [ ] **Step 3: schema.ts에 default 박기**

`src/state/schema.ts` `makeAnswer` / `makeBriefing` 본문에서 `pinned: false` (input에 명시 없을 시) 추가. `Insight`는 이미 v3.27 T1에서 처리됨 — `migration.ts:417` 참고.

- [ ] **Step 4: migration.ts v8 backfill 추가**

`src/state/migration.ts` migrateUser v8 분기에서 `r.answers?.forEach(a => a.pinned = typeof a.pinned === 'boolean' ? a.pinned : false)` 정규화. `r.briefings` 동일.

- [ ] **Step 5: green 확인**

```bash
npm test -- tests/state/schema tests/state/migration
```

기대: PASS.

- [ ] **Step 6: archive.ts read-side `?? false` 9곳 제거**

`src/ui/handlers/archive.ts` 다음 9 위치에서 `?? false` 제거:

| line | before | after |
|---|---|---|
| :510 | `makePinButton('answer', a.id, a.pinned ?? false)` | `makePinButton('answer', a.id, a.pinned)` |
| :556 | `makePinButton('insight', i.id, i.pinned ?? false)` | `..., i.pinned)` |
| :580 | `answers.filter((a) => a.pinned).length` | (이미 truthy check, 변경 없음) |
| :626 | `card.append(makePinButton('scrap', b.id, b.pinned ?? false))` | `..., b.pinned)` |
| :648 | `sortPinThenDesc(pool, (b) => b.pinned ?? false, ...)` | `..., (b) => b.pinned, ...` |
| :662 | `sortPinThenDesc(pool, (i) => i.pinned ?? false, ...)` | `..., (i) => i.pinned, ...` |
| :674 | `pinned: a.pinned ?? false` | `pinned: a.pinned` |
| :675 | `pinned: b.pinned ?? false` | `pinned: b.pinned` |
| :676 | `pinned: i.pinned ?? false` | `pinned: i.pinned` |
| :716 | `sortPinThenDesc(filtered, (a) => a.pinned ?? false, ...)` | `..., (a) => a.pinned, ...` |

:580/:581/:582는 이미 `.pinned` truthy filter라 `?? false` 없음 (boolean 강제 후 truthy check 자연 동작).

`sortPinThenDesc`의 signature 확인 — 두 번째 인자가 `boolean` 강제 타입이면 변경 OK. `boolean | undefined` 허용이면 다른 처리 필요. T2 진입 전 `Read sortPinThenDesc` 확인.

- [ ] **Step 7: TS strict + lint + vitest 양쪽 TZ 영향 spec**

```bash
npx tsc --noEmit
npm run lint
TZ=Asia/Seoul npm test -- tests/state tests/ui/handlers/archive
TZ=America/New_York npm test -- tests/state tests/ui/handlers/archive
```

기대: 모두 PASS, 양쪽 TZ same count.

- [ ] **Step 8: per-task review dispatch (large refactor)**

```text
Agent(subagent_type=pr-review-toolkit:code-reviewer, prompt=v3.28 T2 diff 검토 — pinned write-side normalize. read-side `?? false` 9곳 제거가 invariant 보존하는지, sortPinThenDesc signature 정합인지, migration backfill이 v7→v8 lazy migrate 멱등성 만족하는지 점검. P0/P1/P2 분류.)
```

P0/P1 in-cycle fix 후 commit.

- [ ] **Step 9: bundle 측정**

```bash
npm run build && gzip -c dist/assets/index-*.js | wc -c
```

기대: 이전 측정값 → −200 B 내외.

- [ ] **Step 10: commit**

```bash
git add src/state/schema.ts src/state/migration.ts src/ui/handlers/archive.ts tests/state/
git commit -m "$(cat <<'EOF'
refactor(v3.28 T2): pinned write-side normalize (P2-2)

- schema.ts: makeAnswer/makeBriefing — pinned default false
- migration.ts: v8 backfill — Answer.pinned/Briefing.pinned ?? false 정규화
- archive.ts: read-side ?? false 9곳 제거 (write-side 정규화 후 dead)
- tests/state/schema: 3 new spec (write default + migrate backfill)
- Bundle CLI gzip: <prev> → <new> B
EOF
)"
```

---

## Task 3: Migration chain simplify + parser dead path (LOW surface)

**Files:**
- Modify: `src/state/user.ts:137` — `schemaVersion === 2 || 3 || ... || 8` chain re-order (v8 우선 prepend) + dead drop
- Modify: `src/state/user.ts:202` `:217` `:227` `:234` `:251` — 각 chain re-order
- (선택) Modify: v3.25 insight parser dead branch (T0 추가 grep 결과)

**Verification**: vitest state/user/migration + 양쪽 TZ.

- [ ] **Step 1: 회귀 test (TDD red) — 필요 시**

기존 migration spec이 v2~v8 chain을 검증하는지 확인. 부족하면 v8 단축 path 회귀 spec 추가.

```ts
it('v8 user should pass through migration without re-write', () => {
  const v8 = { schemaVersion: 8, /* valid shape */ };
  const result = migrateUser(v8);
  expect(result.schemaVersion).toBe(8);
});
```

- [ ] **Step 2: user.ts:137 chain re-order**

```ts
// Before
let user = (parsed?.schemaVersion === 2 || parsed?.schemaVersion === 3 || ... || parsed?.schemaVersion === 8)
  ? migrateUser(parsed)
  : ...;

// After (v8 우선 — 가장 흔한 path 짧게)
const v = parsed?.schemaVersion;
let user = (v === 8 || v === 7 || v === 6 || v === 5 || v === 4 || v === 3 || v === 2)
  ? migrateUser(parsed)
  : ...;
```

또는 `Array.includes` 단축:

```ts
const v = parsed?.schemaVersion;
let user = (typeof v === 'number' && v >= 2 && v <= 8)
  ? migrateUser(parsed)
  : ...;
```

후자가 더 짧음 — TS narrowing 보존되는지 확인. 안 되면 전자.

- [ ] **Step 3: :202 / :217 / :227 / :234 / :251 chain 단축**

각 위치의 `r.schemaVersion === N || N+1 || ...` chain을 range check (`r.schemaVersion >= N && r.schemaVersion <= 8`)로 압축.

TS narrowing이 깨지면 ternary나 `if` 분기로 유지. 표면이 좁으면 격하 (commit 0).

- [ ] **Step 4: (선택) parser dead path** — T0 추가 grep 결과 반영

v3.25 insight parser가 있다면 fallback dead branch 제거. surface 측정 후 ROI 평가.

- [ ] **Step 5: TS strict + lint + vitest 양쪽 TZ**

```bash
npx tsc --noEmit
npm run lint
TZ=Asia/Seoul npm test -- tests/state/user tests/state/migration
TZ=America/New_York npm test -- tests/state/user tests/state/migration
```

기대: PASS.

- [ ] **Step 6: bundle 측정**

기대: −80 B 내외.

- [ ] **Step 7: commit**

```bash
git add src/state/user.ts (parser 파일 등)
git commit -m "refactor(v3.28 T3): migration chain re-order — v8 우선 prepend / range check 압축"
```

---

## Task 4: Dead import sweep

**Files:**
- 전체 `src/` — T0 Step 4의 grep 결과로 확정
- precedent: `closeModal` (v3.27 T8 `archive.ts`)

**Verification**: TS strict (unused import는 자연 catch) + lint + vitest 전체.

- [ ] **Step 1: dead import grep**

```bash
# unused import 후보
npx tsc --noEmit  # TS6133 unused 자연 노출
# 또는 ESLint unused-imports rule (있으면 자동 fix)
npm run lint -- --rule '@typescript-eslint/no-unused-vars: error'
```

- [ ] **Step 2: side-effect-only import sweep**

```bash
grep -rEn "^import '[^']+';$" src/ | head -20
# CSS / 폴리필 / module side-effect import — 필요 vs dead 점검
```

각 entry 수동 점검 — dead면 제거, side-effect 의존이면 보존.

- [ ] **Step 3: 제거 + 단일 commit**

```bash
git add src/...
git commit -m "refactor(v3.28 T4): dead import sweep — <files N> entries"
```

- [ ] **Step 4: TS strict + lint + vitest 전체 영향 (smoke 제외)**

```bash
npx tsc --noEmit
npm run lint
npm test  # 빠른 KST 1회 — TZ NY는 T6 최종에서
```

기대: 모두 PASS.

- [ ] **Step 5: bundle 측정** — 기대 −50 B 내외.

---

## Task 5: LOW utility const inline + innerHTML indent compaction

**Files:**
- T0 Step 4의 grep 결과로 확정 — single-use const + template literal indent surface

**Verification**: vitest 영향 spec.

- [ ] **Step 1: T0 추가 grep 결과 반영**

T0 Step 4에서 측정한 LOW surface 목록 — 각 항목별 ROI (Δ B) 추정.

- single-use `const FOO = '...'` → caller 1곳 inline
- multi-line template literal `\``...indent...\`` → indent 제거 (gzip 압축 후 ROI 확인)
- helper 함수 단일 사용 — caller inline화 (단, 가독성 손실 시 격하)

- [ ] **Step 2: 항목별 변경**

각 항목별로 별도 step + commit (rollback granular).

예시 — `src/ui/handlers/settings.ts` template literal:

```ts
// Before
element.innerHTML = `
  <div class="foo">
    <span>${label}</span>
  </div>
`;

// After
element.innerHTML = `<div class="foo"><span>${label}</span></div>`;
```

ROI 측정: 변경 전후 build bundle gzip diff (file별).

- [ ] **Step 3: per-task review (large refactor)**

```text
Agent(subagent_type=pr-review-toolkit:code-reviewer, prompt=v3.28 T5 diff 검토 — LOW utility inline + innerHTML indent. 가독성 손실 / runtime semantics 변경 없는지 점검.)
```

- [ ] **Step 4: TS strict + lint + vitest 영향 spec**

- [ ] **Step 5: bundle 측정** — 누적 Δ가 −1231 B 근접하는지 확인. 미달 시 T6 진입 전 추가 grep + amend 또는 v3.29로 carry.

- [ ] **Step 6: commit (per-item)**

---

## Task 6: 사이클-끝 검증 + Codex 최종 review

**Files:**
- 없음 (검증 + retro 준비)

- [ ] **Step 1: 3종 검증 (양쪽 TZ + smoke)**

```bash
npx tsc --noEmit
npm run lint
TZ=Asia/Seoul npm test            # 1217 baseline
TZ=America/New_York npm test      # 1217 baseline same count
npm run test:smoke                # 97/97
```

기대: 모두 PASS, vitest 양쪽 TZ 동일 카운트.

- [ ] **Step 2: 최종 bundle 측정 + DoD 확인**

```bash
npm run build && gzip -c dist/assets/index-*.js | wc -c
```

기대: **≤ 41500 B** (DoD 만족). 미달 시 risk R3 — v3.29로 잔여 carry-forward 격하 명시.

- [ ] **Step 3: Codex 최종 review dispatch**

`codex:codex-rescue` 또는 fallback `pr-review-toolkit:code-reviewer` — 사이클 전체 diff 검토. `DEPLOY_APPROVED` / `APPROVED_WITH_NOTES` / `REJECT` 분류 요청.

CLI 한도 fallback: controller self-review (v3.20 / v3.24 precedent), retro에 명시.

- [ ] **Step 4: P0 in-cycle fix** — P0가 있으면 deploy 게이트 차단, 수정 후 재검증.

P1/P2는 v3.29 carry-forward 격하.

---

## Task 7: Retro + 배포 commit

**Files:**
- Create: `.claude/completions/2026-05-11-v3.28-trim.md` (CLAUDE.md template)
- Update: `~/.claude/projects/.../memory/project_daily_growth_v328.md` + MEMORY.md 인덱스 1줄
- Update (필요 시): `.claude/COMMON_MISTAKES.md` 등 lessons graduate

- [ ] **Step 1: retro doc 작성**

CLAUDE.md "Cycle Workflow" 표준 retro 항목:
- 사이클 요약 (task / commit / vitest / smoke / bundle / TS strict / lint)
- 핵심 변경
- 신규 lessons (있으면)
- 신규 P2 carry-forward (있으면)
- 차기 사이클 후보

- [ ] **Step 2: memory project file + MEMORY.md update**

`~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v328.md` 작성. MEMORY.md 인덱스에 1줄 추가 (≤ 200 chars).

- [ ] **Step 3: vault sync** (CLAUDE.md "Obsidian Vault Sync 정책")

```bash
VAULT="/Users/hayden/Hayden/20_Projects/Dev_Project/SJ_Daily Growth(Ai assistant)"
cp docs/superpowers/specs/2026-05-11-v3.28-trim-design.md "$VAULT/02-Cycles/"
cp ~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v328.md "$VAULT/03-Lessons/"
cp ~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md "$VAULT/03-Lessons/"
# 00-Index.md 사이클 타임라인 1줄 추가는 controller가 수동 (vault에 사용자 추가 노트 보존)
```

- [ ] **Step 4: retro commit**

```bash
git add .claude/completions/2026-05-11-v3.28-trim.md
git commit -m "docs(v3.28 T7 retro): bundle <Δ> B / vitest <N> / smoke 97 / Codex 최종 <verdict>"
```

- [ ] **Step 5: 사용자 "배포해줘" 명시 대기 → firebase deploy**

```bash
firebase deploy --only hosting
```

⚠️ **사용자 명시 승인 전 실행 금지** (CLAUDE.md 절대 규칙).

배포 후 live URL 확인 + memory project file에 "✅ 배포 완료 (yyyy-mm-dd, main HEAD `<hash>`, Firebase live)" 업데이트.

---

## DoD (사이클 끝 일괄 확인)

- [ ] bundle CLI gzip ≤ **41500 B** (현재 42731, Δ ≥ **−1231 B**)
- [ ] TS strict 0
- [ ] Lint baseline 유지 (0 errors 또는 baseline 동일)
- [ ] Vitest 양쪽 TZ regression 0 (1217 baseline same count)
- [ ] Smoke 97/97
- [ ] Codex 사전 review dispatch (또는 fallback 명시)
- [ ] Codex 최종 review dispatch (또는 fallback 명시)
- [ ] retro doc + memory + MEMORY.md + vault sync

---

## Risk

| # | Risk | Mitigation |
|---|---|---|
| R1 | dead import 제거가 dynamic import 회귀 유발 | T4 Step 2에서 side-effect import 수동 점검, 의심 시 보존 |
| R2 | CSS orphan 제거가 historic browser cache 회귀 | Vite asset hash로 자연 해소 |
| R3 | trim 목표 미달 (LOW surface 좁음) | T5에서 누적 Δ 측정, 부족하면 v3.29로 잔여 격하 — DoD `APPROVED_WITH_NOTES` 허용 |
| R4 | `pinned` write-side normalize에서 `sortPinThenDesc` signature mismatch | T2 Step 6 전에 signature 확인, narrow type 필요 시 helper signature 같이 수정 |
| R5 | migration chain range check (`v >= 2 && v <= 8`)로 narrowing 깨짐 | TS strict가 catch — narrowing 깨지면 chain 유지 또는 explicit assertion |

---

## Self-Review (plan 작성 후 checklist)

**1. Spec coverage:**
- spec §1.2 P2-1 → T1 ✅
- spec §1.2 P2-2 → T2 ✅
- spec §1.2 v3.24/v3.26 bundle GREEN carry → T1~T5 누적 ✅
- spec §1.3 후보 grep → T0 Step 4 + T3/T4/T5 ✅
- spec §3 DoD → T6 + 최종 DoD checklist ✅

**2. Placeholder 검사:**
- "TBD" / "TODO" / "implement later" / "기타" — plan body 안에 없음 ✅
- T0 Step 4 / T3 (선택) / T5 — "T0 추가 grep 결과 반영"으로 명시. grep 명령은 실제 명령 (placeholder 아님).
- 단 T2 Step 1의 spec body는 "실제 schema에 맞게" 표기 — `Read src/state/schema.ts` 후 정확한 field로 채워야 함. **T2 Step 1 진입 전 schema.ts Read 필수**.

**3. Type consistency:**
- `makeAnswer` / `makeBriefing` / `migrateUser` / `sortPinThenDesc` — 모두 동일 이름 사용 ✅
- `Answer.pinned` / `Briefing.pinned` / `Insight.pinned` — schema.ts 일관 ✅

**4. Plan ↔ Spec 정합:**
- spec §2 task table T0~T7 → plan T0~T7 동일 ✅
- spec §2 "예상 누적 Δ −830 B"은 spec 시점 추정 — plan에서 −620 B로 보수 보정. 실측은 사이클 중. ✅
- chat-bubble은 grep으로 active code 확인 → spec §1.3 후보에서 제외. T0 Step 4 grep 결과로 plan v2 amend 가능.

---

**작성**: 2026-05-11
**baseline**: v3.27 deploy 직후 (CLI gzip 42731 B, vitest 1217, smoke 97/97)
**디스크립터**: dedicated bundle trim — 누적 drift 회수
