# v3.28 Bundle Trim Implementation Plan (v2 — Codex 사전 review P0=5 + P1=4 흡수)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v3.27까지 누적된 bundle drift 회수 — CLI gzip **42731 B → ≤ 41500 B** (Δ ≥ **−1231 B**). T5 surface 측정 결과 미달 시 잔여를 v3.29로 carry-forward + `APPROVED_WITH_NOTES` 허용.

**Architecture:** Dead/orphan 제거 + **Option A — type narrow + 3-layer backfill** → archive.ts read-side `?? false` 9곳 제거 + migration chain typed-guarded re-order + LOW utility/innerHTML 실측 inline. **신규 기능 0 / runtime schema 변경 0 / CSP 0 / API 변경 0**. type 좁힘은 compile-time invariant 강화만 — runtime shape는 migration backfill로 보장. main 직배포 패턴.

**Tech Stack:** Vite + TypeScript / Vitest (양쪽 TZ KST+NY) / Playwright smoke 97/97 / Firebase Hosting.

**Baseline (2026-05-11)**: v3.27 deploy tip 직후 build — CLI gzip **42731 B** / vitest 1217 / smoke 97/97 / TS strict 0 / lint baseline.

**Spec**: [docs/superpowers/specs/2026-05-11-v3.28-trim-design.md](../specs/2026-05-11-v3.28-trim-design.md) (amend `cbd4de6`)

**Codex 사전 review verdict**: `REQUIRES_FIX` (P0=5 + P1=4 모두 in-cycle 흡수 — 이 plan v2가 그 흡수본).

---

## Plan v1 → v2 변경 요약 (Codex P0/P1 흡수)

| # | Codex 지적 | plan v2 흡수 |
|---|---|---|
| P0-1 | `makeBriefing` 미존재 | T2에서 `makeBriefing` 참조 제거. Briefing은 `loadBriefings` map 단계의 copy-based backfill로 처리 (briefings.ts:40-49 amend). |
| P0-2 | `migrateUserToV8`은 User만 처리 (answers/briefings 도달 X) | T2 backfill을 `migrateAnswer` (migration.ts:11-44) + `loadBriefings` (briefings.ts:40-49) 양쪽에 배치. |
| P0-3 | in-place `forEach` 가 v8 same-ref early-return의 idempotency 깨뜨림 | copy-based만 사용 (`{ ...r, pinned: ... }`). pass-through 분기도 copy로 변경. |
| P0-4 | `?? false` 9곳 제거 시 TS strict fail (`pinned?: boolean`) | **Option A**: Answer/Briefing/Insight `pinned?: boolean` → `pinned: boolean` 타입 좁힘 → archive.ts `makePinButton(...,pinned: boolean)` / `sortPinThenDesc(getPinned: (t) => boolean)` 자연 만족. |
| P0-5 | T5 `−300 B` 표면 측정 안 됨 (DoD 미달 risk) | T0 Step 4에 LOW surface grep mandatory. 미달 시 `APPROVED_WITH_NOTES` + 잔여 v3.29 carry — Risk R3 명문화. |
| P1-1 | T3 range check 시 `typeof v === 'number'` guard 필요 | T3 Step 2에 `const v = typeof parsed?.schemaVersion === 'number' ? parsed.schemaVersion : -1` 가드 추가. |
| P1-2 | spec ↔ plan task mapping 불일치 | spec §2 amend (`cbd4de6`)로 plan T3~T7 desc 동기화 완료. |
| P1-3 | chat-bubble 제거 시 home.ts call site 처리 | spec §1.3에서 chat-bubble active 명시 → 후보 제외 (no-op). |
| P1-4 | parser dead path는 spec coverage 확인 후 제거 | T3는 migration chain만 처리. parser dead는 별도 branch-level proof 필요 시 T4 또는 v3.29로 격하. |

---

## File Structure (실측 기반)

| 파일 | 역할 | 예상 Δ |
|---|---|---|
| `src/styles/layout.css` | `#insightsTab` orphan 3 selector 제거 (`:71/:90/:104`) | −60 B |
| `src/styles/pages/stats.css` / `archive.css` | comment ref 정리 (cosmetic) | ±0 |
| `src/state/schema.ts:23-24` | `Answer.pinned?: boolean` → `pinned: boolean` + `makeAnswer` default false | +30 B |
| `src/state/briefings.ts:26-27, 36-53` | `Briefing.pinned?: boolean` → `pinned: boolean` + `loadBriefings` copy-based backfill | +40 B |
| `src/state/user.ts` (Insight type 위치) | `Insight.pinned?: boolean` → `pinned: boolean` | ±0 (이미 v3.27 T1에서 v8 backfill 존재) |
| `src/state/migration.ts:11-44` | `migrateAnswer` 본문에 pinned 정규화 (copy-based, pass-through 분기 포함) | +40 B |
| `src/ui/handlers/archive.ts` | read-side `?? false` 9곳 제거 (`:510, :556, :626, :648, :662, :674-676, :716`) | −200 B |
| `src/state/user.ts:137, 202, 217, 227, 234, 251` | typed schema-version guard + chain re-order/range check (6 위치) | −80 B |
| 전체 src dead import sweep | TS6133 + side-effect-only 수동 점검 | −50 B |
| LOW utility / innerHTML | T0 Step 4 mandatory grep 결과로 확정 | 실측 |

**확정 budget Δ: −390 B** (DoD −1231 B 대비 −841 B 부족 — **T5 surface가 결정 변수**. 미달 시 R3 적용).

---

## Conventions (every task)

- **TS strict**: `npx tsc --noEmit` → 0 errors (모든 task 후)
- **Lint**: `npm run lint` → 0 errors (v3.14.4 graduated)
- **Vitest 영향 범위**: 변경된 spec만 우선 → T6에서 양쪽 TZ 전체 재확인
- **Bundle 측정**: `npm run build && gzip -c dist/assets/index-*.js | wc -c` (v3.14.5 canonical, vite reporter 사용 금지)
- **Commit**: `feat:`/`refactor:`/`fix:`/`docs:` conventional + `<scope>` 표기 (`v3.28 T<N>`)
- **per-task review**: large refactor (T2 / T5) 만 `pr-review-toolkit:code-reviewer` agent dispatch

---

## Task 0: Spec/plan write + Codex 사전 review (진행 중 — completion 단계)

- [x] **Step 1: Plan v1 write + commit** (`db3536f`)
- [x] **Step 2: Codex 사전 review dispatch** — `codex:codex-rescue` `REQUIRES_FIX` P0=5 + P1=4
- [x] **Step 3a: Spec amend** (`cbd4de6`) — §1.3 chat-bubble active 표기 + §2 task desc 동기화
- [x] **Step 3b: Plan v2 write + commit** — 이 파일 (다음 commit)

- [ ] **Step 4: T5 LOW surface grep mandatory** (T1 진입 전 또는 T5 진입 직전에 실측)

```bash
# Single-use const (LOW utility — caller 1곳이면 inline 후보)
grep -rEn "^(export )?const [A-Z_]+ ?= ?['\"]" src/state/ src/ui/ src/services/ | head -40

# innerHTML multi-line template literal (indent 압축 후보)
grep -rEnB1 'innerHTML\s*=\s*`$' src/ui/ | head -40

# Long string literal duplicates (dedup 후보)
grep -rEn "'[a-z-]{15,}'" src/ui/ src/state/ | sort -t: -k3 | uniq -c -f2 | sort -rn | head -20

# Side-effect-only import (T4 자료)
grep -rEn "^import '[^']+';$" src/ | head -20
```

측정 결과 surface가 −300 B 이상이면 T5 진행. 미만이면 plan v3 amend로 `APPROVED_WITH_NOTES` + 잔여 v3.29 carry로 격하.

---

## Task 1: CSS orphan sweep — `#insightsTab` (P2-1)

**Files:**
- Modify: `src/styles/layout.css:71` `:90` `:104` — `#insightsTab` selector 제거
- Modify: `src/styles/pages/stats.css:9`, `src/styles/pages/archive.css:9` — comment ref 정리

**Verification**: TS strict / lint / smoke 97/97.

- [ ] **Step 1: layout.css selector 제거**

`Read src/styles/layout.css` 후 정확한 token만 제거:
- `:71` — selector 그룹 (`#homeTab, #archiveTab, #insightsTab, #statsTab, #settingsTab`) 에서 `#insightsTab,` 1줄 (또는 그룹 형태 따라) 제거
- `:90` — `#insightsTab { ... }` 단독 rule 전체 제거
- `:104` — `:71`과 동일 패턴

- [ ] **Step 2: comment ref 정리**

`src/styles/pages/stats.css:9` / `archive.css:9` — 주석 안의 `#insightsTab` token 제거 (cosmetic).

- [ ] **Step 3: TS strict + lint + smoke 영향 spec**

```bash
npx tsc --noEmit
npm run lint
npm test -- tests/smoke
```

기대: TS 0 / lint 0 / smoke PASS 유지.

- [ ] **Step 4: bundle 측정**

```bash
npm run build && gzip -c dist/assets/index-*.js | wc -c
```

기대: 42731 → ~42671 B (−60 B).

- [ ] **Step 5: commit**

```bash
git add src/styles/layout.css src/styles/pages/stats.css src/styles/pages/archive.css
git commit -m "refactor(v3.28 T1): #insightsTab CSS orphan sweep (P2-1) — 3 dead selector + comment ref"
```

---

## Task 2: pinned write-side normalize — Option A (P2-2)

**전체 흐름** (Codex P0-1~P0-4 흡수):

1. Type narrow: `Answer.pinned?: boolean` → `pinned: boolean` (schema.ts / briefings.ts / user.ts Insight)
2. Write-side default: `makeAnswer`에 `pinned: false` default
3. 3-layer copy-based backfill:
   - `migrateAnswer` (migration.ts:11-44) — pass-through 분기 + legacy 분기 모두 copy로 정규화
   - `loadBriefings` (briefings.ts:40-49) — map 단계에 copy + pinned 정규화
   - `migrateUserToV8` (migration.ts) — Insight.pinned는 이미 copy-based (P0-3 conform, 유지)
4. archive.ts read-side `?? false` 9곳 제거 (TS strict narrow된 후 자연 통과)
5. idempotency + write default + backfill 회귀 test

**Files:**
- Modify: `src/state/schema.ts:24, 33` — type narrow + `makeAnswer` default
- Modify: `src/state/briefings.ts:27, 40-49` — type narrow + `loadBriefings` backfill
- Modify: `src/state/user.ts` (Insight type 위치) — type narrow
- Modify: `src/state/migration.ts:11-44` — `migrateAnswer` 본문 pinned 정규화 (copy-based)
- Modify: `src/ui/handlers/archive.ts:510, 556, 626, 648, 662, 674-676, 716` — read-side `?? false` 제거
- Test: 신규 또는 기존 spec — `tests/state/schema.test.ts` 또는 `tests/state/migration.test.ts` 또는 `tests/state/persistence.test.ts` 또는 `tests/state/briefings.test.ts`. 기존 spec 위치 진입 전 grep로 확정.

- [ ] **Step 1: 기존 spec 위치 grep**

```bash
ls tests/state/ tests/unit/ 2>/dev/null
grep -rEn "makeAnswer|migrateAnswer|loadBriefings" tests/ | head -20
```

→ 적절한 spec 파일에 추가 (신설 가능 — `tests/state/pinned-normalize.spec.ts`).

- [ ] **Step 2: 회귀 test 작성 (TDD red)**

신규 spec — 4 test:

```ts
import { describe, it, expect } from 'vitest';
import { makeAnswer, type Answer } from '../../src/state/schema';
import { migrateAnswer } from '../../src/state/migration';
import { loadBriefings, saveBriefings, type Briefing } from '../../src/state/briefings';

describe('v3.28 T2: pinned write-side normalize', () => {
  it('makeAnswer should default pinned to false when not provided', () => {
    const a = makeAnswer({
      id: 'a1',
      questionId: 'q1',
      text: '답변',
      authorId: 'self',
    });
    expect(a.pinned).toBe(false);
  });

  it('makeAnswer should respect explicit pinned=true', () => {
    const a = makeAnswer({
      id: 'a2',
      questionId: 'q1',
      text: '답변',
      authorId: 'self',
      pinned: true,
    });
    expect(a.pinned).toBe(true);
  });

  it('migrateAnswer pass-through branch should backfill pinned to false (copy-based, no mutation)', () => {
    const v1 = { schemaVersion: 1, id: 'a3', questionId: 'q', text: 't', authorId: 'self', createdAt: '2026-05-11T00:00:00.000Z' };
    const out = migrateAnswer(v1);
    expect(out.pinned).toBe(false);
    expect((v1 as { pinned?: boolean }).pinned).toBeUndefined(); // 원본 불변 (P0-3 idempotency)
  });

  it('loadBriefings should backfill pinned to false for legacy entries (copy-based)', () => {
    localStorage.setItem('briefings', JSON.stringify([
      { id: 'b1', date: '2026-05-11', url: 'https://x', title: 't', summary: 's', scrapped: false, read: false, memo: '' },
    ]));
    const list = loadBriefings();
    expect(list[0]?.pinned).toBe(false);
    // re-load idempotent
    saveBriefings(list);
    const list2 = loadBriefings();
    expect(list2[0]?.pinned).toBe(false);
  });
});
```

`tests/state/briefings.test.ts`가 이미 있으면 거기 추가. 없으면 신설.

- [ ] **Step 3: red 확인**

```bash
npm test -- tests/state
```

기대: 4 FAIL (makeAnswer default 없음 / migrateAnswer backfill 없음 / loadBriefings backfill 없음).

- [ ] **Step 4: schema.ts type narrow + `makeAnswer` default**

```ts
// src/state/schema.ts:24
/** v3.28 T2: pinned write-side normalize (P2-2). default false. */
pinned: boolean;

// src/state/schema.ts:33
export function makeAnswer(input: Omit<Answer, 'schemaVersion' | 'createdAt'>): Answer {
  return {
    pinned: false,
    ...input,
    schemaVersion: CURRENT_SCHEMA_VERSION,
    createdAt: new Date().toISOString(),
  };
}
```

`pinned: false` spread 앞에 배치 → input이 `pinned`를 명시하면 override, 아니면 false default.

- [ ] **Step 5: briefings.ts type narrow + `loadBriefings` backfill**

```ts
// src/state/briefings.ts:27
/** v3.28 T2: pinned write-side normalize (P2-2). default false. */
pinned: boolean;

// src/state/briefings.ts:36-53 — loadBriefings copy-based backfill
export function loadBriefings(): Briefing[] {
  try {
    const raw = JSON.parse(localStorage.getItem(KEY) ?? '[]') as unknown;
    if (!Array.isArray(raw)) return [];
    return raw.map((b) => {
      const briefing = b as Briefing & { pinned?: boolean };
      const normalized: Briefing = {
        ...briefing,
        pinned: typeof briefing.pinned === 'boolean' ? briefing.pinned : false,
      };
      // Drop imageUrl only if present-but-invalid; leave undefined alone.
      if (normalized.imageUrl !== undefined && !isHttpsUrl(normalized.imageUrl)) {
        delete normalized.imageUrl;
      }
      return normalized;
    });
  } catch {
    return [];
  }
}
```

Briefing 생성 경로 (`home.ts` RSS fetch 등)에서 `pinned: false`를 명시할지는 별도 — `loadBriefings` backfill이 가장 안전한 boundary. 단, 신규 briefing append 시점에 `pinned`가 빠지면 type-time error → TS strict가 catch (호출 site grep + 보완).

```bash
# 신규 briefing append 시점 grep
grep -rn "scrapped:\s*false\|read:\s*false" src/ui/ src/services/ | head -10
```

각 위치에 `pinned: false` 추가.

- [ ] **Step 6: user.ts Insight type narrow**

`src/state/user.ts` Insight type 정의 위치 grep 후 `pinned?: boolean` → `pinned: boolean`. v8 migration backfill은 `migration.ts:412-419`에 이미 copy-based 존재 → P0-3 conform.

- [ ] **Step 7: migration.ts `migrateAnswer` 본문 pinned 정규화 (copy-based)**

```ts
// src/state/migration.ts:11-44
export function migrateAnswer(raw: unknown): Answer {
  const r = (raw ?? {}) as Partial<Answer> & Record<string, unknown>;

  // Already Phase B + has text → copy + pinned backfill (P0-3: no in-place mutation)
  if (isVersioned(r) && r.schemaVersion === CURRENT_SCHEMA_VERSION && typeof r.text === 'string') {
    return {
      ...(r as Answer),
      pinned: typeof r.pinned === 'boolean' ? r.pinned : false,
    };
  }

  // Legacy v2.0 branch — return object literal에 pinned 추가
  // ... (기존 legacy mapping 그대로)
  return {
    id: String(r.id ?? `a_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`),
    questionId: String(r.questionId ?? ''),
    text: String(r.text ?? legacyAnswer),
    authorId: String(r.authorId ?? 'self'),
    createdAt,
    type: legacyType ?? (typeof r.type === 'string' ? (r.type as string) : undefined),
    evaluation: legacyEval ?? (r.evaluation as { score: number; feedback: string } | undefined),
    date: legacyDate || (typeof r.date === 'string' ? (r.date as string) : undefined),
    pinned: typeof r.pinned === 'boolean' ? r.pinned : false,
    schemaVersion: CURRENT_SCHEMA_VERSION,
  };
}
```

- [ ] **Step 8: green 확인**

```bash
npm test -- tests/state
```

기대: 4 PASS + 기존 spec regression 0.

- [ ] **Step 9: archive.ts read-side `?? false` 9곳 제거**

`src/ui/handlers/archive.ts` 9 위치:

| line | before | after |
|---|---|---|
| :510 | `makePinButton('answer', a.id, a.pinned ?? false)` | `makePinButton('answer', a.id, a.pinned)` |
| :556 | `makePinButton('insight', i.id, i.pinned ?? false)` | `..., i.pinned)` |
| :626 | `makePinButton('scrap', b.id, b.pinned ?? false)` | `..., b.pinned)` |
| :648 | `sortPinThenDesc(pool, (b) => b.pinned ?? false, ...)` | `..., (b) => b.pinned, ...` |
| :662 | `sortPinThenDesc(pool, (i) => i.pinned ?? false, ...)` | `..., (i) => i.pinned, ...` |
| :674 | `pinned: a.pinned ?? false` | `pinned: a.pinned` |
| :675 | `pinned: b.pinned ?? false` | `pinned: b.pinned` |
| :676 | `pinned: i.pinned ?? false` | `pinned: i.pinned` |
| :716 | `sortPinThenDesc(filtered, (a) => a.pinned ?? false, ...)` | `..., (a) => a.pinned, ...` |

`:580/:581/:582`는 이미 `.pinned` truthy filter — 변경 없음.

`makePinButton(...pinned: boolean)` / `sortPinThenDesc(...getPinned: (t) => boolean)` 모두 strict `boolean` signature — type narrow 후 자연 통과.

- [ ] **Step 10: TS strict + lint + vitest 양쪽 TZ**

```bash
npx tsc --noEmit
npm run lint
TZ=Asia/Seoul npm test -- tests/state tests/ui/handlers
TZ=America/New_York npm test -- tests/state tests/ui/handlers
```

기대: 모두 PASS, 양쪽 TZ same count.

- [ ] **Step 11: per-task review dispatch (large refactor)**

```
Agent(subagent_type=pr-review-toolkit:code-reviewer, prompt=v3.28 T2 diff 검토 — pinned Option A (type narrow + 3-layer copy-based backfill). 다음 invariant 점검:
1. migrateAnswer pass-through 분기가 copy-based로 원본 불변
2. loadBriefings map의 normalized return이 imageUrl validate와 정합
3. archive.ts 9곳 제거 후 TS strict + runtime 일관
4. 신규 briefing append 시점에 pinned: false 명시되었는지
5. idempotency: 반복 load → save → load same shape

P0/P1/P2 분류.)
```

P0/P1 in-cycle fix 후 commit.

- [ ] **Step 12: bundle 측정**

기대: 이전 측정값 → −200 B 내외 (read-side 9곳 제거 + type 좁힘 + backfill 추가의 net).

- [ ] **Step 13: commit**

```bash
git add src/state/schema.ts src/state/briefings.ts src/state/user.ts src/state/migration.ts src/ui/handlers/archive.ts src/ui/ src/services/ tests/state/
git commit -m "$(cat <<'EOF'
refactor(v3.28 T2): pinned write-side normalize Option A (P2-2)

- schema/briefings/user: Answer.pinned/Briefing.pinned/Insight.pinned optional→required boolean (compile-time invariant)
- makeAnswer: pinned default false (input override 허용)
- migrateAnswer: pass-through + legacy 분기 모두 copy-based backfill (P0-3 idempotency)
- loadBriefings: map 단계에 copy-based pinned 정규화
- archive.ts: read-side ?? false 9곳 제거 (TS strict narrow된 후 자연 통과)
- 신규 briefing append 시점 N곳: pinned: false 명시
- tests/state: 4 신규 spec (default / explicit / pass-through copy / load idempotent)
- Bundle CLI gzip: <prev> → <new> B

Codex 사전 review P0-1~P0-4 흡수 (db3536f → cbd4de6 → 본 commit)
EOF
)"
```

---

## Task 3: Migration chain typed guard + re-order (P1-1 흡수)

**Files:**
- Modify: `src/state/user.ts:137, 202, 217, 227, 234, 251` — typed schema-version guard + chain re-order

**Verification**: vitest state/user/migration + 양쪽 TZ.

- [ ] **Step 1: 회귀 test 확인**

```bash
grep -rEn "schemaVersion === [0-9]" tests/ | head -10
```

기존 migration spec이 v2~v8 chain을 검증하는지 확인. 부족하면 v8 단축 path + invalid schemaVersion guard 회귀 spec 추가.

- [ ] **Step 2: user.ts:137 typed guard + range check**

```ts
// Before
let user = (parsed?.schemaVersion === 2 || parsed?.schemaVersion === 3 || parsed?.schemaVersion === 4 || parsed?.schemaVersion === 5 || parsed?.schemaVersion === 6 || parsed?.schemaVersion === 7 || parsed?.schemaVersion === 8)
  ? migrateUser(parsed)
  : ...;

// After
const sv = typeof parsed?.schemaVersion === 'number' ? parsed.schemaVersion : -1;
let user = (sv >= 2 && sv <= 8)
  ? migrateUser(parsed)
  : ...;
```

`Record<string, unknown>` narrowing 보존 (P1-1 가드).

- [ ] **Step 3: :202 / :217 / :227 / :234 / :251 chain 압축**

각 위치도 동일 패턴:

```ts
// Before (예: :202)
if (r.schemaVersion === 4 || r.schemaVersion === 5 || r.schemaVersion === 6 || r.schemaVersion === 7 || r.schemaVersion === 8) { ... }

// After
const sv = typeof r.schemaVersion === 'number' ? r.schemaVersion : -1;
if (sv >= 4 && sv <= 8) { ... }
```

각 chain은 시작 v가 다르므로 (4, 5, 6, 7, 8) range lower bound 조정. `sv` 변수는 scope 안에서 한 번 선언하고 재사용 (또는 helper 함수 도입 시 +B 비용 점검).

TS narrowing이 깨지면 explicit assertion or chain 유지. 안 깨지면 OK.

- [ ] **Step 4: TS strict + lint + vitest 양쪽 TZ**

```bash
npx tsc --noEmit
npm run lint
TZ=Asia/Seoul npm test -- tests/state/user tests/state/migration
TZ=America/New_York npm test -- tests/state/user tests/state/migration
```

기대: PASS.

- [ ] **Step 5: bundle 측정**

기대: −80 B 내외.

- [ ] **Step 6: commit**

```bash
git add src/state/user.ts
git commit -m "refactor(v3.28 T3): migration chain typed-guard + range check 압축 (P1-1)"
```

---

## Task 4: Dead import sweep

**Files:**
- 전체 `src/` — T0 Step 4 grep 결과 또는 TS6133 unused-vars rule 활용
- precedent: `closeModal` (v3.27 T8 `archive.ts`)

**Verification**: TS strict (unused import 자연 catch) + lint + vitest 전체.

- [ ] **Step 1: dead import grep**

```bash
# TS6133 unused 자연 노출
npx tsc --noEmit 2>&1 | grep -E "TS6133|TS6196" | head -30
# ESLint unused-imports
npm run lint 2>&1 | grep -E "no-unused-vars|unused-imports" | head -30
```

- [ ] **Step 2: side-effect-only import 점검**

```bash
grep -rEn "^import '[^']+';$" src/ | head -20
```

각 entry 수동 점검 — dead면 제거, side-effect 의존 (CSS / 폴리필) 이면 보존.

- [ ] **Step 3: 제거 + commit**

```bash
git add src/...
git commit -m "refactor(v3.28 T4): dead import sweep — <files N> entries"
```

- [ ] **Step 4: TS strict + lint + vitest 빠른 KST 1회**

```bash
npx tsc --noEmit
npm run lint
npm test  # KST baseline; TZ NY는 T6에서
```

기대: PASS.

- [ ] **Step 5: bundle 측정** — 기대 −50 B 내외.

---

## Task 5: LOW utility const inline + innerHTML indent (T0 grep mandatory 후 진행)

**전제**: T0 Step 4 LOW surface 측정 결과가 **누적 −300 B 이상**일 때만 진행. 미만이면 plan v3 amend로 격하 (잔여 v3.29 carry, `APPROVED_WITH_NOTES` 허용).

**Files:**
- T0 Step 4 결과로 확정. innerHTML one-line은 home.ts:8 / settings.ts:37 이미 처리됨 — 다른 후보 발굴 필요.

- [ ] **Step 1: T0 grep 결과 정리**

surface별 ROI 추정 (B 단위):
- `const A = '...'` (single-use 18+자 literal): caller 1곳 inline 시 declaration overhead 회수
- `\`...indent...\`` template literal: indent 제거 (gzip 압축 효과 측정 — raw vs gzip diff)
- duplicate string literal (15+ char × 2+ 곳): const 추출 → caller 수만큼 회수
- helper 함수 (10 lines 미만, single call site): caller inline → declaration overhead 회수

- [ ] **Step 2: 항목별 변경 + commit (per-item granular rollback)**

surface가 발굴되면 항목별 별도 commit (rollback 단위).

가독성 손실 시 격하 (commit 0).

- [ ] **Step 3: per-task review dispatch (large refactor)**

```
Agent(subagent_type=pr-review-toolkit:code-reviewer, prompt=v3.28 T5 diff 검토 — LOW utility inline + innerHTML indent. 가독성 / runtime semantics 변경 / type 안전성 점검. P0/P1/P2 분류.)
```

- [ ] **Step 4: TS strict + lint + vitest 영향 spec**

- [ ] **Step 5: bundle 누적 측정**

T1+T2+T3+T4+T5 누적 Δ 측정. DoD 41500 B 도달 시 T6 진입. 미달이면 plan v3 amend (잔여 v3.29 carry + `APPROVED_WITH_NOTES`).

---

## Task 6: 사이클-끝 검증 + Codex 최종 review

**Files:**
- 없음 (검증 + retro 준비)

- [ ] **Step 1: 3종 검증 (양쪽 TZ + smoke)**

```bash
npx tsc --noEmit
npm run lint
TZ=Asia/Seoul npm test            # 1217 baseline
TZ=America/New_York npm test      # 1217 baseline same count
npm run test:smoke                # 97/97
```

기대: 모두 PASS, vitest 양쪽 TZ 동일 카운트 (T2의 4 신규 spec으로 1221 예상).

- [ ] **Step 2: 최종 bundle 측정 + DoD 평가**

```bash
npm run build && gzip -c dist/assets/index-*.js | wc -c
```

DoD 41500 B 미달 시: plan v3 amend로 잔여 v3.29 carry 격하 + 최종 review verdict `APPROVED_WITH_NOTES` 허용. P0-5 mitigation 발동.

- [ ] **Step 3: Codex 최종 review dispatch**

`codex:codex-rescue` — 사이클 전체 diff 검토. `DEPLOY_APPROVED` / `APPROVED_WITH_NOTES` / `REJECT` 분류.

CLI 한도 fallback: `pr-review-toolkit:code-reviewer` self-review (v3.20 / v3.24 precedent), retro에 명시.

- [ ] **Step 4: P0 in-cycle fix** — P0가 있으면 deploy 게이트 차단. P1/P2는 v3.29 carry-forward 격하.

---

## Task 7: Retro + 배포 commit

**Files:**
- Create: `.claude/completions/2026-05-11-v3.28-trim.md`
- Update: `~/.claude/projects/.../memory/project_daily_growth_v328.md` + MEMORY.md 인덱스 1줄

- [ ] **Step 1: retro doc 작성** (CLAUDE.md "Cycle Workflow" 표준 항목)

- [ ] **Step 2: memory project file + MEMORY.md update** (≤ 200 chars index entry)

- [ ] **Step 3: vault sync**

```bash
VAULT="/Users/hayden/Hayden/20_Projects/Dev_Project/SJ_Daily Growth(Ai assistant)"
cp docs/superpowers/specs/2026-05-11-v3.28-trim-design.md "$VAULT/02-Cycles/"
cp ~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v328.md "$VAULT/03-Lessons/"
cp ~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md "$VAULT/03-Lessons/"
```

- [ ] **Step 4: retro commit**

```bash
git add .claude/completions/2026-05-11-v3.28-trim.md
git commit -m "docs(v3.28 T7 retro): bundle <Δ> B / vitest <N> / smoke 97 / Codex 최종 <verdict>"
```

- [ ] **Step 5: 사용자 "배포해줘" 명시 대기 → firebase deploy**

```bash
firebase deploy --only hosting
```

⚠️ **사용자 명시 승인 전 실행 금지** (CLAUDE.md 절대 규칙).

배포 후 live URL 확인 + memory project file 갱신.

---

## DoD (사이클 끝 일괄 확인)

- [ ] bundle CLI gzip ≤ **41500 B** (현재 42731, Δ ≥ **−1231 B**)
  - **미달 허용 조건**: T5 surface 측정 결과 부족 시 — 잔여 v3.29 carry + 최종 review verdict `APPROVED_WITH_NOTES` (P0-5 mitigation, R3 발동)
- [ ] TS strict 0
- [ ] Lint baseline 유지
- [ ] Vitest 양쪽 TZ regression 0 (1217 baseline, T2 +4 → 1221 예상 same count)
- [ ] Smoke 97/97
- [ ] Codex 사전 review dispatch (또는 fallback 명시) — **완료 `db3536f` 대상 dispatch, REQUIRES_FIX P0=5+P1=4 모두 in-cycle 흡수 (cbd4de6 + plan v2 commit)**
- [ ] Codex 최종 review dispatch (T6 Step 3, fallback 명시)
- [ ] retro doc + memory + MEMORY.md + vault sync

---

## Risk

| # | Risk | Mitigation |
|---|---|---|
| R1 | dead import 제거가 dynamic import 회귀 유발 | T4 Step 2 side-effect import 수동 점검, 의심 시 보존 |
| R2 | CSS orphan 제거가 historic browser cache 회귀 | Vite asset hash 자동 처리 |
| R3 | T5 LOW surface 부족 → DoD 41500 B 미달 | T0 Step 4 측정 mandatory. 미달 시 `APPROVED_WITH_NOTES` + 잔여 v3.29 carry (P0-5 흡수) |
| R4 | 신규 briefing append 시점에 `pinned: false` 누락 → TS strict fail | T2 Step 5에서 `scrapped: false` grep으로 모든 append site 보완 |
| R5 | T3 range check (`sv >= N && sv <= 8`)가 TS narrowing 깨뜨림 | typed guard (`typeof === 'number'`)로 P1-1 흡수, 그래도 깨지면 chain 유지 |
| R6 | T2 backfill이 기존 spec idempotency 깨뜨림 | T2 Step 2 신규 spec에 `re-load → save → re-load same shape` 명시 (P0-3 conform) |

---

## Self-Review (plan v2 작성 후 checklist)

**1. Spec coverage:**
- spec §1.2 P2-1 (`#insightsTab` CSS orphan) → T1 ✅
- spec §1.2 P2-2 (`Answer/Briefing.pinned` write-side normalize) → T2 Option A ✅
- spec §1.2 v3.24 / v3.26 bundle GREEN carry → T1~T5 누적 ✅ (T5 미달 시 R3)
- spec §1.3 후보 grep → T0 Step 4 + T3 / T4 / T5 ✅ (chat-bubble 명시적 제외 / parser는 branch-level proof 격하)
- spec §3 DoD → T6 + 최종 DoD checklist ✅

**2. Placeholder 검사:**
- "TBD" / "TODO" / "fill in" — plan body 없음 ✅
- T0 Step 4 / T5 grep — 실제 명령 (placeholder 아님)
- T2 Step 1 — `tests/state/` 위치 grep으로 결정 (구체적 다음 step)

**3. Type consistency:**
- `makeAnswer` / `migrateAnswer` / `loadBriefings` / `makePinButton` / `sortPinThenDesc` — 일관 ✅
- `Answer.pinned` / `Briefing.pinned` / `Insight.pinned` — 모두 `boolean` (narrow 후) ✅
- 모든 backfill은 copy-based (P0-3 conform) ✅

**4. Plan ↔ Spec mapping reconcile (P1-2):**
- spec §2 amend (`cbd4de6`)로 T3~T7 desc plan v2와 일치 ✅
- spec §1.3 chat-bubble active 표기 ✅

**5. Codex 사전 review verdict 흡수 명시:**
- P0=5 + P1=4 → 위 "Plan v1 → v2 변경 요약" 표에 1:1 매핑 ✅
- P2=NONE — v3.29 carry 격하 항목 없음

---

**작성**: 2026-05-11 (plan v1: `db3536f` / spec amend: `cbd4de6` / plan v2: 본 commit)
**baseline**: v3.27 deploy 직후 (CLI gzip 42731 B, vitest 1217, smoke 97/97)
**디스크립터**: dedicated bundle trim — Codex P0/P1 in-cycle 흡수 후 T1 진입 대기
