# v3.44 Knip Cleanup + Codex CLI stdin Graduate Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Knip finding 9건 (false-positive 7 unused exports + 2 unused devDeps)을 config 정밀화 + devDep 제거로 0~2건으로 줄이고, v3.43 carry D (Codex CLI stdin redirect 의무)를 CLAUDE.md 한 줄로 graduate.

**Architecture:** Lightweight cycle — 코드 변경 0줄, config + devDep + docs 갱신만. T1 knip.json 정밀화, T2 devDep 제거 + CLAUDE.md wording, T3 3종 검증 + Codex 최종, T4 retro + 배포 + Obsidian sync. dist bundle 영향 0 예상.

**Tech Stack:** Knip dead-code analyzer (^6.14.1, dev only), Vite + Firebase Hosting (production stack 변경 없음).

**Spec reference:** [docs/superpowers/specs/2026-05-20-v3.44-knip-cleanup-design.md](../specs/2026-05-20-v3.44-knip-cleanup-design.md)

---

## File Structure

**Modified (T1)**:
- `knip.json` — `ignoreExportsUsedInFile: true` 추가 + workspace `.`.entry에 `src/main.ts` 추가

**Modified (T2)**:
- `package.json` — `devDependencies`에서 `http-server` + `prettier` 제거
- `package-lock.json` — `npm install` 후 자동 갱신
- `CLAUDE.md` — "v3.31 L1 graduation" 섹션 끝에 stdin redirect 의무 wording 한 줄 추가

**No new files.**

---

## Task 0: 사전 Codex Review (small cycle optional, CLAUDE.md graduate)

**Files:**
- Output: `/tmp/v3.44-codex-pre.txt`

본 cycle은 lightweight (≤4 main task + 코드 0줄 변경). CLAUDE.md "small cycle exception" 적용 — direct CLI 1차 시도 의무, 실패 시 controller self-review fallback OK. **stdin redirect `< /dev/null` 의무 명시** (v3.43 L4 적용).

- [ ] **Step 1: Codex 사전 review CLI dispatch (`< /dev/null` 포함)**

```bash
HEAD_HASH=$(git rev-parse HEAD); codex exec --skip-git-repo-check -o /tmp/v3.44-codex-pre.txt "v3.44 사이클 spec + plan 사전 review 요청.

본 cycle은 lightweight (v3.43 carry P3 Knip cleanup + carry D Codex CLI stdin redirect graduate). 코드 변경 0줄.

CLAUDE.md graduated checklist 5항목은 본 cycle 특성상 대부분 N/A (코드 변경 없음).

대신 다음 review:

1. knip.json ignoreExportsUsedInFile:true가 진짜 unused same-file export를 가릴 risk
2. workspace .entry에 src/main.ts 추가가 다른 entry 자동 인식에 영향 가능성
3. prettier/http-server 제거가 외부 도구 (editor extension, CI)에서 transitive 의존 가능성
4. CLAUDE.md \"v3.31 L1 graduation\" 섹션에 stdin redirect 의무 wording 한 줄 추가 시 wording 정합
5. 누락된 cleanup 후보 (v3.43 retro 내 다른 항목)

분류: P0 (in-cycle 차단) / P1 (in-cycle 흡수) / P2 (carry) / DoD / QUESTION.

파일:
- spec: docs/superpowers/specs/2026-05-20-v3.44-knip-cleanup-design.md
- plan: docs/superpowers/plans/2026-05-20-v3.44-knip-cleanup-plan.md

Repo HEAD: ${HEAD_HASH}" < /dev/null 2>&1 | tail -5
```

Expected: `/tmp/v3.44-codex-pre.txt` 생성 + 분류 결과. CLI stall 시 `< /dev/null` 효과로 즉시 종료. 한도 시 `pr-review-toolkit:code-reviewer` fallback.

- [ ] **Step 2: 결과 분석 + 사용자 보고**

```bash
cat /tmp/v3.44-codex-pre.txt
```

분류:
- **P0 catch** → plan v2 1:1 흡수 commit, T1 진입 X
- **P1만** → in-cycle 반영 위치 결정 (T1/T2 어디서), plan v2 commit, T1 진입 OK
- **P2/DoD/QUESTION만** → retro 흡수 또는 carry, plan 그대로 T1 진입

- [ ] **Step 3: plan v2 흡수 (필요 시)**

```bash
git add docs/superpowers/plans/2026-05-20-v3.44-knip-cleanup-plan.md
git commit -m "docs(v3.44 T0): plan v2 — Codex 사전 review P0/P1 in-cycle 흡수"
```

---

## Task 1: knip.json Config 정밀화

**Files:**
- Modify: `knip.json` (project root)

### 1.1 Knip baseline 재확인

- [ ] **Step 1: 현재 finding count 확인**

```bash
npm run knip 2>&1 | tail -20
```

Expected: finding 9건 (7 unused exports + 2 unused devDeps) + 3 configuration hints. v3.43 마지막 결과와 동일.

### 1.2 knip.json 정밀화 적용

- [ ] **Step 1: knip.json Edit — ignoreExportsUsedInFile + main.ts entry**

`knip.json` 현재 상태:

```json
{
  "$schema": "https://unpkg.com/knip@latest/schema.json",
  "ignore": [
    "tests/helpers/**",
    "tests/smoke/_helpers/**",
    "functions/lib/**"
  ],
  "ignoreBinaries": [
    "firebase"
  ],
  "workspaces": {
    ".": {
      "entry": ["public/sw.js"]
    },
    "functions": {
      "entry": ["src/index.ts", "src/sendAnswerDm.ts", "vitest.config.ts"]
    }
  }
}
```

Edit operation:

```
old_string:
  "ignoreBinaries": [
    "firebase"
  ],
  "workspaces": {
    ".": {
      "entry": ["public/sw.js"]
    },

new_string:
  "ignoreBinaries": [
    "firebase"
  ],
  "ignoreExportsUsedInFile": true,
  "workspaces": {
    ".": {
      "entry": ["src/main.ts", "public/sw.js"]
    },
```

- [ ] **Step 2: Knip 재실행 + finding count 검증**

```bash
npm run knip 2>&1 | tee /tmp/v3.44-knip-after-config.txt | tail -20
```

Expected: finding **2건** (devDep 2건만 잔존, 7 unused exports 모두 해소). configuration hints는 별도.

- 실패 시 (예: finding 7건↑ 잔존):
  - `ignoreExportsUsedInFile` 적용 확인 (knip cache 또는 syntax error 의심)
  - 또는 false-positive로 미해소된 export별 분석

### 1.3 T1 verify

- [ ] **Step 1: TS + Lint 영향 0 확인 (config 변경이 빌드/타입에 영향 X)**

```bash
npx tsc --noEmit 2>&1 | tail -3
npm run lint 2>&1 | tail -3
```

Expected: 0 errors, 0 warnings (baseline 유지).

---

## Task 2: devDep 제거 + Codex CLI stdin Graduate

**Files:**
- Modify: `package.json`
- Modify: `package-lock.json` (자동)
- Modify: `CLAUDE.md`

### 2.1 package.json devDep 제거

- [ ] **Step 1: package.json Edit — http-server + prettier 제거**

현재 `package.json` devDependencies (line 15-28):

```json
  "devDependencies": {
    "@eslint/js": "^8.57.1",
    "@playwright/test": "^1.42.0",
    "@types/node": "^20.11.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.57.0",
    "http-server": "^14.1.1",
    "jsdom": "^24.0.0",
    "knip": "^6.14.1",
    "prettier": "^3.2.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^1.4.0"
  },
```

Edit operation:

```
old_string:
    "eslint": "^8.57.0",
    "http-server": "^14.1.1",
    "jsdom": "^24.0.0",
    "knip": "^6.14.1",
    "prettier": "^3.2.0",
    "typescript": "^5.4.0",

new_string:
    "eslint": "^8.57.0",
    "jsdom": "^24.0.0",
    "knip": "^6.14.1",
    "typescript": "^5.4.0",
```

- [ ] **Step 2: npm install (lock 갱신 + 제거 deps 정리)**

```bash
npm install 2>&1 | tail -5
```

Expected: deps removed message + lock 갱신. transitive 영향 분석 출력.

- [ ] **Step 3: Knip 재실행 — devDep finding 해소 확인**

```bash
npm run knip 2>&1 | tail -15
```

Expected: finding **0건** (devDep 2건 + unused exports 7건 모두 해소). configuration hints만 남음.

### 2.2 CLAUDE.md — Codex CLI stdin redirect 의무 wording

- [ ] **Step 1: 정확한 anchor 위치 확인**

```bash
grep -n "v3.31 L1 graduation\|따라서 Codex 2-pass는 direct CLI 우선" CLAUDE.md
```

Expected: line ~184 + line ~192 (sub-section start + end paragraph).

- [ ] **Step 2: CLAUDE.md Edit — wording 한 줄 추가**

Edit operation:

```
old_string:
v3.30 T0에서 subagent dispatch가 background drop되어 결과 회수에 실패했다. v3.30 T7에서는 `codex exec` 직접 호출이 production stale count P1을 catch했다. 따라서 Codex 2-pass는 direct CLI 우선, 실패 시 controller self-review fallback을 retro에 명시한다.

new_string:
v3.30 T0에서 subagent dispatch가 background drop되어 결과 회수에 실패했다. v3.30 T7에서는 `codex exec` 직접 호출이 production stale count P1을 catch했다. 따라서 Codex 2-pass는 direct CLI 우선, 실패 시 controller self-review fallback을 retro에 명시한다.

**stdin redirect 의무 (v3.44 graduate, v3.43 L4 ROI)**: `codex exec "$PROMPT"` foreground/background 호출 시 `< /dev/null` redirect 명시. 미명시 시 "Reading additional input from stdin..." 메시지로 stall + output file 미생성. v3.43 T3 1차 시도가 background로 stall, 2차 시도 `< /dev/null` foreground로 정상 완료한 사례에서 graduate.
```

- [ ] **Step 3: 변경 확인**

```bash
grep -n "stdin redirect 의무" CLAUDE.md
```

Expected: 1 line.

### 2.3 T2 commit

- [ ] **Step 1: 통합 staging + 단일 commit**

```bash
git add knip.json package.json package-lock.json CLAUDE.md
git status --short
git commit -m "$(cat <<'EOF'
chore(v3.44 T1+T2): Knip config 정밀화 + devDep 2건 제거 + stdin graduate

T1: knip.json에 ignoreExportsUsedInFile:true + workspace .entry에 src/main.ts
추가 → finding 7 unused exports false-positive 모두 해소.

T2: package.json devDep http-server (사용처 0) + prettier (.prettierrc 없음,
scripts 등록 안 됨, editor 자체 format 대체) 둘 다 제거 → finding 2 unused
devDeps 해소. npm install로 lock 갱신.

T2 graduate: CLAUDE.md "v3.31 L1 graduation" 섹션에 Codex CLI stdin redirect
의무 wording 한 줄 추가 (v3.43 L4 ROI — < /dev/null 명시 안 하면 stall).

Knip finding: 9건 → 0건 (configuration hints만 잔존).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

Expected: 4 files changed, 1 commit. configuration hints는 dev tool 권장사항이라 finding count 외부 (정상).

- [ ] **Step 2: 통합 verify (TS + lint + knip)**

```bash
npx tsc --noEmit 2>&1 | tail -3 && echo "---" && npm run lint 2>&1 | tail -3 && echo "---" && npm run knip 2>&1 | tail -10
```

Expected:
- TS strict: 0 errors
- Lint: 0 errors
- Knip: 0 finding (configuration hints만)

---

## Task 3: 3종 검증 + Codex 최종 Review + Bundle Quad Record

**Files:**
- Read: `dist/assets/*.js` (bundle 측정)
- Output: `/tmp/v3.44-codex-final.txt`

### 3.1 3종 검증

- [ ] **Step 1: Vitest 양쪽 TZ**

```bash
TZ=Asia/Seoul npm test 2>&1 | tail -5
TZ=America/New_York npm test 2>&1 | tail -5
```

Expected: 각 1546/1546 PASS.

- [ ] **Step 2: Functions vitest**

```bash
cd /Users/hayden/Hayden_AX_Project/my_ai_assistance/functions && npm test 2>&1 | tail -5 && cd /Users/hayden/Hayden_AX_Project/my_ai_assistance
```

Expected: 24/24 PASS. **절대 경로 사용 의무** — cwd shift 방지 (v3.43 T3 사례).

- [ ] **Step 3: Smoke (cold-start N=3)**

```bash
cd /Users/hayden/Hayden_AX_Project/my_ai_assistance && npm run test:smoke 2>&1 | tail -10
```

Expected: 109/109 PASS. (cwd 확인 필수.)

### 3.2 Bundle Quad Record

- [ ] **Step 1: 클린 build**

```bash
cd /Users/hayden/Hayden_AX_Project/my_ai_assistance && npm run build 2>&1 | tail -10
```

Expected: build success. dist/ 갱신.

- [ ] **Step 2: 4 chunk gzip 측정**

```bash
echo "index:          $(gzip -c dist/assets/index-*.js | wc -c | tr -d ' ') B"
echo "archive:        $(gzip -c dist/assets/archive-*.js | wc -c | tr -d ' ') B"
echo "appCheck:       $(gzip -c dist/assets/appCheck-*.js | wc -c | tr -d ' ') B"
echo "translateToast: $(gzip -c dist/assets/translateToast-*.js | wc -c | tr -d ' ') B"
```

Expected (v3.43 → v3.44 ±50 B):
- index: 9,379 → 9,379 ±50
- archive: 8,968 → 8,968 ±50
- appCheck: 13,215 → 13,215 ±50
- translateToast: 2,136 → 2,136 ±50

config + devDep 제거이므로 dist 영향 0 예상.

### 3.3 Codex 최종 review

- [ ] **Step 1: CLI dispatch (`< /dev/null` 포함, v3.44 stdin graduate 적용)**

```bash
HEAD_HASH=$(git rev-parse HEAD); codex exec --skip-git-repo-check -o /tmp/v3.44-codex-final.txt "v3.44 deploy gate review 요청.

이번 사이클은 lightweight (Knip cleanup + Codex CLI stdin graduate). 신규 기능 0, schema migration 0, 코드 변경 0줄.

확인 요청:

1. knip.json ignoreExportsUsedInFile:true 적용 후 진짜 unused same-file export catch 영향 (finding 0 검증)
2. workspace .entry에 src/main.ts 추가가 다른 entry 자동 인식에 영향 없음 확인
3. http-server + prettier devDep 제거 후 production/dev workflow 영향 0 확인
4. CLAUDE.md \"v3.31 L1 graduation\" 섹션의 stdin redirect 의무 wording 정합 + future-proof
5. Bundle quad record ±50 B 유지

분류: DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT.

Repo HEAD: ${HEAD_HASH}" < /dev/null 2>&1 | tail -5
```

Expected: `/tmp/v3.44-codex-final.txt` 생성 + 분류 결과.

- [ ] **Step 2: 분류 + 사용자 보고**

```bash
cat /tmp/v3.44-codex-final.txt
```

분류:
- **DEPLOY_APPROVED** → T4 retro + 사용자 "배포해줘" 대기
- **APPROVED_WITH_NOTES** → P2 carry 명시 후 deploy gate 충족
- **REJECT** → in-cycle fix + 재 review

CLI 한도 시 fallback (`pr-review-toolkit:code-reviewer`) + 사용자 즉시 알림 (CLAUDE.md "fallback 정책 3번").

---

## Task 4: Retro + 배포 + Obsidian Sync

**Files:**
- Create: `/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v344.md`
- Modify: `MEMORY.md` (index entry 추가)
- Copy: Obsidian vault sync (5 files)

### 4.1 Retro 메모리 작성

- [ ] **Step 1: 신규 memory file 작성**

`/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v344.md` 신규:

```markdown
---
name: daily-growth-v344
description: DG v3.44 사이클 — Knip cleanup (finding 9→0) + Codex CLI stdin redirect 의무 graduate. devDep 2건 제거 (http-server + prettier). 신규 기능 0, schema 0, 코드 변경 0줄.
metadata:
  node_type: memory
  type: project
---

# DG v3.44 Knip Cleanup + Codex CLI stdin Graduate

**기간**: 2026-05-20 (1 day, lightweight cycle, v3.41~v3.44 동일 일자 4사이클)
**Type**: Cleanup + meta graduate. 신규 기능 0, schema migration 0, 코드 변경 0줄.
**Reference precedent**: v3.43 (Knip 도입 + lesson graduate), v3.42 (light carry), v3.24 (메타 graduate).

## 사이클 트리거

v3.43 deploy 직후 controller 추천 후 사용자 "B 여기서 바로 진행" 명시. v3.43 carry P3 (Knip finding 9건) cleanup + carry D (Codex CLI stdin graduate) 묶음.

## Codex 2-pass Review

- 사전 review: <분류 결과 + P0/P1/P2 catch>
- 최종 review: <DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT>

## Commits

| Commit | Task | 내용 |
|---|---|---|
| `f4e014b` | T0 | spec v1 |
| `<T0 plan v1>` | T0 | plan v1 |
| `<T1+T2>` | T1+T2 | knip config + devDep 제거 + stdin graduate |
| (commit 없음) | T3 | verify + bundle + Codex 최종 |
| `<T4 retro>` | T4 | retro memo |

## 검증

- TS strict: 0 errors
- Lint: 0 errors
- Vitest 양쪽 TZ: 1546/1546
- Functions: 24/24
- Smoke: 109/109
- Knip: 9 → 0 finding (configuration hints만)

## Bundle Quad Record (v3.43 → v3.44)

| Chunk | v3.43 | v3.44 | Δ |
|---|---:|---:|---:|
| index | 9,379 | <측정> | <±50> |
| archive | 8,968 | <측정> | <±50> |
| appCheck | 13,215 | <측정> | <±50> |
| translateToast | 2,136 | <측정> | <±50> |

## 신규 Lessons

- (메타 lesson — 사이클 자체에서 도출 가능)

## v3.45 Carry-Forward

| ID | 항목 | 출처 | 우선순위 |
|---|---|---|---|
| C1 | T1 Phase B backend enforce + 202 + UX copy | v3.41 carry | **HIGH** (2026-05-21+ 24h 후) |
| C7 | Firebase API key GCP referrer | v3.43 v2 강등 | M (C1 묶음) |
| C4 | first briefing storage thrash | v3.41 carry | L |
| C5 | state↔ui layer cycle | v3.41 carry | L (별도 사이클) |
| v3.17 §7.2 | 식물 detail modal / Streak Freeze 등 | v3.17 retro | M |

## 사용자 결정 사항

- ✅ scope 채택: Knip cleanup + Codex CLI stdin graduate
- ✅ prettier devDep 제거 (사용자 명시)
- ✅ http-server devDep 제거 (controller)
- ✅ knip config 정밀화 (ignoreExportsUsedInFile + workspace .entry main.ts)
- ✅ stdin graduate 한 줄 wording 흡수
- ❌ C1 Phase B (24h 후 별도)
- ❌ C7 재진입 (C1 묶음)
```

- [ ] **Step 2: MEMORY.md index entry 추가 (EOF append)**

```bash
echo "- [DG v3.44 Knip Cleanup + Codex CLI stdin Graduate](project_daily_growth_v344.md) — **<상태 + commit hash>** — Knip finding 9→0 (config 정밀화 + devDep 2건 제거) + Codex CLI stdin redirect 의무 CLAUDE.md graduate. 신규 기능 0, schema 0, 코드 변경 0줄. v3.45 carry: C1 Phase B HIGH (24h 후) + C7 + v3.17 §7.2 신규 기능." >> /Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md
```

(상태/hash는 실측 후 치환.)

### 4.2 retro commit

- [ ] **Step 1: commit**

```bash
git add docs/superpowers/specs/2026-05-20-v3.44-knip-cleanup-design.md 2>/dev/null
git commit --allow-empty -m "$(cat <<'EOF'
docs(v3.44 T4): retro — Knip cleanup + Codex CLI stdin graduate 완료

사이클 종료. Knip 9→0 + devDep 2건 제거 + stdin wording graduate. 신규 기능 0,
schema 0, 코드 변경 0줄. Bundle Δ ±50 B 이내. Codex 최종 <분류>. v3.45 carry:
C1 Phase B HIGH (24h 후), C7 재진입, v3.17 §7.2 신규 기능.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

### 4.3 배포 (사용자 명시 시점)

- [ ] **Step 1: 사용자 "배포해줘" 명시 대기**

CLAUDE.md "절대 규칙": 사용자 명시 전 deploy 금지.

- [ ] **Step 2: build + deploy**

```bash
cd /Users/hayden/Hayden_AX_Project/my_ai_assistance && npm run build && firebase deploy --only hosting
```

Expected: Firebase Hosting deploy success. URL: `https://my-ai-assistant-904f3.web.app`.

### 4.4 Obsidian Sync

- [ ] **Step 1: vault 경로 변수 + sync**

```bash
VAULT="/Users/hayden/Hayden/20_Projects/Dev_Project/SJ_Daily Growth(Ai assistant)"
cp docs/superpowers/specs/2026-05-20-v3.44-knip-cleanup-design.md "$VAULT/02-Cycles/"
cp /Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v344.md "$VAULT/03-Lessons/"
cp /Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md "$VAULT/03-Lessons/"
cp CLAUDE.md "$VAULT/01-Workflow/CLAUDE-project.md"
```

- [ ] **Step 2: 00-Index.md 사용자 추가 권고**

00-Index.md는 사용자 편집 영역 (controller readonly). 한 줄 추가 권고:

```
| v3.44 | 2026-05-20 | Knip cleanup + Codex CLI stdin graduate | finding 9→0 / devDep 2건 제거 / stdin wording graduate | 배포 완료 |
```

- [ ] **Step 3: sync 완료 알림**

사용자에게 vault path + sync 완료 한 줄 보고.

---

## Self-Review (post-write 점검)

- [x] **Spec coverage**: spec §1 (T1 knip config) + §2 (T2 devDep + stdin) + §3 (T3 verify) + §4 (T4 retro) 모두 plan task로 mapping됨.
- [x] **Placeholder scan**: TBD/TODO/implement later 없음. Codex prompts 안에 escape된 따옴표 (`\"`) 사용으로 heredoc 충돌 회피. `< /dev/null` 명시 의무 (v3.43 L4 적용).
- [x] **Type consistency**: knip.json `ignoreExportsUsedInFile: true` / Bundle chunk 이름 (`index-*`, `archive-*`, `appCheck-*`, `translateToast-*`) / Codex CLI heredoc 패턴 (`HEAD_HASH=$(git rev-parse HEAD)`) 일관.
- [x] **No-code task의 TDD 대안**: T1 config — knip dry-run으로 finding count 검증 패턴. T2 docs — grep으로 변경 확인. T3 verify — TS/lint/test/bundle 모두 baseline 유지 확인. TDD 적용 가능한 신규 spec 없음 (lightweight cycle).

---

## v3.45 Carry-Forward (Plan reference)

| ID | 항목 | 출처 | 우선순위 |
|---|---|---|---|
| C1 | T1 Phase B backend enforce + 202 + UX copy | v3.41 carry | **HIGH** (2026-05-21+ 24h 후) |
| C7 | Firebase API key GCP referrer | v3.43 v2 강등 | M (C1 묶음 권장) |
| C4 | first briefing storage thrash | v3.41 carry | L |
| C5 | state↔ui layer cycle 정리 | v3.41 carry | L (별도 사이클) |
| v3.17 §7.2 | 식물 detail modal / Streak Freeze 등 | v3.17 retro | M (신규 기능 cycle) |
| Codex CLI retroactive | v3.43 사전 review direct CLI 재시도 | CLAUDE.md "fallback 정책 6번" | L (권고) |
