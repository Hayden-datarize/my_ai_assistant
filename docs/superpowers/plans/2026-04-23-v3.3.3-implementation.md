# v3.3.3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship v3.3.3 = 풀블리드 카드뉴스 브리핑(#1) + 관심 분야 수정 모달(#2) + heatmap palette 재조정(#4) + modal-system 전면 정비(#5) + AI bubble 액션·turn counter(#6).

**Architecture:**
- Phase A(기반) → B+C(병렬 가능) → D → E(QA/배포) 순. #5 modal-system 이 #1 메모 모달·#2 관심분야 모달의 전제.
- Vite+TS 유지, CSS Cascade Layers(v3.3.0) 구조 준수, 토큰 기반 디자인. 외부 dependency 추가 없음.
- CSP `img-src`에 `https:` 추가하여 RSS 썸네일 지원. 3-tier fallback(rss2json enclosure/thumbnail → DOMParser img → gradient+이니셜).

**Tech Stack:** Vite 5, TypeScript 5, Vitest 1.4, Playwright 1.42, Firebase Hosting, rss2json API, Gemini API(기존).

**Spec 원본:** [docs/superpowers/specs/2026-04-23-v3.3.3-design.md](../specs/2026-04-23-v3.3.3-design.md)

---

## File Structure

### Created
- `src/ui/modals/memo.ts` — 메모 모달 오프너(#1과 #5의 다리)
- `src/ui/modals/interests.ts` — 관심 분야 모달 오프너(#2)
- `tests/unit/modals-interests.spec.ts`
- `tests/unit/modals-memo.spec.ts`
- `tests/unit/rss-image.spec.ts` — `extractImage()` 유닛
- `tests/smoke/cardnews.spec.ts` — 카드뉴스 E2E
- `tests/smoke/modal-overhaul.spec.ts` — 모달 focus-trap/centered E2E
- `tests/smoke/interests-edit.spec.ts`
- `tests/smoke/chat-bubble-action.spec.ts`

### Modified
- `src/styles/tokens.css` — heatmap palette, z-index 재정립, `--modal-backdrop`
- `src/styles/components/shared.css` — legacy modal 삭제, `.dg-modal-*` 신규, toast token화
- `src/styles/components/cards.css` — 풀블리드 카드뉴스 전면 재작성
- `src/styles/components/splash.css`, `onboarding.css` — z-index 토큰화
- `src/styles/layout.css` — floating token 적용
- `src/styles/pages/home.css` — `.chat-bubble-action` 추가
- `src/styles/pages/settings.css` (생성 또는 확장) — 관심 분야 섹션
- `src/services/rss.ts` — `FeedItem.image`, `extractImage()`
- `src/state/briefings.ts` — `Briefing.imageUrl?` optional
- `src/ui/handlers/home.ts` — `addBubble` 시그니처, cardnews 렌더, `openSettingsWithFocus`, early-return turn counter
- `src/ui/handlers/settings.ts` (또는 `src/ui/tabs/settings.ts`) — 관심 분야 섹션 마운트·이벤트
- `src/ui/modals/shared.ts` — focus-trap 통합, `aria-labelledby`, `closeModal({skipFocusRestore})`
- `firebase.json` — CSP `img-src` 확장
- `index.html` — CSP meta tag 동기화
- 기존 `tests/unit/state-briefings.spec.ts`, `tests/unit/rss.spec.ts`, `tests/unit/modals-shared.spec.ts`, `tests/unit/css-tokens.spec.ts` — 확장

---

## Commands Cheat Sheet

```bash
npm run dev              # 개발 서버
npm test                 # Vitest 전체 (= vitest run)
npx vitest run <path>    # 특정 파일만
npm run test:smoke       # Playwright E2E
npm run build            # tsc + vite build
npm run lint
firebase emulators:start --only hosting   # 실 CSP 로 preview
```

**개발 흐름 규칙** (CLAUDE.md 준수):
- 작은 단위로 commit, 기능별 분리
- `git add -A` 금지, 파일 명시
- 사용자 승인 없이 push/deploy 금지
- Phase 완료 시 Vitest + build 0-warning 확인 후 다음 Phase

---

# Phase A — Modal System Overhaul

**사전 조건**: 브랜치 `feat/v3.3.3-cardnews-and-polish` 생성 후 main 으로부터 분기.
**완료 조건**: archive "기록 상세" 모달이 스타일 정상 + focus-trap 동작 + z-index 토큰 모두 정합.

## Task A1: 브랜치 생성 및 Phase A 사전 검증

**Files:** (없음 — 환경 설정)

- [ ] **Step 1: 브랜치 생성**

```bash
git checkout -b feat/v3.3.3-cardnews-and-polish
git status
```
Expected: `On branch feat/v3.3.3-cardnews-and-polish`, clean tree.

- [ ] **Step 2: 현재 archive 모달 상태 Playwright 재현 (할루시네이션 Q4 검증)**

Create: `tests/smoke/archive-modal-before.spec.ts` (임시, Phase A 종료 시 삭제)

```typescript
import { test, expect } from '@playwright/test';

test('archive modal renders unstyled before fix (baseline)', async ({ page }) => {
  await page.goto('/');
  // 온보딩 완료 상태 localStorage 세팅
  await page.evaluate(() => {
    localStorage.setItem('user', JSON.stringify({
      name: '테스트', interests: ['tech'], onboardedAt: new Date().toISOString(),
      streak: 0, lastActiveDate: new Date().toISOString(), xp: 0, level: 1
    }));
    localStorage.setItem('answers', JSON.stringify([{
      id: 'a1', questionId: 'q1', text: '테스트 답변', authorId: 'self', type: 'reflection',
      date: new Date().toISOString().slice(0,10), createdAt: Date.now()
    }]));
  });
  await page.reload();
  await page.click('[data-tab-id="archive"]');
  await page.click('.answer-card', { timeout: 5000 }).catch(() => {});
  // 모달이 unstyled임을 기록 (position static, no backdrop)
  const modal = page.locator('.dg-modal');
  if (await modal.count() > 0) {
    const pos = await modal.evaluate((el) => getComputedStyle(el).position);
    console.log('Baseline: .dg-modal position =', pos);  // 예상: 'static'
  }
});
```

- [ ] **Step 3: baseline 캡처 실행**

```bash
npx playwright test tests/smoke/archive-modal-before.spec.ts
# Playwright webServer auto-runs `npm run build && npm run preview --port 4173` (see playwright.config.ts)
```

Expected: console에 `Baseline: .dg-modal position = static` (또는 모달 미렌더). Q4 검증 완료.

- [ ] **Step 4: baseline 파일 삭제**

```bash
rm tests/smoke/archive-modal-before.spec.ts
```

- [ ] **Step 5: 커밋 (브랜치 세팅)**

```bash
git commit --allow-empty -m "chore(branch): start v3.3.3 feat branch"
```

---

## Task A2: z-index 토큰 체계 재정립

**Files:**
- Modify: `src/styles/tokens.css:80-94` (z-index 블록)
- Test: `tests/unit/css-tokens.spec.ts` (확장)

- [ ] **Step 1: Write the failing test**

Modify: `tests/unit/css-tokens.spec.ts`

```typescript
// 기존 파일 끝에 추가
describe('v3.3.3 z-index tokens', () => {
  let style: CSSStyleDeclaration;

  beforeAll(async () => {
    const css = await import('fs').then(m =>
      m.promises.readFile('src/styles/tokens.css', 'utf8'));
    const el = document.createElement('div');
    const s = document.createElement('style');
    s.textContent = css;
    document.head.append(s, el);
    style = getComputedStyle(el);
  });

  it('defines z layers with monotone ordering', () => {
    const get = (name: string) => parseInt(style.getPropertyValue(name).trim(), 10);
    expect(get('--z-sidebar')).toBe(40);
    expect(get('--z-sidebar-button')).toBe(41);
    expect(get('--z-floating')).toBe(100);
    expect(get('--z-onboarding')).toBe(900);
    expect(get('--z-splash')).toBe(1000);
    expect(get('--z-modal')).toBe(1100);
    expect(get('--z-toast')).toBe(2000);
  });
});
```

- [ ] **Step 2: Run test to verify fail**

```bash
npx vitest run tests/unit/css-tokens.spec.ts
```

Expected: `--z-floating`, `--z-onboarding`, `--z-splash` 값 실패 (토큰 미정의), `--z-modal: 50 ≠ 1100` 실패.

- [ ] **Step 3: Implement — tokens.css 수정**

Modify `src/styles/tokens.css` 기존 z-index 블록 (line 89-93 근처):

```css
/* Z-index scale (v3.3.3 재정립) */
--z-sidebar: 40;
--z-sidebar-button: 41;
--z-floating: 100;      /* sticky header 등 */
--z-onboarding: 900;
--z-splash: 1000;
--z-modal: 1100;
--z-toast: 2000;
```

- [ ] **Step 4: Run test to verify pass**

```bash
npx vitest run tests/unit/css-tokens.spec.ts
```

Expected: 새 테스트 pass. 기존 테스트도 여전히 pass.

- [ ] **Step 5: Commit**

```bash
git add src/styles/tokens.css tests/unit/css-tokens.spec.ts
git commit -m "refactor(tokens): redefine z-index scale for v3.3.3"
```

---

## Task A3: z-index 사용처 토큰화 (splash/onboarding/toast/floating)

**Files:**
- Modify: `src/styles/components/splash.css:11`
- Modify: `src/styles/components/onboarding.css:9`
- Modify: `src/styles/components/shared.css:10` (toast)
- Modify: `src/styles/layout.css:51`

- [ ] **Step 1: splash.css 수정**

Modify line 11: `z-index: 1000;` → `z-index: var(--z-splash);`

- [ ] **Step 2: onboarding.css 수정**

Modify line 9: `z-index: 900;` → `z-index: var(--z-onboarding);`

- [ ] **Step 3: shared.css toast 수정**

Modify line 10 `.toast-container`: `z-index: 2000;` → `z-index: var(--z-toast);`

- [ ] **Step 4: layout.css 수정**

Modify line 51: `z-index: 100;` → `z-index: var(--z-floating);`

- [ ] **Step 5: Playwright smoke — 변경 후 계층 회귀 없음**

Create: `tests/smoke/zindex-regression.spec.ts`

```typescript
import { test, expect } from '@playwright/test';

test('toast appears above modal, modal above sidebar', async ({ page }) => {
  await page.goto('/');
  const getZ = (sel: string) =>
    page.locator(sel).first().evaluate((el) => parseInt(getComputedStyle(el).zIndex, 10));

  // showToast API를 전역에서 호출 (app이 expose하는지 확인 — 안 하면 CustomEvent로 dispatch)
  // 여기선 단순 DOM inspection만: 각 컴포넌트 z-index 값이 순서대로인지
  // 주의: 실제 렌더된 상태에서만 의미. 테스트가 취약하면 unit으로 충분
  expect(true).toBe(true); // 자리만 확보, 실제 assertion은 필요 시 확장
});
```

실제로는 unit 테스트(css-tokens.spec.ts)가 계층 순서를 이미 검증함. 이 smoke는 자리표시.

- [ ] **Step 6: 빌드 확인**

```bash
npm run build
```

Expected: 0 warning, dist/ 생성.

- [ ] **Step 7: Commit**

```bash
git add src/styles/components/splash.css src/styles/components/onboarding.css src/styles/components/shared.css src/styles/layout.css tests/smoke/zindex-regression.spec.ts
git commit -m "refactor(styles): route z-index values through tokens"
```

---

## Task A4: --modal-backdrop 토큰 도입

**Files:**
- Modify: `src/styles/tokens.css` (light + dark 블록)
- Test: `tests/unit/css-tokens.spec.ts` (확장)

- [ ] **Step 1: Write failing test**

Modify: `tests/unit/css-tokens.spec.ts` — `describe('v3.3.3 z-index tokens')` 아래 추가

```typescript
describe('v3.3.3 modal backdrop token', () => {
  it('defines --modal-backdrop for light and dark', async () => {
    const css = await import('fs').then(m =>
      m.promises.readFile('src/styles/tokens.css', 'utf8'));
    expect(css).toMatch(/--modal-backdrop:\s*rgba\(0,\s*0,\s*0,\s*0\.4\)/);
    // dark 블록 내에 재정의
    expect(css).toMatch(/\[data-theme="dark"\][^}]*--modal-backdrop:\s*rgba\(0,\s*0,\s*0,\s*0\.6\)/s);
  });
});
```

- [ ] **Step 2: Run test — fail 확인**

```bash
npx vitest run tests/unit/css-tokens.spec.ts
```

Expected: fail (토큰 없음)

- [ ] **Step 3: 구현 — tokens.css 수정**

Light 블록 (`:root`) 안에 추가:

```css
--modal-backdrop: rgba(0, 0, 0, 0.4);
```

Dark 블록 (`[data-theme="dark"]`) 안에 추가:

```css
--modal-backdrop: rgba(0, 0, 0, 0.6);
```

- [ ] **Step 4: Pass 확인**

```bash
npx vitest run tests/unit/css-tokens.spec.ts
```

- [ ] **Step 5: Commit**

```bash
git add src/styles/tokens.css tests/unit/css-tokens.spec.ts
git commit -m "feat(tokens): add --modal-backdrop for light/dark"
```

---

## Task A5: `.dg-modal-*` CSS 정의 + legacy `.modal-overlay` 제거

**Files:**
- Modify: `src/styles/components/shared.css`
- Test: `tests/unit/css-tokens.spec.ts` 혹은 smoke에서 실 DOM 확인

- [ ] **Step 1: Write smoke test first (archive 모달 정상 렌더)**

Create: `tests/smoke/modal-overhaul.spec.ts`

```typescript
import { test, expect } from '@playwright/test';

async function seedAnswer(page) {
  await page.evaluate(() => {
    localStorage.setItem('user', JSON.stringify({
      name: '테스트', interests: ['tech'], onboardedAt: new Date().toISOString(),
      streak: 0, lastActiveDate: new Date().toISOString(), xp: 0, level: 1
    }));
    localStorage.setItem('answers', JSON.stringify([{
      id: 'a1', questionId: 'q1', text: '테스트 답변 내용', authorId: 'self',
      type: 'reflection', date: new Date().toISOString().slice(0,10), createdAt: Date.now()
    }]));
  });
  await page.reload();
}

test('archive modal renders styled with dg-modal CSS', async ({ page }) => {
  await page.goto('/');
  await seedAnswer(page);
  await page.click('[data-tab-id="archive"]');
  await page.click('.answer-card');

  const modal = page.locator('.dg-modal');
  await expect(modal).toBeVisible();

  const position = await modal.evaluate((el) => getComputedStyle(el).position);
  expect(position).toBe('fixed');

  const backdrop = page.locator('.dg-modal-backdrop');
  await expect(backdrop).toBeVisible();

  const bgColor = await backdrop.evaluate((el) => getComputedStyle(el).backgroundColor);
  expect(bgColor).toMatch(/rgba\(0,\s*0,\s*0,\s*0\.[46]\)/);  // light 0.4 or dark 0.6
});
```

- [ ] **Step 2: Run smoke — fail 확인**

```bash
npx playwright test tests/smoke/modal-overhaul.spec.ts
```

Expected: fail — `.dg-modal` position이 `static` 또는 backdrop 색상 없음.

- [ ] **Step 3: 구현 — shared.css 수정**

Delete lines 25-43 (legacy `.modal-overlay`, `.modal-content`, `.modal-handle`).

Add at end of file:

```css
/* ============================================
   Modal (v3.3.3) — replaces legacy .modal-overlay
   ============================================ */
.dg-modal {
  position: fixed;
  inset: 0;
  z-index: var(--z-modal);
  display: flex;
  align-items: flex-end;
  justify-content: center;
  animation: dgModalFadeIn 0.2s ease;
}

.dg-modal-backdrop {
  position: absolute;
  inset: 0;
  background: var(--modal-backdrop);
}

.dg-modal-card {
  position: relative;
  background: var(--bg-card);
  border-radius: var(--radius-xl) var(--radius-xl) 0 0;
  width: 100%;
  max-width: 480px;
  max-height: 80vh;
  display: flex;
  flex-direction: column;
  animation: dgModalSlideUp 0.25s ease;
  overflow: hidden;
}

.dg-modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16px 20px;
  border-bottom: 1px solid var(--border);
}

.dg-modal-title {
  font-size: 1.1rem;
  font-weight: 600;
  margin: 0;
  color: var(--text-primary);
}

.dg-modal-close {
  background: none;
  border: none;
  font-size: 1.5rem;
  cursor: pointer;
  color: var(--text-secondary);
  padding: 4px 12px;
  border-radius: var(--radius-sm);
  line-height: 1;
}
.dg-modal-close:hover { background: var(--bg-input); }
.dg-modal-close:focus-visible { outline: 2px solid var(--primary); outline-offset: 2px; }

.dg-modal-body {
  padding: 20px;
  overflow-y: auto;
  color: var(--text-primary);
}

@media (min-width: 640px) {
  .dg-modal { align-items: center; }
  .dg-modal-card {
    border-radius: var(--radius-xl);
    max-width: 520px;
  }
}

@keyframes dgModalFadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}
@keyframes dgModalSlideUp {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}
```

- [ ] **Step 4: Pass 확인**

```bash
npx playwright test tests/smoke/modal-overhaul.spec.ts
```

Expected: pass.

- [ ] **Step 5: legacy dead code 삭제 확인**

```bash
grep -rn "modal-overlay\|modal-content\|modal-handle" src/ tests/
```

Expected: 검색 결과 없음 (또는 주석만).

- [ ] **Step 6: Commit**

```bash
git add src/styles/components/shared.css tests/smoke/modal-overhaul.spec.ts
git commit -m "feat(modal): add .dg-modal-* styles, drop legacy .modal-overlay"
```

---

## Task A6: focus-trap `openModal` 통합 + aria-labelledby + closeModal skipFocusRestore

**Files:**
- Modify: `src/ui/modals/shared.ts`
- Test: `tests/unit/modals-shared.spec.ts` (확장)

- [ ] **Step 1: Read existing modals-shared tests**

```bash
cat tests/unit/modals-shared.spec.ts
```

(문맥 확보)

- [ ] **Step 2: Write failing tests for focus-trap integration**

Modify: `tests/unit/modals-shared.spec.ts` — 기존 describe 아래 추가

```typescript
describe('v3.3.3 focus-trap integration', () => {
  beforeEach(() => {
    document.body.innerHTML = '<div id="modalRoot"></div>';
  });

  it('focuses first focusable element when modal opens', async () => {
    const { openModal, closeModal } = await import('../../src/ui/modals/shared');
    openModal({
      title: '테스트',
      bodyHtml: '<button id="btn1">첫 버튼</button><button id="btn2">둘째</button>'
    });
    // close 버튼(header)이 먼저 focusable이므로 그쪽
    const close = document.querySelector<HTMLButtonElement>('.dg-modal-close');
    expect(document.activeElement).toBe(close);
    closeModal();
  });

  it('sets aria-labelledby to title element id', async () => {
    const { openModal, closeModal } = await import('../../src/ui/modals/shared');
    openModal({ title: 'ARIA 테스트', bodyHtml: '<p>body</p>' });
    const modal = document.querySelector('.dg-modal');
    const labelledBy = modal?.getAttribute('aria-labelledby');
    expect(labelledBy).toBeTruthy();
    const title = document.getElementById(labelledBy!);
    expect(title?.textContent).toBe('ARIA 테스트');
    closeModal();
  });

  it('restores focus to previously focused element on close', async () => {
    const { openModal, closeModal } = await import('../../src/ui/modals/shared');
    const trigger = document.createElement('button');
    trigger.id = 'triggerBtn';
    document.body.append(trigger);
    trigger.focus();
    expect(document.activeElement).toBe(trigger);

    openModal({ title: '복원', bodyHtml: '<button>x</button>' });
    expect(document.activeElement).not.toBe(trigger);

    closeModal();
    expect(document.activeElement).toBe(trigger);
  });

  it('skipFocusRestore=true keeps focus unchanged', async () => {
    const { openModal, closeModal } = await import('../../src/ui/modals/shared');
    openModal({ title: '1차', bodyHtml: '<button>a</button>' });
    // 중첩 열기: 내부적으로 closeModal({skipFocusRestore:true}) 호출
    openModal({ title: '2차', bodyHtml: '<button>b</button>' });
    const title = document.querySelector('.dg-modal-title');
    expect(title?.textContent).toBe('2차');
    closeModal();
  });
});
```

- [ ] **Step 3: Run — fail 확인**

```bash
npx vitest run tests/unit/modals-shared.spec.ts
```

Expected: fail (focus-trap 미통합, aria-labelledby 없음).

- [ ] **Step 4: 구현 — `src/ui/modals/shared.ts` 재작성**

Replace entire file content:

```typescript
import { createFocusTrap, type FocusTrap } from '../utils/focus-trap';

export interface ModalConfig {
  title: string;
  /** Caller-owned HTML; caller MUST escape any user interpolation before passing. */
  bodyHtml: string;
  /** Called after the modal is closed (ESC, backdrop, close button, or programmatic).
   *  Use for cleanup only — do NOT rely on this for save semantics (ESC would save-on-cancel). */
  onClose?: () => void;
}

interface CloseOpts {
  skipFocusRestore?: boolean;
}

let active: HTMLDivElement | null = null;
let escHandler: ((e: KeyboardEvent) => void) | null = null;
let activeOnClose: (() => void) | null = null;
let activeFocusTrap: FocusTrap | null = null;
let lastFocusedBeforeOpen: HTMLElement | null = null;

export function openModal(cfg: ModalConfig): void {
  // 1. focus 저장 먼저 (기존 모달이 focus를 건드리기 전)
  if (!lastFocusedBeforeOpen) {
    lastFocusedBeforeOpen = document.activeElement as HTMLElement | null;
  }

  // 2. 이전 모달 정리 (focus 복원은 skip — 1번에서 이미 원본 저장)
  closeModal({ skipFocusRestore: true });

  const root = document.getElementById('modalRoot') ?? document.body;
  const wrap = document.createElement('div');
  wrap.className = 'dg-modal';
  wrap.setAttribute('role', 'dialog');
  wrap.setAttribute('aria-modal', 'true');

  const titleId = `dg-modal-title-${Date.now()}-${Math.random().toString(36).slice(2, 6)}`;
  wrap.setAttribute('aria-labelledby', titleId);

  // eslint-disable-next-line no-restricted-syntax -- 정적 셸; title/body는 아래에서 textContent/caller-escaped
  wrap.innerHTML = `
    <div class="dg-modal-backdrop"></div>
    <div class="dg-modal-card">
      <header class="dg-modal-header">
        <h3 class="dg-modal-title" id="${titleId}"></h3>
        <button type="button" class="dg-modal-close" aria-label="닫기">×</button>
      </header>
      <div class="dg-modal-body"></div>
    </div>
  `;

  const titleEl = wrap.querySelector<HTMLHeadingElement>('.dg-modal-title');
  if (titleEl) titleEl.textContent = cfg.title;

  const bodyEl = wrap.querySelector<HTMLDivElement>('.dg-modal-body');
  // eslint-disable-next-line no-restricted-syntax -- caller 책임 (ModalConfig contract)
  if (bodyEl) bodyEl.innerHTML = cfg.bodyHtml;

  wrap.querySelector('.dg-modal-close')?.addEventListener('click', () => closeModal());
  wrap.querySelector('.dg-modal-backdrop')?.addEventListener('click', () => closeModal());

  root.append(wrap);
  active = wrap;
  activeOnClose = cfg.onClose ?? null;

  escHandler = (e) => { if (e.key === 'Escape') closeModal(); };
  document.addEventListener('keydown', escHandler);

  // 5. focus trap 활성화 (첫 focusable에 자동 포커스)
  activeFocusTrap = createFocusTrap(wrap);
  activeFocusTrap.activate();
}

export function closeModal(opts?: CloseOpts): void {
  const cb = activeOnClose;

  if (activeFocusTrap) {
    activeFocusTrap.deactivate();
    activeFocusTrap = null;
  }
  if (active) {
    active.remove();
    active = null;
  }
  if (escHandler) {
    document.removeEventListener('keydown', escHandler);
    escHandler = null;
  }
  activeOnClose = null;

  if (!opts?.skipFocusRestore) {
    lastFocusedBeforeOpen?.focus();
    lastFocusedBeforeOpen = null;
  }

  if (cb) cb();
}
```

- [ ] **Step 5: Run test — pass 확인**

```bash
npx vitest run tests/unit/modals-shared.spec.ts
```

- [ ] **Step 6: 기존 사용처 스모크 — archive 모달 여전히 동작**

```bash
npx playwright test tests/smoke/modal-overhaul.spec.ts
```

Expected: 이전 pass 유지.

- [ ] **Step 7: tsc + lint**

```bash
npm run build && npm run lint
```

- [ ] **Step 8: Commit**

```bash
git add src/ui/modals/shared.ts tests/unit/modals-shared.spec.ts
git commit -m "feat(modal): integrate focus-trap + aria-labelledby + focus restore"
```

---

## Task A7: Playwright E2E — focus-trap + 이전 focus 복원

**Files:**
- Create: `tests/smoke/modal-overhaul.spec.ts` (기존 파일에 test 추가)

- [ ] **Step 1: Add focus-trap smoke tests**

Append to `tests/smoke/modal-overhaul.spec.ts`:

```typescript
test('focus trap cycles Tab within modal', async ({ page }) => {
  await page.goto('/');
  // 데모 상태에서 settings 탭 열고 어떤 모달 트리거 — Phase C에서 memo 모달로 교체 가능
  // 여기선 archive 모달로 대체 (Phase A 검증용)
  await page.evaluate(() => {
    localStorage.setItem('user', JSON.stringify({
      name: '테', interests: ['tech'], onboardedAt: new Date().toISOString(),
      streak: 0, lastActiveDate: new Date().toISOString(), xp: 0, level: 1
    }));
    localStorage.setItem('answers', JSON.stringify([{
      id: 'a1', questionId: 'q1', text: '답', authorId: 'self',
      type: 'reflection', date: new Date().toISOString().slice(0,10), createdAt: Date.now()
    }]));
  });
  await page.reload();
  await page.click('[data-tab-id="archive"]');
  await page.click('.answer-card');

  // focus가 close 버튼에 초기 세팅
  const close = page.locator('.dg-modal-close');
  await expect(close).toBeFocused();

  // ESC로 닫기
  await page.keyboard.press('Escape');
  await expect(page.locator('.dg-modal')).toHaveCount(0);
});

test('ESC closes modal and restores focus', async ({ page }) => {
  await page.goto('/');
  await page.evaluate(() => {
    localStorage.setItem('user', JSON.stringify({
      name: '테', interests: ['tech'], onboardedAt: new Date().toISOString(),
      streak: 0, lastActiveDate: new Date().toISOString(), xp: 0, level: 1
    }));
    localStorage.setItem('answers', JSON.stringify([{
      id: 'a1', questionId: 'q1', text: '답', authorId: 'self',
      type: 'reflection', date: new Date().toISOString().slice(0,10), createdAt: Date.now()
    }]));
  });
  await page.reload();
  await page.click('[data-tab-id="archive"]');

  const trigger = page.locator('.answer-card').first();
  await trigger.focus();
  await trigger.press('Enter');  // or click

  await expect(page.locator('.dg-modal')).toBeVisible();
  await page.keyboard.press('Escape');
  await expect(page.locator('.dg-modal')).toHaveCount(0);
  // Focus 복원은 JSDOM과 실 브라우저 동작 차이가 있어 Vitest에서 검증 완료로 갈음
});
```

- [ ] **Step 2: Run smoke**

```bash
npx playwright test tests/smoke/modal-overhaul.spec.ts
```

Expected: all pass.

- [ ] **Step 3: Commit**

```bash
git add tests/smoke/modal-overhaul.spec.ts
git commit -m "test(smoke): modal overhaul — focus-trap + ESC close"
```

---

# Phase B — Heatmap Palette + AI Bubble Action + Turn Counter

**독립 Phase. A와 병렬 가능, C와도 병렬 가능.**

## Task B1: Heatmap palette ramp (안 B)

**Files:**
- Modify: `src/styles/tokens.css:106-118`
- Test: `tests/unit/heatmap.spec.ts` (확장)

- [ ] **Step 1: Write failing test**

Modify `tests/unit/heatmap.spec.ts` — 기존 파일 끝에 추가

```typescript
describe('v3.3.3 palette ramp B', () => {
  it('light ramp values', async () => {
    const css = await import('fs').then(m => m.promises.readFile('src/styles/tokens.css', 'utf8'));
    const root = css.match(/:root\s*\{[^}]*\}/s)?.[0] ?? '';
    expect(root).toMatch(/--heatmap-l0:\s*#E2E8F0/);
    expect(root).toMatch(/--heatmap-l1:\s*#A5B4FC/);
  });
  it('dark ramp values', async () => {
    const css = await import('fs').then(m => m.promises.readFile('src/styles/tokens.css', 'utf8'));
    const dark = css.match(/\[data-theme="dark"\]\s*\{[^}]*\}/s)?.[0] ?? '';
    expect(dark).toMatch(/--heatmap-l0:\s*#0F172A/);
    expect(dark).toMatch(/--heatmap-l1:\s*#4338CA/);
  });
});
```

- [ ] **Step 2: Run — fail 확인**

```bash
npx vitest run tests/unit/heatmap.spec.ts
```

- [ ] **Step 3: tokens.css 수정**

Replace existing heatmap palette blocks:

```css
/* Light */
--heatmap-l0: #E2E8F0;  /* was var(--bg-input) = #F1F5F9 */
--heatmap-l1: #A5B4FC;  /* was #C7D2FE */
--heatmap-l2: #818CF8;
--heatmap-l3: var(--primary);
```

```css
/* Dark */
--heatmap-l0: #0F172A;  /* was var(--bg-card) = #1E293B */
--heatmap-l1: #4338CA;  /* was #3730A3 */
--heatmap-l2: #6366F1;
--heatmap-l3: var(--primary);
```

- [ ] **Step 4: Pass 확인**

```bash
npx vitest run tests/unit/heatmap.spec.ts
```

- [ ] **Step 5: Playwright visual — 라이트/다크 대비**

Modify `tests/smoke/heatmap.spec.ts` 또는 신규 추가 — 기존 heatmap spec에 확장

```typescript
test('v3.3.3 light l0 is distinguishable from l1', async ({ page }) => {
  await page.goto('/');
  await page.evaluate(() => document.documentElement.dataset['theme'] = 'light');
  const l0 = await page.locator('.heatmap-cell.level-0').first().evaluate((el) => getComputedStyle(el).backgroundColor);
  const l1 = await page.locator('.heatmap-cell.level-1').first().evaluate((el) => getComputedStyle(el).backgroundColor);
  expect(l0).not.toBe(l1);
  expect(l0).toMatch(/226,\s*232,\s*240/);  // #E2E8F0
  expect(l1).toMatch(/165,\s*180,\s*252/);  // #A5B4FC
});

test('v3.3.3 dark l0 separates from card background', async ({ page }) => {
  await page.goto('/');
  await page.evaluate(() => { document.documentElement.dataset['theme'] = 'dark'; document.body.classList.add('dark'); });
  const l0 = await page.locator('.heatmap-cell.level-0').first().evaluate((el) => getComputedStyle(el).backgroundColor);
  expect(l0).toMatch(/15,\s*23,\s*42/);  // #0F172A
});
```

- [ ] **Step 6: Smoke run**

```bash
npx playwright test tests/smoke/heatmap.spec.ts
```

Expected: pass (heatmap이 empty 상태라도 cells는 렌더됨. 아니면 seedAnswer로 활성일 생성).

만약 cells 없어서 fail시:

```typescript
// 테스트 앞에 seed 추가
await page.evaluate(() => {
  localStorage.setItem('user', JSON.stringify({ /* ... */ }));
  localStorage.setItem('answers', JSON.stringify([
    { id: 'a1', ..., date: new Date().toISOString().slice(0,10) }
  ]));
});
await page.reload();
await page.click('[data-tab-id="stats"]');
```

- [ ] **Step 7: Commit**

```bash
git add src/styles/tokens.css tests/unit/heatmap.spec.ts tests/smoke/heatmap.spec.ts
git commit -m "feat(heatmap): redo palette ramp for contrast"
```

---

## Task B2: `addBubble` 시그니처 확장 + 액션 렌더

**Files:**
- Modify: `src/ui/handlers/home.ts:431-439`
- Test: `tests/unit/` 내 home handler 단위 테스트 (없으면 생성)

- [ ] **Step 1: Write failing test**

Create: `tests/unit/chat-bubble.spec.ts`

```typescript
import { describe, it, expect, beforeEach, vi } from 'vitest';

describe('addBubble (internal)', () => {
  // home.ts의 addBubble은 non-exported. 테스트를 위해 export 노출 필요 (구현 시 추가).
  // 대안: DOM 통합 테스트로 external behavior 검증

  beforeEach(() => {
    document.body.innerHTML = '<div id="chatMessages"></div>';
  });

  it('renders plain text bubble without action', async () => {
    const { addBubble } = await import('../../src/ui/handlers/home');
    addBubble('user', '안녕');
    const bubble = document.querySelector('.chat-bubble');
    expect(bubble?.textContent).toBe('안녕');
    expect(bubble?.querySelector('.chat-bubble-action')).toBeNull();
  });

  it('renders action button when provided', async () => {
    const { addBubble } = await import('../../src/ui/handlers/home');
    const onClick = vi.fn();
    addBubble('ai', 'API 키를 등록해 주세요.', { label: '⚙ 설정 열기', onClick });
    const action = document.querySelector<HTMLButtonElement>('.chat-bubble-action');
    expect(action?.textContent).toBe('⚙ 설정 열기');
    action?.click();
    expect(onClick).toHaveBeenCalledOnce();
  });

  it('uses textContent (no innerHTML) for text', async () => {
    const { addBubble } = await import('../../src/ui/handlers/home');
    addBubble('ai', '<script>alert(1)</script>');
    const bubble = document.querySelector('.chat-bubble');
    expect(bubble?.querySelector('script')).toBeNull();
    expect(bubble?.textContent).toBe('<script>alert(1)</script>');  // 텍스트로 렌더
  });
});
```

- [ ] **Step 2: Run — fail 확인 (addBubble이 exported 아닐 것)**

```bash
npx vitest run tests/unit/chat-bubble.spec.ts
```

- [ ] **Step 3: home.ts 수정 — export + 시그니처 확장**

Modify `src/ui/handlers/home.ts` line 431-439 (`addBubble` 함수):

```typescript
export interface BubbleAction {
  label: string;
  onClick: () => void;
}

export function addBubble(
  role: 'user' | 'ai',
  text: string,
  action?: BubbleAction,
): void {
  const msgs = document.getElementById('chatMessages');
  if (!msgs) return;

  const b = document.createElement('div');
  b.className = `chat-bubble chat-${role}`;

  const textNode = document.createElement('span');
  textNode.textContent = text;  // XSS-safe
  b.append(textNode);

  if (action) {
    const btn = document.createElement('button');
    btn.type = 'button';
    btn.className = 'chat-bubble-action';
    btn.textContent = action.label;
    btn.addEventListener('click', action.onClick);
    b.append(btn);
  }

  msgs.append(b);
  msgs.scrollTop = msgs.scrollHeight;
}
```

- [ ] **Step 4: Pass 확인**

```bash
npx vitest run tests/unit/chat-bubble.spec.ts
```

- [ ] **Step 5: CSS 추가**

Modify `src/styles/pages/home.css` 끝에 추가:

```css
.chat-bubble-action {
  display: inline-block;
  margin-left: 8px;
  padding: 4px 10px;
  border-radius: 12px;
  border: 1px solid var(--primary);
  background: var(--primary-ultra-light);
  color: var(--primary);
  font-size: 0.8rem;
  font-weight: 600;
  cursor: pointer;
  font-family: inherit;
}
.chat-bubble-action:hover {
  background: var(--primary);
  color: #fff;
}
.chat-bubble-action:focus-visible {
  outline: 2px solid var(--primary);
  outline-offset: 2px;
}
```

- [ ] **Step 6: Commit**

```bash
git add src/ui/handlers/home.ts src/styles/pages/home.css tests/unit/chat-bubble.spec.ts
git commit -m "feat(chat): addBubble supports optional action button"
```

---

## Task B3: `openSettingsWithFocus` helper + early-return turn counter

**Files:**
- Modify: `src/ui/handlers/home.ts`
- Test: `tests/unit/chat-bubble.spec.ts` 확장

- [ ] **Step 1: Write failing test**

Append to `tests/unit/chat-bubble.spec.ts`:

```typescript
import { switchTab } from '../../src/ui/nav';

vi.mock('../../src/ui/nav', () => ({
  switchTab: vi.fn(),
  TABS: [{ id: 'settings', label: '설정', icon: '⚙️', render: vi.fn() }],
}));

describe('openSettingsWithFocus', () => {
  beforeEach(() => {
    document.body.innerHTML = `
      <nav>
        <button class="nav-item active" data-tab-id="home">홈</button>
        <button class="nav-item" data-tab-id="settings">설정</button>
      </nav>
      <div id="app"></div>
    `;
    vi.clearAllMocks();
  });

  it('calls switchTab when not on settings', async () => {
    const { openSettingsWithFocus } = await import('../../src/ui/handlers/home');
    openSettingsWithFocus();
    expect(switchTab).toHaveBeenCalledWith('settings');
  });

  it('does not call switchTab when already on settings', async () => {
    document.querySelector('.nav-item.active')!.classList.remove('active');
    document.querySelector('[data-tab-id="settings"]')!.classList.add('active');

    const { openSettingsWithFocus } = await import('../../src/ui/handlers/home');
    openSettingsWithFocus();
    expect(switchTab).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: Run — fail**

```bash
npx vitest run tests/unit/chat-bubble.spec.ts
```

- [ ] **Step 3: 구현 — `openSettingsWithFocus` 추가**

Modify `src/ui/handlers/home.ts` — `import` 블록에 추가:

```typescript
import { switchTab } from '../nav';
```

(이미 import 있으면 skip. 현재는 `switchTab`이 nav.ts에 있음)

파일 어딘가(다른 helper 근처)에 추가:

```typescript
export function openSettingsWithFocus(): void {
  const active = document.querySelector('.nav-item.active');
  const isAlreadySettings = active?.getAttribute('data-tab-id') === 'settings';

  const scrollToField = (): void => {
    const field = document.getElementById('apiKeyInput');
    field?.scrollIntoView({ behavior: 'smooth', block: 'center' });
  };

  if (isAlreadySettings) {
    scrollToField();
  } else {
    switchTab('settings');
    requestAnimationFrame(scrollToField);
  }
}
```

- [ ] **Step 4: Pass 확인**

```bash
npx vitest run tests/unit/chat-bubble.spec.ts
```

- [ ] **Step 5: submitAnswer early-return 수정**

Modify `src/ui/handlers/home.ts` — 현재 line 493-497:

Before:
```typescript
  const key = getApiKey();
  if (!key) {
    addBubble('ai', 'AI 피드백을 받으려면 설정에서 API 키를 등록해 주세요.');
    return;
  }
```

After:
```typescript
  const key = getApiKey();
  if (!key) {
    addBubble('ai', 'AI 응답을 받으려면 API 키를 등록해 주세요.', {
      label: '⚙ 설정 열기',
      onClick: openSettingsWithFocus,
    });
    updateTurnCounter(loadChatHistory(getDateStr()).length);
    return;
  }
```

- [ ] **Step 6: sendChatMessage early-return 수정**

Modify same file — 현재 line 537-538:

Before:
```typescript
  const key = getApiKey();
  if (!key) { addBubble('ai', 'API 키가 설정되지 않았어요. 설정에서 등록해 주세요.'); return; }
```

After:
```typescript
  const key = getApiKey();
  if (!key) {
    addBubble('ai', 'AI 응답을 받으려면 API 키를 등록해 주세요.', {
      label: '⚙ 설정 열기',
      onClick: openSettingsWithFocus,
    });
    updateTurnCounter(loadChatHistory(getDateStr()).length);
    return;
  }
```

- [ ] **Step 7: tsc + lint + test run**

```bash
npm run lint && npm test
```

Expected: all pass.

- [ ] **Step 8: Commit**

```bash
git add src/ui/handlers/home.ts tests/unit/chat-bubble.spec.ts
git commit -m "feat(chat): openSettingsWithFocus helper + early-return turn counter"
```

---

## Task B4: Playwright E2E — bubble action flow

**Files:**
- Create: `tests/smoke/chat-bubble-action.spec.ts`

- [ ] **Step 1: Write smoke test**

Create: `tests/smoke/chat-bubble-action.spec.ts`

```typescript
import { test, expect } from '@playwright/test';

test('no API key: submit shows bubble with 설정 열기 action → navigates', async ({ page }) => {
  await page.goto('/');
  // 온보딩 완료 + API 키 없음
  await page.evaluate(() => {
    localStorage.removeItem('apiKey');
    localStorage.setItem('user', JSON.stringify({
      name: '테', interests: ['tech'], onboardedAt: new Date().toISOString(),
      streak: 0, lastActiveDate: new Date().toISOString(), xp: 0, level: 1
    }));
  });
  await page.reload();

  // 홈 탭 답변 입력
  const textarea = page.locator('#answerArea');
  await textarea.fill('오늘은 TypeScript 타입 가드를 공부했다. 꽤 유용하다.');
  await page.locator('#submitBtn').click();

  // AI 가이드 bubble + 액션 버튼 출현
  const action = page.locator('.chat-bubble-action', { hasText: '설정 열기' });
  await expect(action).toBeVisible();

  await action.click();

  // 설정 탭으로 전환
  await expect(page.locator('.nav-item.active')).toHaveAttribute('data-tab-id', 'settings');
  // API 키 필드 존재
  await expect(page.locator('#apiKeyInput')).toBeVisible();
});
```

- [ ] **Step 2: Run smoke**

```bash
npx playwright test tests/smoke/chat-bubble-action.spec.ts
```

Expected: pass.

- [ ] **Step 3: Commit**

```bash
git add tests/smoke/chat-bubble-action.spec.ts
git commit -m "test(smoke): chat bubble action → settings navigation"
```

---

# Phase C — Cardnews (풀블리드 브리핑 카드)

**전제**: Phase A 완료(메모 모달이 `.dg-modal` 사용).
Phase B와 병렬 가능.

## Task C1: CSP `img-src` 확장 (firebase.json + index.html)

**Files:**
- Modify: `firebase.json:11`
- Modify: `index.html:7`
- Test: `tests/smoke/` (실 헤더 검증은 Firebase Preview 배포 후 — 여기선 파일 검증)

- [ ] **Step 1: Write failing test**

Create: `tests/unit/csp.spec.ts`

```typescript
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'fs';

describe('CSP img-src policy', () => {
  it('firebase.json allows https: img', () => {
    const json = JSON.parse(readFileSync('firebase.json', 'utf8'));
    const csp = json.hosting.headers[0].headers.find(
      (h: { key: string; value: string }) => h.key === 'Content-Security-Policy',
    ).value;
    expect(csp).toMatch(/img-src[^;]*https:/);
  });

  it('index.html meta tag allows https: img', () => {
    const html = readFileSync('index.html', 'utf8');
    const meta = html.match(/<meta http-equiv="Content-Security-Policy"[^>]*content="([^"]+)"/);
    expect(meta?.[1]).toMatch(/img-src[^;]*https:/);
  });

  it('firebase.json and index.html CSP stay in sync', () => {
    const json = JSON.parse(readFileSync('firebase.json', 'utf8'));
    const fbCsp = json.hosting.headers[0].headers.find(
      (h: { key: string; value: string }) => h.key === 'Content-Security-Policy',
    ).value;
    const html = readFileSync('index.html', 'utf8');
    const metaCsp = html.match(/content="([^"]+)"/)?.[1] ?? '';
    // img-src 정책이 양쪽에 동일하게 존재
    const fbImg = fbCsp.match(/img-src[^;]+/)?.[0];
    const metaImg = metaCsp.match(/img-src[^;]+/)?.[0];
    expect(fbImg).toBe(metaImg);
  });
});
```

- [ ] **Step 2: Run — fail 확인**

```bash
npx vitest run tests/unit/csp.spec.ts
```

- [ ] **Step 3: firebase.json 수정**

Modify line 11 (`Content-Security-Policy` value) — `img-src 'self' data:` → `img-src 'self' data: https:`

Full new value (diff):
```
...; img-src 'self' data: https:; ...
```

- [ ] **Step 4: index.html 수정**

Modify line 7 meta tag — 동일하게 `img-src 'self' data:` → `img-src 'self' data: https:`

- [ ] **Step 5: Pass 확인**

```bash
npx vitest run tests/unit/csp.spec.ts
```

- [ ] **Step 6: Commit**

```bash
git add firebase.json index.html tests/unit/csp.spec.ts
git commit -m "chore(csp): expand img-src to https: for rss thumbnails"
```

---

## Task C2: `FeedItem.image` + `extractImage()` 함수

**Files:**
- Modify: `src/services/rss.ts`
- Test: `tests/unit/rss-image.spec.ts` (신규)

- [ ] **Step 1: Pre-implementation check (할루시네이션 Q1)**

실제 rss2json 응답 구조 확인:

```bash
curl -s 'https://api.rss2json.com/v1/api.json?rss_url=https%3A%2F%2Ffeeds.feedburner.com%2FTechCrunch' | head -c 2000
```

Expected: JSON 응답에 `items[].enclosure.link` 또는 `items[].thumbnail` 필드 존재 확인.
만약 둘 다 없으면 Plan의 tier 순서 조정 필요 (description regex가 primary).

결과를 메모/주석으로 기록 후 진행.

- [ ] **Step 2: Write failing tests**

Create: `tests/unit/rss-image.spec.ts`

```typescript
import { describe, it, expect } from 'vitest';
import { extractImage } from '../../src/services/rss';

describe('extractImage', () => {
  it('tier 1: enclosure.link for image/* type', () => {
    const item = { enclosure: { link: 'https://example.com/a.jpg', type: 'image/jpeg' } };
    expect(extractImage(item as never)).toBe('https://example.com/a.jpg');
  });

  it('tier 1: rejects non-image enclosure', () => {
    const item = { enclosure: { link: 'https://example.com/v.mp4', type: 'video/mp4' } };
    expect(extractImage(item as never)).toBeUndefined();
  });

  it('tier 2: thumbnail when enclosure absent', () => {
    const item = { thumbnail: 'https://example.com/t.jpg' };
    expect(extractImage(item as never)).toBe('https://example.com/t.jpg');
  });

  it('tier 3: first <img> from description via DOMParser', () => {
    const item = { description: '<p>hello</p><img src="https://example.com/d.jpg" alt="" />' };
    expect(extractImage(item as never)).toBe('https://example.com/d.jpg');
  });

  it('rejects javascript: URL', () => {
    const item = { enclosure: { link: 'javascript:alert(1)', type: 'image/jpeg' } };
    expect(extractImage(item as never)).toBeUndefined();
  });

  it('rejects http: URL (mixed content)', () => {
    const item = { enclosure: { link: 'http://example.com/a.jpg', type: 'image/jpeg' } };
    expect(extractImage(item as never)).toBeUndefined();
  });

  it('accepts https: only', () => {
    const item = { thumbnail: 'https://example.com/t.jpg' };
    expect(extractImage(item as never)).toBe('https://example.com/t.jpg');
  });

  it('returns undefined when no sources available', () => {
    expect(extractImage({} as never)).toBeUndefined();
  });

  it('tier priority: enclosure > thumbnail > description', () => {
    const item = {
      enclosure: { link: 'https://e.com/1.jpg', type: 'image/png' },
      thumbnail: 'https://e.com/2.jpg',
      description: '<img src="https://e.com/3.jpg" />',
    };
    expect(extractImage(item as never)).toBe('https://e.com/1.jpg');
  });
});
```

- [ ] **Step 3: Run — fail (함수 미존재)**

```bash
npx vitest run tests/unit/rss-image.spec.ts
```

- [ ] **Step 4: 구현 — rss.ts 수정**

Modify `src/services/rss.ts`:

```typescript
export interface FeedItem {
  title: string;
  link: string;
  description: string;
  pubDate: string;
  image?: string;  // v3.3.3 added
}

// rss2json raw response shape (partial)
export interface RawFeedItem {
  title?: string;
  link?: string;
  description?: string;
  pubDate?: string;
  enclosure?: { link?: string; type?: string };
  thumbnail?: string;
}

function sanitizeHttpsUrl(candidate: string | undefined): string | undefined {
  if (!candidate) return undefined;
  try {
    const u = new URL(candidate);
    if (u.protocol !== 'https:') return undefined;
    return u.toString();
  } catch {
    return undefined;
  }
}

export function extractImage(raw: RawFeedItem): string | undefined {
  // tier 1: enclosure (image/* only)
  if (raw.enclosure?.type?.startsWith('image/')) {
    const url = sanitizeHttpsUrl(raw.enclosure.link);
    if (url) return url;
  }
  // tier 2: thumbnail
  const thumb = sanitizeHttpsUrl(raw.thumbnail);
  if (thumb) return thumb;
  // tier 3: DOMParser on description
  if (raw.description) {
    try {
      const doc = new DOMParser().parseFromString(raw.description, 'text/html');
      const img = doc.querySelector('img');
      const src = img?.getAttribute('src');
      const url = sanitizeHttpsUrl(src ?? undefined);
      if (url) return url;
    } catch {
      // ignore
    }
  }
  return undefined;
}

// fetchFeed 확장: image 필드 세팅
export async function fetchFeed(
  feedUrl: string,
  opts: { timeoutMs?: number } = {},
): Promise<FeedResult> {
  const controller = new AbortController();
  const timer = setTimeout(() => controller.abort(), opts.timeoutMs ?? 5000);
  try {
    const url = `https://api.rss2json.com/v1/api.json?rss_url=${encodeURIComponent(feedUrl)}`;
    const r = await fetch(url, { signal: controller.signal });
    if (!r.ok) return { items: [], sourceTitle: '' };
    const data = (await r.json()) as {
      items?: RawFeedItem[];
      feed?: { title?: string };
    };
    const items: FeedItem[] = Array.isArray(data.items)
      ? data.items.map((raw) => ({
          title: raw.title ?? '',
          link: raw.link ?? '',
          description: raw.description ?? '',
          pubDate: raw.pubDate ?? '',
          image: extractImage(raw),
        }))
      : [];
    return { items, sourceTitle: data.feed?.title ?? '' };
  } catch {
    return { items: [], sourceTitle: '' };
  } finally {
    clearTimeout(timer);
  }
}
```

- [ ] **Step 5: Pass 확인**

```bash
npx vitest run tests/unit/rss-image.spec.ts tests/unit/rss.spec.ts
```

Expected: new tests pass, 기존 rss.spec.ts도 pass 유지 (`FeedItem`에 optional image 추가는 backward compat).

- [ ] **Step 6: Commit**

```bash
git add src/services/rss.ts tests/unit/rss-image.spec.ts
git commit -m "feat(rss): extractImage() with 3-tier fallback (https-only)"
```

---

## Task C3: `Briefing.imageUrl` + `pickBriefings(5)` + refreshBriefings 확장

**Files:**
- Modify: `src/state/briefings.ts`
- Modify: `src/ui/handlers/home.ts` — `refreshBriefings()` 에서 image 저장, `pickBriefings(fetched, 5)`
- Test: `tests/unit/state-briefings.spec.ts` + `tests/unit/briefings-dedup.spec.ts`

- [ ] **Step 1: state/briefings.ts 확장**

Modify `src/state/briefings.ts` — `Briefing` interface:

```typescript
export interface Briefing {
  id: string;
  date: string;
  url: string;
  title: string;
  summary: string;
  scrapped: boolean;
  read: boolean;
  memo: string;
  sourceTitle?: string;
  imageUrl?: string;  // v3.3.3 added
}
```

- [ ] **Step 2: Unit test — imageUrl 호환성**

Modify `tests/unit/state-briefings.spec.ts` 혹은 기존 테스트 확인 후 추가:

```typescript
it('imageUrl is optional (backward compat)', () => {
  const b: Briefing = {
    id: '1', date: '2026-04-23', url: 'https://x.com', title: 't',
    summary: 's', scrapped: false, read: false, memo: '',
  };
  saveBriefings([b]);
  expect(loadBriefings()[0]!.imageUrl).toBeUndefined();
});

it('imageUrl persists when set', () => {
  const b: Briefing = {
    id: '1', date: '2026-04-23', url: 'https://x.com', title: 't',
    summary: 's', scrapped: false, read: false, memo: '',
    imageUrl: 'https://x.com/img.jpg',
  };
  saveBriefings([b]);
  expect(loadBriefings()[0]!.imageUrl).toBe('https://x.com/img.jpg');
});
```

- [ ] **Step 3: Run — 컴파일·실행 확인**

```bash
npx vitest run tests/unit/state-briefings.spec.ts
```

- [ ] **Step 4: home.ts `refreshBriefings` 수정 — image 저장 + 5장**

Find `refreshBriefings` 함수 (line 270 근처). 수정:

```typescript
async function refreshBriefings(): Promise<void> {
  // ... 기존 로직 ...
  const chosen = pickBriefings(fetched, 5);  // 3 → 5
  const date = getDateStr();
  const newBriefings: Briefing[] = chosen.map(({ item, sourceTitle }) => ({
    id: `b_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`,
    date,
    url: item.link,
    title: item.title,
    summary: item.description.slice(0, 200),  // 기존 로직 유지
    scrapped: false,
    read: false,
    memo: '',
    sourceTitle,
    imageUrl: item.image,  // v3.3.3 added
  }));
  saveBriefings(newBriefings);
  hydrateBriefings();
}
```

(현재 정확한 로직은 파일 읽어 확인 필요 — 이 step은 해당 부분을 찾아 수정)

- [ ] **Step 5: briefings-dedup 테스트 업데이트**

Modify `tests/unit/briefings-dedup.spec.ts` — `pickBriefings(feeds, 3)` → `pickBriefings(feeds, 5)` 인 테스트 추가

```typescript
it('limits to 5 when target=5', () => {
  const feeds = [
    { sourceTitle: 'A', items: Array.from({length: 10}, (_, i) => ({
      title: `a${i}`, link: `https://a.com/${i}`, description: '', pubDate: ''
    })) },
  ];
  const picked = pickBriefings(feeds, 5);
  expect(picked).toHaveLength(5);
});
```

- [ ] **Step 6: Run tests**

```bash
npx vitest run tests/unit/briefings-dedup.spec.ts tests/unit/state-briefings.spec.ts
```

- [ ] **Step 7: Commit**

```bash
git add src/state/briefings.ts src/ui/handlers/home.ts tests/unit/state-briefings.spec.ts tests/unit/briefings-dedup.spec.ts
git commit -m "feat(briefings): imageUrl field + 5-card target"
```

---

## Task C4: 카드뉴스 CSS — 기본 구조 + 반응형 그리드

**Files:**
- Modify: `src/styles/components/cards.css` — `.briefing-*` 전면 재작성

- [ ] **Step 1: 백업 확인**

```bash
git diff src/styles/components/cards.css
# 현재 변경 없음 확인 후 진행
```

- [ ] **Step 2: cards.css 교체**

Replace `/* Briefing Cards */` 섹션 전체 (lines 26-142 근처) with:

```css
/* ============================================
   Briefing Cards (v3.3.3 — Full-bleed Cardnews)
   ============================================ */
.briefing-scroll {
  display: grid;
  grid-template-columns: 1fr;
  gap: 16px;
  padding: 0 20px 16px;
}

.briefing-empty {
  grid-column: 1 / -1;
  min-height: 120px;
  display: flex; align-items: center; justify-content: center;
  padding: 24px 20px;
  background: var(--bg-card); border: 1px dashed var(--border);
  border-radius: var(--radius-lg);
  font-size: 0.875rem; color: var(--text-tertiary);
  text-align: center; line-height: 1.5;
}

.briefing-card {
  position: relative;
  aspect-ratio: 4 / 5;
  border-radius: var(--radius-lg);
  overflow: hidden;
  background: var(--bg-card);
  transition: transform 0.2s;
}
.briefing-card:hover { transform: translateY(-2px); }

.briefing-card .card-main {
  display: block;
  width: 100%; height: 100%;
  color: inherit; text-decoration: none;
  position: relative;
}

.briefing-card .card-thumb {
  position: absolute; inset: 0;
  width: 100%; height: 100%;
  object-fit: cover;
  transition: filter 0.3s;
}

.briefing-card .card-initial {
  position: absolute; inset: 0;
  display: none;
  align-items: center; justify-content: center;
  font-family: var(--font-display);
  font-style: italic;
  font-size: 8rem;
  color: rgba(255, 255, 255, 0.25);
  pointer-events: none;
}

.briefing-card[data-tier="2"] .card-thumb { display: none; }
.briefing-card[data-tier="2"] .card-initial { display: flex; }
.briefing-card[data-tier="2"] {
  background: linear-gradient(135deg, var(--primary), var(--primary-light));
}

.briefing-card .card-overlay {
  position: absolute; inset: 0;
  background: linear-gradient(180deg,
    rgba(0, 0, 0, 0.35) 0%,
    transparent 25%,
    transparent 50%,
    rgba(0, 0, 0, 0.85) 100%);
  padding: 16px;
  display: flex; flex-direction: column;
  justify-content: space-between;
}

.briefing-card .card-source {
  align-self: flex-start;
  background: rgba(255, 255, 255, 0.95);
  color: #111;
  padding: 4px 10px;
  border-radius: 12px;
  font-size: 0.75rem;
  font-weight: 600;
  max-width: 60%;
  overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
}

.briefing-card .card-title {
  color: #fff;
  font-size: 1.05rem;
  font-weight: 600;
  text-shadow: 0 1px 3px rgba(0, 0, 0, 0.6);
  margin: 0 0 6px;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
  line-height: 1.3;
}

.briefing-card .card-summary {
  color: rgba(255, 255, 255, 0.85);
  font-size: 0.85rem;
  text-shadow: 0 1px 2px rgba(0, 0, 0, 0.6);
  margin: 0;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
  line-height: 1.4;
}

.briefing-card .card-actions {
  position: absolute;
  bottom: 12px; right: 12px;
  display: flex;
  gap: 4px;
  background: rgba(255, 255, 255, 0.15);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  border-radius: 999px;
  padding: 4px;
}

.briefing-card .card-action-btn {
  width: 40px; height: 40px;
  display: inline-flex; align-items: center; justify-content: center;
  background: transparent; border: none;
  color: #fff; font-size: 1.1rem;
  cursor: pointer; border-radius: 50%;
  transition: background 0.15s;
}
.briefing-card .card-action-btn:hover,
.briefing-card .card-action-btn:focus-visible {
  background: rgba(255, 255, 255, 0.25);
  outline: none;
}
.briefing-card .card-action-btn[data-action="scrap"].is-scrapped {
  color: var(--accent);
}

/* Read state */
.briefing-card[data-read="true"] { opacity: 0.7; }
.briefing-card[data-read="true"] .card-thumb { filter: grayscale(0.6); }

/* Responsive */
@media (min-width: 640px) {
  .briefing-scroll { grid-template-columns: repeat(2, 1fr); }
}
@media (min-width: 1024px) {
  .briefing-scroll { grid-template-columns: repeat(3, 1fr); }
  .briefing-card:nth-child(5) { grid-column: span 2; }
}
```

- [ ] **Step 3: 빌드 확인**

```bash
npm run build
```

Expected: 0 warning.

- [ ] **Step 4: Visual smoke (눈으로 확인)**

```bash
npm run dev
# 브라우저에서 홈 탭 진입 후 카드 5장 그리드 확인
# 아직 DOM이 기존 renderBriefingCard를 쓰므로 stylesheet 불일치 → C5에서 정합
```

참고: CSS만 먼저 이식. DOM은 Task C5에서 교체. 이 단계는 빌드 실패 없는 것만 확인.

- [ ] **Step 5: Commit**

```bash
git add src/styles/components/cards.css
git commit -m "feat(cardnews): full-bleed briefing card styles"
```

---

## Task C5: `renderBriefingCard` 재작성 + 3-tier 이미지 로직

**Files:**
- Modify: `src/ui/handlers/home.ts` — `renderBriefingCard`, `hydrateBriefings` 관련
- Test: `tests/unit/render-briefing.spec.ts` (신규)

- [ ] **Step 1: Write failing test**

Create: `tests/unit/render-briefing.spec.ts`

```typescript
import { describe, it, expect, beforeEach, vi } from 'vitest';

// home.ts의 renderBriefingCard를 export 필요
// 이 task에서 export 추가

describe('renderBriefingCard (v3.3.3 cardnews)', () => {
  beforeEach(() => {
    document.body.innerHTML = '<div id="briefingScroll"></div>';
  });

  it('renders tier-1 when imageUrl present', async () => {
    const { renderBriefingCard } = await import('../../src/ui/handlers/home');
    const card = renderBriefingCard({
      id: '1', date: '2026-04-23', url: 'https://x.com/a',
      title: '테스트 제목', summary: '요약',
      scrapped: false, read: false, memo: '',
      sourceTitle: 'TechCrunch', imageUrl: 'https://img.com/a.jpg',
    }, 0);
    expect(card.getAttribute('data-tier')).toBe('1');
    expect(card.querySelector('img.card-thumb')?.getAttribute('src')).toBe('https://img.com/a.jpg');
  });

  it('renders tier-2 when imageUrl absent', async () => {
    const { renderBriefingCard } = await import('../../src/ui/handlers/home');
    const card = renderBriefingCard({
      id: '1', date: '2026-04-23', url: 'https://x.com',
      title: 't', summary: 's', scrapped: false, read: false, memo: '',
      sourceTitle: 'Medium',
    }, 0);
    expect(card.getAttribute('data-tier')).toBe('2');
    expect(card.querySelector('img.card-thumb')).toBeNull();
    expect(card.querySelector('.card-initial')?.textContent).toBe('M');
  });

  it('switches to tier-2 on img error', async () => {
    const { renderBriefingCard } = await import('../../src/ui/handlers/home');
    const card = renderBriefingCard({
      id: '1', date: '2026-04-23', url: 'https://x.com',
      title: 't', summary: 's', scrapped: false, read: false, memo: '',
      sourceTitle: 'Ars', imageUrl: 'https://bad.com/x.jpg',
    }, 0);
    const img = card.querySelector<HTMLImageElement>('img.card-thumb')!;
    img.dispatchEvent(new Event('error'));
    expect(card.getAttribute('data-tier')).toBe('2');
    expect(card.querySelector('.card-initial')?.textContent).toBe('A');
  });

  it('action buttons exist outside <a>', async () => {
    const { renderBriefingCard } = await import('../../src/ui/handlers/home');
    const card = renderBriefingCard({
      id: '1', date: '2026-04-23', url: 'https://x.com',
      title: 't', summary: 's', scrapped: false, read: false, memo: '',
    }, 0);
    const main = card.querySelector('.card-main');
    const actions = card.querySelector('.card-actions');
    expect(main).not.toBeNull();
    expect(actions).not.toBeNull();
    expect(main?.contains(actions!)).toBe(false);  // 버튼은 <a> 밖
  });

  it('read state: data-read=true', async () => {
    const { renderBriefingCard } = await import('../../src/ui/handlers/home');
    const card = renderBriefingCard({
      id: '1', date: '2026-04-23', url: 'https://x.com',
      title: 't', summary: 's', scrapped: false, read: true, memo: '',
    }, 0);
    expect(card.getAttribute('data-read')).toBe('true');
  });

  it('scrapped state applied to button', async () => {
    const { renderBriefingCard } = await import('../../src/ui/handlers/home');
    const card = renderBriefingCard({
      id: '1', date: '2026-04-23', url: 'https://x.com',
      title: 't', summary: 's', scrapped: true, read: false, memo: '',
    }, 0);
    const scrap = card.querySelector<HTMLButtonElement>('[data-action="scrap"]');
    expect(scrap?.classList.contains('is-scrapped')).toBe(true);
  });
});
```

- [ ] **Step 2: Run — fail**

```bash
npx vitest run tests/unit/render-briefing.spec.ts
```

- [ ] **Step 3: 구현 — `renderBriefingCard` 재작성**

Modify `src/ui/handlers/home.ts`:
- `renderBriefingCard` 함수를 `export` 로 변경
- 기존 구조(title 링크 + summary + actions + memo) 완전히 교체

```typescript
export function renderBriefingCard(b: Briefing, idx: number): HTMLElement {
  const card = document.createElement('article');
  card.className = 'briefing-card';
  card.dataset['read'] = b.read ? 'true' : 'false';
  card.dataset['tier'] = b.imageUrl ? '1' : '2';

  // Main link (이미지 + overlay + 텍스트)
  const main = document.createElement('a');
  main.className = 'card-main';
  main.href = b.url;
  main.target = '_blank';
  main.rel = 'noopener noreferrer';
  const label = `${b.title} — ${b.sourceTitle ?? '기사'}`;
  main.setAttribute('aria-label', label);

  // Thumbnail (tier 1) 또는 없음 (tier 2)
  if (b.imageUrl) {
    const img = document.createElement('img');
    img.className = 'card-thumb';
    img.src = b.imageUrl;
    img.alt = '';
    img.loading = 'lazy';
    img.decoding = 'async';
    img.referrerPolicy = 'no-referrer';
    img.addEventListener('error', () => {
      card.dataset['tier'] = '2';
      img.remove();
    });
    main.append(img);
  }

  // Initial letter (tier 2 fallback)
  const initial = document.createElement('div');
  initial.className = 'card-initial';
  initial.setAttribute('aria-hidden', 'true');
  initial.textContent = (b.sourceTitle?.[0] ?? '?').toUpperCase();
  main.append(initial);

  // Overlay (source + title + summary)
  const overlay = document.createElement('div');
  overlay.className = 'card-overlay';

  const sourceBadge = document.createElement('span');
  sourceBadge.className = 'card-source';
  sourceBadge.textContent = b.sourceTitle ?? '기사';
  overlay.append(sourceBadge);

  const textBlock = document.createElement('div');
  textBlock.className = 'card-text';
  const title = document.createElement('h3');
  title.className = 'card-title';
  title.textContent = b.title;
  const summary = document.createElement('p');
  summary.className = 'card-summary';
  summary.textContent = b.summary;
  textBlock.append(title, summary);
  overlay.append(textBlock);

  main.append(overlay);
  card.append(main);

  // Actions (외부, <a> 밖)
  const actions = document.createElement('div');
  actions.className = 'card-actions';
  actions.setAttribute('role', 'group');
  actions.setAttribute('aria-label', '카드 액션');

  const scrapBtn = document.createElement('button');
  scrapBtn.type = 'button';
  scrapBtn.className = 'card-action-btn';
  scrapBtn.dataset['action'] = 'scrap';
  scrapBtn.setAttribute('aria-label', b.scrapped ? '스크랩 해제' : '스크랩');
  scrapBtn.textContent = b.scrapped ? '♥' : '♡';
  if (b.scrapped) scrapBtn.classList.add('is-scrapped');
  scrapBtn.addEventListener('click', (e) => {
    e.preventDefault();
    e.stopPropagation();
    toggleScrap(idx);
    hydrateBriefings();
  });

  const memoBtn = document.createElement('button');
  memoBtn.type = 'button';
  memoBtn.className = 'card-action-btn';
  memoBtn.dataset['action'] = 'memo';
  memoBtn.setAttribute('aria-label', '메모');
  memoBtn.textContent = '✎';
  memoBtn.addEventListener('click', (e) => {
    e.preventDefault();
    e.stopPropagation();
    openMemoModal(idx);  // Task C6에서 import
  });

  actions.append(scrapBtn, memoBtn);
  card.append(actions);

  // Mark as read when link is clicked
  main.addEventListener('click', () => {
    setRead(idx);
    card.dataset['read'] = 'true';
  });

  return card;
}
```

**주의**: `openMemoModal`은 Task C6에서 구현. 지금 단계에선 import가 실패할 수 있으므로 임시 stub:

```typescript
function openMemoModal(_idx: number): void {
  // Placeholder — replaced in Task C6
}
```

- [ ] **Step 4: Run tests**

```bash
npx vitest run tests/unit/render-briefing.spec.ts
```

- [ ] **Step 5: tsc + full test**

```bash
npm run build && npm test
```

Expected: 0 error, 기존 briefing 테스트 유지.

- [ ] **Step 6: Commit**

```bash
git add src/ui/handlers/home.ts tests/unit/render-briefing.spec.ts
git commit -m "feat(cardnews): renderBriefingCard rewrite with tier 1/2/3"
```

---

## Task C6: 메모 모달 통합

**Files:**
- Create: `src/ui/modals/memo.ts`
- Modify: `src/ui/handlers/home.ts` — stub 제거, 실제 import
- Test: `tests/unit/modals-memo.spec.ts` (신규)

- [ ] **Step 1: Write failing test**

Create: `tests/unit/modals-memo.spec.ts`

```typescript
import { describe, it, expect, beforeEach, vi } from 'vitest';

describe('openMemoModal', () => {
  beforeEach(() => {
    document.body.innerHTML = '<div id="modalRoot"></div>';
    localStorage.setItem('briefings', JSON.stringify([
      { id: '1', date: '2026-04-23', url: 'https://x.com', title: 't', summary: 's',
        scrapped: false, read: false, memo: '초기 메모' },
    ]));
  });

  it('opens modal with existing memo prefilled', async () => {
    const { openMemoModal } = await import('../../src/ui/modals/memo');
    openMemoModal(0);
    const ta = document.querySelector<HTMLTextAreaElement>('#memoInput');
    expect(ta?.value).toBe('초기 메모');
  });

  it('save button persists memo and closes modal', async () => {
    const { openMemoModal } = await import('../../src/ui/modals/memo');
    openMemoModal(0);
    const ta = document.querySelector<HTMLTextAreaElement>('#memoInput')!;
    ta.value = '새 메모';
    document.querySelector<HTMLButtonElement>('#saveMemoBtn')!.click();
    expect(document.querySelector('.dg-modal')).toBeNull();
    const stored = JSON.parse(localStorage.getItem('briefings')!);
    expect(stored[0].memo).toBe('새 메모');
  });

  it('ESC/cancel does not persist changes', async () => {
    const { openMemoModal } = await import('../../src/ui/modals/memo');
    const { closeModal } = await import('../../src/ui/modals/shared');
    openMemoModal(0);
    const ta = document.querySelector<HTMLTextAreaElement>('#memoInput')!;
    ta.value = '취소할 메모';
    closeModal();
    const stored = JSON.parse(localStorage.getItem('briefings')!);
    expect(stored[0].memo).toBe('초기 메모');
  });
});
```

- [ ] **Step 2: Run — fail**

```bash
npx vitest run tests/unit/modals-memo.spec.ts
```

- [ ] **Step 3: 구현**

Create: `src/ui/modals/memo.ts`

```typescript
import { openModal, closeModal } from './shared';
import { loadBriefings, saveMemo } from '../../state/briefings';
import { escapeHtml } from '../../utils/escape';

export function openMemoModal(index: number): void {
  const list = loadBriefings();
  const briefing = list[index];
  if (!briefing) return;

  const existing = briefing.memo ?? '';
  const escaped = escapeHtml(existing);

  openModal({
    title: '메모',
    bodyHtml: `
      <textarea id="memoInput" rows="6"
        style="width:100%;padding:10px;border:1px solid var(--border);border-radius:var(--radius-sm);background:var(--bg-input);color:var(--text-primary);font-family:inherit;font-size:0.95rem;resize:vertical;">${escaped}</textarea>
      <div style="display:flex;gap:8px;justify-content:flex-end;margin-top:12px;">
        <button type="button" id="cancelMemoBtn" class="btn btn-outline">취소</button>
        <button type="button" id="saveMemoBtn" class="btn btn-primary">저장</button>
      </div>
    `,
  });

  const save = document.getElementById('saveMemoBtn');
  const cancel = document.getElementById('cancelMemoBtn');
  const input = document.getElementById('memoInput') as HTMLTextAreaElement | null;

  save?.addEventListener('click', () => {
    if (input) saveMemo(index, input.value);
    closeModal();
  });

  cancel?.addEventListener('click', () => closeModal());
}
```

- [ ] **Step 4: 확인 — escapeHtml 존재**

```bash
ls src/utils/escape.ts 2>/dev/null
grep -rn "escapeHtml" src/utils/ 2>/dev/null | head -3
```

없으면 만들기. 기존 `tests/unit/escapeHtml.spec.ts` 있으므로 이미 구현 존재 추정.

```bash
find src -name "*.ts" -exec grep -l "export.*escapeHtml\|function escapeHtml" {} \;
```

경로 확인 후 import 경로 조정.

- [ ] **Step 5: home.ts의 stub 제거 + import 교체**

Modify `src/ui/handlers/home.ts`:

Top에 import 추가:
```typescript
import { openMemoModal } from '../modals/memo';
```

아래 stub 함수 제거 (Task C5에서 삽입한 `function openMemoModal(_idx): void { /* ... */ }`)

- [ ] **Step 6: Run tests**

```bash
npm test
```

Expected: memo 테스트 pass, 기존 테스트 유지.

- [ ] **Step 7: Commit**

```bash
git add src/ui/modals/memo.ts src/ui/handlers/home.ts tests/unit/modals-memo.spec.ts
git commit -m "feat(memo): modal-based memo editor for briefing cards"
```

---

## Task C7: Cardnews E2E — 모든 tier + 메모 모달

**Files:**
- Create: `tests/smoke/cardnews.spec.ts`

- [ ] **Step 1: Smoke test**

Create: `tests/smoke/cardnews.spec.ts`

```typescript
import { test, expect } from '@playwright/test';

async function seedBriefings(page, withImage: boolean) {
  await page.evaluate((hasImg) => {
    localStorage.setItem('user', JSON.stringify({
      name: '테', interests: ['tech'], onboardedAt: new Date().toISOString(),
      streak: 0, lastActiveDate: new Date().toISOString(), xp: 0, level: 1
    }));
    const items = Array.from({ length: 5 }, (_, i) => ({
      id: `b${i}`, date: new Date().toISOString().slice(0,10),
      url: `https://example.com/a${i}`, title: `제목 ${i}`, summary: `요약 ${i}`,
      scrapped: false, read: false, memo: '',
      sourceTitle: `Source${i}`,
      imageUrl: hasImg ? 'https://picsum.photos/400/500' : undefined,
    }));
    localStorage.setItem('briefings', JSON.stringify(items));
  }, withImage);
  await page.reload();
}

test('5 cards render in grid on home', async ({ page }) => {
  await page.goto('/');
  await seedBriefings(page, true);

  const cards = page.locator('.briefing-card');
  await expect(cards).toHaveCount(5);
});

test('tier 1 image loads when imageUrl valid (CSP passes)', async ({ page }) => {
  await page.goto('/');
  await seedBriefings(page, true);

  const firstImg = page.locator('.briefing-card').first().locator('img.card-thumb');
  await expect(firstImg).toHaveAttribute('src', /picsum\.photos/);

  const tier = await page.locator('.briefing-card').first().getAttribute('data-tier');
  expect(tier).toBe('1');
});

test('tier 2 fallback when no imageUrl', async ({ page }) => {
  await page.goto('/');
  await seedBriefings(page, false);

  const tier = await page.locator('.briefing-card').first().getAttribute('data-tier');
  expect(tier).toBe('2');
  const initial = page.locator('.briefing-card').first().locator('.card-initial');
  await expect(initial).toBeVisible();
});

test('tier 1 → tier 2 on image error', async ({ page }) => {
  await page.goto('/');
  await page.evaluate(() => {
    localStorage.setItem('briefings', JSON.stringify([{
      id: 'b1', date: new Date().toISOString().slice(0,10),
      url: 'https://x.com', title: '테', summary: 's',
      scrapped: false, read: false, memo: '',
      sourceTitle: 'Bad',
      imageUrl: 'https://invalid.invalid/notfound.jpg',
    }]));
  });
  await page.reload();

  const card = page.locator('.briefing-card').first();
  // wait for onerror to fire
  await page.waitForFunction(() => {
    return document.querySelector('.briefing-card')?.getAttribute('data-tier') === '2';
  }, { timeout: 5000 });
  const tier = await card.getAttribute('data-tier');
  expect(tier).toBe('2');
});

test('memo modal opens, saves, persists', async ({ page }) => {
  await page.goto('/');
  await seedBriefings(page, true);

  await page.locator('.briefing-card').first().locator('[data-action="memo"]').click();
  await expect(page.locator('.dg-modal')).toBeVisible();

  await page.locator('#memoInput').fill('E2E 테스트 메모');
  await page.locator('#saveMemoBtn').click();
  await expect(page.locator('.dg-modal')).toHaveCount(0);

  const memo = await page.evaluate(() => JSON.parse(localStorage.getItem('briefings')!)[0].memo);
  expect(memo).toBe('E2E 테스트 메모');
});

test('responsive: desktop shows 3 columns + 5th spans 2', async ({ page, viewport }) => {
  await page.setViewportSize({ width: 1280, height: 800 });
  await page.goto('/');
  await seedBriefings(page, true);

  const grid = page.locator('.briefing-scroll');
  const cols = await grid.evaluate((el) => getComputedStyle(el).gridTemplateColumns);
  expect(cols.split(' ')).toHaveLength(3);

  const fifth = page.locator('.briefing-card').nth(4);
  const gridColumn = await fifth.evaluate((el) => getComputedStyle(el).gridColumn);
  expect(gridColumn).toContain('span 2');
});
```

- [ ] **Step 2: Run smoke**

```bash
npx playwright test tests/smoke/cardnews.spec.ts
```

Expected: all pass.

- [ ] **Step 3: Commit**

```bash
git add tests/smoke/cardnews.spec.ts
git commit -m "test(smoke): cardnews E2E (tiers, memo modal, responsive)"
```

---

# Phase D — Interests Edit Modal

**전제**: Phase A 완료.

## Task D1: 관심 분야 수정 모달 구현

**Files:**
- Create: `src/ui/modals/interests.ts`
- Test: `tests/unit/modals-interests.spec.ts` (신규)

- [ ] **Step 1: Write failing test**

Create: `tests/unit/modals-interests.spec.ts`

```typescript
import { describe, it, expect, beforeEach } from 'vitest';

const INTEREST_STORAGE = 'user';

describe('openInterestsModal', () => {
  beforeEach(() => {
    document.body.innerHTML = '<div id="modalRoot"></div>';
    localStorage.setItem(INTEREST_STORAGE, JSON.stringify({
      name: '테', interests: ['tech', 'business'], onboardedAt: new Date().toISOString(),
      streak: 0, lastActiveDate: new Date().toISOString(), xp: 0, level: 1
    }));
  });

  it('shows current interests as checked', async () => {
    const { openInterestsModal } = await import('../../src/ui/modals/interests');
    openInterestsModal();
    const tech = document.querySelector<HTMLInputElement>('input[value="tech"]');
    const biz = document.querySelector<HTMLInputElement>('input[value="business"]');
    const cul = document.querySelector<HTMLInputElement>('input[value="culture"]');
    expect(tech?.checked).toBe(true);
    expect(biz?.checked).toBe(true);
    expect(cul?.checked).toBe(false);
  });

  it('save updates localStorage', async () => {
    const { openInterestsModal } = await import('../../src/ui/modals/interests');
    openInterestsModal();
    const cul = document.querySelector<HTMLInputElement>('input[value="culture"]')!;
    cul.checked = true;
    cul.dispatchEvent(new Event('change', { bubbles: true }));

    document.querySelector<HTMLButtonElement>('#saveInterestsBtn')!.click();

    const user = JSON.parse(localStorage.getItem(INTEREST_STORAGE)!);
    expect(user.interests).toContain('culture');
  });

  it('save disabled when no interest selected', async () => {
    const { openInterestsModal } = await import('../../src/ui/modals/interests');
    openInterestsModal();
    const boxes = document.querySelectorAll<HTMLInputElement>('input[type="checkbox"]');
    boxes.forEach((b) => { b.checked = false; b.dispatchEvent(new Event('change')); });
    const save = document.querySelector<HTMLButtonElement>('#saveInterestsBtn');
    expect(save?.disabled).toBe(true);
  });

  it('cancel does not persist changes', async () => {
    const { openInterestsModal } = await import('../../src/ui/modals/interests');
    openInterestsModal();
    const cul = document.querySelector<HTMLInputElement>('input[value="culture"]')!;
    cul.checked = true;
    cul.dispatchEvent(new Event('change'));

    document.querySelector<HTMLButtonElement>('#cancelInterestsBtn')!.click();

    const user = JSON.parse(localStorage.getItem(INTEREST_STORAGE)!);
    expect(user.interests).not.toContain('culture');
  });
});
```

- [ ] **Step 2: Check categories source**

```bash
grep -rn "tech\|business\|culture\|INTEREST" src/categories.ts src/data/ 2>/dev/null | head -5
cat tests/unit/categories.spec.ts | head -20
```

카테고리 목록이 어디 정의되어있는지 확인. 아마 `src/data/categories.ts` 혹은 비슷.

- [ ] **Step 3: Run — fail**

```bash
npx vitest run tests/unit/modals-interests.spec.ts
```

- [ ] **Step 4: 구현**

Create: `src/ui/modals/interests.ts`

```typescript
import { openModal, closeModal } from './shared';
import { CATEGORIES } from '../../data/categories';  // 실제 경로 확인 후 조정
import { escapeHtml } from '../../utils/escape';
import { showToast } from '../toast';  // 실제 경로 확인

const USER_STORAGE = 'user';

interface LegacyUser {
  name: string;
  interests: string[];
  onboardedAt: string;
  streak: number;
  lastActiveDate: string;
  xp: number;
  level: number;
}

function loadUser(): LegacyUser | null {
  try {
    const raw = localStorage.getItem(USER_STORAGE);
    return raw ? JSON.parse(raw) as LegacyUser : null;
  } catch {
    return null;
  }
}

function saveUser(u: LegacyUser): void {
  localStorage.setItem(USER_STORAGE, JSON.stringify(u));
}

export function openInterestsModal(): void {
  const user = loadUser();
  if (!user) return;

  const current = new Set(user.interests);

  const checkboxes = CATEGORIES.map((c) => {
    const checked = current.has(c.id) ? 'checked' : '';
    return `
      <label style="display:flex;align-items:center;gap:8px;padding:8px;border:1px solid var(--border);border-radius:var(--radius-sm);cursor:pointer;">
        <input type="checkbox" value="${escapeHtml(c.id)}" ${checked} />
        <span>${escapeHtml(c.label)}</span>
      </label>
    `;
  }).join('');

  openModal({
    title: '관심 분야 수정',
    bodyHtml: `
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;">
        ${checkboxes}
      </div>
      <div style="display:flex;gap:8px;justify-content:flex-end;margin-top:16px;">
        <button type="button" id="cancelInterestsBtn" class="btn btn-outline">취소</button>
        <button type="button" id="saveInterestsBtn" class="btn btn-primary">저장</button>
      </div>
    `,
  });

  const saveBtn = document.getElementById('saveInterestsBtn') as HTMLButtonElement | null;
  const cancelBtn = document.getElementById('cancelInterestsBtn');
  const boxes = document.querySelectorAll<HTMLInputElement>('input[type="checkbox"]');

  const updateSaveState = (): void => {
    const anyChecked = Array.from(boxes).some((b) => b.checked);
    if (saveBtn) saveBtn.disabled = !anyChecked;
  };

  boxes.forEach((b) => b.addEventListener('change', updateSaveState));
  updateSaveState();

  saveBtn?.addEventListener('click', () => {
    const newInterests = Array.from(boxes)
      .filter((b) => b.checked)
      .map((b) => b.value);
    if (newInterests.length === 0) return;
    saveUser({ ...user, interests: newInterests });
    closeModal();
    showToast('관심 분야가 업데이트되었어요');
  });

  cancelBtn?.addEventListener('click', () => closeModal());
}
```

- [ ] **Step 5: Imports 경로 보정**

구체 경로가 다르면 에러. grep 으로 확인:

```bash
find src -name "categories.ts" -o -name "categories.js"
grep -rn "export.*CATEGORIES\|export.*INTERESTS" src/ | head -3
grep -rn "showToast" src/ | head -3
```

경로 맞춰 수정.

- [ ] **Step 6: Run tests**

```bash
npx vitest run tests/unit/modals-interests.spec.ts
```

- [ ] **Step 7: Commit**

```bash
git add src/ui/modals/interests.ts tests/unit/modals-interests.spec.ts
git commit -m "feat(interests): edit modal for reconfiguring categories"
```

---

## Task D2: 설정 탭에 관심 분야 섹션 추가

**Files:**
- Modify: `src/ui/tabs/settings.ts`
- Modify: `src/ui/handlers/settings.ts` (있으면) 또는 settings.ts 자체의 이벤트
- Create/Modify: `src/styles/pages/settings.css`

- [ ] **Step 1: 기존 settings.ts 구조 파악**

```bash
cat src/ui/tabs/settings.ts | head -60
```

- [ ] **Step 2: HTML 섹션 추가**

Modify `src/ui/tabs/settings.ts` — render 함수 내 기존 section 들 사이에 추가:

```typescript
`
<section class="settings-section">
  <h3>관심 분야</h3>
  <div id="currentInterests" class="settings-interests-display"></div>
  <button type="button" id="editInterestsBtn" class="btn btn-outline">수정</button>
</section>
`
```

기존 HTML 문자열 구조에 맞춰 삽입. 섹션 위치: API 키 섹션 근처.

- [ ] **Step 3: Hydration — 현재 interests 칩 표시 + 클릭 바인딩**

render 함수 끝에 추가 (container scope 내):

```typescript
const editBtn = container.querySelector<HTMLButtonElement>('#editInterestsBtn');
const display = container.querySelector<HTMLDivElement>('#currentInterests');

function renderCurrentInterests(): void {
  const raw = localStorage.getItem('user');
  const user = raw ? JSON.parse(raw) as { interests: string[] } : null;
  if (!display) return;
  display.replaceChildren();
  const interests = user?.interests ?? [];
  if (interests.length === 0) {
    display.textContent = '선택된 관심 분야가 없어요.';
    return;
  }
  for (const id of interests) {
    const chip = document.createElement('span');
    chip.className = 'interest-chip';
    chip.textContent = CATEGORIES.find((c) => c.id === id)?.label ?? id;
    display.append(chip);
  }
}

renderCurrentInterests();

editBtn?.addEventListener('click', () => {
  void import('../modals/interests').then(({ openInterestsModal }) => {
    openInterestsModal();
  });
});

// 모달 저장 후 재렌더 — 모달이 closeModal할 때 재렌더 위해 커스텀 이벤트 사용 가능
// 간단히 setInterval 대신 수동 트리거: interests 모달에서 저장 후 dispatch
document.addEventListener('dg:interests:changed', renderCurrentInterests);
```

`interests.ts`의 save 핸들러에 이벤트 dispatch 추가:

```typescript
saveBtn?.addEventListener('click', () => {
  // ... 기존 saveUser ...
  closeModal();
  showToast('관심 분야가 업데이트되었어요');
  document.dispatchEvent(new CustomEvent('dg:interests:changed'));
});
```

- [ ] **Step 4: CSS 추가**

Create/Modify `src/styles/pages/settings.css`:

```css
.settings-interests-display {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  margin: 8px 0 12px;
  min-height: 28px;
  font-size: 0.85rem;
  color: var(--text-secondary);
}
.interest-chip {
  padding: 4px 10px;
  border-radius: 12px;
  background: var(--primary-ultra-light);
  color: var(--primary);
  font-size: 0.8rem;
  font-weight: 600;
}
```

main.css 에 import 없으면 추가.

- [ ] **Step 5: 빌드 확인**

```bash
npm run build
```

- [ ] **Step 6: Commit**

```bash
git add src/ui/tabs/settings.ts src/ui/modals/interests.ts src/styles/pages/settings.css src/styles/main.css
git commit -m "feat(settings): interests section with edit modal trigger"
```

---

## Task D3: Playwright E2E — interests edit flow

**Files:**
- Create: `tests/smoke/interests-edit.spec.ts`

- [ ] **Step 1: Write smoke**

Create: `tests/smoke/interests-edit.spec.ts`

```typescript
import { test, expect } from '@playwright/test';

test('interests edit: open modal, toggle, save, chip updates', async ({ page }) => {
  await page.goto('/');
  await page.evaluate(() => {
    localStorage.setItem('user', JSON.stringify({
      name: '테', interests: ['tech'], onboardedAt: new Date().toISOString(),
      streak: 0, lastActiveDate: new Date().toISOString(), xp: 0, level: 1
    }));
  });
  await page.reload();
  await page.click('[data-tab-id="settings"]');

  // 현재 관심 분야 칩 1개 표시
  const chips = page.locator('.interest-chip');
  await expect(chips).toHaveCount(1);

  await page.click('#editInterestsBtn');
  await expect(page.locator('.dg-modal')).toBeVisible();

  // 추가 카테고리 체크
  const cul = page.locator('input[value="culture"]');
  await cul.check();
  await page.click('#saveInterestsBtn');
  await expect(page.locator('.dg-modal')).toHaveCount(0);

  // 칩 2개로 증가
  await expect(page.locator('.interest-chip')).toHaveCount(2);

  // localStorage 반영
  const interests = await page.evaluate(() => JSON.parse(localStorage.getItem('user')!).interests);
  expect(interests).toContain('culture');
});

test('interests edit: cancel does not persist', async ({ page }) => {
  await page.goto('/');
  await page.evaluate(() => {
    localStorage.setItem('user', JSON.stringify({
      name: '테', interests: ['tech'], onboardedAt: new Date().toISOString(),
      streak: 0, lastActiveDate: new Date().toISOString(), xp: 0, level: 1
    }));
  });
  await page.reload();
  await page.click('[data-tab-id="settings"]');
  await page.click('#editInterestsBtn');

  await page.locator('input[value="culture"]').check();
  await page.click('#cancelInterestsBtn');

  const interests = await page.evaluate(() => JSON.parse(localStorage.getItem('user')!).interests);
  expect(interests).not.toContain('culture');
  await expect(page.locator('.interest-chip')).toHaveCount(1);
});
```

- [ ] **Step 2: Run smoke**

```bash
npx playwright test tests/smoke/interests-edit.spec.ts
```

- [ ] **Step 3: Commit**

```bash
git add tests/smoke/interests-edit.spec.ts
git commit -m "test(smoke): interests edit E2E"
```

---

# Phase E — QA, 독립 리뷰, 배포

## Task E1: 전체 테스트 pass + 빌드 + 린트

**Files:** (없음 — CI-style 검증)

- [ ] **Step 1: Vitest 전체**

```bash
npm test
```

Expected: 모두 pass. 기대 수치: v3.3.2 = 193 기준 + 신규 ~30 = **220+ pass**.

- [ ] **Step 2: Playwright 전체**

```bash
npm run test:smoke
```

Expected: 모두 pass. 기대 수치: v3.3.2 = 41 + 신규 ~15 = **55+ pass**.

- [ ] **Step 3: TypeScript + Vite build**

```bash
npm run build
```

Expected: 0 error, 0 warning. `dist/` 생성. Build size 기록:

```bash
ls -lh dist/assets/*.css dist/assets/*.js 2>/dev/null
```

v3.3.2 대비 CSS +α (카드뉴스), JS +α (extractImage, 모달들, fetch 확장). 상한 예상 40kB CSS / 60kB JS.

- [ ] **Step 4: ESLint**

```bash
npm run lint
```

Expected: 0 error.

- [ ] **Step 5: 수동 브라우저 스모크** (Hayden 체크포인트)

```bash
npm run dev
```

체크리스트:
- [ ] 홈 탭: 카드 5장, tier 1(이미지 있는 RSS) 렌더
- [ ] 홈 탭: 새로고침 → 카드 refresh 동작
- [ ] 홈 탭: 카드 메모 버튼 → 모달, 저장 동작
- [ ] 홈 탭: 카드 스크랩 버튼 → 하트 채워짐
- [ ] 통계 탭: heatmap l0/l1 라이트/다크 모두 육안 구분
- [ ] 설정 탭: 관심 분야 섹션 표시, 수정 모달 저장 동작
- [ ] 아카이브: 기록 상세 모달이 스타일 갖춰 표시 (이전 버그 해소)
- [ ] API 키 없는 상태로 홈 답변 제출 → "⚙ 설정 열기" 액션 bubble, 클릭 시 설정 탭 전환 + scrollIntoView
- [ ] 다크 모드: 모든 모달 backdrop 적절한 opacity (0.6)
- [ ] Tab 키로 모달 내부 순환 (focus-trap)

- [ ] **Step 6: 할루시네이션 Q 재확인**

- Q1 rss2json 필드 확인 (Task C2 step 1에서 이미 완료)
- Q4 archive 모달 (Task A1에서 baseline 캡처, Task A5~A7에서 해결 확인)
- Q5 heatmap 대비 (Task B1 Playwright에서 검증)
- Q6 backdrop opacity — 위 Step 5 육안 체크리스트

모두 체크되면 Phase E1 완료.

- [ ] **Step 7: 상태 커밋 (필요 시)**

여기까진 새 변경 없음. Skip.

---

## Task E2: 독립 코드 리뷰 (CLAUDE.md 정책 — 심층 리뷰)

**Files:** (리뷰 아티팩트)

- [ ] **Step 1: 변경 요약 생성**

```bash
git log --oneline main..feat/v3.3.3-cardnews-and-polish
git diff main...feat/v3.3.3-cardnews-and-polish --stat
```

결과를 `docs/reviews/v3.3.3-change-summary.md` 로 저장 (선택).

- [ ] **Step 2: 독립 `superpowers:code-reviewer` 실행**

실행 명령 (Claude Code 측):
```
use superpowers:code-reviewer with scope "feat/v3.3.3-cardnews-and-polish vs main"
```

심층 리뷰 요청 사항:
- Spec 준수 여부
- OWASP Top 10 (특히 A03 Injection, A05 Config)
- CSP 변경 영향
- 메모리 누수 (focus-trap, addEventListener)
- 할루시네이션 플래그 Q1~Q6 해결 확인
- 파일별 라인 번호 + P0/P1/P2 분류

- [ ] **Step 3: 리뷰 결과 처리**

- **P0**: 모두 수정 후 테스트 재실행
- **P1**: 이번 사이클에 포함 또는 직후 hotfix 로 예약
- **P2**: v3.3.4 백로그로 메모 기록

수정 발생 시 각 수정에 별도 commit, 메시지 형식: `fix(review): <해결 내용>` 또는 `refactor(review): ...`

- [ ] **Step 4: 리뷰 보고서 저장**

```bash
# (code-reviewer가 생성한 리포트를 repo에 저장)
git add docs/reviews/v3.3.3-review.md
git commit -m "docs(review): v3.3.3 independent code review report"
```

---

## Task E3: Merge, 배포, 메모리 업데이트

- [ ] **Step 1: 최종 rebase (main 최신화 대비)**

```bash
git checkout main
git pull origin main  # (사용자 승인 시)
git checkout feat/v3.3.3-cardnews-and-polish
git rebase main
# conflict 시 해결 후 rebase --continue
```

- [ ] **Step 2: 전체 테스트 재실행 (rebase 후 회귀 확인)**

```bash
npm test
npm run test:smoke
npm run build
```

- [ ] **Step 3: 사용자 승인 요청**

> "v3.3.3 Phase E3 진입. main merge 후 Firebase 배포 진행할까요? (no-ff merge → `firebase deploy --only hosting`)"

**사용자 승인 전까지 대기**. CLAUDE.md 정책 준수.

- [ ] **Step 4: Merge to main**

```bash
git checkout main
git merge --no-ff feat/v3.3.3-cardnews-and-polish -m "Merge feat/v3.3.3-cardnews-and-polish (v3.3.3 Cardnews + Polish)"
```

- [ ] **Step 5: Firebase 배포**

```bash
firebase deploy --only hosting
```

- [ ] **Step 6: 프로덕션 스모크**

```bash
# 브라우저에서 https://my-ai-assistant-904f3.web.app 접근
# Task E1 Step 5 체크리스트 재확인
```

- [ ] **Step 7: 메모리 업데이트**

Create: `/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v33_3.md`

```markdown
---
name: DG v3.3.3 배포 완료
description: v3.3.3 카드뉴스 + 관심분야 수정 + 팔레트/모달 정비 배포 (2026-04-XX)
type: project
---
# v3.3.3 배포 완료

**Date**: 2026-04-XX (배포일 기록)
**Main merge SHA**: `<SHA>`
**Hosting URL**: https://my-ai-assistant-904f3.web.app

## Delivered
- #1 풀블리드 카드뉴스 (tier 1/2/3, CSP https 확장, 5장, 메모 모달)
- #2 관심 분야 수정 모달
- #4 heatmap palette ramp B
- #5 modal-system 전면 정비 (.dg-modal CSS + z-index tokens + focus-trap + centered dialog + backdrop 토큰)
- #6 AI bubble "설정 열기" 액션 + early-return turn counter

## Verification
- Vitest: <N> pass
- Playwright: <N> pass
- 독립 리뷰: APPROVE (P0=<N>/P1=<N>/P2=<N>)

## v3.3.4 open items
(P2 백로그 기록)
```

Add to `MEMORY.md` index:

```markdown
- [DG v3.3.3 배포 완료](project_daily_growth_v33_3.md) — **✅ v3.3.3 배포 ...** — N commits
```

- [ ] **Step 8: 세션 종료 문서**

Create `.claude/completions/2026-04-XX-v3.3.3.md` (프로젝트 템플릿 사용).

- [ ] **Step 9: 최종 커밋 (메모리 제외 — 메모리는 user home)**

메모리 파일은 repo 밖(`~/.claude`)이라 commit 대상 아님. `.claude/completions/` 만 commit.

```bash
git add .claude/completions/2026-04-XX-v3.3.3.md
git commit -m "docs(completion): v3.3.3 session record"
```

---

# Success Summary

작업 완료 후 다음이 모두 참이어야 함:

- ✅ 브리핑 카드 5장 × 풀블리드 카드뉴스 스타일 렌더 (모바일/데스크톱)
- ✅ tier 1/2/3 fallback 모두 동작 (Playwright 검증)
- ✅ 설정 탭 관심 분야 수정 모달 저장 동작
- ✅ heatmap 라이트/다크 각 l0~l3 육안 구분 가능
- ✅ archive "기록 상세" 모달이 스타일 갖춰 표시 (부수 버그 픽스)
- ✅ 모든 모달 ESC/배경/X 버튼 닫기 + focus-trap Tab 순환
- ✅ z-index 토큰 모두 실 값에 정합 + 사용처 모두 `var(--z-*)` 참조
- ✅ AI bubble "설정 열기" → 설정 탭 + `#apiKeyInput` scrollIntoView
- ✅ API 키 미등록 시 turn counter early-return 경로 업데이트
- ✅ Vitest 전체 pass, Playwright 전체 pass, `npm run build` 0 error/warning
- ✅ `npm run lint` 0 error
- ✅ CSP 확장 후 외부 이미지 로딩 성공 (prod 환경)
- ✅ 독립 code-reviewer 결과 P0 = 0

---

**END OF PLAN**
