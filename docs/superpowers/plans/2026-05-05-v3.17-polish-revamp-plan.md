# v3.17 Polish & Revamp Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Carry-forward 4건(bundle trim / scroll-margin / smoke N=3 / modal listener audit) + UI revamp B2 (M2 base + M3 accent, P1 home+stats hero polish 3건). archive/settings/missions는 토큰 propagate로 자동 모던화.

**Architecture:** T1-T2 토큰 base 일괄 업데이트 → T3-T5 hero polish가 새 토큰을 사용 → T6-T8 carry-forward (CSS scroll-margin / shared.ts AbortController graduate / smoke layout N=3) → T9 bundle trim final 회수. 사이클 진입 전 사전 review, T9 직후 최종 review.

**Tech Stack:** Vite + TypeScript SPA, CSS `@layer` (tokens/base/components/utilities), vitest (jsdom), Playwright smoke. canonical 번들 측정 = `gzip -c dist/assets/index-*.js | wc -c` (v3.14.5 graduation).

**Spec:** [docs/superpowers/specs/2026-05-05-v3.17-polish-revamp-design.md](../specs/2026-05-05-v3.17-polish-revamp-design.md)

---

## T0: Plan kickoff + 사전 review

**Files:**
- Modify: `docs/superpowers/plans/2026-05-05-v3.17-polish-revamp-plan.md` (이 파일)

**목적**: plan을 commit하고 spec + plan을 입력으로 사전 review를 받아 P0/P1 in-cycle 반영.

- [ ] **Step 1: plan commit**

```bash
git add docs/superpowers/plans/2026-05-05-v3.17-polish-revamp-plan.md
git commit -m "docs(v3.17 T0): implementation plan — 9 task (T1-T2 토큰 / T3-T5 hero polish / T6-T9 carry-forward)"
```

- [ ] **Step 2: 사전 review dispatch**

`superpowers:code-reviewer` subagent를 spec + plan 입력으로 실행. P0/P1/P2 분류 받기. (Codex CLI 권한 가능 시 병행 fallback로 같이 dispatch.)

- [ ] **Step 3: review 결과 plan에 반영**

P0/P1 발견 시 plan을 즉시 수정 (해당 task 단계에 fix step 추가, 필요 시 신규 sub-task). P2는 §7.2 backlog seed에 추가. plan diff commit:

```bash
git add docs/superpowers/plans/2026-05-05-v3.17-polish-revamp-plan.md
git commit -m "docs(v3.17 T0 review): 사전 review P{0,1} 반영 / P2 backlog seed"
```

(P0/P1 0건이면 step 3 skip.)

---

## T1: 토큰 base 업데이트 (color / shadow / radius / motion)

**Files:**
- Modify: `src/styles/tokens.css` (`:root` light + theme-invariant blocks)
- Create: `tests/styles/tokens.spec.ts`

**목적**: 토큰 단에서 "올드함" 3대 원인(얕은 shadow / 보통 채도 / 작은 radius) 일괄 해결 + M3 accent 시각 어휘(spring motion / gradient) 도입.

- [ ] **Step 1: 신규 토큰 spec 작성 (failing test)**

Create `tests/styles/tokens.spec.ts`:

```typescript
import { describe, it, expect, beforeAll } from 'vitest';
import { readFileSync } from 'node:fs';
import { join } from 'node:path';

let css: string;

beforeAll(() => {
  css = readFileSync(join(process.cwd(), 'src/styles/tokens.css'), 'utf-8');
});

describe('v3.17 tokens', () => {
  it('exposes --primary-vivid #6366F1', () => {
    expect(css).toMatch(/--primary-vivid:\s*#6366F1\b/);
  });

  it('exposes --primary-gradient with linear-gradient', () => {
    expect(css).toMatch(/--primary-gradient:\s*linear-gradient/);
  });

  it('exposes --shadow-glow focus ring', () => {
    expect(css).toMatch(/--shadow-glow:\s*0 0 0 4px rgba\(99,\s*102,\s*241,\s*0\.15\)/);
  });

  it('exposes --radius-2xl: 24px', () => {
    expect(css).toMatch(/--radius-2xl:\s*24px/);
  });

  it('exposes --ease-spring cubic-bezier', () => {
    expect(css).toMatch(/--ease-spring:\s*cubic-bezier\(0\.34,\s*1\.56,\s*0\.64,\s*1\)/);
  });

  it('--shadow-md is multi-layer (3D depth)', () => {
    expect(css).toMatch(/--shadow-md:.*rgba.*,.*rgba/);
  });
});
```

- [ ] **Step 2: 테스트 fail 확인**

```bash
npm test -- tokens.spec
```
Expected: 6/6 fail (토큰 미정의).

- [ ] **Step 3: 토큰 업데이트**

`src/styles/tokens.css`의 `:root` 블록(line 5~35)에 신규/변경:

```css
/* 신규 — line 5~10 사이 추가 (--primary 다음 줄) */
--primary-vivid: #6366F1;
--primary-gradient: linear-gradient(135deg, #6366F1 0%, #818CF8 100%);
--accent-gradient: linear-gradient(135deg, #F59E0B 0%, #FBBF24 100%);

/* 변경 — line 22~24 (--shadow-sm/md/lg 재정의, 기존 라인 교체) */
--shadow-sm: 0 1px 2px rgba(15,23,42,0.06), 0 1px 1px rgba(15,23,42,0.04);
--shadow-md: 0 4px 12px rgba(15,23,42,0.08), 0 2px 4px rgba(15,23,42,0.04);
--shadow-lg: 0 12px 32px rgba(15,23,42,0.12), 0 4px 8px rgba(15,23,42,0.06);

/* 신규 — radius 블록 끝(line 28 다음)에 추가 */
--radius-2xl: 24px;
```

theme-invariant `:root` 블록(line 63~120)에 신규 추가 (기존 `--ease-standard` 다음):

```css
/* line 110 이후 추가 */
--ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);
--ease-out-soft: cubic-bezier(0.22, 1, 0.36, 1);
--shadow-glow: 0 0 0 4px rgba(99, 102, 241, 0.15);
```

- [ ] **Step 4: 테스트 PASS 확인**

```bash
npm test -- tokens.spec
```
Expected: 6/6 PASS.

- [ ] **Step 5: 회귀 가드 (전체)**

```bash
npx tsc --noEmit && npm run lint && npm test
```
Expected: TS 0 errors, lint 0 errors, vitest baseline 839 → 845 (+6).

- [ ] **Step 6: 사용자 시각 확인 + commit**

dev server에서 home/stats 1회 cycle, "shadow가 더 깊어졌고 radius 크게 보임"이 확인되면 commit:

```bash
git add src/styles/tokens.css tests/styles/tokens.spec.ts
git commit -m "feat(v3.17 T1): 토큰 base 업데이트 — primary-vivid + shadow 3-tier depth + radius-2xl + spring easing"
```

- [ ] **Step 7: per-task review**

`pr-review-toolkit:code-reviewer` 또는 superpowers:code-reviewer subagent dispatch with diff. minor 발견은 in-cycle fix.

---

## T2: 다크 모드 warm charcoal

**Files:**
- Modify: `src/styles/tokens.css` (`[data-theme="dark"]` block, line 37~58 + heatmap dark line 122~128)
- Modify: `tests/styles/tokens.spec.ts` (T1 spec에 다크 토큰 assertion 추가)

**목적**: 현 dark `--bg: #0F172A`(차가운 slate-900) → 따뜻한 charcoal로 야간 사용감 부드럽게.

- [ ] **Step 1: 다크 spec 추가 (failing)**

`tests/styles/tokens.spec.ts`에 추가:

```typescript
describe('v3.17 dark warm charcoal', () => {
  it('--bg dark = #161823 (warm charcoal)', () => {
    expect(css).toMatch(/\[data-theme="dark"\][\s\S]*?--bg:\s*#161823/);
  });
});
```

- [ ] **Step 2: 테스트 fail 확인**

```bash
npm test -- tokens.spec
```
Expected: 7/7 중 1 fail.

- [ ] **Step 3: 다크 토큰 업데이트**

`src/styles/tokens.css` `[data-theme="dark"]` 블록에서 (line 37~58):

```css
--bg: #161823;          /* was #0F172A */
--bg-card: #22242F;     /* was #1E293B */
--bg-input: #363846;    /* was #334155 */
--border: #363846;      /* was #334155 */
```

heatmap dark 블록(line 122~128)에서:

```css
--heatmap-l0: #161823;  /* was #0F172A — sync with --bg */
```

- [ ] **Step 4: 테스트 PASS 확인**

```bash
npm test -- tokens.spec
```
Expected: 7/7 PASS.

- [ ] **Step 5: 다크 컨트라스트 자체 검증**

`#F1F5F9` (text-primary) × `#161823` (bg) 컨트라스트 비 계산 (수동 또는 https://webaim.org/resources/contrastchecker/ 활용). 4.5:1 이상 확인. 미달 시 `--text-primary`를 `#F8FAFC`로 한 단계 ↑ 또는 `--bg`를 `#1A1C28`로 한 단계 ↓ 조정.

- [ ] **Step 6: 다크 모드 수동 1회 cycle**

```bash
npm run dev
```

브라우저 dev tools에서 `document.documentElement.dataset.theme = 'dark'` 적용 후 home → stats → archive → settings 시각 확인. 가독성 + 카드 분리감 + heatmap 명료도 OK.

- [ ] **Step 7: 회귀 가드 + commit**

```bash
npx tsc --noEmit && npm run lint && npm test
git add src/styles/tokens.css tests/styles/tokens.spec.ts
git commit -m "feat(v3.17 T2): 다크 warm charcoal (#0F172A → #161823) + WCAG 4.5:1 검증"
```

- [ ] **Step 8: per-task review**

---

## T3: home 카드뉴스 + 오늘의 질문 hero polish

**Files:**
- Modify: `src/styles/components/cards.css` (`.briefing-card` block, line 52~199)
- Modify: `src/styles/pages/home.css` (daily-task hero — 정확 selector grep으로 pin)
- Create: `tests/ui/components/briefing-card-polish.spec.ts`

**목적**: home 첫 진입 시각 비중 80% 영역. 평평한 카드 + 얕은 shadow → depth + hover lift + 둥근 라운드.

- [ ] **Step 1: home.css에서 daily-task hero selector grep**

```bash
grep -n "daily-task\|task-input\|today-task" src/styles/pages/home.css
```

가장 hero 컨테이너 selector 확정 (예: `.daily-task` 또는 `#dailyTaskCard`). 이하 단계에서 그 selector 사용.

- [ ] **Step 2: 신규 spec 작성 (failing)**

Create `tests/ui/components/briefing-card-polish.spec.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';
import { join } from 'node:path';

const cardsCss = readFileSync(join(process.cwd(), 'src/styles/components/cards.css'), 'utf-8');
const homeCss = readFileSync(join(process.cwd(), 'src/styles/pages/home.css'), 'utf-8');

describe('v3.17 T3 — briefing card hero polish', () => {
  it('briefing-card has radius-2xl', () => {
    expect(cardsCss).toMatch(/\.briefing-card\s*\{[^}]*border-radius:\s*var\(--radius-2xl\)/);
  });

  it('briefing-card hover guarded by @media (hover: hover)', () => {
    expect(cardsCss).toMatch(/@media\s*\(hover:\s*hover\)\s*\{[\s\S]*?\.briefing-card:hover[\s\S]*?translateY\(-2px\)/);
  });

  it('briefing-card hover uses spring easing', () => {
    expect(cardsCss).toMatch(/var\(--ease-spring\)/);
  });

  it('home hero has shadow-lg + gradient background', () => {
    expect(homeCss).toMatch(/box-shadow:\s*var\(--shadow-lg\)/);
    expect(homeCss).toMatch(/linear-gradient/);
  });
});
```

- [ ] **Step 3: 테스트 fail 확인**

```bash
npm test -- briefing-card-polish
```
Expected: 4/4 fail.

- [ ] **Step 4: cards.css 업데이트**

`src/styles/components/cards.css` line 52 근처 `.briefing-card { ... }` 블록의 기존 `border-radius` 값을 `var(--radius-2xl)`로 교체. 기존 `box-shadow` 추가/교체 → `var(--shadow-md)`.

기존 line 60 `.briefing-card:hover { transform: translateY(-2px); }`를 다음으로 교체:

```css
@media (hover: hover) {
  .briefing-card:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
    transition:
      transform var(--duration-base) var(--ease-spring),
      box-shadow var(--duration-base) var(--ease-out-soft);
  }
}
.briefing-card:active { transform: translateY(0); }
```

- [ ] **Step 5: home.css daily-task hero 업데이트**

Step 1에서 확정한 selector 영역에 추가:

```css
.daily-task /* 또는 step 1에서 확정한 selector */ {
  box-shadow: var(--shadow-lg);
  background: linear-gradient(180deg,
    var(--bg-card) 0%,
    color-mix(in srgb, var(--primary-ultra-light) 30%, var(--bg-card)) 100%
  );
}
```

- [ ] **Step 6: 테스트 PASS 확인**

```bash
npm test -- briefing-card-polish
```
Expected: 4/4 PASS.

- [ ] **Step 7: dev server 시각 확인**

```bash
npm run dev
```

home 진입 → 카드뉴스 hover (desktop) 시 lift + 그림자 깊어지는지 / 오늘의 질문 hero가 다른 카드와 구별되는 강조 보이는지 확인.

- [ ] **Step 8: 회귀 가드 + commit**

```bash
npx tsc --noEmit && npm run lint && npm test
git add src/styles/components/cards.css src/styles/pages/home.css tests/ui/components/briefing-card-polish.spec.ts
git commit -m "feat(v3.17 T3): home 카드뉴스 hero polish — radius-2xl + spring hover lift + 오늘의 질문 gradient"
```

- [ ] **Step 9: per-task review**

---

## T4: stats 히트맵 polish

**Files:**
- Modify: `src/styles/components/heatmap.css`
- Create: `tests/ui/components/heatmap-polish.spec.ts`

**목적**: 히트맵 cell 단조로움 + hover 피드백 부재 → M3 accent로 색·모션.

- [ ] **Step 1: 신규 spec 작성 (failing)**

Create `tests/ui/components/heatmap-polish.spec.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';
import { join } from 'node:path';

const css = readFileSync(join(process.cwd(), 'src/styles/components/heatmap.css'), 'utf-8');

describe('v3.17 T4 — heatmap polish', () => {
  it('heatmap-cell has 6px radius', () => {
    expect(css).toMatch(/\.heatmap-cell\s*\{[^}]*border-radius:\s*6px/);
  });

  it('heatmap-cell hover scale guarded by @media (hover: hover)', () => {
    expect(css).toMatch(/@media\s*\(hover:\s*hover\)[\s\S]*?\.heatmap-cell:hover[\s\S]*?scale\(1\.15\)/);
  });

  it('heatmap-cell hover uses shadow-glow', () => {
    expect(css).toMatch(/var\(--shadow-glow\)/);
  });
});
```

- [ ] **Step 2: 테스트 fail 확인**

```bash
npm test -- heatmap-polish
```
Expected: 3/3 fail.

- [ ] **Step 3: heatmap.css 업데이트**

`.heatmap-cell` 블록에서 `border-radius`를 `6px`로 변경(또는 기존이 4px이면 교체).
기존 `transition` 보강 또는 추가:

```css
.heatmap-cell {
  border-radius: 6px;
  transition: transform var(--duration-fast), background-color var(--duration-base);
}
@media (hover: hover) {
  .heatmap-cell:hover {
    transform: scale(1.15);
    box-shadow: var(--shadow-glow);
    z-index: 1;
  }
}
```

라벨 색상: 기존 `color: var(--text-tertiary)` 사용 selector를 `var(--text-secondary)`로 교체 (grep 후 site-by-site).

- [ ] **Step 4: 테스트 PASS 확인**

```bash
npm test -- heatmap-polish
```
Expected: 3/3 PASS.

- [ ] **Step 5: dev server 시각 + grid overflow 확인**

```bash
npm run dev
```

stats 진입 → heatmap hover 시 scale 1.15 + glow ring 보이는지, 인접 cell overflow 안 되는지(z-index raise + grid gap 충분한지) 확인.

- [ ] **Step 6: 회귀 가드 + commit**

```bash
npx tsc --noEmit && npm run lint && npm test
git add src/styles/components/heatmap.css tests/ui/components/heatmap-polish.spec.ts
git commit -m "feat(v3.17 T4): stats 히트맵 polish — radius 6px + scale 1.15 hover + shadow-glow"
```

- [ ] **Step 7: per-task review**

---

## T5: stats 정원 polish

**Files:**
- Modify: `src/styles/components/garden.css`
- Create: `tests/ui/components/garden-polish.spec.ts`

**목적**: 정원 grid의 식물 카드 ↔ 빈자리 시각 분리감 없음 → ambient 배경 + 식물 카드 depth + dotted 빈자리.

- [ ] **Step 1: garden 빈자리 selector 확인**

```bash
grep -n "garden-empty\|garden-placeholder\|empty-slot" src/styles/components/garden.css src/ui/components/*.ts
```

확정 (현재 `.garden-empty` line 80에 존재). 이하 단계에서 그 selector 사용.

- [ ] **Step 2: 신규 spec 작성 (failing)**

Create `tests/ui/components/garden-polish.spec.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';
import { join } from 'node:path';

const css = readFileSync(join(process.cwd(), 'src/styles/components/garden.css'), 'utf-8');

describe('v3.17 T5 — garden polish', () => {
  it('garden-grid has ambient gradient background', () => {
    expect(css).toMatch(/\.garden-grid\s*\{[^}]*background:[^;]*linear-gradient/);
  });

  it('garden-card has shadow-md + spring transition', () => {
    expect(css).toMatch(/\.garden-card\s*\{[^}]*box-shadow:\s*var\(--shadow-md\)/);
    expect(css).toMatch(/var\(--ease-spring\)/);
  });

  it('garden-empty has dashed border', () => {
    expect(css).toMatch(/\.garden-empty\s*\{[^}]*border:\s*1\.5px\s+dashed/);
  });
});
```

- [ ] **Step 3: 테스트 fail 확인**

```bash
npm test -- garden-polish
```
Expected: 3/3 fail.

- [ ] **Step 4: garden.css 업데이트**

`.garden-grid` 블록 (line 7~14)에 추가:

```css
.garden-grid {
  /* 기존 grid 정의 유지 */
  background: linear-gradient(180deg,
    transparent 0%,
    color-mix(in srgb, var(--primary) 3%, transparent) 100%
  );
}
```

`.garden-card` 블록 (line 15~26)에 추가:

```css
.garden-card {
  /* 기존 padding/flex 유지 */
  border-radius: var(--radius-xl);
  box-shadow: var(--shadow-md);
  transition:
    transform var(--duration-base) var(--ease-spring),
    box-shadow var(--duration-base);
}
@media (hover: hover) {
  .garden-card:hover {
    transform: translateY(-1px) scale(1.02);
    box-shadow: var(--shadow-lg);
  }
}
```

`.garden-empty` 블록 (line 80~)에 추가:

```css
.garden-empty {
  border: 1.5px dashed var(--border);
  border-radius: var(--radius-lg);
  background: var(--bg-input);
}
```

- [ ] **Step 5: 테스트 PASS 확인**

```bash
npm test -- garden-polish
```
Expected: 3/3 PASS.

- [ ] **Step 6: dev server 시각 확인**

```bash
npm run dev
```

stats → 정원 영역에서 ambient 톤 / 식물 카드 hover lift / 빈자리 dashed 분리감 확인. grid jump 없는지 확인 (transform-only).

- [ ] **Step 7: 회귀 가드 + commit**

```bash
npx tsc --noEmit && npm run lint && npm test
git add src/styles/components/garden.css tests/ui/components/garden-polish.spec.ts
git commit -m "feat(v3.17 T5): stats 정원 polish — ambient grid bg + 식물 카드 depth + 빈자리 dashed"
```

- [ ] **Step 8: 1차 번들 측정 (T9 trim 목표 가늠)**

```bash
npm run build
gzip -c dist/assets/index-*.js | wc -c
```

baseline 34887 B 대비 +값 기록 (T9에서 회수 목표 결정용).

- [ ] **Step 9: per-task review**

---

## T6: scroll-margin CSS UX (carry-B)

**Files:**
- Modify: `src/styles/base.css` (전역 `:where()` selector — line 33 이전 추가)
- Create: `tests/styles/scroll-margin.spec.ts`

**목적**: v3.16 T6 mobile submit fixed-nav overlap의 본질 해법. JS scrollIntoView fix는 유지 + CSS layer 보강.

- [ ] **Step 1: 신규 spec 작성 (failing)**

Create `tests/styles/scroll-margin.spec.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';
import { join } from 'node:path';

const css = readFileSync(join(process.cwd(), 'src/styles/base.css'), 'utf-8');

describe('v3.17 T6 — scroll-margin CSS UX', () => {
  it('input/textarea/data-scroll-target have scroll-margin-top', () => {
    expect(css).toMatch(/:where\(input,\s*textarea,\s*\[data-scroll-target\]\)/);
    expect(css).toMatch(/scroll-margin-top:\s*calc\(var\(--nav-height\)\s*\+\s*16px\)/);
  });
});
```

- [ ] **Step 2: 테스트 fail 확인**

```bash
npm test -- scroll-margin
```
Expected: 1 fail.

- [ ] **Step 3: base.css 업데이트**

`src/styles/base.css` 끝에 추가:

```css
/* v3.17 T6 — fixed-nav overlap 본질 해법 (carry-B from v3.16 lesson #4) */
:where(input, textarea, [data-scroll-target]) {
  scroll-margin-top: calc(var(--nav-height) + 16px);
  scroll-margin-bottom: var(--content-bottom-pad);
}
```

- [ ] **Step 4: 테스트 PASS 확인**

```bash
npm test -- scroll-margin
```
Expected: 1 PASS.

- [ ] **Step 5: 회귀 가드**

```bash
npx tsc --noEmit && npm run lint && npm test && npm run test:smoke -- layout
```
Expected: 모두 PASS. layout smoke 자연 cover (T6 fix가 보강 layer).

- [ ] **Step 6: commit**

```bash
git add src/styles/base.css tests/styles/scroll-margin.spec.ts
git commit -m "feat(v3.17 T6): scroll-margin CSS UX — fixed-nav overlap 본질 해법 (v3.16 carry-B)"
```

- [ ] **Step 7: per-task review**

---

## T7: modal listener — shared.ts AbortController graduate (carry-D)

**Files:**
- Modify: `src/ui/modals/shared.ts` (line 17, 67, 98~101)
- Create: `tests/ui/modals/shared-cleanup.spec.ts`

**목적**: v3.16 C3 welcome-garden AbortController fix의 idiom을 shared.ts에 graduate. cross-modal audit 결과 다른 모달은 모두 `openModal()` helper 통과 → shared.ts 한 곳 정비로 전체 cover. (sidebar.ts:108은 singleton lifetime, scope 외.)

**Audit 결과 (plan 진입 전 확인)**:
- `shared.ts:67/99` — manual cleanup OK, 단 idiom 통일 위해 AbortController graduate.
- `welcome-garden.ts:60-87` — 이미 AbortController (v3.16 C3 ✅).
- `welcome-gamification.ts:70 / answer-detail:51 / badge-detail:23 / interests:64 / memo:27` — 모두 `openModal` 통과 ✅.
- `sidebar.ts:108` — singleton, leak 없음 (scope 외, 사이클 retro에 노트).

- [ ] **Step 1: 신규 spec 작성 (failing)**

Create `tests/ui/modals/shared-cleanup.spec.ts`:

```typescript
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { openModal, closeModal } from '../../../src/ui/modals/shared';

describe('v3.17 T7 — shared modal AbortController cleanup', () => {
  beforeEach(() => {
    document.body.innerHTML = '<div id="modalRoot"></div>';
  });

  afterEach(() => {
    closeModal();
    document.body.innerHTML = '';
  });

  it('open → close → ESC press → no handler call (listener removed)', () => {
    let closeCalls = 0;
    openModal({
      title: 'test',
      bodyHtml: '<p>x</p>',
      onClose: () => closeCalls++,
    });
    closeModal();
    expect(closeCalls).toBe(1);

    // 이후 ESC press가 closeModal을 다시 트리거하지 않아야 함
    const evt = new KeyboardEvent('keydown', { key: 'Escape' });
    document.dispatchEvent(evt);
    expect(closeCalls).toBe(1); // 변화 없음
  });

  it('open A → open B → close → 5회 ESC → handler 1회만 호출 (single listener invariant)', () => {
    let closeCalls = 0;
    openModal({ title: 'A', bodyHtml: '<p>a</p>', onClose: () => closeCalls++ });
    openModal({ title: 'B', bodyHtml: '<p>b</p>', onClose: () => closeCalls++ });
    closeModal();
    for (let i = 0; i < 5; i++) {
      document.dispatchEvent(new KeyboardEvent('keydown', { key: 'Escape' }));
    }
    expect(closeCalls).toBe(2); // open A 교체 시 1회, closeModal() 1회. 이후 ESC는 무효.
  });
});
```

- [ ] **Step 2: 테스트 fail 확인**

```bash
npm test -- shared-cleanup
```
Expected: 0~2 fail. (현 manual cleanup이 단일 close에서는 통과할 수 있음. AbortController graduate 후에도 동일 통과여야 함 — regression guard 목적.)

- [ ] **Step 3: shared.ts AbortController graduate**

`src/ui/modals/shared.ts` 변경:

line 17: `let escHandler: ((e: KeyboardEvent) => void) | null = null;` → `let escController: AbortController | null = null;`

line 66~67 교체:
```typescript
escController = new AbortController();
document.addEventListener('keydown', (e) => {
  if (e.key === 'Escape') closeModal();
}, { signal: escController.signal });
```

line 98~101 교체:
```typescript
if (escController) {
  escController.abort();
  escController = null;
}
```

- [ ] **Step 4: 테스트 PASS 확인**

```bash
npm test -- shared-cleanup
```
Expected: 2/2 PASS.

- [ ] **Step 5: 모든 모달 spec PASS 확인 (regression)**

```bash
npm test -- modal
```
Expected: 기존 모달 spec 전체 PASS (welcome-gamification / answer-detail / badge-detail / interests / memo).

- [ ] **Step 6: 회귀 가드 + commit**

```bash
npx tsc --noEmit && npm run lint && npm test
git add src/ui/modals/shared.ts tests/ui/modals/shared-cleanup.spec.ts
git commit -m "refactor(v3.17 T7): shared modal AbortController graduate — v3.16 C3 idiom 통일 (carry-D audit 결과 단일 site)"
```

- [ ] **Step 7: per-task review**

---

## T8: smoke layout N=3 regression guard (carry-C)

**Files:**
- Modify: `tests/smoke/layout.spec.ts`

**목적**: v3.16 T6 fix의 일회성 N=10 검증을 영구 N=3 regression guard로 graduate.

- [ ] **Step 1: 현 layout spec 구조 파악**

```bash
cat tests/smoke/layout.spec.ts
```

확인 사항: 기존 test() block 이름과 구조. 어느 test에 N=3 적용할지 결정 (보통 mobile submit + sticky-nav coverage가 핵심).

- [ ] **Step 2: N=3 적용**

`tests/smoke/layout.spec.ts` 핵심 test block을 다음 패턴으로 변경:

```typescript
// 기존: test('mobile submit visible under sticky nav', async ({ page }) => { ... });

// 변경: N=3 명시 loop (가시성 ↑, v3.16 retro 권고)
for (let n = 1; n <= 3; n++) {
  test(`mobile submit visible under sticky nav (N=${n})`, async ({ page }) => {
    /* 기존 본문 그대로 */
  });
}
```

또는 기존 test 1개 유지하면서 `test.describe.configure({ retries: 2 })`를 그 describe에 적용 — 가시성 측면에서 N=3 loop 권고.

- [ ] **Step 3: smoke 실행 (전체 PASS 확인)**

```bash
npm run test:smoke -- layout
```
Expected: 95 → 97 PASS (기존 1 → 3 expansion). 또는 retries 패턴이면 95 그대로 (단 1 spec이 N=3 retry).

- [ ] **Step 4: cold-start fresh fork 검증 (v3.14.4 graduation)**

```bash
npx playwright test tests/smoke/layout.spec.ts --workers=1 --reporter=list
```
Expected: 모든 N PASS.

- [ ] **Step 5: commit**

```bash
git add tests/smoke/layout.spec.ts
git commit -m "test(v3.17 T8): smoke layout N=3 regression guard graduate (carry-C)"
```

- [ ] **Step 6: per-task review**

---

## T9: bundle 추가 trim (carry-A)

**Files:**
- Modify: 후보 — `src/styles/components/cards.css` 또는 `src/ui/modals/*.ts` (1차 측정 후 grep으로 정밀 결정)

**목적**: v3.15 hard cap +0.43 over 누적 회수. v3.16 −212 B 이어 본 사이클 −300~−500 B 추가 회수 목표.

- [ ] **Step 1: 현재 번들 측정 (canonical)**

```bash
npm run build
gzip -c dist/assets/index-*.js | wc -c
```

값 기록 (T5 step 8 측정값과 비교 가능). baseline 34887 + (T1~T8 추가량).

- [ ] **Step 2: trim 후보 식별 — dead old shadow/radius**

```bash
# T1~T2에서 신규 토큰 도입 후 dead 사용처 검색
grep -rn "shadow:.*rgba(0,0,0" src/styles/ # 기존 hardcoded shadow가 있는지
grep -rn "border-radius:\s*\(8px\|12px\|16px\|20px\)" src/styles/ # 토큰 미사용 site
```

발견된 hardcoded site 중 토큰화 가능한 것을 토큰으로 교체 (CSS 줄 수 ↓, gzip 효율 ↑).

- [ ] **Step 3: 비-critical 모달 dynamic import 후보 검토**

```bash
grep -n "import.*welcome-gamification\|import.*welcome-garden\|import.*image-bigview" src/ -r
```

main entry에서 static import 중인 모달 중 첫 paint 외인 것을 dynamic import (`await import('./modals/welcome-garden')`)로 변경 가능한지 검토. 단 사용 빈도 낮은 것만 (welcome-* 모달은 schema migrate 시점만 진입).

**위험**: dynamic import 변경은 lazy chunk 분리되므로 gzip total은 늘어날 수도 있음. 첫 paint 번들만 감소 (Lighthouse FCP 개선). 본 task는 첫 paint 번들 기준으로 측정.

- [ ] **Step 4: utility class purge audit**

```bash
grep -rn "^\s*\.\(util-\|u-\)" src/styles/utilities.css | head
```

`src/styles/utilities.css` 안에서 실사용되지 않는 util class가 있는지 — 사용처 grep 후 0건이면 제거.

```bash
# 예시: .util-mt-4가 실사용 0건이면 제거
grep -rn "util-mt-4" src/ tests/
```

- [ ] **Step 5: 1~2건 trim 적용 후 재측정**

선택된 trim 1~2건만 적용. 각 변경 후:

```bash
npm run build && gzip -c dist/assets/index-*.js | wc -c
```

목표: −300 B 이상 감소. 미달 시 step 2~4 재방문.

**중요 — false-negative 방지 (v3.13 matchKeyword lesson)**: 제거 대상 식별 시 단순 grep만 신뢰하지 말고, 변경 후 반드시 전체 vitest + smoke 재실행.

- [ ] **Step 6: 전체 회귀 검증 (3종)**

```bash
npx tsc --noEmit && npm run lint
npm test
npm run test:smoke
```
Expected: 모두 PASS. 신규 fail 0건.

- [ ] **Step 7: 최종 번들 기록**

```bash
npm run build
gzip -c dist/assets/index-*.js | wc -c
```

값 기록. **DoD 검증**:
- 절대값: 35887~36887 B (목표 +1~+2 kB) → DoD 충족.
- hard cap +2.5 kB over (37387 B 초과) → 추가 trim 필요 또는 hero polish rollback (T5 ambient gradient 등 cheapest).

- [ ] **Step 8: commit**

```bash
git add <변경 파일>
git commit -m "perf(v3.17 T9): bundle trim — <후보 1~2건 요약> (Δ -<n> B / canonical gzip)"
```

- [ ] **Step 9: 최종 review dispatch**

`superpowers:code-reviewer` subagent를 모든 task commit (T1~T9) + 빌드 결과 + 번들 측정 입력으로 dispatch. 분류:
- **DEPLOY_APPROVED** → 사용자 "배포해줘" 대기.
- **APPROVED_WITH_NOTES** → P2 v3.18 backlog로, 배포 OK.
- **REJECT** → P0/P1 in-cycle hotfix만 허용 (v3.15 T16.1 패턴), 신규 기능 추가 금지.

- [ ] **Step 10: per-task review (필요 시)**

---

## T10: 사이클 retro

**Files:**
- Memory update: `~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v317.md` (신규)
- Memory index update: `~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md`
- Spec status update: `docs/superpowers/specs/2026-05-05-v3.17-polish-revamp-design.md` (DRAFT → DEPLOYED)

**목적**: 사이클 종료. 결과/lessons/v3.18+ carry-forward 기록.

- [ ] **Step 1: retro 메모리 작성**

다음 형식으로 신규 memory 파일:
- 결과 (main HEAD / Firebase live / commits / vitest/smoke)
- 사전 + 최종 review 결과 (P0/P1/P2 분류, in-cycle 반영 횟수)
- 번들 절대값 + Δ
- per-task review dispatch 횟수
- 새 lessons (예: T7 audit이 single-site로 축소된 사례 — "spec 단계 estimate vs plan 단계 grep 확인 시 scope 축소 사례").
- v3.18+ carry-forward seed

- [ ] **Step 2: spec status DRAFT → DEPLOYED**

`docs/superpowers/specs/2026-05-05-v3.17-polish-revamp-design.md` 상단:
```markdown
> Status: **DEPLOYED** (2026-MM-DD, main HEAD `<sha>`, Firebase live)
```

- [ ] **Step 3: retro commit**

```bash
git add docs/superpowers/specs/2026-05-05-v3.17-polish-revamp-design.md
git commit -m "docs(v3.17 T10): retro — Polish & Revamp 사이클 종료 + spec DEPLOYED + v3.18 carry-forward seed"
```

(memory 파일은 별도 path이므로 git에 들어가지 않음 — 메모리 시스템 자체에 저장.)

---

## Self-Review

### Spec coverage
- §3 T1 (토큰 base) ↔ Plan T1 ✅
- §3 T2 (다크 warm) ↔ Plan T2 ✅
- §3 T3 (home hero) ↔ Plan T3 ✅
- §3 T4 (히트맵) ↔ Plan T4 ✅
- §3 T5 (정원) ↔ Plan T5 ✅
- §3 T6 (scroll-margin) ↔ Plan T6 ✅
- §3 T7 (modal audit) ↔ Plan T7 ✅ (audit 결과 single-site 축소 명시)
- §3 T8 (smoke N=3) ↔ Plan T8 ✅
- §3 T9 (bundle trim) ↔ Plan T9 ✅
- §6 사전+최종 review ↔ Plan T0 (사전) + T9 step 9 (최종) ✅
- §4 DoD 3종 검증 ↔ 각 task step 회귀 가드 ✅

### Placeholder scan
- ❌ 없음. 모든 step에 실제 코드/명령/기대 출력 명시.
- T3 step 1에서 daily-task hero selector를 grep으로 step 1에서 결정한 뒤 step 5에서 사용 — 의도된 plan-내 의존, OK.
- T9 step 2~5는 plan 단계 trim 후보가 1차 측정 후 결정 — "후보 식별 → 1~2건 적용" 패턴으로 명시, placeholder 아님.

### Type/method consistency
- T1에서 정의한 신규 토큰(`--primary-vivid`, `--shadow-glow`, `--radius-2xl`, `--ease-spring`, `--ease-out-soft`)이 T3/T4/T5에서 일관되게 var() 참조됨 ✅
- shared.ts AbortController 패턴 (`escController` / `signal`) T7 step 3 + spec 일치 ✅

Pass.
