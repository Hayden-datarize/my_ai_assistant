# v3.16 Polish — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v3.15 finale carry-forward 6건 일괄 소진 (P1-1 + P2 3건 + bundle drift + layout flake) + Codex 2-pass 게이트 통과 + bundle baseline 회복.

**Architecture:** 신규 아키텍처 변경 없음. 모든 항목이 schema v4 + 기존 함수 시그니처 내 minimal patch. atomic single-write idiom (v3.13/v3.15 graduated) + saveOk pattern (v3.15 T16.1) 재사용. layout flake는 bounded fix (1 task budget escape valve).

**Tech Stack:** Vite + TypeScript + Vitest + Playwright + Firebase Hosting (직배포).

**Spec:** [docs/superpowers/specs/2026-05-04-v3.16-polish-design.md](../specs/2026-05-04-v3.16-polish-design.md)

---

## File Structure

| File | Responsibility | Tasks |
|---|---|---|
| `src/state/briefings.ts` | mutateWithSweep memo→scrap catch-up emit | T1 (C1) |
| `src/ui/components/garden-grid.ts` | inner `id="gardenMini"` 제거 (outer placeholder만 유지) | T2 (C2) |
| `src/ui/modals/welcome-garden.ts` | `keydown(Escape)` listener AbortController 패턴 | T3 (C3) |
| `src/state/backfillGarden.ts` | `b.memo` undefined guard | T4 (C4) |
| `src/ui/components/garden-grid.ts` + 후보 선택 | bundle trim (T0 baseline diff 후 결정) | T5 (C5) |
| `tests/smoke/layout.spec.ts` 또는 `desktop-layout.spec.ts` | flake 진단 + bounded fix | T6 (C6) |
| `tests/unit/state/briefings-plant-hook.spec.ts` | C1 회귀 spec | T1 |
| `tests/unit/tabs/home-garden-mini.spec.ts` | C2 id 유일성 spec (기존 spec에 case 추가) | T2 |
| `tests/smoke/v3.15-garden.spec.ts:27` | C2 fix 후 stale 코멘트 정리 (NOTE 줄 제거) | T2 |
| `tests/unit/modals/welcome-garden.spec.ts` | C3 listener cleanup spec | T3 |
| `tests/unit/state/backfill.spec.ts` | C4 undefined fixture spec | T4 |

---

## Task 0: Baseline 측정

**Files:**
- Create: `docs/superpowers/plans/2026-05-04-v3.16-baseline.md`

- [ ] **Step 1: Vitest count + bundle CLI gzip + N=3 smoke 측정**

```bash
# vitest pass count
npm test 2>&1 | tail -5 | tee /tmp/v316-vitest-baseline.txt

# bundle build + CLI gzip canonical (v3.14.5 graduation)
npm run build 2>&1 | tail -10 | tee /tmp/v316-build-baseline.txt
gzip -c dist/assets/index-*.js | wc -c | tee /tmp/v316-bundle-baseline.txt

# smoke N=3 (cold-start fresh fork)
for i in 1 2 3; do
  echo "=== smoke run $i ==="
  npm run test:smoke 2>&1 | tail -10
done | tee /tmp/v316-smoke-baseline.txt
```

Expected:
- vitest: 828 pass (v3.15 baseline)
- bundle: ~34280 bytes (34.28 kB)
- smoke: 95 pass (1 layout pre-existing flake 가능)

- [ ] **Step 2: baseline 문서 작성**

다음 내용으로 `docs/superpowers/plans/2026-05-04-v3.16-baseline.md` 생성:

```markdown
# v3.16 Baseline (T0)

측정일: 2026-05-04

| 항목 | 값 |
|---|---|
| Vitest pass | <측정값> / 828 expected |
| Bundle CLI gzip | <측정값> bytes (<측정값/1024>.<...> kB) |
| Smoke N=3 | <pass>/95 each run |
| TS strict | 0 errors (`npx tsc --noEmit`) |
| Lint | 0 errors (`npm run lint`) |
| Layout flake observed in N=3 | <yes/no> |
```

- [ ] **Step 3: Commit baseline**

```bash
git add docs/superpowers/plans/2026-05-04-v3.16-baseline.md
git commit -m "docs(v3.16 T0): baseline 측정 (vitest / bundle CLI gzip / smoke N=3)"
```

---

## Codex 사전 review (T1 시작 전 — 별도 슬롯)

- [ ] **사전 review 실행 (controller / 사용자 트리거)**

Spec + 본 plan을 Codex에 review 요청. 분류 P0/P1/P2.

- P0/P1 발견 → in-cycle 반영 (plan task 추가/수정 후 spec §10에 결과 append).
- P2만 → spec §10에 기록 후 v3.17 backlog로 분류 가능.
- 권한 issue → controller self-review fallback (v3.14.5 패턴), spec §10에 명시.

결과를 spec §10 placeholder에 기록 후 commit:

```bash
git add docs/superpowers/specs/2026-05-04-v3.16-polish-design.md
git commit -m "docs(v3.16 §10): Codex 사전 review 결과 기록 (Pn=N / in-cycle 반영 N건)"
```

---

## Task 1: C1 — briefings memo→scrap order catch-up (P1-1)

**Files:**
- Modify: `src/state/briefings.ts` (line 101-110, scrap 분기에 memo catch-up 추가)
- Test: `tests/unit/state/briefings-plant-hook.spec.ts` (신규 case 4건 추가)

**배경**: backfill 정책 = "memo는 스크랩된 briefing의 memo만 카운트". 현재 live path memo 분기 (line 108-109)는 정책 일치하지만, **scrap 토글 ON 시점에 기존 memo catch-up 누락** (Codex final 1차 REJECT P1-1).

- [ ] **Step 1: 실패 테스트 작성 — memo 먼저 → scrap ON 시 memo +1 catch-up**

`tests/unit/state/briefings-plant-hook.spec.ts` 끝에 다음 case 4건 추가 (기존 describe block 안):

```typescript
  it('C1 catch-up: memo 먼저 → scrap ON → memo +1 + scrap +1 (cumulative +2)', () => {
    const u = makeUser(['recruiting']);
    setCachedUser(u);
    saveBriefingsForTest([{
      ...makeBriefing('채용 동향'),
      scrapped: false,
      memo: '메모 먼저',  // 비스크랩 + memo 존재
    }]);
    const before = u.plantStateByInterest.recruiting?.cumulativeActivity ?? 0;

    toggleScrap(0);  // scrap OFF → ON 토글

    const after = getCachedUser()!.plantStateByInterest.recruiting!.cumulativeActivity;
    expect(after - before).toBe(2);  // scrap +1 + memo catch-up +1
  });

  it('C1 baseline (regression guard): scrap 먼저 → memo 추가 → memo +1 (기존 동작 유지)', () => {
    const u = makeUser(['recruiting']);
    setCachedUser(u);
    saveBriefingsForTest([{
      ...makeBriefing('채용 동향'),
      scrapped: true,
      memo: '',  // 스크랩 + 빈 memo
    }]);
    const before = u.plantStateByInterest.recruiting?.cumulativeActivity ?? 0;

    saveMemo(0, '나중에 작성');

    const after = getCachedUser()!.plantStateByInterest.recruiting!.cumulativeActivity;
    expect(after - before).toBe(1);  // memo +1만
  });

  it('C1 regression: 비스크랩 + memo only → cumulative 변화 없음 (정책 일치)', () => {
    const u = makeUser(['recruiting']);
    setCachedUser(u);
    saveBriefingsForTest([{
      ...makeBriefing('채용 동향'),
      scrapped: false,
      memo: '',
    }]);
    const before = u.plantStateByInterest.recruiting?.cumulativeActivity ?? 0;

    saveMemo(0, '비스크랩에 memo만 추가');

    const after = getCachedUser()!.plantStateByInterest.recruiting!.cumulativeActivity;
    expect(after - before).toBe(0);  // 비스크랩 memo는 카운트 안 함
  });

  it('C1 regression: scrap OFF 토글 → catch-up 없음', () => {
    const u = makeUser(['recruiting']);
    setCachedUser(u);
    saveBriefingsForTest([{
      ...makeBriefing('채용 동향'),
      scrapped: true,
      memo: '메모 있음',
    }]);
    const before = u.plantStateByInterest.recruiting?.cumulativeActivity ?? 0;

    toggleScrap(0);  // scrap ON → OFF (역방향)

    const after = getCachedUser()!.plantStateByInterest.recruiting!.cumulativeActivity;
    expect(after - before).toBe(0);  // OFF 시 회수 없음 (현 정책 — backfill snapshot 기반)
  });
```

기존 helper (`makeUser`, `makeBriefing`, `saveBriefingsForTest`, `setCachedUser`)가 spec 상단에 있는지 먼저 grep 확인. 없으면 기존 case에서 사용한 패턴을 그대로 따라 추가.

- [ ] **Step 2: 테스트 실패 확인**

```bash
npm test -- tests/unit/state/briefings-plant-hook.spec.ts 2>&1 | tail -30
```

Expected: 4 신규 case 중 첫 case (memo→scrap catch-up) FAIL — `expect(after - before).toBe(2)` but receives `1`. 나머지 3 regression case는 PASS (현재 동작이 이미 일치).

- [ ] **Step 3: src/state/briefings.ts scrap 분기에 memo catch-up 추가**

Edit `src/state/briefings.ts` line 103-110 영역:

기존:
```typescript
    if (action === 'scrap' && target.scrapped && !wasScrapped) {
      tickPlantsByBriefingInMemory(u, target, 1);  // false→true 전환만
    }
    // P1-1 fix (v3.15 T16.1): memo tick은 스크랩된 briefing에만 적용 (backfill 정책과 정합).
    // 스크랩 안 한 briefing의 memo는 활동으로 간주하지 않음.
    if (action === 'memo' && target.scrapped && memoWasEmpty && target.memo.trim().length > 0) {
      tickPlantsByBriefingInMemory(u, target, 1);  // 스크랩+빈→non-빈 전환만
    }
```

변경:
```typescript
    if (action === 'scrap' && target.scrapped && !wasScrapped) {
      tickPlantsByBriefingInMemory(u, target, 1);  // scrap +1 (false→true 전환)
      // C1 catch-up (v3.16): scrap ON 시점에 기존 memo가 있으면 memo도 retroactive +1
      // (backfill 정책 "scrap된 briefing의 memo만 카운트"와 정합).
      if (target.memo && target.memo.trim().length > 0) {
        tickPlantsByBriefingInMemory(u, target, 1);  // memo catch-up +1
      }
    }
    // P1-1 fix (v3.15 T16.1): memo tick은 스크랩된 briefing에만 적용 (backfill 정책과 정합).
    // 스크랩 안 한 briefing의 memo는 활동으로 간주하지 않음.
    if (action === 'memo' && target.scrapped && memoWasEmpty && target.memo.trim().length > 0) {
      tickPlantsByBriefingInMemory(u, target, 1);  // 스크랩+빈→non-빈 전환만
    }
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
npm test -- tests/unit/state/briefings-plant-hook.spec.ts 2>&1 | tail -20
```

Expected: 모든 case PASS (기존 + 신규 4건).

- [ ] **Step 5: TS strict + lint + 영향 범위 vitest**

```bash
npx tsc --noEmit 2>&1 | tail -5
npm run lint -- src/state/briefings.ts 2>&1 | tail -5
npm test -- tests/unit/state/ 2>&1 | tail -10
```

Expected: 0 errors / 0 warnings / all PASS.

- [ ] **Step 6: 신규 case N=5 안정성**

```bash
for i in 1 2 3 4 5; do
  echo "=== run $i ==="
  npm test -- tests/unit/state/briefings-plant-hook.spec.ts 2>&1 | tail -5
done
```

Expected: 5/5 PASS, 동일 count.

- [ ] **Step 7: Commit**

```bash
git add src/state/briefings.ts tests/unit/state/briefings-plant-hook.spec.ts
git commit -m "$(cat <<'EOF'
fix(v3.16 T1): C1 — briefings memo→scrap order catch-up (Codex P1-1)

scrap 토글 ON 시점에 기존 memo가 있으면 memo +1 retroactive catch-up emit.
backfill 정책 ("scrap된 briefing의 memo만 카운트")과 live path 일관성 회복.

신규 spec 4건 (catch-up + 3 regression guard).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: C2 — garden-mini DOM id 중복 제거 (P2-1)

**Files:**
- Modify: `src/ui/components/garden-grid.ts:103` (inner `id="gardenMini"` 제거)
- Test: `tests/unit/tabs/home-garden-mini.spec.ts` (id 유일성 assertion 추가)

**배경**: `home.ts:44` outer placeholder `<div id="gardenMini"></div>` + `garden-grid.ts:103` inner row `<div class="garden-mini-row" id="gardenMini">` → 동일 id 중복. 외부 핸들러 `home.ts:327 getElementById('gardenMini')`는 outer만 참조 (querySelector first match).

- [ ] **Step 1: 실패 테스트 작성 — id 유일성**

`tests/unit/tabs/home-garden-mini.spec.ts` 끝에 추가:

```typescript
  it('C2 (v3.16): #gardenMini ID는 DOM 전체에서 유일 (outer placeholder만)', () => {
    const c = setupHomeWithUser();  // 기존 helper 패턴
    renderGardenMini(c.querySelector('#gardenMini') as HTMLElement, makeUserWithPlants());
    const matches = c.querySelectorAll('#gardenMini');
    expect(matches.length).toBe(1);  // outer 1개만
    // inner row는 class로만 식별
    expect(c.querySelectorAll('.garden-mini-row').length).toBe(1);
  });
```

기존 spec의 helper (`setupHomeWithUser`, `makeUserWithPlants` 등)가 정확한 이름으로 존재하는지 grep으로 확인. 없으면 spec 상단 패턴을 따라 inline 정의.

- [ ] **Step 2: 테스트 실패 확인**

```bash
npm test -- tests/unit/tabs/home-garden-mini.spec.ts 2>&1 | tail -15
```

Expected: 신규 case FAIL — `expect(matches.length).toBe(1)` but receives `2`.

- [ ] **Step 3: garden-grid.ts inner id 제거**

Edit `src/ui/components/garden-grid.ts` line 103:

기존:
```typescript
  root.innerHTML = `<div class="garden-mini-row" id="gardenMini">${cells}</div>`;
```

변경:
```typescript
  root.innerHTML = `<div class="garden-mini-row">${cells}</div>`;
```

- [ ] **Step 4: 외부 selector 영향 grep 확인 + smoke comment 정리**

```bash
grep -rn "garden-mini-row\|gardenMini" src/ tests/ index.html 2>/dev/null
```

확인 사항:
- `home.ts:327 getElementById('gardenMini')` — outer placeholder 참조 (영향 없음)
- 테스트 selector — `.garden-mini-row` 또는 `#gardenMini` 사용 — outer 참조는 그대로 유지
- CSS 셀렉터 `#gardenMini { ... }` 가 inner를 의도했다면 별도 fix 필요

CSS grep:
```bash
grep -rn "#gardenMini" src/styles/ 2>/dev/null
```

CSS에서 `#gardenMini` selector가 inner row 스타일링에 쓰였다면 → `.garden-mini-row`로 변경.

**Stale comment 정리 (Codex 사전 P2-1)**: `tests/smoke/v3.15-garden.spec.ts:27` 의 NOTE 코멘트가 기존 버그 (id 중복) 를 설명함 — fix 후 무의미해지므로 삭제:

```typescript
// 기존 (line 26~27):
//    // 홈 탭이 기본 active — renderGardenMini가 .garden-mini-row를 #gardenMini placeholder 안에 inject
//    // NOTE: #gardenMini ID가 outer placeholder와 inner row 둘 다에 존재하므로 .garden-mini-row를 사용

// 변경 (NOTE 줄만 제거):
//    // 홈 탭이 기본 active — renderGardenMini가 .garden-mini-row를 #gardenMini placeholder 안에 inject
```

- [ ] **Step 5: 테스트 통과 확인**

```bash
npm test -- tests/unit/tabs/home-garden-mini.spec.ts tests/smoke/v3.15-garden.spec.ts 2>&1 | tail -15
npm test -- tests/unit/components/ 2>&1 | tail -10
```

Expected: all PASS.

- [ ] **Step 6: TS strict + lint**

```bash
npx tsc --noEmit 2>&1 | tail -5
npm run lint -- src/ui/components/garden-grid.ts 2>&1 | tail -5
```

Expected: 0 errors / 0 warnings.

- [ ] **Step 7: Commit**

```bash
git add src/ui/components/garden-grid.ts tests/unit/tabs/home-garden-mini.spec.ts
# CSS 변경 있으면 추가
git diff --stat src/styles/ 2>&1 | tail -3
# 변경 있으면 git add src/styles/<file>

git commit -m "$(cat <<'EOF'
fix(v3.16 T2): C2 — garden-mini DOM id 중복 제거 (Codex P2-1)

inner .garden-mini-row 의 id="gardenMini" 제거.
outer placeholder (#gardenMini) 1개만 유지 → DOM HTML invariant 회복.
외부 핸들러 (home.ts getElementById) 영향 없음 (outer 참조).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: C3 — welcome-garden Escape listener cleanup (P2-2)

**Files:**
- Modify: `src/ui/modals/welcome-garden.ts` (line 78-83 + close 함수)
- Test: `tests/unit/modals/welcome-garden.spec.ts` (cleanup spec 신규)

**배경**: 현재 코드 (line 78-83) — keydown listener는 ESC 시점에만 self-remove. 버튼/backdrop close path는 listener 미제거 → 모달 닫힘 후에도 ESC listener 잔존.

- [ ] **Step 1: 실패 테스트 작성 — close 후 ESC listener inactive**

`tests/unit/modals/welcome-garden.spec.ts` 끝에 추가:

```typescript
  // Tests 1-2: 기본 동작 invariant (ESC로 close 작동)
  it('C3 (v3.16) baseline: open 후 ESC press → 모달 닫힘 (기존 동작 유지)', () => {
    setCachedUser(makeUser({ gardenIntroduced: false, interests: ['recruiting'] }));
    maybeShowWelcomeGarden();
    expect(document.querySelector('.welcome-garden-modal')).toBeTruthy();
    document.dispatchEvent(new KeyboardEvent('keydown', { key: 'Escape' }));
    expect(document.querySelector('.welcome-garden-modal')).toBeNull();
  });

  // Test 3: 핵심 cleanup 검증 — AbortController.signal.aborted 직접 spy
  it('C3 (v3.16) cleanup: 버튼 close → AbortController.signal.aborted=true (listener 즉시 정리)', () => {
    // AbortController 인스턴스를 capture해서 abort 여부 직접 검증.
    // 이 spy 없이는 listener leak이 user-observable side-effect로 안 드러남
    // (close() 내부 if-guard 때문) — 진정한 TDD를 위해 controller signal 직접 관찰.
    const OriginalAbortController = global.AbortController;
    const captured: AbortController[] = [];
    global.AbortController = class extends OriginalAbortController {
      constructor() {
        super();
        captured.push(this);
      }
    } as typeof AbortController;

    try {
      setCachedUser(makeUser({ gardenIntroduced: false, interests: ['recruiting'] }));
      maybeShowWelcomeGarden();
      expect(captured.length).toBeGreaterThanOrEqual(1);
      const escController = captured[captured.length - 1];  // welcome-garden이 만든 controller (가장 최근)
      expect(escController.signal.aborted).toBe(false);

      (document.querySelector('#welcomeGardenCloseBtn') as HTMLButtonElement).click();
      expect(escController.signal.aborted).toBe(true);  // 핵심: 버튼 close path도 abort 트리거
    } finally {
      global.AbortController = OriginalAbortController;
    }
  });

  it('C3 (v3.16) cleanup: backdrop click → AbortController.signal.aborted=true', () => {
    const OriginalAbortController = global.AbortController;
    const captured: AbortController[] = [];
    global.AbortController = class extends OriginalAbortController {
      constructor() {
        super();
        captured.push(this);
      }
    } as typeof AbortController;

    try {
      setCachedUser(makeUser({ gardenIntroduced: false, interests: ['recruiting'] }));
      maybeShowWelcomeGarden();
      const escController = captured[captured.length - 1];
      const dialog = document.querySelector('.welcome-garden-modal') as HTMLElement;

      // backdrop click — target === dialog 강제
      const clickEvent = new MouseEvent('click', { bubbles: true });
      Object.defineProperty(clickEvent, 'target', { value: dialog });
      dialog.dispatchEvent(clickEvent);

      expect(escController.signal.aborted).toBe(true);
      expect(document.querySelector('.welcome-garden-modal')).toBeNull();
    } finally {
      global.AbortController = OriginalAbortController;
    }
  });
```

기존 spec helper (`setCachedUser`, `makeUser`, `maybeShowWelcomeGarden` import 등)가 정확한 형태로 import되어 있는지 확인. 없으면 spec 상단 패턴 따라 추가.

- [ ] **Step 2: 테스트 실패 확인**

```bash
npm test -- tests/unit/modals/welcome-garden.spec.ts 2>&1 | tail -25
```

Expected (fix 전 동작):
- baseline test (test 1: ESC 닫힘) → **PASS** (기존 listener가 ESC만은 처리하므로)
- cleanup test 2 (close button → AbortController spy) → **FAIL** — `expect(captured.length).toBeGreaterThanOrEqual(1)` 0 받음 (현 코드에 AbortController 인스턴스 없음)
- cleanup test 3 (backdrop → AbortController spy) → **FAIL** — 동일 이유

(test 2, 3가 fix 전 FAIL → fix 후 PASS = 진정한 TDD red→green.)

- [ ] **Step 3: AbortController 패턴으로 fix**

Edit `src/ui/modals/welcome-garden.ts` 함수 본문 (line 59~83):

기존 close 함수 + listener:
```typescript
  function close(navigate: boolean): void {
    dialog.remove();
    const u = getCachedUser();
    if (u && !u.gardenIntroduced) {
      u.gardenIntroduced = true;
      try { saveUser(u); } catch { /* Quota silent — 다음 진입 재시도 */ }
    }
    if (navigate) {
      switchTab('stats');
      requestAnimationFrame(() => scrollToGardenSection());
    }
  }

  dialog.querySelector('#welcomeGardenViewBtn')?.addEventListener('click', () => close(true));
  dialog.querySelector('#welcomeGardenCloseBtn')?.addEventListener('click', () => close(false));
  dialog.addEventListener('click', (e) => { if (e.target === dialog) close(false); });
  document.addEventListener('keydown', function onEsc(e: KeyboardEvent) {
    if (e.key === 'Escape') {
      document.removeEventListener('keydown', onEsc);
      close(false);
    }
  });
```

변경:
```typescript
  // C3 (v3.16): AbortController로 single cleanup point — close path 모두에서 listener 자동 제거
  const escController = new AbortController();

  function close(navigate: boolean): void {
    escController.abort();  // keydown listener 즉시 제거
    dialog.remove();
    const u = getCachedUser();
    if (u && !u.gardenIntroduced) {
      u.gardenIntroduced = true;
      try { saveUser(u); } catch { /* Quota silent — 다음 진입 재시도 */ }
    }
    if (navigate) {
      switchTab('stats');
      requestAnimationFrame(() => scrollToGardenSection());
    }
  }

  dialog.querySelector('#welcomeGardenViewBtn')?.addEventListener('click', () => close(true));
  dialog.querySelector('#welcomeGardenCloseBtn')?.addEventListener('click', () => close(false));
  dialog.addEventListener('click', (e) => { if (e.target === dialog) close(false); });
  document.addEventListener(
    'keydown',
    (e: KeyboardEvent) => {
      if (e.key === 'Escape') close(false);
    },
    { signal: escController.signal },
  );
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
npm test -- tests/unit/modals/welcome-garden.spec.ts 2>&1 | tail -20
```

Expected: 신규 3 case 모두 PASS, 기존 case PASS 유지.

- [ ] **Step 5: TS strict + lint + 영향 범위**

```bash
npx tsc --noEmit 2>&1 | tail -5
npm run lint -- src/ui/modals/welcome-garden.ts 2>&1 | tail -5
npm test -- tests/unit/modals/ 2>&1 | tail -10
```

Expected: 0 errors / all PASS.

- [ ] **Step 6: N=5 안정성**

```bash
for i in 1 2 3 4 5; do
  npm test -- tests/unit/modals/welcome-garden.spec.ts 2>&1 | tail -3
done
```

Expected: 5/5 PASS.

- [ ] **Step 7: Commit**

```bash
git add src/ui/modals/welcome-garden.ts tests/unit/modals/welcome-garden.spec.ts
git commit -m "$(cat <<'EOF'
fix(v3.16 T3): C3 — welcome-garden Escape listener cleanup (Codex P2-2)

AbortController 패턴으로 close path 모두 (버튼/backdrop/ESC) 에서
keydown listener 단일 cleanup. 반복 open/close 시 listener 누적 방지.

신규 spec 3건 (close 후 ESC inactive + backdrop close + 반복 누적 0).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: C4 — backfillGarden memo undefined guard (P2-3)

**Files:**
- Modify: `src/state/backfillGarden.ts:27`
- Test: `tests/unit/state/backfill.spec.ts` (undefined fixture 추가)

**배경**: `b.memo.trim().length > 0` (line 27) — `b.memo`가 `undefined` 시 throw. Briefing type은 memo:string 이지만 legacy / loose cast 시 undefined 가능.

- [ ] **Step 1: 실패 테스트 작성 — memo undefined briefing**

`tests/unit/state/backfill.spec.ts` 끝에 추가:

```typescript
  it('C4 (v3.16): briefing.memo가 undefined여도 backfillGarden은 throw하지 않음', () => {
    const u = makeUser({ interests: ['recruiting'] });
    saveBriefingsForTest([
      // memo undefined fixture (loose cast / legacy)
      { ...makeBriefing('채용 동향'), scrapped: true, memo: undefined as unknown as string },
      // 정상 briefing 같이
      { ...makeBriefing('AI 트렌드'), scrapped: true, memo: '정상' },
    ]);

    expect(() => backfillGarden(u)).not.toThrow();

    // 정상 briefing의 memo는 카운트, undefined는 0으로 처리
    const plant = u.plantStateByInterest.recruiting;
    expect(plant).toBeDefined();
    // matched.length=2 (둘 다 scrapped + recruiting 매칭) + memoCount=1 (정상 1건만) = 3
    // recruiting 키워드 매칭 여부에 따라 달라질 수 있음 — 핵심은 throw 안 함
    expect(plant!.cumulativeActivity).toBeGreaterThanOrEqual(0);
  });
```

기존 helper (`makeUser`, `makeBriefing`, `saveBriefingsForTest`) import 또는 in-spec 정의 패턴 따라 추가.

- [ ] **Step 2: 테스트 실패 확인**

```bash
npm test -- tests/unit/state/backfill.spec.ts 2>&1 | tail -15
```

Expected: FAIL — `Cannot read properties of undefined (reading 'trim')`.

- [ ] **Step 3: backfillGarden.ts memo guard 추가**

Edit `src/state/backfillGarden.ts:27`:

기존:
```typescript
    const memoCount = matched.filter(b => b.memo.trim().length > 0).length;
```

변경:
```typescript
    // C4 (v3.16): memo가 undefined인 legacy/loose briefing 방어
    const memoCount = matched.filter(b => !!b.memo && b.memo.trim().length > 0).length;
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
npm test -- tests/unit/state/backfill.spec.ts 2>&1 | tail -15
```

Expected: PASS, 기존 case 영향 없음.

- [ ] **Step 5: TS strict + lint + 영향 범위**

```bash
npx tsc --noEmit 2>&1 | tail -5
npm run lint -- src/state/backfillGarden.ts 2>&1 | tail -5
npm test -- tests/unit/state/ 2>&1 | tail -10
```

Expected: 0 errors / all PASS.

- [ ] **Step 6: Commit**

```bash
git add src/state/backfillGarden.ts tests/unit/state/backfill.spec.ts
git commit -m "$(cat <<'EOF'
fix(v3.16 T4): C4 — backfillGarden memo undefined guard (Codex P2-3)

b.memo가 undefined인 legacy/loose briefing 방어 — !!b.memo 가드 추가.
기존 동작 (memo trim > 0 시 카운트) 동일.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: C5 — Bundle trim Standard (-0.5~-0.7 kB)

**Files:**
- Investigate first: bundle source-map / vite reporter chunked output
- Modify (후보 — T5에서 측정 후 선택):
  - `src/ui/components/garden-grid.ts` (INTEREST_LABEL emoji 인라인 정리, 미사용 helper)
  - `src/state/plantCatalog.ts` (emoji literal 중복)
  - `src/utils/*` (0-reference export sweep)
  - `src/styles/*` (dead CSS rule)

**배경**: T0 bundle baseline = 35099 bytes (34.28 kB). v3.15 hard cap +0.43 over → v3.16 target -0.5~-0.7 kB로 baseline 회복.

**TDD 적용** (Codex 사전 P1-2 fix): bundle trim 작업은 Step 1에서 budget gate red 측정 → trim 적용 → green 측정으로 진정한 red→green 사이클 보장.

- [ ] **Step 1: Bundle budget gate (red) — fix 전 측정으로 fail 확인**

```bash
cd /Users/hayden/Hayden_AX_Project/my_ai_assistance

# 현재 bundle 측정 (T0 결과 재확인)
npm run build 2>&1 | tail -5
BUNDLE_NOW=$(gzip -c dist/assets/index-*.js | wc -c)
BUNDLE_TARGET=$((35099 - 512))  # baseline 35099 - 512 bytes (-0.5 kB target)
echo "Current bundle: $BUNDLE_NOW bytes"
echo "Target ceiling: $BUNDLE_TARGET bytes (baseline -512)"
if [ "$BUNDLE_NOW" -le "$BUNDLE_TARGET" ]; then
  echo "GREEN — already under target, no trim needed"
  exit 0
else
  echo "RED — bundle exceeds target by $((BUNDLE_NOW - BUNDLE_TARGET)) bytes, trim required"
fi
```

Expected: **RED** — bundle 35099 > 34587 (target), excess ~512 bytes. Trim 진입.

(만약 GREEN이면 T5 자동 종료 — Step 8 Commit으로 직행, "T5 N/A — bundle already under target" 명시.)

- [ ] **Step 2: Bundle 분석 — 큰 모듈 식별**

```bash
npm run build -- --mode production 2>&1 | tail -20
# vite reporter 의 file size 분석
ls -laS dist/assets/ | head -10

# source-map 기반 큰 모듈 (선택 — vite source-map enable 필요)
# 또는 미사용 export 분석
npx ts-prune 2>&1 | head -30 || echo "ts-prune unavailable"
```

ts-prune 결과를 기록하고 0-reference export 후보 선별.

- [ ] **Step 3: Trim 후보 우선순위 결정 — 1~2개 선택**

후보 평가표 (실제 측정값으로 채움):

| 후보 | 예상 절감 | 위험 | 진입 |
|---|---|---|---|
| 미사용 helper (ts-prune 0-ref) | ~0.2~0.5 kB | 낮음 | 직접 제거 |
| INTEREST_LABEL → categories.ts label 가공 | ~0.1~0.2 kB | 중간 (label 정확성) | label.replace(/^[^ ]+ /, '') |
| Plant emoji literal 중복 | ~0.1 kB | 낮음 | 단일 const map |
| Dead CSS rule | ~0.1~0.3 kB | 중간 (visual regression) | **strict evidence 필수 (Step 4-CSS branch)** |

**선택 원칙**: 절감 ≥ 0.5 kB 달성 + 위험 합 "낮음~중간" 이내. 비-CSS 후보 우선.

**CSS 후보 진입 시 (P2-2 fix)**: 다음 evidence 모두 충족:

1. 정확한 selector 이름 명시 (예: `.garden-mini-cell--placeholder-X`)
2. 해당 selector 0-reference grep 결과 (src/ + tests/ + index.html 전수): `grep -rn 'CLASS_NAME\|#ID_NAME' src/ tests/ index.html`
3. 제거 전후 smoke 시각 비교 (garden-mini, stats grid 두 영역) — Playwright screenshot 비교 또는 fresh fork render N=3
4. 위 3개 모두 통과 시에만 CSS rule 제거 진행, 아니면 **이 후보 skip → 다른 trim 선택**

- [ ] **Step 4: 선택한 trim 1개 적용 → 측정**

(예: 미사용 helper 제거 시)

- 0-reference export grep으로 caller 0건 확인
- 제거 + 영향 vitest 실행

```bash
# 선택한 변경 적용 후
npm run build 2>&1 | tail -5
gzip -c dist/assets/index-*.js | wc -c | tee /tmp/v316-bundle-after-trim1.txt

# 차이 계산 (baseline 대비)
echo "Baseline: $(cat /tmp/v316-bundle-baseline.txt) bytes"
echo "After:    $(cat /tmp/v316-bundle-after-trim1.txt) bytes"
```

목표 미달 시 → Step 5 trim 2 적용. 달성 시 → Step 6 회귀 안전망.

- [ ] **Step 5: (조건부) Trim 2개째 적용 → 재측정**

Step 4와 동일 절차. 누적 -0.5 kB 도달 시 종료.

목표 (-0.5~-0.7 kB) 미달 시 → DoD soft 인정 (v3.13 hard cap 인정 선례), retro에 명시.

- [ ] **Step 6: 회귀 안전망 spec 추가**

Trim 영역에 따라:

(case A) 미사용 helper 제거 → 회귀 spec 불필요 (이미 0-ref).

(case B) INTEREST_LABEL 가공 → label 정확성 spec 추가.

기존 `tests/unit/tabs/home-garden-mini.spec.ts` 또는 새 spec 위치 (선택한 trim 모듈에 가까운 곳) 에 추가:

```typescript
  it('C5 (v3.16): INTEREST_LABEL은 모든 catalog ID에 대해 빈 문자열 아님', () => {
    const ids = ['recruiting', 'onboarding', 'culture', 'hr_system', 'labor_law',
                 'leadership', 'pm', 'ai_ml', 'data', 'startup',
                 'marketing', 'productivity', 'career', 'communication', 'self_dev'];
    for (const id of ids) {
      expect(INTEREST_LABEL[id]).toBeTruthy();
      expect(INTEREST_LABEL[id].length).toBeGreaterThan(0);
    }
  });
```

(case C) Dead CSS rule 제거 → Step 3 evidence 4건이 회귀 안전망 역할.

- [ ] **Step 7: Bundle budget gate (green) — fix 후 측정으로 pass 확인**

```bash
npm run build 2>&1 | tail -5
BUNDLE_AFTER=$(gzip -c dist/assets/index-*.js | wc -c)
BUNDLE_TARGET=34587  # baseline 35099 - 512
echo "After trim: $BUNDLE_AFTER bytes (Δ from baseline: $((BUNDLE_AFTER - 35099)))"
if [ "$BUNDLE_AFTER" -le "$BUNDLE_TARGET" ]; then
  echo "GREEN — target -512 bytes achieved"
else
  echo "AMBER — under -512 target (Δ $((BUNDLE_TARGET - BUNDLE_AFTER))). DoD soft 인정 (Step 8 commit message에 명시)"
fi
```

전체 검증:

```bash
npx tsc --noEmit 2>&1 | tail -5
npm run lint 2>&1 | tail -5
npm test 2>&1 | tail -10
npm run test:smoke 2>&1 | tail -10
```

Expected: TS 0 errors / lint 0 / vitest all PASS / smoke 동일 수준 (T0 baseline 대비 visual regression 0건).

- [ ] **Step 8: Commit**

```bash
git add <변경 파일들>
git commit -m "$(cat <<'EOF'
perf(v3.16 T5): C5 — bundle trim Standard (<측정값> kB → <측정값> kB, Δ <차이>)

<선택한 trim 항목 1~2개 명시>

DoD: -0.5~-0.7 kB target. 측정값 기록 (T0 baseline 대비).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: C6 — layout flake repro N=10 → diagnose → bounded fix

**Files:**
- Test: `tests/smoke/layout.spec.ts` (또는 `desktop-layout.spec.ts` — repro 후 confirm)

**배경**: pre-existing intermittent flake. v3.14.4 cold-start 4건 중 일부 + v3.15 T16/T16.1에서 1회씩 재현. **bounded fix** — 1 task budget (≤4시간) escape valve.

- [ ] **Step 1: Repro N=10 — flake frequency 측정**

```bash
# layout.spec.ts 만 N=10 fresh fork
for i in $(seq 1 10); do
  echo "=== run $i ==="
  npx playwright test tests/smoke/layout.spec.ts --reporter=line 2>&1 | tail -5
done | tee /tmp/v316-layout-flake-n10.txt

# 또한 desktop-layout.spec.ts 도 동시 측정
for i in $(seq 1 10); do
  echo "=== desktop run $i ==="
  npx playwright test tests/smoke/desktop-layout.spec.ts --reporter=line 2>&1 | tail -5
done | tee /tmp/v316-desktop-layout-flake-n10.txt
```

결과 분석:
- 0/10 fail → flake 자연 소실, Step 5로 직행 (escape valve = closed without fix)
- 1~3/10 fail → bounded fix 진입 (Step 2)
- ≥4/10 fail → escape valve 발동 (Step 6 직행)

- [ ] **Step 2: 실패 케이스 root cause 가설 도출**

실패한 spec의 specific case 출력 분석:
- timing race? → expect의 timeout 만료, locator wait 실패
- spec drift? → DOM/CSS 변경으로 selector miss
- env? → 폰트 로드, viewport rounding, animation 미정착

```bash
# 실패한 run 1개를 trace로 다시
npx playwright test tests/smoke/layout.spec.ts --trace=on 2>&1 | tail -20
# 결과: playwright-report/ 디렉토리에 trace.zip
# trace 분석 (UI: npx playwright show-trace playwright-report/trace.zip)
```

가설 1줄 정리 (예: "drawer translateX 애니메이션 60ms — `await page.waitForFunction` 누락 시 fail").

- [ ] **Step 3: Fix 비용 추정**

| Fix 유형 | 예상 비용 | Action |
|---|---|---|
| `await expect(...).toBeVisible({ timeout: 2000 })` 또는 `waitForLoadState('networkidle')` 추가 | 5분 | Step 4 진행 |
| Selector 정확화 (e.g. `.locator('drawer').waitFor({ state: 'attached' })`) | 15분 | Step 4 진행 |
| CSS animation finish wait helper 도입 | 30분~1시간 | Step 4 진행 |
| 신규 fixture 또는 테스트 재구조화 | >1 task | **Step 6 escape valve** |

- [ ] **Step 4: Fix 적용 (1 task budget 이내)**

가설에 따라 layout.spec.ts (또는 desktop-layout.spec.ts) 의 해당 case에 wait/timeout/locator 강화.

**핵심 원칙 (Codex 사전 P2-3)**: wait condition은 assertion이 검증하는 invariant와 **같은 기하적 속성**을 polling해야 함 (예: bottom-nav가 submit을 가리지 않음 = `submitBox.bottom <= navBox.top`).

T0에서 확인된 실제 flake: **mobile (375x667) bottom-nav + submit 가림** (`tests/smoke/layout.spec.ts:49`). 가설 — submit button이 keyboard 또는 viewport scroll 안정 전 측정되어 boundingBox가 nav 아래로 밀려나는 race. 의도한 invariant: `submitBox.y + submitBox.height <= navBox.y`.

예시 fix (mobile case에 geometric invariant polling 추가):

```typescript
test('mobile (375x667) keeps bottom nav + submit is not covered', async ({ page }) => {
  await seedUser(page);
  await page.setViewportSize({ width: 375, height: 667 });
  await page.goto('/');
  await expect(page.locator('#homeTab')).toBeVisible();

  const nav = page.locator('#bottomNav');
  await expect(nav).toBeVisible();  // ensure nav is settled

  const submit = page.locator('#submitBtn');
  await submit.scrollIntoViewIfNeeded();

  // C6 (v3.16): assertion이 검증할 geometric invariant를 직접 polling — Codex 사전 P2-3
  await expect.poll(async () => {
    const navBox = await nav.boundingBox();
    const submitBox = await submit.boundingBox();
    if (!navBox || !submitBox) return null;
    return submitBox.y + submitBox.height <= navBox.y;  // submit이 nav 위에 있는가
  }, { timeout: 2000, intervals: [100, 200, 400] }).toBe(true);

  // 기존 assertion 유지 (geometric stability 보장 후 재확인)
  const navBox = await nav.boundingBox();
  const submitBox = await submit.boundingBox();
  expect(navBox).not.toBeNull();
  expect(submitBox).not.toBeNull();
  expect(navBox!.y + navBox!.height).toBeGreaterThan(600);
  expect(submitBox!.y + submitBox!.height).toBeLessThanOrEqual(navBox!.y);
});
```

(실제 코드는 Step 2 root cause 가설에 맞게 조정 — 위는 mobile race 가설 시 패턴 예시.)

만약 desktop drawer 관련 flake로 진단되면 동일 원칙 — assertion이 검증하는 attribute (`data-open="false"` + `boundingBox().x + width <= 0` for offscreen) 를 polling.

- [ ] **Step 5: N=5 verify (또는 N=10)**

```bash
for i in $(seq 1 10); do
  echo "=== verify run $i ==="
  npx playwright test tests/smoke/layout.spec.ts tests/smoke/desktop-layout.spec.ts --reporter=line 2>&1 | tail -5
done
```

Expected: 0/10 fail (또는 ≥9/10 PASS — 1회 잔존 시 escape valve로 격하).

- [ ] **Step 6: (조건부) Escape valve — diagnose-only 격하**

Step 1에서 ≥4/10 fail이거나, Step 4 fix 후 N=10에서도 flake 잔존 시:

`project_daily_growth_v316.md` 메모리 파일 (T8에서 작성 예정) 에 다음을 carry-forward:

```markdown
## C6 carry-forward (v3.17)

**증상**: layout.spec.ts (또는 desktop-layout.spec.ts) intermittent flake.
**Repro 명령**: `npx playwright test tests/smoke/layout.spec.ts --repeat-each=10`
**관찰 frequency**: <측정값>/10
**Root cause 가설**: <Step 2 결과 1줄>
**시도한 fix**: <Step 4 적용 내용>, 효과 미달
**다음 시도 후보**: <fix 후보 ranked>
```

`.skip` 격리는 하지 않음 (회귀 안전망 유지). PR 생성 시 known flake 명시.

- [ ] **Step 7: Commit**

(Step 4 fix 적용 시):

```bash
git add tests/smoke/layout.spec.ts  # 또는 desktop-layout.spec.ts
git commit -m "$(cat <<'EOF'
fix(v3.16 T6): C6 — layout flake fix (<root cause 1줄>)

N=10 repro 결과 <측정값>/10 fail → <root cause 가설> 확인 →
<fix 내용> 적용 → N=10 verify <0~1>/10 fail.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

(Step 6 escape valve 시):

```bash
# fix commit 없음 (또는 진단 메모만)
echo "C6 escape valve — v3.17 carry-forward" >> /tmp/v316-cycle-notes.txt
# T8 retro에서 메모리 carry-forward
```

---

## Task 7: Codex 최종 review (deploy gate)

- [ ] **Step 1: 사이클 끝 일괄 검증**

```bash
npm run lint 2>&1 | tail -5
npm test 2>&1 | tail -10
npm run test:smoke 2>&1 | tail -10
npm run build 2>&1 | tail -5
gzip -c dist/assets/index-*.js | wc -c | tee /tmp/v316-bundle-final.txt
```

Expected: lint 0 / vitest all PASS / smoke all PASS (또는 layout flake escape) / bundle 측정값 기록.

- [ ] **Step 2: Codex 최종 review 트리거 (controller / 사용자)**

Spec + plan + 모든 commit (T1~T6) 을 Codex에 전달. 분류 `DEPLOY_APPROVED` / `APPROVED_WITH_NOTES` / `REJECT`.

권한 issue 시 controller self-review fallback (v3.14.5 패턴) — spec §11에 명시.

- [ ] **Step 3: REJECT 시 in-cycle hotfix → 2차 review**

P0/P1 in-cycle fix만 허용 (v3.15 T16.1 패턴). 신규 기능 금지. 2차 review로 APPROVED 도달 후 진행.

- [ ] **Step 4: 결과 spec §11 placeholder에 기록**

```bash
# spec §11 (사이클 retro) placeholder를 실제 결과로 채움
# 11.0 핵심 결과 / 11.1 사전 review / 11.2 최종 review / 11.3 lessons / 11.4 v3.17 권고
```

(T8 retro에서 본격 작성, 본 task에서는 review 결과만 §11.2로 기록)

- [ ] **Step 5: Commit (필요 시)**

(in-cycle hotfix 있으면 별도 commit. review 결과 기록만이면 spec 변경 commit)

```bash
git add docs/superpowers/specs/2026-05-04-v3.16-polish-design.md
git commit -m "docs(v3.16 §11.2): Codex 최종 review 결과 기록 (<APPROVED/APPROVED_WITH_NOTES> — Pn=N)"
```

---

## Task 8: 사이클 retro

**Files:**
- Modify: `docs/superpowers/specs/2026-05-04-v3.16-polish-design.md` (§11)
- Update: 메모리 — 신규 entry `project_daily_growth_v316.md` + MEMORY.md index

- [ ] **Step 1: spec §11 retro 섹션 작성**

v3.14.5 §8 / v3.15 §11 retro 패턴 follow:
- 11.0 핵심 결과 (사이클 기간 / commits 수 / vitest count / bundle 변화 / deploy hash)
- 11.1 Codex 사전 review 결과 + in-cycle 반영 항목
- 11.2 Codex 최종 review 결과 (DEPLOY_APPROVED 계열)
- 11.3 신규 lessons (1~3건)
- 11.4 v3.17 권고 (carry-forward + 차기 후보)
- 11.5 사이클 메타 관찰

- [ ] **Step 2: 메모리 entry 작성**

`/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v316.md` 신규 작성:

```markdown
---
name: DG v3.16 Polish 배포 완료
description: v3.15 finale carry-forward 6건 일괄 소진 + Codex 2-pass 게이트 통과 사이클
type: project
---

**✅ 배포 완료 (2026-05-04, main HEAD <hash>, Firebase live)** — <commits 수> commits · <vitest count> vitest (+<delta>) + <smoke count> Playwright smoke · Codex 2-pass (사전 / 최종 결과). <핵심 변경 1줄 요약>. Bundle <baseline>→<final> kB (<delta>). schema/CSP/migration 0줄. <신규 lessons 1~2건 1줄 요약>. **carry-forward**: <v3.17로 넘어간 항목들>.
```

- [ ] **Step 3: MEMORY.md index 업데이트**

`/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md` 에 한 줄 추가 (v3.15 entry 다음):

```markdown
- [DG v3.16 Polish](project_daily_growth_v316.md) — <description 1줄>
```

- [ ] **Step 4: 배포 (사용자 명시 승인 후)**

```bash
# 사용자 "배포해줘" 명시 시점에만
firebase deploy --only hosting
```

- [ ] **Step 5: Commit retro**

```bash
git add docs/superpowers/specs/2026-05-04-v3.16-polish-design.md
git commit -m "$(cat <<'EOF'
docs(v3.16 T8): 사이클 retro — Codex 2-pass + <commits>건 + <APPROVED>

§11 핵심 결과 + lessons + v3.17 권고 작성.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Verification (사이클 끝)

| 항목 | Target | 실측 (T8 작성) |
|---|---|---|
| Vitest pass | +15~25 over baseline 828 | <측정> |
| Bundle CLI gzip | 34.28 → 33.6~33.8 kB (-0.5~-0.7) | <측정> |
| Playwright smoke | 95/95 (또는 flake escape 명시) | <측정> |
| TS strict | 0 errors | <측정> |
| Lint | 0 errors | <측정> |
| Codex 사전 | P0/P1 = 0 또는 in-cycle 반영 | <결과> |
| Codex 최종 | APPROVED 계열 | <결과> |
| **Closed items** | v3.15 carry-forward 6/6 | <체크> |
