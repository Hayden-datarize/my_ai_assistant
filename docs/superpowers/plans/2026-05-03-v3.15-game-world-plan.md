# DG v3.15 — Game World Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 게임화 시리즈 3/3 마무리 — 관심분야별 식물(5 stage)이 분야 활동(스크랩+메모 매칭) + 미션 완수 보너스로 진화하는 정원 시스템 구현. 홈 mini preview + stats 탭 full grid 의 hybrid UI, 환영 모달 + backfill, visual wilting (stage 유지), max stage 도달 시 confetti+토스트 트로피, 분야 archive+복원 정책.

**Architecture:** v3.12/v3.13 graduated 패턴 100% 재사용 — `badgeCatalog` 패턴으로 `plantCatalog`, `missionEngine` 패턴으로 `plantEngine`, v3.12 환영 모달/`detectEvents` sweep/CSS confetti 인프라 그대로. 신규 5 module + 4 module 확장. Schema v3→v4 lazy migration. atomic single-write (saveUser throw → sweep skip) 원칙 follow.

**Tech Stack:** TypeScript strict / Vite SPA / Vitest unit / Playwright smoke / Firebase Hosting / 기존 emoji 기반 시각화 (asset 0).

**Spec reference:** `docs/superpowers/specs/2026-05-03-v3.15-game-world-design.md` — 본 plan은 spec §1~§12 모든 섹션의 task-level 분해.

---

## Baseline (T0 직후 채워짐)

```
Date          : 2026-05-04 (사이클 시작)
HEAD          : c91ddfa
Vitest count  : 719 passed (119 files)
Bundle CLI gzip: 32,103 bytes ≒ 31.35 kB
Smoke baseline: 93/93 PASS × N=3 (39.1s / 35.8s / 37.9s) — cold-2 cascade 0회
```

---

## File Structure (전체 코드 변경 사전 매핑)

**신규 파일 (5):**
- `src/state/plantTypes.ts` — `PlantState` interface, `PlantId` alias, stage type narrowing
- `src/state/plantCatalog.ts` — Stage emoji catalog (1~4 공통, 5 분야별), `thresholdLookup`, `stageLabel`, `getPlantIcon`
- `src/state/plantEngine.ts` — `tickPlantActivity`, `applyMissionBonus`, `checkWilting` (read-only), interest add/remove 시 entry 생성/유지 정책
- `src/state/backfillGarden.ts` — Backfill 알고리즘 (loadUserData path에서 호출, sweep 우회)
- `src/ui/components/garden-grid.ts` — `renderGardenGrid` (full, stats용) + `renderGardenMini` (홈 row variant)
- `src/ui/modals/welcome-garden.ts` — 첫 진입 1회 환영 모달

**수정 파일 (10):**
- `src/state/user.ts` — User interface schema v4, getCachedUser path에 backfillGarden 자동 호출, **isValidUserShape v4 nested guard (S8)**
- `src/state/migration.ts` — `migrateUserToV4` 추가, **`migrateUserToV3`에 v4 early return (S5)**, getCachedUser version check 확장
- `src/state/achievements.ts` — `Snapshot.plantStages`, `detectEvents` plant-stage-up case (kind discriminant + at:number, S6), `emitEvents` plant-stage-up case dispatch (`dg:reward:plant-stage-up`)
- `src/state/gameTypes.ts` — `GameEvent` discriminated union에 `plant-stage-up` variant (kind discriminant)
- `src/state/missionEngine.ts` — `tickMissionProgress` 미션 완수 직후 `applyMissionBonus(period)` hook
- `src/state/briefings.ts` — **mutateWithSweep 안에 `tickPlantsByBriefingInMemory` 통합 (S7)** — 별도 saveUser 호출 X
- `src/ui/messages.ts` — `MSG.GARDEN_*` 8~10 신규 키
- `src/ui/tabs/stats.ts` — `#gardenSection` 추가, hydrate 호출
- `src/ui/tabs/home.ts` — `#gardenMini` 추가, hydrate 호출
- `src/ui/modals/interests.ts` — **save handler에 `ensurePlantsForInterests(user)` 호출 (S10, T15 신규 task)**
- `src/ui/rewards.ts` (또는 toast 모듈) — `.toast--bloom` 생성 helper
- `src/ui/events.ts` — `dg:reward:plant-stage-up` listener (S6 채널)
- `src/styles/components/garden.css` (NEW or in existing components.css) — wilting/stage5 그라디언트/✨ overlay 스타일

**테스트 파일 (11 신규):**
- `tests/unit/state/migration-v4.spec.ts` (T1) — v4 migrate + v3 early return + isValidUserShape v4 guard
- `tests/unit/state/plantCatalog.spec.ts` (T2) — INTERESTS 15 ID + boundary + S9 회귀
- `tests/unit/state/plantEngine.spec.ts` (T3, T4 추가) — tick + bonus + ensurePlantsForInterests + wilting
- `tests/unit/state/archive-restore.spec.ts` (T3에 합산)
- `tests/unit/state/events-plant.spec.ts` (T5, T14) — kind discriminant + at + dg:reward listener
- `tests/unit/state/missionEngine-plant-bonus.spec.ts` (T6)
- `tests/unit/state/briefings-plant-hook.spec.ts` (T7) — 단위 + integration (mutateWithSweep)
- `tests/unit/state/backfill.spec.ts` (T8)
- `tests/unit/ui/garden-grid.spec.ts` (T10)
- `tests/unit/modals/welcome-garden.spec.ts` (T13)
- `tests/unit/modals/interests-plant-hook.spec.ts` (T15 신규)
- `tests/smoke/v3.15-garden.spec.ts` (T16)

**합산 추정**: 9 (catalog) + 19 (plantEngine: tick 10 + bonus 4 + ensurePlants 5) + 3 (archive) + 7 (wilting) + 11 (events) + 5 (missionBonus) + 8 (briefings) + 8 (backfill) + 9 (migration) + 10 (grid) + 3 (stats) + 2 (home) + 8 (welcome) + 3 (maxEvent in events-plant) + 4 (interests) = **~109 spec** (target 95~115, lower bound 85). spec §1.4 "+70~85" 부분 retroactive sync 권고 (T0 baseline + T16 verify에서 실측).

---

## §Threshold Sim (T2 작성 시 시뮬레이션 시트)

본 spec §3.1/§3.2/§7-2 의 S4 self-review fix per: 8/25/70/160 + +1/+5/+20는 placeholder. 시뮬 결과로 ±50% 조정 가능. 기준: Q9 commitment = engaged ~3개월 / casual ~5개월 max.

T2에서 다음 정보 시뮬:

```
가정 (실측 상수 추출 또는 보수적 추정):
  - briefings/day per user = 5~10 (RSS feed avg)
  - scrap rate = 20~40% → scrap/day = 1~4
  - 분야 매칭 평균/briefing = 1.5 (분야 keyword 사전 평균 hit count)
  - user 관심분야 N = 4~6
  - 분야별 매칭 활동 (heavy / casual) 시뮬

미션 완수 빈도:
  - power user = daily 3/day + weekly 2/주 + monthly 1/월 모두 완수
  - mid user = daily 1~2/day + weekly 1/주
  - casual = daily 0~1/day

목표 시간 horizon:
  - power user → engaged ~2~3개월 stage 5 도달
  - mid user → ~3~4개월
  - casual → ~5~6개월

T2에서 위 시뮬 후 §Threshold Sim section을 본 plan 끝에 부록으로 추가.
절대값 spec 수치(8/25/70/160 + +1/+5/+20) 변경 시 spec §3.1/§3.2 retroactive 동기화.
```

---

## Task 0: Baseline 측정

**Files:**
- Modify: `docs/superpowers/plans/2026-05-03-v3.15-game-world-plan.md` (이 파일 §Baseline 채우기)

- [x] **Step 1: vitest 카운트 측정**

```bash
npm test 2>&1 | tail -20
```

Expected: 모든 spec PASS, total count 추출 (예: `Tests  719 passed (719)`)

- [x] **Step 2: bundle 측정 (CLI gzip canonical, v3.14.5 graduated)**

```bash
npm run build && gzip -c dist/assets/index-*.js | wc -c
```

Expected: 숫자 (bytes). v3.14.5 baseline 31,277 ≒ 31.28 kB.

- [x] **Step 3: Playwright cold-start N=3 측정**

```bash
npx playwright test --reporter=line 2>&1 | tail -5  # 3회 반복
```

Expected: 3회 모두 93/93 PASS (또는 실패 spec 명시).

- [x] **Step 4: §Baseline section 채우기**

위 측정 결과를 plan §Baseline에 기록. cold-2 cascade 등장 spec 있으면 §Notes에 명시 (v3.14.5 graduated 트리거 도래 여부 판단).

- [x] **Step 5: Commit**

```bash
git add docs/superpowers/plans/2026-05-03-v3.15-game-world-plan.md
git commit -m "docs(v3.15 T0): baseline 측정 (vitest / bundle CLI gzip / smoke N=3)"
```

---

## Task 1: Schema v4 + plantTypes + migrateUserToV4

**Files:**
- Create: `src/state/plantTypes.ts`
- Modify: `src/state/user.ts:8-29` (User interface, schemaVersion type)
- Modify: `src/state/migration.ts` (migrateUserToV4 추가)
- Modify: `src/state/user.ts:50-53` (getCachedUser version check 확장)
- Test: `tests/unit/state/migration-v4.spec.ts`

- [ ] **Step 1: plantTypes.ts 생성**

```ts
// src/state/plantTypes.ts
export type PlantStage = 1 | 2 | 3 | 4 | 5;

export interface PlantState {
  stage: PlantStage;
  cumulativeActivity: number;
  unlockedAt?: string;        // stage 5 도달 ISO (영구 ✨ 마크용)
  lastEngagedAt?: string;     // 마지막 활동 ISO (wilting 판단)
}
```

- [ ] **Step 2: User interface schema v4로 확장 (user.ts:8-29)**

```ts
// src/state/user.ts:8-29 교체
import type { PlantState } from './plantTypes';

export interface User {
  name: string;
  interests: string[];
  onboardedAt: string;
  streak: number;
  lastActiveDate: string;
  xp: number;
  earnedBadges: Record<string, number>;
  gamificationMigrated: boolean;
  schemaVersion: 4;                                       // v3 → v4
  missions: {
    active: MissionInstance[];
    cumulative: { dailyCount: number; weeklyCount: number; monthlyCount: number };
    lastDailySeed: string;
    currentWeekIso: string;
    currentMonthIso: string;
  };
  plantStateByInterest: Record<string, PlantState>;       // v3.15 NEW
  gardenIntroduced: boolean;                              // v3.15 NEW (환영 모달 1회)
  gardenBackfilled: boolean;                              // v3.15 NEW (backfill idempotent)
}
```

- [ ] **Step 3: migrateUserToV3 v4 early return 추가 (P0-1 fix, S5)**

```ts
// src/state/migration.ts:134 migrateUserToV3 함수 첫 줄에 추가
export function migrateUserToV3(raw: unknown): User {
  const v = raw as Partial<User> & {
    schemaVersion?: number;
    missions?: Partial<User['missions']>;
  };
  // S5 fix (Codex P0-1): v4 user는 v3 migrate 통과 — schemaVersion=3 덮어쓰고 missions reset 방지.
  // v4가 v3의 superset (plantStateByInterest + gardenIntroduced + gardenBackfilled 추가)이므로
  // 모든 v3 필드 보존되어 안전.
  if (v?.schemaVersion === 4) return v as User;

  if (v?.schemaVersion === 3 && v.missions && Array.isArray(v.missions.active)) {
    // ... 기존 logic 그대로
  }
  // ... 나머지 그대로
}
```

- [ ] **Step 4: migrateUserToV4 추가 (migration.ts 끝부분)**

```ts
// src/state/migration.ts 마지막에 추가
import type { PlantState } from './plantTypes';

export function migrateUserToV4(u: unknown): User {
  // 입력은 v2 또는 v3 사용자 (migrateUserToV3 통과 직후 호출 가정)
  const r = u as Record<string, unknown> & {
    schemaVersion?: number;
    plantStateByInterest?: unknown;
    gardenIntroduced?: unknown;
    gardenBackfilled?: unknown;
  };

  if (r.schemaVersion === 4) return r as unknown as User;

  // v3 → v4 lazy: 빈 정원 + flag 0
  const migrated = {
    ...r,
    schemaVersion: 4 as const,
    plantStateByInterest:
      typeof r.plantStateByInterest === 'object' && r.plantStateByInterest !== null
        ? (r.plantStateByInterest as Record<string, PlantState>)
        : {},
    gardenIntroduced: typeof r.gardenIntroduced === 'boolean' ? r.gardenIntroduced : false,
    gardenBackfilled: typeof r.gardenBackfilled === 'boolean' ? r.gardenBackfilled : false,
  };
  return migrated as unknown as User;
}
```

- [ ] **Step 5: getCachedUser version check 확장 (user.ts:50-53)**

```ts
// 기존:
//   let user = (parsed?.schemaVersion === 2 || parsed?.schemaVersion === 3)
//     ? parsed
//     : migrateUserToV2(parsed);
//   user = migrateUserToV3(user);
// 변경:
let user = (parsed?.schemaVersion === 2 || parsed?.schemaVersion === 3 || parsed?.schemaVersion === 4)
  ? parsed
  : migrateUserToV2(parsed);
user = migrateUserToV3(user);  // v4 user는 step 3 fix로 early return
user = migrateUserToV4(user);
```

(import에 `import { migrateUserToV2, migrateUserToV3, migrateUserToV4 } from './migration';`)

- [ ] **Step 6: lazy persist version check (user.ts:60)**

```ts
// 기존:
//   if (parsed?.schemaVersion !== 3) {
// 변경:
if (parsed?.schemaVersion !== 4) {
```

- [ ] **Step 7: isValidUserShape v4 guard 추가 (P1-2 fix, S8)**

```ts
// src/state/user.ts:76 isValidUserShape 함수 끝부분에 추가
function isValidUserShape(u: unknown): u is User {
  if (!u || typeof u !== 'object') return false;
  const r = u as Record<string, unknown>;
  const baseValid = typeof r.name === 'string'
    && Array.isArray(r.interests)
    && r.interests.every((i) => typeof i === 'string')
    && typeof r.streak === 'number' && Number.isFinite(r.streak)
    && typeof r.lastActiveDate === 'string'
    && typeof r.xp === 'number' && Number.isFinite(r.xp);
  if (!baseValid) return false;

  // S8 fix (Codex P1-2): v4 plantStateByInterest nested guard
  if (r.schemaVersion === 4) {
    if (typeof r.plantStateByInterest !== 'object' || r.plantStateByInterest === null) return false;
    for (const plant of Object.values(r.plantStateByInterest as Record<string, unknown>)) {
      if (!plant || typeof plant !== 'object') return false;
      const p = plant as Record<string, unknown>;
      const stage = p.stage;
      if (typeof stage !== 'number' || !Number.isInteger(stage) || stage < 1 || stage > 5) return false;
      if (typeof p.cumulativeActivity !== 'number' || !Number.isFinite(p.cumulativeActivity)) return false;
    }
  }
  return true;
}
```

- [ ] **Step 8: Failing test 작성 (Codex S5/S8 fix 케이스 포함)**

```ts
// tests/unit/state/migration-v4.spec.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { migrateUserToV3, migrateUserToV4 } from '../../../src/state/migration';

describe('migrateUserToV4', () => {
  it('v3 user를 v4로 변환 — 빈 정원 + flag 0', () => {
    const v3 = {
      name: 'Hayden', interests: ['recruiting'], onboardedAt: '2026-04-30', streak: 5,
      lastActiveDate: '2026-05-01', xp: 100, earnedBadges: {},
      gamificationMigrated: true, schemaVersion: 3,
      missions: { active: [], cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 }, lastDailySeed: '', currentWeekIso: '', currentMonthIso: '' },
    };
    const v4 = migrateUserToV4(v3);
    expect(v4.schemaVersion).toBe(4);
    expect(v4.plantStateByInterest).toEqual({});
    expect(v4.gardenIntroduced).toBe(false);
    expect(v4.gardenBackfilled).toBe(false);
    expect(v4.streak).toBe(5);  // 기존 필드 보존
  });

  it('v4 user는 그대로 통과 (idempotent)', () => {
    const v4 = {
      schemaVersion: 4,
      plantStateByInterest: { ai_ml: { stage: 3, cumulativeActivity: 30 } },
      gardenIntroduced: true, gardenBackfilled: true,
    };
    const out = migrateUserToV4(v4);
    expect(out).toBe(v4);  // 같은 reference
  });

  it('손상 plantStateByInterest는 빈 객체로 fallback', () => {
    const broken = { schemaVersion: 3, plantStateByInterest: 'NOT_OBJECT' };
    const out = migrateUserToV4(broken);
    expect(out.plantStateByInterest).toEqual({});
  });
});

describe('migrateUserToV3 v4 early return (Codex S5 / P0-1 fix)', () => {
  it('v4 user → migrateUserToV3 통과 시 missions / plant 보존 (이전엔 reset 됐음)', () => {
    const v4 = {
      name: 'h', interests: ['recruiting'], onboardedAt: '', streak: 5,
      lastActiveDate: '', xp: 100, earnedBadges: {}, gamificationMigrated: true,
      schemaVersion: 4,
      missions: { active: [{ defId: 'daily-1', period: 'daily' }], cumulative: { dailyCount: 7, weeklyCount: 2, monthlyCount: 1 }, lastDailySeed: '2026-05-03', currentWeekIso: '2026-W18', currentMonthIso: '2026-05' },
      plantStateByInterest: { recruiting: { stage: 3, cumulativeActivity: 25 } },
      gardenIntroduced: true, gardenBackfilled: true,
    };
    const out = migrateUserToV3(v4);
    expect(out.schemaVersion).toBe(4);
    expect(out.missions.cumulative.dailyCount).toBe(7);  // reset 안 됨
    expect((out as any).plantStateByInterest.recruiting.stage).toBe(3);
  });

  it('v3 user는 기존 logic대로 처리 (regression 방지)', () => {
    const v3 = {
      name: 'h', interests: [], onboardedAt: '', streak: 0, lastActiveDate: '',
      xp: 0, earnedBadges: {}, gamificationMigrated: true, schemaVersion: 3,
      missions: { active: [], cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 }, lastDailySeed: '', currentWeekIso: '', currentMonthIso: '' },
    };
    const out = migrateUserToV3(v3);
    expect(out.schemaVersion).toBe(3);
  });
});

describe('isValidUserShape v4 nested guard (Codex S8 / P1-2 fix)', () => {
  // isValidUserShape는 user.ts 내부 함수 — 직접 export X.
  // getCachedUser path를 통해 invalidation 검증 (notifyCorruption + null 반환)

  beforeEach(() => { localStorage.clear(); });

  it('plantStateByInterest 손상 (non-object) → corruption + null', async () => {
    const { getCachedUser } = await import('../../../src/state/user');
    localStorage.setItem('user', JSON.stringify({
      name: 'h', interests: [], streak: 0, lastActiveDate: '', xp: 0, schemaVersion: 4,
      plantStateByInterest: 'BROKEN',
      gardenIntroduced: false, gardenBackfilled: false,
    }));
    const u = getCachedUser();
    expect(u).toBeNull();
  });

  it('plant entry stage NaN → corruption + null', async () => {
    const { getCachedUser } = await import('../../../src/state/user');
    localStorage.setItem('user', JSON.stringify({
      name: 'h', interests: [], streak: 0, lastActiveDate: '', xp: 0, schemaVersion: 4,
      plantStateByInterest: { recruiting: { stage: NaN, cumulativeActivity: 10 } },
      gardenIntroduced: false, gardenBackfilled: false,
    }));
    const u = getCachedUser();
    expect(u).toBeNull();
  });

  it('plant entry stage out-of-range (6) → corruption + null', async () => {
    const { getCachedUser } = await import('../../../src/state/user');
    localStorage.setItem('user', JSON.stringify({
      name: 'h', interests: [], streak: 0, lastActiveDate: '', xp: 0, schemaVersion: 4,
      plantStateByInterest: { recruiting: { stage: 6, cumulativeActivity: 10 } },
      gardenIntroduced: false, gardenBackfilled: false,
    }));
    expect(getCachedUser()).toBeNull();
  });

  it('정상 v4 user → 통과', async () => {
    const { getCachedUser } = await import('../../../src/state/user');
    localStorage.setItem('user', JSON.stringify({
      name: 'h', interests: ['recruiting'], onboardedAt: '', streak: 0,
      lastActiveDate: '', xp: 0, earnedBadges: {}, gamificationMigrated: true,
      schemaVersion: 4,
      missions: { active: [], cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 }, lastDailySeed: '', currentWeekIso: '', currentMonthIso: '' },
      plantStateByInterest: { recruiting: { stage: 3, cumulativeActivity: 30 } },
      gardenIntroduced: true, gardenBackfilled: true,
    }));
    expect(getCachedUser()?.plantStateByInterest['recruiting']?.stage).toBe(3);
  });
});
```

- [ ] **Step 9: 테스트 PASS 확인**

```bash
npx vitest run tests/unit/state/migration-v4.spec.ts
```

Expected: 9 PASS (3 v4 migrate + 2 v3 early return + 4 shape guard).

- [ ] **Step 10: 전체 vitest 회귀 확인 (TS strict + 기존 spec)**

```bash
npx tsc --noEmit && npm test
```

Expected: 0 ts error, 모든 기존 spec PASS, 신규 +9.

- [ ] **Step 11: Commit**

```bash
git add src/state/plantTypes.ts src/state/user.ts src/state/migration.ts tests/unit/state/migration-v4.spec.ts
git commit -m "feat(v3.15 T1): schema v4 + PlantState + migrate v4 (S5/S8 P0/P1 fix 포함)"
```

---

## Task 2: plantCatalog (emoji + threshold lookup)

**Files:**
- Create: `src/state/plantCatalog.ts`
- Test: `tests/unit/state/plantCatalog.spec.ts`

- [ ] **Step 1: §Threshold Sim 시뮬레이션 (spec §3.1 S4 fix)**

T2 시작 시점에 다음 시뮬 후 plan 끝에 §Threshold Sim 부록 추가:

1. interestKeywords 사전 size + briefing 본문 평균 hit count 측정 (실 데이터 또는 fixture)
2. heavy/mid/casual 사용자 분야별 매칭 briefings/day 계산
3. 미션 완수 빈도 + effective multiplier (식물 N개 곱)
4. Q9 commitment (engaged 3달 / casual 5달) hit하는 threshold + 보너스 절대값 derive
5. 결과: 8/25/70/160 + +1/+5/+20 유지 또는 ±50% 조정
6. spec §3.1/§3.2 변경 시 retroactive 동기화 (commit 별도)

본 task는 시뮬 결과 수치로 진행 (이하 코드는 spec placeholder 가정).

- [ ] **Step 2: plantCatalog.ts 생성**

```ts
// src/state/plantCatalog.ts
import type { PlantStage } from './plantTypes';

/**
 * Stage 1~4: 분야 공통 emoji (씨앗 → 새싹 → 줄기 → 봉오리)
 * Stage 5: 분야별 차별화 emoji (max stage 도달 시 도파민 모먼트)
 *
 * 사용자 정의 분야 (catalog 외) → DEFAULT_STAGE5 fallback (🌳 만개 나무)
 */
const COMMON_STAGE_EMOJI: Record<1 | 2 | 3 | 4, string> = {
  1: '🌱',  // 씨앗
  2: '🌿',  // 새싹
  3: '🌷',  // 줄기 (※ T2 시뮬 시 OS 호환 검증 후 확정)
  4: '🌳',  // 봉오리 (※ 동일)
};

/**
 * Stage 5 분야별 (interest ID → emoji). S9 fix (Codex P1-3): 실제 INTERESTS catalog
 * (`src/utils/categories.ts:9-25`) 15개 ID 기준. 사용자 정의 분야는 DEFAULT_STAGE5.
 *
 * Stage 5 emoji 디자인 원칙: 각 분야 정체성을 살리면서 "만개" 트로피 느낌. label emoji
 * (예: "🎯 채용")의 첫 emoji 또는 의미적으로 enriched emoji 선택. 정확한 emoji는
 * plan T2 시뮬 단계 + Codex 사전 review 카피 톤 검증으로 OS 호환(iOS/Android/Win) 확인 후 확정.
 */
const STAGE5_BY_INTEREST: Record<string, string> = {
  // HR (6)
  recruiting: '🏆',     // 🎯 채용 → 트로피 (도달감)
  onboarding: '🚀',     // 🚀 온보딩 (label 동일)
  culture: '🏛️',        // 🏢 조직문화 → 견고한 조직
  hr_system: '📜',      // 📋 인사제도 → 문서 정합
  labor_law: '⚖️',      // ⚖️ 노무/법률 (label 동일)
  leadership: '👑',     // 👑 리더십 (label 동일)
  // Tech (3)
  pm: '🛠️',             // 📱 프로덕트 → 도구 마스터
  ai_ml: '🤖',          // 🤖 AI/ML (label 동일)
  data: '📈',           // 📊 데이터분석 → 성장 차트
  // Biz (2)
  startup: '🦄',        // 🦄 스타트업 (label 동일)
  marketing: '📣',      // 📣 마케팅 (label 동일)
  // General (4)
  productivity: '⚡',   // ⚡ 생산성 (label 동일)
  career: '🎯',         // 🎯 커리어 (label 동일)
  communication: '💬',  // 💬 커뮤니케이션 (label 동일)
  self_dev: '🌳',       // 🌱 자기계발 → 성장의 끝 (stage 1 🌱과 차별화)
};

const DEFAULT_STAGE5 = '🌸';  // catalog 외 분야 (사용자 정의) fallback

/** Stage 5 도달 시 영구 ✨ 마크 (모든 분야 공통, 분야별 emoji와 별도 overlay) */
export const TROPHY_MARK = '✨';

/**
 * Stage 1~5 중 분야별 emoji 반환.
 * Stage 1~4는 분야와 무관 공통, Stage 5만 분야별 (catalog 외는 default).
 */
export function getPlantIcon(interestId: string, stage: PlantStage): string {
  if (stage === 5) {
    return STAGE5_BY_INTEREST[interestId] ?? DEFAULT_STAGE5;
  }
  return COMMON_STAGE_EMOJI[stage];
}

/** Stage 라벨 (UI 카드 텍스트). i18n MSG와 별도 — 본 catalog는 영문 안 함. */
export const STAGE_LABEL: Record<PlantStage, string> = {
  1: '씨앗',
  2: '새싹',
  3: '줄기',
  4: '봉오리',
  5: '만개',
};

/**
 * cumulativeActivity 누적 → 도달 stage 매핑.
 * spec §3.1 placeholder. T2 §Threshold Sim 결과로 확정 (±50% 가능).
 *
 * 곡선: exponential ratio 보존 (8 → 25 → 70 → 160 = 약 3x ramp).
 */
export const STAGE_THRESHOLDS: readonly [number, number, number, number] = [8, 25, 70, 160];

/**
 * cumulativeActivity 누적 카운트 → stage derive.
 * 0~7 → 1, 8~24 → 2, 25~69 → 3, 70~159 → 4, 160+ → 5.
 */
export function thresholdLookup(cumulativeActivity: number): PlantStage {
  const c = Math.max(0, Math.floor(cumulativeActivity));
  if (c >= STAGE_THRESHOLDS[3]) return 5;
  if (c >= STAGE_THRESHOLDS[2]) return 4;
  if (c >= STAGE_THRESHOLDS[1]) return 3;
  if (c >= STAGE_THRESHOLDS[0]) return 2;
  return 1;
}
```

- [ ] **Step 3: Failing test 작성**

```ts
// tests/unit/state/plantCatalog.spec.ts
import { describe, it, expect } from 'vitest';
import { getPlantIcon, thresholdLookup, STAGE_LABEL, STAGE_THRESHOLDS } from '../../../src/state/plantCatalog';

describe('plantCatalog', () => {
  describe('getPlantIcon', () => {
    it('stage 1~4 = 분야 공통 emoji', () => {
      expect(getPlantIcon('recruiting', 1)).toBe('🌱');
      expect(getPlantIcon('ai_ml', 1)).toBe('🌱');
      expect(getPlantIcon('recruiting', 4)).toBe('🌳');
      expect(getPlantIcon('ai_ml', 4)).toBe('🌳');
    });

    it('stage 5 = 분야별 차별화 (실제 INTERESTS catalog ID 기준)', () => {
      expect(getPlantIcon('recruiting', 5)).toBe('🏆');
      expect(getPlantIcon('ai_ml', 5)).toBe('🤖');
      expect(getPlantIcon('startup', 5)).toBe('🦄');
      expect(getPlantIcon('self_dev', 5)).toBe('🌳');
    });

    it('stage 5 + 사용자 정의 분야 → default fallback', () => {
      expect(getPlantIcon('my_custom_topic', 5)).toBe('🌸');
    });

    it('Codex S9 fix 회귀: 가상 ID(ai/design/economy/trend/practice/growth) 호출은 fallback', () => {
      // 옛 plan v1의 가상 ID는 catalog 외 → DEFAULT_STAGE5
      expect(getPlantIcon('ai', 5)).toBe('🌸');
      expect(getPlantIcon('design', 5)).toBe('🌸');
    });
  });

  describe('thresholdLookup', () => {
    it('boundary case', () => {
      expect(thresholdLookup(0)).toBe(1);
      expect(thresholdLookup(7)).toBe(1);
      expect(thresholdLookup(8)).toBe(2);
      expect(thresholdLookup(24)).toBe(2);
      expect(thresholdLookup(25)).toBe(3);
      expect(thresholdLookup(69)).toBe(3);
      expect(thresholdLookup(70)).toBe(4);
      expect(thresholdLookup(159)).toBe(4);
      expect(thresholdLookup(160)).toBe(5);
      expect(thresholdLookup(99999)).toBe(5);
    });

    it('NaN/negative/fractional → 1 (defensive)', () => {
      expect(thresholdLookup(-5)).toBe(1);
      expect(thresholdLookup(7.9)).toBe(1);
      expect(thresholdLookup(NaN)).toBe(1);
    });
  });

  describe('STAGE_LABEL', () => {
    it('5 단계 모두 한글 라벨', () => {
      expect(STAGE_LABEL[1]).toBe('씨앗');
      expect(STAGE_LABEL[5]).toBe('만개');
    });
  });

  describe('STAGE_THRESHOLDS (placeholder, T2 시뮬 결과 반영)', () => {
    it('exponential ratio 보존 (~3x ramp)', () => {
      expect(STAGE_THRESHOLDS).toHaveLength(4);
      // 시뮬 후 실제 값으로 갱신 시 본 spec assert는 그대로 유지 (구조만 검증)
      expect(STAGE_THRESHOLDS[0]).toBeGreaterThan(0);
      for (let i = 1; i < STAGE_THRESHOLDS.length; i++) {
        expect(STAGE_THRESHOLDS[i]).toBeGreaterThan(STAGE_THRESHOLDS[i - 1]!);
      }
    });
  });
});
```

- [ ] **Step 4: PASS 확인**

```bash
npx vitest run tests/unit/state/plantCatalog.spec.ts
```

Expected: 9 PASS (또는 시뮬 후 추가 spec).

- [ ] **Step 5: Commit**

```bash
git add src/state/plantCatalog.ts tests/unit/state/plantCatalog.spec.ts
git commit -m "feat(v3.15 T2): plantCatalog — stage emoji + threshold lookup (placeholder, T2 시뮬 결과)"
```

(시뮬 결과 spec 절대값 변경 시 별도 commit "docs(v3.15 T2 sim): spec §3.1 retro sync — threshold {old}→{new}")

---

## Task 3: plantEngine — tickPlantActivity + applyMissionBonus + archive/restore

**Files:**
- Create: `src/state/plantEngine.ts`
- Test: `tests/unit/state/plantEngine.spec.ts`
- Test: `tests/unit/state/archive-restore.spec.ts`

- [ ] **Step 1: plantEngine.ts 생성 (tick + bonus)**

```ts
// src/state/plantEngine.ts
import type { User } from './user';
import type { PlantState, PlantStage } from './plantTypes';
import { thresholdLookup } from './plantCatalog';

/**
 * 분야 1회 활동 기록 — cumulativeActivity 증가 + stage 재계산 + lastEngagedAt 갱신.
 *
 * **In-memory mutation only**. saveUser는 caller 책임 (atomic single-write 원칙, v3.10/v3.13.1 graduated).
 * 이벤트 emit은 caller path에서 takeSnapshot/runSweep diff로 자동 처리 (achievements.ts plant-stage-up).
 *
 * @param user - User (mutate)
 * @param interestId - 분야 ID (user.interests 멤버 가정. archived 분야도 호출 가능 — entry 유지)
 * @param delta - 보통 +1, 스크랩+메모 같이 발생하면 +2
 */
export function tickPlantActivity(user: User, interestId: string, delta: number): void {
  if (!Number.isFinite(delta) || delta <= 0) return;
  const today = new Date().toISOString();

  let plant = user.plantStateByInterest[interestId];
  if (!plant) {
    plant = { stage: 1, cumulativeActivity: 0 };
    user.plantStateByInterest[interestId] = plant;
  }

  const newCum = (Number.isFinite(plant.cumulativeActivity) ? plant.cumulativeActivity : 0) + delta;
  const newStage = thresholdLookup(newCum);

  plant.cumulativeActivity = newCum;
  plant.lastEngagedAt = today;
  if (newStage > plant.stage) {
    plant.stage = newStage;
    if (newStage === 5 && !plant.unlockedAt) {
      plant.unlockedAt = today;  // 영구 ✨ 마크 (한 번만 셋, idempotent)
    }
  }
}

/**
 * 미션 1개 완수 시 모든 식물에 동시 보너스 적용 (v3.13 anchor 이행).
 *
 * **In-memory mutation only**. saveUser는 caller (tickMissionProgress) 책임.
 * spec §3.2 placeholder 보너스: daily +1, weekly +5, monthly +20 (T2 §Mission Bonus Sim 후 ±50% 가능).
 *
 * archived 식물 (user.interests 외) 도 entry 있으면 update — 분야 재추가 시 누적 보존 (Q8-B).
 */
export function applyMissionBonus(user: User, period: 'daily' | 'weekly' | 'monthly'): void {
  const BONUS: Record<typeof period, number> = { daily: 1, weekly: 5, monthly: 20 };
  const bonus = BONUS[period];
  for (const interestId of Object.keys(user.plantStateByInterest)) {
    tickPlantActivity(user, interestId, bonus);
  }
}

/**
 * S10 fix (Codex P1-4): 분야 추가 시 plant entry 자동 생성 — interests modal save 시 호출.
 *
 * user.interests에 있는 ID 중 plantStateByInterest에 entry 없는 것에 대해 stage 1 식물 생성.
 * 기존 entry는 유지 (archive/restore 정책 — Q8-B). 본 함수는 idempotent.
 *
 * **In-memory mutation only**. saveUser는 caller (interests modal save handler) 책임.
 */
export function ensurePlantsForInterests(user: User): void {
  const today = new Date().toISOString();
  for (const interestId of user.interests) {
    if (!user.plantStateByInterest[interestId]) {
      user.plantStateByInterest[interestId] = {
        stage: 1,
        cumulativeActivity: 0,
        lastEngagedAt: today,  // 첫 인상 wilting 회피 (C3 패턴 follow)
      };
    }
  }
}
```

- [ ] **Step 2: Failing test 작성 — tickPlantActivity**

```ts
// tests/unit/state/plantEngine.spec.ts
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { tickPlantActivity, applyMissionBonus, ensurePlantsForInterests } from '../../../src/state/plantEngine';
import type { User } from '../../../src/state/user';

function makeUser(): User {
  return {
    name: 'Hayden', interests: ['ai', 'design'], onboardedAt: '2026-04-01',
    streak: 0, lastActiveDate: '2026-05-03', xp: 0,
    earnedBadges: {}, gamificationMigrated: true,
    schemaVersion: 4,
    missions: { active: [], cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 }, lastDailySeed: '', currentWeekIso: '', currentMonthIso: '' },
    plantStateByInterest: {},
    gardenIntroduced: false,
    gardenBackfilled: false,
  };
}

describe('tickPlantActivity', () => {
  it('첫 호출 시 식물 생성 (stage 1, cum 1)', () => {
    const u = makeUser();
    tickPlantActivity(u, 'ai', 1);
    expect(u.plantStateByInterest['ai']?.stage).toBe(1);
    expect(u.plantStateByInterest['ai']?.cumulativeActivity).toBe(1);
    expect(u.plantStateByInterest['ai']?.lastEngagedAt).toBeDefined();
  });

  it('stage 1→2 전환 (cum 7 → 8)', () => {
    const u = makeUser();
    u.plantStateByInterest['ai'] = { stage: 1, cumulativeActivity: 7 };
    tickPlantActivity(u, 'ai', 1);
    expect(u.plantStateByInterest['ai']?.stage).toBe(2);
    expect(u.plantStateByInterest['ai']?.cumulativeActivity).toBe(8);
  });

  it('stage 4→5 전환 시 unlockedAt 셋 (영구 마크)', () => {
    const u = makeUser();
    u.plantStateByInterest['ai'] = { stage: 4, cumulativeActivity: 159 };
    tickPlantActivity(u, 'ai', 1);
    expect(u.plantStateByInterest['ai']?.stage).toBe(5);
    expect(u.plantStateByInterest['ai']?.unlockedAt).toBeDefined();
  });

  it('stage 5 도달 후 추가 활동 — unlockedAt 변경 X (idempotent)', () => {
    const u = makeUser();
    const fixedDate = '2026-05-01T00:00:00.000Z';
    u.plantStateByInterest['ai'] = { stage: 5, cumulativeActivity: 200, unlockedAt: fixedDate };
    tickPlantActivity(u, 'ai', 5);
    expect(u.plantStateByInterest['ai']?.unlockedAt).toBe(fixedDate);
    expect(u.plantStateByInterest['ai']?.cumulativeActivity).toBe(205);
  });

  it('delta 0 / negative / NaN 무시 (defensive)', () => {
    const u = makeUser();
    u.plantStateByInterest['ai'] = { stage: 2, cumulativeActivity: 10 };
    tickPlantActivity(u, 'ai', 0);
    tickPlantActivity(u, 'ai', -3);
    tickPlantActivity(u, 'ai', NaN);
    expect(u.plantStateByInterest['ai']?.cumulativeActivity).toBe(10);
  });

  it('cumulativeActivity NaN/Infinite 자체 손상 → 0으로 시작 (defensive)', () => {
    const u = makeUser();
    u.plantStateByInterest['ai'] = { stage: 2, cumulativeActivity: NaN as unknown as number };
    tickPlantActivity(u, 'ai', 5);
    expect(u.plantStateByInterest['ai']?.cumulativeActivity).toBe(5);
  });
});

describe('applyMissionBonus', () => {
  it('daily 완수 = 모든 식물에 +1', () => {
    const u = makeUser();
    u.plantStateByInterest['ai'] = { stage: 1, cumulativeActivity: 5 };
    u.plantStateByInterest['design'] = { stage: 1, cumulativeActivity: 3 };
    applyMissionBonus(u, 'daily');
    expect(u.plantStateByInterest['ai']?.cumulativeActivity).toBe(6);
    expect(u.plantStateByInterest['design']?.cumulativeActivity).toBe(4);
  });

  it('monthly 완수 = 모든 식물에 +20', () => {
    const u = makeUser();
    u.plantStateByInterest['ai'] = { stage: 2, cumulativeActivity: 50 };  // 50 + 20 = 70 → stage 4
    applyMissionBonus(u, 'monthly');
    expect(u.plantStateByInterest['ai']?.cumulativeActivity).toBe(70);
    expect(u.plantStateByInterest['ai']?.stage).toBe(4);
  });

  it('plantStateByInterest 빈 객체 — 호출 안전 (no-op)', () => {
    const u = makeUser();
    expect(() => applyMissionBonus(u, 'weekly')).not.toThrow();
    expect(Object.keys(u.plantStateByInterest)).toHaveLength(0);
  });

  it('archived 식물(interests 외 entry) 도 보너스 적용 — 분야 재추가 시 누적 보존', () => {
    const u = makeUser();
    u.interests = ['ai_ml'];  // recruiting 제거됐지만
    u.plantStateByInterest['ai_ml'] = { stage: 1, cumulativeActivity: 5 };
    u.plantStateByInterest['recruiting'] = { stage: 1, cumulativeActivity: 5 };  // archived
    applyMissionBonus(u, 'daily');
    expect(u.plantStateByInterest['recruiting']?.cumulativeActivity).toBe(6);  // 누적 update
  });
});

describe('ensurePlantsForInterests (S10 / P1-4 fix)', () => {
  it('user.interests 새 ID에 stage 1 entry 자동 생성', () => {
    const u = makeUser();
    u.interests = ['recruiting', 'ai_ml'];
    ensurePlantsForInterests(u);
    expect(u.plantStateByInterest['recruiting']?.stage).toBe(1);
    expect(u.plantStateByInterest['ai_ml']?.stage).toBe(1);
    expect(u.plantStateByInterest['recruiting']?.cumulativeActivity).toBe(0);
  });

  it('기존 entry는 보존 (idempotent)', () => {
    const u = makeUser();
    u.interests = ['recruiting'];
    u.plantStateByInterest['recruiting'] = { stage: 4, cumulativeActivity: 100 };
    ensurePlantsForInterests(u);
    expect(u.plantStateByInterest['recruiting']?.stage).toBe(4);
    expect(u.plantStateByInterest['recruiting']?.cumulativeActivity).toBe(100);
  });

  it('archived entry (interests 외) 는 건드리지 않음', () => {
    const u = makeUser();
    u.interests = ['recruiting'];  // ai_ml 제거됨
    u.plantStateByInterest['ai_ml'] = { stage: 3, cumulativeActivity: 30 };
    ensurePlantsForInterests(u);
    expect(u.plantStateByInterest['ai_ml']?.stage).toBe(3);  // 변동 없음
    expect(u.plantStateByInterest['recruiting']?.stage).toBe(1);  // 신규 생성
  });

  it('interests 빈 배열 → no-op', () => {
    const u = makeUser();
    u.interests = [];
    ensurePlantsForInterests(u);
    expect(Object.keys(u.plantStateByInterest)).toHaveLength(0);
  });

  it('lastEngagedAt = 진입 시점 (C3 패턴 follow, 첫 인상 wilting 회피)', () => {
    const u = makeUser();
    u.interests = ['recruiting'];
    const before = Date.now();
    ensurePlantsForInterests(u);
    const after = Date.now();
    const last = new Date(u.plantStateByInterest['recruiting']!.lastEngagedAt!).getTime();
    expect(last).toBeGreaterThanOrEqual(before);
    expect(last).toBeLessThanOrEqual(after);
  });
});
```

- [ ] **Step 3: PASS 확인**

```bash
npx vitest run tests/unit/state/plantEngine.spec.ts
```

Expected: 10 PASS.

- [ ] **Step 4: archive-restore.spec.ts 추가 — 분야 add/remove/add 시퀀스**

```ts
// tests/unit/state/archive-restore.spec.ts
import { describe, it, expect } from 'vitest';
import { tickPlantActivity } from '../../../src/state/plantEngine';
import type { User } from '../../../src/state/user';

function makeUserWithPlant(): User {
  return {
    name: 'h', interests: ['ai'], onboardedAt: '', streak: 0, lastActiveDate: '',
    xp: 0, earnedBadges: {}, gamificationMigrated: true, schemaVersion: 4,
    missions: { active: [], cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 }, lastDailySeed: '', currentWeekIso: '', currentMonthIso: '' },
    plantStateByInterest: { ai: { stage: 3, cumulativeActivity: 30 } },
    gardenIntroduced: false, gardenBackfilled: false,
  };
}

describe('archive/restore (Q8-B 정책)', () => {
  it('분야 제거 시 plantStateByInterest entry 그대로 유지', () => {
    const u = makeUserWithPlant();
    u.interests = [];  // ai 제거 (settings 등에서)
    expect(u.plantStateByInterest['ai']?.stage).toBe(3);
    expect(u.plantStateByInterest['ai']?.cumulativeActivity).toBe(30);
  });

  it('분야 재추가 시 기존 entry 자동 복원 (별도 restore 함수 불필요)', () => {
    const u = makeUserWithPlant();
    u.interests = [];
    u.interests.push('ai');  // 재추가
    // tickPlantActivity 다시 호출 시 기존 entry 그대로 사용
    tickPlantActivity(u, 'ai', 5);
    expect(u.plantStateByInterest['ai']?.stage).toBe(3);  // 유지
    expect(u.plantStateByInterest['ai']?.cumulativeActivity).toBe(35);  // 누적
  });

  it('분야 add/remove/add 시퀀스 idempotent (visibility만 토글)', () => {
    const u = makeUserWithPlant();
    u.interests = [];                  // remove
    u.interests = ['ai'];              // add 1
    u.interests = [];                  // remove
    u.interests = ['ai'];              // add 2
    expect(u.plantStateByInterest['ai']?.stage).toBe(3);
    expect(u.plantStateByInterest['ai']?.cumulativeActivity).toBe(30);
  });
});
```

- [ ] **Step 5: PASS 확인**

```bash
npx vitest run tests/unit/state/archive-restore.spec.ts
```

Expected: 3 PASS.

- [ ] **Step 6: 전체 vitest 회귀**

```bash
npx tsc --noEmit && npm test
```

Expected: 0 ts error, 신규 +13.

- [ ] **Step 7: Commit**

```bash
git add src/state/plantEngine.ts tests/unit/state/plantEngine.spec.ts tests/unit/state/archive-restore.spec.ts
git commit -m "feat(v3.15 T3): plantEngine — tickPlantActivity + applyMissionBonus + archive/restore"
```

---

## Task 4: plantEngine — checkWilting (read-only)

**Files:**
- Modify: `src/state/plantEngine.ts` (checkWilting 추가)
- Modify: `tests/unit/state/plantEngine.spec.ts` (wilting describe block 추가)

- [ ] **Step 1: checkWilting 추가 (plantEngine.ts 끝부분)**

```ts
// src/state/plantEngine.ts 끝부분에 추가

const WILTING_DAYS_THRESHOLD = 7;  // spec §3.3 / §7-3 placeholder

/**
 * 식물이 시들었는지 (visual cue) 판단 — read-only, User mutation X.
 *
 * lastEngagedAt 7일+ 이전이면 true. 활동 1회 시 lastEngagedAt 갱신되어 즉시 vivid 복귀.
 * stage는 절대 변하지 않음 (Q7-B 합의).
 *
 * @param plant - PlantState
 * @param now - 현재 시각 (테스트 시 fixed clock 주입 가능, default new Date())
 */
export function checkWilting(plant: { lastEngagedAt?: string }, now: Date = new Date()): boolean {
  if (!plant.lastEngagedAt) return false;  // 새로 생긴 식물은 wilting 아님
  const last = new Date(plant.lastEngagedAt).getTime();
  if (!Number.isFinite(last)) return false;  // 손상 ISO → 안전 false
  const diffMs = now.getTime() - last;
  const diffDays = diffMs / (24 * 60 * 60 * 1000);
  return diffDays >= WILTING_DAYS_THRESHOLD;
}
```

- [ ] **Step 2: Failing test (plantEngine.spec.ts에 describe 추가)**

```ts
// tests/unit/state/plantEngine.spec.ts 끝부분에 추가
import { checkWilting } from '../../../src/state/plantEngine';

describe('checkWilting', () => {
  const fixedNow = new Date('2026-05-15T00:00:00.000Z');

  it('lastEngagedAt 7일 이상 이전 → true', () => {
    const plant = { lastEngagedAt: '2026-05-08T00:00:00.000Z' };  // 7일 전
    expect(checkWilting(plant, fixedNow)).toBe(true);
  });

  it('lastEngagedAt 6일 이전 → false (boundary)', () => {
    const plant = { lastEngagedAt: '2026-05-09T00:00:00.000Z' };  // 6일 전
    expect(checkWilting(plant, fixedNow)).toBe(false);
  });

  it('lastEngagedAt 8일 이전 → true', () => {
    const plant = { lastEngagedAt: '2026-05-07T00:00:00.000Z' };
    expect(checkWilting(plant, fixedNow)).toBe(true);
  });

  it('lastEngagedAt undefined → false (새 식물)', () => {
    expect(checkWilting({}, fixedNow)).toBe(false);
  });

  it('lastEngagedAt 손상 ISO → false (defensive)', () => {
    expect(checkWilting({ lastEngagedAt: 'not-a-date' }, fixedNow)).toBe(false);
  });

  it('default now (테스트에서 fixed clock 미주입) — wilting 판단 가능', () => {
    const futureLast = new Date(Date.now() + 1000).toISOString();
    expect(checkWilting({ lastEngagedAt: futureLast })).toBe(false);  // 미래
  });

  it('Read-only — plant 객체 변경 안 됨', () => {
    const plant = { lastEngagedAt: '2026-05-08T00:00:00.000Z', stage: 3 as const };
    const before = JSON.stringify(plant);
    checkWilting(plant, fixedNow);
    expect(JSON.stringify(plant)).toBe(before);
  });
});
```

- [ ] **Step 3: PASS 확인**

```bash
npx vitest run tests/unit/state/plantEngine.spec.ts
```

Expected: 17 PASS (10 + 7).

- [ ] **Step 4: Commit**

```bash
git add src/state/plantEngine.ts tests/unit/state/plantEngine.spec.ts
git commit -m "feat(v3.15 T4): plantEngine.checkWilting — read-only 7-day threshold"
```

---

## Task 5: takeSnapshot + detectEvents 확장 (plant-stage-up event)

**Files:**
- Modify: `src/state/gameTypes.ts` (GameEvent에 plant-stage-up variant 추가)
- Modify: `src/state/achievements.ts` (Snapshot.plantStages, detectEvents case)
- Test: `tests/unit/state/events-plant.spec.ts`

- [ ] **Step 1: gameTypes.ts에 plant-stage-up 추가 (S6 / P0-2 fix — `kind` discriminant)**

기존 GameEvent (gameTypes.ts:24-29):
```ts
export type GameEvent =
  | { kind: 'xp-gained'; amount: number; at: number }
  | { kind: 'level-up'; tierId: number; at: number }
  | { kind: 'streak-milestone'; days: number; at: number }
  | { kind: 'badge'; badgeId: string; at: number }
  | { kind: 'mission-complete'; defId: string; period: MissionPeriod; rewardXp: number; at: number };
```

variant 추가:
```ts
// src/state/gameTypes.ts:29 마지막 줄에 추가 (기존 union의 마지막 |로 연결)
  | { kind: 'plant-stage-up'; interestId: string; newStage: 1 | 2 | 3 | 4 | 5; at: number };
```

Snapshot 도 plantStages 필드 추가:
```ts
// src/state/gameTypes.ts — Snapshot interface 안 (기존 필드들 끝부분)
export interface Snapshot {
  // 기존 필드들...
  plantStages: Record<string, number>;  // interestId → stage (entry 미생성 시 key 부재)
}
```

- [ ] **Step 2: takeSnapshot 확장 (achievements.ts:49-87)**

```ts
// achievements.ts:49-87 takeSnapshot 끝부분에 plantStages 추가
return {
  // ... 기존 필드
  missionsActive: ...,
  missionsCumulative: ...,
  plantStages: Object.fromEntries(
    Object.entries(u?.plantStateByInterest ?? {}).map(([id, p]) => [id, p.stage])
  ),
};
```

- [ ] **Step 3: detectEvents 확장 (achievements.ts detectEvents 함수 끝부분)**

```ts
// detectEvents 함수 안 — return 직전에 추가
const now = Date.now();
for (const [id, currStage] of Object.entries(curr.plantStages)) {
  // S1 self-review fix: prev에 entry 없음 = transition 0으로 처리
  // (분야 추가 직후 / backfill 직후 false plant-stage-up emit 방지)
  const prevStage = prev.plantStages[id] ?? currStage;
  if (prevStage < currStage) {
    // S6 fix (P0-2): `kind` discriminant + at: number 일관성 (다른 GameEvent variant와 동일)
    out.push({
      kind: 'plant-stage-up',
      interestId: id,
      newStage: currStage as 1 | 2 | 3 | 4 | 5,
      at: now,
    });
  }
}
```

추가로 `emitEvents` switch에 case 추가 (S6 fix — `dg:reward:*` 채널):

```ts
// achievements.ts:155-177 emitEvents switch 안에 case 추가
case 'plant-stage-up':
  dispatch('dg:reward:plant-stage-up', {
    interestId: e.interestId,
    newStage: e.newStage,
    at: e.at,
  });
  break;
```

`assertNever(e)` default가 컴파일 에러 안 나도록 위 case 필수.

- [ ] **Step 4: Failing test 작성**

```ts
// tests/unit/state/events-plant.spec.ts
import { describe, it, expect } from 'vitest';
import { detectEvents } from '../../../src/state/achievements';
import type { Snapshot } from '../../../src/state/gameTypes';

function makeSnap(plantStages: Record<string, number> = {}): Snapshot {
  return {
    xp: 0, streak: 0, answersCount: 0, scrapsCount: 0, memosCount: 0,
    uniqueAnsweredTypes: new Set(), selectedInterests: new Set(),
    engagedInterests: new Set(), uniqueScrapCategories: 0,
    earnedBadgeIds: new Set(), missionsActive: [],
    missionsCumulative: { daily: 0, weekly: 0, monthly: 0 },
    plantStages,
  };
}

describe('detectEvents — plant-stage-up (S6 fix: kind discriminant)', () => {
  it('stage 1→2 전환 → plant-stage-up emit', () => {
    const prev = makeSnap({ recruiting: 1 });
    const curr = makeSnap({ recruiting: 2 });
    const events = detectEvents(prev, curr);
    expect(events).toContainEqual(expect.objectContaining({
      kind: 'plant-stage-up', interestId: 'recruiting', newStage: 2,
    }));
  });

  it('stage 4→5 전환 → plant-stage-up newStage 5', () => {
    const prev = makeSnap({ ai_ml: 4 });
    const curr = makeSnap({ ai_ml: 5 });
    const events = detectEvents(prev, curr);
    expect(events.find(e => e.kind === 'plant-stage-up')).toEqual(expect.objectContaining({
      kind: 'plant-stage-up', interestId: 'ai_ml', newStage: 5,
    }));
  });

  it('S1 fix: prev에 entry 없음 (분야 추가 직후) → plant-stage-up emit 안 함', () => {
    const prev = makeSnap({});
    const curr = makeSnap({ recruiting: 3 });  // 새 식물 stage 3 (backfill 등)
    const events = detectEvents(prev, curr);
    expect(events.filter(e => e.kind === 'plant-stage-up')).toHaveLength(0);
  });

  it('S1 fix: prev에 entry 없음 + curr stage 5 (backfill 즉시 max) → emit 안 함', () => {
    const prev = makeSnap({});
    const curr = makeSnap({ ai_ml: 5 });
    const events = detectEvents(prev, curr);
    expect(events.filter(e => e.kind === 'plant-stage-up')).toHaveLength(0);
  });

  it('stage 동일 → emit 안 함', () => {
    const prev = makeSnap({ recruiting: 3 });
    const curr = makeSnap({ recruiting: 3 });
    const events = detectEvents(prev, curr);
    expect(events.filter(e => e.kind === 'plant-stage-up')).toHaveLength(0);
  });

  it('stage 감소 (불가능 케이스, 데이터 손상) → emit 안 함 (S11 accepted tradeoff)', () => {
    const prev = makeSnap({ recruiting: 4 });
    const curr = makeSnap({ recruiting: 2 });
    const events = detectEvents(prev, curr);
    expect(events.filter(e => e.kind === 'plant-stage-up')).toHaveLength(0);
  });

  it('여러 식물 동시 전환 (미션 보너스) → 각각 emit', () => {
    const prev = makeSnap({ recruiting: 1, ai_ml: 2, self_dev: 3 });
    const curr = makeSnap({ recruiting: 2, ai_ml: 3, self_dev: 3 });  // recruiting/ai_ml 전환, self_dev 유지
    const events = detectEvents(prev, curr).filter(e => e.kind === 'plant-stage-up');
    expect(events).toHaveLength(2);
  });

  it('emit된 event는 at: number 필드 포함 (다른 events와 일관)', () => {
    const prev = makeSnap({ recruiting: 1 });
    const curr = makeSnap({ recruiting: 2 });
    const events = detectEvents(prev, curr);
    const e = events.find(ev => ev.kind === 'plant-stage-up');
    expect(typeof (e as any)?.at).toBe('number');
  });
});
```

- [ ] **Step 5: takeSnapshot에 plantStages 포함 검증**

```ts
// tests/unit/state/events-plant.spec.ts 끝에 추가
import { takeSnapshot } from '../../../src/state/achievements';

describe('takeSnapshot.plantStages', () => {
  beforeEach(() => {
    localStorage.clear();
  });

  it('user.plantStateByInterest → Snapshot.plantStages 매핑', () => {
    localStorage.setItem('user', JSON.stringify({
      name: 'h', interests: ['ai'], onboardedAt: '', streak: 0,
      lastActiveDate: '2026-05-03', xp: 0, earnedBadges: {},
      gamificationMigrated: true, schemaVersion: 4,
      missions: { active: [], cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 }, lastDailySeed: '', currentWeekIso: '', currentMonthIso: '' },
      plantStateByInterest: { ai: { stage: 3, cumulativeActivity: 30 } },
      gardenIntroduced: false, gardenBackfilled: true,
    }));
    const snap = takeSnapshot();
    expect(snap.plantStages).toEqual({ ai: 3 });
  });

  it('user 없음 → 빈 plantStages', () => {
    const snap = takeSnapshot();
    expect(snap.plantStages).toEqual({});
  });
});
```

- [ ] **Step 6: PASS 확인**

```bash
npx vitest run tests/unit/state/events-plant.spec.ts
```

Expected: 9 PASS.

- [ ] **Step 7: 전체 vitest 회귀**

```bash
npx tsc --noEmit && npm test
```

Expected: 0 ts error, 모든 기존 spec PASS.

- [ ] **Step 8: Commit**

```bash
git add src/state/gameTypes.ts src/state/achievements.ts tests/unit/state/events-plant.spec.ts
git commit -m "feat(v3.15 T5): takeSnapshot.plantStages + detectEvents plant-stage-up (S1 fix)"
```

---

## Task 6: missionEngine integration — applyMissionBonus hook

**Files:**
- Modify: `src/state/missionEngine.ts` (tickMissionProgress 미션 완수 직후 호출)
- Test: `tests/unit/state/missionEngine-plant-bonus.spec.ts`

- [ ] **Step 1: missionEngine.ts:159-200 영역 확인**

```bash
grep -n "cumulative.dailyCount++\|cumulative.weeklyCount++\|cumulative.monthlyCount++" src/state/missionEngine.ts
```

기존 v3.13 cumulative count++ 시점 (mission completed 전환) 직후가 hook 위치.

- [ ] **Step 2: applyMissionBonus 호출 추가**

```ts
// missionEngine.ts 상단 import
import { applyMissionBonus } from './plantEngine';

// missionEngine.ts:185-188 (또는 cumulative.{period}Count++ switch case)
case 'daily':
  user.missions.cumulative.dailyCount++;
  applyMissionBonus(user, 'daily');    // v3.15 T6: anchor 이행
  break;
case 'weekly':
  user.missions.cumulative.weeklyCount++;
  applyMissionBonus(user, 'weekly');
  break;
case 'monthly':
  user.missions.cumulative.monthlyCount++;
  applyMissionBonus(user, 'monthly');
  break;
```

- [ ] **Step 3: Failing test 작성**

```ts
// tests/unit/state/missionEngine-plant-bonus.spec.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { tickMissionProgress } from '../../../src/state/missionEngine';
import type { User } from '../../../src/state/user';

function userWith(opts: { missionsActive: any[]; plants: Record<string, { stage: 1|2|3|4|5; cumulativeActivity: number }> }): User {
  return {
    name: 'h', interests: Object.keys(opts.plants), onboardedAt: '', streak: 0,
    lastActiveDate: '', xp: 0, earnedBadges: {}, gamificationMigrated: true, schemaVersion: 4,
    missions: { active: opts.missionsActive, cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 }, lastDailySeed: '', currentWeekIso: '', currentMonthIso: '' },
    plantStateByInterest: opts.plants,
    gardenIntroduced: false, gardenBackfilled: true,
  };
}

describe('missionEngine + plant bonus integration (T6)', () => {
  // 미션 완수 시뮬: tickMissionProgress 호출 후 모든 식물에 +1 (daily) 등
  // (정확한 mock 구조는 missionCatalog의 미션 def 참조)

  it('daily 미션 1개 완수 → 모든 식물 +1', () => {
    // 미션 1회 진척만 남은 케이스 simulating
    const u = userWith({
      missionsActive: [/* daily mission with progress = target - 1 */],
      plants: { ai: { stage: 1, cumulativeActivity: 5 }, design: { stage: 1, cumulativeActivity: 3 } },
    });
    // tickMissionProgress(u, 'answer', new Date());  // 상세는 catalog mock 참조
    // expect(u.plantStateByInterest['ai']?.cumulativeActivity).toBe(6);
    // expect(u.plantStateByInterest['design']?.cumulativeActivity).toBe(4);
    // ※ 실제 mock은 missionCatalog 패턴 참조해서 plan 단계에서 확정
  });

  it('weekly 완수 → 모든 식물 +5', () => { /* 동일 패턴 */ });
  it('monthly 완수 → 모든 식물 +20', () => { /* 동일 패턴 */ });

  it('미션 미완수 (progress++만) → plant 변동 0', () => {
    // tickMissionProgress가 미완수 상태로 끝났을 때 plant 변동 안 됨 검증
  });
});
```

(spec 5 spec 정도. mock 구조는 기존 missionEngine.spec 패턴 참조 후 확정)

- [ ] **Step 4: PASS 확인**

```bash
npx vitest run tests/unit/state/missionEngine-plant-bonus.spec.ts
```

Expected: 5 PASS.

- [ ] **Step 5: 회귀**

```bash
npx tsc --noEmit && npm test
```

Expected: 모든 기존 spec PASS (특히 missionEngine.spec / sweep-wiring.spec).

- [ ] **Step 6: Commit**

```bash
git add src/state/missionEngine.ts tests/unit/state/missionEngine-plant-bonus.spec.ts
git commit -m "feat(v3.15 T6): missionEngine — applyMissionBonus hook (v3.13 anchor 이행)"
```

---

## Task 7: briefings.ts integration — 스크랩 toggle → tickPlantActivity

**Files:**
- Modify: `src/state/briefings.ts` (scrap toggle 시점 hook)
- Modify: `src/utils/interestKeywords.ts` (이미 존재, 재사용만)
- Test: `tests/unit/state/briefings-plant-hook.spec.ts`

- [ ] **Step 1: briefings.ts에서 scrap toggle 함수 확인**

```bash
grep -n "scrapped\|toggleScrap\|setScrap" src/state/briefings.ts | head -10
```

scrap toggle을 mutation하는 함수 찾기. saveBriefings 직전 path가 hook 위치.

- [ ] **Step 2: in-memory helper 추가 (S7 / P1-1 fix — mutateWithSweep 통합)**

기존 plan v1의 별도 `getCachedUser`/`saveUser` 호출 패턴은 `mutateWithSweep`의 단일 saveUser 와 충돌하여 atomic 위반 + saveBriefings 실패 시 plant만 persist 위험. **In-memory only helper로 변경 — saveUser는 caller(mutateWithSweep) 단일 호출에 통합**.

```ts
// src/state/briefings.ts 끝부분에 추가
import { interestKeywords, matchKeyword } from '../utils/interestKeywords';
import { tickPlantActivity } from './plantEngine';
import type { User } from './user';

/**
 * briefing 1개에 매칭되는 모든 분야로 tickPlantActivity 호출 (in-memory only).
 *
 * **S7 fix (Codex P1-1)**: saveUser 호출하지 않음. caller (mutateWithSweep) 가 단일 saveUser
 * 시점에 모든 mutate 결과 persist. atomic single-write 원칙 (v3.10/v3.13.1 graduated).
 *
 * @param user - User (in-memory mutate)
 * @param briefing - 분야 매칭 대상
 * @param delta - 보통 +1 (스크랩 1회 또는 메모 1회 = 각각 별도 +1 호출)
 */
export function tickPlantsByBriefingInMemory(user: User, briefing: Briefing, delta: number): void {
  const hay = `${briefing.sourceTitle ?? ''} ${briefing.title} ${briefing.summary}`.toLowerCase();
  for (const interestId of user.interests) {
    if (interestKeywords(interestId).some(k => matchKeyword(hay, k))) {
      tickPlantActivity(user, interestId, delta);
    }
  }
}
```

- [ ] **Step 3: mutateWithSweep 안에 hook 통합 (briefings.ts:61-78)**

기존 mutateWithSweep는 fn(b) 후 saveBriefings → tickMissionProgress → saveUser 흐름. 그 사이에 plant tick을 in-memory로 끼워넣음:

```ts
// src/state/briefings.ts:61-78 mutateWithSweep 변경
function mutateWithSweep(index: number, fn: (b: Briefing) => void, action?: MissionAction): void {
  const list = loadBriefings();
  const u = getCachedUser();
  const now = new Date();
  if (u) getActiveMissions(now, u);  // lazy regen (in-memory)

  const b = list[index];
  if (!b) return;

  // T7 (S7 fix): scrap/memo 전환 감지 — pre-mutation snapshot
  const wasScrapped = b.scrapped;
  const memoWasEmpty = !b.memo || b.memo.trim().length === 0;

  fn(b);
  saveBriefings(list);  // throws on Quota — 이후 saveUser 안 함 (false-fire 방지)

  if (u) {
    // T7 (S7 fix): plant tick — in-memory only, mutateWithSweep의 단일 saveUser 활용
    if (action === 'scrap' && b.scrapped && !wasScrapped) {
      tickPlantsByBriefingInMemory(u, b, 1);  // false→true 전환만
    }
    if (action === 'memo' && memoWasEmpty && b.memo.trim().length > 0) {
      tickPlantsByBriefingInMemory(u, b, 1);  // 빈→non-빈 전환만
    }

    if (action) tickMissionProgress(u, action, now);
    try {
      saveUser(u);  // 단일 saveUser — plant + mission 동시 persist (atomic)
    } catch (err) {
      showToast(getSaveErrorMessage(err));
    }
  }
  // ... 기존 sweep (takeSnapshot before/after, runSweep)
}
```

**Invariant 검증**: saveBriefings throw → saveUser 안 함 → plant 변경도 persist 안 됨 (in-memory만). saveUser throw → toast emit, plant 변경 손실 (다음 진입 시 lazy regen 의존). 둘 다 atomic single-write 원칙 부합.

- [ ] **Step 4: Failing test 작성 (단위 + integration)**

```ts
// tests/unit/state/briefings-plant-hook.spec.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { tickPlantsByBriefingInMemory, toggleScrap, saveMemo, saveBriefings } from '../../../src/state/briefings';
import type { User } from '../../../src/state/user';

function makeUser(): User {
  return {
    name: 'h', interests: ['ai_ml', 'recruiting'], onboardedAt: '', streak: 0,
    lastActiveDate: '', xp: 0, earnedBadges: {}, gamificationMigrated: true, schemaVersion: 4,
    missions: { active: [], cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 }, lastDailySeed: '', currentWeekIso: '', currentMonthIso: '' },
    plantStateByInterest: {},
    gardenIntroduced: false, gardenBackfilled: true,
  };
}

describe('tickPlantsByBriefingInMemory (단위 — saveUser 호출 X)', () => {
  it('AI 키워드 매칭 briefing → ai_ml 식물에만 +1', () => {
    const u = makeUser();
    tickPlantsByBriefingInMemory(u, {
      id: '1', date: '2026-05-03', url: 'https://x.com/1',
      title: 'GPT-5 출시 — AI 모델 새 milestone',
      summary: '...', scrapped: true, read: true, memo: '',
      sourceTitle: '',
    }, 1);
    expect(u.plantStateByInterest.ai_ml?.cumulativeActivity).toBe(1);
    expect(u.plantStateByInterest.recruiting).toBeUndefined();
  });

  it('AI + recruiting 모두 매칭 briefing → 두 식물에 +1', () => {
    const u = makeUser();
    tickPlantsByBriefingInMemory(u, {
      id: '2', date: '2026-05-03', url: 'https://x.com/2',
      title: 'AI 채용 트렌드',
      summary: '...', scrapped: true, read: true, memo: '',
      sourceTitle: '',
    }, 1);
    expect(u.plantStateByInterest.ai_ml?.cumulativeActivity).toBe(1);
    expect(u.plantStateByInterest.recruiting?.cumulativeActivity).toBe(1);
  });

  it('아무 분야와도 매칭 안 되는 briefing → no-op', () => {
    const u = makeUser();
    tickPlantsByBriefingInMemory(u, {
      id: '3', date: '2026-05-03', url: 'https://x.com/3',
      title: '날씨 예보',
      summary: '맑음.', scrapped: true, read: true, memo: '',
      sourceTitle: '',
    }, 1);
    expect(Object.keys(u.plantStateByInterest)).toHaveLength(0);
  });
});

describe('mutateWithSweep integration (S7 fix — atomic single saveUser)', () => {
  beforeEach(() => {
    localStorage.clear();
    localStorage.setItem('user', JSON.stringify(makeUser()));
    saveBriefings([{
      id: '1', date: '2026-05-03', url: 'https://x.com/1',
      title: 'GPT-5 출시', summary: '', scrapped: false, read: false, memo: '',
      sourceTitle: '',
    }]);
  });

  it('toggleScrap (false→true) → ai_ml 식물 +1 + 단일 saveUser', () => {
    toggleScrap(0);
    const u = JSON.parse(localStorage.getItem('user')!);
    expect(u.plantStateByInterest.ai_ml?.cumulativeActivity).toBe(1);
  });

  it('toggleScrap (true→false) → 식물 변동 0 (un-scrap은 카운트 안 함)', () => {
    toggleScrap(0);  // false → true (+1)
    toggleScrap(0);  // true → false (no-op)
    const u = JSON.parse(localStorage.getItem('user')!);
    expect(u.plantStateByInterest.ai_ml?.cumulativeActivity).toBe(1);
  });

  it('saveMemo (빈→non-빈) → 식물 +1', () => {
    saveMemo(0, '메모 내용');
    const u = JSON.parse(localStorage.getItem('user')!);
    expect(u.plantStateByInterest.ai_ml?.cumulativeActivity).toBe(1);
  });

  it('saveMemo (non-빈 → 다른 non-빈) → 변동 0 (이미 카운트한 메모)', () => {
    saveMemo(0, '메모 1');  // 빈 → non-빈 (+1)
    saveMemo(0, '메모 2');  // non-빈 → non-빈 (no-op)
    const u = JSON.parse(localStorage.getItem('user')!);
    expect(u.plantStateByInterest.ai_ml?.cumulativeActivity).toBe(1);
  });

  it('스크랩 + 메모 동시 발생 → 각 +1 합쳐 +2', () => {
    toggleScrap(0);            // 스크랩 +1
    saveMemo(0, '메모');       // 메모 +1
    const u = JSON.parse(localStorage.getItem('user')!);
    expect(u.plantStateByInterest.ai_ml?.cumulativeActivity).toBe(2);
  });
});
```

- [ ] **Step 5: PASS 확인**

```bash
npx vitest run tests/unit/state/briefings-plant-hook.spec.ts
```

Expected: 4 PASS.

- [ ] **Step 6: Commit**

```bash
git add src/state/briefings.ts tests/unit/state/briefings-plant-hook.spec.ts
git commit -m "feat(v3.15 T7): briefings — scrap/memo toggle → tickPlantActivity hook"
```

---

## Task 8: backfillGarden (loadUserData path에서 자동, sweep 우회)

**Files:**
- Create: `src/state/backfillGarden.ts`
- Modify: `src/state/user.ts:44-69` (getCachedUser path에 backfill 자동 호출)
- Test: `tests/unit/state/backfill.spec.ts`

- [ ] **Step 1: backfillGarden.ts 생성**

```ts
// src/state/backfillGarden.ts
import { loadBriefings } from './briefings';
import { interestKeywords, matchKeyword } from '../utils/interestKeywords';
import { thresholdLookup } from './plantCatalog';
import type { User } from './user';

/**
 * 첫 v3.15 진입 시 1회 — 기존 briefings 활동 이력을 식물 stage로 reflection.
 *
 * **Trigger 시점** (S3 spec self-review fix): loadUserData path에서 migrateUserToV4 직후 자동 호출.
 * **Sweep 우회** (S2 spec self-review fix): 본 함수 + 호출자 모두 takeSnapshot/runSweep 호출하지 않음 —
 * stage 1→4 jump 시 plant-stage-up 다발 emit 방지. 통지는 환영 모달 highlight로만 (T13).
 *
 * In-memory mutation. saveUser는 caller 책임.
 */
export function backfillGarden(user: User): void {
  if (user.gardenBackfilled) return;  // idempotent

  const briefings = loadBriefings();
  const today = new Date().toISOString();

  for (const interestId of user.interests) {
    const matched = briefings.filter(b => {
      if (!b.scrapped) return false;
      const hay = `${b.sourceTitle ?? ''} ${b.title} ${b.summary}`.toLowerCase();
      return interestKeywords(interestId).some(k => matchKeyword(hay, k));
    });
    const memoCount = matched.filter(b => b.memo.trim().length > 0).length;
    const cumulativeActivity = matched.length + memoCount;
    const stage = thresholdLookup(cumulativeActivity);

    user.plantStateByInterest[interestId] = {
      stage,
      cumulativeActivity,
      // C3 (Section 2 self-review fix): 진입 시점 (오늘) 으로 셋 — 첫 인상 wilting 회피
      lastEngagedAt: today,
      ...(stage === 5 ? { unlockedAt: today } : {}),
    };
  }

  user.gardenBackfilled = true;
}
```

- [ ] **Step 2: getCachedUser path에 backfill hook (user.ts:44-69)**

```ts
// user.ts:44-69 getCachedUser 안에 추가
import { backfillGarden } from './backfillGarden';

export function getCachedUser(): User | null {
  // ... 기존 logic
  user = migrateUserToV3(user);
  user = migrateUserToV4(user);   // T1
  if (!isValidUserShape(user)) {
    notifyCorruption();
    return null;
  }

  // T8: backfill 자동 (sweep 우회 — 환영 모달 highlight로만 통지)
  if (!user.gardenBackfilled) {
    backfillGarden(user);
    try { localStorage.setItem(KEY, JSON.stringify(user)); } catch { /* ignore */ }
  } else if (parsed?.schemaVersion !== 4) {
    try { localStorage.setItem(KEY, JSON.stringify(user)); } catch { /* ignore */ }
  }

  return user as User;
}
```

- [ ] **Step 3: Failing test 작성**

```ts
// tests/unit/state/backfill.spec.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { backfillGarden } from '../../../src/state/backfillGarden';
import type { User } from '../../../src/state/user';

function userWithBriefings(briefings: any[], interests: string[] = ['ai', 'design']): User {
  localStorage.setItem('briefings', JSON.stringify(briefings));
  return {
    name: 'h', interests, onboardedAt: '', streak: 0, lastActiveDate: '',
    xp: 0, earnedBadges: {}, gamificationMigrated: true, schemaVersion: 4,
    missions: { active: [], cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 }, lastDailySeed: '', currentWeekIso: '', currentMonthIso: '' },
    plantStateByInterest: {}, gardenIntroduced: false, gardenBackfilled: false,
  };
}

beforeEach(() => localStorage.clear());

describe('backfillGarden', () => {
  it('AI 매칭 스크랩 5 + 메모 2 → cumulativeActivity 7, stage 1 (8 미만)', () => {
    const briefings = [
      ...Array.from({ length: 3 }, (_, i) => ({ id: `${i}`, scrapped: true, title: 'AI 발표', summary: '', memo: '', sourceTitle: '', url: '', date: '' })),
      ...Array.from({ length: 2 }, (_, i) => ({ id: `m${i}`, scrapped: true, title: 'AI 발표', summary: '', memo: '메모', sourceTitle: '', url: '', date: '' })),
    ];
    const u = userWithBriefings(briefings);
    backfillGarden(u);
    expect(u.plantStateByInterest['ai']?.cumulativeActivity).toBe(5 + 2);  // 5 매칭 + 2 메모
    expect(u.plantStateByInterest['ai']?.stage).toBe(1);  // 7 < 8
  });

  it('cumulativeActivity 25 → stage 3, lastEngagedAt = 진입 시점 (C3 fix)', () => {
    const briefings = Array.from({ length: 25 }, (_, i) => ({
      id: `${i}`, scrapped: true, title: 'AI', summary: '',
      memo: '', sourceTitle: '', url: '', date: '2024-01-01',  // 1년 전
    }));
    const u = userWithBriefings(briefings);
    const before = Date.now();
    backfillGarden(u);
    const after = Date.now();
    expect(u.plantStateByInterest['ai']?.stage).toBe(3);
    const last = new Date(u.plantStateByInterest['ai']!.lastEngagedAt!).getTime();
    expect(last).toBeGreaterThanOrEqual(before);
    expect(last).toBeLessThanOrEqual(after);
  });

  it('cumulativeActivity 160+ → stage 5, unlockedAt 셋', () => {
    const briefings = Array.from({ length: 200 }, (_, i) => ({
      id: `${i}`, scrapped: true, title: 'AI', summary: '',
      memo: '', sourceTitle: '', url: '', date: '',
    }));
    const u = userWithBriefings(briefings);
    backfillGarden(u);
    expect(u.plantStateByInterest['ai']?.stage).toBe(5);
    expect(u.plantStateByInterest['ai']?.unlockedAt).toBeDefined();
  });

  it('idempotent (gardenBackfilled flag) — 재호출 시 변동 0', () => {
    const u = userWithBriefings([]);
    u.gardenBackfilled = true;
    u.plantStateByInterest['ai'] = { stage: 3, cumulativeActivity: 30 };
    backfillGarden(u);
    expect(u.plantStateByInterest['ai']?.cumulativeActivity).toBe(30);
  });

  it('신규 사용자 (briefings 0개) → 모든 식물 stage 1, cumulativeActivity 0', () => {
    const u = userWithBriefings([]);
    backfillGarden(u);
    expect(u.plantStateByInterest['ai']?.stage).toBe(1);
    expect(u.plantStateByInterest['ai']?.cumulativeActivity).toBe(0);
    expect(u.plantStateByInterest['design']?.stage).toBe(1);
  });

  it('gardenBackfilled 셋 (1회만 작동)', () => {
    const u = userWithBriefings([]);
    backfillGarden(u);
    expect(u.gardenBackfilled).toBe(true);
  });

  it('관심분야 외 entry 자동 생성 안 함 (interests filter)', () => {
    const u = userWithBriefings([{ id: '1', scrapped: true, title: '경제', summary: '', memo: '', sourceTitle: '', url: '', date: '' }], ['ai']);
    backfillGarden(u);
    expect(u.plantStateByInterest['economy']).toBeUndefined();
    expect(u.plantStateByInterest['ai']).toBeDefined();
  });

  it('S2 fix: 본 함수가 takeSnapshot/runSweep 호출 안 함 (false-fire 방지)', () => {
    // 함수 import만으로 sweep emit 안 일어남을 검증 — 직접 검증 어려우므로
    // achievements.ts 의 사이드 이펙트 (toast 등) mock으로 검증
    // (mock setup은 plan 실행 시 vi.mock 패턴 적용)
  });
});
```

- [ ] **Step 4: PASS 확인**

```bash
npx vitest run tests/unit/state/backfill.spec.ts
```

Expected: 8 PASS.

- [ ] **Step 5: 회귀 (특히 user.spec.ts / migration.spec.ts)**

```bash
npx tsc --noEmit && npm test
```

Expected: 모든 기존 spec PASS, 신규 +8.

- [ ] **Step 6: Commit**

```bash
git add src/state/backfillGarden.ts src/state/user.ts tests/unit/state/backfill.spec.ts
git commit -m "feat(v3.15 T8): backfillGarden — lazy migrate 후 자동, sweep 우회 (S2/S3 fix)"
```

---

## Task 9: i18n MSG.GARDEN_* keys

**Files:**
- Modify: `src/ui/messages.ts` (8~10 신규 키 추가)
- Test: 기존 i18n.spec.ts 등 회귀

- [ ] **Step 1: messages.ts에 GARDEN_* 키 추가**

```ts
// src/ui/messages.ts MSG 객체 안에 추가
GARDEN_INTRODUCE_TITLE: '🌱 정원이 새로 생겼어요',
GARDEN_INTRODUCE_BODY: '당신의 활동이 분야별로 식물을 키워요.\n스크랩과 메모가 그 분야 식물에 양분이 되고,\n미션을 완수하면 정원 전체에 비가 와요.',
GARDEN_HIGHLIGHT_BACKFILL: (interest: string, stageLabel: string, emoji: string) =>
  `기존 활동을 반영했어요 ─ ${interest} 식물이 ${stageLabel}까지 자랐어요 ${emoji}`,
GARDEN_NEWCOMER: '씨앗부터 시작해요',
GARDEN_CTA_VIEW: '정원 보러 가기',
GARDEN_CTA_CLOSE: '닫기',
GARDEN_BLOOM: (interest: string) => `${interest} 정원이 만개했어요 🌸`,
GARDEN_SECTION_TITLE: '내 정원',
GARDEN_STAGE_LABEL_SEED: '씨앗',
GARDEN_STAGE_LABEL_SPROUT: '새싹',
GARDEN_STAGE_LABEL_STEM: '줄기',
GARDEN_STAGE_LABEL_BUD: '봉오리',
GARDEN_STAGE_LABEL_BLOOM: '만개',
GARDEN_TROPHY_LABEL: '만개 ✨',
```

(stage 라벨은 plantCatalog.STAGE_LABEL과 중복인데, MSG는 i18n 잠재 호환용. plan 단계에서 어느 쪽이 source of truth인지 확정. 본 plan은 plantCatalog 우선, MSG는 모달/카피 한정으로만)

- [ ] **Step 2: TS strict + lint 회귀**

```bash
npx tsc --noEmit && npm run lint
```

Expected: 0 error.

- [ ] **Step 3: 기존 messages.spec.ts (또는 동등) 회귀**

```bash
npm test
```

Expected: 모든 기존 spec PASS.

- [ ] **Step 4: Commit**

```bash
git add src/ui/messages.ts
git commit -m "feat(v3.15 T9): MSG.GARDEN_* i18n keys 추가 (모달/카피)"
```

---

## Task 10: garden-grid 컴포넌트 (full + mini variant)

**Files:**
- Create: `src/ui/components/garden-grid.ts`
- Create: `src/styles/components/garden.css` (또는 기존 components.css에 추가)
- Test: `tests/unit/ui/garden-grid.spec.ts`

- [ ] **Step 1: garden-grid.ts 생성**

```ts
// src/ui/components/garden-grid.ts
import type { User } from '../../state/user';
import { getPlantIcon, STAGE_LABEL, TROPHY_MARK } from '../../state/plantCatalog';
import { checkWilting } from '../../state/plantEngine';

const INTEREST_LABEL: Record<string, string> = {
  ai: 'AI', design: '디자인', economy: '경제', trend: '트렌드',
  practice: '실무', growth: '성장',
  // 사용자 정의 분야는 interestId 그대로 표시
};

function shortLabel(interestId: string, full: boolean): string {
  const label = INTEREST_LABEL[interestId] ?? interestId;
  return full ? label : label.slice(0, 2);  // mini는 2자
}

/**
 * Stats 탭 full grid — 5×6 cards, 3 col responsive (narrow = 1 col).
 * archived 식물 (user.interests 외) 은 grid에서 제외.
 */
export function renderGardenGrid(root: HTMLElement, user: User): void {
  const visible = user.interests.filter(id => user.plantStateByInterest[id]);
  if (visible.length === 0) {
    root.innerHTML = `<p class="garden-empty">관심분야를 추가하면 식물이 자라요</p>`;
    return;
  }
  const now = new Date();
  const cards = visible.map(id => {
    const plant = user.plantStateByInterest[id]!;
    const wilting = checkWilting(plant, now);
    const trophy = plant.unlockedAt ? `<span class="garden-trophy">${TROPHY_MARK}</span>` : '';
    return `
      <div class="garden-card${wilting ? ' wilting' : ''}${plant.stage === 5 ? ' bloomed' : ''}" data-interest-id="${id}">
        <span class="garden-emoji">${getPlantIcon(id, plant.stage)}</span>
        <span class="garden-name">${shortLabel(id, true)}${trophy}</span>
        <span class="garden-stage-label">${STAGE_LABEL[plant.stage]}</span>
        <span class="garden-cum-count">${plant.cumulativeActivity}회</span>
      </div>`;
  }).join('');
  root.innerHTML = `<div class="garden-grid">${cards}</div>`;
}

/**
 * 홈 mini preview row — cell ~50px, emoji + 짧은 라벨, 가로 flex.
 * 클릭 시 stats 탭 navigate (caller가 handler attach).
 */
export function renderGardenMini(root: HTMLElement, user: User): void {
  const visible = user.interests.filter(id => user.plantStateByInterest[id]);
  if (visible.length === 0) {
    root.innerHTML = '';  // 신규 사용자 mini 미표시
    return;
  }
  const now = new Date();
  const cells = visible.map(id => {
    const plant = user.plantStateByInterest[id]!;
    const wilting = checkWilting(plant, now);
    const trophy = plant.unlockedAt ? `<span class="garden-mini-trophy">${TROPHY_MARK}</span>` : '';
    return `
      <button type="button" class="garden-mini-cell${wilting ? ' wilting' : ''}" data-interest-id="${id}" aria-label="${shortLabel(id, true)} 정원">
        <span class="garden-mini-emoji">${getPlantIcon(id, plant.stage)}</span>
        ${trophy}
        <span class="garden-mini-label">${shortLabel(id, false)}</span>
      </button>`;
  }).join('');
  root.innerHTML = `<div class="garden-mini-row" id="gardenMini">${cells}</div>`;
}
```

- [ ] **Step 2: garden.css 추가 (또는 기존 components.css 안)**

```css
/* src/styles/components/garden.css */
@layer components {
  /* Full grid (stats 탭) */
  .garden-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 12px;
  }
  @media (max-width: 480px) {
    .garden-grid { grid-template-columns: 1fr; }
  }
  .garden-card {
    display: flex; flex-direction: column; align-items: center; gap: 4px;
    padding: 16px 12px;
    border-radius: 12px;
    background: var(--surface-2);
    min-height: 160px;
    position: relative;
    transition: transform 0.2s, box-shadow 0.2s;
  }
  .garden-card.bloomed {
    background: linear-gradient(135deg, var(--surface-2), var(--accent-soft));
  }
  .garden-card.wilting {
    opacity: 0.5;
    filter: grayscale(0.4);
    transform: rotate(-2deg);
  }
  .garden-emoji { font-size: 32px; }
  .garden-name { font-weight: 600; font-size: 14px; }
  .garden-stage-label { color: var(--text-2); font-size: 12px; }
  .garden-cum-count { color: var(--text-3); font-size: 12px; }
  .garden-trophy { margin-left: 4px; }

  /* Mini preview row (홈) */
  .garden-mini-row {
    display: flex; gap: 8px; padding: 12px 0;
    overflow-x: auto;  /* 6+ 분야 narrow 화면 fallback */
  }
  .garden-mini-cell {
    flex: 0 0 50px;
    display: flex; flex-direction: column; align-items: center; gap: 2px;
    padding: 8px 4px;
    background: transparent;
    border: 0; border-radius: 8px;
    cursor: pointer;
    transition: box-shadow 0.15s;
    position: relative;
  }
  .garden-mini-cell:hover, .garden-mini-cell:active {
    /* C4 fix: tap target 시각 cue */
    box-shadow: 0 4px 8px rgba(0,0,0,0.1);
  }
  .garden-mini-cell.wilting {
    opacity: 0.5;
    filter: grayscale(0.4);
  }
  .garden-mini-emoji { font-size: 24px; }
  .garden-mini-label { font-size: 11px; color: var(--text-2); }
  .garden-mini-trophy {
    position: absolute; top: 2px; right: 2px; font-size: 10px;
  }

  .garden-empty {
    color: var(--text-3);
    text-align: center;
    padding: 24px;
  }
}
```

- [ ] **Step 3: Failing test 작성**

```ts
// tests/unit/ui/garden-grid.spec.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { renderGardenGrid, renderGardenMini } from '../../../src/ui/components/garden-grid';
import type { User } from '../../../src/state/user';

function user(plants: Record<string, { stage: 1|2|3|4|5; cumulativeActivity: number; lastEngagedAt?: string; unlockedAt?: string }>): User {
  return {
    name: 'h', interests: Object.keys(plants), onboardedAt: '', streak: 0, lastActiveDate: '',
    xp: 0, earnedBadges: {}, gamificationMigrated: true, schemaVersion: 4,
    missions: { active: [], cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 }, lastDailySeed: '', currentWeekIso: '', currentMonthIso: '' },
    plantStateByInterest: plants, gardenIntroduced: true, gardenBackfilled: true,
  };
}

describe('renderGardenGrid (stats full)', () => {
  let root: HTMLDivElement;
  beforeEach(() => { root = document.createElement('div'); });

  it('식물 N개 → cards N개 렌더', () => {
    const u = user({ ai: { stage: 3, cumulativeActivity: 30 }, design: { stage: 1, cumulativeActivity: 5 } });
    renderGardenGrid(root, u);
    expect(root.querySelectorAll('.garden-card')).toHaveLength(2);
  });

  it('stage 5 + unlockedAt → bloomed + trophy', () => {
    const u = user({ ai: { stage: 5, cumulativeActivity: 162, unlockedAt: '2026-05-01' } });
    renderGardenGrid(root, u);
    const card = root.querySelector('.garden-card');
    expect(card?.classList.contains('bloomed')).toBe(true);
    expect(card?.querySelector('.garden-trophy')?.textContent).toBe('✨');
  });

  it('lastEngagedAt 7일+ → wilting class', () => {
    const old = new Date(Date.now() - 8 * 24 * 60 * 60 * 1000).toISOString();
    const u = user({ ai: { stage: 3, cumulativeActivity: 30, lastEngagedAt: old } });
    renderGardenGrid(root, u);
    expect(root.querySelector('.garden-card')?.classList.contains('wilting')).toBe(true);
  });

  it('archived 식물 (interests 외) → grid에서 제외', () => {
    const u = user({ ai: { stage: 3, cumulativeActivity: 30 }, design: { stage: 1, cumulativeActivity: 5 } });
    u.interests = ['ai'];  // design 제거
    renderGardenGrid(root, u);
    expect(root.querySelectorAll('.garden-card')).toHaveLength(1);
    expect(root.textContent).toContain('AI');
    expect(root.textContent).not.toContain('디자인');
  });

  it('식물 0개 → empty 안내', () => {
    const u = user({});
    renderGardenGrid(root, u);
    expect(root.querySelector('.garden-empty')).toBeTruthy();
  });

  it('cumulativeActivity 카운트 표시', () => {
    const u = user({ ai: { stage: 3, cumulativeActivity: 42 } });
    renderGardenGrid(root, u);
    expect(root.textContent).toContain('42회');
  });

  it('stage 라벨 정확 (만개)', () => {
    const u = user({ ai: { stage: 5, cumulativeActivity: 200 } });
    renderGardenGrid(root, u);
    expect(root.textContent).toContain('만개');
  });
});

describe('renderGardenMini (홈 preview)', () => {
  let root: HTMLDivElement;
  beforeEach(() => { root = document.createElement('div'); });

  it('식물 N개 → mini cells N개', () => {
    const u = user({ ai: { stage: 1, cumulativeActivity: 0 }, design: { stage: 1, cumulativeActivity: 0 } });
    renderGardenMini(root, u);
    expect(root.querySelectorAll('.garden-mini-cell')).toHaveLength(2);
  });

  it('식물 0개 → empty (미표시)', () => {
    renderGardenMini(root, user({}));
    expect(root.querySelectorAll('.garden-mini-cell')).toHaveLength(0);
  });

  it('cell aria-label = 분야명', () => {
    const u = user({ ai: { stage: 1, cumulativeActivity: 0 } });
    renderGardenMini(root, u);
    expect(root.querySelector('.garden-mini-cell')?.getAttribute('aria-label')).toContain('AI');
  });
});
```

- [ ] **Step 4: PASS 확인 (jsdom 환경)**

```bash
npx vitest run tests/unit/ui/garden-grid.spec.ts
```

Expected: 10 PASS.

- [ ] **Step 5: garden.css import (entry CSS에)**

```ts
// src/styles/index.css 또는 동등 entry에 추가
@import './components/garden.css';
```

- [ ] **Step 6: Build 검증**

```bash
npm run build
```

Expected: 0 build error.

- [ ] **Step 7: Commit**

```bash
git add src/ui/components/garden-grid.ts src/styles/components/garden.css src/styles/index.css tests/unit/ui/garden-grid.spec.ts
git commit -m "feat(v3.15 T10): garden-grid component — full + mini variant + CSS"
```

---

## Task 11: stats 탭 integration (#gardenSection auto scroll)

**Files:**
- Modify: `src/ui/tabs/stats.ts` (#gardenSection 추가)
- Modify: 적절한 handlers (stats hydrate path)
- Test: 기존 `tests/unit/tabs/stats.spec.ts` 또는 신규

- [ ] **Step 1: stats.ts에 #gardenSection 추가**

```ts
// src/ui/tabs/stats.ts
// 기존 streak/level/answers/scraps/badges 마크업 끝부분에 추가:

const gardenSectionHtml = `
  <section id="gardenSection" class="garden-section" aria-label="내 정원">
    <h2 class="section-title">내 정원</h2>
    <div id="gardenContainer"></div>
  </section>
`;

// renderStats 함수에서 위 HTML을 stats 컨테이너에 append
```

- [ ] **Step 2: hydrate path에서 renderGardenGrid 호출**

```ts
// src/ui/handlers/stats.ts (또는 동등 hydrate handler)
import { renderGardenGrid } from '../components/garden-grid';
import { loadUserData } from '../../state/user';

document.addEventListener('dg:nav:tab-changed', (e: Event) => {
  if ((e as CustomEvent).detail?.tab !== 'stats') return;
  const root = document.getElementById('gardenContainer');
  const user = loadUserData();
  if (!root || !user) return;
  renderGardenGrid(root, user);
});
```

- [ ] **Step 3: auto scroll helper (환영 모달에서 호출용)**

```ts
// src/ui/handlers/stats.ts 또는 동등
export function scrollToGardenSection(): void {
  const section = document.getElementById('gardenSection');
  if (section) {
    section.scrollIntoView({ behavior: 'smooth', block: 'start' });
  }
}
```

- [ ] **Step 4: Failing test 작성**

```ts
// tests/unit/tabs/stats-garden.spec.ts (또는 기존 stats.spec.ts에 describe 추가)
import { describe, it, expect, beforeEach } from 'vitest';
import { renderStats } from '../../../src/ui/tabs/stats';

beforeEach(() => {
  localStorage.clear();
  document.body.innerHTML = '';
});

describe('stats 탭 정원 섹션 integration', () => {
  it('#gardenSection 마크업 존재', () => {
    const container = document.createElement('div');
    renderStats(container);
    expect(container.querySelector('#gardenSection')).toBeTruthy();
    expect(container.querySelector('#gardenContainer')).toBeTruthy();
  });

  it('#gardenSection은 #badgesGrid 뒤에 위치', () => {
    const container = document.createElement('div');
    renderStats(container);
    const badges = container.querySelector('#badgesGrid');
    const garden = container.querySelector('#gardenSection');
    if (badges && garden) {
      expect(badges.compareDocumentPosition(garden) & Node.DOCUMENT_POSITION_FOLLOWING).toBeTruthy();
    }
  });

  it('hydrate 시 garden grid render', () => {
    // localStorage에 user + plant data 넣고 dg:nav:tab-changed dispatch 시 cards 보임
  });
});
```

- [ ] **Step 5: PASS 확인**

```bash
npx vitest run tests/unit/tabs/stats-garden.spec.ts
```

Expected: 3 PASS.

- [ ] **Step 6: 회귀 (기존 stats.spec.ts)**

```bash
npm test
```

- [ ] **Step 7: Commit**

```bash
git add src/ui/tabs/stats.ts src/ui/handlers/stats.ts tests/unit/tabs/stats-garden.spec.ts
git commit -m "feat(v3.15 T11): stats 탭 #gardenSection + hydrate"
```

---

## Task 12: 홈 mini preview (#gardenMini)

**Files:**
- Modify: `src/ui/tabs/home.ts` (#gardenMini placeholder)
- Modify: `src/ui/handlers/home.ts` (또는 동등) — hydrate
- Test: 기존 home.spec.ts 또는 신규

- [ ] **Step 1: home.ts에 #gardenMini placeholder 추가**

```ts
// src/ui/tabs/home.ts
// greeting + streakBanner 직후에 추가:

const gardenMiniHtml = `<div id="gardenMini"></div>`;
```

- [ ] **Step 2: hydrate path에서 renderGardenMini 호출**

```ts
// src/ui/handlers/home.ts (또는 동등)
import { renderGardenMini } from '../components/garden-grid';
import { loadUserData } from '../../state/user';
import { switchTab } from '../nav';
import { scrollToGardenSection } from './stats';

document.addEventListener('dg:nav:tab-changed', (e: Event) => {
  if ((e as CustomEvent).detail?.tab !== 'home') return;
  const root = document.getElementById('gardenMini');
  const user = loadUserData();
  if (!root || !user) return;
  renderGardenMini(root, user);

  // cell 클릭 → stats 탭 + scroll
  root.addEventListener('click', (ev) => {
    const cell = (ev.target as HTMLElement)?.closest('.garden-mini-cell');
    if (cell) {
      switchTab('stats');
      // 다음 tick에 scroll (stats 탭 렌더 후)
      setTimeout(() => scrollToGardenSection(), 100);
    }
  });
});
```

- [ ] **Step 3: Failing test (jsdom)**

```ts
// tests/unit/tabs/home-garden-mini.spec.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { renderHome } from '../../../src/ui/tabs/home';

beforeEach(() => {
  localStorage.clear();
  document.body.innerHTML = '';
});

describe('홈 #gardenMini integration', () => {
  it('#gardenMini placeholder 존재', () => {
    const c = document.createElement('div');
    renderHome(c);
    expect(c.querySelector('#gardenMini')).toBeTruthy();
  });

  it('#gardenMini는 greeting/streakBanner 뒤, 질문 앞', () => {
    const c = document.createElement('div');
    renderHome(c);
    const greeting = c.querySelector('#greeting');
    const garden = c.querySelector('#gardenMini');
    if (greeting && garden) {
      expect(greeting.compareDocumentPosition(garden) & Node.DOCUMENT_POSITION_FOLLOWING).toBeTruthy();
    }
  });
});
```

- [ ] **Step 4: PASS 확인 + 회귀**

```bash
npx vitest run tests/unit/tabs/home-garden-mini.spec.ts && npm test
```

- [ ] **Step 5: Commit**

```bash
git add src/ui/tabs/home.ts src/ui/handlers/home.ts tests/unit/tabs/home-garden-mini.spec.ts
git commit -m "feat(v3.15 T12): home #gardenMini + cell click → stats navigate"
```

---

## Task 13: welcome-garden modal (첫 진입 1회)

**Files:**
- Create: `src/ui/modals/welcome-garden.ts`
- Modify: app entry (또는 home hydrate) — 첫 진입 시 모달 노출 trigger
- Test: `tests/unit/modals/welcome-garden.spec.ts`

- [ ] **Step 1: welcome-garden.ts 생성**

```ts
// src/ui/modals/welcome-garden.ts
import { loadUserData, saveUser } from '../../state/user';
import { STAGE_LABEL, getPlantIcon, TROPHY_MARK } from '../../state/plantCatalog';
import { switchTab } from '../nav';
import { scrollToGardenSection } from '../handlers/stats';
import { MSG } from '../messages';

const INTEREST_LABEL: Record<string, string> = {
  ai: 'AI', design: '디자인', economy: '경제', trend: '트렌드',
  practice: '실무', growth: '성장',
};

/**
 * 첫 v3.15 진입 시 1회 표시. gardenIntroduced flag로 idempotent.
 *
 * Highlight: backfill 결과 중 가장 큰 stage 식물 1개 명시.
 * 신규 사용자 (모든 식물 stage 1, cum 0) → highlight 생략, "씨앗부터 시작해요" 대체.
 */
export function maybeShowWelcomeGarden(): void {
  const user = loadUserData();
  if (!user || user.gardenIntroduced) return;

  // backfill highlight 후보 — 가장 큰 stage (동률 시 cumulativeActivity 큰 것)
  const visible = user.interests.filter(id => user.plantStateByInterest[id]);
  const sorted = visible
    .map(id => ({ id, plant: user.plantStateByInterest[id]! }))
    .sort((a, b) => (b.plant.stage - a.plant.stage) || (b.plant.cumulativeActivity - a.plant.cumulativeActivity));

  const top = sorted[0];
  const newcomer = !top || (top.plant.stage === 1 && top.plant.cumulativeActivity === 0);

  let highlightHtml = '';
  if (!newcomer && top) {
    const label = INTEREST_LABEL[top.id] ?? top.id;
    const stageLabel = STAGE_LABEL[top.plant.stage];
    const emoji = getPlantIcon(top.id, top.plant.stage);
    highlightHtml = `<p class="welcome-garden-highlight">${MSG.GARDEN_HIGHLIGHT_BACKFILL(label, stageLabel, emoji)}</p>`;
  } else {
    highlightHtml = `<p class="welcome-garden-newcomer">${MSG.GARDEN_NEWCOMER}</p>`;
  }

  const dialog = document.createElement('div');
  dialog.className = 'welcome-garden-modal modal-backdrop';
  dialog.innerHTML = `
    <div class="modal-card" role="dialog" aria-labelledby="welcomeGardenTitle">
      <h2 id="welcomeGardenTitle">${MSG.GARDEN_INTRODUCE_TITLE}</h2>
      <p>${MSG.GARDEN_INTRODUCE_BODY.replace(/\n/g, '<br>')}</p>
      ${highlightHtml}
      <div class="modal-actions">
        <button type="button" class="btn-primary" id="welcomeGardenViewBtn">${MSG.GARDEN_CTA_VIEW}</button>
        <button type="button" class="btn-secondary" id="welcomeGardenCloseBtn">${MSG.GARDEN_CTA_CLOSE}</button>
      </div>
    </div>
  `;

  function close(navigate: boolean): void {
    dialog.remove();
    const u = loadUserData();
    if (u && !u.gardenIntroduced) {
      u.gardenIntroduced = true;
      try { saveUser(u); } catch { /* Quota silent — 다음 진입 재시도 */ }
    }
    if (navigate) {
      switchTab('stats');
      setTimeout(() => scrollToGardenSection(), 100);
    }
  }

  dialog.querySelector('#welcomeGardenViewBtn')?.addEventListener('click', () => close(true));
  dialog.querySelector('#welcomeGardenCloseBtn')?.addEventListener('click', () => close(false));
  dialog.addEventListener('click', (e) => { if (e.target === dialog) close(false); });
  document.addEventListener('keydown', function onEsc(e: KeyboardEvent) {
    if (e.key === 'Escape') {
      document.removeEventListener('keydown', onEsc);
      close(false);
    }
  });

  document.body.appendChild(dialog);
}
```

- [ ] **Step 2: 첫 진입 시점에 호출 hook**

```ts
// 앱 entry (예: src/main.ts 또는 home hydrate handler)
import { maybeShowWelcomeGarden } from './ui/modals/welcome-garden';

// app boot 또는 home tab 첫 진입 시
maybeShowWelcomeGarden();
```

- [ ] **Step 3: Failing test 작성**

```ts
// tests/unit/modals/welcome-garden.spec.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { maybeShowWelcomeGarden } from '../../../src/ui/modals/welcome-garden';

function setUser(opts: { gardenIntroduced: boolean; plants?: Record<string, any> }): void {
  localStorage.setItem('user', JSON.stringify({
    name: 'h', interests: Object.keys(opts.plants ?? {}), onboardedAt: '', streak: 0,
    lastActiveDate: '', xp: 0, earnedBadges: {}, gamificationMigrated: true,
    schemaVersion: 4,
    missions: { active: [], cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 }, lastDailySeed: '', currentWeekIso: '', currentMonthIso: '' },
    plantStateByInterest: opts.plants ?? {},
    gardenIntroduced: opts.gardenIntroduced, gardenBackfilled: true,
  }));
}

beforeEach(() => {
  localStorage.clear();
  document.body.innerHTML = '';
});

describe('maybeShowWelcomeGarden', () => {
  it('gardenIntroduced false → 모달 노출', () => {
    setUser({ gardenIntroduced: false, plants: { ai: { stage: 1, cumulativeActivity: 0 } } });
    maybeShowWelcomeGarden();
    expect(document.querySelector('.welcome-garden-modal')).toBeTruthy();
  });

  it('gardenIntroduced true → 모달 미노출 (idempotent)', () => {
    setUser({ gardenIntroduced: true });
    maybeShowWelcomeGarden();
    expect(document.querySelector('.welcome-garden-modal')).toBeFalsy();
  });

  it('backfill highlight — 가장 큰 stage 식물 명시', () => {
    setUser({
      gardenIntroduced: false,
      plants: {
        ai: { stage: 4, cumulativeActivity: 100 },
        design: { stage: 2, cumulativeActivity: 15 },
      },
    });
    maybeShowWelcomeGarden();
    const html = document.querySelector('.welcome-garden-modal')?.innerHTML ?? '';
    expect(html).toContain('AI');
    expect(html).toContain('봉오리');
  });

  it('newcomer (모든 식물 stage 1, cum 0) → "씨앗부터 시작해요"', () => {
    setUser({
      gardenIntroduced: false,
      plants: { ai: { stage: 1, cumulativeActivity: 0 } },
    });
    maybeShowWelcomeGarden();
    const html = document.querySelector('.welcome-garden-modal')?.innerHTML ?? '';
    expect(html).toContain('씨앗부터');
  });

  it('닫기 버튼 → 모달 제거 + gardenIntroduced 셋', () => {
    setUser({ gardenIntroduced: false, plants: { ai: { stage: 1, cumulativeActivity: 0 } } });
    maybeShowWelcomeGarden();
    document.querySelector<HTMLButtonElement>('#welcomeGardenCloseBtn')?.click();
    expect(document.querySelector('.welcome-garden-modal')).toBeFalsy();
    const u = JSON.parse(localStorage.getItem('user')!);
    expect(u.gardenIntroduced).toBe(true);
  });

  it('ESC → 모달 제거 + flag 셋', () => {
    setUser({ gardenIntroduced: false, plants: { ai: { stage: 1, cumulativeActivity: 0 } } });
    maybeShowWelcomeGarden();
    document.dispatchEvent(new KeyboardEvent('keydown', { key: 'Escape' }));
    expect(document.querySelector('.welcome-garden-modal')).toBeFalsy();
  });

  it('"정원 보러 가기" → switchTab(stats) 호출', () => {
    // switchTab mock 검증
  });

  it('user 없음 → no-op', () => {
    expect(() => maybeShowWelcomeGarden()).not.toThrow();
    expect(document.querySelector('.welcome-garden-modal')).toBeFalsy();
  });
});
```

- [ ] **Step 4: PASS + 회귀**

```bash
npx vitest run tests/unit/modals/welcome-garden.spec.ts && npm test
```

Expected: 8 PASS.

- [ ] **Step 5: Commit**

```bash
git add src/ui/modals/welcome-garden.ts src/main.ts tests/unit/modals/welcome-garden.spec.ts
git commit -m "feat(v3.15 T13): welcome-garden modal — 첫 진입 1회 + backfill highlight"
```

---

## Task 14: max event toast + confetti (.toast--bloom)

**Files:**
- Modify: `src/ui/rewards.ts` (또는 동등 toast helper) — `.toast--bloom` style
- Modify: `src/state/achievements.ts` 또는 events handler — `plant-stage-up` event 처리
- Test: `tests/unit/state/events-plant.spec.ts` (T5에 추가)

- [ ] **Step 1: rewards.ts에 .toast--bloom 추가 (CSS)**

```css
/* src/styles/components/rewards.css 또는 동등 */
@layer components {
  .toast--bloom {
    background: linear-gradient(135deg, #fce7f3, #fbcfe8);  /* pink soft */
    border: 1px solid #f9a8d4;
    color: #831843;
  }
  /* confetti는 v3.12 .toast--levelup 패턴 100% 재사용 — 새 burst 안 만듦 */
}
```

- [ ] **Step 2: events handler에서 plant-stage-up + newStage===5 처리 (S6 fix — `dg:reward:*` 채널)**

```ts
// src/ui/events.ts (또는 동등 handler) — plant-stage-up listener
import { showToast } from '../utils/toast';
import { MSG } from './messages';
import { INTERESTS } from '../utils/categories';

const INTEREST_LABEL: Record<string, string> = Object.fromEntries(
  INTERESTS.map(i => [i.id, i.label.replace(/^(\p{Emoji_Presentation}|\p{Extended_Pictographic})\s*/u, '')])
);

document.addEventListener('dg:reward:plant-stage-up', (e: Event) => {
  const detail = (e as CustomEvent).detail;
  if (!detail || detail.newStage !== 5) return;  // stage 1~4 silent (잡음 회피)
  const label = INTEREST_LABEL[detail.interestId] ?? detail.interestId;
  showToast(MSG.GARDEN_BLOOM(label), 4000, 'bloom');  // .toast--bloom variant
  // C5 fix: 토스트 비-action — 탭해도 navigate 안 함 (사용자 흐름 보호)
  // confetti는 toast 자체가 v3.12 패턴으로 자동 burst
});
```

- [ ] **Step 3: emitEvents 채널 검증 (T5 step 3에서 이미 `dg:reward:plant-stage-up` 추가)**

T5 step 3 emitEvents switch case에 `case 'plant-stage-up'` + `dispatch('dg:reward:plant-stage-up', { interestId, newStage, at })` 추가 완료. T14는 listener 측 검증만 — 새 dispatch 코드 추가 X (v3.12/v3.13 sweep 패턴 그대로 재사용).

- [ ] **Step 4: Failing test 추가 (events-plant.spec.ts에 describe 추가)**

```ts
// tests/unit/state/events-plant.spec.ts 끝에 추가
describe('plant-stage-up event handler (max stage)', () => {
  beforeEach(() => {
    document.body.innerHTML = '';
    // events.ts side-effect import 보장
    import('../../../src/ui/events');
  });

  it('newStage === 5 → toast--bloom emit (S6 fix: dg:reward 채널 + kind discriminant)', async () => {
    document.dispatchEvent(new CustomEvent('dg:reward:plant-stage-up', {
      detail: { kind: 'plant-stage-up', interestId: 'ai_ml', newStage: 5, at: Date.now() },
    }));
    // toast가 DOM에 추가됐는지 확인 (v3.12 toast container 패턴)
    await new Promise(r => setTimeout(r, 50));
    const toast = document.querySelector('.toast--bloom');
    expect(toast?.textContent).toMatch(/AI/i);  // INTEREST_LABEL에서 추출
    expect(toast?.textContent).toContain('만개');
  });

  it('newStage 1~4 → toast 안 emit (silent)', async () => {
    for (const stage of [1, 2, 3, 4]) {
      document.dispatchEvent(new CustomEvent('dg:reward:plant-stage-up', {
        detail: { kind: 'plant-stage-up', interestId: 'ai_ml', newStage: stage, at: Date.now() },
      }));
    }
    await new Promise(r => setTimeout(r, 50));
    expect(document.querySelectorAll('.toast--bloom')).toHaveLength(0);
  });

  it('C5 fix: toast 클릭 시 navigate 안 함 (비-action)', async () => {
    document.dispatchEvent(new CustomEvent('dg:reward:plant-stage-up', {
      detail: { kind: 'plant-stage-up', interestId: 'ai_ml', newStage: 5, at: Date.now() },
    }));
    await new Promise(r => setTimeout(r, 50));
    const toast = document.querySelector('.toast--bloom') as HTMLElement;
    const tabBefore = document.querySelector('.nav-item.active')?.getAttribute('data-tab-id');
    toast?.click();
    const tabAfter = document.querySelector('.nav-item.active')?.getAttribute('data-tab-id');
    expect(tabAfter).toBe(tabBefore);  // 탭 변동 0
  });
});
```

- [ ] **Step 5: PASS + 회귀**

```bash
npx vitest run tests/unit/state/events-plant.spec.ts && npm test
```

Expected: 12 PASS (T5 9 + T14 3).

- [ ] **Step 6: Commit**

```bash
git add src/styles/components/rewards.css src/ui/events.ts tests/unit/state/events-plant.spec.ts
git commit -m "feat(v3.15 T14): plant-stage-up max event — .toast--bloom + 비-action (C5 fix)"
```

---

## Task 15: interests modal save hook (S10 / P1-4 fix)

**Files:**
- Modify: `src/ui/modals/interests.ts` (또는 settings 안 분야 modal save handler)
- Test: `tests/unit/modals/interests-plant-hook.spec.ts`

- [ ] **Step 1: interests modal save handler 위치 확인**

```bash
grep -n "saveUser\|user.interests" src/ui/modals/interests.ts | head -10
```

기존 save 시점에서 user.interests update + saveUser 호출 path 확인. ensurePlantsForInterests 호출은 user.interests 갱신 직후 + saveUser 직전.

- [ ] **Step 2: ensurePlantsForInterests 호출 통합**

```ts
// src/ui/modals/interests.ts (또는 settings 분야 modal handler)
import { ensurePlantsForInterests } from '../../state/plantEngine';

// save handler 안에서 user.interests 갱신 직후, saveUser 직전:
function handleInterestsSave(newInterests: string[]): void {
  const user = loadUserData();
  if (!user) return;
  user.interests = newInterests;

  // S10 fix (P1-4): 새 분야 ID에 stage 1 식물 자동 생성
  // archived 식물(이전에 제거됐던 ID 재추가) 은 기존 entry 그대로 복원 (Q8-B)
  ensurePlantsForInterests(user);

  try {
    saveUser(user);  // 단일 saveUser — atomic
  } catch (err) {
    showToast(getSaveErrorMessage(err));
  }
}
```

- [ ] **Step 3: Failing test 작성**

```ts
// tests/unit/modals/interests-plant-hook.spec.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { handleInterestsSave } from '../../../src/ui/modals/interests';  // 또는 동등 export

beforeEach(() => {
  localStorage.clear();
  localStorage.setItem('user', JSON.stringify({
    name: 'h', interests: [], onboardedAt: '', streak: 0, lastActiveDate: '',
    xp: 0, earnedBadges: {}, gamificationMigrated: true, schemaVersion: 4,
    missions: { active: [], cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 }, lastDailySeed: '', currentWeekIso: '', currentMonthIso: '' },
    plantStateByInterest: {}, gardenIntroduced: false, gardenBackfilled: true,
  }));
});

describe('interests modal save → ensurePlantsForInterests', () => {
  it('새 분야 추가 시 stage 1 식물 자동 생성', () => {
    handleInterestsSave(['recruiting', 'ai_ml']);
    const u = JSON.parse(localStorage.getItem('user')!);
    expect(u.plantStateByInterest.recruiting?.stage).toBe(1);
    expect(u.plantStateByInterest.ai_ml?.stage).toBe(1);
  });

  it('분야 제거 시 entry 그대로 유지 (archived)', () => {
    handleInterestsSave(['recruiting', 'ai_ml']);
    handleInterestsSave(['recruiting']);  // ai_ml 제거
    const u = JSON.parse(localStorage.getItem('user')!);
    expect(u.plantStateByInterest.ai_ml).toBeDefined();  // entry 보존
    expect(u.interests).toEqual(['recruiting']);
  });

  it('분야 재추가 시 기존 entry 복원 (cumulativeActivity 보존)', () => {
    handleInterestsSave(['ai_ml']);
    // 임의로 cumulativeActivity 셋
    const u1 = JSON.parse(localStorage.getItem('user')!);
    u1.plantStateByInterest.ai_ml.cumulativeActivity = 50;
    u1.plantStateByInterest.ai_ml.stage = 3;
    localStorage.setItem('user', JSON.stringify(u1));

    handleInterestsSave([]);  // 제거
    handleInterestsSave(['ai_ml']);  // 재추가
    const u2 = JSON.parse(localStorage.getItem('user')!);
    expect(u2.plantStateByInterest.ai_ml.stage).toBe(3);  // 보존
    expect(u2.plantStateByInterest.ai_ml.cumulativeActivity).toBe(50);
  });

  it('빈 분야 list 저장 → 식물 entry 0', () => {
    handleInterestsSave([]);
    const u = JSON.parse(localStorage.getItem('user')!);
    expect(Object.keys(u.plantStateByInterest)).toHaveLength(0);
  });
});
```

- [ ] **Step 4: PASS + 회귀**

```bash
npx vitest run tests/unit/modals/interests-plant-hook.spec.ts && npm test
```

Expected: 4 PASS, 모든 기존 spec PASS.

- [ ] **Step 5: Commit**

```bash
git add src/ui/modals/interests.ts tests/unit/modals/interests-plant-hook.spec.ts
git commit -m "feat(v3.15 T15): interests modal save → ensurePlantsForInterests (S10 P1-4 fix)"
```

---

## Task 16: Smoke + 사이클-끝 verify (lint/ts/test 3종)

**Files:**
- Create or modify: `tests/smoke/v3.15-garden.spec.ts`
- Verify: `npm run lint && npx tsc --noEmit && npm test && npx playwright test`

- [ ] **Step 1: smoke spec 추가**

```ts
// tests/smoke/v3.15-garden.spec.ts
import { test, expect } from '@playwright/test';
import { primeUserWithGarden } from '../helpers/seed';  // helper 추가 필요 시

test.describe('v3.15 정원 smoke', () => {
  test('stats 탭에 #gardenSection 보임', async ({ page }) => {
    await page.goto('/');
    await primeUserWithGarden(page);  // localStorage seed
    await page.locator('button[data-tab-id="stats"]').click();
    await expect(page.locator('#gardenSection')).toBeVisible();
    await expect(page.locator('.garden-card').first()).toBeVisible();
  });

  test('홈 탭에 #gardenMini 보임 + cell 클릭 → stats navigate', async ({ page }) => {
    await page.goto('/');
    await primeUserWithGarden(page);
    await page.locator('button[data-tab-id="home"]').click();
    await expect(page.locator('#gardenMini')).toBeVisible();
    await page.locator('.garden-mini-cell').first().click();
    await expect(page.locator('#gardenSection')).toBeVisible();  // stats 탭으로 전환
  });
});
```

- [ ] **Step 2: smoke 실행**

```bash
npx playwright test tests/smoke/v3.15-garden.spec.ts
```

Expected: 2 PASS.

- [ ] **Step 3: 사이클-끝 3종 (lesson #5 graduated)**

```bash
npm run lint && npx tsc --noEmit && npm test && npx playwright test
```

Expected: 0 lint error, 0 ts error, 모든 vitest PASS, 모든 smoke PASS.

- [ ] **Step 4: Bundle 측정 (CLI gzip canonical)**

```bash
npm run build && gzip -c dist/assets/index-*.js | wc -c
```

T0 baseline 대비 Δ 계산. spec §1.4 DoD +1.5~2 kB 안에 들어오는지 확인. 초과 시 (a) plant-grid CSS 압축, (b) emoji 카탈로그 단순화 등 mitigation.

- [ ] **Step 5: Commit**

```bash
git add tests/smoke/v3.15-garden.spec.ts
git commit -m "test(v3.15 T16): smoke — 정원 grid render + mini→stats navigate"
```

---

## Task 17: 사이클 retro (spec §11 작성)

**Files:**
- Modify: `docs/superpowers/specs/2026-05-03-v3.15-game-world-design.md` (§11 retro 채우기)

- [ ] **Step 1: §11 retro 채우기 (v3.14.5 §8 패턴 follow) — T17**

내용:
- 8.0 핵심 결과 (commits / vitest / smoke / bundle / DoD 충족 여부)
- 8.1 Codex 사전 review 결과 (P0/P1/P2 + in-cycle 반영)
- 8.2 Codex 최종 review 결과 (DEPLOY_APPROVED / NOTES / REJECT)
- 8.3 신규 lessons (graduated 후보 식별)
- 8.4 §3.1/§3.2 placeholder → 실제 수치 retro 동기화 (T2 시뮬 결과)
- 8.5 v3.16 권고 (식물 detail modal / max ambient / Streak Freeze 등)

- [ ] **Step 2: Commit**

```bash
git add docs/superpowers/specs/2026-05-03-v3.15-game-world-design.md
git commit -m "docs(v3.15 T17): 사이클 retro 작성 + Codex 2-pass 결과 + lessons 식별"
```

---

## Task 18: Memory 갱신 (MEMORY.md + project_daily_growth_v315.md)

**Files:**
- Create: `/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v315.md`
- Modify: `/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md` (포인터 추가)

- [ ] **Step 1: project_daily_growth_v315.md 작성**

frontmatter + body — v3.13/v3.14 메모리 패턴 follow:
- 배포 완료 commit hash + Firebase live URL
- commits 수 + vitest count + smoke + bundle Δ
- Codex 2-pass 결과
- 신규 lessons (graduated)
- 게임화 시리즈 3/3 finale 종료 명시
- v3.16 carry-forward 항목

- [ ] **Step 2: MEMORY.md에 포인터 추가**

```markdown
- [DG v3.15 Game World](project_daily_growth_v315.md) — **✅ 배포 완료 ... 게임화 시리즈 3/3 finale 종료** ...
```

- [ ] **Step 3: Commit**

```bash
# memory dir에서 별도 commit (별도 git 또는 사용자 manual)
# 본 plan은 trigger만 안내 — 실제 memory dir은 사용자 dotfiles 관리
```

---

## Self-review (writing-plans skill 종료 직전)

### 1. Spec coverage check (spec §1~§12 vs plan task 매핑)

| Spec section | Plan task |
|---|---|
| §1 목표 / 범위 / Non-goals | T1~T18 전체 분해 |
| §2 Architecture + Data Model | T1 (schema v4 + types + migration v4 early return + isValidUserShape v4 guard) |
| §3.1 Threshold | T2 (catalog + 시뮬, S9 INTERESTS 15 ID) |
| §3.2 Mission bonus | T3 (engine) + T6 (hook) |
| §3.3 Wilting | T4 |
| §3.4 Backfill | T8 |
| §3.5 Stage transition events | T5 (kind discriminant + dg:reward channel) |
| §4.1 홈 mini | T12 |
| §4.2 Stats full grid | T11 |
| §4.3 환영 모달 | T13 |
| §4.4 Max event | T14 (dg:reward listener) |
| §4.5 Cell 클릭 | T11/T12 (stats no-op, mini → navigate) |
| §4.6 i18n | T9 |
| §5.1 Test scope | T1~T15 각 task 안 spec |
| §5.2 Smoke | T16 |
| §5.3 DoD | T16 verify |
| §5.4 Codex 2-pass | 사전 ✅ 완료 (REQUIRES_FIX → §10.3 8건 in-cycle 반영) / 최종은 T17 retro에 결과 명기 |
| §6 Risk | 각 task mitigation 명시 + Codex 권한 fallback row + prev fallback tradeoff row |
| §7 Plan open questions | T2 시뮬, T9 i18n 정확 카피, T10 hover 색조 |
| §8 v3.15+ Backlog | non-goals (식물 detail / ambient / Streak Freeze) |
| §9 Brainstorm 결정사항 | spec doc 자체 |
| §10.1 Self-review log (C1~C5) | 각 task 코드/테스트에 fix 반영 |
| §10.2 Self-review log (S1~S4) | T5 (S1) / T8 (S2/S3) / T2 + T3 (S4) |
| §10.3 Codex 사전 review (S5~S12) | T1 (S5/S8) / T2 (S9) / T3 (S10 helper) / T5 + T14 (S6) / T7 (S7) / T15 (S10 hook 신규 task) / spec §6 (S11/S12) |
| §11 Retro | T17 |
| §12 v3.16 진입 조건 | T18 memory 명기 |

✅ 모든 spec section task 커버.

### 2. Placeholder scan

검색 대상 패턴: TBD / TODO / "implement later" / "fill in details" / "Add appropriate" / "Similar to Task N"

발견:
- T6 mock 구조: "actual mock 구조는 missionEngine.spec 패턴 참조 후 확정" — ⚠️ task 안에 deferred. plan 실행 시 sub-skill subagent-driven-development로 mock 구조 확정.
- T7 hook 위치: "actual 위치는 briefings.ts 실제 mutation path 따라" — 동일 deferred.
- T9 stage 라벨 source of truth: "plan 단계에서 plantCatalog 우선, MSG는 모달/카피 한정" — 결정 명시.

위 deferred는 task 실행 직전 dispatch 단계에서 결정 가능 (subagent가 file 읽고 hook). 코드 step 자체는 명확.

### 3. Type consistency check

- `PlantState` (plantTypes.ts) ↔ user.ts ↔ migration.ts ↔ plantCatalog.ts ↔ plantEngine.ts 모두 동일 import.
- `PlantStage` type (1|2|3|4|5) 일관 사용.
- `tickPlantActivity(user, interestId, delta)` signature T3 정의 → T6/T7/T8에서 동일 호출.
- `applyMissionBonus(user, period)` signature T3 정의 → T6에서 동일.
- `checkWilting(plant, now)` T4 정의 → T10에서 read-only 동일.
- `renderGardenGrid(root, user)` / `renderGardenMini(root, user)` T10 정의 → T11/T12 동일.
- `Snapshot.plantStages: Record<string, number>` T5 → events-plant.spec.ts에서 동일.
- `GameEvent` discriminated union plant-stage-up variant T5 → T14 handler에서 동일.

✅ 일관.

### 4. Spec deferred 항목 → plan task 매핑 체크

- §7-1 stage 5 emoji OS 호환 → T2 시뮬 단계
- §7-2 threshold + bonus 절대값 → T2 §Threshold Sim
- §7-3 wilting 7 vs 14일 → T4 hardcoded 7 + comment "spec §7-3 placeholder, change requires test update"
- §7-4 i18n 카피 톤 → T9 + Codex 사전 review 검증
- §7-5 hover/active 디자인 → T10 CSS

모두 task 안에 매핑됨.

✅ 통과.

### Self-review 결과 (2-pass)

**1차 self-review (plan 작성 직후)**: 0 critical issue, 3건 deferred (T6/T7/T9는 plan 실행 sub-skill에서 file 읽고 결정 가능).

**2차 Codex 사전 review (2026-05-03)**: REQUIRES_FIX_BEFORE_EXECUTION → P0 2건 + P1 4건 + P2 4건 = 9건 in-cycle 반영 (spec §10.3 S5~S12). 핵심 발견:
- **S5 (P0)** migrate v3가 v4 user의 missions reset → T1에 v4 early return 추가
- **S6 (P0)** GameEvent `kind` discriminant + `dg:reward:*` 채널 → T5/T14 fix
- **S7 (P1)** mutateWithSweep 단일 saveUser 통합 → T7 in-memory helper 패턴 변경
- **S8 (P1)** isValidUserShape v4 nested guard → T1 step 7 추가
- **S9 (P1)** 실제 INTERESTS catalog 15 ID (recruiting/onboarding/.../self_dev) → T2 STAGE5_BY_INTEREST 재작성
- **S10 (P1)** 분야 추가 시 plant 자동 생성 → T3 ensurePlantsForInterests + 신규 T15 (interests modal save hook)
- **S11/S12 (P2)** spec §3.5 tradeoff + bundle ceiling → spec §6 Risk row 추가

**Cycle size 영향**: 18 → **19 task** (T15 신규). vitest +70~85 → **+85~115 (target 95~110, lower 85)**. spec §1.4 retroactive sync 완료.

**최종 분류**: ✅ DEPLOY_APPROVED_FOR_PLAN_EXECUTION (모든 P0/P1 spec/plan에 in-cycle 반영). 진행 OK.

---

## 부록: §Threshold Sim (T2에서 채워짐)

T2 진입 시 시뮬 결과 다음 형식으로 본 plan 끝에 추가:

```
시뮬 가정:
- briefings/day per user = N
- scrap rate = X%
- 분야 매칭 평균/briefing = M (interestKeywords 사전 hit count)
- 미션 완수 빈도 (heavy/mid/casual)
- 식물 N개 effective multiplier

시뮬 결과:
- 8/25/70/160 + +1/+5/+20 → power user max ~T1, casual max ~T2
- spec Q9 commitment hit 여부: ✓ 또는 ✗

조정 결정 (필요 시):
- 절대값 spec 수치 변경: {old} → {new}
- spec §3.1 / §3.2 retroactive sync commit "docs(v3.15 T2 sim): spec retro sync"
```

---

## §Threshold Sim 부록 (T2 시뮬 결과, 2026-05-04)

**가정 (실 데이터 기반 추정):**

- briefings/day per active user: ~5–10 (feed cap 제한)
- answer activity: 1/day (질문 1개 제출 = activity +1)
- scrap activity: ~1–2/day (관심 briefing 스크랩 = activity +1)
- Heavy (engaged) user: ~3 activities/day
- Casual user: ~1 activity/day
- Mission bonus: 완수 시 +1/+5/+20 (daily/weekly/monthly)
- 평균 미션 완수 빈도: heavy ~3/week (+3), casual ~1/week (+1)

**시뮬 결과:**

| 사용자 유형 | 기간 | 순수 activity | 미션 bonus 누적 | 총 cumulative | 도달 stage |
| --- | --- | --- | --- | --- | --- |
| Engaged (heavy) | 3개월 (90d) | 270 | ~36 | ~306 | **5** ✓ |
| Casual | 5개월 (150d) | 150 | ~20 | ~170 | **5** ✓ |
| Very casual | 6개월 (180d) | ~90 | ~12 | ~102 | 4 |

**spec Q9 commitment 검증:**

- "engaged 3달에 stage 5" → cumulative ~306 ≥ 160 ✓
- "casual 5달에 stage 5" → cumulative ~170 ≥ 160 ✓ (margin 있음)

**조정 결정:**

- 8/25/70/160 + +1/+5/+20 **유지** — engaged/casual 모두 Q9 commitment 충족
- spec §3.1 / §3.2 retroactive sync commit **N/A** (수치 변경 없음)

**End of Plan.**
