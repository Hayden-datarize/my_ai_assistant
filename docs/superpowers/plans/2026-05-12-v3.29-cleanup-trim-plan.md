# v3.29 Cleanup + Trim Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v3.28 carry C1(bundle 1278 B trim) + C2(Insight pinned backfill) + F2(검색 keyword `<mark>` highlight) 묶음 처리 — 절대 bundle ≤ 41500 B GREEN strict 복귀.

**Architecture:** T1+T2(F1+F2)는 cleanup invariant/UX 추가(트림 집계 외, +170 B). T3+T4+T5(F3+F4+F5)는 trim 본주(집계 target Δ ≤ −1448 B). T3는 진짜 `await import()` 동적 분리 + Vite `manualChunks` 보강 — Codex 사전 review P1-3 격상 흡수.

**Tech Stack:** TypeScript strict / Vite 6 / Vitest jsdom (TZ=Asia/Seoul + TZ=America/New_York 양쪽) / Playwright smoke / Firebase Hosting / Codex 2-pass review.

**Spec:** `docs/superpowers/specs/2026-05-12-v3.29-cleanup-trim-design.md`

---

## File Structure

| 파일 | 역할 | Task |
|---|---|---|
| `src/state/migration.ts` | already-v8 branch에 `insights[].pinned` 백필 + same-reference 조건부 유지 | T1 |
| `tests/state/migration-v8.spec.ts` | same-reference 단언을 idempotent-or-backfill로 분기 + 신규 spec 3개 | T1 |
| `src/utils/highlight.ts` (신설) | `highlightHtml(text, query)` — escapeHtml + regex escape + `<mark>` wrap | T2 |
| `tests/utils/highlight.spec.ts` (신설) | XSS 시도 5종 + boundary + NFC + 빈 쿼리 + multi-occurrence | T2 |
| `src/ui/handlers/archive.ts:488-565` | `renderAnswerCard` / `renderInsightCard` body에 highlight 적용 (검색 활성 시) | T2 |
| `src/main.ts` + `src/ui/nav.ts` | tab 렌더러를 정적 import → `await import()` 동적 전환 | T3 |
| `vite.config.ts` | `build.rollupOptions.output.manualChunks` 보강 (vendor split) | T3 |
| `tests/smoke/dynamic-tab-load.smoke.ts` (신설) | 첫 navigate FOUC 회귀 + tab lazy chunk 로드 검증 (N=3) | T3 |
| `src/styles/{tokens,base,utilities}.css` 외 | unused token / `@layer base` 미사용 sweep | T4 |
| `src/**/*.ts` | dead import 2nd-pass (TS6133 + side-effect import 수동) | T5 |

---

## Conventions

- **TDD**: 각 task는 (a) 실패하는 spec 작성 → (b) 실패 확인 → (c) 구현 → (d) PASS 확인 → (e) commit.
- **Bundle 측정**: `npm run build && gzip -c dist/assets/index-*.js | wc -c` — CLI gzip canonical (v3.14.5 graduated).
- **양쪽 TZ**: `npm test` (KST default) + `TZ=America/New_York npm test` 양쪽 PASS.
- **Commit 분리**: T1/T2(cleanup)와 T3/T4/T5(trim) 별도 commit. L2 mitigation.
- **Per-task review**: 큰 변경(T2 helper 신설 / T3 dynamic import)만 `pr-review-toolkit:code-reviewer` agent dispatch.

---

## Task 0: Spec + Plan (완료)

**Status**: ✅ 완료 (commit `dd95541` spec / 본 commit plan v1).

- spec `docs/superpowers/specs/2026-05-12-v3.29-cleanup-trim-design.md` (commit `dd95541`)
- Codex 사전 review: PROCEED_AFTER_FIXES (P0=0 / P1=3 / P2=2) — 모두 in-cycle 흡수
- Self-review 보정: 트림 집계 target −1278 → **−1448 B** 재산정 (5곳 정정)

---

## Task 1: F1 — `migrateUserToV8` already-v8 backfill (cleanup invariant)

**Files:**
- Modify: `src/state/migration.ts:406-437`
- Test: `tests/state/migration-v8.spec.ts:43-51` + 신규 spec 3개

**Codex 흡수**: P1-1 — already-v8 early-return은 `insights[].pinned` 누락 허용 (v3.27 T1 chain superset에서 same-reference 강제했지만 invariant 갭). copy-based backfill로 보강, 단 fully-valid v8은 same-reference 보존 (idempotency).

- [ ] **Step 1: 실패 spec 추가 (already-v8 `pinned: undefined` 백필)**

`tests/state/migration-v8.spec.ts` describe 블록 안에 추가:

```ts
it('v8 user with insight.pinned: undefined → 백필 copy (P1 흡수)', () => {
  const v8In = {
    schemaVersion: 8,
    insights: [{ ...baseV7Insight }],  // pinned 키 누락
    xpHistory: [],
  };
  const v8Out = migrateUserToV8(v8In);
  expect(v8Out).not.toBe(v8In as unknown);  // copy
  expect(v8Out.insights[0]?.pinned).toBe(false);  // 백필
  expect(v8Out.insights[0]?.id).toBe('i1');  // 보존
});

it('v8 user with mixed pinned (true + undefined) → true 보존 + 누락 false 백필', () => {
  const v8In = {
    schemaVersion: 8,
    insights: [
      { ...baseV7Insight, id: 'a', pinned: true },
      { ...baseV7Insight, id: 'b' },  // pinned 누락
    ],
    xpHistory: [],
  };
  const v8Out = migrateUserToV8(v8In);
  expect(v8Out.insights[0]?.pinned).toBe(true);
  expect(v8Out.insights[1]?.pinned).toBe(false);
});

it('v8 user with xpHistory non-array → [] 백필', () => {
  const v8In = {
    schemaVersion: 8,
    insights: [{ ...baseV7Insight, pinned: false }],
    xpHistory: 'corrupt' as unknown,  // 손상
  };
  const v8Out = migrateUserToV8(v8In);
  expect(v8Out.xpHistory).toEqual([]);
});
```

- [ ] **Step 2: 실행하여 실패 확인**

```bash
npm test -- migration-v8.spec.ts
```

Expected: 신규 3 spec FAIL (current already-v8 early-return은 pinned 누락 그대로 return).

- [ ] **Step 3: `migration.ts:413` 분기 교체 — copy-based 백필**

`src/state/migration.ts` `migrateUserToV8`의 `if (r.schemaVersion === 8) return r as unknown as User;` 라인을 교체:

```ts
  if (r.schemaVersion === 8) {
    // 이미 v8이지만 already-v8 path에서도 pinned/xpHistory invariant 보장 (P1 흡수)
    const rawInsights = Array.isArray(r.insights) ? r.insights : [];
    const xpHistoryValid = Array.isArray(r.xpHistory);

    // 모든 insight의 pinned가 boolean이고 xpHistory가 array면 same-reference 유지 (idempotency)
    const allPinnedValid = rawInsights.every((i) => {
      const ie = (i ?? {}) as Record<string, unknown>;
      return typeof ie.pinned === 'boolean';
    });

    if (allPinnedValid && xpHistoryValid) {
      return r as unknown as User;  // 진짜 idempotent
    }

    // Copy-based 백필 (in-place mutation 금지)
    const insights = rawInsights.map((i) => {
      const ie = (i ?? {}) as Record<string, unknown>;
      return {
        ...ie,
        pinned: typeof ie.pinned === 'boolean' ? ie.pinned : false,
      };
    });
    return {
      ...r,
      insights,
      xpHistory: xpHistoryValid ? r.xpHistory : [],
    } as unknown as User;
  }
```

- [ ] **Step 4: 실행하여 PASS + 회귀 없음 확인**

```bash
npm test -- migration-v8.spec.ts && TZ=America/New_York npm test -- migration-v8.spec.ts
```

Expected: 양쪽 TZ에서 7~8 spec all PASS (4 baseline + 3 신규, `it.skip` 없음).

- [ ] **Step 5: 전체 vitest + lint regression 0 확인**

```bash
npm test 2>&1 | tail -5 && TZ=America/New_York npm test 2>&1 | tail -5 && npm run lint 2>&1 | tail -3
```

Expected: 양쪽 TZ vitest 1227 PASS (1224 + 3 신규) / lint baseline 30 유지.

- [ ] **Step 6: commit (cleanup, 트림 집계 외)**

```bash
git add src/state/migration.ts tests/state/migration-v8.spec.ts
git commit -m "$(cat <<'EOF'
refactor(v3.29 T1): migrateUserToV8 already-v8 backfill (Codex 사전 P1-1)

- already-v8 branch: pinned/xpHistory invariant 보장 + idempotent fast-path
  · 모든 pinned boolean + xpHistory array면 same-reference 유지
  · 그 외 copy-based backfill (in-place mutation 금지)
- xpHistory non-array → [] 백필 (손상 복구)
- 신규 spec 3개 (already-v8 pinned 누락 / mixed / xpHistory 손상)

trim 집계 외 (L2 mitigation — cleanup invariant).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: F2 — `src/utils/highlight.ts` helper + archive 적용 (cleanup UX)

**Files:**
- Create: `src/utils/highlight.ts`
- Create: `tests/utils/highlight.spec.ts`
- Modify: `src/ui/handlers/archive.ts:488-565` (`renderAnswerCard` + `renderInsightCard` body)

**Codex 흡수**: P1-2 — 현재 archive 검색은 filter-only, 카드 body는 `textContent`. highlight 경로 자체가 없어 helper 신규 정의 + 자체 spec 의무. XSS R3 강제 — escapeHtml + regex escape + `<mark>` only.

- [ ] **Step 1: highlight helper spec 작성 (XSS 시도 5종 + boundary + NFC + empty)**

`tests/utils/highlight.spec.ts` 신설:

```ts
import { describe, it, expect } from 'vitest';
import { highlightHtml } from '../../src/utils/highlight';

describe('highlightHtml (v3.29 T2)', () => {
  it('빈 query → 원문 escape만, mark 없음', () => {
    expect(highlightHtml('Hello <b>X</b>', '')).toBe('Hello &lt;b&gt;X&lt;/b&gt;');
  });

  it('match 없으면 원문 escape', () => {
    expect(highlightHtml('Hello world', 'xyz')).toBe('Hello world');
  });

  it('단일 match — <mark> 감싸기 + 외부 escape 유지', () => {
    expect(highlightHtml('Hello world', 'world')).toBe('Hello <mark>world</mark>');
  });

  it('case-insensitive match (NFC 후 lower)', () => {
    expect(highlightHtml('Hello World', 'world')).toBe('Hello <mark>World</mark>');
  });

  it('multi-occurrence — 모두 wrap', () => {
    expect(highlightHtml('aba aba', 'a')).toBe('<mark>a</mark>b<mark>a</mark> <mark>a</mark>b<mark>a</mark>');
  });

  it('XSS: text 안에 <script> → escape', () => {
    expect(highlightHtml('<script>alert(1)</script>', 'alert')).toBe('&lt;script&gt;<mark>alert</mark>(1)&lt;/script&gt;');
  });

  it('XSS: query 안에 regex special → 리터럴 매치', () => {
    expect(highlightHtml('a.b.c', '.')).toBe('a<mark>.</mark>b<mark>.</mark>c');
  });

  it('XSS: query 안에 < > → escape + literal regex', () => {
    expect(highlightHtml('a<b>c', '<b>')).toBe('a<mark>&lt;b&gt;</mark>c');
  });

  it('XSS: text + query 모두에 <img onerror> → script 실행 0', () => {
    const out = highlightHtml('<img onerror="x">', 'img');
    expect(out).not.toContain('onerror=');
    expect(out).toContain('&lt;');
    expect(out).toContain('<mark>img</mark>');
  });

  it('NFC normalize — 합성된/풀어쓴 한글 동일 매치', () => {
    const composed = '한국';  // 일반 합성
    const decomposed = '한국';  // 분해형
    expect(highlightHtml(composed, decomposed)).toContain('<mark>');
  });
});
```

- [ ] **Step 2: 실패 확인**

```bash
npm test -- highlight.spec.ts
```

Expected: FAIL — `Cannot find module '../../src/utils/highlight'`.

- [ ] **Step 3: `src/utils/highlight.ts` 구현**

`src/utils/highlight.ts` 신설:

```ts
/**
 * v3.29 T2: archive 검색 keyword `<mark>` highlight helper.
 *
 * 보안 invariant (Codex 사전 P1-2 흡수):
 * - text는 escapeHtml로 모두 sanitize
 * - query는 escapeRegex로 리터럴 매칭 (regex injection 차단)
 * - `<mark>` span만 raw HTML — 외부 입력 전체가 escape 후 mark만 inject
 * - NFC normalize → case-insensitive (archive.ts:380 precedent 재사용)
 */

function escapeHtml(s: string): string {
  return s
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;')
    .replace(/'/g, '&#39;');
}

function escapeRegex(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

export function highlightHtml(text: string, query: string): string {
  if (!query) return escapeHtml(text);
  const q = query.normalize('NFC');
  const t = text.normalize('NFC');
  const re = new RegExp(escapeRegex(q), 'gi');
  if (!re.test(t)) return escapeHtml(text);

  // re-run with capture (test() advances lastIndex)
  re.lastIndex = 0;
  let out = '';
  let lastIdx = 0;
  let m: RegExpExecArray | null;
  while ((m = re.exec(t)) !== null) {
    out += escapeHtml(t.slice(lastIdx, m.index));
    out += `<mark>${escapeHtml(m[0])}</mark>`;
    lastIdx = m.index + m[0].length;
    if (m[0].length === 0) re.lastIndex++;  // zero-width 방지
  }
  out += escapeHtml(t.slice(lastIdx));
  return out;
}
```

- [ ] **Step 4: PASS 확인**

```bash
npm test -- highlight.spec.ts && TZ=America/New_York npm test -- highlight.spec.ts
```

Expected: 양쪽 TZ에서 10 spec all PASS.

- [ ] **Step 5: `archive.ts` `renderAnswerCard` + `renderInsightCard` 적용**

`src/ui/handlers/archive.ts` top import에 추가:

```ts
import { highlightHtml } from '../../utils/highlight';
```

`renderAnswerCard:535`의 `body.textContent = a.text;`를 교체:

```ts
  // v3.29 T2: 검색 활성 시 keyword <mark> highlight (XSS-safe helper)
  if (currentQuery) {
    body.innerHTML = highlightHtml(a.text, currentQuery);
  } else {
    body.textContent = a.text;
  }
```

`renderInsightCard:561`의 `body.textContent = i.text;`도 동일 패턴으로 교체:

```ts
  if (currentQuery) {
    body.innerHTML = highlightHtml(i.text, currentQuery);
  } else {
    body.textContent = i.text;
  }
```

- [ ] **Step 6: archive 통합 회귀 + smoke 확인**

```bash
npm test 2>&1 | tail -5 && TZ=America/New_York npm test 2>&1 | tail -5 && npm run lint 2>&1 | tail -3
```

Expected: 양쪽 TZ vitest 1237 PASS (1227 + 10 신규 highlight) / lint 30 유지.

- [ ] **Step 7: bundle Δ 측정 + commit**

```bash
npm run build && gzip -c dist/assets/index-*.js | wc -c
```

예상: 42778 + ~150 B = ~42928 B (트림 집계 외, +170 B 예산 안).

```bash
git add src/utils/highlight.ts tests/utils/highlight.spec.ts src/ui/handlers/archive.ts
git commit -m "$(cat <<'EOF'
feat(v3.29 T2): archive 검색 keyword <mark> highlight (Codex 사전 P1-2)

- src/utils/highlight.ts 신설 — escapeHtml + escapeRegex + NFC + <mark> wrap
- archive renderAnswerCard / renderInsightCard body 적용 (currentQuery 활성 시만)
- XSS 안전 보장 (10 spec: 빈 query / no match / single / multi / case-insensitive
  / <script> escape / regex special / <img onerror> / NFC)

trim 집계 외 (L2 mitigation — cleanup UX). bundle Δ +~150 B 예산 안.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: F3 — tab 렌더러 진짜 dynamic import + manualChunks (trim main)

**Files:**
- Modify: `src/main.ts:1-12, 24-36`
- Modify: `src/ui/nav.ts:1-19, 44-60`
- Modify: `vite.config.ts` (build.rollupOptions.output.manualChunks 추가)
- Create: `tests/smoke/dynamic-tab-load.smoke.ts` (smoke 회귀 N=3)

**Codex 흡수**: P1-3 + R4 P1 격상 — `manualChunks`만으론 초기 전송 미감소. `nav.ts`의 `TABS[].render` 정적 import를 `await import()` 동적 함수로 전환. `main.ts` `mount*Handlers()` 호출도 first-mount eager + 이후 lazy로 재구성.

- [ ] **Step 1: nav.ts dynamic 전환 — TABS schema 변경**

`src/ui/nav.ts` 교체:

```ts
import { qs } from '../utils/dom';

export type TabId = 'home' | 'archive' | 'stats' | 'settings' | 'missions';

interface TabDef {
  id: TabId;
  label: string;
  icon: string;
  load: () => Promise<(c: HTMLElement) => void>;
}

// v3.29 T3 (Codex 사전 P1-3): tab 렌더러를 정적 import → dynamic import 전환.
// home은 first-mount eager (cold-start FOUC 회피), 나머지는 navigate 시 lazy.
export const TABS: Array<TabDef> = [
  { id: 'home', label: '홈', icon: '🏠', load: () => import('./tabs/home').then((m) => m.renderHome) },
  { id: 'archive', label: '아카이브', icon: '📚', load: () => import('./tabs/archive').then((m) => m.renderArchive) },
  { id: 'stats', label: '통계', icon: '📊', load: () => import('./tabs/stats').then((m) => m.renderStats) },
  { id: 'settings', label: '설정', icon: '⚙️', load: () => import('./tabs/settings').then((m) => m.renderSettings) },
  { id: 'missions', label: '미션', icon: '🎯', load: () => import('./tabs/missions').then((m) => m.renderMissions) },
];

export function mountNav(): void {
  const nav = qs<HTMLElement>('#bottomNav');
  nav.replaceChildren();
  for (const t of TABS) {
    const btn = document.createElement('button');
    btn.type = 'button';
    btn.className = 'nav-item';
    btn.dataset['tabId'] = t.id;

    const icon = document.createElement('span');
    icon.className = 'nav-icon';
    icon.textContent = t.icon;
    btn.append(icon);

    const label = document.createElement('span');
    label.textContent = t.label;
    btn.append(label);

    btn.addEventListener('click', () => { void switchTab(t.id); });
    nav.append(btn);
  }
}

export async function switchTab(id: TabId): Promise<void> {
  const tab = TABS.find((t) => t.id === id);
  if (!tab) throw new Error(`Unknown tab: ${id}`);
  const render = await tab.load();
  const app = qs<HTMLElement>('#app');
  app.replaceChildren();
  render(app);

  const buttons = document.querySelectorAll<HTMLButtonElement>('.nav-item');
  buttons.forEach((b) => {
    b.classList.toggle('active', b.dataset['tabId'] === id);
  });

  document.dispatchEvent(new CustomEvent('dg:nav:tab-changed', { detail: { tab: id } }));
}
```

- [ ] **Step 2: main.ts에서 handler mount도 lazy로 전환**

`src/main.ts`의 정적 import 5개를 제거하고 `bootMainApp`을 async 함수로:

```ts
import { mountNav, switchTab } from './ui/nav';
import { mountSidebar } from './ui/sidebar';
import { mountRewards } from './ui/rewards';
import { renderOnboarding } from './ui/onboarding';
import { maybeShowWelcomeGarden } from './ui/modals/welcome-garden';
import { qs } from './utils/dom';

const USER_STORAGE = 'user';

function hasOnboarded(): boolean {
  try {
    const raw = localStorage.getItem(USER_STORAGE);
    if (!raw) return false;
    const u = JSON.parse(raw) as { interests?: unknown[] };
    return Array.isArray(u.interests) && u.interests.length > 0;
  } catch { return false; }
}

async function bootMainApp(): Promise<void> {
  // v3.29 T3: handler mount lazy — home은 첫 switchTab 직전 eager, 나머지는 사용 시점 lazy
  // (현재 mountHomeHandlers 등이 dg:* 이벤트 listener 등록 → 모두 boot 시점 필요)
  // 따라서 모두 dynamic import 후 await 병렬:
  const [home, archive, stats, missions] = await Promise.all([
    import('./ui/handlers/home'),
    import('./ui/handlers/archive'),
    import('./ui/handlers/stats'),
    import('./ui/handlers/missions'),
  ]);
  home.mountHomeHandlers();
  archive.mountArchiveHandlers();
  stats.mountStatsHandlers();
  missions.mountMissionsHandlers();

  mountNav();
  mountSidebar();
  mountRewards();
  await switchTab('home');
  maybeShowWelcomeGarden();
}

function bootOnboarding(): void {
  const app = qs<HTMLElement>('#app');
  renderOnboarding(app);
  document.addEventListener('dg:onboarded', () => {
    location.reload();
  }, { once: true });
}

function boot(): void {
  if (hasOnboarded()) {
    void bootMainApp();
  } else {
    bootOnboarding();
  }
  registerServiceWorker();
}

function registerServiceWorker(): void {
  if ('serviceWorker' in navigator) {
    navigator.serviceWorker.register('/sw.js').catch((err) => {
      console.warn('SW registration failed', err);
    });
  }
}

if (document.readyState === 'loading') {
  document.addEventListener('DOMContentLoaded', boot);
} else {
  boot();
}
```

- [ ] **Step 3: vite.config.ts manualChunks 보강 (vendor split)**

`vite.config.ts` `build` 블록 교체:

```ts
import { defineConfig } from 'vite';

export default defineConfig({
  build: {
    target: 'es2020',
    sourcemap: true,
    rollupOptions: {
      output: {
        manualChunks: {
          // v3.29 T3: vendor split — 자주 변경되지 않는 외부 lib 분리 (cache 효율)
          // 현재 외부 deps 없음 (Vite SPA + raw TS) — placeholder, 향후 추가 시 활성
        },
      },
    },
  },
  test: {
    environment: 'jsdom',
    include: ['tests/**/*.spec.ts'],
    exclude: ['tests/smoke/**'],
    env: {
      TZ: 'Asia/Seoul',
    },
  },
});
```

(현재 외부 deps 없어서 manualChunks placeholder. Real win은 `await import()` 자체.)

- [ ] **Step 4: dynamic tab smoke spec 신설**

`tests/smoke/dynamic-tab-load.smoke.ts` 신설:

```ts
import { test, expect } from '@playwright/test';

// v3.29 T3: tab dynamic import 회귀 — first-mount FOUC 0 + chunk 로드 검증
test.describe('v3.29 T3 dynamic tab load', () => {
  test('cold-start home → archive → stats — 각 navigate 후 < 500ms 내 render', async ({ page }) => {
    await page.goto('/');
    // home 첫 render 확인
    await expect(page.locator('#app')).toBeVisible({ timeout: 5000 });

    // archive tab click → 500ms 안에 archive-search 입력란 등장
    await page.locator('[data-tab-id="archive"]').click();
    await expect(page.locator('input[type="search"]').first()).toBeVisible({ timeout: 500 });

    // stats tab click → 500ms 안에 .stats-panel 등 등장
    await page.locator('[data-tab-id="stats"]').click();
    await expect(page.locator('[class*="stats"]').first()).toBeVisible({ timeout: 500 });
  });
});
```

(셀렉터는 실제 DOM 구조 따라 적응 — `npm run test:smoke -- --headed`로 1회 visual check.)

- [ ] **Step 5: build + smoke + 양쪽 TZ vitest 확인**

```bash
npm run build 2>&1 | tail -10
```

Expected: build 성공, `dist/assets/`에 tab별 chunk 5개 추가 (`home-*.js`, `archive-*.js`, `stats-*.js`, `settings-*.js`, `missions-*.js`).

```bash
npm run test:smoke
```

Expected: 97 + 신규 1 = 98 PASS (cold-start home → archive → stats).

```bash
npm test 2>&1 | tail -5 && TZ=America/New_York npm test 2>&1 | tail -5
```

Expected: vitest 1237 PASS 양쪽 TZ (T3는 vitest count 영향 없음 — smoke만 +1).

- [ ] **Step 6: bundle 측정 (T3 직후 중간 gate — R4 trigger)**

```bash
gzip -c dist/assets/index-*.js | wc -c
```

목표: ≤ 42000 B (T3 단독으로 −800~−1500 B trim 집계).

**R4 trigger**: 측정 결과 > 42178 B (즉 Δ > −600 B)면 T4/T5 ROI 재투자 결정 필요 — retro에 명시 후 v3.30 carry.

- [ ] **Step 7: commit (trim 집계)**

```bash
git add src/main.ts src/ui/nav.ts vite.config.ts tests/smoke/dynamic-tab-load.smoke.ts
git commit -m "$(cat <<'EOF'
refactor(v3.29 T3): tab 렌더러 진짜 dynamic import (Codex 사전 P1-3 / R4 P1)

- nav.ts TABS render → load: () => Promise<render> (await import())
- main.ts handler mount Promise.all로 async 전환 (boot 시점 listener 등록 보존)
- vite.config.ts manualChunks placeholder (vendor split 향후 확장)
- smoke 신규: dynamic-tab-load.smoke.ts — cold home→archive→stats navigate

trim 집계 (target Δ ≤ −1448 B 중 T3 단독 −800~−1500 B 예상).
R4 P1: T3 후 bundle > 42178 B면 T4/T5 ROI 재투자.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: F4 — CSS unused token / `@layer base` purge (trim safety)

**Files:**
- Modify: `src/styles/tokens.css` / `base.css` / `utilities.css` 외 unused token
- Test: (CSS는 spec 직접 없음 — smoke + visual로 회귀 검증)

- [ ] **Step 1: 현재 CSS 토큰 사용 빈도 grep**

```bash
grep -rhoE -- '--[a-z][a-z0-9-]+' src/styles | sort -u > /tmp/v329-tokens-defined.txt
grep -rhoE -- 'var\(--[a-z][a-z0-9-]+' src/{styles,ui,utils} src/*.ts | sort -u | sed 's/var(//' > /tmp/v329-tokens-used.txt
comm -23 /tmp/v329-tokens-defined.txt /tmp/v329-tokens-used.txt
```

Expected: 미사용 토큰 후보 N개 출력.

- [ ] **Step 2: 후보 검증 — false-positive 제거 (dynamic class / 조건부 사용 확인)**

각 후보 토큰에 대해 `grep -rn '<token>' src` — `@media` / `:hover` / pseudo-class에서 동적으로 쓰일 수 있으므로 confirmed-unused만 후보 유지. **false-positive 1건이라도 있으면 후보 제거**.

- [ ] **Step 3: confirmed-unused token 제거 + build**

해당 CSS 파일들에서 토큰 정의 라인 삭제. 그 후:

```bash
npm run build && gzip -c dist/assets/index-*.js | wc -c
```

Expected: T3 baseline 대비 Δ −150~−400 B.

- [ ] **Step 4: smoke + 양쪽 TZ vitest 회귀 확인**

```bash
npm run test:smoke && npm test 2>&1 | tail -3 && TZ=America/New_York npm test 2>&1 | tail -3
```

Expected: smoke 98 PASS / vitest 1237 양쪽 TZ.

- [ ] **Step 5: commit (trim 집계)**

```bash
git add src/styles/
git commit -m "$(cat <<'EOF'
refactor(v3.29 T4): CSS unused token 2nd-pass purge

- src/styles/*.css에서 confirmed-unused CSS 변수 N개 제거
- grep 2-pass (정의 vs var() 호출) — false-positive 0 확인 후 적용

trim 집계 (Δ −150~−400 B 예상).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

(N과 actual Δ는 측정 결과에 맞춰 commit message 갱신.)

---

## Task 5: F5 — dead import + side-effect import 2nd-pass (trim safety)

**Files:**
- Modify: `src/**/*.ts` (TS6133 + side-effect import만 신중하게 제거)

- [ ] **Step 1: TS strict TS6133 사전 점검**

```bash
npx tsc --noEmit 2>&1 | grep -E 'TS6133|TS6196' | head -20
```

Expected: dead identifier 0 또는 소수. TS6133이 있으면 첫 후보.

- [ ] **Step 2: side-effect import 수동 grep**

```bash
grep -rnE "^import '.*';" src --include='*.ts' | grep -v '\.css'
```

각 line에 대해 해당 모듈이 side-effect 등록(`addEventListener`, polyfill 등)을 하는지 확인. **불확실하면 보존**.

- [ ] **Step 3: confirmed-dead import 제거 + build**

각 dead import line 삭제. 그 후:

```bash
npm run build && gzip -c dist/assets/index-*.js | wc -c
```

Expected: T4 baseline 대비 Δ −50~−200 B.

- [ ] **Step 4: 3종 검증 + 양쪽 TZ + smoke**

```bash
npx tsc --noEmit && npm run lint && npm test 2>&1 | tail -3 && TZ=America/New_York npm test 2>&1 | tail -3 && npm run test:smoke
```

Expected: TS strict 0 / lint baseline 30 / vitest 1237 양쪽 TZ / smoke 98.

- [ ] **Step 5: commit (trim 집계)**

```bash
git add src/
git commit -m "$(cat <<'EOF'
refactor(v3.29 T5): dead import + side-effect import 2nd-pass sweep

- TS strict TS6133 후속 + side-effect import 수동 검증 0 false-positive
- closeModal precedent (v3.27 T8) 확산

trim 집계 (Δ −50~−200 B 예상).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: 3종 검증 + 양쪽 TZ + smoke + bundle 최종 측정 + Codex 최종 review

**Files:** (검증만, code 변경 0)

- [ ] **Step 1: 3종 검증 일괄**

```bash
npx tsc --noEmit && npm run lint && npm test && TZ=America/New_York npm test && npm run test:smoke
```

Expected: TS 0 errors / lint baseline 30 / vitest **1237 PASS 양쪽 TZ** / smoke **98 PASS**.

- [ ] **Step 2: cold-start smoke N=3 (R1 mitigation 검증)**

```bash
for i in 1 2 3; do npm run test:smoke 2>&1 | tail -3; done
```

Expected: 3회 모두 98 PASS (flake 0). T3 dynamic import FOUC 회귀 없음.

- [ ] **Step 3: bundle 최종 측정 (절대 DoD gate)**

```bash
npm run build && gzip -c dist/assets/index-*.js | wc -c
```

Expected: ≤ **41500 B** (GREEN strict).

- 통과: Codex 최종 review 진입
- 미달: retro에 R4 사유 명시 + v3.30 carry → Codex 최종 review 시 AMBER 보고

- [ ] **Step 4: Codex 최종 review dispatch (`--resume` thread)**

`codex:codex-rescue` agent dispatch:
- thread: `a1b1baad858237076` (사전 review에서 resume)
- 입력: T1~T5 commit 5개 diff summary + 3종 검증 결과 + bundle 최종값
- 요청: `DEPLOY_APPROVED` / `APPROVED_WITH_NOTES` / `REJECT` 분류

CLI 한도 시 fallback: `pr-review-toolkit:code-reviewer` agent (controller self-review, v3.14.5/v3.20/v3.24 precedent).

- [ ] **Step 5: T6 verification commit (docs-only)**

```bash
git commit --allow-empty -m "$(cat <<'EOF'
docs(v3.29 T6): 사이클 끝 검증 통과 — Codex 최종 review dispatch

3종 양쪽 TZ:
- npx tsc --noEmit: 0 errors
- npm run lint: baseline 30 (v3.27 잔존)
- vitest KST/NY: 1237/1237 PASS (1224 baseline + T1 3 + T2 10)
- smoke: 98/98 PASS (N=3 cold-start 0 flake)
- bundle CLI gzip: 42778 → <FINAL> B (Δ <DELTA> B, GREEN <STATUS>)

Codex 최종 review (--resume a1b1baad858237076): <RESULT>

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

(`<FINAL>`, `<DELTA>`, `<STATUS>`, `<RESULT>`는 실측 후 채움.)

---

## Task 7: Retro + memory + Obsidian vault sync + (배포해줘 시) firebase deploy

**Files:**
- Modify: `docs/superpowers/specs/2026-05-12-v3.29-cleanup-trim-design.md` (§7 Retro append)
- Create: `~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v329.md`
- Modify: `~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md` (한 줄 추가)
- Copy to: `/Users/hayden/Hayden/20_Projects/Dev_Project/SJ_Daily Growth(Ai assistant)/02-Cycles/` + `03-Lessons/`

- [ ] **Step 1: spec §7 Retro append**

`docs/superpowers/specs/2026-05-12-v3.29-cleanup-trim-design.md`의 `## §7. Retro (T7 사이클 완료 시 append)` 섹션을 작성:
- 7.1 사이클 요약 (Task / Commit / vitest / smoke / TS / lint / bundle / Codex 결과)
- 7.2 DoD 평가 (절대 41500 / 트림 집계 −1448 B 충족 여부)
- 7.3 핵심 변경 (T1~T5 commit hash 표)
- 7.4 신규 lessons (L2.2 graduate 후보 등)
- 7.5 v3.30 carry-forward (R4 미달 시 잔여 + 검색 fuzzy + Plant action + ...)

- [ ] **Step 2: memory file 신설**

`~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v329.md` 신설 (frontmatter + 한 줄 요약).

- [ ] **Step 3: MEMORY.md 한 줄 추가**

`~/.claude/.../memory/MEMORY.md`에 한 줄 entry 추가 (200자 이하).

- [ ] **Step 4: Obsidian vault sync (단방향, 신규 파일만)**

```bash
VAULT="/Users/hayden/Hayden/20_Projects/Dev_Project/SJ_Daily Growth(Ai assistant)"
cp -n docs/superpowers/specs/2026-05-12-v3.29-cleanup-trim-design.md "$VAULT/02-Cycles/"
cp -n ~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v329.md "$VAULT/03-Lessons/"
cp ~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md "$VAULT/03-Lessons/MEMORY.md"
```

(`cp -n` 신규만, 기존 retro 덮어쓰기 X. `MEMORY.md`만 덮어쓰기 OK — index 파일.)

- [ ] **Step 5: T7 retro commit + (배포해줘 시) deploy**

```bash
git add docs/superpowers/specs/2026-05-12-v3.29-cleanup-trim-design.md
git commit -m "$(cat <<'EOF'
docs(v3.29 T7 retro): 사이클-끝 + Codex 최종 <RESULT> + v3.30 carry

[3-5줄 요약: Task 수 / commit 수 / vitest / smoke / bundle / Codex 결과 / 신규 lessons]

Memory 갱신:
- ~/.claude/.../memory/project_daily_growth_v329.md 신규
- MEMORY.md 한 줄 entry 추가

Obsidian vault sync (Daily Growth):
- 02-Cycles/2026-05-12-v3.29-cleanup-trim-design.md (retro append)
- 03-Lessons/project_daily_growth_v329.md + MEMORY.md

사용자 "배포해줘" 명시 대기 — npm run build && firebase deploy --only hosting.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

사용자 "배포해줘" 명시 시:

```bash
npm run build && firebase deploy --only hosting
```

---

## Self-Review (plan 작성 직후, 2026-05-12)

**1. Spec coverage**:
- F1 → T1 ✅ (already-v8 backfill + same-reference fast-path + 신규 spec 3)
- F2 → T2 ✅ (highlight.ts 신설 + archive 적용 + 10 spec)
- F3 → T3 ✅ (nav.ts dynamic + main.ts async + vite manualChunks + smoke spec)
- F4 → T4 ✅ (CSS purge 5 step)
- F5 → T5 ✅ (dead import + side-effect 5 step)
- §3 DoD 모두 → T6 검증 ✅
- §7 Retro → T7 ✅
- Codex 사전 P1×3 모두 흡수 → 각 task에 "Codex 흡수" 명시 ✅
- §5.1 self-review L2.2 lesson 후보 → T7 §7.4 신규 lessons에서 graduate 검토 ✅

**2. Placeholder scan**:
- T4 Step 5 commit message 안의 `N과 actual Δ` 갱신 — 측정 결과 채움 명시
- T6 Step 5 commit message 안의 `<FINAL>`, `<DELTA>`, `<STATUS>`, `<RESULT>` — 실측 후 채움 명시
- T7 Step 5 commit message 안의 `[3-5줄 요약]` + `<RESULT>` — 실측 후 채움 명시
- 모두 의도된 placeholder (실측 의존) — 명시 OK.

**3. Type consistency**:
- T3 nav.ts `TabDef.load: () => Promise<(c: HTMLElement) => void>` — switchTab에서 `await tab.load()` → render(app) 호출 일관 ✅
- T1 `migrateUserToV8` return type unchanged (`User`) — 기존 chain superset 단언 영향 0 ✅
- T2 `highlightHtml(text, query): string` — archive.ts `innerHTML =` 사용 일관 ✅

**Fix inline**: 없음. 명시 placeholder만 존재.

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-05-12-v3.29-cleanup-trim-plan.md`. Two execution options:**

1. **Subagent-Driven (recommended)** — 각 task 별 fresh subagent dispatch + per-task review (T2 helper / T3 dynamic import은 reviewer agent 추가). 본 프로젝트의 표준 워크플로.
2. **Inline Execution** — 본 세션에서 batch 실행, T6 전 checkpoint review.

**어느 쪽으로 진행할까요?**
