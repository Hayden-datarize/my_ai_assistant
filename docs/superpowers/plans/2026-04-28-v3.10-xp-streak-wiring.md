# v3.10 — XP/Streak Wiring 정리 (Implementation Plan)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Production에서 한 번도 작동하지 않은 streak 카운터를 살리고, 데드 코드 `recordActivity` + `checkAndUpdateStreak`를 단일 atomic primitive `recordDailyAnswer(xpDelta)`로 통합한 뒤 `applyAnswerActivity` (handlers/home.ts)에 wiring한다. v3.7 P2-3 (rollback xp on save fail)는 atomic single-write로 자연 해소.

**Architecture:** state/user.ts SSOT 원칙. 두 데드 함수를 삭제하고, 한 번의 `saveUser` 호출로 streak 전이 + XP/level 누적 + lastActiveDate 갱신을 처리하는 `recordDailyAnswer`를 추가. UI 레이어 `applyAnswerActivity`는 try/catch + toast만 가진 얇은 wrapper로 축소. 시그니처를 무인자로 변경해 caller의 stale user 참조 패턴 자연 제거.

**Tech Stack:** TypeScript / Vite SPA / Vitest (jsdom) / Vanilla DOM (no framework) / Firebase Hosting / `vi.setSystemTime` for date mocking.

**Spec:** [docs/superpowers/specs/2026-04-28-v3.10-xp-streak-wiring-design.md](../specs/2026-04-28-v3.10-xp-streak-wiring-design.md)

---

## File Structure

| File | Action | Responsibility |
|---|---|---|
| `src/state/user.ts` | Modify | `recordActivity` + `checkAndUpdateStreak` 삭제, `recordDailyAnswer(xpDelta: number): void` 추가 (atomic single-write), `getSaveErrorMessage` JSDoc 업데이트 |
| `src/ui/handlers/home.ts` | Modify | `applyAnswerActivity` 시그니처 `(user: LegacyUser): void` → `(): void` + 본문 축소, line 707-712 호출부 갱신, line 31 import 정리 |
| `tests/unit/state-user.spec.ts` | Modify | 10 → 13 it-block (4 dead-function 테스트 → recordDailyAnswer 4건 대체, 3 신규: 첫 사용자/null 가드/throw propagate, 6 보존) |
| `tests/unit/home-answer-save-error.spec.ts` | Modify | 3 → 4 it-block (무인자 호출로 갱신 + "save fail 시 localStorage 무변경" 신규 1건) |
| `tests/unit/home-streak-integration.spec.ts` | Create | 4 it-block — Day1 첫 답변 / Day2 연속 / Day4 끊김 / 같은 날 재제출 with `vi.setSystemTime` |

**기존 호출처 grep 확정** (T2 작업 전 재확인 필요):

- `applyAnswerActivity` 사용처 = `src/ui/handlers/home.ts:710` (submitAnswer 내부) + `tests/unit/home-answer-save-error.spec.ts` (3 it-block)
- `recordActivity` / `checkAndUpdateStreak` 사용처 = `tests/unit/state-user.spec.ts` 외 0건 (`git grep` 확인 완료)
- `LegacyUser` 사용처 = `src/ui/handlers/home.ts:31` import + `:72` 시그니처. 다른 모듈 0건
- `saveUser` 사용처 = `src/ui/handlers/home.ts:77` (applyAnswerActivity 내부) + 다른 import 위치들 (ex: modals/interests). 본 파일 단독 사용은 :77만이며 T2 후 삭제 가능

---

## Task 0: Spec + Plan 커밋

**Files:**

- Already created: `docs/superpowers/specs/2026-04-28-v3.10-xp-streak-wiring-design.md`
- Already created: `docs/superpowers/plans/2026-04-28-v3.10-xp-streak-wiring.md`

### Why this task

직배포 패턴 (origin remote 미등록) 일관 + v3.4~v3.9 cycle convention 따라 spec + plan을 단일 커밋으로 시작점 마킹. 본 commit은 docs only — implementation 0 LoC.

### Steps

- [ ] **Step 0.1**: 신규 spec/plan 파일 staging

  ```bash
  git add docs/superpowers/specs/2026-04-28-v3.10-xp-streak-wiring-design.md \
          docs/superpowers/plans/2026-04-28-v3.10-xp-streak-wiring.md
  ```

- [ ] **Step 0.2**: feature 브랜치 생성 (직배포 패턴, no remote tracking)

  ```bash
  git checkout -b feat/v3.10-xp-streak-wiring
  ```

  Expected: `Switched to a new branch 'feat/v3.10-xp-streak-wiring'`

- [ ] **Step 0.3**: 단일 docs 커밋

  ```bash
  git commit -m "docs(v3.10): add spec + plan for XP/streak wiring"
  ```

  Expected: 2 files changed (spec + plan)

---

## Task 1: `state/user.ts` — `recordDailyAnswer` 도입 + 데드 함수 삭제

**Files:**

- Modify: `src/state/user.ts:40-68` (recordActivity + checkAndUpdateStreak 삭제), `:86-88` (JSDoc 업데이트)
- Test: `tests/unit/state-user.spec.ts` (10 → 13 it-block)

### Why this task

`recordActivity` + `checkAndUpdateStreak` 두 함수는 production 호출처가 0건인 데드 코드. 두 함수를 별개로 wiring하면 saveUser가 2회 호출되어 partial-state 부정합 가능. 단일 `recordDailyAnswer(xpDelta)`로 통합하면 (1) atomic single-write 보장, (2) v3.7 P2-3 자연 해소, (3) 향후 caller가 호출 순서를 신경 쓸 필요 없음.

### Steps

- [ ] **Step 1.1**: 기존 호출처 final grep으로 안전성 재확인

  ```bash
  git grep -nE "recordActivity|checkAndUpdateStreak" -- 'src/' ':!src/state/user.ts'
  ```

  Expected: 0 hits. 1건이라도 나오면 멈추고 해당 모듈 검토 필요(spec 가정 위반).

- [ ] **Step 1.2**: 기존 vitest 8 streak/recordActivity 관련 테스트가 새 시나리오로 어떻게 매핑되는지 확인하기 위해 현재 spec read

  ```bash
  sed -n '1,70p' tests/unit/state-user.spec.ts
  ```

  대체 매핑:
  - `recordActivity increments xp and levels up per 100 xp` → `recordDailyAnswer increments xp and levels up per 100 xp` (signature만 변경, assertion 동일)
  - `checkAndUpdateStreak bumps streak when last active was yesterday` → `recordDailyAnswer bumps streak when last active was yesterday`
  - `checkAndUpdateStreak resets to 1 when last active older than 1 day` → `recordDailyAnswer resets streak to 1 after gap`
  - `checkAndUpdateStreak is idempotent when already ran today` → `recordDailyAnswer keeps streak idempotent on same-day reinvoke (xp still accumulates)`

- [ ] **Step 1.3**: 실패하는 테스트 먼저 작성 — `tests/unit/state-user.spec.ts` 전면 재작성 (TDD red 단계)

  ```typescript
  import { describe, it, expect, beforeEach, vi } from 'vitest';
  import { loadUserData, saveUser, recordDailyAnswer, getSaveErrorMessage } from '../../src/state/user';
  import { MSG } from '../../src/ui/messages';

  describe('state/user', () => {
    beforeEach(() => localStorage.clear());

    it('loadUserData returns null when none saved', () => {
      expect(loadUserData()).toBeNull();
    });

    it('saveUser and loadUserData roundtrip', () => {
      const u = { name: 'H', interests: ['ai_ml'], onboardedAt: '2026-04-19', streak: 0, lastActiveDate: '', xp: 0, level: 1 };
      saveUser(u);
      expect(loadUserData()).toEqual(u);
    });

    it('recordDailyAnswer increments xp and levels up per 100 xp', () => {
      vi.setSystemTime(new Date('2026-04-19'));
      saveUser({ name: 'H', interests: [], onboardedAt: '', streak: 0, lastActiveDate: '2026-04-19', xp: 90, level: 1 });
      recordDailyAnswer(20);
      const u = loadUserData()!;
      expect(u.xp).toBe(110);
      expect(u.level).toBe(2);
    });

    it('recordDailyAnswer bumps streak when last active was yesterday', () => {
      vi.setSystemTime(new Date('2026-04-19'));
      saveUser({ name: 'H', interests: [], onboardedAt: '', streak: 3, lastActiveDate: '2026-04-18', xp: 0, level: 1 });
      recordDailyAnswer(10);
      expect(loadUserData()!.streak).toBe(4);
    });

    it('recordDailyAnswer resets streak to 1 after gap', () => {
      vi.setSystemTime(new Date('2026-04-19'));
      saveUser({ name: 'H', interests: [], onboardedAt: '', streak: 3, lastActiveDate: '2026-04-15', xp: 0, level: 1 });
      recordDailyAnswer(10);
      expect(loadUserData()!.streak).toBe(1);
    });

    it('recordDailyAnswer keeps streak idempotent on same-day reinvoke (xp still accumulates)', () => {
      vi.setSystemTime(new Date('2026-04-19'));
      saveUser({ name: 'H', interests: [], onboardedAt: '', streak: 5, lastActiveDate: '2026-04-19', xp: 20, level: 1 });
      recordDailyAnswer(10);
      const u = loadUserData()!;
      expect(u.streak).toBe(5);
      expect(u.xp).toBe(30);
    });

    it('recordDailyAnswer starts streak at 1 for fresh user (lastActiveDate="")', () => {
      vi.setSystemTime(new Date('2026-04-19'));
      saveUser({ name: 'H', interests: [], onboardedAt: '2026-04-19', streak: 0, lastActiveDate: '', xp: 0, level: 1 });
      recordDailyAnswer(10);
      const u = loadUserData()!;
      expect(u.streak).toBe(1);
      expect(u.lastActiveDate).toBe('2026-04-19');
    });

    it('recordDailyAnswer no-ops when no user cached', () => {
      vi.setSystemTime(new Date('2026-04-19'));
      // localStorage empty (beforeEach)
      expect(() => recordDailyAnswer(10)).not.toThrow();
      expect(loadUserData()).toBeNull();
    });

    it('recordDailyAnswer propagates DOMException when setItem throws QuotaExceededError', () => {
      vi.setSystemTime(new Date('2026-04-19'));
      saveUser({ name: 'H', interests: [], onboardedAt: '', streak: 0, lastActiveDate: '', xp: 0, level: 1 });
      const spy = vi.spyOn(Storage.prototype, 'setItem').mockImplementationOnce(() => {
        throw new DOMException('quota', 'QuotaExceededError');
      });
      expect(() => recordDailyAnswer(10)).toThrow(DOMException);
      spy.mockRestore();
    });

    it('getSaveErrorMessage returns quota message for QuotaExceededError', () => {
      const err = new DOMException('quota', 'QuotaExceededError');
      expect(getSaveErrorMessage(err)).toBe(MSG.SAVE_QUOTA_EXCEEDED);
    });

    it('getSaveErrorMessage returns fallback message for generic Error', () => {
      const err = new Error('boom');
      expect(getSaveErrorMessage(err)).toBe(MSG.SAVE_FAILED);
    });

    it('getSaveErrorMessage returns fallback message for string error', () => {
      expect(getSaveErrorMessage('some string')).toBe(MSG.SAVE_FAILED);
    });

    it('saveUser throws DOMException when setItem throws QuotaExceededError', () => {
      const spy = vi.spyOn(Storage.prototype, 'setItem').mockImplementationOnce(() => {
        throw new DOMException('quota', 'QuotaExceededError');
      });
      const u = { name: 'H', interests: [], onboardedAt: '', streak: 0, lastActiveDate: '', xp: 0, level: 1 };
      expect(() => saveUser(u)).toThrow(DOMException);
      spy.mockRestore();
    });
  });
  ```

  주의: 1.7번 spec(`recordDailyAnswer no-ops`)에서 `lastActiveDate=''` 같은 값이 정상 시작 케이스라 `vi.setSystemTime`은 보존(특정 날짜 고정으로 결정론).

- [ ] **Step 1.4**: 테스트 실행해 RED 확인

  ```bash
  npm test -- state-user.spec.ts
  ```

  Expected: import resolution failure or 8건 fail with `recordDailyAnswer is not a function` / 새 it-block 5건 fail. (정확한 메시지는 vitest 출력 참조.)

- [ ] **Step 1.5**: `src/state/user.ts` 수정 — 데드 함수 2개 삭제 + `recordDailyAnswer` 추가 + JSDoc 업데이트

  파일 전체를 다음으로 교체:

  ```typescript
  import { getDateStr } from '../utils/dates';
  import { MSG } from '../ui/messages';

  export interface User {
    name: string;
    interests: string[];
    onboardedAt: string;
    streak: number;
    lastActiveDate: string;
    xp: number;
    level: number;
  }

  /** home.ts / stats.ts 레거시 호환 alias */
  export type LegacyUser = User;

  const KEY = 'user';

  export function getCachedUser(): LegacyUser | null {
    try {
      const raw = localStorage.getItem(KEY);
      return raw ? (JSON.parse(raw) as LegacyUser) : null;
    } catch {
      return null;
    }
  }

  export function loadUserData(): User | null {
    return getCachedUser();
  }

  /**
   * 사용자 데이터를 localStorage에 저장한다.
   * @throws {DOMException} 저장 공간 초과 시 (QuotaExceededError)
   */
  export function saveUser(u: User): void {
    localStorage.setItem(KEY, JSON.stringify(u));
  }

  /**
   * 답변 제출 1회의 사용자 활동을 atomic single-write로 기록한다.
   * - streak: 어제 활동했으면 +1, 아니면 1로 리셋. 같은 날 재제출은 idempotent.
   * - xp: xpDelta 누적. level = 1 + floor(xp / 100).
   * - lastActiveDate: 오늘로 갱신.
   *
   * 사용자 데이터가 없으면 silent return.
   *
   * @throws {DOMException} 저장 공간 초과 시 (QuotaExceededError) — caller에서 처리
   */
  export function recordDailyAnswer(xpDelta: number): void {
    const u = loadUserData();
    if (!u) return;
    const today = getDateStr();

    if (u.lastActiveDate !== today) {
      const y = new Date(today);
      y.setDate(y.getDate() - 1);
      const yesterday = getDateStr(y);
      u.streak = u.lastActiveDate === yesterday ? u.streak + 1 : 1;
    }

    u.xp += xpDelta;
    u.level = 1 + Math.floor(u.xp / 100);
    u.lastActiveDate = today;

    saveUser(u);
  }

  export function updateGreeting(rootId = 'greeting'): void {
    const el = document.getElementById(rootId);
    const u = loadUserData();
    if (!el || !u) return;
    const h = new Date().getHours();
    const period = h < 5 ? '늦은 밤' : h < 12 ? '좋은 아침' : h < 18 ? '좋은 오후' : '좋은 저녁';
    el.textContent = `${period}, ${u.name}`;
  }

  export function updateStreakBanner(rootId = 'streakBanner'): void {
    const el = document.getElementById(rootId);
    const u = loadUserData();
    if (!el || !u) return;
    el.textContent = u.streak > 0 ? `🔥 ${u.streak}일 연속 성장 중` : '오늘부터 다시 시작해봐요';
  }

  /**
   * saveUser / recordDailyAnswer 에서 던져진 에러를
   * 사용자 표시용 메시지로 변환한다 (단일 진입점).
   */
  export function getSaveErrorMessage(err: unknown): string {
    if (err instanceof DOMException && err.name === 'QuotaExceededError') {
      return MSG.SAVE_QUOTA_EXCEEDED;
    }
    return MSG.SAVE_FAILED;
  }
  ```

- [ ] **Step 1.6**: 테스트 실행해 GREEN 확인

  ```bash
  npm test -- state-user.spec.ts
  ```

  Expected: 13 it-block 모두 PASS.

- [ ] **Step 1.7**: 빌드/타입체크/lint 회귀 확인

  ```bash
  npm run lint && npx tsc --noEmit
  ```

  Expected: 0 error. 만약 다른 파일이 `recordActivity` / `checkAndUpdateStreak` import한다면 여기서 type error 발생 (Step 1.1 grep으로 사전 차단했지만 안전망).

- [ ] **Step 1.8**: 전체 회귀 테스트 (T2 진입 전 baseline 확인)

  ```bash
  npm test
  ```

  Expected: ~408 PASS (405 baseline + 3 신규 — 단, 이때 home-answer-save-error.spec은 아직 갱신 전이라 그대로 PASS, T2에서 갱신).

- [ ] **Step 1.9**: 커밋

  ```bash
  git add src/state/user.ts tests/unit/state-user.spec.ts
  git commit -m "feat(v3.10 T1): replace dead recordActivity/checkAndUpdateStreak with atomic recordDailyAnswer"
  ```

---

## Task 2: `handlers/home.ts` — `applyAnswerActivity` 축소 + submitAnswer 호출부 갱신

**Files:**

- Modify: `src/ui/handlers/home.ts:31` (import line), `:62-81` (applyAnswerActivity 함수), `:707-712` (submitAnswer 호출부)
- Test: `tests/unit/home-answer-save-error.spec.ts` (3 → 4 it-block)

### Why this task

T1에서 추가한 `recordDailyAnswer`를 production wiring. 시그니처를 무인자로 변경해 caller의 stale `user` reference 문제를 자연 제거. 본 단계로 production streak 카운터가 처음으로 작동하기 시작한다.

### Steps

- [ ] **Step 2.1**: 기존 `home-answer-save-error.spec.ts` 갱신 — 무인자 호출 + "save fail 시 localStorage 무변경" 신규 1건 추가 (TDD red — 먼저 변경)

  파일 전체를 다음으로 교체:

  ```typescript
  import { describe, it, expect, beforeEach, vi, afterEach } from 'vitest';

  describe('home — applyAnswerActivity 에러 처리', () => {
    beforeEach(() => {
      localStorage.clear();
      document.body.replaceChildren();
      const root = document.createElement('div');
      root.id = 'modalRoot';
      document.body.appendChild(root);
      vi.setSystemTime(new Date('2026-04-19'));
    });

    afterEach(() => {
      vi.restoreAllMocks();
      vi.useRealTimers();
    });

    it('shows quota toast when localStorage rejects with QuotaExceededError', async () => {
      const user = {
        name: 'H', interests: ['ai_ml'], onboardedAt: '2026-04-19',
        streak: 0, lastActiveDate: '', xp: 0, level: 1,
      };
      localStorage.setItem('user', JSON.stringify(user));

      vi.spyOn(Storage.prototype, 'setItem').mockImplementation((key) => {
        if (key === 'user') throw new DOMException('quota', 'QuotaExceededError');
      });

      const { applyAnswerActivity } = await import('../../src/ui/handlers/home');
      applyAnswerActivity();

      const toast = document.querySelector('.toast');
      expect(toast?.textContent).toContain('저장 공간이 가득 찼어요');
    });

    it('shows fallback toast for generic save error', async () => {
      const user = {
        name: 'H', interests: ['ai_ml'], onboardedAt: '2026-04-19',
        streak: 0, lastActiveDate: '', xp: 0, level: 1,
      };
      localStorage.setItem('user', JSON.stringify(user));

      vi.spyOn(Storage.prototype, 'setItem').mockImplementation((key) => {
        if (key === 'user') throw new Error('disk full');
      });

      const { applyAnswerActivity } = await import('../../src/ui/handlers/home');
      applyAnswerActivity();

      const toast = document.querySelector('.toast');
      expect(toast?.textContent).toContain('저장하지 못했어요');
    });

    it('does not throw when saveUser fails — caller flow continues', async () => {
      const user = {
        name: 'H', interests: ['ai_ml'], onboardedAt: '2026-04-19',
        streak: 0, lastActiveDate: '', xp: 0, level: 1,
      };
      localStorage.setItem('user', JSON.stringify(user));

      vi.spyOn(Storage.prototype, 'setItem').mockImplementation((key) => {
        if (key === 'user') throw new DOMException('quota', 'QuotaExceededError');
      });

      const { applyAnswerActivity } = await import('../../src/ui/handlers/home');
      expect(() => applyAnswerActivity()).not.toThrow();
    });

    it('does not modify localStorage when saveUser fails (atomic single-write)', async () => {
      const user = {
        name: 'H', interests: ['ai_ml'], onboardedAt: '2026-04-19',
        streak: 3, lastActiveDate: '2026-04-15', xp: 50, level: 1,
      };
      const original = JSON.stringify(user);
      localStorage.setItem('user', original);

      vi.spyOn(Storage.prototype, 'setItem').mockImplementation((key, value) => {
        if (key === 'user' && value !== original) {
          throw new DOMException('quota', 'QuotaExceededError');
        }
      });

      const { applyAnswerActivity } = await import('../../src/ui/handlers/home');
      applyAnswerActivity();

      // mock은 첫 변경 시도(recordDailyAnswer 내부 saveUser)만 throw — 토스트 표시 후 localStorage는 original 그대로
      expect(localStorage.getItem('user')).toBe(original);
    });
  });
  ```

- [ ] **Step 2.2**: 테스트 실행해 RED 확인

  ```bash
  npm test -- home-answer-save-error.spec.ts
  ```

  Expected: TS error or runtime fail — `applyAnswerActivity()`는 `(user: LegacyUser): void` 시그니처라 무인자 호출 시 `Argument of type ... missing` type error.

- [ ] **Step 2.3**: `src/ui/handlers/home.ts` line 31 import 정리

  Before:

  ```typescript
  import { getCachedUser, saveUser, getSaveErrorMessage, type LegacyUser } from '../../state/user';
  ```

  After:

  ```typescript
  import { getCachedUser, getSaveErrorMessage, recordDailyAnswer } from '../../state/user';
  ```

  변경:
  - `saveUser` 삭제 (T2 후 미사용)
  - `LegacyUser` 삭제 (T2 후 미사용)
  - `recordDailyAnswer` 추가
  - `getCachedUser`, `getSaveErrorMessage` 유지

- [ ] **Step 2.4**: `applyAnswerActivity` 함수 본문 + 시그니처 교체 (line 62-81)

  Before:

  ```typescript
  /**
   * 답변 제출 후 사용자 활동(XP/level/lastActiveDate) 적용 + persist.
   * `saveUser` 실패 시 분기 토스트 표시 후 silent return — UI/메모리 상태는
   * 이미 변경되어 있으므로 후속 흐름은 그대로 진행된다 (chat bubble 등).
   *
   * 새로고침 시 persist 안 된 변경은 사라진다는 사실은 토스트가 안내.
   *
   * @internal
   */
  export function applyAnswerActivity(user: LegacyUser): void {
    user.xp += 10;
    user.level = 1 + Math.floor(user.xp / 100);
    user.lastActiveDate = getDateStr();
    try {
      saveUser(user);
    } catch (e) {
      showToast(getSaveErrorMessage(e));
    }
  }
  ```

  After:

  ```typescript
  /**
   * 답변 제출 후 사용자 활동(streak + XP/level + lastActiveDate)을 적용한다.
   * 저장 실패 시 분기 토스트 표시 — atomic single-write이므로 in-memory와
   * persisted state 모두 변경되지 않아 별도 rollback이 필요 없다.
   *
   * @internal
   */
  export function applyAnswerActivity(): void {
    try {
      recordDailyAnswer(10);
    } catch (e) {
      showToast(getSaveErrorMessage(e));
    }
  }
  ```

- [ ] **Step 2.5**: submitAnswer 호출부 갱신 (line 707-712)

  Before:

  ```typescript
    // record activity
    const user = getCachedUser();
    if (user) {
      applyAnswerActivity(user);
      hydrateGreetingAndStreak();
    }
  ```

  After:

  ```typescript
    // record daily answer activity (streak + XP)
    applyAnswerActivity();
    hydrateGreetingAndStreak();
  ```

- [ ] **Step 2.5b**: `hydrateGreetingAndStreak` export 추가 (line 300)

  T3에서 통합 테스트가 직접 호출하기 위해. 본 함수는 home.ts 내부 helper지만 streak DOM 검증을 위한 가장 좁은 surface area라 export 한 줄이 가장 보수적.

  Before:

  ```typescript
  function hydrateGreetingAndStreak(): void {
  ```

  After:

  ```typescript
  /** @internal — exported for integration tests; production caller is hydrateHome + submitAnswer 내부 */
  export function hydrateGreetingAndStreak(): void {
  ```

  주의: 이 함수는 `hydrateHome` 내부와 submitAnswer 본문에서도 호출되므로 export 추가는 호환성 깨지지 않음. 기존 호출처 `hydrateHome` 내부 (line 293) + `submitAnswer` (line 711) 둘 다 영향 없음.

- [ ] **Step 2.6**: `getDateStr` import의 사용처 확인 — applyAnswerActivity에서 사용했었으니 다른 곳에서 쓰는지 확인

  ```bash
  git grep -n "getDateStr" src/ui/handlers/home.ts
  ```

  Expected: line 23 import + 여러 사용처 (hydrateBriefings, refreshBriefings, hydrateQuestion, hydrateChatHistory, submitAnswer, sendChatMessage, exportData) — applyAnswerActivity 외에서 사용. import 유지.

- [ ] **Step 2.7**: 테스트 실행해 GREEN 확인

  ```bash
  npm test -- home-answer-save-error.spec.ts
  ```

  Expected: 4 it-block PASS.

- [ ] **Step 2.8**: 빌드/타입체크/lint 회귀 확인

  ```bash
  npm run lint && npx tsc --noEmit
  ```

  Expected: 0 error.

- [ ] **Step 2.9**: 전체 회귀 테스트

  ```bash
  npm test
  ```

  Expected: ~409 PASS (T1 8 baseline + 1 신규 home-answer-save-error). T3 4건은 아직 미작성.

- [ ] **Step 2.10**: 커밋

  ```bash
  git add src/ui/handlers/home.ts tests/unit/home-answer-save-error.spec.ts
  git commit -m "feat(v3.10 T2): wire applyAnswerActivity to recordDailyAnswer (streak now persists)"
  ```

---

## Task 3: `home-streak-integration.spec.ts` — submitAnswer streak 전이 4 시나리오

**Files:**

- Create: `tests/unit/home-streak-integration.spec.ts`

### Why this task

T1 unit 검증은 `recordDailyAnswer`만 검증. T2 wiring 후 `applyAnswerActivity()` 진입점부터 streak/xp/level이 정상 반영되는지 + DOM `#streakCount`/`#xpBadge` 갱신까지 통합 검증. 4 시나리오 (Day1 / 연속 / 끊김 / 같은 날)를 명시적으로 표현.

### Steps

- [ ] **Step 3.1**: spec 파일 신규 작성

  파일 `tests/unit/home-streak-integration.spec.ts`:

  ```typescript
  import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';

  function setupDOM(): void {
    document.body.replaceChildren();
    const root = document.createElement('div');
    root.id = 'modalRoot';
    document.body.appendChild(root);

    // applyAnswerActivity 토스트 컨테이너용 + hydrateGreetingAndStreak 대상
    const greeting = document.createElement('div');
    greeting.id = 'greetingText';
    document.body.appendChild(greeting);

    const streak = document.createElement('span');
    streak.id = 'streakCount';
    streak.textContent = '0';
    document.body.appendChild(streak);

    const xp = document.createElement('span');
    xp.id = 'xpBadge';
    xp.textContent = '0 XP';
    document.body.appendChild(xp);
  }

  function seedUser(overrides: Partial<{ streak: number; lastActiveDate: string; xp: number; level: number }>): void {
    const user = {
      name: 'H',
      interests: ['ai_ml'],
      onboardedAt: '2026-04-15',
      streak: overrides.streak ?? 0,
      lastActiveDate: overrides.lastActiveDate ?? '',
      xp: overrides.xp ?? 0,
      level: overrides.level ?? 1,
    };
    localStorage.setItem('user', JSON.stringify(user));
  }

  describe('submitAnswer 통합 — streak 전이', () => {
    beforeEach(() => {
      localStorage.clear();
      vi.resetModules();
      setupDOM();
    });

    afterEach(() => {
      vi.useRealTimers();
    });

    it('Day 1 첫 답변: lastActiveDate="" → streak 0 → 1, xp 0 → 10', async () => {
      vi.setSystemTime(new Date('2026-04-19'));
      seedUser({ streak: 0, lastActiveDate: '', xp: 0 });

      const { applyAnswerActivity, hydrateGreetingAndStreak } = await import('../../src/ui/handlers/home');
      applyAnswerActivity();
      hydrateGreetingAndStreak();

      expect(document.getElementById('streakCount')?.textContent).toBe('1');
      expect(document.getElementById('xpBadge')?.textContent).toBe('10 XP');
      const persisted = JSON.parse(localStorage.getItem('user')!);
      expect(persisted.lastActiveDate).toBe('2026-04-19');
    });

    it('Day 2 연속 답변: streak 1 → 2, xp 10 → 20', async () => {
      vi.setSystemTime(new Date('2026-04-20'));
      seedUser({ streak: 1, lastActiveDate: '2026-04-19', xp: 10 });

      const { applyAnswerActivity, hydrateGreetingAndStreak } = await import('../../src/ui/handlers/home');
      applyAnswerActivity();
      hydrateGreetingAndStreak();

      expect(document.getElementById('streakCount')?.textContent).toBe('2');
      expect(document.getElementById('xpBadge')?.textContent).toBe('20 XP');
    });

    it('Day 4 끊긴 후 답변: streak 2 → 1 (gap reset)', async () => {
      vi.setSystemTime(new Date('2026-04-22'));
      seedUser({ streak: 2, lastActiveDate: '2026-04-20', xp: 20 });

      const { applyAnswerActivity, hydrateGreetingAndStreak } = await import('../../src/ui/handlers/home');
      applyAnswerActivity();
      hydrateGreetingAndStreak();

      expect(document.getElementById('streakCount')?.textContent).toBe('1');
      expect(document.getElementById('xpBadge')?.textContent).toBe('30 XP');
      const persisted = JSON.parse(localStorage.getItem('user')!);
      expect(persisted.lastActiveDate).toBe('2026-04-22');
    });

    it('같은 날 재제출: streak idempotent, xp 누적', async () => {
      vi.setSystemTime(new Date('2026-04-19'));
      seedUser({ streak: 5, lastActiveDate: '2026-04-19', xp: 20 });

      const { applyAnswerActivity, hydrateGreetingAndStreak } = await import('../../src/ui/handlers/home');
      applyAnswerActivity();
      applyAnswerActivity();
      hydrateGreetingAndStreak();

      expect(document.getElementById('streakCount')?.textContent).toBe('5');
      expect(document.getElementById('xpBadge')?.textContent).toBe('40 XP');
    });
  });
  ```

  주의: T2에서 `hydrateGreetingAndStreak`을 export로 변경(Step 2.5b)했으므로 직접 호출. `hydrateHome`은 RSS/Gemini 등 외부 의존을 끌고 들어가 jsdom flake 위험이 있어 회피. 본 spec은 순수 wiring만 통합 검증 (`applyAnswerActivity` 진입점 + `hydrateGreetingAndStreak` DOM 갱신).

- [ ] **Step 3.2**: 테스트 실행해 GREEN 확인 (T1+T2 완료된 상태이므로 즉시 GREEN 기대)

  ```bash
  npm test -- home-streak-integration.spec.ts
  ```

  Expected: 4 it-block 모두 PASS.

- [ ] **Step 3.3**: N=5 연속 실행으로 flake 확인

  ```bash
  for i in 1 2 3 4 5; do echo "=== run $i ==="; npm test -- home-streak-integration.spec.ts || break; done
  ```

  Expected: 5/5 모두 PASS. 1건이라도 fail 시 timer/race issue 있으므로 조사 필요.

- [ ] **Step 3.4**: 빌드/lint 회귀 확인

  ```bash
  npm run lint && npx tsc --noEmit
  ```

  Expected: 0 error.

- [ ] **Step 3.5**: 전체 회귀 테스트

  ```bash
  npm test
  ```

  Expected: ~413 PASS (405 baseline + T1 net +3 + T2 net +1 + T3 +4 = +8 net).

- [ ] **Step 3.6**: 빌드 사이즈 확인

  ```bash
  npm run build
  ```

  Expected: dist/ 생성. CSS / JS gzip 사이즈 변화 ±0.3 kB 이내 (소규모 변경, T1 함수 1개 교체 + T2 함수 1개 축소).

- [ ] **Step 3.7**: 커밋

  ```bash
  git add tests/unit/home-streak-integration.spec.ts
  git commit -m "test(v3.10 T3): integration tests for streak transitions in submitAnswer flow"
  ```

---

## Task 4 (Conditional): 사후 정합 — Plan/Spec 사실 오류 발견 시

**Files:**

- (Possibly) Modify: `docs/superpowers/specs/2026-04-28-v3.10-xp-streak-wiring-design.md`
- (Possibly) Modify: `docs/superpowers/plans/2026-04-28-v3.10-xp-streak-wiring.md`

### Why this task

T1~T3 진행 중 plan/spec 명시 사실이 코드 현실과 다르다고 발견되면(v3.9 T7 패턴) 사후 retroactive sync 커밋. 발견 0건이면 skip.

### Trigger 조건

다음 중 하나 발견:

- spec/plan 명시 line range가 실제 코드와 다른 경우
- spec 가정 (예: "사용처 0건")이 실제 grep과 다른 경우
- spec/plan에서 누락된 import 변경이 코드 변경 시 발견된 경우
- implementer가 plan보다 안전한 변경을 적용한 경우 (v3.9 T1/T2/T6 expansion 사례 재현)

### Steps

- [ ] **Step 4.1**: T1~T3 완료 후 spec/plan을 다시 한 번 읽어 발견된 불일치 정리

  ```bash
  git diff main...HEAD -- docs/superpowers/
  cat docs/superpowers/specs/2026-04-28-v3.10-xp-streak-wiring-design.md
  cat docs/superpowers/plans/2026-04-28-v3.10-xp-streak-wiring.md
  ```

- [ ] **Step 4.2**: 발견된 불일치 inline 수정

  (파일별 정확한 변경은 발견 시 결정)

- [ ] **Step 4.3**: 사후 동기화 커밋

  ```bash
  git add docs/superpowers/
  git commit -m "docs(v3.10 T4): retroactive sync of spec/plan with shipped code"
  ```

---

## Verification (DoD)

본 사이클 종료 전 체크:

- [ ] vitest 405 → ~413 PASS (T1 net +3 = 10→13, T2 net +1 = 3→4, T3 +4 신규 spec)
- [ ] Playwright smoke 81 PASS (회귀 없음, 신규 추가 없음)
- [ ] N=5 연속 통과 (T3 신규 spec)
- [ ] Lint 0 error 0 warn
- [ ] 빌드 success, 번들 사이즈 변화 ±0.3 kB gzip 이내
- [ ] `git grep -n "recordActivity\|checkAndUpdateStreak" -- 'src/'` 결과 0 hits
- [ ] `git grep -n "applyAnswerActivity(user" -- 'src/' 'tests/'` 결과 0 hits (모두 무인자 호출)
- [ ] 독립 표준 리뷰 (`superpowers:code-reviewer`, sonnet) APPROVE — P0=0 / P1≤1 (in-cycle 반영) / P2 자유 (v3.11+ backlog)
- [ ] 수동 smoke (사용자 결정): 본인 Daily Growth 앱에서 답변 1건 제출 → 홈 탭 `🔥 N` 카운터가 1 이상 표시

## Risks 재확인 (spec §10 참조)

- 외부 모듈 import 잠복 (Step 1.1 grep 사전 차단)
- vi.setSystemTime + getDateStr 불일치 (T3 N=5 검증)
- atomic write 가정 (브라우저 spec 보장, mitigation 불필요)
- LegacyUser import 정리 (Step 2.6에서 다른 사용처 검증)

## Cycle 마무리 후 작업

- (사용자 동의 시) `feat/v3.10-xp-streak-wiring` 브랜치 main 머지 + Firebase deploy
- 메모리 업데이트: `project_daily_growth_v310_xp_streak_wiring.md` 신설 + `MEMORY.md` index 갱신
