# v3.41 Security & Stability Hardening — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Codex 사전 audit (2026-05-20) 기반 P0×1 + P1×5 일괄 흡수. Schema migration 0. 신규 기능 0. P2×3 → v3.42 carry.

**Architecture:** M cleanup cycle (8 task). Security focus 4 task (T1/T2/T3/T4) + Stability 1 (T5) + Dead control 1 (T6) + Verify + Close. **Plan v2 — Codex 사전 review (PROCEED_AFTER_FIXES, P0×0 + P1×8 + P2×3) 흡수 완료**. 2-phase rollout (Phase A/B 분리), bundle quad record, T6 exact deletion list, T4 write+read+render 3-layer 등 in-cycle 반영.

**Tech Stack:** Vite + TypeScript + Vitest + Playwright + Firebase Hosting + Firebase Functions (Node 22 2nd Gen).

**Spec**: [docs/superpowers/specs/2026-05-20-v3.41-security-stability-hardening-design.md](../specs/2026-05-20-v3.41-security-stability-hardening-design.md)

---

## File Structure

### 신규 파일
- `src/services/appCheck.ts` — T1: Firebase App Check init + `getAppCheckToken()` helper
- `src/utils/reportAsync.ts` — T5: `reportAsync(label, promise)` 단일 helper
- `functions/src/__tests__/sendAnswerDm.spec.ts` — T1: App Check enforcement + enumeration 차단 regression
- `tests/unit/reportAsync.spec.ts` — T5: reject/resolve/toast mount 실패 흐름
- `tests/smoke/sendAnswerDm.spec.ts` — T1: App Check token 부재 시 401 + 유효 token + invalid email 시 generic 400 (선택)

### 수정 대상 파일

**Frontend src/**
- `src/main.ts` — T5: 3 dynamic import site `void import(...)` → `reportAsync(label, ...)`
- `src/services/slack.ts` — T1: fetch header에 `X-Firebase-AppCheck` 추가
- `src/services/gemini.ts` — T2: `parseEvaluationResponse()` export
- `src/services/rss.ts` — T4: `fetchFeed` 응답 normalize url 검증
- `src/state/briefings.ts` — T4: `saveBriefings` entry url 검증 + comment 갱신
- `src/state/migration.ts` — T2: `migrateAnswer` evaluation normalize
- `src/ui/handlers/archive.ts` — T2/T6: score 렌더 `escapeHtml(String(...))` + `handleArchivePeriodChange` 제거
- `src/ui/handlers/stats.ts` — T2: 동일
- `src/ui/tabs/archive.ts` — T6: period filter `<select>` 마크업 제거
- `src/utils/url.ts` — T4: `isSafeUrl()` 신설 (http+https 허용, javascript:/data: 차단)

**Frontend public/**
- `public/sw.js` — T3: `CACHE_NAME` v9→v10 + method/path 가드 + navigation network-first

**Frontend index.html**
- `index.html` — T1: App Check debug token meta (env 분기, optional)

**Backend functions/**
- `functions/src/sendAnswerDm.ts` — T1: `enforceAppCheck: true` + error response generic화

**Tests**
- `tests/unit/gemini-parse.spec.ts` — T2: `parseEvaluationResponse` table-driven
- `tests/unit/migration.spec.ts` — T2: evaluation normalize 회귀
- `tests/unit/briefings.spec.ts` — T4: url scheme validation
- `tests/unit/rss.spec.ts` — T4: invalid url drop
- `tests/smoke/sw.spec.ts` 또는 신설 — T3: navigation network-first + POST pass-through
- `tests/smoke/archive-unified.spec.ts` — T6: period filter selector grep + 제거

**Config**
- `package.json` — T1: `firebase` (v10+) dep 추가 (이미 있으면 skip)
- `firebase.json` — T1: App Check 활성화 확인 (서버 config는 console 작업)

**Docs**
- `docs/superpowers/specs/2026-05-20-v3.41-security-stability-hardening-design.md` — Codex review 후 v2 amend + T8 §9 retro

---

## Task 0: Codex 사전 Review Dispatch + Plan v2 흡수 (P0 gate)

**Goal**: spec v1 + plan v1 → Codex direct CLI dispatch → P0/P1 결과 plan v2 in-cycle 흡수. CLAUDE.md graduated mandatory step.

**Files:**
- Read: `docs/superpowers/specs/2026-05-20-v3.41-security-stability-hardening-design.md`
- Read: `docs/superpowers/plans/2026-05-20-v3.41-security-stability-hardening-plan.md`
- Write: `/tmp/v3.41-codex-pre.txt` (review 결과)
- Modify (필요 시): spec v2 (inline `[v2 — Codex P0/P1/P2-x]` markers)

### Steps

- [ ] **Step 1: Codex 사전 review prompt 작성**

`/tmp/v3.41-pre-prompt.md`에:

```
v3.41 사이클 spec + plan v1 사전 review.

검토 시점: T2 진입 전. 목적: P0/P1 catch 후 plan v2 in-cycle 흡수.

대상:
- docs/superpowers/specs/2026-05-20-v3.41-security-stability-hardening-design.md
- docs/superpowers/plans/2026-05-20-v3.41-security-stability-hardening-plan.md

배경:
- Codex 직전 audit (`/tmp/v3.41-codex-audit.txt`) P0×1 + P1×5 + P2×3 → spec scope.
- CLAUDE.md graduated checklist (5항):
  1. Schema chain superset (v3.41은 schema 0줄 — skip)
  2. interest id ≠ keyword 토큰 (matchesInterest 사용) — T2/T4 영향 없음
  3. predicate-defined vs wired (caller grep + wiring sites)
  4. mutation paths 전수 grep
  5. test mock path stale 여부

리뷰 축:
1. T1 App Check rollout 위험 — 사용자 token 발급 race + bundle +~10 KB gzip canonical 영향
2. T2 parseEvaluationResponse 1:1 mapping completeness — Gemini schema 변형 케이스 누락
3. T3 SW navigation network-first → existing offline assumption 깨질 여지
4. T4 isSafeUrl http 허용 결정 — production RSS feed http 비율 실측 권고
5. T5 reportAsync toast circular dep — main.ts boot timing
6. T6 dead control 제거 시 CustomEvent listener residual
7. Cross-task: T1 bundle 증가가 T7 dual record 측정에 미치는 영향

출력: P0/P1/P2 분류 + file:line + 구체적 fix 제안.
```

- [ ] **Step 2: Dispatch**

```bash
codex exec --skip-git-repo-check -o /tmp/v3.41-codex-pre.txt "$(cat /tmp/v3.41-pre-prompt.md)"
```

Background dispatch (run_in_background=true). harness notifies on completion.

- [ ] **Step 3: P0/P1 흡수 결정**

review 결과:
- **P0**: spec v2 inline `[v2 — Codex P0-N]` 마커 + plan v2 task amend
- **P1**: 동일 패턴 + in-cycle 의무 (T1~T6에 흡수)
- **P2**: §9.6 v3.42 carry

CLI 한도 시 controller fallback (CLAUDE.md graduated): `pr-review-toolkit:code-reviewer` agent 대체.

- [ ] **Step 4: spec v2 + plan v2 commit**

```bash
git add docs/superpowers/specs/2026-05-20-v3.41-security-stability-hardening-design.md docs/superpowers/plans/2026-05-20-v3.41-security-stability-hardening-plan.md
git commit -m "docs(v3.41 T0): spec v2 + plan v2 — Codex 사전 review P0×N + P1×N in-cycle 흡수"
```

### Verification

- [ ] `/tmp/v3.41-codex-pre.txt` 존재 + classification 명시
- [ ] spec v2 inline 마커가 plan task amend와 1:1 trace
- [ ] T2 진입 전 commit 존재

---

## Task 1: Slack DM Relay App Check Enforcement (P0 F1, M effort)

**Goal**: `sendAnswerDm` 함수에 Firebase App Check enforcement 추가 + frontend token 발급 + error response generic화로 직원 enumeration 차단.

**Files:**
- Create: `src/services/appCheck.ts`
- Create: `functions/src/__tests__/sendAnswerDm.spec.ts`
- Modify: `src/services/slack.ts`
- Modify: `functions/src/sendAnswerDm.ts`
- Modify: `package.json` (필요 시)
- Modify: `index.html` (debug token meta, optional)

### 1.1 사용자 사전 작업 (Firebase Console) — [v2 — Codex P1-1] 2-phase rollout

사용자 1회 작업 — controller가 안내:

**Phase A (T1 deploy 직전)**:
- [ ] Firebase Console → App Check → Apps → "Web app"
- [ ] **reCAPTCHA v3 site key 발급** (default, 무료). GCP billing 활성 확인되면 Enterprise 가능.
- [ ] site key를 `.env.local`에 `VITE_APP_CHECK_SITE_KEY=...` 기록
- [ ] **Functions 보호 OFF 유지** (Phase B에서 ON). 현재 "Monitor" 모드만 활성화.
- [ ] (개발용) Debug token 생성 → Playwright/local 환경에서 사용

**Phase B (T8 retro 후, 최소 24h monitor 후 별도 deploy)**:
- [ ] Phase A frontend deploy 후 24h 동안 `Console → App Check → Functions: sendAnswerDm` 메트릭 monitor
- [ ] token 발급률 ≥ 99% 확인 (Console에 "Verified requests" / "Total requests" 비율)
- [ ] `enforceAppCheck: true` backend deploy
- [ ] Console에서 `sendAnswerDm` "Enforce" toggle ON
- [ ] Production에서 정상 사용자 1명 (Hayden 본인) Slack DM 전송 동작 확인

**완료 보고**: 사용자 → controller에 site key + debug token 전달 (Phase A 직전).

### 1.2 Frontend App Check init

- [ ] **Step 1: firebase dep 확인/추가**

```bash
npm ls firebase 2>&1 | head -3
# 없으면:
npm install firebase
```

- [ ] **Step 2: `src/services/appCheck.ts` 신설** — **[v2 — Codex P1-1] ReCaptchaV3Provider default**

```ts
import { initializeApp } from 'firebase/app';
import { initializeAppCheck, ReCaptchaV3Provider, getToken } from 'firebase/app-check';

const SITE_KEY = import.meta.env.VITE_APP_CHECK_SITE_KEY ?? '';

// 개발용 debug token (production 빌드에서 자동 stripped)
declare global {
  // eslint-disable-next-line no-var
  var FIREBASE_APPCHECK_DEBUG_TOKEN: string | boolean | undefined;
}
if (import.meta.env.DEV) {
  self.FIREBASE_APPCHECK_DEBUG_TOKEN = true;
}

const firebaseApp = initializeApp({
  apiKey: import.meta.env.VITE_FIREBASE_API_KEY,
  authDomain: 'my-ai-assistant-904f3.firebaseapp.com',
  projectId: 'my-ai-assistant-904f3',
  appId: import.meta.env.VITE_FIREBASE_APP_ID,
});

const appCheck = SITE_KEY
  ? initializeAppCheck(firebaseApp, {
      provider: new ReCaptchaV3Provider(SITE_KEY),
      isTokenAutoRefreshEnabled: true,
    })
  : null;

export async function getAppCheckToken(): Promise<string | null> {
  if (!appCheck) return null;
  try {
    const result = await getToken(appCheck, /* forceRefresh */ false);
    return result.token;
  } catch (err) {
    console.warn('[appCheck] getToken failed', err);
    return null;
  }
}
```

- [ ] **Step 3: `.env.example` + `.env` 갱신**

```
VITE_APP_CHECK_SITE_KEY=<site-key>
VITE_FIREBASE_API_KEY=<api-key>
VITE_FIREBASE_APP_ID=<app-id>
```

`.env`는 .gitignore에 이미 있음 (확인). `.env.example`만 commit.

- [ ] **Step 4: `src/services/slack.ts` header 추가**

기존 `fetch('/api/sendAnswerDm', {...})` 직전에:

```ts
const token = await getAppCheckToken();
const headers: Record<string, string> = { 'Content-Type': 'application/json' };
if (token) headers['X-Firebase-AppCheck'] = token;
const res = await fetch('/api/sendAnswerDm', { method: 'POST', headers, body: JSON.stringify(payload) });
```

### 1.3 Backend enforceAppCheck — [v2 — Codex P1-1 / P1-8]

**Phase A (T1 commit 단계)**: `enforceAppCheck` 추가 X. error response generic화 X. 기존 backend 그대로 유지. 단 frontend는 token 발급 + header 전송 (monitor mode).

**Phase B (T8 retro 후, 별도 commit/deploy)**:

- [ ] **Step 5: `functions/src/sendAnswerDm.ts` 수정 (Phase B)**

```ts
export const sendAnswerDm = onRequest(
  {
    secrets: [SLACK_BOT_TOKEN],
    maxInstances: 1,
    cors: CORS_ORIGINS,
    region: 'us-central1',
    enforceAppCheck: true,    // ← Phase B
  },
  async (req, res) => {
    // 기존 method/email/answer/question 검증 유지
    // ...
    try {
      // ...
      await sendDmAsBot(...);
      res.status(200).json({ ok: true });
    } catch (e) {
      const msg = e instanceof Error ? e.message : 'unknown';
      // [v2 — Codex P1-8] enumeration 차단 — user_not_found도 generic 202 accepted
      if (msg === 'user_not_found') {
        console.warn('[sendAnswerDm] user_not_found', body.email);  // log만 남김
        res.status(202).json({ ok: true, accepted: true });  // 사용자 측에서는 "접수"로 보임
        return;
      }
      if (msg === 'dm_open_failed' || msg === 'post_message_failed' || msg === 'lookup_failed') {
        res.status(502).json({ error: msg });
      } else if (msg.startsWith('http_5')) {
        res.status(502).json({ error: 'slack_upstream' });
      } else {
        res.status(500).json({ error: 'internal' });
      }
    }
  },
);
```

- [ ] **Step 5b: `src/ui/tabs/settings.ts` 테스트 버튼 success copy 갱신**

기존 success path (`{ok:true}` 받으면 "전송 성공") → 202의 경우 별도 copy:

```ts
// settings.ts 의 sendTestDm response 핸들링
if (res.status === 202) {
  showToast('전송 요청을 접수했어요. DM이 오지 않으면 봇 설치/이메일을 확인해주세요.');
} else if (res.ok) {
  showToast('테스트 DM 전송 성공!');
}
```

### 1.4 Regression test

- [ ] **Step 6: `functions/src/__tests__/sendAnswerDm.spec.ts` 신설**

테스트 케이스 (Phase B 기준):
1. App Check token 없을 때 → onRequest framework가 401 차단 (mock으로 framework behavior simulate)
2. 유효 token + `@gmail.com` 이메일 → 400 `invalid_email`
3. **[v2 — Codex P1-8]** 유효 token + `nonexistent@datarize.ai` → **202 `{ok: true, accepted: true}`** (enumeration 차단 확인 — old 동작은 404)
4. 유효 token + 정상 → 200 `{ok: true}`
5. rate limit 동작 유지 (429 응답)
6. `dm_open_failed` upstream error → 502
7. Slack `http_5xx` → 502 `slack_upstream`

### 1.5 Bundle 측정 — [v2 — Codex P1-2] quad record

- [ ] **Step 7: build + quad record 측정**

```bash
rm -rf dist && npm run build

# Baseline 실측 (Codex T0 2026-05-20):
#   index-*.js gzip = 9,034 B (9.11 KB)
#   archive-*.js gzip 합산 = 9,119 B (9.12 KB)

echo "=== T1 v3.41 bundle quad record ==="
INDEX_JS=$(gzip -c dist/assets/index-*.js | wc -c | tr -d ' ')
HOME_JS=$(gzip -c dist/assets/home-*.js 2>/dev/null | wc -c | tr -d ' ')
SETTINGS_JS=$(gzip -c dist/assets/settings-*.js 2>/dev/null | wc -c | tr -d ' ')
ARCHIVE_JS=$(gzip -c dist/assets/archive-*.js 2>/dev/null | wc -c | tr -d ' ')
FIREBASE_CHUNK=$(ls dist/assets/firebase*.js 2>/dev/null | xargs -I{} gzip -c {} | wc -c | tr -d ' ')
echo "index: $INDEX_JS B (Δ vs 9034)"
echo "home:  $HOME_JS B"
echo "settings: $SETTINGS_JS B"
echo "archive: $ARCHIVE_JS B (vs 9119)"
echo "firebase shared: $FIREBASE_CHUNK B"
echo "canonical 합산 (index+home+settings+firebase): $((INDEX_JS + HOME_JS + SETTINGS_JS + FIREBASE_CHUNK)) B"
```

**DoD**: canonical 합산 Δ ≤ **+15 KB gzip**. over 시 **in-cycle lazy load 의무** — `src/services/slack.ts` 내부 `fetch` 직전 `await import('./appCheck')` 동적 import.

### Verification

- [ ] `npx tsc --noEmit` 0 errors
- [ ] `npm run lint` 0 errors
- [ ] `TZ=Asia/Seoul npm test` + `TZ=America/New_York npm test` PASS
- [ ] `npm --prefix functions test` PASS (sendAnswerDm spec 신규 5건 포함)
- [ ] `npm run test:smoke` PASS (Slack 관련 spec 동작 확인)
- [ ] App Check token이 실제 brackets 안에 들어가는지 DevTools Network에서 1회 확인 (manual)
- [ ] Bundle canonical Δ ≤ +15 KB gzip (over 시 검토)

### Commit

```
fix(v3.41 T1 P0 F1): Slack DM relay enforceAppCheck + enumeration 차단

- src/services/appCheck.ts 신설 (Firebase App Check init)
- src/services/slack.ts X-Firebase-AppCheck header
- functions/src/sendAnswerDm.ts enforceAppCheck:true + user_not_found 200 generic
- functions/src/__tests__/sendAnswerDm.spec.ts 신규 5건
```

---

## Task 2: AI Evaluation Score Guard (P1 F2, S effort)

**Goal**: Gemini 평가 응답 score field runtime type guard + render path escape.

**Files:**
- Modify: `src/services/gemini.ts` (`parseEvaluationResponse` export)
- Modify: `src/state/migration.ts` (`migrateAnswer` normalize)
- Modify: `src/ui/handlers/archive.ts:1162` (escape 적용)
- Modify: `src/ui/handlers/stats.ts:450` (escape 적용)
- Create: `tests/unit/gemini-parse.spec.ts` 또는 기존 spec 확장
- Modify: `tests/unit/migration.spec.ts`

### Steps

- [ ] **Step 1: 현재 evaluation 호출 path 전수 grep**

```bash
grep -rn "parseJsonText<{ score" src/ tests/
grep -rn "evaluation\.score\|evaluation\.feedback" src/ tests/
```

- [ ] **Step 2: `parseEvaluationResponse` 신설** — **[v2 — Codex P1-3] try/catch + fallback 확정**

`src/services/gemini.ts`:

```ts
export function parseEvaluationResponse(text: string): { score: 1 | 2 | 3 | 4 | 5; feedback: string } {
  const fallback = { score: 3 as const, feedback: '' };
  let raw: unknown;
  try {
    raw = parseJsonText<unknown>(text);
  } catch (err) {
    console.warn('[parseEvaluationResponse] parse failed', err);
    return fallback;
  }
  if (!raw || typeof raw !== 'object') return fallback;
  let score: 1 | 2 | 3 | 4 | 5 = 3;
  if ('score' in raw) {
    const s = Number((raw as { score: unknown }).score);
    if (Number.isInteger(s) && s >= 1 && s <= 5) score = s as 1 | 2 | 3 | 4 | 5;
    else console.warn('[parseEvaluationResponse] invalid score', s);
  }
  let feedback = '';
  if ('feedback' in raw) {
    const f = (raw as { feedback: unknown }).feedback;
    if (typeof f === 'string') feedback = f.slice(0, 500);
  }
  return { score, feedback };
}
```

- [ ] **Step 3: 호출처 마이그레이션**

기존 `parseJsonText<{score, feedback}>(text)` 호출처 전수 → `parseEvaluationResponse(text)` 사용.

- [ ] **Step 4: `migrateAnswer` normalize**

`src/state/migration.ts`에서 answer.evaluation 진입 시 score/feedback normalize 적용 (legacy 손상 데이터 cleanup).

- [ ] **Step 5: 렌더 path escape**

`src/ui/handlers/archive.ts:1162` 또는 인근:
```ts
parts.push(`<div class="archive-detail-eval">AI 평가 ${escapeHtml(String(a.evaluation.score))}점 — ${escapeHtml(a.evaluation.feedback)}</div>`);
```

`src/ui/handlers/stats.ts:450` 동일 패턴 적용.

- [ ] **Step 6: spec**

`tests/unit/gemini-parse.spec.ts` table-driven:
- `'{"score": 3, "feedback": "good"}'` → `{score: 3, feedback: 'good'}`
- `'{"score": "5", "feedback": "x"}'` → `{score: 5, feedback: 'x'}` (string → Number coerce OK)
- `'{"score": 6, "feedback": null}'` → `{score: 3, feedback: ''}` (out-of-range fallback)
- `'{"score": -1, "feedback": ""}'` → `{score: 3, feedback: ''}`
- `'{"score": 2.5, "feedback": "x"}'` → `{score: 3, feedback: 'x'}` (non-integer fallback)
- `'<script>alert(1)</script>'` 형태 invalid JSON → throw 또는 default fallback (parseJsonText 동작 따라)
- `'{}'` → `{score: 3, feedback: ''}`

### Verification

- [ ] `npx tsc --noEmit`
- [ ] `npm run lint`
- [ ] `TZ=Asia/Seoul npm test gemini-parse migration` + NY 동일
- [ ] N=5: `for i in 1 2 3 4 5; do TZ=Asia/Seoul npm test -- gemini-parse migration; done` 5/5

### Commit

```
fix(v3.41 T2 P1 F2): AI evaluation score runtime guard + render escape

- src/services/gemini.ts parseEvaluationResponse export (clamp 1-5 + warn)
- src/state/migration.ts migrateAnswer evaluation normalize
- src/ui/handlers/{archive,stats}.ts score escape 적용
- tests/unit/gemini-parse.spec.ts table-driven +N
```

---

## Task 3: Service Worker v9→v10 Bump + GET-only Intercept (P1 F3, S effort)

**Goal**: SW navigation network-first + non-GET pass-through + CACHE_NAME bump.

**Files:**
- Modify: `public/sw.js`
- Create or modify: `tests/smoke/sw.spec.ts` (또는 기존 SW spec)

### Steps

- [ ] **Step 1: 현재 sw.js fetch handler 분석**

```bash
cat public/sw.js
```

핵심 로직 파악: 현재 stale-while-revalidate가 모든 request에 적용.

- [ ] **Step 2: CACHE_NAME bump**

`public/sw.js:2`:
```js
const CACHE_NAME = 'daily-growth-v10';  // v9 → v10
```

- [ ] **Step 3: fetch handler 분기 추가**

```js
self.addEventListener('fetch', event => {
  const req = event.request;
  // 1) Non-GET (POST/PUT/DELETE) → cache 우회
  if (req.method !== 'GET') return;
  // 2) /api/ → functions 호출 우회
  const url = new URL(req.url);
  if (url.pathname.startsWith('/api/')) return;
  // 3) Navigation → network-first
  if (req.mode === 'navigate') {
    event.respondWith(
      fetch(req).catch(() => caches.match('/index.html'))
    );
    return;
  }
  // 4) 기타 정적 asset → 기존 stale-while-revalidate 유지
  event.respondWith(
    caches.match(req).then(cached => {
      const fetchPromise = fetch(req).then(response => {
        // ... 기존 로직
      });
      return cached || fetchPromise;
    })
  );
});
```

- [ ] **Step 4: cache.put guard**

기존 `cache.put()` 호출처에서 `response.status === 200 && req.method === 'GET'` 확인 (1번 guard로 method 이미 차단됨 — 방어층).

- [ ] **Step 5: 기존 spec 보존 + 갱신** — **[v2 — Codex P1-4]**

**기존 spec 2개 영향**:
- `tests/unit/sw.spec.ts:15` (v3.20 image/font pass-through regression guard)
- `tests/lint/sw-passthrough.spec.ts:24` (정적 분석 lint)

**갱신 케이스** (기존 + 신규):
1. ✅ **기존 보존**: image/font pass-through (v3.20 hotfix 보호)
2. **신규**: POST `/api/sendAnswerDm` → SW가 intercept하지 않음 (network에 직접 도달)
3. **신규**: Navigation `/` → network 성공 시 network 응답, network 실패 시 cached `/index.html`
4. ✅ **기존 보존**: Hashed asset `/assets/index-XXX.js` → cache-first

(선택) Playwright smoke `tests/smoke/sw.spec.ts` 신설 — production SW 동작 확인.

### Verification

- [ ] `npm run test:smoke` PASS
- [ ] DevTools → Application → Service Workers에서 새 SW 등록 확인 (manual)
- [ ] 새 SW 등록 후 `/api/sendAnswerDm` POST가 SW에 의해 intercept되지 않는지 Network 탭 확인 (manual)

### Commit

```
fix(v3.41 T3 P1 F3): SW v9→v10 bump + GET-only intercept + nav network-first

- public/sw.js CACHE_NAME daily-growth-v10
- method/path 가드 + navigation network-first with /index.html fallback
- tests/smoke/sw.spec.ts POST pass-through + nav fallback
```

---

## Task 4: Briefing URL Scheme Validation (P1 F4, S effort)

**Goal**: RSS briefing url field에 `isSafeUrl` 강제로 `javascript:` / `data:` 차단.

**Files:**
- Modify: `src/utils/url.ts` (`isSafeUrl` 신설)
- Modify: `src/state/briefings.ts` (`saveBriefings` entry 검증)
- Modify: `src/services/rss.ts` (`fetchFeed` 응답 normalize)
- Modify: `tests/unit/briefings.spec.ts`
- Modify or create: `tests/unit/rss.spec.ts`

### Steps

- [ ] **Step 1: production RSS feed http 비율 실측** — **[v2 — Codex P1-5 실측 완료]**

`src/ui/handlers/home.ts:749` `interestToFeeds` 매핑 grep 결과 — **전수 https**, http 0건. 그러나 import/restore 경계 및 future-proof로 `isSafeUrl` (http+https 허용) 유지. `javascript:`/`data:`/`vbscript:`/`file:` 차단.

- [ ] **Step 2: `src/utils/url.ts` `isSafeUrl` 신설**

```ts
const SAFE_SCHEMES = ['http:', 'https:'];

export function isSafeUrl(value: unknown): value is string {
  if (typeof value !== 'string') return false;
  try {
    const u = new URL(value);
    return SAFE_SCHEMES.includes(u.protocol);
  } catch {
    return false;
  }
}
```

기존 `isHttpsUrl`는 유지 (image용으로 더 엄격).

- [ ] **Step 3: `src/state/briefings.ts` write + read 양쪽 boundary** — **[v2 — Codex P1-7]**

```ts
// 공통 helper (export)
function normalizeBriefingItem<T extends { url?: string; imageUrl?: string }>(item: T): T | null {
  if (typeof item.url !== 'string' || !isSafeUrl(item.url)) {
    console.warn('[briefings] dropping invalid url', item.url);
    return null;  // drop
  }
  // imageUrl 기존 isHttpsUrl 유지
  if (item.imageUrl !== undefined && !isHttpsUrl(item.imageUrl)) {
    delete item.imageUrl;
  }
  return item;
}

// saveBriefings + loadBriefings 양쪽에서 호출
```

`src/state/briefings.ts:35-37` comment도 갱신 (write + read + render 3-layer).

- [ ] **Step 4: `src/services/rss.ts` boundary 검증**

`fetchFeed` 응답 매핑 단계에서 `isSafeUrl` 적용. invalid url 가진 item drop.

- [ ] **Step 5: 렌더 path safeHref 의무화** — **[v2 — Codex P1-7] defense-in-depth 의무**

`src/ui/handlers/home.ts:489`:
```ts
// before: main.href = b.url;
main.href = isSafeUrl(b.url) ? b.url : '#';
```

`src/ui/handlers/archive.ts:1167`:
```ts
// before: parts.push(`<a class="archive-detail-link" href="${escapeHtml(b.url)}" ...>`);
const safeUrl = isSafeUrl(b.url) ? b.url : '#';
parts.push(`<a class="archive-detail-link" href="${escapeHtml(safeUrl)}" ...>`);
```

write/read boundary가 닫혀도 손상된 localStorage/import 데이터를 위한 defense-in-depth.

- [ ] **Step 6: spec**

`tests/unit/briefings.spec.ts`:
- `url: 'https://...'` → 유지
- `url: 'http://...'` → 유지 (isSafeUrl)
- `url: 'javascript:alert(1)'` → drop
- `url: 'data:text/html,...'` → drop
- `url: 'vbscript:...'` → drop
- `url: 'file:///etc/passwd'` → drop
- `url: ''` → drop
- `url: 123` (non-string) → drop
- 8 item batch 중 3 invalid → 5건만 저장

### Verification

- [ ] `npx tsc --noEmit`
- [ ] `npm run lint`
- [ ] `TZ=Asia/Seoul npm test url briefings rss` + NY 동일
- [ ] N=5

### Commit

```
fix(v3.41 T4 P1 F4): briefing url scheme validation (isSafeUrl 신설)

- src/utils/url.ts isSafeUrl 신설 (http+https only)
- src/state/briefings.ts saveBriefings entry url 검증 + comment 갱신
- src/services/rss.ts fetchFeed response normalize
- tests/unit/briefings.spec.ts + url.spec.ts table-driven +N
```

---

## Task 5: reportAsync Helper + main.ts Wrap (P1 F5, S effort)

**Goal**: boot dynamic import silent fail 차단. v3.40 L1 silent-fail lesson closure.

**Files:**
- Create: `src/utils/reportAsync.ts`
- Modify: `src/main.ts` (3 site wrap)
- Create: `tests/unit/reportAsync.spec.ts`

### Steps

- [ ] **Step 0: 순환 의존성 grep** — **[v2 — Codex P2-2]**

```bash
rg "reportAsync|from .*toast" src/utils src/state src/ui
```

확인: `src/utils/toast.ts`의 import 0건, module-level DOM side-effect 0건 (`showToast()` 호출 시점에만 DOM 조작). **circular dep false alarm 확정** → reportAsync에서 toast.ts import 안전.

- [ ] **Step 1: `src/utils/reportAsync.ts` 신설**

```ts
import { showToast } from './toast';

export function reportAsync<T>(label: string, p: Promise<T>): Promise<T | undefined> {
  return p.catch(err => {
    console.warn(`[reportAsync:${label}]`, err);
    try {
      showToast(`${label} 로드 실패 — 새로고침 해주세요`);
    } catch {
      // toast not mounted yet — boot race OK
    }
    return undefined;
  });
}
```

- [ ] **Step 2: `src/main.ts` 마이그레이션**

기존:
```ts
void import('./ui/modals/welcome-garden').then(({ maybeShowWelcomeGarden }) => {
  maybeShowWelcomeGarden();
});
```

→

```ts
reportAsync('welcome-garden', import('./ui/modals/welcome-garden').then(({ maybeShowWelcomeGarden }) => {
  maybeShowWelcomeGarden();
}));
```

3개 site 동일.

`bootMainApp()`도 wrap:
```ts
reportAsync('bootMainApp', bootMainApp());
```

- [ ] **Step 3: spec**

`tests/unit/reportAsync.spec.ts`:
- resolve 정상 → return value passthrough
- reject → console.warn + showToast 호출 + undefined return
- showToast throw → catch + undefined return (boot race)

```ts
vi.mock('../src/utils/toast', () => ({ showToast: vi.fn() }));
```

### Verification

- [ ] `npx tsc --noEmit`
- [ ] `npm run lint`
- [ ] `TZ=Asia/Seoul npm test reportAsync` + NY 동일
- [ ] N=5

### Commit

```
fix(v3.41 T5 P1 F5): reportAsync helper + main.ts dynamic import wrap

- src/utils/reportAsync.ts 신설 (silent fail closure L1)
- src/main.ts 3 dynamic import + bootMainApp wrap
- tests/unit/reportAsync.spec.ts +3
```

---

## Task 6: Archive Period Filter Dead Control 숨김 (P1 F6, S effort) — [v2 — Codex P1-6] exact deletion list

**Goal**: "이번 주/이번 달" select 마크업 + handler + listener + EventMap/EVENT_NAMES/V32_DEFERRED + test mock 일괄 제거.

**Files (정확한 인벤토리 — 사전 review 검증)**:
- Modify: `src/ui/tabs/archive.ts:28` (DOM `<select class="archive-period" id="archivePeriod">`)
- Modify: `src/ui/tabs/archive.ts:120-122` (dispatcher `addEventListener('change', dispatch)`)
- Modify: `src/ui/handlers/archive-listeners.ts:19-22` (`on('dg:archive:period-change', ...)`)
- Modify: `src/ui/events.ts:29` (`EventMap` entry)
- Modify: `src/ui/events.ts:80` (`EVENT_NAMES` 배열, 28→27)
- Modify: `src/ui/events.ts:100-103` (`V32_DEFERRED_EVENTS` 배열, 2→1)
- Modify: `tests/unit/events.spec.ts:43-44` (expect 28→27)
- Modify: `tests/unit/events.spec.ts:50` (V32_DEFERRED length 2→1)
- Modify: `tests/unit/handlers-register.spec.ts` (mock 있다면)
- Modify: `src/styles/pages/archive.css` (`.archive-period` rule 있다면)
- Modify: `src/ui/handlers/archive.ts:115` (`handleArchivePeriodChange()` 함수 있다면 — Codex spec line 참조로 확인)

### Steps

- [ ] **Step 1: 정확한 인벤토리 재확인 (Edit 직전)**

```bash
grep -rn "archivePeriod\|period-change\|handleArchivePeriodChange" src/ tests/
```

각 site:line 1:1 match 확인. 위 list와 deviation 있으면 plan 갱신.

- [ ] **Step 2: DOM 마크업 제거 (`src/ui/tabs/archive.ts:28`)**

`<select class="archive-period" id="archivePeriod">...3 option...</select>` block 제거. 인근 wrapper `<div style="display:flex;gap:8px;...">` 구조도 검토 — period 제거 후 filter chips만 남으면 flex 의미 사라지면 wrapper 정리.

- [ ] **Step 3: Dispatcher 제거 (`src/ui/tabs/archive.ts:120-122`)**

```ts
// 제거 대상
const archivePeriod = container.querySelector<HTMLSelectElement>('#archivePeriod');
if (archivePeriod) archivePeriod.addEventListener('change', () => {
  document.dispatchEvent(new CustomEvent('dg:archive:period-change'));
});
```

- [ ] **Step 4: Listener 제거 (`src/ui/handlers/archive-listeners.ts:19-22`)**

```ts
// 제거 대상
on('dg:archive:period-change', () => {
  ...
  .catch((err) => warn('period-change', err));
});
```

- [ ] **Step 5: events.ts 3곳 제거**

`src/ui/events.ts`:
- `:29` EventMap: `'dg:archive:period-change': undefined;` 제거
- `:80` EVENT_NAMES 배열에서 `'dg:archive:period-change'` 제거
- `:100-103` V32_DEFERRED_EVENTS 배열에서 `'dg:archive:period-change'` 제거 (`dismiss-backup`만 남음)

- [ ] **Step 6: test spec 갱신**

`tests/unit/events.spec.ts`:
- `:43-44` `expect(EVENT_NAMES).toHaveLength(28)` → `27`, set size 28 → 27
- `:50` `expect(V32_DEFERRED_EVENTS).toHaveLength(2)` → `1`
- 신규 assertion: `expect(EVENT_NAMES).not.toContain('dg:archive:period-change')` (defense)

`tests/unit/handlers-register.spec.ts` — period 관련 mock 있다면 정리.

- [ ] **Step 7: CSS rule 제거**

`src/styles/pages/archive.css` (또는 components/forms.css) — `.archive-period` selector 있다면 제거.

- [ ] **Step 8: 영향 smoke 확인**

```bash
grep -rn "archivePeriod\|period-change" tests/smoke/
```

Playwright smoke가 period select를 click하는 케이스 있다면 제거 (현재는 없을 가능성 높음).

### Verification

- [ ] `npx tsc --noEmit`
- [ ] `npm run lint`
- [ ] `npm test` PASS
- [ ] `npm run test:smoke` PASS (archive unified + interest filter 등 기존 spec 영향 없음)
- [ ] Bundle archive chunk 측정 — 미세 trim 예상

### Commit

```
refactor(v3.41 T6 P1 F6): archive period filter dead control 일괄 제거

- src/ui/tabs/archive.ts DOM 마크업 제거
- src/ui/handlers/archive.ts handleArchivePeriodChange 제거
- src/styles/pages/archive.css 관련 rule 제거
- tests/smoke spec 갱신
```

---

## Task 7: 3종 검증 + Bundle Dual Record + N=5

**Goal**: 전체 사이클 final verification + bundle measurement + flake check.

**Files:**
- Read: `dist/assets/index-*.js`, `dist/assets/archive-*.js`
- Write: `/tmp/v3.41-T7-verify.txt` (측정 기록)

### Steps

- [ ] **Step 1: clean build**

```bash
rm -rf dist node_modules/.vite-temp && npm run build
```

- [ ] **Step 2: TS strict + Lint**

```bash
npx tsc --noEmit
npm run lint
```

- [ ] **Step 3: Vitest 양쪽 TZ**

```bash
TZ=Asia/Seoul npm test
TZ=America/New_York npm test
```

- [ ] **Step 4: Functions test**

```bash
npm --prefix functions test
```

- [ ] **Step 5: Smoke**

```bash
npm run test:smoke
```

- [ ] **Step 6: Bundle quad record** — **[v2 — Codex P1-2]**

```bash
INDEX_JS=$(gzip -c dist/assets/index-*.js | wc -c | tr -d ' ')
HOME_JS=$(gzip -c dist/assets/home-*.js 2>/dev/null | wc -c | tr -d ' ')
SETTINGS_JS=$(gzip -c dist/assets/settings-*.js 2>/dev/null | wc -c | tr -d ' ')
ARCHIVE_JS=$(gzip -c dist/assets/archive-*.js 2>/dev/null | wc -c | tr -d ' ')
FIREBASE_CHUNK=$(ls dist/assets/firebase*.js 2>/dev/null | xargs -I{} gzip -c {} 2>/dev/null | wc -c | tr -d ' ')
CANONICAL=$((INDEX_JS + HOME_JS + SETTINGS_JS + ${FIREBASE_CHUNK:-0}))
echo "index: $INDEX_JS B (v3.40 baseline 9034)"
echo "home: $HOME_JS B"
echo "settings: $SETTINGS_JS B"
echo "archive: $ARCHIVE_JS B (v3.40 baseline 9119)"
echo "firebase shared: ${FIREBASE_CHUNK:-0} B"
echo "canonical 합산: $CANONICAL B (DoD ≤ +15 KB gzip from baseline)"
```

기록: spec §10 verification matrix + retro §9.3. DoD over 시 in-cycle lazy load fix.

- [ ] **Step 7: N=5 신규 spec**

```bash
for i in 1 2 3 4 5; do
  TZ=Asia/Seoul npm test -- gemini-parse reportAsync briefings url || echo "FAIL run $i"
done
```

5/5 PASS 의무.

### Verification

- [ ] TS 0 errors
- [ ] Lint 0 errors
- [ ] Vitest 양쪽 TZ PASS
- [ ] Functions test PASS
- [ ] Smoke PASS
- [ ] Bundle canonical Δ vs v3.40 baseline 기록 (T1 영향 명시)
- [ ] N=5 5/5

### Commit

```
test(v3.41 T7): final 3종 검증 + bundle dual record + N=5 5/5
```

---

## Task 8: Codex 최종 Review + Retro + Obsidian Sync

**Goal**: Codex CLI direct dispatch (mandatory deploy gate) + retro 작성 + Obsidian sync.

**Files:**
- Write: `/tmp/v3.41-codex-final.txt`
- Modify: `docs/superpowers/specs/2026-05-20-v3.41-security-stability-hardening-design.md` (§9 retro 추가)
- Copy to: `/Users/hayden/Hayden/20_Projects/Dev_Project/SJ_Daily Growth(Ai assistant)/02-Cycles/`

### Steps

- [ ] **Step 1: Codex 최종 review prompt 작성**

```
v3.41 사이클 최종 review.

대상: T1~T6 commit 전체 diff + spec v2 + plan v2

분류:
- DEPLOY_APPROVED — 즉시 firebase deploy
- APPROVED_WITH_NOTES — P1×N + P2×M, in-cycle 흡수 가능한 P1만 추가 commit
- REJECT — P0 발견 시 사이클 미종료

리뷰 축:
1. App Check token 발급 race / production rollout 안전성
2. parseEvaluationResponse coverage
3. SW v10 navigation 분기 offline UX
4. isSafeUrl decision (http 허용)
5. reportAsync circular dep
6. dead control 제거 residual

CLAUDE.md graduated:
- mutation paths 전수 grep
- predicate-defined vs wired
- test mock path stale
```

- [ ] **Step 2: Dispatch**

```bash
codex exec --skip-git-repo-check -o /tmp/v3.41-codex-final.txt "<prompt>"
```

- [ ] **Step 3: 결과 처리**

- **DEPLOY_APPROVED** → §9.4 기록 후 T8 retro 마무리
- **APPROVED_WITH_NOTES** → P1 in-cycle 흡수 commit + retro §9.5에 명시
- **REJECT** → P0 fix commit 후 재dispatch

CLI 한도 시 controller fallback (CLAUDE.md graduated): `pr-review-toolkit:code-reviewer`.

- [ ] **Step 4: Retro 작성**

spec §9 섹션 추가:
- §9.1 commit summary
- §9.2 task별 산출물 + verification
- §9.3 bundle 측정 (dual record, T1 영향 명시)
- §9.4 사전 review 흡수 + 최종 review 결과
- §9.5 신규 lessons
- §9.6 v3.42 carry-forward (P2 F7/F8/F9 + 신규)
- §9.7 사용자 결정 사항
- §9.8 다음 사이클 권고

- [ ] **Step 5: MEMORY.md + project memory file 갱신**

`/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/` 경로에:
- 신규: `project_daily_growth_v341.md`
- MEMORY.md 인덱스에 1줄 추가

- [ ] **Step 6: Obsidian sync (사용자 "배포해줘" 명시 후)**

```bash
VAULT="/Users/hayden/Hayden/20_Projects/Dev_Project/SJ_Daily Growth(Ai assistant)"
cp "docs/superpowers/specs/2026-05-20-v3.41-security-stability-hardening-design.md" "$VAULT/02-Cycles/"
cp "/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v341.md" "$VAULT/03-Lessons/"
cp "/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md" "$VAULT/03-Lessons/"
# 00-Index.md 사이클 타임라인 표 갱신 (manual edit)
```

- [ ] **Step 7: 사용자 배포 승인 대기 → `firebase deploy --only hosting`**

```bash
npm run build
firebase deploy --only hosting
firebase deploy --only functions:sendAnswerDm
```

functions deploy 시 App Check enforcement 활성화 — 사용자 Firebase Console에서 "Enforce" toggle ON 사전 확인 의무.

### Verification

- [ ] Codex 최종 review 결과 명시 (DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT)
- [ ] §9 retro 8 sub-section 모두 작성
- [ ] Obsidian sync (cycle 완료 후)
- [ ] 사용자 명시 배포 후 production 검증 (Slack DM 정상 작동 + DevTools에 App Check header 확인)

### Commit

```
docs(v3.41 T8): retro §9.1~§9.8 + Codex 최종 review + Obsidian sync
```

---

## Risk Summary

| Task | Risk | Mitigation |
|---|---|---|
| T0 | Codex CLI 한도 | controller fallback (graduated) |
| T1 | App Check Console 작업 사용자 의존 | site key 발급 단계별 안내. fallback `enforceAppCheck:false` |
| T1 | Bundle +~10 KB gzip | 측정 후 over 시 dynamic import 분리 검토 |
| T1 | Frontend race (token 발급 전 fetch) | `getToken({forceRefresh:false})` + SDK auto-refresh |
| T2 | Gemini schema 변형 silent fallback | console.warn |
| T3 | Offline UX 미세 변경 | hashed asset cache-first 유지 |
| T4 | http feed drop 가능 | isSafeUrl로 http 허용 (javascript:/data:만 차단) |
| T5 | Toast circular import | try/catch wrap |
| T6 | v3.42+ 재구현 비용 | retro carry note (search/pin 통합 검토) |
| Cross | Schema migration 0 | v8 유지 — 검증 불필요 |
| Cross | Production rollout 순서 | functions deploy 전 frontend deploy 분리 가능 (1차 token 발급, 2차 enforce ON) |

---

## Estimated Bundle Impact — [v2 — Codex P1-2] quad record

**Baseline (Codex T0 2026-05-20)**: index 9,034 B / archive 9,119 B (둘 다 gzip).

| Task | Δ index | Δ home | Δ settings | Δ archive | Δ firebase chunk | 비고 |
|---|---|---|---|---|---|---|
| T1 | 0~+200 B | +5~12 KB | 미세 | 0 | 신규 chunk possible | slack.ts 정적 import → home에 emit. 또는 firebase shared chunk로 분리 (vite 결정) |
| T2 | 0 | 0 | 0 | +~30 B | 0 | parseEvaluationResponse는 services에 + archive/stats handler에 escape 1줄 |
| T3 | 0 | 0 | 0 | 0 | 0 | sw.js 별도 (vite bundling 외) |
| T4 | +~30 B | 0 | 0 | +~10 B | 0 | isSafeUrl helper |
| T5 | +~80 B | 0 | 0 | 0 | 0 | reportAsync helper (utils) — main.ts에서 사용 |
| T6 | 0 | 0 | 0 | -100~-300 B | 0 | dead control 제거 |
| **Net** | **+~100 B** | **+5~12 KB** | **미세** | **-50~-150 B** | **+~5 KB or shared** | T1 dominant — over 시 lazy load |

**DoD**: canonical (index + home + settings + firebase shared) Δ ≤ **+15 KB gzip**. over 시 **in-cycle lazy load 의무** (`slack.ts` 내부 `await import('./appCheck')`).
