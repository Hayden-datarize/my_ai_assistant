# v3.36 Plant Action Layer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v3.21 carry 6개월 누적 청산 — Plant detail modal 하단에 navigation chip 1개 추가 + archive search prefill 동적 흐름.

**Architecture:** plant-detail.ts bodyHtml → bodyNode migration (v3.26 T3 contract). makeActionChip(interestId) DOM button + navigateToInterestArchive(closeModal → switchTab('archive') → 동적 import handleArchiveSearch). 한국어 keyword 우선 추출 (interestKeywords 영문 id fallback). void Promise.catch로 click handler async safety.

**Tech Stack:** TypeScript / Vite SPA / Vitest / Playwright / ESLint / CSS custom properties.

**Spec**: `docs/superpowers/specs/2026-05-17-v3.36-plant-action-layer-design.md` (v2 — Codex 사전 review P0=0/P1=4/P2=5 + Coverage Gap 모두 in-cycle 흡수).

**Baseline (T0)**: HEAD `977437d` / bundle 19080 (index) + 7182 (archive 합산) + **8182 (stats 합산, v3.36 신규 측정)** / vitest 1336 / smoke 100 / lint 0.

---

### Task 0: spec v2 + plan v1 묶음 T0 commit

**Files:**
- Modify: `docs/superpowers/specs/2026-05-17-v3.36-plant-action-layer-design.md` (v2, 이미 P1/P2 흡수 완료)
- Create: `docs/superpowers/plans/2026-05-17-v3.36-plant-action-layer-plan.md` (본 파일)

- [ ] **Step 1: stats chunk baseline 측정** ✅ 실측 완료

```bash
gzip -c dist/assets/stats-*.js | wc -c
```
Actual: **8182 B** (stats handler + stats render 두 chunk 합산).

- [ ] **Step 2: T0 commit (spec v2 + plan v1 묶음, Codex 사전 review 이미 받음)**

```bash
git add docs/superpowers/specs/2026-05-17-v3.36-plant-action-layer-design.md docs/superpowers/plans/2026-05-17-v3.36-plant-action-layer-plan.md
git commit -m "$(cat <<'EOF'
docs(v3.36 T0): spec v2 + plan v1 + Codex 사전 review CLI direct (P0=0/P1=4/P2=5 + Coverage Gap 모두 in-cycle 흡수)

baseline: HEAD 977437d / bundle 19080 (index) + 7182 (archive 합산) + 8182 (stats 합산, v3.36 신규 측정) / vitest 1336 / smoke 100 / lint 0.

Codex 사전 review 흡수:
- P1-1 stats chunk 정정 — plant-detail이 stats chunk로 split됨, handleArchiveSearch는 동적 import 의무 (archive handler 의존성 누수 차단)
- P1-2 CSS token 매핑 — 현행 tokens.css (--primary / --primary-ultra-light / --space-2/4 / --font-size-sm / --duration-fast / --ease-spring / --focus-ring) + 999px pill 직접
- P1-3 interestKeywords[0]는 영문 id이라 한국어 archive content 매칭 안 됨 — pickSearchKeyword에서 한국어 우선 fallback to id
- P1-4 async navigation rejection — void Promise.catch + console.warn

P2 + Coverage Gap 흡수:
- 테스트 위치: tests/ui/modals/plant-detail.spec.ts 기존 확장 (신규 file X)
- smoke 확장: tests/smoke/plant-detail-modal.spec.ts에 chip click → archive prefill 1건 추가
- bundle triple record: index + archive + stats 3중 측정
EOF
)"
```

---

### Task 1: plant-detail.ts bodyNode migration + action chip + navigateToInterestArchive + 신규 spec 3건

**Files:**
- Modify: `src/ui/modals/plant-detail.ts` (renderBody return type string → HTMLElement + makeActionChip + navigateToInterestArchive + pickSearchKeyword)
- Modify: `tests/ui/modals/plant-detail.spec.ts` (3 신규 spec 추가)

- [ ] **Step 1: 신규 spec 3건 추가 (TDD red gate)**

`tests/ui/modals/plant-detail.spec.ts` 끝에 추가:

```ts
import { interestKeywords } from '../../../src/utils/interestKeywords';

describe('plant-detail action chip (v3.36)', () => {
  beforeEach(() => {
    document.body.replaceChildren();
    localStorage.clear();
  });

  it('chip render — .plant-action-chip button DOM 존재 + aria-label', () => {
    saveUser(mkUser({
      interests: ['leadership'],
      plantStateByInterest: { leadership: { stage: 2, cumulativeActivity: 10 } } satisfies Record<string, PlantState>,
    }));
    openPlantDetailModal('leadership');
    const chip = document.querySelector<HTMLButtonElement>('.plant-action-chip');
    expect(chip).toBeTruthy();
    expect(chip!.tagName).toBe('BUTTON');
    expect(chip!.getAttribute('aria-label')).toBe('이 분야 키워드로 archive 검색');
    expect(chip!.textContent).toContain('archive 탐색');
  });

  it('chip click → modal 제거 + #archiveTab 활성 + #archiveSearch에 한국어 keyword prefill', async () => {
    // jsdom: #app 컨테이너에서 switchTab이 archive 탭 렌더하므로 사전 setup 필요
    document.body.innerHTML = '<div id="app"></div>';
    saveUser(mkUser({
      interests: ['leadership'],
      plantStateByInterest: { leadership: { stage: 2, cumulativeActivity: 10 } } satisfies Record<string, PlantState>,
    }));
    openPlantDetailModal('leadership');
    const chip = document.querySelector<HTMLButtonElement>('.plant-action-chip')!;
    chip.click();
    // void Promise — microtask 처리 대기
    await new Promise((r) => setTimeout(r, 0));
    await new Promise((r) => setTimeout(r, 0));

    // modal 제거 검증
    expect(document.querySelector('.dg-modal')).toBeFalsy();
    // archive search input에 한국어 keyword prefill
    const input = document.querySelector<HTMLInputElement>('#archiveSearch');
    expect(input).toBeTruthy();
    expect(input!.value).toBe('리더십');  // interestKeywords('leadership')에서 한국어 첫 token
  });

  it('pickSearchKeyword fallback — interestKeywords가 한국어 token 0개일 때 첫 영문 id 사용', () => {
    // 직접 export 안 됨 — interestKeywords 동작 확인으로 invariant 검증
    const tokens = interestKeywords('leadership');
    const koreanFirst = tokens.find((t) => /[가-힯]/.test(t));
    expect(koreanFirst).toBe('리더십');  // 한국어 token 존재 시
    // unknown id (categories에 없음) → [id.toLowerCase()] 반환, 한국어 token 0개
    const unknownTokens = interestKeywords('xyz_unknown');
    const unknownFallback = unknownTokens.find((t) => /[가-힯]/.test(t)) ?? unknownTokens[0];
    expect(unknownFallback).toBe('xyz_unknown');
  });
});
```

- [ ] **Step 2: 3 신규 spec fail 확인**

```bash
npm test -- tests/ui/modals/plant-detail.spec.ts --run 2>&1 | tail -10
```
Expected: 3 fail (`.plant-action-chip` 없음 / chip click no-op / pickSearchKeyword 첫 케이스는 PASS 가능, 두 번째 PASS — 마지막 spec은 helper만 검증이라 빨간 게이트는 1+2번만).

- [ ] **Step 3: plant-detail.ts 변경 — bodyNode + chip + navigate**

```ts
import { openModal, closeModal } from './shared';
import { loadUserData } from '../../state/user';
import { INTEREST_LABEL } from '../components/garden-grid';
import { getPlantIcon, STAGE_LABEL, STAGE_THRESHOLDS, TROPHY_MARK } from '../../state/plantCatalog';
import { checkWilting } from '../../state/plantEngine';
import { escapeHtml } from '../../utils/escapeHtml';
import { getAnswerCountByInterest, getScrapCountByInterest } from '../../utils/interestCounts';
import { getKstDateStr, formatRelative } from '../../utils/dates';
import { interestKeywords } from '../../utils/interestKeywords';
import { switchTab } from '../nav';
import type { User } from '../../state/user';
import type { PlantState } from '../../state/plantTypes';

/**
 * Plant detail modal — stats 탭 garden card 클릭 시 표시.
 * v3.36 T1: bodyHtml → bodyNode migration (v3.26 T3 contract) + action chip + navigateToInterestArchive.
 */
export function openPlantDetailModal(interestId: string): void {
  const u = loadUserData();
  if (!u) return;
  const plant = u.plantStateByInterest[interestId];
  if (!plant) return;

  const title = INTEREST_LABEL[interestId] ?? interestId;
  const bodyNode = renderBody(u, interestId, plant);

  openModal({
    title: `${title} 정원`,
    bodyNode,
  });
}

function renderBody(_u: User, interestId: string, plant: PlantState): HTMLElement {
  const icon = escapeHtml(getPlantIcon(interestId, plant.stage));
  const stageLabel = escapeHtml(STAGE_LABEL[plant.stage]);
  const cum = Number.isFinite(plant.cumulativeActivity) ? Math.floor(plant.cumulativeActivity) : 0;

  const stageHtml = `
    <div class="plant-detail-stage">
      <span class="plant-detail-emoji">${icon}</span>
      <span class="plant-detail-stage-label">${stageLabel}</span>
    </div>`;

  const progressHtml = plant.stage === 5
    ? `<div class="plant-detail-progress">만개 ${escapeHtml(TROPHY_MARK)}</div>`
    : `<div class="plant-detail-progress">${cum} / ${STAGE_THRESHOLDS[plant.stage - 1]}</div>`;

  const unlockedHtml = plant.unlockedAt
    ? `<div class="plant-detail-unlocked">✨ ${escapeHtml(formatKoreanDate(plant.unlockedAt))} 도달</div>`
    : '';

  const engagedHtml = plant.lastEngagedAt
    ? `<div class="plant-detail-engaged">마지막 활동 ${escapeHtml(formatRelative(plant.lastEngagedAt))}</div>`
    : `<div class="plant-detail-engaged">활동 기록 없음</div>`;

  const wilting = checkWilting(plant, new Date());
  const wiltingHtml = wilting
    ? `<div class="plant-detail-wilting">🥀 7일 이상 활동이 없어요. 다시 가꿔주세요</div>`
    : '';

  const answers = getAnswerCountByInterest(interestId);
  const scraps = getScrapCountByInterest(interestId);
  const countsHtml = `<div class="plant-detail-counts">${answers}개 답변 · ${scraps}개 스크랩</div>`;

  const root = document.createElement('div');
  root.className = 'plant-detail-modal';
  // eslint-disable-next-line no-restricted-syntax -- bodyNode 내부 static HTML, 각 sub-string은 escapeHtml 적용 끝
  root.innerHTML = stageHtml + progressHtml + unlockedHtml + engagedHtml + wiltingHtml + countsHtml;

  // v3.36 T1: action chip (DOM element + onclick listener)
  root.append(makeActionChip(interestId));

  return root;
}

function makeActionChip(interestId: string): HTMLButtonElement {
  const btn = document.createElement('button');
  btn.type = 'button';
  btn.className = 'plant-action-chip';
  btn.textContent = '📚 이 분야로 archive 탐색';
  btn.setAttribute('aria-label', '이 분야 키워드로 archive 검색');
  btn.addEventListener('click', () => {
    void navigateToInterestArchive(interestId).catch((err) => {
      console.warn('[plant-action] navigate failed', err);
    });
  });
  return btn;
}

/**
 * Codex P1-3 흡수: 한국어 token 우선 추출 (interestKeywords [0]은 영문 id이라 한국어 archive 매칭 거의 안 됨).
 */
function pickSearchKeyword(interestId: string): string | undefined {
  const tokens = interestKeywords(interestId);
  return tokens.find((t) => /[가-힯]/.test(t)) ?? tokens[0];
}

async function navigateToInterestArchive(interestId: string): Promise<void> {
  const keyword = pickSearchKeyword(interestId);
  if (!keyword) return;

  closeModal();
  await switchTab('archive');

  const input = document.querySelector<HTMLInputElement>('#archiveSearch');
  if (!input) return;
  input.value = keyword;

  // Codex P1-1 흡수: 동적 import — plant-detail은 stats chunk이라 archive handler를 끌어오지 않도록
  const { handleArchiveSearch } = await import('../handlers/archive');
  handleArchiveSearch();
}

/**
 * ISO → "YYYY년 M월 D일" (KST anchor).
 * v3.22 T4: 머신 TZ 무관 — getKstDateStr (Intl Asia/Seoul) 재사용.
 * @internal — spec 직접 호출용 export.
 */
export function formatKoreanDate(iso: string): string {
  const [y, m, d] = getKstDateStr(new Date(iso)).split('-');
  return `${y}년 ${Number(m)}월 ${Number(d)}일`;
}
```

- [ ] **Step 4: 3 spec PASS 확인**

```bash
npm test -- tests/ui/modals/plant-detail.spec.ts --run 2>&1 | tail -10
```
Expected: 모든 spec passed (기존 + 3 신규).

- [ ] **Step 5: tsc + lint**

```bash
npx tsc --noEmit && npm run lint
```
Expected: 0 errors / 0 problems.

- [ ] **Step 6: commit T1**

```bash
git add src/ui/modals/plant-detail.ts tests/ui/modals/plant-detail.spec.ts
git commit -m "$(cat <<'EOF'
feat(v3.36 T1): plant-detail bodyNode migration + action chip + navigateToInterestArchive

v3.21 carry 6개월 누적 청산 (v3.22~v3.35 14 사이클 deferred). Plant detail modal 하단에 navigation chip 추가:
- bodyHtml → bodyNode migration (v3.26 T3 contract — listener 보존)
- makeActionChip: button + onclick + void Promise.catch (Codex P1-4 async safety)
- navigateToInterestArchive: closeModal → await switchTab('archive') → handleArchiveSearch 동적 import (Codex P1-1 archive handler stats chunk 누수 차단)
- pickSearchKeyword: 한국어 token 우선 + fallback to interestKeywords[0] (Codex P1-3, 영문 id 매칭 약점 보완)

신규 spec 3건 (tests/ui/modals/plant-detail.spec.ts 확장): chip render / chip click flow / pickSearchKeyword 한국어 fallback invariant.
EOF
)"
```

---

### Task 2: CSS `.plant-action-chip` 추가

**Files:**
- Modify: `src/styles/components/plant-detail.css` 또는 `src/styles/pages/stats.css` (실제 위치 확인 후 작성)

- [ ] **Step 1: 기존 plant-detail CSS 위치 확인**

```bash
grep -rln "plant-detail-modal\|plant-detail-stage" src/styles/ | head -5
```
Expected: 단일 file 또는 stats.css. 발견된 file에 .plant-action-chip 규칙 추가.

- [ ] **Step 2: CSS 추가 (현행 tokens.css 매핑 — Codex P1-2 흡수)**

발견된 file 끝에 추가:

```css
/* v3.36 T2: plant detail modal action chip */
.plant-action-chip {
  margin-top: var(--space-4);
  padding: var(--space-2) var(--space-4);
  background: var(--primary-ultra-light);
  color: var(--primary);
  border: 1px solid var(--primary);
  border-radius: 999px;
  font-size: var(--font-size-sm);
  font-weight: 600;
  cursor: pointer;
  width: 100%;
  text-align: center;
  transition: background var(--duration-fast) var(--ease-spring);
}

.plant-action-chip:hover {
  background: var(--primary);
  color: white;
}

.plant-action-chip:focus-visible {
  box-shadow: var(--focus-ring);
  outline: none;
}
```

- [ ] **Step 3: build 확인 + chip 시각 정합**

```bash
npm run build 2>&1 | tail -3
```
Expected: built successfully.

- [ ] **Step 4: commit T2**

```bash
git add src/styles/
git commit -m "$(cat <<'EOF'
style(v3.36 T2): .plant-action-chip CSS — 현행 tokens.css 매핑 (Codex P1-2 흡수)

pill button (999px radius, --primary-ultra-light background, hover --primary). focus-visible box-shadow with --focus-ring. spacing tokens --space-2/4, font-size-sm, transition duration-fast + ease-spring. 신규 design token 0개.
EOF
)"
```

---

### Task 3: smoke 확장 — chip click → archive prefill 검증 (Codex Coverage Gap)

**Files:**
- Modify: `tests/smoke/plant-detail-modal.spec.ts`

- [ ] **Step 1: 기존 smoke 파일 구조 확인**

```bash
sed -n '1,40p' tests/smoke/plant-detail-modal.spec.ts
```
Expected: Playwright test fixture. saveUser/plantState seed pattern + click → modal 검증.

- [ ] **Step 2: chip click → archive prefill case 1건 추가 (파일 끝)**

```ts
test('v3.36: plant detail chip → archive 탭 + 검색어 prefill', async ({ page }) => {
  // 기존 smoke의 setup 패턴 재사용 — primeOnboardedUser + plantState seed (leadership stage 2)
  await primeOnboardedUser(page, {
    interests: ['leadership'],
    plantStateByInterest: { leadership: { stage: 2, cumulativeActivity: 10 } },
  });
  await page.goto('/');

  // stats 탭 진입 + leadership 정원 카드 클릭 → modal open
  await page.locator('[data-tab="stats"]').click();
  await page.locator('.garden-grid button[data-interest-id="leadership"]').click();
  await expect(page.locator('.plant-detail-modal')).toBeVisible();

  // chip click
  await page.locator('.plant-action-chip').click();

  // modal 제거 + archive 탭 활성 + search input prefill 검증
  await expect(page.locator('.dg-modal')).toBeHidden();
  await expect(page.locator('[data-tab="archive"].active, [data-tab="archive"][aria-selected="true"]')).toBeVisible();
  await expect(page.locator('#archiveSearch')).toHaveValue('리더십');
});
```

**Note**: `primeOnboardedUser` helper signature 및 stats tab/garden-card selector는 기존 smoke 파일 패턴에 정확히 맞춤 — Step 1에서 확인된 실제 selector 사용.

- [ ] **Step 3: smoke 1건 PASS 확인**

```bash
npm run test:smoke -- plant-detail-modal 2>&1 | tail -10
```
Expected: 모든 plant-detail-modal smoke PASS (기존 + 1 신규).

- [ ] **Step 4: commit T3**

```bash
git add tests/smoke/plant-detail-modal.spec.ts
git commit -m "$(cat <<'EOF'
test(v3.36 T3): smoke 확장 — plant action chip → archive 검색어 prefill end-to-end

Codex Coverage Gap 흡수. plant detail modal → chip click → modal 제거 + archive 탭 활성 + #archiveSearch.value=리더십 검증 (real switchTab + dynamic import + handleArchiveSearch 흐름).
EOF
)"
```

---

### Task 5: 사이클-끝 검증 + bundle triple + Codex 최종 review

- [ ] **Step 1: 3종 검증**

```bash
npx tsc --noEmit && npm run lint && npm test -- --run
```
Expected: 0 errors / 0 problems / **1339 passed** (baseline 1336 + T1 +3).

- [ ] **Step 2: 양쪽 TZ vitest**

```bash
TZ=Asia/Seoul npm test -- --run 2>&1 | tail -3
TZ=America/New_York npm test -- --run 2>&1 | tail -3
```
Expected: 양쪽 1339 drift 0.

- [ ] **Step 3: smoke**

```bash
npm run test:smoke 2>&1 | tail -5
```
Expected: **101 passed** (baseline 100 + T3 +1).

- [ ] **Step 4: build + bundle triple 측정**

```bash
npm run build && echo "=== index ===" && gzip -c dist/assets/index-*.js | wc -c && echo "=== archive ===" && gzip -c dist/assets/archive-*.js | wc -c && echo "=== stats ===" && gzip -c dist/assets/stats-*.js | wc -c
```
Expected:
- index ≤ 19380 (GREEN strict ≤ +300)
- archive 7182 ± 0 (dynamic import이라 archive handler 코드는 archive chunk 그대로)
- stats ≤ 8382 (≤ +200 신규 chip + navigateToInterestArchive 함수)

- [ ] **Step 5: 신규 spec N=5 + smoke spec N=3**

```bash
for i in 1 2 3 4 5; do npm test -- tests/ui/modals/plant-detail.spec.ts --run 2>&1 | grep -E "Tests" | head -1; done
for i in 1 2 3; do npm run test:smoke -- plant-detail-modal 2>&1 | grep -E "passed|failed" | head -1; done
```
Expected: 5/5 vitest 0 flake + 3/3 smoke 0 flake.

- [ ] **Step 6: Codex 최종 review CLI direct**

```bash
codex exec --skip-git-repo-check -o /tmp/v3.36-codex-final.txt "v3.36 Plant action layer 사이클 최종 review.

Cycle range: 122dd43..HEAD (T0~T3 commits).

Spec: docs/superpowers/specs/2026-05-17-v3.36-plant-action-layer-design.md (v2)
Plan: docs/superpowers/plans/2026-05-17-v3.36-plant-action-layer-plan.md

변경 요약:
- T1 src/ui/modals/plant-detail.ts: bodyHtml→bodyNode migration + makeActionChip + navigateToInterestArchive (closeModal → await switchTab('archive') → 동적 import handleArchiveSearch) + pickSearchKeyword (한국어 우선 fallback to id).
- T1 tests/ui/modals/plant-detail.spec.ts: 3 신규 spec (chip render / chip click flow / keyword 한국어 fallback invariant).
- T2 .plant-action-chip CSS (현행 tokens.css 매핑).
- T3 tests/smoke/plant-detail-modal.spec.ts: chip click → archive prefill E2E 1건.

검증:
- tsc 0 / lint 0
- vitest 1339 양쪽 TZ drift 0
- smoke 101/101
- Bundle triple: index 19080→___ / archive 7182→___ / stats 8182→___

Classification: DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT. P0/P1/P2 명시.

중점:
1. dynamic import 동작 (archive handler가 stats chunk로 끌려오는지 archive chunk 그대로인지).
2. pickSearchKeyword 한국어 fallback 정확도.
3. XSS — chip 코드 리터럴, search input은 raw 값 OK.
4. focus race — closeModal → switchTab focus 손실 risk.
5. spec coverage gap." < /dev/null 2>&1 | tail -50
```

Expected: APPROVED_WITH_NOTES 이상.

---

### Task 6: Retro

- [ ] **Step 1: spec §6 retro 채움 (final HEAD / task table / lint / bundle triple / Codex 결과 / lessons / carry)**

- [ ] **Step 2: MEMORY.md entry + 신규 memory file `project_daily_growth_v336.md`**

- [ ] **Step 3: Obsidian vault sync (02-Cycles / 03-Lessons / 00-Index)**

- [ ] **Step 4: commit T6**

```bash
git add docs/superpowers/specs/2026-05-17-v3.36-plant-action-layer-design.md
git commit -m "$(cat <<'EOF'
docs(v3.36 T6 retro): 사이클 완료 — Plant action layer (v3.21 carry 6개월 청산)

bundle triple (index ___ / archive ___ / stats ___) / vitest 1339 / smoke 101 / lint 0. v3.21 carry 14 사이클 누적 청산. Codex 사전 P1=4/P2=5 모두 in-cycle / 최종 ___.
EOF
)"
```

---

## Self-Review

**Spec coverage**:
- §2.1 file 변경 7건 → T1 (4 file) + T2 (CSS) + T3 (smoke) ✓
- §2.2 bodyNode migration → T1 Step 3 ✓
- §2.3 navigateToInterestArchive (dynamic import + 한국어 keyword + void catch) → T1 Step 3 ✓
- §2.4 CSS 현행 token 매핑 → T2 Step 2 ✓
- §3.1/3.2 bundle triple → T5 Step 4 ✓
- §5 5 task → T0~T3 + T5 + T6 ✓
- spec §4 risk 모두 spec/plan 단계에서 결정됨

**Placeholder scan**: T0 commit msg / T6 retro msg에 `___`는 사이클 진행 후 채움. 그 외 placeholder 0.

**Type consistency**: `openPlantDetailModal(interestId: string): void` / `renderBody(...): HTMLElement` / `makeActionChip(interestId): HTMLButtonElement` / `navigateToInterestArchive(interestId): Promise<void>` / `pickSearchKeyword(interestId): string | undefined` — 모두 spec § 2.3 정합.
