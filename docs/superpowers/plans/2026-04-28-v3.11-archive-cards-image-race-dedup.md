# v3.11 Implementation Plan — Archive 카드 풍부화 + 이미지 race fix + 브리핑 dedup/소스 확장

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Daily Growth v3.11 사이클 — 사용자 입력 3건(archive 카드 풍부화 / 홈 이미지 race fix / 브리핑 중복 방지 + 소스 확장)을 한 사이클에 통합 배포.

**Architecture:**
- 신규 `state/seen.ts` 모듈로 `seenBriefings` localStorage 추적 (URL 키 + 30d retention + 500 LRU cap)
- `interestToFeed` 1:1 → `interestToFeeds` 1:N 매핑 (6→15 unique feeds, KR 8 + EN 7)
- `renderBriefingCard`을 home.ts에서 export → archive 스크랩 카드에서 재사용 (시각 통일)
- archive 답변 카드는 신규 `renderAnswerCard`로 재구성 (질문 미리보기 + 유형 칩 + 답변 3줄 클램프)
- `Answer.questionText?: string` optional 필드 + submitAnswer 시점 저장. 기존 답변은 graceful degrade

**Tech Stack:** TypeScript, Vite, Vitest, Playwright, localStorage, no framework (vanilla DOM)

**Spec:** `docs/superpowers/specs/2026-04-28-v3.11-archive-cards-image-race-dedup-design.md`

**Baseline:** main `7491486` (v3.10 머지 직후) + `dd47380`/`77edbfc`(v3.11 spec 커밋)

---

## File Structure

| 파일 | 변경 종류 | 책임 |
|---|---|---|
| `src/state/seen.ts` | **신규** | seenBriefings persistence (load/save/isSeen/recordSeen/purgeExpiredSeen). Briefing 모델과 분리 |
| `src/state/schema.ts` | **수정** | `Answer.questionText?: string` optional 필드 추가 |
| `src/ui/handlers/home.ts` | **수정** | `interestToFeeds` (1:N), `refreshBriefings` dedup wiring, 첫 3장 eager, submitAnswer questionText 저장, `renderBriefingCard` export |
| `src/ui/handlers/archive.ts` | **수정** | rerenderList — 답변 카드는 `renderAnswerCard` 신설, 스크랩 카드는 `renderBriefingCard` 재사용 |
| `src/styles/main.css` (또는 archive 관련 layer) | **수정** | 답변 카드 풍부 layout 토큰. 스크랩 카드는 briefing 카드 토큰 재사용 (overlay/뱃지/이미지) |
| `tests/unit/seen.spec.ts` | **신규** | seen module 단위 테스트 (~6 it-block) |
| `tests/unit/home-dedup.spec.ts` | **신규** | refreshBriefings dedup 통합 (mock fetchFeed + mock seen) (~4) |
| `tests/unit/home-rss.spec.ts` 또는 별도 | **확장** | interestToFeeds 1:N 검증 + cap 8 (~3) |
| `tests/unit/archive-cards.spec.ts` | **확장** | renderAnswerCard 신규 layout (~5) |
| `tests/unit/archive-scrap-card.spec.ts` | **신규** | scrap 카드 = briefing 카드 시각 동일성 (~3) |
| `tests/unit/eager-loading.spec.ts` | **신규** | idx<3 → eager / idx≥3 → lazy (~3) |
| `tests/unit/answer-schema.spec.ts` 또는 기존 schema 테스트 확장 | **확장** | questionText optional roundtrip (~1) |
| `tests/playwright/v311-archive-cards.spec.ts` | **신규** | archive 탭 카드 가시 + 답변/스크랩 모두 그려짐 (1~2 smoke) |

**참고**: CSP `img-src 'self' data: https:` 와일드카드 허용 확인됨(`index.html`). 신규 RSS 소스의 외부 image 호스트 추가 **불필요**.

---

## Task 0: Baseline + Branch 셋업

**Files:**
- Modify: 없음 (메타 작업)

- [ ] **Step 0.1: 브랜치 생성**

```bash
git checkout main
git pull --ff-only origin main 2>/dev/null || true   # origin 미등록일 수 있음, 무시
git checkout -b feat/v3.11-archive-cards-dedup-sources
```

- [ ] **Step 0.2: 기존 테스트 baseline 확인**

```bash
npm run test 2>&1 | tail -5
```
Expected: `Tests  413 passed (413)` (v3.10 baseline)

```bash
npm run test:smoke 2>&1 | tail -5
```
Expected: `81 passed`

```bash
npm run lint 2>&1 | tail -5
```
Expected: 0 error 0 warn

- [ ] **Step 0.3: 번들 baseline 기록**

```bash
npm run build 2>&1 | grep -E "dist/.*\.js|dist/.*\.css"
```
기록할 값: JS gzip / CSS gzip (예: `gzip: 24.25 kB` JS + `gzip: 7.29 kB` CSS) — 본 plan T8 verification gate에서 비교용.

- [ ] **Step 0.4: T0 commit (메타만, 코드 변경 없음 — skip 또는 plan 동기화 시점에 한 번에)**

T1부터 코드 변경 시작.

---

## Task 1: `state/seen.ts` 모듈 + 단위 테스트 (TDD)

**Files:**
- Create: `src/state/seen.ts`
- Create: `tests/unit/seen.spec.ts`

- [ ] **Step 1.1: 실패하는 테스트 작성**

`tests/unit/seen.spec.ts`:

```typescript
import { describe, it, expect, beforeEach, vi, afterEach } from 'vitest';
import { isSeen, recordSeen, purgeExpiredSeen } from '../../src/state/seen';

const KEY = 'seenBriefings';
const TTL_MS = 30 * 24 * 60 * 60 * 1000;

describe('state/seen', () => {
  beforeEach(() => localStorage.clear());
  afterEach(() => vi.useRealTimers());

  it('isSeen returns false when localStorage empty', () => {
    expect(isSeen('https://example.com/a')).toBe(false);
  });

  it('isSeen returns false on malformed JSON', () => {
    localStorage.setItem(KEY, '{not valid json');
    expect(isSeen('https://example.com/a')).toBe(false);
  });

  it('recordSeen stores urls with current timestamp', () => {
    const now = Date.parse('2026-04-28T00:00:00Z');
    recordSeen(['https://x.com/1', 'https://x.com/2'], now);
    const stored = JSON.parse(localStorage.getItem(KEY)!);
    expect(stored).toHaveLength(2);
    expect(stored.every((r: { firstSeenAt: number }) => r.firstSeenAt === now)).toBe(true);
  });

  it('recordSeen is idempotent for existing url (firstSeenAt unchanged)', () => {
    const t1 = Date.parse('2026-04-01T00:00:00Z');
    const t2 = Date.parse('2026-04-28T00:00:00Z');
    recordSeen(['https://x.com/1'], t1);
    recordSeen(['https://x.com/1'], t2);
    const stored = JSON.parse(localStorage.getItem(KEY)!);
    expect(stored).toHaveLength(1);
    expect(stored[0].firstSeenAt).toBe(t1);   // 처음 본 시점 유지
  });

  it('recordSeen LRU cap = 500 (drops oldest)', () => {
    const now = Date.parse('2026-04-28T00:00:00Z');
    // 500개 채운 후 추가 1개 → MAX 유지, 가장 오래된 항목 drop
    const initial = Array.from({ length: 500 }, (_, i) => ({
      url: `https://x.com/${i}`,
      firstSeenAt: now - (500 - i) * 1000,   // url0이 가장 오래됨
    }));
    localStorage.setItem(KEY, JSON.stringify(initial));
    recordSeen(['https://x.com/new'], now);
    const stored = JSON.parse(localStorage.getItem(KEY)!) as Array<{ url: string }>;
    expect(stored).toHaveLength(500);
    expect(stored.find((r) => r.url === 'https://x.com/new')).toBeDefined();
    expect(stored.find((r) => r.url === 'https://x.com/0')).toBeUndefined();
  });

  it('isSeen respects 30d retention', () => {
    const now = Date.parse('2026-04-28T00:00:00Z');
    const day29 = now - 29 * 24 * 60 * 60 * 1000;
    const day31 = now - 31 * 24 * 60 * 60 * 1000;
    localStorage.setItem(KEY, JSON.stringify([
      { url: 'https://recent.com', firstSeenAt: day29 },
      { url: 'https://old.com', firstSeenAt: day31 },
    ]));
    expect(isSeen('https://recent.com', now)).toBe(true);
    expect(isSeen('https://old.com', now)).toBe(false);
  });

  it('purgeExpiredSeen removes only expired records', () => {
    const now = Date.parse('2026-04-28T00:00:00Z');
    localStorage.setItem(KEY, JSON.stringify([
      { url: 'https://recent.com', firstSeenAt: now - 1000 },
      { url: 'https://old.com', firstSeenAt: now - TTL_MS - 1000 },
    ]));
    purgeExpiredSeen(now);
    const stored = JSON.parse(localStorage.getItem(KEY)!) as Array<{ url: string }>;
    expect(stored).toHaveLength(1);
    expect(stored[0].url).toBe('https://recent.com');
  });
});
```

- [ ] **Step 1.2: 테스트 실패 확인**

```bash
npm run test -- tests/unit/seen.spec.ts 2>&1 | tail -10
```
Expected: `FAIL` — 모듈 미존재

- [ ] **Step 1.3: 모듈 구현**

`src/state/seen.ts`:

```typescript
const KEY = 'seenBriefings';
const MAX = 500;
const TTL_MS = 30 * 24 * 60 * 60 * 1000;

interface SeenRecord {
  url: string;
  firstSeenAt: number;
}

function loadSeen(): SeenRecord[] {
  try {
    const raw = JSON.parse(localStorage.getItem(KEY) ?? '[]') as unknown;
    if (!Array.isArray(raw)) return [];
    return raw.filter(
      (r): r is SeenRecord =>
        typeof r === 'object'
        && r !== null
        && typeof (r as SeenRecord).url === 'string'
        && typeof (r as SeenRecord).firstSeenAt === 'number',
    );
  } catch {
    return [];
  }
}

function saveSeen(list: SeenRecord[]): void {
  localStorage.setItem(KEY, JSON.stringify(list));
}

/** 30일 retention 안에 있으면 true (= 노출 차단 신호). */
export function isSeen(url: string, now: number = Date.now()): boolean {
  const rec = loadSeen().find((r) => r.url === url);
  return !!rec && rec.firstSeenAt + TTL_MS > now;
}

/** chosen url 목록을 seen에 기록. 기존 url의 firstSeenAt은 보존(idempotent). LRU cap = 500. */
export function recordSeen(urls: string[], now: number = Date.now()): void {
  if (urls.length === 0) return;
  const existing = loadSeen();
  const existingMap = new Map<string, SeenRecord>(existing.map((r) => [r.url, r]));
  for (const url of urls) {
    if (!existingMap.has(url)) existingMap.set(url, { url, firstSeenAt: now });
  }
  const next = Array.from(existingMap.values())
    .sort((a, b) => b.firstSeenAt - a.firstSeenAt)   // 최신 우선
    .slice(0, MAX);
  saveSeen(next);
}

/** retention 만료된 항목 제거 (옵셔널 housekeeping). */
export function purgeExpiredSeen(now: number = Date.now()): void {
  const list = loadSeen();
  const next = list.filter((r) => r.firstSeenAt + TTL_MS > now);
  if (next.length !== list.length) saveSeen(next);
}
```

- [ ] **Step 1.4: 테스트 통과 확인**

```bash
npm run test -- tests/unit/seen.spec.ts 2>&1 | tail -5
```
Expected: `Tests  7 passed (7)`

- [ ] **Step 1.5: N=5 안정성 확인**

```bash
for i in 1 2 3 4 5; do npm run test -- tests/unit/seen.spec.ts 2>&1 | tail -1; done
```
Expected: 5번 모두 `passed`

- [ ] **Step 1.6: Lint + tsc**

```bash
npm run lint 2>&1 | tail -3
npm run build 2>&1 | tail -10
```
Expected: 0 error 0 warn / build OK

- [ ] **Step 1.7: Commit**

```bash
git add src/state/seen.ts tests/unit/seen.spec.ts
git commit -m "feat(v3.11 T1): add state/seen module for briefing dedup tracking

- URL key + firstSeenAt epoch ms
- isSeen(url, now): 30d retention check
- recordSeen(urls, now): idempotent + 500 LRU cap
- purgeExpiredSeen(now): housekeeping
- 7 it-block (empty/malformed/store/idempotent/LRU/retention/purge)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: `Answer.questionText` optional 필드 + submitAnswer 저장

**Files:**
- Modify: `src/state/schema.ts:9-21` (인터페이스 확장)
- Modify: `src/ui/handlers/home.ts:693-700` (submitAnswer)
- Create: `tests/unit/answer-schema.spec.ts` (또는 기존 확장)

- [ ] **Step 2.1: 실패하는 테스트 작성**

`tests/unit/answer-schema.spec.ts` (신규):

```typescript
import { describe, it, expect } from 'vitest';
import { makeAnswer, type Answer } from '../../src/state/schema';

describe('Answer.questionText optional', () => {
  it('makeAnswer accepts and persists questionText field', () => {
    const a: Answer = makeAnswer({
      id: 'a1',
      questionId: 'q1',
      text: 'my answer',
      authorId: 'self',
      questionText: '오늘의 채용 미스매치 원인은?',
      type: '분석',
      date: '2026-04-28',
    });
    expect(a.questionText).toBe('오늘의 채용 미스매치 원인은?');
    expect(a.schemaVersion).toBe(1);
  });

  it('makeAnswer omits questionText when not provided (graceful for legacy)', () => {
    const a: Answer = makeAnswer({
      id: 'a2',
      questionId: 'q2',
      text: 'old answer',
      authorId: 'self',
    });
    expect(a.questionText).toBeUndefined();
  });
});
```

- [ ] **Step 2.2: 테스트 실패 확인**

```bash
npm run test -- tests/unit/answer-schema.spec.ts 2>&1 | tail -10
```
Expected: TS error — `Object literal may only specify known properties, and 'questionText' does not exist in type ...`

- [ ] **Step 2.3: schema 확장**

`src/state/schema.ts:9-21`:

```typescript
export interface Answer extends Versioned {
  id: string;
  questionId: string;
  text: string;
  authorId: string;
  createdAt: string;
  /** v3.11: 답변 시점의 질문 본문. 기존 답변엔 없음(undefined → archive에서 graceful degrade). */
  questionText?: string;
  /** legacy: question type label ('분석' | '전환' | '실무' | '성장' | '트렌드' | etc.). */
  type?: string;
  evaluation?: { score: number; feedback: string };
  date?: string;
}
```

- [ ] **Step 2.4: 테스트 통과 확인**

```bash
npm run test -- tests/unit/answer-schema.spec.ts 2>&1 | tail -5
```
Expected: `Tests  2 passed (2)`

- [ ] **Step 2.5: submitAnswer에 questionText wiring**

`src/ui/handlers/home.ts:693`:

```typescript
const answer = makeAnswer({
  id: `a_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`,
  questionId,
  text,
  questionText,                        // line 689의 const questionText 재사용
  authorId: 'self',
  type: questionType,
  date: getDateStr(),
});
```

- [ ] **Step 2.6: 전체 테스트 + lint + build 확인**

```bash
npm run test 2>&1 | tail -3
npm run lint 2>&1 | tail -3
npm run build 2>&1 | tail -5
```
Expected: 모두 통과 (415 it-block: 413 → 415, +2 신규)

- [ ] **Step 2.7: Commit**

```bash
git add src/state/schema.ts src/ui/handlers/home.ts tests/unit/answer-schema.spec.ts
git commit -m "feat(v3.11 T2): add Answer.questionText optional field + submitAnswer wiring

- Schema: Answer.questionText?: string (graceful degrade for legacy answers)
- submitAnswer: persist questionText alongside questionId
- 2 new it-block (with + without questionText)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: `interestToFeeds` 1:N + refreshBriefings dedup wiring

**Files:**
- Modify: `src/ui/handlers/home.ts:474, 519-538` (interestToFeed → interestToFeeds + refresh wiring)
- Create: `tests/unit/home-dedup.spec.ts`
- Modify: `tests/unit/briefings-dedup.spec.ts` (interestToFeeds 검증 추가)

- [ ] **Step 3.1: dedup 통합 테스트 작성 (실패)**

`tests/unit/home-dedup.spec.ts` (신규):

```typescript
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { pickBriefings } from '../../src/ui/handlers/home';
import type { FeedResult } from '../../src/services/rss';
import { recordSeen } from '../../src/state/seen';

describe('refreshBriefings dedup behavior (unit-level)', () => {
  beforeEach(() => localStorage.clear());

  // pickBriefings는 feeds 입력 → unseen-only로 사전 필터한 결과를 받아 round-robin
  it('filtered FeedResult removes seen urls before pickBriefings', () => {
    const now = Date.parse('2026-04-28T00:00:00Z');
    recordSeen(['https://a.com/1'], now);   // 미리 본 것

    const feed: FeedResult = {
      sourceTitle: 'A',
      items: [
        { title: 't1', link: 'https://a.com/1', description: 'd1', pubDate: '', image: undefined },
        { title: 't2', link: 'https://a.com/2', description: 'd2', pubDate: '', image: undefined },
      ],
    };
    // production 코드는 feed.items를 isSeen(item.link)로 필터한 새 FeedResult를 pickBriefings에 전달
    const filtered: FeedResult = {
      ...feed,
      items: feed.items.filter((it) => !require('../../src/state/seen').isSeen(it.link, now)),
    };
    const picked = pickBriefings([filtered], 5);
    expect(picked.map((p) => p.item.link)).toEqual(['https://a.com/2']);
  });

  it('preserves newest-first ordering after filter', () => {
    const now = Date.parse('2026-04-28T00:00:00Z');
    // index 0이 newest by RSS convention
    const feed: FeedResult = {
      sourceTitle: 'A',
      items: [
        { title: 'newest', link: 'https://a.com/n', description: '', pubDate: '', image: undefined },
        { title: 'older', link: 'https://a.com/o', description: '', pubDate: '', image: undefined },
      ],
    };
    const picked = pickBriefings([feed], 5);
    expect(picked[0]?.item.link).toBe('https://a.com/n');   // round-robin이라 0번부터 진행
  });

  it('dedup leaves picked.length < 5 → caller should fallback to unfiltered feeds', () => {
    // 모든 item이 seen이라 unseen 없음
    const now = Date.parse('2026-04-28T00:00:00Z');
    recordSeen(['https://a.com/1', 'https://a.com/2'], now);

    const feed: FeedResult = {
      sourceTitle: 'A',
      items: [
        { title: 't1', link: 'https://a.com/1', description: '', pubDate: '', image: undefined },
        { title: 't2', link: 'https://a.com/2', description: '', pubDate: '', image: undefined },
      ],
    };
    const seen = require('../../src/state/seen');
    const filtered: FeedResult = { ...feed, items: feed.items.filter((it) => !seen.isSeen(it.link, now)) };
    const picked = pickBriefings([filtered], 5);
    expect(picked).toHaveLength(0);
    // fallback: 원본으로 재호출
    const fallback = pickBriefings([feed], 5);
    expect(fallback).toHaveLength(2);
  });

  it('recordSeen called with chosen urls after pick', () => {
    // refreshBriefings 끝 단계의 invariant
    const now = Date.parse('2026-04-28T00:00:00Z');
    const urls = ['https://a.com/1', 'https://b.com/2'];
    recordSeen(urls, now);
    const seen = require('../../src/state/seen');
    expect(seen.isSeen('https://a.com/1', now)).toBe(true);
    expect(seen.isSeen('https://b.com/2', now)).toBe(true);
  });
});
```

`tests/unit/briefings-dedup.spec.ts` 확장 (interestToFeeds 추가, 기존 it-block 보존):

```typescript
import { describe, it, expect } from 'vitest';
import { interestToFeeds } from '../../src/ui/handlers/home';

describe('interestToFeeds 1:N mapping', () => {
  it('returns 2-3 feeds per known interest id', () => {
    expect(interestToFeeds('ai_ml').length).toBeGreaterThanOrEqual(2);
    expect(interestToFeeds('startup').length).toBeGreaterThanOrEqual(2);
    expect(interestToFeeds('career').length).toBeGreaterThanOrEqual(2);
  });

  it('returns empty array for unknown interest', () => {
    expect(interestToFeeds('does_not_exist')).toEqual([]);
  });

  it('all returned urls are absolute https', () => {
    const all = ['recruiting', 'ai_ml', 'startup', 'self_dev'].flatMap(interestToFeeds);
    expect(all.every((u) => u.startsWith('https://'))).toBe(true);
  });
});
```

- [ ] **Step 3.2: 테스트 실패 확인**

```bash
npm run test -- tests/unit/home-dedup.spec.ts tests/unit/briefings-dedup.spec.ts 2>&1 | tail -10
```
Expected: FAIL — `interestToFeeds is not a function` 등

- [ ] **Step 3.3: `interestToFeeds` 1:N 구현**

`src/ui/handlers/home.ts:519-538` (interestToFeed 함수 전체 교체):

```typescript
export function interestToFeeds(interestId: string): string[] {
  const map: Record<string, string[]> = {
    recruiting: ['https://medium.com/feed/daangn', 'https://www.lennysnewsletter.com/feed'],
    onboarding: ['https://medium.com/feed/daangn', 'https://www.lennysnewsletter.com/feed'],
    culture: ['https://medium.com/feed/daangn', 'https://blog.pragmaticengineer.com/rss/'],
    hr_system: ['https://outstanding.kr/feed', 'https://www.mckinsey.com/insights/rss'],
    labor_law: ['https://outstanding.kr/feed', 'https://www.mckinsey.com/insights/rss'],
    leadership: [
      'https://medium.com/feed/daangn',
      'https://blog.pragmaticengineer.com/rss/',
      'https://www.lennysnewsletter.com/feed',
    ],
    pm: [
      'https://toss.tech/rss.xml',
      'https://www.lennysnewsletter.com/feed',
      'https://blog.bytebytego.com/feed',
    ],
    ai_ml: [
      'https://tech.kakao.com/feed/',
      'https://openai.com/news/rss.xml',
      'https://news.hada.io/rss/news',
    ],
    data: [
      'https://d2.naver.com/d2.atom',
      'https://blog.bytebytego.com/feed',
      'https://news.hada.io/rss/news',
    ],
    startup: [
      'https://outstanding.kr/feed',
      'https://techcrunch.com/feed/',
      'https://news.hada.io/rss/news',
    ],
    marketing: ['https://www.mobiinside.co.kr/feed', 'https://www.lennysnewsletter.com/feed'],
    productivity: ['https://www.lifehacker.co.kr/feed', 'https://blog.pragmaticengineer.com/rss/'],
    career: [
      'https://www.lifehacker.co.kr/feed',
      'https://blog.pragmaticengineer.com/rss/',
      'https://www.lennysnewsletter.com/feed',
    ],
    communication: ['https://www.lifehacker.co.kr/feed', 'https://blog.pragmaticengineer.com/rss/'],
    self_dev: [
      'https://www.lifehacker.co.kr/feed',
      'https://martinfowler.com/feed.atom',
      'https://blog.pragmaticengineer.com/rss/',
    ],
  };
  return map[interestId] ?? [];
}

// 하위 호환: 기존 호출 흔적이 있을 수 있음. interestToFeeds(...)[0] 첫 번째만 필요한 경우.
// ⚠️ T3 Step 3.4에서 refreshBriefings의 호출부를 수정하므로 이 wrapper는 불필요할 가능성 큼.
// grep 결과 호출처 0건이면 본 wrapper 추가하지 말 것 (YAGNI).
```

`refreshBriefings` 호출부 수정 (`src/ui/handlers/home.ts:472-494` 근처):

```typescript
const picks = user.interests.slice(0, 3);
// 1:N 매핑 → flatten → Set dedup → 최대 8개 cap
const allFeedUrls = Array.from(new Set(picks.flatMap(interestToFeeds)));
const feedUrls = allFeedUrls.slice(0, 8);

const fetched = await Promise.all(
  feedUrls.map((u) => fetchFeed(u, { timeoutMs: 5000 })),
);

// dedup: 각 FeedResult.items에서 isSeen인 것 제거
const now = Date.now();
purgeExpiredSeen(now);   // housekeeping (옵셔널이지만 진입 시 한 번 정리)
const filtered = fetched.map((f) => ({ ...f, items: f.items.filter((it) => !isSeen(it.link, now)) }));

// pick: unseen 우선, 부족하면 원본으로 fallback
let chosen = pickBriefings(filtered, 5);
if (chosen.length < 5) {
  chosen = pickBriefings(fetched, 5);   // fallback: expired-seen 자동 허용
}

const today = getDateStr();
const stored: Briefing[] = chosen.map(({ item, sourceTitle }, i) => ({
  id: `b_${Date.now()}_${i}`,
  date: today,
  url: item.link,
  title: item.title,
  summary: stripTags(item.description).slice(0, 200),
  scrapped: false,
  read: false,
  memo: '',
  ...(sourceTitle ? { sourceTitle } : {}),
  ...(item.image ? { imageUrl: item.image } : {}),
}));
saveBriefings(stored);
recordSeen(stored.map((b) => b.url), now);   // ★ chosen 5장 기록
hydrateBriefings();
```

상단 import 보강:

```typescript
import { isSeen, recordSeen, purgeExpiredSeen } from '../../state/seen';
```

- [ ] **Step 3.4: 테스트 통과 + 전체 + N=5**

```bash
npm run test -- tests/unit/home-dedup.spec.ts tests/unit/briefings-dedup.spec.ts 2>&1 | tail -5
npm run test 2>&1 | tail -3
for i in 1 2 3 4 5; do npm run test -- tests/unit/home-dedup.spec.ts 2>&1 | tail -1; done
```
Expected: 신규 4 + 확장 3 = 7 it-block 통과, 전체 422 통과 (+7), N=5 안정.

- [ ] **Step 3.5: lint + build**

```bash
npm run lint 2>&1 | tail -3 && npm run build 2>&1 | tail -10
```
Expected: 0 error 0 warn / build OK.

- [ ] **Step 3.6: 수동 smoke (의존성 없는 환경에서 dev server 띄우고 브라우저 확인 가능 시)**

```bash
npm run dev &
DEV_PID=$!
sleep 3
# 브라우저: localhost:5173 → 홈 → 새로고침 5회 → DevTools localStorage["seenBriefings"] 확인
# 5회 모두 "다른 url" 노출 확인 후
kill $DEV_PID
```

- [ ] **Step 3.7: Commit**

```bash
git add src/ui/handlers/home.ts tests/unit/home-dedup.spec.ts tests/unit/briefings-dedup.spec.ts
git commit -m "feat(v3.11 T3): interestToFeeds 1:N + refreshBriefings dedup wiring

- 6 unique feeds → 15 (KR 8 + EN 7), all rss2json verified
- interestToFeed → interestToFeeds (Record<string, string[]>)
- refreshBriefings: flatMap + Set dedup + cap 8 + isSeen filter + fallback
- recordSeen called with chosen urls after pick
- 4 new it-block (home-dedup) + 3 (interestToFeeds extension)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: 첫 3장 `loading="eager"`

**Files:**
- Modify: `src/ui/handlers/home.ts:371` (1줄)
- Create: `tests/unit/eager-loading.spec.ts`

- [ ] **Step 4.1: 실패 테스트 작성**

`tests/unit/eager-loading.spec.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { renderBriefingCard } from '../../src/ui/handlers/home';
import type { Briefing } from '../../src/state/briefings';

const sample = (id: string, withImg: boolean): Briefing => ({
  id,
  date: '2026-04-28',
  url: `https://example.com/${id}`,
  title: `t-${id}`,
  summary: 's',
  scrapped: false,
  read: false,
  memo: '',
  sourceTitle: 'src',
  ...(withImg ? { imageUrl: 'https://img.example.com/x.jpg' } : {}),
});

describe('renderBriefingCard image loading attr', () => {
  it('idx 0,1,2 → loading=eager (with imageUrl)', () => {
    for (let idx = 0; idx < 3; idx++) {
      const card = renderBriefingCard(sample(`b${idx}`, true), idx);
      const img = card.querySelector<HTMLImageElement>('img.card-thumb');
      expect(img?.getAttribute('loading')).toBe('eager');
    }
  });

  it('idx >= 3 → loading=lazy (with imageUrl)', () => {
    const card = renderBriefingCard(sample('b3', true), 3);
    const img = card.querySelector<HTMLImageElement>('img.card-thumb');
    expect(img?.getAttribute('loading')).toBe('lazy');
  });

  it('no imageUrl → no img element regardless of idx', () => {
    const card = renderBriefingCard(sample('b0', false), 0);
    expect(card.querySelector('img.card-thumb')).toBeNull();
  });
});
```

- [ ] **Step 4.2: 실패 확인**

```bash
npm run test -- tests/unit/eager-loading.spec.ts 2>&1 | tail -10
```
Expected: 첫 2 it-block FAIL (둘 다 'lazy' 받음), 마지막 PASS.

- [ ] **Step 4.3: 1줄 수정**

`src/ui/handlers/home.ts:371`:

```typescript
img.setAttribute('loading', idx < 3 ? 'eager' : 'lazy');
```

- [ ] **Step 4.4: 통과 + N=5 + lint + build**

```bash
npm run test -- tests/unit/eager-loading.spec.ts 2>&1 | tail -5
npm run test 2>&1 | tail -3
for i in 1 2 3 4 5; do npm run test -- tests/unit/eager-loading.spec.ts 2>&1 | tail -1; done
npm run lint 2>&1 | tail -3 && npm run build 2>&1 | tail -10
```
Expected: 신규 3 통과, 전체 425 통과(+3), N=5 안정.

- [ ] **Step 4.5: 수동 검증 (시크릿 창 첫 진입)**

```bash
npm run dev &
DEV_PID=$!
sleep 3
# 시크릿 모드 → localhost:5173 → 홈 → DevTools Network 탭 캐시 비활성화 → 새로고침
# 첫 3장 카드의 image element가 letter fallback 단계 없이 즉시 paint 되는지 확인
kill $DEV_PID
```

- [ ] **Step 4.6: Commit**

```bash
git add src/ui/handlers/home.ts tests/unit/eager-loading.spec.ts
git commit -m "fix(v3.11 T4): eager loading for first 3 briefing card images

- idx<3 → loading='eager' / idx≥3 → loading='lazy'
- Eliminates letter-fallback flash on cold first visit
- 3 new it-block (idx 0/1/2 eager, idx 3 lazy, no-imageUrl)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: `renderBriefingCard` export → archive 스크랩 카드 재사용

**Files:**
- Modify: `src/ui/handlers/home.ts:349` (export 추가 — 이미 export로 보이지만 확인 후 명시)
- Modify: `src/ui/handlers/archive.ts:326-358` (스크랩 카드 분기 → renderBriefingCard 호출)
- Modify: CSS — archive 컨텍스트용 `.archive-card--scrap` 토큰 (briefing 카드 스타일 상속 + ✕ 버튼 위치)
- Create: `tests/unit/archive-scrap-card.spec.ts`

- [ ] **Step 5.1: home.ts에서 renderBriefingCard export 확인 + 보강**

`src/ui/handlers/home.ts:349`는 `export function renderBriefingCard` 이미 있음(grep으로 확인). 변경 없으면 skip.

```bash
grep -n "^export function renderBriefingCard" src/ui/handlers/home.ts
```
Expected: 1 match.

- [ ] **Step 5.2: 실패 테스트 작성**

`tests/unit/archive-scrap-card.spec.ts`:

```typescript
import { describe, it, expect, beforeEach } from 'vitest';
import { rerenderList } from '../../src/ui/handlers/archive';
import { saveBriefings } from '../../src/state/briefings';

describe('archive scrap card uses briefing-card visual structure', () => {
  beforeEach(() => {
    localStorage.clear();
    document.body.innerHTML = '<div id="archiveList"></div>';
  });

  it('scrapped briefing renders with .card-thumb or .card-initial + .card-overlay', () => {
    saveBriefings([{
      id: 'b1', date: '2026-04-28',
      url: 'https://example.com/a', title: 'sample', summary: 'sum',
      scrapped: true, read: false, memo: '', sourceTitle: 'Src',
      imageUrl: 'https://img.example.com/x.jpg',
    }]);
    // archive currentFilter 'scrap' 시뮬레이션 — rerenderList 직전에 filter set
    document.dispatchEvent(new CustomEvent('dg:archive:filter', { detail: { filter: 'scrap' } }));
    rerenderList();
    const card = document.querySelector('.archive-card--scrap');
    expect(card).not.toBeNull();
    // briefing 카드와 동일 시각 구조 검증
    expect(card!.querySelector('.card-overlay')).not.toBeNull();
    expect(card!.querySelector('.card-source')?.textContent).toBe('Src');
    expect(card!.querySelector('.card-thumb, .card-initial')).not.toBeNull();
  });

  it('scrap card retains data-briefing-id for delegation', () => {
    saveBriefings([{
      id: 'b-x', date: '2026-04-28',
      url: 'https://example.com/y', title: 't', summary: 's',
      scrapped: true, read: false, memo: '', sourceTitle: 'S',
    }]);
    document.dispatchEvent(new CustomEvent('dg:archive:filter', { detail: { filter: 'scrap' } }));
    rerenderList();
    const card = document.querySelector<HTMLElement>('.archive-card--scrap');
    expect(card?.dataset['briefingId']).toBe('b-x');
  });

  it('scrap card has ✕ delete button (archive-card-delete)', () => {
    saveBriefings([{
      id: 'b1', date: '2026-04-28',
      url: 'https://example.com/a', title: 't', summary: 's',
      scrapped: true, read: false, memo: '', sourceTitle: 'S',
    }]);
    document.dispatchEvent(new CustomEvent('dg:archive:filter', { detail: { filter: 'scrap' } }));
    rerenderList();
    const btn = document.querySelector('.archive-card--scrap .archive-card-delete');
    expect(btn).not.toBeNull();
    expect(btn?.getAttribute('aria-label')).toBe('스크랩 해제');
  });
});
```

- [ ] **Step 5.3: 실패 확인**

```bash
npm run test -- tests/unit/archive-scrap-card.spec.ts 2>&1 | tail -10
```
Expected: 신규 카드 구조(`.card-overlay`, `.card-thumb`, `.card-source`) 미존재 → FAIL.

- [ ] **Step 5.4: archive scrap 분기 재작성**

`src/ui/handlers/archive.ts:326-358` 교체:

```typescript
// 상단 import에 추가
// import { renderBriefingCard } from './home';

if (currentFilter === 'scrap') {
  const scrapped = briefings.filter((b) => b.scrapped);
  if (scrapped.length === 0) {
    list.textContent = '아직 스크랩한 기사가 없어요.';
    return;
  }
  scrapped.forEach((b, idx) => {
    const card = renderBriefingCard(b, idx);
    card.classList.add('archive-card', 'archive-card--scrap');   // archive-only 토큰
    card.dataset['briefingId'] = b.id;                            // 위임 핸들러 호환

    const deleteBtn = document.createElement('button');
    deleteBtn.className = 'archive-card-delete';
    deleteBtn.type = 'button';
    deleteBtn.setAttribute('aria-label', '스크랩 해제');
    deleteBtn.textContent = '×';
    card.append(deleteBtn);

    list.append(card);
  });
  return;
}
```

기존 `import { rerenderList } ...` 흐름과 호환 — `archive-card-delete` 위임 핸들러는 그대로 작동.

- [ ] **Step 5.5: CSS 토큰 — `.archive-card--scrap` (✕ 버튼 위치 보정)**

`src/styles/main.css` 또는 archive 관련 layer에 추가:

```css
/* v3.11 — archive scrap 카드는 briefing 카드 시각을 재사용. ✕ 버튼만 absolute 위치. */
.archive-card--scrap {
  position: relative;
  /* briefing-card의 background/borders/shadow 상속 — 별도 토큰 추가 없음 */
}

.archive-card--scrap .archive-card-delete {
  position: absolute;
  top: 8px;
  right: 8px;
  z-index: 2;          /* card-overlay 위에 떠야 클릭 가능 */
  background: rgba(0, 0, 0, 0.5);
  color: white;
  border: none;
  border-radius: 50%;
  width: 28px;
  height: 28px;
  cursor: pointer;
}
```

(정확한 토큰 변수명은 기존 archive 또는 modal CSS에 맞춰 align — 본 plan은 시각 의도만 제시, executor가 기존 토큰 발견 시 그것 사용)

- [ ] **Step 5.6: 통과 + N=5 + lint + build**

```bash
npm run test -- tests/unit/archive-scrap-card.spec.ts 2>&1 | tail -5
npm run test 2>&1 | tail -3
for i in 1 2 3 4 5; do npm run test -- tests/unit/archive-scrap-card.spec.ts 2>&1 | tail -1; done
npm run lint 2>&1 | tail -3 && npm run build 2>&1 | tail -10
```
Expected: 신규 3 + 전체 428 통과(+3), N=5 안정.

- [ ] **Step 5.7: Commit**

```bash
git add src/ui/handlers/archive.ts src/ui/handlers/home.ts src/styles/main.css tests/unit/archive-scrap-card.spec.ts
git commit -m "feat(v3.11 T5): archive scrap card reuses renderBriefingCard for visual unification

- archive scrap branch now calls renderBriefingCard(b, idx) → identical visual to home
- adds .archive-card--scrap modifier + absolute ✕ button positioning
- preserves data-briefing-id delegation + aria-label '스크랩 해제'
- 3 new it-block (visual structure, briefing-id, delete button)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: archive 답변 카드 풍부화 — `renderAnswerCard`

**Files:**
- Modify: `src/ui/handlers/archive.ts:374-410` (답변 카드 rendering 분기 → renderAnswerCard 호출)
- Modify: `src/styles/main.css` — `.archive-card--answer`, `.archive-card-header`, `.archive-type-chip`, `.archive-card-question`, `.archive-card-question--missing`, `.archive-card-body` 토큰
- Modify: `tests/unit/archive-cards.spec.ts` (확장 또는 신규 작성)

- [ ] **Step 6.1: 실패 테스트 작성**

`tests/unit/archive-cards.spec.ts` (확장 또는 신규):

```typescript
import { describe, it, expect, beforeEach } from 'vitest';
import { rerenderList } from '../../src/ui/handlers/archive';
import { saveAnswers } from '../../src/state/persistence';
import { makeAnswer } from '../../src/state/schema';

describe('archive answer card rich layout (v3.11)', () => {
  beforeEach(() => {
    localStorage.clear();
    document.body.innerHTML = '<div id="archiveList"></div>';
  });

  it('renders question preview when questionText present', () => {
    saveAnswers([makeAnswer({
      id: 'a1', questionId: 'q1', text: 'my answer body...', authorId: 'self',
      questionText: '오늘의 채용 미스매치 원인은?',
      type: '분석', date: '2026-04-28',
    })]);
    rerenderList();
    const q = document.querySelector('.archive-card--answer .archive-card-question');
    expect(q?.textContent).toContain('오늘의 채용 미스매치 원인은?');
    expect(q?.classList.contains('archive-card-question--missing')).toBe(false);
  });

  it('renders muted "질문 정보 없음" when questionText absent (legacy)', () => {
    saveAnswers([makeAnswer({
      id: 'a2', questionId: 'q2', text: 'old answer', authorId: 'self',
      type: '분석', date: '2026-04-28',
    })]);
    rerenderList();
    const q = document.querySelector('.archive-card--answer .archive-card-question--missing');
    expect(q?.textContent).toContain('질문 정보 없음');
  });

  it('renders type chip with toKoType label', () => {
    saveAnswers([makeAnswer({
      id: 'a3', questionId: 'q3', text: 't', authorId: 'self',
      type: '분석', date: '2026-04-28',
    })]);
    rerenderList();
    const chip = document.querySelector('.archive-card--answer .archive-type-chip');
    expect(chip?.textContent).toMatch(/분석/);
  });

  it('answer body uses CSS line-clamp class (no JS truncation)', () => {
    const longText = 'a'.repeat(500);
    saveAnswers([makeAnswer({
      id: 'a4', questionId: 'q4', text: longText, authorId: 'self',
      questionText: 'q', type: '분석', date: '2026-04-28',
    })]);
    rerenderList();
    const body = document.querySelector('.archive-card--answer .archive-card-body');
    expect(body?.textContent?.length).toBe(500);   // 풀 텍스트, CSS만 클램프
  });

  it('answer card retains data-answer-id + delete button', () => {
    saveAnswers([makeAnswer({
      id: 'a-keep', questionId: 'q', text: 't', authorId: 'self',
      questionText: 'q', type: '분석', date: '2026-04-28',
    })]);
    rerenderList();
    const card = document.querySelector<HTMLElement>('.archive-card--answer');
    expect(card?.dataset['answerId']).toBe('a-keep');
    expect(card?.querySelector('.archive-card-delete')?.getAttribute('aria-label')).toBe('답변 삭제');
  });
});
```

- [ ] **Step 6.2: 실패 확인**

```bash
npm run test -- tests/unit/archive-cards.spec.ts 2>&1 | tail -10
```
Expected: `.archive-card--answer` 미존재 → FAIL.

- [ ] **Step 6.3: `renderAnswerCard` 신규 함수 + 호출부 wiring**

`src/ui/handlers/archive.ts`:

```typescript
function renderAnswerCard(a: Answer): HTMLElement {
  const card = document.createElement('article');
  card.className = 'archive-card archive-card--answer';
  card.dataset['answerId'] = a.id;

  // Header: 유형 칩 + 날짜 + ✕
  const header = document.createElement('div');
  header.className = 'archive-card-header';

  if (a.type) {
    const chip = document.createElement('span');
    chip.className = 'archive-type-chip';
    chip.textContent = toKoType(a.type);
    header.append(chip);
  }

  const date = document.createElement('time');
  date.className = 'archive-date';
  date.textContent = a.date ?? (a.createdAt ? new Date(a.createdAt).toLocaleDateString('ko-KR') : '');
  header.append(date);

  const deleteBtn = document.createElement('button');
  deleteBtn.className = 'archive-card-delete';
  deleteBtn.type = 'button';
  deleteBtn.setAttribute('aria-label', '답변 삭제');
  deleteBtn.textContent = '×';
  header.append(deleteBtn);

  card.append(header);

  // Question preview (1줄)
  const q = document.createElement('p');
  if (a.questionText) {
    q.className = 'archive-card-question';
    q.textContent = `❓ ${a.questionText}`;
  } else {
    q.className = 'archive-card-question archive-card-question--missing';
    q.textContent = '질문 정보 없음 (이전 버전 답변)';
  }
  card.append(q);

  // Answer body (CSS line-clamp 3 — JS truncation 안 함)
  const body = document.createElement('p');
  body.className = 'archive-card-body';
  body.textContent = a.text;
  card.append(body);

  return card;
}
```

`rerenderList`의 답변 분기(`for (const a of filtered) { ... }`) 교체:

```typescript
for (const a of filtered) {
  list.append(renderAnswerCard(a));
}
```

상단 import에 `import type { Answer } from '../../state/schema';` 추가 필요 시.

- [ ] **Step 6.4: CSS 토큰**

`src/styles/main.css` (또는 적절한 layer):

```css
.archive-card--answer {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-md, 12px);
  padding: 16px;
  margin-bottom: 12px;
  position: relative;
}

.archive-card--answer .archive-card-header {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 8px;
}

.archive-card--answer .archive-card-header time {
  margin-left: auto;
  font-size: var(--font-size-sm);
  color: var(--text-muted);
}

.archive-card--answer .archive-card-header .archive-card-delete {
  margin-left: 8px;
  background: transparent;
  border: none;
  font-size: 18px;
  cursor: pointer;
  color: var(--text-muted);
}

.archive-type-chip {
  background: var(--surface-2, #f3f4f6);
  border-radius: 999px;
  padding: 2px 10px;
  font-size: var(--font-size-sm);
  font-weight: 500;
}

.archive-card-question {
  font-size: var(--font-size-md);
  color: var(--text);
  margin: 0 0 8px 0;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.archive-card-question--missing {
  color: var(--text-muted);
  font-style: italic;
}

.archive-card-body {
  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
  overflow: hidden;
  margin: 0;
  color: var(--text);
}
```

(CSS 토큰 이름은 기존 디자인 시스템에 맞춰 align — executor가 `--surface`/`--text` 등을 grep 후 정확한 이름 사용)

- [ ] **Step 6.5: 통과 + N=5 + lint + build**

```bash
npm run test -- tests/unit/archive-cards.spec.ts 2>&1 | tail -5
npm run test 2>&1 | tail -3
for i in 1 2 3 4 5; do npm run test -- tests/unit/archive-cards.spec.ts 2>&1 | tail -1; done
npm run lint 2>&1 | tail -3 && npm run build 2>&1 | tail -10
```
Expected: 5 it-block 통과, 전체 433 통과(+5), N=5 안정.

- [ ] **Step 6.6: Commit**

```bash
git add src/ui/handlers/archive.ts src/styles/main.css tests/unit/archive-cards.spec.ts
git commit -m "feat(v3.11 T6): rich answer card layout (renderAnswerCard)

- 신규 renderAnswerCard: 유형 칩 + 날짜 + ✕ 헤더 + 질문 preview + 답변 3줄 클램프
- questionText 없는 (legacy) 답변 → 'questions 정보 없음' muted graceful degrade
- CSS line-clamp 3 (JS 140자 truncation 제거)
- 5 new it-block (with/without questionText, type chip, line-clamp, data-answer-id)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 7: 답변 detail modal (read-only)

**Files:**
- Modify: `src/ui/handlers/archive.ts` — 답변 카드 클릭 핸들러에 모달 open 추가
- Create: `src/ui/modals/answer-detail.ts` — 답변 상세 모달 component (modal-system 재사용)
- Modify: `tests/unit/modals/answer-detail.spec.ts` 또는 archive 관련 spec 확장

- [ ] **Step 7.1: 기존 modal 시스템 패턴 확인**

```bash
ls src/ui/modals/ && grep -n "openModal\|modal-system" src/ui/modals/*.ts | head -20
```

기존 모달 1건 (예: interests modal)을 reference로 사용. 동일 패턴 따를 것.

- [ ] **Step 7.2: 실패 테스트 작성**

`tests/unit/modals/answer-detail.spec.ts`:

```typescript
import { describe, it, expect, beforeEach } from 'vitest';
import { openAnswerDetail } from '../../../src/ui/modals/answer-detail';
import { makeAnswer } from '../../../src/state/schema';

describe('answer-detail modal', () => {
  beforeEach(() => {
    document.body.innerHTML = '';
  });

  it('renders question text + answer body in modal', () => {
    const a = makeAnswer({
      id: 'a1', questionId: 'q1', text: 'full answer here',
      authorId: 'self',
      questionText: '오늘의 질문',
      type: '분석', date: '2026-04-28',
    });
    openAnswerDetail(a);
    expect(document.body.textContent).toContain('오늘의 질문');
    expect(document.body.textContent).toContain('full answer here');
  });

  it('shows "질문 정보 없음" for legacy answer (no questionText)', () => {
    const a = makeAnswer({
      id: 'a2', questionId: 'q2', text: 'legacy answer',
      authorId: 'self', type: '분석', date: '2026-04-28',
    });
    openAnswerDetail(a);
    expect(document.body.textContent).toContain('질문 정보 없음');
    expect(document.body.textContent).toContain('legacy answer');
  });

  it('renders evaluation feedback when present', () => {
    const a = makeAnswer({
      id: 'a3', questionId: 'q3', text: 'a',
      authorId: 'self',
      questionText: 'q',
      evaluation: { score: 8, feedback: 'great insight!' },
    });
    openAnswerDetail(a);
    expect(document.body.textContent).toContain('great insight!');
  });
});
```

- [ ] **Step 7.3: 실패 확인**

```bash
npm run test -- tests/unit/modals/answer-detail.spec.ts 2>&1 | tail -10
```
Expected: FAIL — 모듈 미존재.

- [ ] **Step 7.4: `answer-detail.ts` 모달 구현 (기존 modal-system 재사용)**

`src/ui/modals/answer-detail.ts`:

```typescript
/**
 * Answer detail modal — read-only. Shows full question + full answer +
 * evaluation feedback (if present). Legacy answers (no questionText) show
 * "질문 정보 없음" placeholder.
 */
import type { Answer } from '../../state/schema';
import { openModal } from '../modal-system';   // ⚠️ 실제 모달 진입점은 executor가 grep 후 확인

export function openAnswerDetail(a: Answer): void {
  const root = document.createElement('div');
  root.className = 'answer-detail-modal';

  const qTitle = document.createElement('h3');
  qTitle.textContent = a.questionText ?? '질문 정보 없음 (이전 버전 답변)';
  if (!a.questionText) qTitle.classList.add('archive-card-question--missing');
  root.append(qTitle);

  const meta = document.createElement('p');
  meta.className = 'answer-detail-meta';
  meta.textContent = `${a.type ? `[${a.type}] ` : ''}${a.date ?? ''}`;
  root.append(meta);

  const body = document.createElement('p');
  body.className = 'answer-detail-body';
  body.textContent = a.text;
  root.append(body);

  if (a.evaluation?.feedback) {
    const fb = document.createElement('p');
    fb.className = 'answer-detail-feedback';
    fb.textContent = `💡 ${a.evaluation.feedback}`;
    root.append(fb);
  }

  // 기존 modal-system openModal API에 맞춰 호출
  openModal({ title: '내 답변', content: root });
}
```

⚠️ **executor 주의**: `openModal` API 시그니처 + import 경로는 기존 modal 1건의 패턴(예: `src/ui/modals/interests.ts`)을 그대로 따를 것. 본 plan은 의도만 제시.

- [ ] **Step 7.5: archive 답변 카드 클릭 → 모달 open wiring**

`src/ui/handlers/archive.ts` — 기존 `handleCardClick` 위임에 답변 분기 추가 (이미 있으면 모달 호출만 추가):

```typescript
// archive-card--answer 클릭 (✕ 버튼 제외) → 답변 detail 모달
if (card.classList.contains('archive-card--answer')
    && !(e.target as HTMLElement).closest('.archive-card-delete')) {
  const id = card.dataset['answerId'];
  if (!id) return;
  const a = loadAnswers().find((x) => x.id === id);
  if (a) {
    void import('../modals/answer-detail').then(({ openAnswerDetail }) => {
      openAnswerDetail(a);
    });
  }
  return;
}
```

dynamic import는 본 모듈 번들 사이즈 보호 + v3.4 lazy-load 패턴 일관성.

- [ ] **Step 7.6: 통과 + N=5 + lint + build**

```bash
npm run test -- tests/unit/modals/answer-detail.spec.ts 2>&1 | tail -5
npm run test 2>&1 | tail -3
for i in 1 2 3 4 5; do npm run test -- tests/unit/modals/answer-detail.spec.ts 2>&1 | tail -1; done
npm run lint 2>&1 | tail -3 && npm run build 2>&1 | tail -10
```
Expected: 신규 3 + 전체 436 통과(+3), N=5 안정.

- [ ] **Step 7.7: Commit**

```bash
git add src/ui/modals/answer-detail.ts src/ui/handlers/archive.ts tests/unit/modals/answer-detail.spec.ts
git commit -m "feat(v3.11 T7): read-only answer detail modal

- openAnswerDetail(a): full question + full answer + evaluation feedback
- legacy answers (no questionText) → '질문 정보 없음' placeholder
- archive answer card click (excluding ✕) → dynamic import + open
- 3 new it-block (with questionText, legacy, evaluation feedback)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 8: Playwright smoke + spec/plan retro sync + final verification

**Files:**
- Create: `tests/playwright/v311-archive-cards.spec.ts` — 신규 smoke
- Modify: spec/plan retro sync (구현 중 발견된 불일치 5+ 건)

- [ ] **Step 8.1: Playwright smoke**

`tests/playwright/v311-archive-cards.spec.ts`:

```typescript
import { test, expect } from '@playwright/test';

test('v3.11 archive 탭에 답변 카드 + 스크랩 카드 가시', async ({ page }) => {
  await page.goto('/');
  // 온보딩 완료 상태 가정 (기존 smoke의 setup 패턴 재사용 — executor가 기존 spec 헬퍼 사용)
  // ... (기존 setup 헬퍼 재사용)

  await page.getByRole('button', { name: /아카이브/ }).click();
  // 답변 카드 가시
  await expect(page.locator('.archive-card--answer').first()).toBeVisible();
  // 스크랩 필터 → briefing 카드 시각으로 가시
  await page.getByRole('button', { name: /스크랩/ }).click();
  await expect(page.locator('.archive-card--scrap .card-overlay').first()).toBeVisible();
});

test('v3.11 홈 카드 첫 3장 loading=eager', async ({ page }) => {
  await page.goto('/');
  // ... (setup)
  const eagerCount = await page.locator('.briefing-card img.card-thumb[loading="eager"]').count();
  expect(eagerCount).toBeGreaterThanOrEqual(1);
  expect(eagerCount).toBeLessThanOrEqual(3);
});
```

- [ ] **Step 8.2: smoke 실행**

```bash
npm run test:smoke -- tests/playwright/v311-archive-cards.spec.ts 2>&1 | tail -10
```
Expected: 2 PASS.

- [ ] **Step 8.3: 전체 smoke 회귀**

```bash
npm run test:smoke 2>&1 | tail -5
```
Expected: 81 → 83 PASS (+2).

- [ ] **Step 8.4: 번들 크기 비교**

```bash
npm run build 2>&1 | grep -E "dist/.*\.js|dist/.*\.css"
```

T0 baseline 대비 JS gzip 변동 기록. spec §6 DoD `+1.5 kB gzip` 충족 여부 확인. 초과 시 hot-spot 분석(unused 카드 토큰, dynamic import 누수 등) 후 trim.

- [ ] **Step 8.5: spec/plan retro sync (구현 중 발견 사항 반영)**

구현 중 spec/plan과 다른 결정/수정이 있다면 spec과 plan에 retro sync (v3.10 T4와 동일 패턴):

- 모달 API 시그니처 실제 채택값
- 번들 baseline → 최종 변동
- it-block 실제 카운트 (계획 ~25 vs 실제)
- 의도하지 않게 건드린 인접 코드

`docs/superpowers/specs/2026-04-28-v3.11-archive-cards-image-race-dedup-design.md` + `docs/superpowers/plans/2026-04-28-v3.11-archive-cards-image-race-dedup.md` 양쪽 동기화.

- [ ] **Step 8.6: 독립 표준 코드 리뷰 디스패치 (CLAUDE.md 절대 규칙 준수)**

```bash
# code-reviewer subagent 또는 superpowers:code-reviewer 호출
# 입력: feat/v3.11-archive-cards-dedup-sources 브랜치 vs main의 git diff
# 깊이: 표준 (사이즈 ~8 commits, reversible, 데이터 파괴 없음)
# 출력: P0/P1/P2 분류 리뷰 보고서
```

P0=0 / P1 ≤ 1 (즉시 in-cycle 반영) 확인.

- [ ] **Step 8.7: Final commit (retro sync + smoke)**

```bash
git add tests/playwright/v311-archive-cards.spec.ts \
        docs/superpowers/specs/2026-04-28-v3.11-archive-cards-image-race-dedup-design.md \
        docs/superpowers/plans/2026-04-28-v3.11-archive-cards-image-race-dedup.md
git commit -m "test(v3.11 T8): playwright smoke (+2) + spec/plan retro sync

- archive 답변/스크랩 카드 가시 smoke
- 홈 카드 첫 3장 loading=eager smoke
- spec/plan retro sync: 모달 API/번들 변동/it-block 실제 카운트 반영

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

- [ ] **Step 8.8: main 머지 (사용자 명시 승인 후만)**

```bash
git checkout main
git merge --no-ff feat/v3.11-archive-cards-dedup-sources -m "Merge feat/v3.11-archive-cards-dedup-sources (archive 카드 풍부화 + 이미지 race fix + 브리핑 dedup/소스 확장)"
```

- [ ] **Step 8.9: Firebase 배포 (사용자 명시 승인 후만)**

```bash
npm run build && firebase deploy --only hosting
```

배포 후 production URL(https://my-ai-assistant-904f3.web.app) 수동 확인:
- 시크릿 창 첫 진입 → 홈 카드 첫 3장 paint
- 새로고침 5회 → 5번 모두 다른 url
- archive 답변 카드 풍부 layout
- archive 스크랩 카드 = home briefing 시각 동일

---

## Verification Gates (per-task 공통)

각 task 완료 시 모두 충족 확인:

- vitest 신규/확장 spec PASS + N=5 안정
- 전체 vitest PASS (T1: 420, T2: 422, T3: 425, T4: 428, T5: 431, T6: 436, T7: 439, T8: 동일)
- tsc/lint 0 error 0 warn
- 번들 baseline 대비 +1.5 kB gzip 이내 (누적 추적)
- 새 외부 의존 없음 (CSP `img-src https:` 와일드카드 → 신규 hosts 자동 허용 ✓)

---

## Rollback Plan

각 task 단위로 `git revert <commit>` 가능. seenBriefings 신규 키이므로 hosting 롤백 시 사용자 데이터 손실 없음(키 자체가 미사용 상태로 남음). `Answer.questionText` 추가도 optional이라 v3.10 코드로 롤백해도 기존 답변 read 시 ignore.

---

## Self-Review (writing-plans skill checklist)

**1. Spec coverage**:

| Spec section | Plan task |
|---|---|
| §2.1 G1 (archive 카드 풍부화) | T5(스크랩) + T6(답변) |
| §2.1 G2 (첫 3장 eager) | T4 |
| §2.1 G3 (seenBriefings dedup) | T1 + T3 |
| §2.1 G4 (interestToFeeds 1:N) | T3 |
| §2.1 G5 (Answer.questionText) | T2 + T6 graceful degrade |
| §2.1 G6 (테스트 +25 / 번들 +1.5 kB) | T8 verification gate |
| §3.2 (refreshBriefings 흐름) | T3 (구현) |
| §4.1~4.7 (각 모듈 상세) | T2/T1/T4/T5/T6 1:1 매핑 |
| §4.8 (답변 detail modal) | T7 |
| §6 (test plan) | 각 task 6.1+ Step |
| §7 (verification gates) | per-task 공통 + T8 |
| §8 R1 (CSP) | **자연 해소** — `img-src 'self' data: https:` 와일드카드 확인 |
| §8 R2 (rate limit) | T3 cap 8 + 5s timeout 적용 |

모든 spec 요구 → task 매핑 ✓.

**2. Placeholder scan**: "TBD"/"TODO"/"실제 코드는 executor가" 등 회피. 단 T7 Step 7.4의 `openModal` API는 "기존 modal 1건의 패턴 따를 것"으로 위임 — 이는 codebase 의존이라 plan에서 100% 명시 불가, executor가 grep 후 확정.

**3. Type consistency**: `interestToFeeds`/`isSeen`/`recordSeen`/`renderAnswerCard`/`renderBriefingCard`/`openAnswerDetail` 시그니처 모든 task에서 일관 사용 ✓.

**4. Scope check**: 8 tasks, ~25 it-block, 8 commits + final = 9 commits, 단일 사이클 (v3.3.3 급) ✓.
