# v3.47 Plant Action Layer (nudge + home CTA) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 식물 detail modal에 "다음 단계 nudge"(표시)와 "홈에서 오늘 브리핑 보기"(재참여 CTA) 버튼을 추가해 v3.21 §8.5 action layer 잔여를 청산한다.

**Architecture:** 변경은 `src/ui/modals/plant-detail.ts`의 `renderBody`에 국한 — read-only 헬퍼(`STAGE_THRESHOLDS`, `checkWilting`) 참조 + `switchTab('home')` 호출. 신규 schema/event/외부 호출 0. CSS는 기존 `.plant-action-chip` 토큰을 grouping 셀렉터로 공유.

**Tech Stack:** Vite + TypeScript (vanilla, 프레임워크 없음), Vitest (jsdom), Playwright smoke.

---

## File Structure

| 파일 | 책임 | 변경 |
|---|---|---|
| `src/ui/modals/plant-detail.ts` | modal 본문 렌더 | nudge HTML + home 버튼(`makeHomeChip`) 추가, `switchTab` import |
| `src/styles/components/modal.css` | modal 스타일 | `.plant-detail-nudge` rule + `.plant-action-home-chip`를 기존 chip grouping에 추가 |
| `tests/ui/modals/plant-detail.spec.ts` | 단위 테스트 | nudge 3케이스 + home 버튼 4조합 + 클릭 spec |
| `tests/smoke/plant-detail-modal.spec.ts` | E2E | 시든 식물 → 홈 버튼 클릭 → 홈 탭 활성 1 시나리오 |

**핵심 설계 결정 (충돌 회피):** home 버튼 클래스는 `plant-action-home-chip` (base `plant-action-chip` **미포함**). 이유: 기존 unit/smoke 테스트가 `.plant-action-chip`로 archive chip을 단일 클릭하는데, home 버튼이 같은 base 클래스를 가지면 stage<5 식물에서 2-match strict-mode 위반. CSS는 `.insight-archive-nav-chip` 선례처럼 comma 셀렉터로 공유.

---

### Task 1: 다음 단계 nudge (stage 1~4)

**Files:**
- Test: `tests/ui/modals/plant-detail.spec.ts` (append describe block)
- Modify: `src/ui/modals/plant-detail.ts:34-69` (`renderBody`)

- [ ] **Step 1: Write failing tests**

Append to `tests/ui/modals/plant-detail.spec.ts`:

```ts
describe('plant-detail nudge (v3.47)', () => {
  beforeEach(() => {
    document.body.replaceChildren();
    localStorage.clear();
  });

  it('stage 2, cum 10 → 일반 nudge "다음 단계까지 15번 더"', () => {
    saveUser(mkUser({
      interests: ['leadership'],
      gardenBackfilled: true,
      plantStateByInterest: { leadership: { stage: 2, cumulativeActivity: 10 } } satisfies Record<string, PlantState>,
    }));
    openPlantDetailModal('leadership');
    const nudge = document.querySelector('.plant-detail-nudge');
    expect(nudge).toBeTruthy();
    expect(nudge!.textContent).toContain('15');
    expect(nudge!.textContent).toContain('더 가꾸면');
  });

  it('stage 2, cum 23 → 임박 nudge "딱 2번!" (remaining ≤ 3)', () => {
    saveUser(mkUser({
      interests: ['leadership'],
      gardenBackfilled: true,
      plantStateByInterest: { leadership: { stage: 2, cumulativeActivity: 23 } } satisfies Record<string, PlantState>,
    }));
    openPlantDetailModal('leadership');
    const nudge = document.querySelector('.plant-detail-nudge')!;
    expect(nudge.textContent).toContain('딱 2번');
  });

  it('stage 5 (만개) → nudge 부재', () => {
    saveUser(mkUser({
      interests: ['leadership'],
      gardenBackfilled: true,
      plantStateByInterest: { leadership: { stage: 5, cumulativeActivity: 200 } } satisfies Record<string, PlantState>,
    }));
    openPlantDetailModal('leadership');
    expect(document.querySelector('.plant-detail-nudge')).toBeFalsy();
  });

  // 사전 review P1-1: <=3 경계(inclusive) 고정
  it('stage 2, cum 22 → remaining 3 경계 → "딱 3번!" (inclusive)', () => {
    saveUser(mkUser({
      interests: ['leadership'],
      gardenBackfilled: true,
      plantStateByInterest: { leadership: { stage: 2, cumulativeActivity: 22 } } satisfies Record<string, PlantState>,
    }));
    openPlantDetailModal('leadership');
    expect(document.querySelector('.plant-detail-nudge')!.textContent).toContain('딱 3번');
  });

  // 사전 review P1-1: 손상 데이터 clamp (cum>=threshold인데 stage 미진화) → remaining 최소 1
  it('stage 4, cum 160 (손상) → clamp remaining 1 → "딱 1번!"', () => {
    saveUser(mkUser({
      interests: ['leadership'],
      gardenBackfilled: true,
      plantStateByInterest: { leadership: { stage: 4, cumulativeActivity: 160 } } satisfies Record<string, PlantState>,
    }));
    openPlantDetailModal('leadership');
    expect(document.querySelector('.plant-detail-nudge')!.textContent).toContain('딱 1번');
  });
});
```

- [ ] **Step 2: Run to verify FAIL**

Run: `npm test -- plant-detail`
Expected: 3 new tests FAIL (`.plant-detail-nudge` null / text mismatch).

- [ ] **Step 3: Implement nudge in `renderBody`**

In `src/ui/modals/plant-detail.ts`, after the `progressHtml` const (line 47), add:

```ts
  const nudgeHtml = plant.stage === 5
    ? ''
    : (() => {
        const threshold = STAGE_THRESHOLDS[plant.stage - 1];
        const remaining = Math.max(1, threshold - cum);
        const copy = remaining <= 3
          ? `🎯 다음 단계까지 딱 ${remaining}번!`
          : `🎯 다음 단계까지 ${remaining}번 더 가꾸면 돼요`;
        return `<div class="plant-detail-nudge">${copy}</div>`;
      })();
```

Then insert `nudgeHtml` into the innerHTML concat (line 69), right after `progressHtml`:

```ts
  root.innerHTML = stageHtml + progressHtml + nudgeHtml + unlockedHtml + engagedHtml + wiltingHtml + countsHtml;
```

> Note: `STAGE_THRESHOLDS` is already imported (line 4). `cum` already computed (line 37). 카피는 정적 문자열 + 정수 `remaining`(유한 guard된 `cum`에서 산출)이라 escapeHtml 불요.

- [ ] **Step 4: Run to verify PASS**

Run: `npm test -- plant-detail`
Expected: all PASS (3 new + existing).

- [ ] **Step 5: tsc + lint**

Run: `npx tsc --noEmit && npm run lint`
Expected: 0 errors.

- [ ] **Step 6: Commit**

```bash
git add src/ui/modals/plant-detail.ts tests/ui/modals/plant-detail.spec.ts
git commit -m "feat(v3.47 T1): plant modal 다음 단계 nudge (stage<5, remaining≤3 강조)"
```

---

### Task 2: 홈 재참여 버튼 — 렌더 + 표시 조건

**Files:**
- Test: `tests/ui/modals/plant-detail.spec.ts` (append)
- Modify: `src/ui/modals/plant-detail.ts` (`renderBody` + new `makeHomeChip`)

표시 조건: `wilting || plant.stage < 5` (= `stage===5 && !wilting`일 때만 숨김).

- [ ] **Step 1: Write failing tests**

Append:

```ts
describe('plant-detail home chip (v3.47)', () => {
  beforeEach(() => {
    document.body.replaceChildren();
    localStorage.clear();
  });

  const seed = (plant: PlantState) =>
    saveUser(mkUser({
      interests: ['leadership'],
      gardenBackfilled: true,
      plantStateByInterest: { leadership: plant } satisfies Record<string, PlantState>,
    }));

  it('stage 2 (건강) → 홈 버튼 존재', () => {
    seed({ stage: 2, cumulativeActivity: 10, lastEngagedAt: new Date().toISOString() });
    openPlantDetailModal('leadership');
    const btn = document.querySelector('.plant-action-home-chip');
    expect(btn).toBeTruthy();
    expect(btn!.getAttribute('aria-label')).toBe('홈으로 이동해 오늘 브리핑 보기');
  });

  it('stage 5 + 건강 → 홈 버튼 부재', () => {
    seed({ stage: 5, cumulativeActivity: 200, lastEngagedAt: new Date().toISOString() });
    openPlantDetailModal('leadership');
    expect(document.querySelector('.plant-action-home-chip')).toBeFalsy();
  });

  it('stage 5 + 시듦(8일 전) → 홈 버튼 존재', () => {
    const eightDaysAgo = new Date(Date.now() - 8 * 24 * 60 * 60 * 1000).toISOString();
    seed({ stage: 5, cumulativeActivity: 200, lastEngagedAt: eightDaysAgo });
    openPlantDetailModal('leadership');
    expect(document.querySelector('.plant-action-home-chip')).toBeTruthy();
  });

  it('stage 1 + 시듦 → 홈 버튼 존재', () => {
    const eightDaysAgo = new Date(Date.now() - 8 * 24 * 60 * 60 * 1000).toISOString();
    seed({ stage: 1, cumulativeActivity: 2, lastEngagedAt: eightDaysAgo });
    openPlantDetailModal('leadership');
    expect(document.querySelector('.plant-action-home-chip')).toBeTruthy();
  });
});
```

- [ ] **Step 2: Run to verify FAIL**

Run: `npm test -- plant-detail`
Expected: 4 new tests FAIL (`.plant-action-home-chip` null).

- [ ] **Step 3: Implement `makeHomeChip` + 조건부 append**

Add `switchTab` import at top of `src/ui/modals/plant-detail.ts` (after the archive-nav import, line 11):

```ts
import { switchTab } from '../nav';
import { closeModal } from './shared';
```

> `openModal`은 이미 `'./shared'`에서 import 중 — `closeModal`을 같은 import 문에 합치거나 별도 줄 추가. 기존 line 1 `import { openModal } from './shared';`를 `import { openModal, closeModal } from './shared';`로 수정하는 편이 깔끔.

In `renderBody`, the `wilting` const already exists (line 57). After `root.append(makeActionChip(interestId));` (line 72), add:

```ts
  // v3.47: 재참여 CTA — 홈으로 이동(전체 피드, 스크랩이 식물 활동 경로).
  // 표시 조건: 시들었거나 아직 만개 전(stage<5). 만개+건강일 때만 숨김.
  if (wilting || plant.stage < 5) {
    root.append(makeHomeChip());
  }
```

Add the new function after `makeActionChip` (after line 89):

```ts
function makeHomeChip(): HTMLButtonElement {
  const btn = document.createElement('button');
  btn.type = 'button';
  btn.className = 'plant-action-home-chip';
  btn.textContent = '🌱 홈에서 오늘 브리핑 보기';
  btn.setAttribute('aria-label', '홈으로 이동해 오늘 브리핑 보기');
  btn.addEventListener('click', () => {
    closeModal();
    // 사전 review P1-3: archive chip과 parity — home chunk load reject 시 unhandled rejection 방지.
    void switchTab('home').catch((err) => {
      console.warn('[plant-action] home nav failed', err);
    });
  });
  return btn;
}
```

- [ ] **Step 4: Run to verify PASS**

Run: `npm test -- plant-detail`
Expected: all PASS.

> ⚠️ jsdom에서 `switchTab('home')`는 dynamic import(home 탭)를 호출하나, 이 4개 테스트는 **클릭하지 않으므로** 실행 안 됨. 클릭 동작은 Task 3에서 mock으로 검증.

- [ ] **Step 5: tsc + lint**

Run: `npx tsc --noEmit && npm run lint`
Expected: 0 errors.

- [ ] **Step 6: Commit**

```bash
git add src/ui/modals/plant-detail.ts tests/ui/modals/plant-detail.spec.ts
git commit -m "feat(v3.47 T2): plant modal 홈 재참여 버튼 (wilting||stage<5 표시)"
```

---

### Task 3: 홈 버튼 클릭 → closeModal + switchTab('home')

**Files:**
- Test: `tests/ui/modals/plant-detail.spec.ts` (append, with `vi.mock`)

- [ ] **Step 1: Write failing test (vi.doMock + 동적 import 패턴)**

> **컨벤션 준수 (실측):** 이 파일의 기존 nav-flow 테스트(line 171~ `navigateToInterestArchive sequence`)는 top-level `vi.mock`이 아니라 **`vi.doMock(...)` + 동적 `import()`** 패턴을 쓴다. top-level `vi.mock`은 hoisting되어 파일 전체를 오염시키므로 **금지**.
>
> **⚠️ 사전 review P0-2 (실측 확인):** 기존 nav-flow describe는 `vi.doMock('../../../src/ui/modals/shared', () => ({ openModal: vi.fn(), closeModal: closeModalSpy }))`를 등록하고 **unmock하지 않는다** (line 209-211). `vi.resetModules()`는 module 캐시만 비우고 등록된 `doMock`은 **해제하지 않는다**. 따라서 Task 3가 그냥 `import('plant-detail')`하면 stale mock된 `openModal`(no-op vi.fn)을 집어들어 **modal DOM이 생성되지 않고** `.plant-action-home-chip`가 null이 된다. → Task 3는 의존 모듈을 **명시적으로 doUnmock**한 뒤 nav만 새로 mock해야 한다.
>
> **P1-2 (순서 증명):** modal 닫힘+switchTab 호출만 보지 말고 `closeModal()`이 `switchTab` **이전**에 일어났는지 증명한다 — `switchTabSpy` 호출 시점에 `.dg-modal`이 이미 없음을 spy 내부에서 assert.

Append describe (파일 끝):

```ts
describe('plant-detail home chip click (v3.47)', () => {
  beforeEach(() => {
    document.body.replaceChildren();
    localStorage.clear();
    // P0-2: 이전 describe가 등록한 shared/archive/interestKeywords doMock 해제 → 실제 모듈 사용.
    vi.doUnmock('../../../src/ui/modals/shared');
    vi.doUnmock('../../../src/ui/handlers/archive');
    vi.doUnmock('../../../src/utils/interestKeywords');
    vi.resetModules();
  });

  afterEach(() => {
    vi.doUnmock('../../../src/ui/nav');
    vi.resetModules();
  });

  it('홈 버튼 클릭 → closeModal 먼저, 그 다음 switchTab("home")', async () => {
    // P1-2: spy 호출 시점에 modal이 이미 닫혀 있어야 순서 보장.
    const switchTabSpy = vi.fn(async () => {
      expect(document.querySelector('.dg-modal')).toBeFalsy();  // closeModal이 먼저 실행됨
    });
    vi.doMock('../../../src/ui/nav', () => ({ switchTab: switchTabSpy }));
    // mock(nav) 반영 + 실제(shared) — fresh 모듈을 동적 import해 static switchTab가 spy를 집어든다.
    const { openPlantDetailModal } = await import('../../../src/ui/modals/plant-detail');

    saveUser(mkUser({
      interests: ['leadership'],
      gardenBackfilled: true,
      plantStateByInterest: { leadership: { stage: 2, cumulativeActivity: 10 } } satisfies Record<string, PlantState>,
    }));
    openPlantDetailModal('leadership');
    expect(document.querySelector('.dg-modal')).toBeTruthy();  // 실제 openModal로 렌더 확인(P0-2 guard)
    const btn = document.querySelector<HTMLButtonElement>('.plant-action-home-chip')!;
    btn.click();

    expect(document.querySelector('.dg-modal')).toBeFalsy();  // closeModal 동작
    expect(switchTabSpy).toHaveBeenCalledWith('home');
  });
});
```

> `afterEach` 사용 시 파일 상단 import에 추가 필요: `import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';` (현재 `afterEach` 미포함 — 확인 후 추가).
> COMMON_MISTAKES §11: production 경로 `'../nav'`/`'./shared'`와 mock 경로 `'../../../src/ui/nav'`/`'../../../src/ui/modals/shared'`가 동일 모듈 가리킴 확인 완료.
> `expect(document.querySelector('.dg-modal')).toBeTruthy()` 라인이 P0-2 회귀 가드 — stale shared mock이 살아있으면 이 줄에서 즉시 FAIL.

- [ ] **Step 2: Run to verify FAIL**

Run: `npm test -- plant-detail`
Expected: Task 2 구현 전이면 `.plant-action-home-chip` null로 FAIL. (Task 2 이후 실행이면 PASS — TDD 순서상 이 spec을 Task 2 구현 후 추가하므로 mock 패턴 검증이 주목적.)

- [ ] **Step 3: 회귀 확인 — 기존 nav-flow 테스트 무손상**

Run: `npm test -- plant-detail`
Expected: 전체 PASS. `vi.doMock`은 이 describe scope 내 동적 import에만 적용 + `afterEach` `doUnmock`으로 격리 → 기존 `navigateToInterestArchive sequence` 테스트(line 171~)는 자체 `vi.doMock`/`vi.resetModules` 유지로 무영향. FAIL 시 격리 누수 — `vi.resetModules()` 위치 점검.

- [ ] **Step 4: N=5 안정성**

Run: `for i in 1 2 3 4 5; do npm test -- plant-detail 2>&1 | tail -3; done`
Expected: 5/5 PASS.

- [ ] **Step 5: tsc + lint + Commit**

```bash
npx tsc --noEmit && npm run lint
git add tests/ui/modals/plant-detail.spec.ts
git commit -m "test(v3.47 T3): 홈 버튼 클릭 → closeModal + switchTab(home) spec"
```

---

### Task 4: CSS — `.plant-detail-nudge` + `.plant-action-home-chip`

**Files:**
- Modify: `src/styles/components/modal.css:41-43` (chip grouping selector) + nudge rule 추가

- [ ] **Step 1: home chip을 기존 chip grouping에 추가**

`src/styles/components/modal.css`의 line 41-42:

```css
.plant-action-chip,
.insight-archive-nav-chip {
```

를 다음으로 변경:

```css
.plant-action-chip,
.insight-archive-nav-chip,
.plant-action-home-chip {
```

`:hover`(line 57) / `:focus-visible`(line 63) grouping에도 `.plant-action-home-chip`를 동일 추가:

```css
.plant-action-chip:hover,
.insight-archive-nav-chip:hover,
.plant-action-home-chip:hover {
```

```css
.plant-action-chip:focus-visible,
.insight-archive-nav-chip:focus-visible,
.plant-action-home-chip:focus-visible {
```

- [ ] **Step 2: nudge rule 추가**

`.plant-detail-wilting` rule(line 32-39) 뒤에 추가:

```css
/* v3.47: 다음 단계 nudge — primary 톤 격려 메시지 */
.plant-detail-nudge {
  color: var(--primary);
  font-weight: 600;
  font-size: var(--font-size-sm);
  margin-top: var(--space-1, 4px);
}
```

> 두 chip(archive + home)이 세로로 쌓일 때 간격은 기존 `.plant-action-chip { margin-top: var(--space-4); }`가 처리 (home chip도 grouping으로 동일 margin 상속).

- [ ] **Step 2b: (사전 review P2-1) base.css scroll-margin parity**

`src/styles/base.css`의 `:where(...)` scroll-margin 셀렉터(line 42-44)에 `.plant-action-chip` 옆에 `.plant-action-home-chip` 추가:

```css
:where(input, textarea, [data-scroll-target],
       .archive-card, .archive-insight-card,
       .plant-action-chip, .plant-action-home-chip, .insight-archive-nav-chip,
       .mission-card, .garden-card) {
```

> `home chip`이 `.plant-action-chip` base를 안 쓰므로 global scroll-margin 미상속 → parity 위해 명시 추가. `tests/unit/css-tokens.spec.ts:217` 정규식 `/\.plant-action-chip,\s*\.insight-archive-nav-chip/`는 substring 매칭이라 중간 삽입 시 깨짐 — `.plant-action-chip, .plant-action-home-chip, .insight-archive-nav-chip` 순서면 정규식이 `\.plant-action-chip,\s*` 다음 `\.insight-archive-nav-chip`를 요구하므로 **FAIL**. 따라서 css-tokens 정규식도 함께 갱신:
>
> `tests/unit/css-tokens.spec.ts:217`을 `expect(baseCss).toMatch(/\.plant-action-chip,\s*\.plant-action-home-chip,\s*\.insight-archive-nav-chip/);`로 수정.

- [ ] **Step 3: build로 CSS 유효성 확인**

Run: `npm run build`
Expected: 빌드 성공 (tsc + vite). CSS 파싱 에러 없음.

- [ ] **Step 4: Commit**

```bash
git add src/styles/components/modal.css src/styles/base.css tests/unit/css-tokens.spec.ts
git commit -m "style(v3.47 T4): .plant-detail-nudge + .plant-action-home-chip (chip 토큰 + scroll-margin parity)"
```

---

### Task 5: Smoke — 시든 식물 → 홈 버튼 → 홈 탭 활성

**Files:**
- Modify: `tests/smoke/plant-detail-modal.spec.ts` (append test)

- [ ] **Step 1: Write smoke test**

Append to `tests/smoke/plant-detail-modal.spec.ts`:

```ts
// v3.47: 시든 식물 재참여 CTA → 홈 탭 이동
test('v3.47: 시든 식물 홈 버튼 → 홈 탭 활성', async ({ page }) => {
  await primeOnboardedUser(page, { interests: ['leadership'], schemaVersion: 4 });
  await page.addInitScript(() => {
    const userRaw = localStorage.getItem('user');
    if (!userRaw) return;
    const u = JSON.parse(userRaw);
    const eightDaysAgo = new Date(Date.now() - 8 * 24 * 60 * 60 * 1000).toISOString();
    u.plantStateByInterest = {
      leadership: { stage: 2, cumulativeActivity: 10, lastEngagedAt: eightDaysAgo },
    };
    u.gardenBackfilled = true;
    u.gardenIntroduced = true;
    localStorage.setItem('user', JSON.stringify(u));
    sessionStorage.setItem('dg.briefings.auto-refresh-tried', '1');
  });

  await page.goto('/');
  await page.locator('#bottomNav button[data-tab-id="stats"]').click();
  const card = page.locator('.garden-card[data-interest-id="leadership"]');
  await expect(card).toBeVisible();
  await card.click();

  const modal = page.locator('.plant-detail-modal');
  await expect(modal).toBeVisible();
  // 시든 식물 → 홈 버튼 보임
  const homeBtn = page.locator('.plant-action-home-chip');
  await expect(homeBtn).toBeVisible();

  await homeBtn.click();
  // modal 닫힘 + 홈 탭 활성
  await expect(modal).not.toBeVisible();
  await expect(page.locator('#bottomNav button[data-tab-id="home"]')).toHaveClass(/active/);
});
```

- [ ] **Step 2: Run smoke**

Run: `npm run test:smoke -- plant-detail-modal`
Expected: 신규 포함 전체 PASS.

- [ ] **Step 3: N=3 cold-fork**

Run: `for i in 1 2 3; do rm -rf node_modules/.vite-temp; npm run test:smoke -- plant-detail-modal 2>&1 | tail -3; done`
Expected: 3/3 0 flake.

> COMMON_MISTAKES §5: 홈 탭 활성 판정은 `toHaveClass(/active/)` (정적 element 아님 — bottom nav active 상태). cold-start 첫 mount 대비 `toBeVisible()` 기본 timeout으로 충분(garden card visibility wait 이미 적용).

- [ ] **Step 4: Commit**

```bash
git add tests/smoke/plant-detail-modal.spec.ts
git commit -m "test(v3.47 T5): smoke — 시든 식물 홈 버튼 → 홈 탭 활성"
```

---

### Task 6: 사이클-끝 검증 + bundle dual-record

**Files:** 없음 (검증 only)

- [ ] **Step 1: 3종 일괄**

Run: `npm run lint && npm test && npm run test:smoke`
Expected: lint 0 / vitest 전체 PASS / smoke 전체 PASS.

- [ ] **Step 2: 양쪽 TZ vitest**

Run: `TZ=America/New_York npm test 2>&1 | tail -5`
Expected: PASS (식물 wilting은 8일 전 ISO라 TZ 무관이지만 회귀 확인).

- [ ] **Step 3: Bundle dual-record**

```bash
npm run build
echo "index:   $(gzip -c dist/assets/index-*.js | wc -c) B"
echo "archive: $(gzip -c dist/assets/archive-*.js | wc -c) B"
```
Expected: index 변화 ~0 (plant-detail은 stats lazy chunk). retro에 두 값 기록. plant-detail이 속한 stats chunk도 측정해 기록(`gzip -c dist/assets/stats-*.js | wc -c` 가능 시).

- [ ] **Step 4: retro 작성**

`docs/superpowers/specs/2026-05-22-v3.47-plant-action-layer-design.md`에 §retro 추가 (test count Δ, bundle 3값, Codex 2-pass 결과, 신규 lessons, v3.48 carry).

- [ ] **Step 5: memory 업데이트**

`MEMORY.md`에 v3.47 한 줄 인덱스 + `memory/project_daily_growth_v347.md` 신규 (한 줄 인덱스 200자 이내 — MEMORY.md 이미 limit 초과 경고 상태이므로 짧게).

---

## Self-Review (writing-plans)

**1. Spec coverage:**
- §3 A) nudge → Task 1 ✓
- §3 B) home 버튼 렌더+조건 → Task 2 ✓ / 클릭 → Task 3 ✓
- §3 CSS → Task 4 ✓
- §4 단위(nudge 3 + home 4 + 클릭) → Task 1/2/3 ✓
- §4 smoke → Task 5 ✓
- §4 3종 게이트 + bundle → Task 6 ✓
- §5 Codex 2-pass → 본 plan 외부(controller가 사전 review를 plan 직후, 최종 review를 T6 후 dispatch)

**2. Placeholder scan:** 모든 step에 실제 코드/명령 포함. Task 3 Step 2의 "필요 시 조정"은 mock scope 충돌 가능성에 대한 명시적 검증 지시(placeholder 아님) — 구현자가 실측 후 분기.

**3. Type consistency:** `PlantState`, `STAGE_THRESHOLDS[stage-1]`, `switchTab('home')`, `closeModal()` 모두 기존 시그니처와 일치. home 버튼 클래스 `plant-action-home-chip` 4개 task에서 동일 표기 ✓.

**Codex 사전 review 반영 (2026-05-22, P0×2 + P1×3 + P2×1 흡수):**
- **P0-1** spec class 불일치 (`plant-action-chip--home` → `.plant-action-home-chip`): spec §3B + DOM order 정정 완료.
- **P0-2** Task 3 mock 격리 불충분 (기존 describe의 `shared` doMock이 unmock 안 됨 → stale no-op `openModal` → modal DOM 미생성): Task 3 beforeEach에 `vi.doUnmock('shared'/'archive'/'interestKeywords')` 추가 + `.dg-modal` truthy 회귀 가드.
- **P1-1** nudge edge: remaining===3 경계 + clamp(stage4 cum160→딱1번) 테스트 2건 추가.
- **P1-2** 순서 증명: switchTabSpy 내부에서 `.dg-modal` 부재 assert (closeModal→switchTab 순서 고정).
- **P1-3** `void switchTab('home').catch(...)` parity 적용.
- **P2-1** base.css scroll-margin `:where()`에 `.plant-action-home-chip` 추가 + css-tokens 정규식 갱신 (in-cycle 흡수).
- nudge DOM order/escaping: Codex "no issue" 확인.

---

**End of plan.**
