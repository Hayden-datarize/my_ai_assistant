# v3.9 — Undo Toast 시스템 통합 + Archive 카드 통일 (Implementation Plan)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Undo 토스트의 unstyled 버그를 잡으면서 기존 `.toast` 시스템에 흡수하고, archive scrap 카드를 답변 카드와 시각/인터랙션 통일하며, v3.8 P2 4건(P2-1 dead key wiring, P2-2 atomic Undo, P2-6 에러 토스트 검증, P2-7 5초 timeout 통합)을 자연 흡수한다.

**Architecture:** `.dg-toast` 클래스 폐기 + 기존 `.toast` 시스템에 `.toast--undo` modifier로 흡수. Undo 토스트는 별도 `#toastContainer--undo` 컨테이너로 하단 중앙 분리. Archive scrap 카드는 `data-briefing-id` 추가 + delegation 패턴 + ✕("스크랩 해제") + select 모드 호환. Atomic Undo는 단일 `saveAnswers([...loadAnswers(), ...snapshot])` write.

**Tech Stack:** TypeScript / Vite SPA / Vitest (jsdom) / Playwright / Vanilla DOM (no framework) / Firebase Hosting.

**Spec:** [docs/superpowers/specs/2026-04-27-v3.9-toast-archive-unify-design.md](../specs/2026-04-27-v3.9-toast-archive-unify-design.md)

---

## File Structure

| File | Action | Responsibility |
|---|---|---|
| `src/utils/toast.ts` | Modify | className `.dg-toast*` → `.toast*`, `#toastContainer` + `#toastContainer--undo` 컨테이너 lazy create |
| `src/styles/components/shared.css` | Modify | `.toast-container--undo` + `.toast--undo` + 액션 버튼 + `toastInUp`/`toastOutDown` + `prefers-reduced-motion` |
| `src/ui/messages.ts` | Modify | `SCRAP_UNDO_TOAST` / `SCRAP_UNDO_RESTORED` / `SCRAP_BULK_CONFIRM(n)` / `SCRAP_BULK_UNDO_TOAST(n)` 추가 |
| `src/ui/handlers/archive.ts` | Modify | scrap 카드 마크업/delegation/✕/select 모드 + 필터 잠금 + atomic Undo + DELETE_UNDO_RESTORED wiring |
| `tests/toast-undo.spec.ts` | Modify | 클래스 검증 `.dg-toast*` → `.toast*`, 컨테이너 ID 검증 |
| `tests/archive-delete-target.spec.ts` | Modify | atomic Undo + DELETE_UNDO_RESTORED 토스트 검증 + storage throw 토스트 spy(P2-6) + 5초 timeout (P2-7) |
| `tests/archive-delete-bulk.spec.ts` | Modify | 동일 4건 |
| `tests/settings-delete-all.spec.ts` | Modify | 동일 (atomic + RESTORED + storage throw + 5초) |
| `tests/messages-delete.spec.ts` | Modify | `SCRAP_*` 키 4개 추가 검증 + `BUTTON_LABEL_KEYS`에 영향 없음 확인 |
| `tests/archive-scrap-delete.spec.ts` | Create | scrap 단건 ✕ → `toggleScrap` + Undo + 복원 |
| `tests/archive-scrap-bulk.spec.ts` | Create | select 모드 + scrap 필터 + 벌크 해제 + Undo |
| `tests/archive-select-filter-lock.spec.ts` | Create | select 진입 → 다른 chip aria-disabled, 종료 → 복원 |
| `tests/smoke/archive-scrap-card.spec.ts` | Create | Playwright — scrap 카드 ✕ + 토스트 시각 + 필터 잠금 cursor |

**기존 `.dg-toast*` 사용처 (T1에서 일괄 교체)**:
- `src/utils/toast.ts:8,13,48` (showToast/showUndoToast)
- 테스트는 `tests/toast-undo.spec.ts` + `archive-delete-*` 안의 클래스 assertion

---

## Task 1: 토스트 시스템 통합 (`.toast--undo` CSS + `.dg-toast*` 폐기)

**Files:**
- Modify: `src/utils/toast.ts:1-76`
- Modify: `src/styles/components/shared.css:6-23` (확장)
- Modify: `tests/toast-undo.spec.ts` (클래스 assertion 업데이트)

### Why this task

v3.8에서 `showUndoToast` 도입 시 `.dg-toast*` 클래스를 새로 만들었지만 CSS 규칙이 0개라 unstyled 상태였다. 기존 `.toast` 시스템에 흡수 + `.toast--undo` modifier로 디자인을 명시한다. Undo 토스트는 하단 중앙(bottom-nav 위)으로 분리.

### Steps

- [ ] **Step 1.1**: 기존 `tests/toast-undo.spec.ts` 읽고 클래스 assertion 식별

  ```bash
  cat tests/toast-undo.spec.ts | grep -nE "dg-toast|className"
  ```

  목표: 클래스명 assertion이 있는 라인 수집 (T1 마지막 단계에서 일괄 교체).

- [ ] **Step 1.2**: 기존 `tests/toast-undo.spec.ts`에 신규 it-block 추가 — `.toast--undo` 클래스 + 컨테이너 분리 검증

  ```typescript
  // tests/toast-undo.spec.ts 끝에 추가
  describe('showUndoToast — v3.9 통합', () => {
    afterEach(() => {
      document.body.innerHTML = '';
      vi.useRealTimers();
    });

    it('Undo 토스트는 .toast.toast--undo 클래스 + #toastContainer--undo 컨테이너에 표시한다', () => {
      const { showUndoToast } = require('../src/utils/toast');
      showUndoToast({ message: '삭제됨', actionLabel: '되돌리기', onUndo: () => {} });
      const container = document.getElementById('toastContainer--undo');
      expect(container).toBeTruthy();
      const toast = container?.querySelector('.toast.toast--undo');
      expect(toast).toBeTruthy();
      expect(document.querySelector('.dg-toast')).toBeNull();
      expect(document.querySelector('.dg-toast--undo')).toBeNull();
    });

    it('일반 토스트는 #toastContainer (top), Undo 토스트는 #toastContainer--undo (bottom)으로 분리된다', () => {
      const { showToast, showUndoToast } = require('../src/utils/toast');
      showToast('일반');
      showUndoToast({ message: '삭제됨', actionLabel: '되돌리기', onUndo: () => {} });
      expect(document.getElementById('toastContainer')?.querySelector('.toast')).toBeTruthy();
      expect(document.getElementById('toastContainer--undo')?.querySelector('.toast--undo')).toBeTruthy();
    });
  });
  ```

- [ ] **Step 1.3**: 테스트 실행 — FAIL 확인

  Run: `npm test -- tests/toast-undo.spec.ts`
  Expected: 신규 2 it-block FAIL (클래스가 아직 `dg-toast`)

- [ ] **Step 1.4**: `src/utils/toast.ts` 전면 교체

  ```typescript
  /**
   * Lightweight transient notification. Appends a styled toast to a top-center
   * container and auto-dismisses after `durationMs`.
   */
  function ensureContainer(id: string, modifier?: 'undo'): HTMLElement {
    let el = document.getElementById(id);
    if (el) return el;
    el = document.createElement('div');
    el.id = id;
    el.className = modifier ? `toast-container toast-container--${modifier}` : 'toast-container';
    const root = document.getElementById('modalRoot') ?? document.body;
    root.append(el);
    return el;
  }

  export function showToast(message: string, durationMs = 2500): void {
    const container = ensureContainer('toastContainer');
    const el = document.createElement('div');
    el.className = 'toast';
    el.setAttribute('role', 'status');
    el.textContent = message;
    container.append(el);
    setTimeout(() => {
      el.classList.add('toast-leaving');
      setTimeout(() => el.remove(), 250);
    }, durationMs);
  }

  // undo 토스트 싱글톤 상태 — 한 번에 하나의 undo 토스트만 표시
  let activeUndoTimer: number | null = null;
  let activeUndoEl: HTMLElement | null = null;

  /**
   * Undo-action toast. Shows a message with an action button that triggers `onUndo`.
   * Only one undo toast is active at a time — calling again dismisses the previous one
   * (discarding its onUndo callback) and shows the new one.
   * Auto-dismisses after `durationMs` (default 5000ms) with a leave animation.
   */
  export function showUndoToast(opts: {
    message: string;
    actionLabel: string;
    onUndo: () => void;
    durationMs?: number;
  }): void {
    const duration = opts.durationMs ?? 5000;

    if (activeUndoTimer !== null) {
      window.clearTimeout(activeUndoTimer);
      activeUndoTimer = null;
    }
    if (activeUndoEl) {
      activeUndoEl.remove();
      activeUndoEl = null;
    }

    const container = ensureContainer('toastContainer--undo', 'undo');
    const el = document.createElement('div');
    el.className = 'toast toast--undo';
    el.setAttribute('role', 'status');

    const msg = document.createElement('span');
    msg.textContent = opts.message;

    const btn = document.createElement('button');
    btn.type = 'button';
    btn.textContent = opts.actionLabel;
    btn.addEventListener('click', () => {
      if (activeUndoTimer !== null) {
        window.clearTimeout(activeUndoTimer);
        activeUndoTimer = null;
      }
      el.remove();
      activeUndoEl = null;
      opts.onUndo();
    });

    el.append(msg, btn);
    container.append(el);
    activeUndoEl = el;

    activeUndoTimer = window.setTimeout(() => {
      el.classList.add('toast-leaving');
      window.setTimeout(() => {
        el.remove();
        activeUndoEl = null;
        activeUndoTimer = null;
      }, 250);
    }, duration);
  }
  ```

  변경점:
  - `dg-toast` → `toast`, `dg-toast--undo` → `toast toast--undo`, `dg-toast-leaving` → `toast-leaving`
  - `ensureContainer` helper 신설 (lazy create, 두 컨테이너 모두 처리)
  - undo 토스트도 leaving 클래스 → 250ms 후 remove (v3.8 동작 보존)

- [ ] **Step 1.5**: `src/styles/components/shared.css`의 Toast 섹션 확장 (line 6-23 교체)

  ```css
  /* Toast — top container (general) */
  .toast-container {
    position: fixed; top: 20px; left: 50%;
    transform: translateX(-50%);
    z-index: var(--z-toast); display: flex;
    flex-direction: column; gap: 8px;
    pointer-events: none; max-width: 400px; width: calc(100% - 40px);
  }
  /* Toast — bottom container (undo, above bottom-nav) */
  .toast-container--undo {
    position: fixed;
    bottom: calc(var(--nav-height) + var(--safe-bottom) + 16px);
    top: auto;
    left: 50%;
    transform: translateX(-50%);
    z-index: var(--z-toast); display: flex;
    flex-direction: column; gap: 8px;
    pointer-events: none; max-width: 400px; width: calc(100% - 40px);
  }
  /* Desktop: bottom-nav가 숨겨지므로 단순 16px */
  @media (min-width: 768px) {
    .toast-container--undo { bottom: calc(var(--safe-bottom) + 16px); }
  }
  .toast {
    padding: 12px 16px; border-radius: var(--radius-md);
    background: var(--bg-card); border: 1px solid var(--border);
    box-shadow: var(--shadow-lg); font-size: 0.85rem;
    display: flex; align-items: center; gap: 8px;
    animation: toastIn 0.3s ease, toastOut 0.3s ease 2.7s forwards;
    pointer-events: auto;
  }
  /* Undo 변형: 하단 진입 + 액션 버튼, 자동 fade out 비활성(JS가 leaving 클래스 제어) */
  .toast.toast--undo {
    justify-content: space-between;
    animation: toastInUp 0.3s ease;
  }
  .toast.toast--undo.toast-leaving {
    animation: toastOutDown 0.25s ease forwards;
  }
  .toast--undo button {
    flex-shrink: 0;
    min-height: 44px; min-width: 64px;
    padding: 6px 12px;
    background: transparent;
    border: 1px solid var(--primary);
    border-radius: var(--radius-sm);
    color: var(--primary);
    font-weight: 600; font-size: 0.85rem;
    cursor: pointer;
  }
  .toast--undo button:hover { background: color-mix(in srgb, var(--primary) 8%, transparent); }
  .toast--undo button:focus-visible { outline: 2px solid var(--primary); outline-offset: 2px; }
  @keyframes toastIn { from { opacity: 0; transform: translateY(-12px); } to { opacity: 1; transform: translateY(0); } }
  @keyframes toastOut { from { opacity: 1; } to { opacity: 0; transform: translateY(-12px); } }
  @keyframes toastInUp { from { opacity: 0; transform: translateY(12px); } to { opacity: 1; transform: translateY(0); } }
  @keyframes toastOutDown { from { opacity: 1; } to { opacity: 0; transform: translateY(12px); } }
  @media (prefers-reduced-motion: reduce) {
    .toast,
    .toast.toast--undo,
    .toast.toast--undo.toast-leaving { animation-duration: 0.01s; }
  }
  ```

  근거: `--nav-height: 64px` + `--safe-bottom` 토큰 활용 (tokens.css:31). desktop은 bottom-nav 숨김(layout.css:85) 기준 768px 분기로 단순화. 액션 버튼 `min-height: 44px`는 WCAG 2.5.5 터치 타겟.

- [ ] **Step 1.6**: 기존 `tests/toast-undo.spec.ts`의 `dg-toast` 클래스 assertion을 `toast`로 일괄 교체

  Run: `grep -n "dg-toast" tests/toast-undo.spec.ts`

  각 라인을 수동으로 교체:
  - `'dg-toast'` → `'toast'`
  - `'dg-toast--undo'` → `'toast--undo'`
  - `'dg-toast-leaving'` → `'toast-leaving'`
  - 컨테이너 query: `document.body` → `document.getElementById('toastContainer--undo')` (해당 컨텍스트만)

- [ ] **Step 1.7**: vitest 전체 실행 — toast 관련 테스트 모두 PASS

  Run: `npm test -- toast`
  Expected: tests/toast-undo.spec.ts + tests/unit/translateToast.spec.ts 모두 PASS

- [ ] **Step 1.8**: 커밋

  ```bash
  git add src/utils/toast.ts src/styles/components/shared.css tests/toast-undo.spec.ts
  git commit -m "feat(v3.9 T1): unify toast system, add .toast--undo modifier with bottom container"
  ```

---

## Task 2: P2-1 (DELETE_UNDO_RESTORED wiring) + P2-2 (atomic Undo)

**Files:**
- Modify: `src/ui/handlers/archive.ts:81-92,140-151` (bulk + target Undo)
- Modify: `src/ui/tabs/settings.ts` (deleteAll Undo — 위치는 grep으로 확인)
- Modify: `tests/archive-delete-target.spec.ts` + `tests/archive-delete-bulk.spec.ts` + `tests/settings-delete-all.spec.ts`

### Why this task

v3.8에서 추가된 `MSG.DELETE_UNDO_RESTORED` 키가 dead code(P2-1)이고, Undo 복원이 `forEach(appendAnswer)`로 N회 read-modify-write라 부분 실패 가능(P2-2). atomic single write + 성공 토스트 wiring을 한 번에.

### Steps

- [ ] **Step 2.1**: settings의 deleteAll Undo 위치 식별

  Run: `grep -n "deleteAllAnswers\|DELETE_UNDO\|showUndoToast" src/ui/tabs/settings.ts`
  Expected: deleteAll 트리거 + Undo 토스트 wiring 라인 발견. snapshot 확보 패턴 확인.

- [ ] **Step 2.2**: `tests/archive-delete-target.spec.ts`에 atomic + RESTORED 토스트 it-block 추가

  ```typescript
  it('Undo 클릭 시 atomic single write로 복원하고 DELETE_UNDO_RESTORED 토스트를 표시한다', async () => {
    const { mountArchiveHandlers, hydrateArchive } = await import('../src/ui/handlers/archive');
    const { saveAnswers } = await import('../src/state/persistence');
    const { MSG } = await import('../src/ui/messages');

    saveAnswers([{ id: 'a1', text: 't1', createdAt: 1 } as any]);
    document.body.innerHTML = '<div id="archiveTab"><div id="archiveList"></div></div>';
    mountArchiveHandlers();
    hydrateArchive();

    // 단건 ✕ 클릭
    document.querySelector<HTMLButtonElement>('.archive-card-delete')!.click();

    // Undo 클릭
    const saveSpy = vi.spyOn(Storage.prototype, 'setItem');
    saveSpy.mockClear();
    document.querySelector<HTMLButtonElement>('.toast--undo button')!.click();

    // atomic: setItem 1회만 (saveAnswers([...current, ...snapshot]))
    const calls = saveSpy.mock.calls.filter(([k]) => k === 'dg.answers');
    expect(calls).toHaveLength(1);

    // DELETE_UNDO_RESTORED 토스트 표시
    expect(document.body.textContent).toContain(MSG.DELETE_UNDO_RESTORED);
  });
  ```

  실제 spec 파일은 기존 setup 패턴(import 방식, beforeEach 등) 따라 정렬 필요. `Answer` 타입은 schema.ts 참조. `vi.spyOn(Storage.prototype, 'setItem')`은 mock 패턴(기존 spec 참고).

- [ ] **Step 2.3**: `tests/archive-delete-bulk.spec.ts` + `tests/settings-delete-all.spec.ts`에 동일 패턴 it-block 추가 (각 1개)

- [ ] **Step 2.4**: 테스트 실행 — FAIL 확인

  Run: `npm test -- archive-delete-target archive-delete-bulk settings-delete-all`
  Expected: 신규 3 it-block FAIL (`forEach(appendAnswer)`이 N회 setItem 호출 + RESTORED 토스트 없음)

- [ ] **Step 2.5**: `src/ui/handlers/archive.ts`의 `handleCardDeleteClick` Undo 콜백 교체 (line 143-150)

  Before:
  ```typescript
  onUndo: () => {
    try {
      appendAnswer(snapshot);
      rerenderList();
    } catch {
      showToast(MSG.DELETE_UNDO_FAILED);
    }
  },
  ```

  After:
  ```typescript
  onUndo: () => {
    try {
      saveAnswers([...loadAnswers(), snapshot]);
      rerenderList();
      showToast(MSG.DELETE_UNDO_RESTORED);
    } catch {
      showToast(MSG.DELETE_UNDO_FAILED);
    }
  },
  ```

  import에 `saveAnswers` 추가 (line 8): `import { loadAnswers, appendAnswer, saveAnswers, deleteAnswerById, deleteAnswersByIds } from '../../state/persistence';`

- [ ] **Step 2.6**: `handleBulkDeleteClick` Undo 콜백 교체 (line 84-91)

  ```typescript
  onUndo: () => {
    try {
      saveAnswers([...loadAnswers(), ...snapshot]);
      rerenderList();
      showToast(MSG.DELETE_UNDO_RESTORED);
    } catch {
      showToast(MSG.DELETE_UNDO_FAILED);
    }
  },
  ```

- [ ] **Step 2.7**: `src/ui/tabs/settings.ts` deleteAll Undo 콜백을 동일 패턴으로 교체 (Step 2.1에서 식별한 위치)

  ```typescript
  // 기존: snapshot.forEach((a) => appendAnswer(a));
  // 신규:
  saveAnswers(snapshot);  // deleteAll은 전체 삭제 후 복원이라 snapshot이 곧 전체
  rerenderList();  // 또는 settings 컨텍스트의 적절한 rerender 함수
  showToast(MSG.DELETE_UNDO_RESTORED);
  ```

  주의: settings의 deleteAll은 archive와 다른 컨텍스트(snapshot이 모든 answers). `[...loadAnswers(), ...snapshot]`이 아니라 `snapshot` 그대로(현재 list는 비어있음). 단, race 우려 없음(deleteAll → undo 5초 안에 새 답변 저장 가능성 ~0).

- [ ] **Step 2.8**: 테스트 실행 — PASS 확인

  Run: `npm test -- archive-delete-target archive-delete-bulk settings-delete-all toast-undo`
  Expected: 모두 PASS, 기존 테스트 회귀 없음

- [ ] **Step 2.9**: 커밋

  ```bash
  git add src/ui/handlers/archive.ts src/ui/tabs/settings.ts tests/archive-delete-target.spec.ts tests/archive-delete-bulk.spec.ts tests/settings-delete-all.spec.ts
  git commit -m "feat(v3.9 T2): atomic Undo restore + wire DELETE_UNDO_RESTORED toast (P2-1, P2-2)"
  ```

---

## Task 3: SCRAP_* messages 추가 + messages-delete.spec.ts 검증

**Files:**
- Modify: `src/ui/messages.ts:13-32` (MSG 객체 확장)
- Modify: `tests/messages-delete.spec.ts` (신규 키 검증)

### Why this task

T4/T5에서 사용할 4개 메시지 키를 i18n Lite 어조 규칙(끝 마침표 + `~했어요`/`~할까요` + 제한 이모지)에 맞게 추가. lessons learned: spec 작성 시 컨벤션 SSOT 미리 확인 ✓ 완료.

### Steps

- [ ] **Step 3.1**: 기존 `tests/messages-delete.spec.ts` 검증 패턴 확인

  Run: `head -40 tests/messages-delete.spec.ts`
  Expected: 키 존재 + 어조 정합 검증 패턴 식별 (BUTTON_LABEL_KEYS exclusion 등).

- [ ] **Step 3.2**: `tests/messages-delete.spec.ts`에 SCRAP_* 키 검증 it-block 추가

  ```typescript
  describe('MSG.SCRAP_* keys (v3.9)', () => {
    it('정의되어 있고 끝 마침표 규칙을 준수한다', () => {
      const { MSG } = require('../src/ui/messages');
      expect(MSG.SCRAP_UNDO_TOAST).toBe('⭐ 스크랩을 해제했어요.');
      expect(MSG.SCRAP_UNDO_RESTORED).toBe('⭐ 스크랩을 복원했어요.');
      expect(typeof MSG.SCRAP_BULK_CONFIRM).toBe('function');
      expect(MSG.SCRAP_BULK_CONFIRM(3)).toContain('3개');
      expect(typeof MSG.SCRAP_BULK_UNDO_TOAST).toBe('function');
      expect(MSG.SCRAP_BULK_UNDO_TOAST(3)).toContain('3개');
    });

    it('어조 일관성: 끝 마침표 + 캐주얼 ~했어요/~할까요', () => {
      const { MSG } = require('../src/ui/messages');
      expect(MSG.SCRAP_UNDO_TOAST.endsWith('.')).toBe(true);
      expect(MSG.SCRAP_UNDO_RESTORED.endsWith('.')).toBe(true);
      expect(MSG.SCRAP_BULK_CONFIRM(1).endsWith('?')).toBe(true);
      expect(MSG.SCRAP_BULK_UNDO_TOAST(1).endsWith('.')).toBe(true);
    });
  });
  ```

- [ ] **Step 3.3**: 테스트 실행 — FAIL 확인

  Run: `npm test -- tests/messages-delete.spec.ts`
  Expected: 신규 2 it-block FAIL (`MSG.SCRAP_*` undefined)

- [ ] **Step 3.4**: `src/ui/messages.ts`의 MSG 객체 끝(line 31)에 4개 키 추가

  ```typescript
    DELETE_UNDO_FAILED: '복원 실패했어요.',
    SCRAP_UNDO_TOAST: '⭐ 스크랩을 해제했어요.',
    SCRAP_UNDO_RESTORED: '⭐ 스크랩을 복원했어요.',
    SCRAP_BULK_CONFIRM: (n: number) =>
      `선택한 스크랩 ${n}개를 해제할까요?\n5초 안에 되돌릴 수 있어요.`,
    SCRAP_BULK_UNDO_TOAST: (n: number) => `${n}개 스크랩을 해제했어요.`,
  } as const;
  ```

  주의: `⭐` 이모지는 messages.ts JSDoc의 "✅/ℹ️/❌ 3종만" 규칙과 충돌 가능. 점검 — JSDoc 원문(line 7): "이모지는 success(✅) / info(ℹ️) / error(❌) 3종만". ⭐는 위반. **결정**: ⭐ 제거하고 plain text로. v3.9 변경 사항 — JSDoc 규칙 우선.

  최종 키 (이모지 제거):
  ```typescript
    SCRAP_UNDO_TOAST: '스크랩을 해제했어요.',
    SCRAP_UNDO_RESTORED: '스크랩을 복원했어요.',
    SCRAP_BULK_CONFIRM: (n: number) =>
      `선택한 스크랩 ${n}개를 해제할까요?\n5초 안에 되돌릴 수 있어요.`,
    SCRAP_BULK_UNDO_TOAST: (n: number) => `${n}개 스크랩을 해제했어요.`,
  ```

  Step 3.2의 expect 값도 `'⭐ '` 접두 제거 — 사후 정합:

  ```typescript
  expect(MSG.SCRAP_UNDO_TOAST).toBe('스크랩을 해제했어요.');
  expect(MSG.SCRAP_UNDO_RESTORED).toBe('스크랩을 복원했어요.');
  ```

  카드 자체는 `⭐ ${title}` 패턴(handlers/archive.ts:226)으로 컨텍스트 명확하므로 토스트에 ⭐ 중복 불필요.

- [ ] **Step 3.5**: 테스트 실행 — PASS 확인

  Run: `npm test -- tests/messages-delete.spec.ts tests/unit/messages.spec.ts`
  Expected: 모두 PASS, 기존 BUTTON_LABEL_KEYS exclusion 회귀 없음

- [ ] **Step 3.6**: 커밋

  ```bash
  git add src/ui/messages.ts tests/messages-delete.spec.ts
  git commit -m "feat(v3.9 T3): add SCRAP_* messages for v3.9 archive scrap unify"
  ```

---

## Task 4: Archive scrap 카드 마크업 통일 (delegation 마이그레이션)

**Files:**
- Modify: `src/ui/handlers/archive.ts:204-235` (rerenderList scrap 분기)
- Modify: `src/ui/handlers/archive.ts:95-116` (handleCardClick scrap 분기 추가)

### Why this task

scrap 카드를 답변 카드와 동일한 delegation 패턴 + `data-briefing-id` + 시각 일관으로 통일. per-card listener 제거(P2-5 risk 회피). ✕ 버튼/select 모드 호환은 T5에서 이어서 처리.

### Steps

- [ ] **Step 4.1**: `tests/archive-scrap-delete.spec.ts` 생성 — 카드 마크업 + click 검증

  ```typescript
  // tests/archive-scrap-delete.spec.ts
  import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';

  describe('Archive scrap 카드 마크업 통일 (T4)', () => {
    beforeEach(() => {
      localStorage.clear();
      document.body.innerHTML = '';
    });
    afterEach(() => vi.restoreAllMocks());

    it('scrap 필터 시 카드는 .archive-card.archive-card--scrap + data-briefing-id를 가진다', async () => {
      const { saveBriefings } = await import('../src/state/briefings');
      const { mountArchiveHandlers, hydrateArchive } = await import('../src/ui/handlers/archive');

      saveBriefings([
        { id: 'b1', date: '2026-04-27', url: 'https://example.com', title: 'T1', summary: 'S1', scrapped: true, read: false, memo: '' } as any,
      ]);
      document.body.innerHTML = `
        <div id="modalRoot"></div>
        <div id="archiveTab">
          <div id="archiveFilters">
            <button class="filter-chip" data-filter="all">전체</button>
            <button class="filter-chip" data-filter="scrap">⭐ 스크랩</button>
          </div>
          <div id="archiveList"></div>
        </div>
      `;
      mountArchiveHandlers();
      // scrap 필터로 전환
      document.querySelector<HTMLButtonElement>('[data-filter="scrap"]')!.click();

      const card = document.querySelector<HTMLElement>('.archive-card.archive-card--scrap');
      expect(card).toBeTruthy();
      expect(card?.dataset['briefingId']).toBe('b1');
      // ✕ 버튼은 T5에서 추가 — 본 task에서는 마크업/delegation만 검증
    });

    it('scrap 카드 click → showArchiveDetail({ kind: "briefing" }) (delegation)', async () => {
      // openModal mock으로 detail 진입 검증
      vi.mock('../src/ui/modals/shared', () => ({
        openModal: vi.fn(),
        closeModal: vi.fn(),
      }));
      const { saveBriefings } = await import('../src/state/briefings');
      const { mountArchiveHandlers } = await import('../src/ui/handlers/archive');
      const { openModal } = await import('../src/ui/modals/shared');

      saveBriefings([
        { id: 'b1', date: '2026-04-27', url: 'https://example.com', title: 'T1', summary: 'S1', scrapped: true, read: false, memo: '' } as any,
      ]);
      document.body.innerHTML = `
        <div id="modalRoot"></div>
        <div id="archiveTab">
          <div id="archiveFilters"><button class="filter-chip" data-filter="scrap">스크랩</button></div>
          <div id="archiveList"></div>
        </div>
      `;
      mountArchiveHandlers();
      document.querySelector<HTMLButtonElement>('[data-filter="scrap"]')!.click();

      document.querySelector<HTMLElement>('.archive-card--scrap')!.click();
      expect(openModal).toHaveBeenCalledWith(expect.objectContaining({ title: '기록 상세' }));
    });
  });
  ```

  주의: 기존 spec들의 mock 패턴(특히 `vi.mock('../src/ui/modals/shared', ...)`)을 따른다. `Briefing` 타입은 `as any` 캐스팅 OK(테스트 픽스처).

- [ ] **Step 4.2**: 테스트 실행 — FAIL 확인

  Run: `npm test -- tests/archive-scrap-delete.spec.ts`
  Expected: 2 it-block 모두 FAIL (마크업이 아직 옛 형태, click이 per-card listener)

- [ ] **Step 4.3**: `src/ui/handlers/archive.ts` rerenderList의 scrap 분기 교체 (line 218-233)

  Before:
  ```typescript
  for (const b of scrapped) {
    const card = document.createElement('article');
    card.className = 'archive-card';
    const date = document.createElement('div');
    date.className = 'archive-date';
    date.textContent = b.date;
    const title = document.createElement('div');
    title.className = 'archive-text';
    title.textContent = `⭐ ${b.title}`;
    const summary = document.createElement('div');
    summary.className = 'archive-summary';
    summary.textContent = b.summary;
    card.append(date, title, summary);
    card.addEventListener('click', () => showArchiveDetail({ kind: 'briefing', briefing: b }));
    list.append(card);
  }
  ```

  After:
  ```typescript
  for (const b of scrapped) {
    const card = document.createElement('article');
    card.className = 'archive-card archive-card--scrap';
    card.dataset['briefingId'] = b.id;

    const date = document.createElement('div');
    date.className = 'archive-date';
    date.textContent = b.date;
    const title = document.createElement('div');
    title.className = 'archive-text';
    title.textContent = `⭐ ${b.title}`;
    const summary = document.createElement('div');
    summary.className = 'archive-summary';
    summary.textContent = b.summary;
    card.append(date, title, summary);
    // per-card listener 제거 — handleCardClick(delegation)이 처리
    list.append(card);
  }
  ```

- [ ] **Step 4.4**: `handleCardClick` (line 96-116)에 scrap 분기 추가

  ```typescript
  function handleCardClick(e: Event): void {
    if ((e.target as HTMLElement).closest('.archive-card-delete')) return;
    const card = (e.target as HTMLElement).closest<HTMLElement>('.archive-card');
    if (!card) return;
    if (selectMode) {
      handleCardClickInSelectMode(e);
      return;
    }

    const answerId = card.dataset['answerId'];
    if (answerId) {
      const answer = loadAnswers().find((a) => a.id === answerId);
      if (answer) showArchiveDetail({ kind: 'answer', answer });
      return;
    }

    // scrap 카드 분기 (T4 신설)
    if (card.classList.contains('archive-card--scrap')) {
      const briefingId = card.dataset['briefingId'];
      const briefing = loadBriefings().find((b) => b.id === briefingId);
      if (briefing) showArchiveDetail({ kind: 'briefing', briefing });
      return;
    }
  }
  ```

- [ ] **Step 4.5**: 테스트 실행 — PASS 확인

  Run: `npm test -- tests/archive-scrap-delete.spec.ts`
  Expected: 2 it-block 모두 PASS

- [ ] **Step 4.6**: archive 관련 회귀 확인

  Run: `npm test -- archive`
  Expected: 모든 archive-* spec PASS (기존 답변 카드 회귀 없음)

- [ ] **Step 4.7**: 커밋

  ```bash
  git add src/ui/handlers/archive.ts tests/archive-scrap-delete.spec.ts
  git commit -m "feat(v3.9 T4): unify archive scrap card markup (data-briefing-id + delegation)"
  ```

---

## Task 5: Scrap ✕("스크랩 해제") + select 모드 호환 + 필터 잠금 + 벌크 해제

**Files:**
- Modify: `src/ui/handlers/archive.ts` (handleCardDeleteClick + handleSelectToggle + handleBulkDeleteClick + handleCardClickInSelectMode)
- Modify: `tests/archive-scrap-delete.spec.ts` (✕ 동작 it-block 추가)
- Create: `tests/archive-scrap-bulk.spec.ts`
- Create: `tests/archive-select-filter-lock.spec.ts`

### Why this task

scrap 단건 ✕("스크랩 해제") 동작 + select 모드에서 scrap 필터 + 벌크 해제. select 모드 진입 시 다른 chip aria-disabled로 잠가 답변/scrap 혼합 선택 차단.

### Steps

- [ ] **Step 5.1**: `tests/archive-scrap-delete.spec.ts`에 ✕ + Undo it-block 추가

  ```typescript
  it('scrap 카드 ✕ 클릭 → toggleScrap → Undo 토스트 표시', async () => {
    const { saveBriefings, loadBriefings } = await import('../src/state/briefings');
    const { mountArchiveHandlers } = await import('../src/ui/handlers/archive');
    const { MSG } = await import('../src/ui/messages');

    saveBriefings([
      { id: 'b1', date: '2026-04-27', url: 'https://example.com', title: 'T1', summary: 'S1', scrapped: true, read: false, memo: '' } as any,
    ]);
    document.body.innerHTML = `
      <div id="modalRoot"></div>
      <div id="archiveTab">
        <div id="archiveFilters"><button class="filter-chip" data-filter="scrap">스크랩</button></div>
        <div id="archiveList"></div>
      </div>
    `;
    mountArchiveHandlers();
    document.querySelector<HTMLButtonElement>('[data-filter="scrap"]')!.click();

    document.querySelector<HTMLButtonElement>('.archive-card--scrap .archive-card-delete')!.click();

    expect(loadBriefings()[0].scrapped).toBe(false);
    expect(document.body.textContent).toContain(MSG.SCRAP_UNDO_TOAST);
  });

  it('Undo 클릭 → scrap 복원 + RESTORED 토스트', async () => {
    /* 동일 setup, ✕ 클릭 → Undo 클릭 → loadBriefings()[0].scrapped === true + MSG.SCRAP_UNDO_RESTORED 토스트 */
  });
  ```

- [ ] **Step 5.2**: `tests/archive-scrap-bulk.spec.ts` 생성

  ```typescript
  import { describe, it, expect, beforeEach, vi } from 'vitest';

  describe('Archive scrap 벌크 해제 (T5)', () => {
    beforeEach(() => {
      localStorage.clear();
      document.body.innerHTML = '';
    });

    it('scrap 필터 + select 모드 + 카드 N개 선택 + 벌크 → 모두 scrapped:false + Undo', async () => {
      const { saveBriefings, loadBriefings } = await import('../src/state/briefings');
      const { mountArchiveHandlers } = await import('../src/ui/handlers/archive');
      const { MSG } = await import('../src/ui/messages');

      saveBriefings([
        { id: 'b1', date: 'd1', url: 'u1', title: 't1', summary: 's1', scrapped: true, read: false, memo: '' },
        { id: 'b2', date: 'd2', url: 'u2', title: 't2', summary: 's2', scrapped: true, read: false, memo: '' },
      ] as any);
      document.body.innerHTML = `
        <div id="modalRoot"></div>
        <div id="archiveTab">
          <button id="archiveSelectToggle">선택</button>
          <button id="archiveBulkDelete" disabled>선택 항목 삭제 (0)</button>
          <div id="archiveFilters"><button class="filter-chip" data-filter="scrap">스크랩</button></div>
          <div id="archiveList"></div>
        </div>
      `;
      vi.spyOn(window, 'confirm').mockReturnValue(true);
      mountArchiveHandlers();
      document.querySelector<HTMLButtonElement>('[data-filter="scrap"]')!.click();
      document.getElementById('archiveSelectToggle')!.click();
      document.querySelectorAll<HTMLElement>('.archive-card--scrap').forEach((c) => c.click());
      document.getElementById('archiveBulkDelete')!.click();

      expect(loadBriefings().every((b) => !b.scrapped)).toBe(true);
      expect(document.body.textContent).toContain(MSG.SCRAP_BULK_UNDO_TOAST(2));
    });
  });
  ```

- [ ] **Step 5.3**: `tests/archive-select-filter-lock.spec.ts` 생성

  ```typescript
  import { describe, it, expect, beforeEach } from 'vitest';

  describe('Select 모드 필터 잠금 (T5)', () => {
    beforeEach(() => {
      localStorage.clear();
      document.body.innerHTML = '';
    });

    it('select 모드 진입 → 다른 filter chip이 aria-disabled=true가 된다', async () => {
      const { saveAnswers } = await import('../src/state/persistence');
      const { mountArchiveHandlers } = await import('../src/ui/handlers/archive');

      saveAnswers([{ id: 'a1', text: 't', createdAt: 1 } as any]);
      document.body.innerHTML = `
        <div id="modalRoot"></div>
        <div id="archiveTab">
          <button id="archiveSelectToggle">선택</button>
          <button id="archiveBulkDelete" disabled>선택 항목 삭제 (0)</button>
          <div id="archiveFilters">
            <button class="filter-chip active" data-filter="all">전체</button>
            <button class="filter-chip" data-filter="분석">분석</button>
            <button class="filter-chip" data-filter="scrap">스크랩</button>
          </div>
          <div id="archiveList"></div>
        </div>
      `;
      mountArchiveHandlers();
      document.getElementById('archiveSelectToggle')!.click();

      const chips = document.querySelectorAll<HTMLButtonElement>('.filter-chip');
      const active = document.querySelector<HTMLButtonElement>('.filter-chip[data-filter="all"]')!;
      expect(active.disabled).toBe(false);  // 현재 필터는 활성
      expect(active.getAttribute('aria-disabled')).not.toBe('true');
      chips.forEach((c) => {
        if (c !== active) {
          expect(c.disabled).toBe(true);
          expect(c.getAttribute('aria-disabled')).toBe('true');
        }
      });
    });

    it('select 모드 종료 → 모든 chip 복원', async () => {
      /* 동일 setup → toggle 두 번 → 모든 chip disabled === false */
    });
  });
  ```

- [ ] **Step 5.4**: 테스트 실행 — FAIL 확인

  Run: `npm test -- archive-scrap-delete archive-scrap-bulk archive-select-filter-lock`
  Expected: 모든 신규 it-block FAIL

- [ ] **Step 5.5**: `src/ui/handlers/archive.ts`의 `handleCardDeleteClick` scrap 분기 추가 (line 118-152 확장)

  현재 코드의 line 127 `if (!id) return;` 다음에 scrap 분기 삽입:

  ```typescript
  function handleCardDeleteClick(e: Event): void {
    const btn = (e.target as HTMLElement).closest<HTMLButtonElement>('.archive-card-delete');
    if (!btn) return;
    if (selectMode) return;
    e.stopPropagation();
    const card = btn.closest<HTMLElement>('.archive-card');
    if (!card) return;

    const answerId = card.dataset['answerId'];
    if (answerId) {
      // 기존 답변 삭제 흐름 (T2 atomic + RESTORED 적용 완료)
      const snapshot = loadAnswers().find((a) => a.id === answerId);
      if (!snapshot) return;
      try {
        deleteAnswerById(answerId);
      } catch (err) {
        showToast(getSaveErrorMessage(err));
        return;
      }
      rerenderList();
      showUndoToast({
        message: MSG.DELETE_UNDO_TOAST,
        actionLabel: MSG.DELETE_UNDO_ACTION,
        onUndo: () => {
          try {
            saveAnswers([...loadAnswers(), snapshot]);
            rerenderList();
            showToast(MSG.DELETE_UNDO_RESTORED);
          } catch {
            showToast(MSG.DELETE_UNDO_FAILED);
          }
        },
      });
      return;
    }

    // scrap 카드 분기 (T5 신설)
    if (card.classList.contains('archive-card--scrap')) {
      const briefingId = card.dataset['briefingId'];
      if (!briefingId) return;
      const briefings = loadBriefings();
      const idx = briefings.findIndex((b) => b.id === briefingId);
      if (idx < 0) return;
      try {
        toggleScrap(idx);
      } catch (err) {
        showToast(getSaveErrorMessage(err));
        return;
      }
      rerenderList();
      showUndoToast({
        message: MSG.SCRAP_UNDO_TOAST,
        actionLabel: MSG.DELETE_UNDO_ACTION,
        onUndo: () => {
          try {
            const cur = loadBriefings();
            const i = cur.findIndex((b) => b.id === briefingId);
            if (i >= 0 && !cur[i].scrapped) toggleScrap(i);
            rerenderList();
            showToast(MSG.SCRAP_UNDO_RESTORED);
          } catch {
            showToast(MSG.DELETE_UNDO_FAILED);
          }
        },
      });
      return;
    }
  }
  ```

  import 추가 (line 9): `import { loadBriefings, toggleScrap, saveBriefings } from '../../state/briefings';`

- [ ] **Step 5.6**: `rerenderList`의 scrap 카드에 ✕ 버튼 추가 (T4 변경 위에 누적)

  T4 step 4.3에서 만든 scrap 분기에 deleteBtn 삽입:

  ```typescript
  for (const b of scrapped) {
    const card = document.createElement('article');
    card.className = 'archive-card archive-card--scrap';
    card.dataset['briefingId'] = b.id;

    const date = document.createElement('div');
    date.className = 'archive-date';
    date.textContent = b.date;
    const title = document.createElement('div');
    title.className = 'archive-text';
    title.textContent = `⭐ ${b.title}`;
    const summary = document.createElement('div');
    summary.className = 'archive-summary';
    summary.textContent = b.summary;

    const deleteBtn = document.createElement('button');
    deleteBtn.className = 'archive-card-delete';
    deleteBtn.type = 'button';
    deleteBtn.setAttribute('aria-label', '스크랩 해제');
    deleteBtn.textContent = '×';

    card.append(date, title, summary, deleteBtn);
    list.append(card);
  }
  ```

- [ ] **Step 5.7**: `handleSelectToggle`에 필터 잠금 추가 (line 49-61 교체)

  ```typescript
  function handleSelectToggle(): void {
    selectMode = !selectMode;
    const toggle = document.getElementById('archiveSelectToggle');
    toggle?.setAttribute('aria-pressed', String(selectMode));
    document.getElementById('archiveTab')?.classList.toggle('archive--select-mode', selectMode);

    // 필터 chip 잠금 (T5 신설)
    document.querySelectorAll<HTMLButtonElement>('.filter-chip').forEach((chip) => {
      const isActive = chip.dataset['filter'] === currentFilter;
      if (isActive) {
        chip.disabled = false;
        chip.removeAttribute('aria-disabled');
      } else {
        chip.disabled = selectMode;
        if (selectMode) chip.setAttribute('aria-disabled', 'true');
        else chip.removeAttribute('aria-disabled');
      }
    });

    if (!selectMode) {
      selectedIds.clear();
      document.querySelectorAll('.archive-card.selected').forEach((c) =>
        c.classList.remove('selected')
      );
    }
    updateBulkButton();
  }
  ```

- [ ] **Step 5.8**: `handleCardClickInSelectMode`에 scrap 카드 분기 추가 (line 31-47 교체)

  ```typescript
  function handleCardClickInSelectMode(e: Event): void {
    const card = (e.target as HTMLElement).closest<HTMLElement>('.archive-card');
    if (!card) return;
    if ((e.target as HTMLElement).closest('.archive-card-delete')) return;
    e.stopPropagation();

    // 답변 vs scrap 분기 — selectedIds는 단일 Set, 필터 잠금으로 mixed 불가
    const id = card.dataset['answerId'] ?? card.dataset['briefingId'];
    if (!id) return;

    if (selectedIds.has(id)) {
      selectedIds.delete(id);
      card.classList.remove('selected');
    } else {
      selectedIds.add(id);
      card.classList.add('selected');
    }
    updateBulkButton();
  }
  ```

- [ ] **Step 5.9**: `handleBulkDeleteClick`에 scrap 분기 추가 (line 63-93 교체)

  ```typescript
  function handleBulkDeleteClick(): void {
    const n = selectedIds.size;
    if (n === 0) return;

    if (currentFilter === 'scrap') {
      if (!window.confirm(MSG.SCRAP_BULK_CONFIRM(n))) return;
      const ids = [...selectedIds];
      const briefings = loadBriefings();
      const snapshot = briefings.filter((b) => ids.includes(b.id) && b.scrapped).map((b) => b.id);
      try {
        // atomic single write — race-safe
        const next = briefings.map((b) =>
          ids.includes(b.id) ? { ...b, scrapped: false } : b
        );
        saveBriefings(next);
      } catch (err) {
        showToast(getSaveErrorMessage(err));
        return;
      }
      selectedIds.clear();
      selectMode = false;
      document.getElementById('archiveSelectToggle')?.setAttribute('aria-pressed', 'false');
      document.getElementById('archiveTab')?.classList.remove('archive--select-mode');
      // 필터 chip 복원
      document.querySelectorAll<HTMLButtonElement>('.filter-chip').forEach((chip) => {
        chip.disabled = false;
        chip.removeAttribute('aria-disabled');
      });
      rerenderList();
      updateBulkButton();
      showUndoToast({
        message: MSG.SCRAP_BULK_UNDO_TOAST(n),
        actionLabel: MSG.DELETE_UNDO_ACTION,
        onUndo: () => {
          try {
            const cur = loadBriefings();
            const restored = cur.map((b) => snapshot.includes(b.id) ? { ...b, scrapped: true } : b);
            saveBriefings(restored);
            rerenderList();
            showToast(MSG.SCRAP_UNDO_RESTORED);
          } catch {
            showToast(MSG.DELETE_UNDO_FAILED);
          }
        },
      });
      return;
    }

    // 기존 답변 벌크 흐름 (T2 atomic + RESTORED 적용 완료)
    if (!window.confirm(MSG.DELETE_CONFIRM_BULK(n))) return;
    const ids = [...selectedIds];
    const snapshot = loadAnswers().filter((a) => selectedIds.has(a.id));
    try {
      deleteAnswersByIds(ids);
    } catch (err) {
      showToast(getSaveErrorMessage(err));
      return;
    }
    selectedIds.clear();
    selectMode = false;
    document.getElementById('archiveSelectToggle')?.setAttribute('aria-pressed', 'false');
    document.getElementById('archiveTab')?.classList.remove('archive--select-mode');
    document.querySelectorAll<HTMLButtonElement>('.filter-chip').forEach((chip) => {
      chip.disabled = false;
      chip.removeAttribute('aria-disabled');
    });
    rerenderList();
    updateBulkButton();
    showUndoToast({
      message: MSG.DELETE_UNDO_TOAST,
      actionLabel: MSG.DELETE_UNDO_ACTION,
      onUndo: () => {
        try {
          saveAnswers([...loadAnswers(), ...snapshot]);
          rerenderList();
          showToast(MSG.DELETE_UNDO_RESTORED);
        } catch {
          showToast(MSG.DELETE_UNDO_FAILED);
        }
      },
    });
  }
  ```

- [ ] **Step 5.10**: 테스트 실행 — PASS 확인

  Run: `npm test -- archive-scrap-delete archive-scrap-bulk archive-select-filter-lock archive-delete`
  Expected: 모두 PASS, 기존 답변 카드 흐름 회귀 없음

- [ ] **Step 5.11**: 커밋

  ```bash
  git add src/ui/handlers/archive.ts tests/archive-scrap-delete.spec.ts tests/archive-scrap-bulk.spec.ts tests/archive-select-filter-lock.spec.ts
  git commit -m "feat(v3.9 T5): scrap card ×/select/bulk + filter lock policy"
  ```

---

## Task 6: P2-6 (storage throw 토스트 spy 검증) + P2-7 (5초 timeout 통합)

**Files:**
- Modify: `tests/archive-delete-target.spec.ts` (P2-6 + P2-7 it-block 추가)
- Modify: `tests/archive-delete-bulk.spec.ts` (동일)
- Modify: `tests/settings-delete-all.spec.ts` (동일)

### Why this task

v3.8 P2-6: target/bulk/settings 3 경로의 storage throw 시 `showToast(getSaveErrorMessage(err))` 호출 자체가 spy로 unverified. P2-7: 5초 후 Undo 버튼 사라짐이 toast-undo.spec.ts 단위 외에 통합 검증 누락. 본 task는 테스트 보강만, 프로덕션 코드 변경 없음.

### Steps

- [ ] **Step 6.1**: `tests/archive-delete-target.spec.ts`에 P2-6 it-block 추가

  ```typescript
  it('storage throw 시 showToast(getSaveErrorMessage(err)) 호출 (P2-6)', async () => {
    const { saveAnswers } = await import('../src/state/persistence');
    const { mountArchiveHandlers, hydrateArchive } = await import('../src/ui/handlers/archive');
    saveAnswers([{ id: 'a1', text: 't', createdAt: 1 } as any]);
    document.body.innerHTML = `<div id="modalRoot"></div><div id="archiveTab"><div id="archiveList"></div></div>`;
    mountArchiveHandlers();
    hydrateArchive();

    // setItem mock throw
    vi.spyOn(Storage.prototype, 'setItem').mockImplementation(() => {
      const err = new DOMException('quota', 'QuotaExceededError');
      throw err;
    });

    document.querySelector<HTMLButtonElement>('.archive-card-delete')!.click();

    // 카드 잔존 + 에러 토스트 표시 (DOM에 quota 메시지 포함)
    expect(document.querySelectorAll('.archive-card')).toHaveLength(1);
    const toastText = document.getElementById('toastContainer')?.textContent ?? '';
    expect(toastText.length).toBeGreaterThan(0);  // showToast 호출 확인
  });
  ```

- [ ] **Step 6.2**: 동일 패턴으로 `tests/archive-delete-bulk.spec.ts`에 P2-6 it-block 추가

  bulk 컨텍스트로 변경: 2 카드 setup → select → bulk delete → setItem throw → bulk button 컨테이너 잔존 + 토스트.

- [ ] **Step 6.3**: 동일 패턴으로 `tests/settings-delete-all.spec.ts`에 P2-6 it-block 추가

  settings 컨텍스트: deleteAll trigger → setItem throw → 답변 잔존 + 토스트.

- [ ] **Step 6.4**: `tests/archive-delete-target.spec.ts`에 P2-7 (5초 timeout 통합) it-block 추가

  ```typescript
  it('5초 후 Undo 버튼이 DOM에서 사라진다 (P2-7 통합)', async () => {
    vi.useFakeTimers();
    const { saveAnswers } = await import('../src/state/persistence');
    const { mountArchiveHandlers, hydrateArchive } = await import('../src/ui/handlers/archive');
    saveAnswers([{ id: 'a1', text: 't', createdAt: 1 } as any]);
    document.body.innerHTML = `<div id="modalRoot"></div><div id="archiveTab"><div id="archiveList"></div></div>`;
    mountArchiveHandlers();
    hydrateArchive();

    document.querySelector<HTMLButtonElement>('.archive-card-delete')!.click();
    expect(document.querySelector('.toast--undo')).toBeTruthy();

    vi.advanceTimersByTime(5000);
    // leaving 클래스 → 250ms 후 remove
    vi.advanceTimersByTime(300);
    expect(document.querySelector('.toast--undo')).toBeFalsy();

    vi.useRealTimers();
  });
  ```

- [ ] **Step 6.5**: `tests/archive-delete-bulk.spec.ts`에 P2-7 it-block 추가 (bulk 컨텍스트)

- [ ] **Step 6.6**: 테스트 실행 — 모두 PASS

  Run: `npm test -- archive-delete-target archive-delete-bulk settings-delete-all`
  Expected: 모든 신규 it-block PASS (T1/T2의 변경 위에 누적)

- [ ] **Step 6.7**: 커밋

  ```bash
  git add tests/archive-delete-target.spec.ts tests/archive-delete-bulk.spec.ts tests/settings-delete-all.spec.ts
  git commit -m "test(v3.9 T6): verify storage throw toast (P2-6) + 5s timeout integration (P2-7)"
  ```

---

## Task 7: Playwright smoke + N=5 안정성 + lint/tsc/build + 메모리 업데이트

**Files:**
- Create: `tests/smoke/archive-scrap-card.spec.ts`
- Modify: 메모리 파일 (작업 후 별도)

### Steps

- [ ] **Step 7.1**: `tests/smoke/archive-scrap-card.spec.ts` 생성

  ```typescript
  import { test, expect } from '@playwright/test';

  test('scrap 카드 ✕ → "스크랩 해제" 토스트 표시 (하단 중앙)', async ({ page }) => {
    await page.goto('/');
    // 사용자 셋업 + 스크랩 1개 fixture (기존 smoke 패턴 참조)
    await page.evaluate(() => {
      localStorage.setItem('briefings', JSON.stringify([
        { id: 'b1', date: '2026-04-27', url: 'https://example.com', title: 'T', summary: 'S', scrapped: true, read: false, memo: '' },
      ]));
    });
    await page.reload();
    await page.locator('[data-tab="archive"]').click();
    await page.locator('[data-filter="scrap"]').click();
    await expect(page.locator('.archive-card--scrap')).toBeVisible();

    await page.locator('.archive-card--scrap .archive-card-delete').click();
    await expect(page.locator('.toast--undo').filter({ hasText: '스크랩을 해제했어요' })).toBeVisible();

    // 토스트가 하단에 위치하는지 시각 확인
    const toast = page.locator('#toastContainer--undo .toast--undo');
    const box = await toast.boundingBox();
    const viewport = page.viewportSize();
    if (box && viewport) {
      expect(box.y).toBeGreaterThan(viewport.height / 2);  // 화면 하단 절반
    }
  });

  test('select 모드 진입 → 다른 chip cursor: not-allowed', async ({ page }) => {
    await page.goto('/');
    await page.locator('[data-tab="archive"]').click();
    await page.locator('#archiveSelectToggle').click();

    const otherChip = page.locator('.filter-chip[data-filter="분석"]');
    await expect(otherChip).toBeDisabled();
    await expect(otherChip).toHaveAttribute('aria-disabled', 'true');
  });
  ```

  주의: 기존 smoke 패턴 (auth/onboarding 우회, fixture 셋업 방식)을 `tests/smoke/archive-delete-target.spec.ts`에서 확인 후 일치시킨다.

- [ ] **Step 7.2**: Playwright 실행 — PASS 확인

  Run: `npm run test:smoke -- archive-scrap-card`
  Expected: 2 테스트 PASS

- [ ] **Step 7.3**: vitest 전체 + N=5 안정성 검증

  ```bash
  for i in 1 2 3 4 5; do
    npm test -- archive-scrap-delete archive-scrap-bulk archive-select-filter-lock toast-undo --reporter=basic > /tmp/v39-run-$i.log 2>&1
    grep -E "Tests" /tmp/v39-run-$i.log | tail -1
  done
  ```

  Expected: 5 runs 모두 PASS, 신규 spec flake 0건

- [ ] **Step 7.4**: lint + tsc + build

  ```bash
  npm run lint && npm run build
  ```

  Expected: 0 error 0 warn. CSS gzip ±0.5kB / JS gzip ±0.5kB.

- [ ] **Step 7.5**: 사후 동기화 — spec/plan의 ⭐ 이모지 결정(Step 3.4 변경) 사후 반영

  Spec §5.6에서 `SCRAP_UNDO_TOAST: '⭐ 스크랩을 해제했어요.'`로 명시했으나 messages.ts JSDoc 어조 규칙(이모지 3종 한정) 위반 발견 → 이모지 제거 결정. spec §5.6 4 키 모두 ⭐ 제거하여 동기화.

  ```bash
  # spec edit: SCRAP_*에서 ⭐ 접두 제거
  ```

- [ ] **Step 7.6**: 커밋

  ```bash
  git add tests/smoke/archive-scrap-card.spec.ts docs/superpowers/specs/2026-04-27-v3.9-toast-archive-unify-design.md
  git commit -m "test(v3.9 T7): smoke + spec sync for messages emoji policy"
  ```

- [ ] **Step 7.7**: 메모리 업데이트 (배포 후)

  배포 완료 후 별도 단계 — `~/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/`에 `project_daily_growth_v39_*.md` 작성, MEMORY.md 갱신.

---

## Verification Gates

| Gate | 기준 |
|---|---|
| **Vitest** | 384 → ~399~410 (+15~26 it-block, T1~T6 합산). 0 fail. |
| **Playwright** | 83 → ~85 (+2). 0 fail. |
| **N=5** | 신규 4 spec(scrap-delete/scrap-bulk/select-filter-lock/smoke) 5/5 PASS, flake 0 |
| **Lint / tsc** | 0 error 0 warn |
| **Build** | CSS gzip ±0.5 kB / JS gzip ±0.5 kB (DoD ±2 kB 충족) |
| **`dg-toast` grep** | 0 (CSS + JS + 테스트 전체) |
| **`DELETE_UNDO_RESTORED` 사용처** | ≥3 (target Undo + bulk Undo + deleteAll Undo) |
| **독립 표준 리뷰** | `superpowers:code-reviewer` (sonnet). APPROVE 목표. P0=0 / P1≤1 / P2 backlog 분류 |
| **Manual smoke** | 사용자 결정 (자동 검증 충분 시 생략 — v3.7~v3.8 패턴) |

## Self-Review

**Spec coverage check** (spec §4 Goals → task mapping):

- [x] Undo 토스트 명시적 스타일 (unstyled 0건) → T1
- [x] 하단 중앙 분리 → T1
- [x] DELETE_UNDO_RESTORED wiring → T2
- [x] atomic Undo 복원 → T2
- [x] scrap ✕ + select 모드 호환 → T5
- [x] select 진입 → 다른 chip aria-disabled → T5
- [x] scrap 카드 시각 일관 → T4 (data-briefing-id + delegation 통일)
- [x] storage throw spy 3 경로 → T6
- [x] 5초 timeout 통합 vitest → T6

**Spec §5.6 신규 messages → T3 ✓**
**Spec §6 Tasks 표 → T1~T7 일치 ✓ (T2/T3 순서 swap: P2-1/P2-2 먼저, scrap messages 다음. 의존성 OK)**
**Spec §7 Open Questions Q1~Q6 → 모두 plan에서 코드 탐색으로 해소 ✓**

**Type consistency check**:
- `Briefing.id` → `dataset['briefingId']` 일관 (T4/T5 모두) ✓
- `selectedIds: Set<string>` → 답변 ID + briefing ID 모두 string 일관 ✓
- `saveBriefings(list: Briefing[])` → state/briefings.ts:45 export 확인됨 ✓
- `getSaveErrorMessage(err)` → state/user.ts에 export, archive.ts:14 import 사용 중 ✓

**Placeholder scan**:
- T2 Step 2.1의 settings.ts deleteAll 위치는 grep으로 확인하는 단계. 정확한 라인 번호 없음 → 의도된 (engineer가 grep으로 식별). 코드 패턴은 명시.
- T7 Step 7.1 Playwright fixture는 "기존 smoke 패턴 참조" 명시 — 의도된 (smoke 환경마다 차이 있음).
- 그 외 모든 step에 코드 블록 + 명확한 expect/expected 명시 ✓

**No half-finished implementations**:
- `selectMode = false` 토글 시 `chip.disabled = false; removeAttribute('aria-disabled')` 양쪽 복원 ✓ (T5 step 5.7)
- bulk delete 후 selectMode 종료 + 필터 chip 복원도 동일 패턴 ✓ (T5 step 5.9)
- onUndo 콜백의 catch 분기 모두 `MSG.DELETE_UNDO_FAILED` 또는 `getSaveErrorMessage` ✓
