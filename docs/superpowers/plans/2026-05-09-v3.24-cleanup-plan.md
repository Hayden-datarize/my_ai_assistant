# DG v3.24 Cleanup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v3.23 carry-forward (P2 4건 + per-task minor 4건 + 메타 graduate 2건)을 한 사이클에 일괄 청산. AMBER bundle drift 완화 (-500~-800 B). 신규 기능 0, schema/CSP/migration 0줄.

**Architecture:** Pure cleanup + meta-docs graduate. byInterest semantics(T6)만 기능 변경 — `statsAggregate.ts` 'unknown' bucket 정밀화. 나머지는 maintainability refactor (DOM API / 함수 분리 / cross-module dep 정리) + test coverage 보강 + bundle trim. 8 task (T0~T7). 선례 v3.18 cleanup / v3.14.5 P2 cleanup.

**Tech Stack:** Vite + TypeScript (strict) + Vitest (jsdom) + Playwright. Firebase Hosting (origin remote 미등록 — main 직배포 패턴).

**Spec:** [`docs/superpowers/specs/2026-05-09-v3.24-cleanup-design.md`](../specs/2026-05-09-v3.24-cleanup-design.md)

---

## File Structure

| 파일 | 역할 | task |
|---|---|---|
| `CLAUDE.md` | "Codex 2-pass Review" 섹션 강화 (사전 review 8사이클 graduate 명문화) | T1 |
| `.claude/COMMON_MISTAKES.md` | §8 신설 — `serviceWorkers: 'block'` 표준 패턴 | T1 |
| `src/ui/modals/stats-range-modal.ts` | `escapeHtml + innerHTML` 혼합 → `textContent + DOM API` | T2 |
| `tests/unit/stats-range-modal-dom.spec.ts` | DOM 구조 검증 (XSS 표면 축소 확인) | T2 |
| `src/styles/main.css` 또는 `src/styles/components/*.css` | dead CSS rule 제거 | T3 |
| `src/services/prompts.ts` | inline trim (영문 부사 / 중복 phrase) | T3 |
| `src/**/*.ts` (dead import scan) | unused import 제거 | T3 |
| `src/ui/chat-bubble.ts` | `createChatPreviewBubble` (DOM only) + `bindChatPreviewBubbleHandlers` (events) 분리 | T4 |
| `tests/unit/chat-bubble-split.spec.ts` | 생성/wiring 분리 검증 | T4 |
| `src/state/user.ts:19-23` | `Insight.text` length / non-empty / max-cap entry guard 의무 명시 | T5 |
| `src/state/insights.ts` (신규 또는 user.ts에 helper 추가) | `validateInsightText(text)` factory guard | T5 |
| `tests/unit/insight-text-validation.spec.ts` | length 0 / 공백 only / max length cap spec | T5 |
| `tests/unit/mount-insights-handlers.spec.ts` | dispatch → 재렌더 jsdom spec | T5 |
| `src/utils/statsAggregate.ts:73-77` | byInterest 'unknown' bucket 정책 (빈 문자열/whitespace 처리, top 제외 또는 라벨링) | T6 |
| `src/ui/modals/stats-range-modal.ts:49-62` | `renderInterestBars` 'unknown' top 라벨링 정합 | T6 |
| `tests/unit/stats-aggregate-byinterest.spec.ts` | 분야 분리 / mixed / null/empty guard ≥3 spec (T6 자체 P0) | T6 |
| `src/utils/dates.ts` | `formatRelative` 신규 export | T7 |
| `src/ui/modals/plant-detail.ts:88-95` | `formatRelative` 제거 (dates.ts로 이전) | T7 |
| `src/ui/tabs/insights.ts:7` | import 경로 `../modals/plant-detail` → `../../utils/dates` | T7 |
| `tests/unit/dates-format-relative.spec.ts` | 이전 후 회귀 spec (음수/0/1/2일 경계) | T7 |
| `docs/superpowers/specs/2026-05-09-v3.24-retro.md` (또는 spec append) | retro + v3.25 carry seed | T7 |

---

## Pre-conditions (T0 commit 직전)

- main HEAD `9a91b9a` (v3.23 T11 retro)
- `npm install` 완료, `node_modules/` 존재
- v3.22 / v3.23 deploy 대기 상태 — T0 deploy 옵션 confirm 의무

---

## Task 0: Plan baseline + Codex 사전 review prep + deploy 옵션 confirm

**Files:**
- Create: `docs/superpowers/plans/2026-05-09-v3.24-cleanup-plan.md` (this file, 이미 존재 가정)
- Modify: 본 plan §Test Count Baseline 표 (Step 2 결과 기록)

- [ ] **Step 1: working tree clean 확인**

```bash
git status -s
```

Expected: untracked docs (`.claudeignore` / `docs/INDEX.md` / `docs/QUICK_REFERENCE.md` / `docs/archive/` / `docs/superpowers/plans/2026-05-01-v3.14.1-p2-cleanup-plan.md`)만. 다른 modified/staged 없음.

- [ ] **Step 2: vitest baseline 측정 (양쪽 TZ)**

```bash
TZ=Asia/Seoul npm test 2>&1 | grep -E "Tests|Test Files" | tail -5
TZ=America/New_York npm test 2>&1 | grep -E "Tests|Test Files" | tail -5
```

Expected: 양쪽 `1079 passed | 0 failed`.
본 plan 본문 §Baseline 표에 실측값 기록.

- [ ] **Step 3: bundle baseline 측정 (CLI gzip canonical)**

```bash
npm run build 2>&1 | tail -10
gzip -c dist/assets/index-*.js | wc -c
```

Expected: `40197` (±50 reproducibility tolerance). 실측값을 본 plan §Baseline 표에 기록.

- [ ] **Step 4: smoke baseline 측정 (cold-fork)**

```bash
rm -rf node_modules/.vite-temp && npm run test:smoke 2>&1 | tail -5
```

Expected: `96 passed`.

- [ ] **Step 5: deploy 옵션 사용자 confirm**

사용자에게 §7 deploy 옵션 (a)/(b)/(c) 제시 + 본 사이클 진입 시점에 결정.

옵션 (b)/(c) 선택 시:
```bash
# v3.22 / v3.23 우선 deploy (사용자 명시 승인 후)
npm run build && firebase deploy --only hosting
```

옵션 (a) 선택 시: T7까지 deploy 보류.

- [ ] **Step 6: Codex 사전 review dispatch (controller 영역, fallback 허용)**

```
codex:rescue invoke — input: "v3.24 cleanup spec + plan 사전 review.
- spec: docs/superpowers/specs/2026-05-09-v3.24-cleanup-design.md
- plan: docs/superpowers/plans/2026-05-09-v3.24-cleanup-plan.md
- 핵심 검증: T6 byInterest semantics edge case (interest=null/Answer.type=undefined/empty/whitespace/'unknown' bucket top 정책), T3 bundle trim production path 진입 위험, T2 stats-range-modal sparkline SVG escape 누락 가능성, T7 formatRelative 이전 시 음수 ms / DST 가드 회귀.
- output: P0/P1/P2 분류, in-cycle 반영 가능 여부."
```

CLI 한도 시 controller self-review (`pr-review-toolkit:code-reviewer`) fallback 허용. 결과를 본 plan §Codex 사전 review 표에 기록 + plan v2 in-cycle 반영.

- [ ] **Step 7: T0 commit (spec + plan + baseline + 사전 review 결과 흡수)**

```bash
git add docs/superpowers/specs/2026-05-09-v3.24-cleanup-design.md \
        docs/superpowers/plans/2026-05-09-v3.24-cleanup-plan.md
git commit -m "$(cat <<'EOF'
docs(v3.24 T0): spec + plan v1 + baseline + codex 사전 review

- spec: cleanup + 메타 graduate 8 task
- plan: T0~T7 break down
- baseline: vitest 1079 / bundle 40197 / smoke 96
- codex 사전 review: <P0/P1/P2 카운트>, in-cycle 반영

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 1: 메타 graduate (CLAUDE.md + COMMON_MISTAKES.md)

**Files:**
- Modify: `CLAUDE.md` (Codex 2-pass Review 섹션)
- Modify: `.claude/COMMON_MISTAKES.md` (§8 신설)

- [ ] **Step 1: CLAUDE.md "Codex 2-pass Review" 섹션 강화**

기존 섹션 (line ~71~91) 사후에 다음 단락 추가:

```markdown
### graduate 명문화 (v3.18~v3.23 7사이클 ROI)

사전 review가 v3.18~v3.23 7사이클 연속 P0 catch (chain superset / KST anchor / TZ sweep / silent corruption guard 등). v3.24 T1에서 graduate:

- **mandatory dispatch**: 각 사이클 T0 plan v1 작성 직후 (T2 진입 전 완료 의무).
- **P0/P1 plan 반영 전 구현 금지**: 사전 review 결과 P0가 있으면 plan v2 in-cycle 흡수, T2 진입 X.
- **CLI 한도 fallback**: controller self-review 허용 (v3.14.5 / v3.20 선례), 단 retro에 명시.
```

- [ ] **Step 2: COMMON_MISTAKES.md §8 신설**

기존 `## 7. 사이클-끝 lint check` 다음에 추가:

```markdown
---

## 8. Smoke + 외부 API mock 시 `serviceWorkers: 'block'` 의무 (2026-05-09, v3.23 T10 → v3.24 T1)

**Symptom**: smoke spec에서 `page.route('**/generativelanguage.googleapis.com/**', fulfill)`로 Gemini API mock을 등록했지만 실제 production API (400) 호출이 발생. mock이 bypass됨.

**Root cause**: 앱이 `public/sw.js` 서비스 워커를 등록하면 `page.route()`가 SW에 가로채이지 않은 요청만 intercept한다. SW가 fetch event를 가로채면 Playwright route handler 미적용. v3.23 stub-cleanup smoke (E2 인사이트 happy path) 디버깅에서 1시간 소진.

**Fix/Prevention**:

- 외부 API mock이 필요한 모든 smoke spec에 file-top `test.use({ serviceWorkers: 'block' })` 명시.
- 선례: `tests/smoke/card-translation.spec.ts` (v3.4)에서 동일 정책 graduate.
- v3.23 T10 (`tests/smoke/v3.23-stub-cleanup.spec.ts`)에서 graduate 확정.

```ts
import { test, expect } from '@playwright/test';

test.use({ serviceWorkers: 'block' });

test('Gemini mock 호출이 실제 API에 새지 않는다', async ({ page }) => {
  await page.route('**/generativelanguage.googleapis.com/**', route =>
    route.fulfill({ status: 200, body: JSON.stringify({ /* mock */ }) }),
  );
  // ...
});
```

**관련 파일**: `tests/smoke/*.spec.ts` (외부 API mock 사용 시 전부)
```

- [ ] **Step 3: 검증**

```bash
npx tsc --noEmit && npm run lint
```

Expected: 0 errors. (docs edit only이므로 영향 없음)

- [ ] **Step 4: T1 commit**

```bash
git add CLAUDE.md .claude/COMMON_MISTAKES.md
git commit -m "$(cat <<'EOF'
docs(v3.24 T1): 메타 graduate (사전 codex review 7사이클 + sw block 패턴)

- CLAUDE.md "Codex 2-pass Review" 섹션 graduate 명문화 (T0 mandatory dispatch + P0 plan 반영 의무)
- COMMON_MISTAKES.md §8 신설: serviceWorkers: 'block' 표준 패턴 (v3.23 T10 ROI)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: stats-range-modal DOM API 리팩토링 (A1 / P2-1)

**Files:**
- Modify: `src/ui/modals/stats-range-modal.ts`
- Test: `tests/unit/stats-range-modal-dom.spec.ts` (신규)

목표: `escapeHtml + innerHTML` 혼합 패턴을 `textContent + DOM API`로 마이그레이션. `bodyHtml` string 조합은 `openModal({ bodyHtml })` 인터페이스 제약상 유지하되, 내부 helper들을 DOM Node 반환으로 바꾸고 마지막에 outerHTML로 직렬화.

**대안**: `openModal`의 contract를 `bodyNode?: Node`도 허용하도록 확장하면 DOM Node를 그대로 전달 가능. 이 사이클에서는 contract 변경을 비목표 (§5)로 두므로, helper만 DOM 반환 + outerHTML로 string 변환.

- [ ] **Step 1: 신규 spec 작성 (failing test)**

`tests/unit/stats-range-modal-dom.spec.ts` 생성:

```ts
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { openStatsRangeModal } from '../../src/ui/modals/stats-range-modal';

vi.mock('../../src/utils/statsAggregate', () => ({
  getStatsRange: () => ({
    totalAnswers: 5,
    longestStreak: 3,
    activeInterests: 2,
    avgPerDay: 0.7,
    byInterest: [
      { id: '<img src=x onerror=alert(1)>', count: 3 },
      { id: 'AI', count: 2 },
    ],
    daily: undefined,
  }),
}));
vi.mock('../../src/utils/statsCache', () => ({
  getStatsCache: () => ({ highlight: 'cached' }),
  setStatsCache: () => {},
}));
vi.mock('../../src/services/gemini', () => ({ generateText: vi.fn() }));
vi.mock('../../src/state/geminiUsage', () => ({ checkAndIncrementGemini: () => true }));
vi.mock('../../src/utils/apiKey', () => ({ getApiKey: () => 'k' }));

describe('stats-range-modal DOM 안전성', () => {
  beforeEach(() => {
    document.body.innerHTML = '<div id="modalRoot"></div>';
  });

  it('byInterest id에 HTML injection 시도해도 escape 처리된다', async () => {
    await openStatsRangeModal({ range: 7 });
    const labels = document.querySelectorAll('.bar-label');
    expect(labels[0].textContent).toBe('<img src=x onerror=alert(1)>');
    // HTML로 해석되지 않음 — img 태그 0개
    expect(document.querySelectorAll('.dg-modal img').length).toBe(0);
  });

  it('stats-stat-card는 4개 (답변/streak/활성분야/일평균)', async () => {
    await openStatsRangeModal({ range: 7 });
    expect(document.querySelectorAll('.stats-stat-card').length).toBe(4);
  });
});
```

- [ ] **Step 2: spec 실행 — fail 확인**

```bash
npx vitest run tests/unit/stats-range-modal-dom.spec.ts 2>&1 | tail -20
```

Expected: 첫 it는 PASS (escapeHtml로 이미 안전), 둘째 it도 PASS (구조 검증). 즉 현재 코드도 통과 → spec은 회귀 안전망.

만약 fail이면 mock 누락 — `openModal` 의존성 (`focus-trap` 등) 추가 mock 필요.

- [ ] **Step 3: 코드 리팩토링 — `renderStatCards` DOM 반환**

`src/ui/modals/stats-range-modal.ts`:

```ts
function buildStatCards(r: StatsRange): HTMLElement {
  const grid = document.createElement('div');
  grid.className = 'stats-stat-grid';
  const cards: Array<[string, string]> = [
    ['답변', String(r.totalAnswers)],
    ['최장 streak', `${r.longestStreak}일`],
    ['활성 분야', String(r.activeInterests)],
    ['일 평균', String(r.avgPerDay)],
  ];
  for (const [label, value] of cards) {
    const card = document.createElement('div');
    card.className = 'stats-stat-card';
    const labelSpan = document.createElement('span');
    labelSpan.className = 'stat-label';
    labelSpan.textContent = label;
    const valueSpan = document.createElement('span');
    valueSpan.className = 'stat-value';
    valueSpan.textContent = value;
    card.appendChild(labelSpan);
    card.appendChild(valueSpan);
    grid.appendChild(card);
  }
  return grid;
}
```

기존 `renderStatCards` (string 반환) 삭제.

- [ ] **Step 4: `buildSparkline` / `buildInterestBars` DOM 반환으로 마이그레이션**

```ts
function buildSparkline(daily: number[]): HTMLElement {
  const max = Math.max(...daily, 1);
  const container = document.createElement('div');
  container.className = 'stats-sparkline';
  container.setAttribute('aria-hidden', 'true');
  for (const v of daily) {
    const bar = document.createElement('div');
    bar.className = 'stats-sparkline-bar';
    bar.style.height = `${Math.round((v / max) * 100)}%`;
    container.appendChild(bar);
  }
  return container;
}

function buildInterestBars(byInterest: StatsRange['byInterest']): HTMLElement | null {
  const first = byInterest[0];
  if (!first) return null;
  const max = first.count;
  const ul = document.createElement('ul');
  ul.className = 'stats-interest-bars';
  for (const i of byInterest) {
    const li = document.createElement('li');
    const label = document.createElement('span');
    label.className = 'bar-label';
    label.textContent = i.id;
    const track = document.createElement('span');
    track.className = 'bar-track';
    const fill = document.createElement('span');
    fill.className = 'bar-fill';
    fill.style.width = `${Math.round((i.count / max) * 100)}%`;
    track.appendChild(fill);
    const count = document.createElement('span');
    count.className = 'bar-count';
    count.textContent = String(i.count);
    li.appendChild(label);
    li.appendChild(track);
    li.appendChild(count);
    ul.appendChild(li);
  }
  return ul;
}
```

기존 `renderSparkline` / `renderInterestBars` 삭제.

- [ ] **Step 5: `openStatsRangeModal` body 조립을 DOM-first로 전환**

```ts
export async function openStatsRangeModal(opts: StatsRangeModalOpts): Promise<void> {
  // ... (기존 cached/apiKey/quota/gemini 로직 그대로) ...

  const bodyContainer = document.createElement('div');
  bodyContainer.appendChild(buildStatCards(r));
  if (opts.range === 30 && r.daily) {
    bodyContainer.appendChild(buildSparkline(r.daily));
  }
  const bars = buildInterestBars(r.byInterest);
  if (bars) bodyContainer.appendChild(bars);
  const highlight = document.createElement('p');
  highlight.className = 'stats-highlight';
  highlight.textContent = highlightText;  // text는 Gemini raw 또는 deterministic — escape 무관
  bodyContainer.appendChild(highlight);

  const title = opts.range === 7 ? '지난 7일' : '지난 30일';
  openModal({ title, bodyHtml: bodyContainer.innerHTML });
}
```

`highlightText`도 escape 제거 가능 (textContent로 자동 안전). 단 deterministic 문자열은 escape 호출 자체가 dead code — 별도 step에서 cleanup.

- [ ] **Step 6: `escapeHtml` import 사용 확인**

기존에 `escapeHtml(out.trim())` / `escapeHtml(top.id)` / `escapeHtml(i.id)` 패턴 사용. DOM API로 마이그 후 모두 dead — `escapeHtml` import 자체 제거.

```ts
// import { escapeHtml } from '../../utils/escapeHtml';  // 삭제
```

`deterministicHighlight`도 escape 제거:
```ts
function deterministicHighlight(r: StatsRange, days: 7 | 30): string {
  const top = r.byInterest[0];
  const topPart = top ? `, ${top.id} 강세` : '';
  const prefix = days === 7 ? '이번 주' : '지난 30일';
  return `${prefix} ${r.totalAnswers}개 답변, 최장 ${r.longestStreak}일${topPart}`;
}
```

- [ ] **Step 7: 검증**

```bash
npx tsc --noEmit && npm run lint
TZ=Asia/Seoul npx vitest run tests/unit/stats-range-modal-dom.spec.ts 2>&1 | tail -10
TZ=Asia/Seoul npx vitest run --reporter=verbose 2>&1 | grep -E "Tests|Test Files" | tail -3
```

Expected: 0 errors / 신규 spec 2 pass / 회귀 0.

- [ ] **Step 8: T2 commit**

```bash
git add src/ui/modals/stats-range-modal.ts tests/unit/stats-range-modal-dom.spec.ts
git commit -m "$(cat <<'EOF'
refactor(v3.24 T2 / A1): stats-range-modal DOM API 마이그레이션

- escapeHtml + innerHTML 혼합 → textContent + DOM API
- renderStatCards/Sparkline/InterestBars → buildStatCards/Sparkline/InterestBars (DOM Node 반환)
- escapeHtml import 제거 (dead code)
- 신규 spec: XSS injection 시도 escape 검증 + stat-card 4개 구조 검증

v3.23 P2-1 carry-forward.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: Bundle trim -500~-800 B (A2 / P2-2)

**Files:**
- Modify: `src/services/prompts.ts` (inline trim)
- Modify: `src/styles/main.css` 또는 `src/styles/components/*.css` (dead CSS rule 제거)
- Modify: `src/**/*.ts` (dead import — grep 기반)

목표: bundle CLI gzip 40197 → 39397~39697 (Δ -500~-800 B). Production code path 진입 금지 — 미사용 CSS / dead import / prompts inline 명사형 trim만.

- [ ] **Step 1: prompts.ts inline 검토**

```bash
cat src/services/prompts.ts | wc -l
```

prompts.ts의 4종 prompt template (statsHighlight 60 / statsNarrative 200 / conversationSummary 400 / insight 80)에서 영문 부사 / 중복 phrase / 군더더기 한국어 trim. 의미 변경 X, 토큰 cap만 유지.

예: `"...상세하게 친절하게..."` → `"...친절하게..."`. plan 실행 시 실제 파일 읽고 case-by-case 적용.

- [ ] **Step 2: 빌드 후 stat-only 측정**

```bash
npm run build && gzip -c dist/assets/index-*.js | wc -c
```

prompts trim 후 측정. 보통 -50~-150 B 수준.

- [ ] **Step 3: dead CSS rule scan**

```bash
# 컴파일된 CSS classnames과 src/ 사용 className 비교
grep -hr "className\|class=" src/ | grep -oE 'class[="]+[a-z-]+[" ]' | sort -u > /tmp/used.txt
# 그리고 main.css의 .xxx selector 추출 후 diff
grep -oE '\.[a-z][a-z0-9-]+' src/styles/main.css | sort -u > /tmp/defined.txt
diff /tmp/defined.txt /tmp/used.txt | grep "^<" | head -20
```

찾은 것 중 안전 제거 가능한 rule만 (선택자 충돌 / 외부 의존 X) 삭제. 1건당 평균 30~80 B 절감.

대안: vite build의 `rollupOptions.output.manualChunks` 살펴서 unused css 자동 제거되는지 확인.

- [ ] **Step 4: dead import scan**

```bash
npx eslint --rule '"unused-imports/no-unused-imports": "error"' src/ 2>&1 | head -30
```

또는 `@typescript-eslint/no-unused-vars` 위반 unused import 제거.

- [ ] **Step 5: 누적 측정 + DoD 확인**

```bash
npm run build && gzip -c dist/assets/index-*.js | wc -c
```

Expected: 39397 ≤ B ≤ 39697 (40197 - {500..800}).

DoD 미달 시 추가 trim source 탐색 — 큰 dependency dynamic import 분리 / 중복 helper 통합 등. **단 production code path semantic 변경 금지.**

- [ ] **Step 6: 회귀 검증**

```bash
npx tsc --noEmit && npm run lint
TZ=Asia/Seoul npx vitest run --reporter=verbose 2>&1 | grep -E "Tests|Test Files" | tail -3
TZ=America/New_York npx vitest run --reporter=verbose 2>&1 | grep -E "Tests|Test Files" | tail -3
rm -rf node_modules/.vite-temp && npm run test:smoke 2>&1 | tail -5
```

Expected: 0 errors / 양쪽 TZ baseline+T2 카운트 / smoke 96+.

- [ ] **Step 7: T3 commit**

```bash
git add -- src/services/prompts.ts src/styles/ src/**/*.ts
git commit -m "$(cat <<'EOF'
perf(v3.24 T3 / A2): bundle trim <-XXX> B (40197 → <Y>)

- prompts.ts inline trim (영문 부사 / 중복 phrase 정리)
- dead CSS rule 제거 (<N>건)
- unused import 정리 (<N>건)

DoD: bundle CLI gzip 39397~39697 (-500~-800 B)
v3.23 P2-2 AMBER drift 완화.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: chat-bubble 역할 분리 (A3 / P2-3)

**Files:**
- Modify: `src/ui/chat-bubble.ts`
- Test: `tests/unit/chat-bubble-split.spec.ts` (신규)

목표: `renderChatPreviewBubble`을 `createChatPreviewBubble` (DOM 생성, 순수) + `bindChatPreviewBubbleHandlers` (이벤트 wiring + auto-remove)로 분리. 기존 export `renderChatPreviewBubble`은 thin wrapper로 보존 (caller 호환성).

- [ ] **Step 1: 신규 spec 작성 (failing test)**

`tests/unit/chat-bubble-split.spec.ts`:

```ts
import { describe, it, expect, beforeEach, vi } from 'vitest';
import {
  createChatPreviewBubble,
  bindChatPreviewBubbleHandlers,
  renderChatPreviewBubble,
} from '../../src/ui/chat-bubble';

beforeEach(() => {
  document.body.innerHTML = '<div id="chatMessages"></div>';
});

describe('createChatPreviewBubble', () => {
  it('DOM Node 생성만 — 이벤트 등록 X', () => {
    const { bubble, primaryBtn, secondaryBtn } = createChatPreviewBubble({
      text: 'hello',
      primaryLabel: '저장',
      secondaryLabel: '취소',
    });
    expect(bubble.classList.contains('chat-preview-bubble')).toBe(true);
    expect(primaryBtn.textContent).toBe('저장');
    expect(secondaryBtn.textContent).toBe('취소');
    // 이벤트가 없으니 click해도 콜백 없음 — 버블 자동 제거 X
    primaryBtn.click();
    expect(bubble.isConnected).toBe(false);  // 아직 DOM 추가 전
  });

  it('text는 textContent로 안전 (XSS escape)', () => {
    const { bubble } = createChatPreviewBubble({
      text: '<img src=x onerror=alert(1)>',
      primaryLabel: 'A',
      secondaryLabel: 'B',
    });
    const textDiv = bubble.querySelector('.chat-preview-text');
    expect(textDiv?.textContent).toBe('<img src=x onerror=alert(1)>');
    expect(bubble.querySelectorAll('img').length).toBe(0);
  });
});

describe('bindChatPreviewBubbleHandlers', () => {
  it('primary click → onPrimary 호출 + bubble 제거', () => {
    const onPrimary = vi.fn();
    const onSecondary = vi.fn();
    const parts = createChatPreviewBubble({
      text: 't',
      primaryLabel: 'P',
      secondaryLabel: 'S',
    });
    document.getElementById('chatMessages')!.appendChild(parts.bubble);
    bindChatPreviewBubbleHandlers(parts, { onPrimary, onSecondary });
    parts.primaryBtn.click();
    expect(onPrimary).toHaveBeenCalledOnce();
    expect(onSecondary).not.toHaveBeenCalled();
    expect(parts.bubble.isConnected).toBe(false);
  });
});

describe('renderChatPreviewBubble (legacy wrapper)', () => {
  it('기존 호출 형태 그대로 동작 (DOM 추가 + 이벤트 wiring)', () => {
    const onPrimary = vi.fn();
    const bubble = renderChatPreviewBubble({
      text: 't',
      primaryLabel: 'P',
      secondaryLabel: 'S',
      onPrimary,
      onSecondary: () => {},
    });
    expect(bubble.isConnected).toBe(true);
    bubble.querySelector<HTMLButtonElement>('.preview-primary')!.click();
    expect(onPrimary).toHaveBeenCalledOnce();
  });
});
```

- [ ] **Step 2: spec 실행 — fail 확인**

```bash
npx vitest run tests/unit/chat-bubble-split.spec.ts 2>&1 | tail -10
```

Expected: FAIL (`createChatPreviewBubble` / `bindChatPreviewBubbleHandlers` 미정의).

- [ ] **Step 3: chat-bubble.ts 분리**

```ts
import { escapeHtml } from '../utils/escapeHtml';

export interface ChatPreviewBubbleOpts {
  text: string;
  primaryLabel: string;
  secondaryLabel: string;
  onPrimary: () => void;
  onSecondary: () => void;
}

export interface ChatPreviewBubbleParts {
  bubble: HTMLDivElement;
  primaryBtn: HTMLButtonElement;
  secondaryBtn: HTMLButtonElement;
}

/**
 * DOM Node만 생성 — 이벤트 wiring 없음. 호출자가 DOM 추가 + bindChatPreviewBubbleHandlers 의무.
 */
export function createChatPreviewBubble(opts: {
  text: string;
  primaryLabel: string;
  secondaryLabel: string;
}): ChatPreviewBubbleParts {
  const bubble = document.createElement('div');
  bubble.className = 'chat-message chat-message-ai chat-preview-bubble';

  const textDiv = document.createElement('div');
  textDiv.className = 'chat-preview-text';
  textDiv.textContent = opts.text;

  const actionsDiv = document.createElement('div');
  actionsDiv.className = 'chat-preview-actions';

  const primaryBtn = document.createElement('button');
  primaryBtn.type = 'button';
  primaryBtn.className = 'btn btn-primary preview-primary';
  primaryBtn.textContent = opts.primaryLabel;

  const secondaryBtn = document.createElement('button');
  secondaryBtn.type = 'button';
  secondaryBtn.className = 'btn btn-secondary preview-secondary';
  secondaryBtn.textContent = opts.secondaryLabel;

  actionsDiv.appendChild(primaryBtn);
  actionsDiv.appendChild(secondaryBtn);
  bubble.appendChild(textDiv);
  bubble.appendChild(actionsDiv);

  return { bubble, primaryBtn, secondaryBtn };
}

/**
 * 이벤트 wiring + auto-remove. createChatPreviewBubble로 만든 parts에 적용.
 */
export function bindChatPreviewBubbleHandlers(
  parts: ChatPreviewBubbleParts,
  handlers: { onPrimary: () => void; onSecondary: () => void },
): void {
  parts.primaryBtn.addEventListener('click', () => {
    handlers.onPrimary();
    parts.bubble.remove();
  });
  parts.secondaryBtn.addEventListener('click', () => {
    handlers.onSecondary();
    parts.bubble.remove();
  });
}

/**
 * Legacy wrapper — DOM 생성 + chatMessages 컨테이너에 추가 + 이벤트 wiring + scrollIntoView.
 * v3.24 T4 분리 후 보존.
 */
export function renderChatPreviewBubble(opts: ChatPreviewBubbleOpts): HTMLDivElement {
  const container = document.getElementById('chatMessages');
  if (!container) throw new Error('chatMessages container not found');

  const parts = createChatPreviewBubble(opts);
  bindChatPreviewBubbleHandlers(parts, opts);

  container.appendChild(parts.bubble);
  parts.bubble.scrollIntoView?.({ behavior: 'smooth', block: 'end' });

  return parts.bubble;
}

export { escapeHtml };
```

- [ ] **Step 4: spec PASS 확인**

```bash
npx vitest run tests/unit/chat-bubble-split.spec.ts 2>&1 | tail -10
```

Expected: 4 pass.

- [ ] **Step 5: 회귀 + 검증**

```bash
npx tsc --noEmit && npm run lint
TZ=Asia/Seoul npx vitest run --reporter=verbose 2>&1 | grep -E "Tests|Test Files" | tail -3
```

Expected: 0 errors / vitest baseline+T2+T4 (=baseline+~5).

- [ ] **Step 6: T4 commit**

```bash
git add src/ui/chat-bubble.ts tests/unit/chat-bubble-split.spec.ts
git commit -m "$(cat <<'EOF'
refactor(v3.24 T4 / A3): chat-bubble 역할 분리 (DOM / events)

- createChatPreviewBubble: DOM Node 순수 생성
- bindChatPreviewBubbleHandlers: 이벤트 wiring + auto-remove
- renderChatPreviewBubble: legacy thin wrapper (caller 호환성 보존)

신규 spec 4 pass (분리/XSS/wiring/legacy wrapper).
v3.23 P2-3 carry-forward.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: Insight.text entry guard + mountInsightsHandlers spec (B1 + B4)

**Files:**
- Modify: `src/state/user.ts:19-23` (Insight invariant 주석 제거 + factory 명시)
- Modify: `src/state/user.ts` (또는 신규 `src/state/insights.ts`) — `validateInsightText(text): string` helper
- Test: `tests/unit/insight-text-validation.spec.ts` (신규, length 0/공백/max-cap)
- Test: `tests/unit/mount-insights-handlers.spec.ts` (신규, dispatch 재렌더)

목표: `Insight.text` entry-level guard + insights 탭 refresh path test coverage.

- [ ] **Step 1: validateInsightText helper 신규 spec (failing)**

`tests/unit/insight-text-validation.spec.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { validateInsightText } from '../../src/state/user';

describe('validateInsightText', () => {
  it('non-empty 정상 텍스트는 그대로 반환', () => {
    expect(validateInsightText('  통찰 한 줄  ')).toBe('통찰 한 줄');
  });

  it('빈 문자열은 throw', () => {
    expect(() => validateInsightText('')).toThrow(/empty|빈/);
  });

  it('공백 only는 throw', () => {
    expect(() => validateInsightText('   \t\n  ')).toThrow(/empty|빈/);
  });

  it('200자 초과는 trim (max cap)', () => {
    const longText = 'a'.repeat(201);
    const result = validateInsightText(longText);
    expect(result.length).toBe(200);
  });

  it('200자 정확은 그대로', () => {
    const exact = 'b'.repeat(200);
    expect(validateInsightText(exact)).toBe(exact);
  });
});
```

- [ ] **Step 2: spec fail 확인**

```bash
npx vitest run tests/unit/insight-text-validation.spec.ts 2>&1 | tail -10
```

Expected: FAIL (`validateInsightText` 미정의).

- [ ] **Step 3: validateInsightText helper 구현**

`src/state/user.ts`에 추가:

```ts
/**
 * v3.24 T5 (B1): Insight.text entry-level guard.
 * - trim 적용
 * - 빈/공백-only 입력은 throw (caller 책임 — Gemini 빈 응답 등)
 * - 200자 max cap (storage bloat + UI overflow 차단)
 *
 * @throws Error 빈 / 공백-only 입력
 */
export function validateInsightText(text: string): string {
  const trimmed = text.trim();
  if (trimmed.length === 0) throw new Error('Insight text empty (빈 또는 공백 only)');
  return trimmed.slice(0, 200);
}
```

기존 `Insight` interface 주석은 다음으로 업데이트:

```ts
/**
 * v3.23 NEW: Gemini 기반 통찰 항목.
 *
 * @invariant entry guard via `validateInsightText` (v3.24 T5).
 * - `text`: caller가 `validateInsightText`로 trim + max-200 cap 적용 의무. UI render 시점에 escapeHtml.
 * - storage layer는 entry-level shape 검증 없음 — `validateInsightText` 호출 책임은 caller.
 */
```

- [ ] **Step 4: caller 마이그레이션 (handlers/home.ts)**

`src/ui/handlers/home.ts`에서 `user.insights.push({ id, text: rawText, createdAt })` 형태 호출처 찾기:

```bash
grep -n "insights.push\|insights = \[" src/ui/handlers/home.ts
```

찾은 위치에서 `text: rawText` → `text: validateInsightText(rawText)` 변경. import 추가 시 `validateInsightText` from `../../state/user`.

handler 단에서 throw 발생 시 toast로 사용자 알림 (Gemini 빈 응답 시). 기존 try/catch 블록 재활용.

- [ ] **Step 5: spec PASS 확인**

```bash
npx vitest run tests/unit/insight-text-validation.spec.ts 2>&1 | tail -10
```

Expected: 5 pass.

- [ ] **Step 6: mountInsightsHandlers spec 작성 (failing)**

`tests/unit/mount-insights-handlers.spec.ts`:

```ts
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { mountInsightsHandlers, renderInsights } from '../../src/ui/tabs/insights';
import { dispatch } from '../../src/ui/events';

vi.mock('../../src/state/user', () => ({
  getCachedUser: () => ({
    insights: [
      { id: 'a', text: 'first', createdAt: '2026-05-01T00:00:00Z' },
      { id: 'b', text: 'second', createdAt: '2026-05-02T00:00:00Z' },
    ],
  }),
}));
vi.mock('../../src/ui/modals/insight-detail', () => ({ openInsightDetailModal: vi.fn() }));

describe('mountInsightsHandlers refresh path', () => {
  beforeEach(() => {
    document.body.innerHTML = '<div id="tabContainer"></div>';
  });

  it('dg:insights:added dispatch 시 grid 재렌더', () => {
    const tab = document.getElementById('tabContainer')!;
    renderInsights(tab);
    expect(tab.querySelectorAll('.insight-card').length).toBe(2);
    mountInsightsHandlers();
    // 재렌더 — 카운트 동일하지만 mock이 같으니 그대로
    dispatch('dg:insights:added', { id: 'c' });
    expect(tab.querySelectorAll('.insight-card').length).toBe(2);
  });

  it('insightsTab DOM 없을 때 (탭 비활성) refresh no-op', () => {
    mountInsightsHandlers();
    // insightsTab 미존재 → refresh 호출되어도 아무 일 없음
    expect(() => dispatch('dg:insights:removed', { id: 'a' })).not.toThrow();
  });

  it('dg:insights:removed dispatch도 동일한 refresh 트리거', () => {
    const tab = document.getElementById('tabContainer')!;
    renderInsights(tab);
    mountInsightsHandlers();
    expect(() => dispatch('dg:insights:removed', { id: 'a' })).not.toThrow();
    expect(tab.querySelectorAll('.insight-card').length).toBe(2);
  });
});
```

- [ ] **Step 7: spec 실행**

```bash
npx vitest run tests/unit/mount-insights-handlers.spec.ts 2>&1 | tail -10
```

Expected: 3 pass (production code 변경 없음 — 단순 spec 보강).

- [ ] **Step 8: 회귀 + 검증**

```bash
npx tsc --noEmit && npm run lint
TZ=Asia/Seoul npx vitest run --reporter=verbose 2>&1 | grep -E "Tests|Test Files" | tail -3
```

Expected: 0 errors / baseline + 8 (T2) + 4 (T4) + 5 (T5-B1) + 3 (T5-B4) = +20 over T0 baseline.

- [ ] **Step 9: T5 commit**

```bash
git add src/state/user.ts src/ui/handlers/home.ts \
        tests/unit/insight-text-validation.spec.ts \
        tests/unit/mount-insights-handlers.spec.ts
git commit -m "$(cat <<'EOF'
test(v3.24 T5 / B1+B4): Insight.text entry guard + mountInsightsHandlers spec

- validateInsightText: trim + non-empty + max-200 cap (caller 의무 graduate)
- handlers/home.ts: validateInsightText 호출로 마이그레이션
- 신규 spec 8건 (validation 5 + refresh path 3)

v3.23 T1 reviewer P2 + T9 reviewer P2 carry-forward.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: byInterest semantics 정밀화 (B2) — **자체 P0 게이트**

**Files:**
- Modify: `src/utils/statsAggregate.ts:73-77` (분야 분리 정책)
- Modify: `src/ui/modals/stats-range-modal.ts:49-62` 또는 buildInterestBars (T2 결과) (라벨링)
- Test: `tests/unit/stats-aggregate-byinterest.spec.ts` (≥3 spec, T6 main work 차단 게이트)

목표: `Answer.type` 활용 정밀화. `??` 만으로는 빈 문자열 / whitespace를 'unknown' bucket에 안 넣음 (string 'unknown'에 들어감 X) — guard 강화. 'unknown' bucket이 byInterest top일 때 표시 정책.

### 정책 결정 (사전 codex review에서 P0/P1 catch 가능)

- **A. 'unknown' 정의**: `a.type` 가 `null` / `undefined` / `''` / whitespace-only → 'unknown' bucket.
- **B. 'unknown' bucket 처리**: byInterest 결과에서 **제외** (top 5 sorting 전 filter). activeInterests count에도 미포함. → `unknown` 답변은 분야 통계에 영향 X.
  - 대안: 포함하되 `id: '미분류'`로 라벨 변경. **A안 (제외) 선택** — '미분류' 라벨이 top일 때 사용자에게 의미 모호하고 chip/필터에서도 빠지므로 일관성.
- **C. byInterest 비어있을 때 (모든 답변 unknown)**: `buildInterestBars`가 null 반환 (이미 처리됨, T2 결과).

- [ ] **Step 1: stats-aggregate-byinterest.spec.ts 작성 (failing, ≥3 P0 spec)**

`tests/unit/stats-aggregate-byinterest.spec.ts`:

```ts
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { getStatsRange } from '../../src/utils/statsAggregate';
import type { Answer } from '../../src/state/schema';

const mkAnswer = (overrides: Partial<Answer>): Answer => ({
  id: overrides.id ?? 'a',
  questionId: 'q',
  text: 't',
  authorId: 'self',
  schemaVersion: 1,
  createdAt: overrides.createdAt ?? '2026-05-08T09:00:00Z',  // KST 18:00
  ...overrides,
});

vi.mock('../../src/state/persistence', () => ({
  loadAnswers: vi.fn(),
}));
import { loadAnswers } from '../../src/state/persistence';

beforeEach(() => {
  vi.useFakeTimers();
  vi.setSystemTime(new Date('2026-05-08T15:00:00Z'));  // KST 2026-05-09 00:00
});

describe('byInterest semantics (v3.24 T6)', () => {
  it('Answer.type 정상 분야는 분리 집계', () => {
    (loadAnswers as ReturnType<typeof vi.fn>).mockReturnValue([
      mkAnswer({ id: '1', type: 'AI' }),
      mkAnswer({ id: '2', type: 'AI' }),
      mkAnswer({ id: '3', type: '디자인' }),
    ]);
    const r = getStatsRange(7);
    expect(r.byInterest).toEqual([
      { id: 'AI', count: 2 },
      { id: '디자인', count: 1 },
    ]);
    expect(r.activeInterests).toBe(2);
  });

  it("type=undefined / null / '' / whitespace는 'unknown' bucket으로 통합 후 제외", () => {
    (loadAnswers as ReturnType<typeof vi.fn>).mockReturnValue([
      mkAnswer({ id: '1', type: 'AI' }),
      mkAnswer({ id: '2', type: undefined }),
      mkAnswer({ id: '3', type: '' }),
      mkAnswer({ id: '4', type: '   ' }),
      mkAnswer({ id: '5', type: '\t\n' }),
    ]);
    const r = getStatsRange(7);
    expect(r.byInterest.find(b => b.id === 'unknown')).toBeUndefined();
    expect(r.byInterest).toEqual([{ id: 'AI', count: 1 }]);
    expect(r.activeInterests).toBe(1);
    expect(r.totalAnswers).toBe(5);  // totalAnswers는 unknown 포함
  });

  it('mixed 분야 + unknown 합쳐도 byInterest는 known만, totalAnswers/avgPerDay는 전체', () => {
    (loadAnswers as ReturnType<typeof vi.fn>).mockReturnValue([
      mkAnswer({ id: '1', type: 'AI' }),
      mkAnswer({ id: '2', type: 'AI' }),
      mkAnswer({ id: '3', type: undefined }),
      mkAnswer({ id: '4', type: '디자인' }),
    ]);
    const r = getStatsRange(7);
    expect(r.totalAnswers).toBe(4);
    expect(r.activeInterests).toBe(2);  // AI + 디자인
    expect(r.byInterest).toEqual([
      { id: 'AI', count: 2 },
      { id: '디자인', count: 1 },
    ]);
  });

  it('전체 unknown 시 byInterest 빈 배열, activeInterests=0', () => {
    (loadAnswers as ReturnType<typeof vi.fn>).mockReturnValue([
      mkAnswer({ id: '1', type: undefined }),
      mkAnswer({ id: '2', type: '' }),
    ]);
    const r = getStatsRange(7);
    expect(r.byInterest).toEqual([]);
    expect(r.activeInterests).toBe(0);
    expect(r.totalAnswers).toBe(2);
  });
});
```

- [ ] **Step 2: spec fail 확인**

```bash
TZ=Asia/Seoul npx vitest run tests/unit/stats-aggregate-byinterest.spec.ts 2>&1 | tail -20
```

Expected: 첫 it는 PASS (이미 정상 동작), 둘째~넷째 it는 FAIL — 현재 코드는 'unknown' bucket을 byInterest에 포함하고 빈 문자열은 그대로 사용.

- [ ] **Step 3: statsAggregate.ts 정책 적용**

`src/utils/statsAggregate.ts:73-83` 영역:

```ts
// 분야별 카운트 — Answer.type 정밀화 (v3.24 T6 / B2)
// 정책: type이 null/undefined/empty/whitespace → 'unknown' bucket → byInterest 제외.
const byInterestMap = new Map<string, number>();
for (const a of inRange) {
  const raw = a.type;
  const id = (typeof raw === 'string' && raw.trim().length > 0) ? raw : 'unknown';
  byInterestMap.set(id, (byInterestMap.get(id) ?? 0) + 1);
}
// 'unknown' bucket은 byInterest 결과 + activeInterests count에서 제외 (분야 통계 의미 보존).
byInterestMap.delete('unknown');
const byInterest = Array.from(byInterestMap.entries())
  .map(([id, count]) => ({ id, count }))
  .sort((a, b) => b.count - a.count)
  .slice(0, 5);
const activeInterests = byInterestMap.size;
```

- [ ] **Step 4: spec 실행 — 4 pass 확인**

```bash
TZ=Asia/Seoul npx vitest run tests/unit/stats-aggregate-byinterest.spec.ts 2>&1 | tail -20
```

Expected: 4 pass (P0 게이트 통과).

- [ ] **Step 5: cache fingerprint 영향 검증**

`stats-range-modal.ts:15-23` `buildFingerprint`가 `byInterest[0]` 의존. 'unknown' 제외 정책 변경으로 fingerprint key 자연 변동 → 기존 cache invalidate.

```bash
grep -n "byInterestTopKey" src/utils/statsCache.ts src/ui/modals/stats-range-modal.ts
```

cache invalidate는 자연스러우니 추가 코드 변경 X. 단 다음 사이클 사용자 첫 모달 open 시 cache miss → Gemini call 1회 발생 (정상 동작).

- [ ] **Step 6: 회귀 + 양쪽 TZ 검증**

```bash
npx tsc --noEmit && npm run lint
TZ=Asia/Seoul npx vitest run --reporter=verbose 2>&1 | grep -E "Tests|Test Files" | tail -3
TZ=America/New_York npx vitest run --reporter=verbose 2>&1 | grep -E "Tests|Test Files" | tail -3
```

Expected: 0 errors / 양쪽 TZ baseline + 8 (T2) + 4 (T4) + 8 (T5) + 4 (T6) = +24.

- [ ] **Step 7: T6 commit**

```bash
git add src/utils/statsAggregate.ts tests/unit/stats-aggregate-byinterest.spec.ts
git commit -m "$(cat <<'EOF'
fix(v3.24 T6 / B2): byInterest 'unknown' bucket 정책 정밀화

- type null/undefined/empty/whitespace → 'unknown' 통합 (기존: ?? 만, '' 통과)
- 'unknown' bucket을 byInterest + activeInterests에서 제외 (분야 통계 의미 보존)
- totalAnswers/avgPerDay는 전체 (unknown 포함)
- cache fingerprint 자연 invalidate (byInterestTopKey 변동)

신규 spec 4건 (T6 자체 P0 게이트):
- 정상 분야 분리
- type undefined/null/empty/whitespace → 제외
- mixed 분야 + unknown 처리
- 전체 unknown → byInterest 빈 배열

v3.23 T6 mid-pass P1-2 carry-forward.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: formatRelative dates.ts 이전 + 사이클 끝 검증 + Codex 최종 review + retro (B3 + 사이클 마감)

**Files:**
- Modify: `src/utils/dates.ts` (formatRelative 추가)
- Modify: `src/ui/modals/plant-detail.ts:88-95` (formatRelative 제거)
- Modify: `src/ui/tabs/insights.ts:7` (import 경로 갱신)
- Test: `tests/unit/dates-format-relative.spec.ts` (신규)
- Create: 메모리 retro `project_daily_growth_v324.md`

### Part A: formatRelative 이전

- [ ] **Step 1: 신규 spec 작성 (failing)**

`tests/unit/dates-format-relative.spec.ts`:

```ts
import { describe, it, expect, beforeEach, vi, afterEach } from 'vitest';
import { formatRelative } from '../../src/utils/dates';

beforeEach(() => {
  vi.useFakeTimers();
  vi.setSystemTime(new Date('2026-05-09T12:00:00Z'));
});
afterEach(() => vi.useRealTimers());

describe('formatRelative (v3.24 T7 — dates.ts 이전)', () => {
  it('미래 ISO (음수 ms)는 "오늘"', () => {
    expect(formatRelative('2026-05-10T12:00:00Z')).toBe('오늘');
  });

  it('당일은 "오늘"', () => {
    expect(formatRelative('2026-05-09T06:00:00Z')).toBe('오늘');
  });

  it('1일 전은 "어제"', () => {
    expect(formatRelative('2026-05-08T12:00:00Z')).toBe('어제');
  });

  it('2일 전은 "2일 전"', () => {
    expect(formatRelative('2026-05-07T12:00:00Z')).toBe('2일 전');
  });

  it('30일 전은 "30일 전"', () => {
    expect(formatRelative('2026-04-09T12:00:00Z')).toBe('30일 전');
  });
});
```

- [ ] **Step 2: spec fail 확인**

```bash
npx vitest run tests/unit/dates-format-relative.spec.ts 2>&1 | tail -10
```

Expected: FAIL (`formatRelative` export 없음 — dates.ts에 미정의).

- [ ] **Step 3: dates.ts에 formatRelative 추가**

`src/utils/dates.ts` 끝에 추가:

```ts
/**
 * ISO → "오늘" / "어제" / "N일 전" 상대 시간 표기.
 * 음수 ms (시계 역행 / 미래 ISO)는 '오늘'으로 가드 (v3.22 T1 로직 보존).
 *
 * v3.24 T7: plant-detail.ts에서 이전 (cross-module dep 해소).
 * 호출처: plant-detail / insights / insight-detail 등.
 */
export function formatRelative(iso: string): string {
  const ms = Date.now() - new Date(iso).getTime();
  if (ms < 0) return '오늘';
  const days = Math.floor(ms / 86400_000);
  if (days === 0) return '오늘';
  if (days === 1) return '어제';
  return `${days}일 전`;
}
```

- [ ] **Step 4: plant-detail.ts에서 formatRelative 제거 + 자체 import**

`src/ui/modals/plant-detail.ts`:

```ts
import { getKstDateStr, formatRelative } from '../../utils/dates';
// ...
// formatRelative 정의 (line 83-95) 삭제
```

`formatKoreanDate`는 plant-detail 고유로 유지 (plant 외 caller 없음).

- [ ] **Step 5: insights.ts import 경로 갱신**

`src/ui/tabs/insights.ts:7`:

```ts
// 기존: import { formatRelative } from '../modals/plant-detail';
import { formatRelative } from '../../utils/dates';
```

- [ ] **Step 6: 다른 caller 확인**

```bash
grep -rn "formatRelative" src/ tests/ | grep -v "node_modules"
```

`insight-detail.ts`는 `createdAt.slice(0, 10)`만 사용 — formatRelative 미사용 (v3.23 T9 결정). 변경 X.

- [ ] **Step 7: spec PASS + 회귀**

```bash
npx vitest run tests/unit/dates-format-relative.spec.ts 2>&1 | tail -10
TZ=Asia/Seoul npx vitest run --reporter=verbose 2>&1 | grep -E "Tests|Test Files" | tail -3
TZ=America/New_York npx vitest run --reporter=verbose 2>&1 | grep -E "Tests|Test Files" | tail -3
```

Expected: 5 pass / 양쪽 TZ baseline + 8 (T2) + 4 (T4) + 8 (T5) + 4 (T6) + 5 (T7) = +29.

### Part B: 사이클 끝 3종 검증

- [ ] **Step 8: TS strict + lint + 양쪽 TZ vitest**

```bash
npx tsc --noEmit
npm run lint
TZ=Asia/Seoul npm test 2>&1 | grep -E "Tests|Test Files" | tail -3
TZ=America/New_York npm test 2>&1 | grep -E "Tests|Test Files" | tail -3
```

Expected: 0 errors / 양쪽 TZ 1108+ pass (1079 baseline + 29).

- [ ] **Step 9: smoke fresh fork + bundle 측정**

```bash
rm -rf node_modules/.vite-temp && npm run build 2>&1 | tail -5
gzip -c dist/assets/index-*.js | wc -c
npm run test:smoke 2>&1 | tail -5
```

Expected: bundle 39397~39697 / smoke 96+.

- [ ] **Step 10: smoke N=3 cold-fork 안정성**

```bash
for i in 1 2 3; do
  rm -rf node_modules/.vite-temp
  npm run test:smoke 2>&1 | tail -3
done
```

Expected: 3/3 모두 96+ pass.

### Part C: Codex 최종 review

- [ ] **Step 11: Codex 최종 review dispatch**

```
codex:rescue invoke — input: "v3.24 cleanup 최종 review.
- diff: git diff <T0 commit>..HEAD
- spec: docs/superpowers/specs/2026-05-09-v3.24-cleanup-design.md
- DoD 검증: bundle 39397~39697 / vitest +29 / smoke 96+ / byInterest spec 4건
- 분류: DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT
- 특히 T6 byInterest 정책 변경의 production 영향 (cache invalidate 자연성 OK?)"
```

CLI 한도 시 controller self-review 허용. 결과를 retro에 명시.

P0 발견 시 in-cycle fix → re-검증. P1 발견 시 in-cycle 흡수 또는 v3.25 carry 결정.

### Part D: Retro + 메모리 + commit

- [ ] **Step 12: Retro 메모리 작성**

`/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v324.md`:

```markdown
---
name: DG v3.24 Cleanup + 메타 graduate
description: v3.23 carry (P2 4건 + per-task minor 4건 + 메타 graduate 2건) 일괄 청산. AMBER bundle drift -500~-800 B 완화. byInterest 'unknown' bucket 정밀화.
type: project
---

# DG v3.24 — Cleanup + 메타 graduate

**상태**: ✅ 사이클 완료, 배포 <대기 또는 완료>
**의존**: v3.23 (`9a91b9a` main, deploy <상태>)

## 사이클 요약

- **종류**: Cleanup + 메타 graduate (v3.18 / v3.14.5 선례)
- **사이즈**: 8 task (T0~T7)
- **Codex 2-pass**:
  - 사전 (T0): <분류> P0×<N>+P1×<N>+P2×<N> → in-cycle 반영 <N>건 / 격하 <N>
  - 최종 (T7): <DEPLOY_APPROVED / APPROVED_WITH_NOTES / REJECT> P0=0/P1=<N>/P2=<N>

## Commit 목록 (8~11건)

| Task | Main | Review fix |
|---|---|---|
| T0 | <hash> spec+plan+baseline+사전review | — |
| T1 | <hash> 메타 graduate | — |
| T2 | <hash> stats-modal DOM | — |
| T3 | <hash> bundle trim | — |
| T4 | <hash> chat-bubble 분리 | — |
| T5 | <hash> Insight guard + spec | — |
| T6 | <hash> byInterest semantics | — |
| T7 | <hash> formatRelative + retro | — |

## 검증 결과 (T7)

| 항목 | 결과 |
|---|---|
| TS strict | 0 errors |
| Lint | 0 errors |
| Vitest TZ=Asia/Seoul | 1108+ pass |
| Vitest TZ=America/New_York | 1108+ pass |
| Smoke | 96+ pass (cold-fork N=3 안정) |
| Bundle CLI gzip | <실측> B (Δ -<N> from 40197) |

## 핵심 변경

- A1 stats-range-modal DOM API 마이그레이션
- A2 bundle trim -<N> B
- A3 chat-bubble createChatPreviewBubble + bindChatPreviewBubbleHandlers 분리
- A4 + C1 메타 graduate (CLAUDE.md + COMMON_MISTAKES.md §8)
- B1 validateInsightText entry guard
- B2 byInterest 'unknown' bucket 제외 정책
- B3 formatRelative → utils/dates.ts 이전
- B4 mountInsightsHandlers refresh path spec

## 신규 lessons

1. **사전 Codex review graduate** — 8사이클 연속 P0 ROI, T1에서 명문화 (CLAUDE.md). v3.25+ 적용.
2. **`serviceWorkers: 'block'` graduate** — COMMON_MISTAKES.md §8 신설.
3. **byInterest 'unknown' 제외 정책** — totalAnswers (전체) vs activeInterests (분류된 것만) 의미 분리. Future feature(분야 chip)에서도 일관.
4. **DOM API 마이그레이션 패턴** — `escapeHtml + innerHTML` → `textContent + DOM` 시 helper들이 Node 반환으로 분리 + 마지막에 outerHTML로 직렬화 (modal contract 변경 비목표).
5. **cross-module dep 정리** — `@internal` 주석은 cross-module 사용 시 무력화. utils/* 이전이 정석.

## v3.25+ carry-forward

(v3.23 잔여 + 본 사이클 신규 P2)

- v3.23 per-task minor 잔여 8건 (T1 `as any` / T2 apiKey 주석 / T3 자정 race / T5 `.bar-label` CSS / T6 `cachedAt` / T8 createdAt UTC / T9 focus-visible / T10 N1·N3)
- v3.22 P2×3 (ESLint dates / Smoke KST race / Intl singleton)
- v3.21 / v3.20 / v3.17 §7.2 잔여
- 신규 기능 (insights chip / archive 통합 / Gemini retry 등)
- v3.24 본 사이클 신규 P2: <Codex 최종 review 결과>
```

- [ ] **Step 13: 메모리 인덱스 갱신**

`/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/MEMORY.md`에 v3.24 line 추가 (one-line, ~150 chars):

```
- [DG v3.24 Cleanup](project_daily_growth_v324.md) — **✅ 사이클 완료 (2026-05-09)** — 8 task · vitest 1108+ + smoke 96+ · bundle <N>→<N> · Codex 2-pass <분류> · 사전 review graduate
```

- [ ] **Step 14: spec retro 섹션 append**

`docs/superpowers/specs/2026-05-09-v3.24-cleanup-design.md` 끝에 §10 Retro 섹션 추가 (commit 목록 + DoD 결과 + Codex 결과 + lessons).

- [ ] **Step 15: T7 final commit**

```bash
git add src/utils/dates.ts \
        src/ui/modals/plant-detail.ts \
        src/ui/tabs/insights.ts \
        tests/unit/dates-format-relative.spec.ts \
        docs/superpowers/specs/2026-05-09-v3.24-cleanup-design.md
git commit -m "$(cat <<'EOF'
refactor(v3.24 T7 / B3 + retro): formatRelative dates.ts 이전 + 사이클 마감

- formatRelative: plant-detail.ts → utils/dates.ts (cross-module dep 해소)
- plant-detail.ts: 자체 import으로 전환
- insights.ts: import 경로 갱신
- 신규 spec 5건 (음수/당일/어제/2일/30일)
- spec retro append + 메모리 retro

T7 검증: TS 0 / lint 0 / vitest 1108+ 양쪽 TZ / smoke 96+ cold-fork N=3 / bundle <N>
Codex 최종: <분류>

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

### Part E: Deploy 옵션 실행 (사용자 승인 필수)

- [ ] **Step 16: Deploy (T0에서 결정한 옵션)**

옵션 (a) 묶음 일괄 deploy 시:
```bash
npm run build && firebase deploy --only hosting  # 사용자 명시 승인 후
```

옵션 (b)/(c)는 v3.22/v3.23이 T0에서 이미 deploy됨 → T7에서는 v3.24만 deploy.

deploy 결과 retro에 추가 commit (`docs(v3.24 T7+): deploy <hash> 완료`).

---

## §Baseline 표 (T0 Step 2~4 결과)

| 항목 | 값 | 측정 시점 |
|---|---|---|
| Vitest TZ=Asia/Seoul pass | 1079 | T0 Step 2 |
| Vitest TZ=America/New_York pass | 1079 | T0 Step 2 |
| Bundle CLI gzip (B) | 40197 | T0 Step 3 |
| Smoke cold-fork pass | 96 | T0 Step 4 |
| Branch HEAD | `9a91b9a` | T0 Step 1 |

(실측값을 T0 실행 시 채울 것)

---

## §Codex 사전 review 결과 (T0 Step 6)

| 항목 | 값 |
|---|---|
| Dispatcher | <codex CLI 또는 controller fallback> |
| 분류 | <P0/P1/P2 카운트> |
| In-cycle 반영 | <건수 + commit hash> |
| Carry to v3.25+ | <건수 + 항목> |

(실행 시 채울 것)

---

## §사이클 metrics 목표 (spec §8 기준)

- **Per-task review dispatch**: T2/T4/T6 spec+quality 2-stage = 6, T1/T3/T5/T7 single = 4 → ~10
- **Codex review dispatch**: 2회 (사전 T0 + 최종 T7)
- **In-cycle review fix commit**: ≤4건
- **commit 카운트**: T0~T7 main 8 + 0~3 review fix = **8~11**
- **사이클 길이**: 1~2일

---

**plan status**: v2 (2026-05-09 — 사전 review patch 적용)
**다음 단계**: v3.22+v3.23 deploy → T0 commit → T1 메타 graduate

---

## §10 사전 Review v2 Patch (P0×2 + P1×4 in-cycle 반영)

T0 사전 review (controller fallback `pr-review-toolkit:code-reviewer`) 결과 **REQUIRES_FIX** — P0×2 + P1×4 + P2×3 (P2는 retro carry).

### Patch 1 (P0-1, ✅ 적용 완료): T6 user-visible 영향 — spec §1 patch 적용

영향 grep 결과 (T0 baseline 측정):
- `src/utils/statsAggregate.ts`: 정의 + 7건 (T6 변경 대상)
- `src/ui/modals/stats-range-modal.ts`: 6건 (line 16, 20, 21, 26, 36, 49, 53, 117 — buildFingerprint / 활성 분야 카드 / renderInterestBars / deterministicHighlight)
- `src/utils/statsCache.ts`: 2~3건 (line 8, 31, 35 — fingerprint 4 field 비교)
- `src/services/prompts.ts`: 4~5건 (line 11, 12, 27, 32, 45-46 — Gemini prompt input)

**비영향 영역 (grep 검증)**: garden / plant unlock / 미션 / archive 필터 / xp / streak — 모두 `byInterest`/`activeInterests` 의존 X. spec §5 비목표 정합 ✅.

spec §1에 user-visible 영향 표 추가 완료.

### Patch 2 (P0-2, 의무 적용): T6 기존 spec 정정 step 추가

**T6 Step 4 (spec PASS 확인) 직후 Step 4.5 의무**:

```bash
# 기존 'unknown' 의존 spec 발굴
grep -rn "'unknown'\|\"unknown\"" tests/ | grep -i "stats\|byInterest\|aggregate"
```

발견된 기존 spec:
- `tests/unit/utils/statsAggregate.spec.ts:162-167`:
  ```ts
  it('type 없는 답변: "unknown"으로 집계', () => {
    ...
    expect(r.byInterest[0]).toEqual({ id: 'unknown', count: 1 });
  });
  ```

**정정 옵션**:
- A) spec content 정정: `expect(r.byInterest[0]).toEqual({ id: 'unknown', count: 1 })` → `expect(r.byInterest).toEqual([])` + `expect(r.activeInterests).toBe(0)` + describe 텍스트도 "unknown 제외" 의미로
- B) **spec 자체 삭제** (권장) — T6 신규 spec 4건 (Step 1)이 cover하고 더 정합 (전체 unknown / mixed 등). 중복 spec 정리.

→ **옵션 B 채택**. line 162-167 (it block) 삭제. 신규 spec이 실질 superset.

### Patch 3 (P1-1, 의무 적용): T7 기존 spec 활용 + 위치 이전

기존 spec [`tests/ui/modals/plant-detail-formatRelative.spec.ts`](../../tests/ui/modals/plant-detail-formatRelative.spec.ts) (5 it, KST instant `+09:00` 기반)이 이미 cover. plan 본문의 신규 spec 작성 (`tests/unit/dates-format-relative.spec.ts`) 대신 **기존 spec 마이그레이션 + 이전**.

**T7 Part A 재구성**:

- ~~Step 1 (신규 spec 작성, fail 확인)~~ → 삭제
- Step 1 (new): `dates.ts`에 `formatRelative` 추가 (plan 본문 기존 Step 3 그대로)
- Step 2 (new): `plant-detail.ts:88-95` `formatRelative` **함수 정의 자체 삭제** + `import { getKstDateStr, formatRelative } from '../../utils/dates';`로 갱신. `formatKoreanDate`는 plant-detail 자체 유지 (caller 없음).
- Step 3 (new): `insights.ts:7` import 경로 변경 (plan 본문 기존 Step 5 그대로)
- Step 4 (new): **기존 spec 위치 이전 + import 경로 갱신**:
  ```bash
  git mv tests/ui/modals/plant-detail-formatRelative.spec.ts \
         tests/unit/utils/dates-format-relative.spec.ts
  ```
  파일 내 import 경로:
  ```ts
  // 기존: import { formatRelative } from '../../../src/ui/modals/plant-detail';
  import { formatRelative } from '../../../src/utils/dates';
  ```
  describe 텍스트도 "v3.22 T1 → v3.24 T7 dates.ts 이전"로 정정.
- Step 5 (new): `insight-detail.ts` (formatRelative 미사용 — `createdAt.slice(0, 10)`만 사용, T0 grep 검증). **변경 없음**.
- Step 6 (new): 회귀 + 양쪽 TZ vitest

신규 spec 작성하지 않으므로 `tests/unit/dates-format-relative.spec.ts` 신규 파일 미생성. vitest 카운트 변동 0 (이전만, 신규 X).

### Patch 4 (P1-2, 의무 적용): T5 caller 마이그레이션 옵션 A 채택

`src/ui/handlers/home.ts:1109~1110` 현재:
```ts
const insightText = text.trim().slice(0, 200);
if (!insightText) { showToast('AI 분석에 실패했어요'); return; }
```

이미 trim + cap + 빈 가드. T5 plan Step 4 (caller 마이그레이션)는 **옵션 A 채택**:

```ts
// handlers/home.ts:1109~1110 변경 후
let insightText: string;
try {
  insightText = validateInsightText(text);
} catch {
  showToast('AI 분석에 실패했어요');
  return;
}
```

추가로 다른 caller grep:
```bash
grep -rn "insights.push\|insights\.\[" src/
```

발견 시 동일 패턴 적용. 미발견 시 single caller 통합으로 마감.

### Patch 5 (P1-3, 의무 적용): T3 dead CSS scan 옵션화 + DoD floor 명시

**T3 Step 3 (dead CSS scan) 가드**:

1. **1차 trim source** (production-safe): `src/services/prompts.ts` inline + dead import 두 가지로 한정.
2. 1차 source로 **-300 B 미달** 시에만 dead CSS scan 진입. 진입 시 PurgeCSS 또는 Playwright DOM verify로 false-positive 가드:
   ```bash
   # PurgeCSS 사용 가능 시
   npx purgecss --css src/styles/main.css --content 'src/**/*.{ts,html}' --output /tmp/purged.css
   diff src/styles/main.css /tmp/purged.css | head -30
   ```
3. 1차 source로 -300 B 도달 시 dead CSS scan 생략 (production path 진입 회피).

**DoD 정정**:
- floor 의무: bundle CLI gzip ≤ 39897 B (= 40197 - 300 B floor)
- target: bundle CLI gzip ∈ [39397, 39697] B (= -500~-800 B, conservative)
- ceiling best-effort: -300 B 달성 시에도 PASS, retro에 trim source 명시

### Patch 6 (P1-4, 의무 적용): T6 cache fingerprint 4 edge case 표

T6 Step 5 (cache fingerprint 영향 검증) 본문에 추가:

| user state | v3.23 fingerprint (top, count) | v3.24 fingerprint | cache 결과 | 의미 |
|---|---|---|---|---|
| 답변 0 | `('', 0)` | `('', 0)` | hit | deterministic 텍스트 동일 (의미 보존) |
| AI 5만 (정상) | `('AI', 5)` | `('AI', 5)` | hit | 동일 (의미 보존) |
| AI 5 + unknown 1 | `('AI', 5)` | `('AI', 5)` | hit | top 동일, byInterestTopCount 동일 (활성 분야 카드 카운트만 2→1 변동, fingerprint와 무관) |
| AI 1 + unknown 5 | `('unknown', 5)` | `('AI', 1)` | **miss** (자연 invalidate) | top 변동 → cache 자동 무효화 |
| 모두 unknown 5 | `('unknown', 5)` | `('', 0)` | **miss** | top 사라짐 → 자동 무효화 |

→ **모든 user state에서 의미 변경 user는 cache miss로 자연 invalidate**. 의미 보존 user는 cache hit (정상). **추가 코드 변경 0** (clearStatsCache() 호출 등 불필요).

단 edge case 3 ("AI 5 + unknown 1")에서 활성 분야 카드 카운트만 변동 (2→1)하지만 cache hit 시 `highlight` 텍스트는 stale 가능. 다만 카드 카운트는 fingerprint와 별개 — `r.activeInterests` 는 매번 새로 계산되어 정확. **highlight 텍스트의 deterministic 부분 ("이번 주 N개 답변, 최장 N일, AI 강세")은 의미 동일** (top 'AI' 그대로). UX 회귀 없음.

### P2 Carry → v3.25+ Retro

| ID | 항목 | 처리 시점 |
|---|---|---|
| P2-1 | spec §7 옵션 (b) 표현 정정 ("3회" → "묶음 build 1회 + 검증 분리") 또는 옵션 (b) 삭제 | v3.24 retro (T7 Part D) |
| P2-2 | `openModal` contract `bodyNode?: Node` 확장 + stats-range-modal innerHTML 직렬화 단계 제거 | v3.25 polish |
| P2-3 | spec §8 commit ceiling 8~11 → 9~13 완화 (deploy retro commit 포함) | v3.24 retro |

---

**v2 patch 적용 commit 시점**: T0 commit (spec/plan v2 + baseline + 사전 review 결과 통합).
