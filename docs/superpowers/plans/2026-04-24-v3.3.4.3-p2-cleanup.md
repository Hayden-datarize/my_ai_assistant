# v3.3.4.3 P2 Cleanup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v3.3.4.2/v3.3.4 잔여 P2 4건(heatmap 라벨/future-guard, briefings max-width cap, aspect parametric smoke) 소진하여 v3.3 시리즈 완전 종결.

**Architecture:** 파일 locality 기반 4 Task + 1 final verification. 각 Task는 TDD(실패 테스트 → 구현 → 통과 → 커밋). A4(cards max-width)를 A3(aspect parametric)보다 먼저 배치해 1920px에서 aspect 오차가 cap 적용 후 안정된 상태에서 검증되도록 함.

**Tech Stack:** Vite + TypeScript(vanilla), Vitest + jsdom(unit), Playwright(smoke), Firebase Hosting.

**Branch:** `feat/v3.3.4.3-p2-cleanup` (이미 생성됨).

---

## Task 1: A1 — heatmap 라벨 "최근 28일" → "이번 주 포함 4주"

**Files:**
- Modify: `src/ui/handlers/stats.ts:68`
- Modify: `tests/unit/heatmap.spec.ts:161,198`

- [ ] **Step 1: 기존 unit test assertion을 새 라벨로 업데이트 (실패 테스트 생성)**

`tests/unit/heatmap.spec.ts:161` 수정:
```ts
  it('shows summary with total + streak when there are answers', async () => {
    vi.setSystemTime(new Date('2026-04-22T03:00:00Z'));
    localStorage.setItem('dg.answers', JSON.stringify([
      { date: '2026-04-21', text: 'a', type: '감정' },
      { date: '2026-04-22', text: 'b', type: '감정' },
      { date: '2026-04-22', text: 'c', type: '감정' },
    ]));
    const mod = await import('../../src/ui/handlers/stats');
    mod.hydrateStats();
    const info = document.getElementById('heatmapInfo');
    expect(info?.textContent).toBe('이번 주 포함 4주 · 3개 달성 · 최장 연속 2일');
  });
```

`tests/unit/heatmap.spec.ts:198` 수정:
```ts
    expect(info?.textContent).toBe('이번 주 포함 4주 · 1개 달성 · 최장 연속 1일');
```

- [ ] **Step 2: 테스트 실행해서 fail 확인**

Run: `npm test -- tests/unit/heatmap.spec.ts`
Expected: 2 failures with message `Expected '이번 주 포함 4주...' but got '최근 28일...'`

- [ ] **Step 3: `setDefaultInfo` 문자열 교체**

`src/ui/handlers/stats.ts:65-69`:
```ts
function setDefaultInfo(info: HTMLElement, total: number, streak: number): void {
  info.textContent = total === 0
    ? '아직 기록이 없어요. 첫 답변을 남겨보세요.'
    : `이번 주 포함 4주 · ${total}개 달성 · 최장 연속 ${streak}일`;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test -- tests/unit/heatmap.spec.ts`
Expected: 모든 테스트 PASS.

- [ ] **Step 5: 커밋**

```bash
git add src/ui/handlers/stats.ts tests/unit/heatmap.spec.ts
git commit -m "fix(stats): rename heatmap summary label '최근 28일' → '이번 주 포함 4주'

Window anchor is current-week-Monday minus 3 weeks (Mon-Sun 4×7 rect),
not a rolling 28-day window. New label matches the visual meaning."
```

---

## Task 2: A2 — heatmap 미래 셀 future-guard

**Files:**
- Modify: `src/ui/handlers/stats.ts` (hydrateHeatmap loop, moveFocus)
- Modify: `src/styles/components/heatmap.css` (add `.is-future` rules)
- Modify: `tests/unit/heatmap.spec.ts` (+6 tests in new describe block)

- [ ] **Step 1: 실패 테스트 6건을 `tests/unit/heatmap.spec.ts` 파일 끝에 추가**

```ts
describe('hydrateHeatmap future guard (v3.3.4.3)', () => {
  beforeEach(() => {
    localStorage.clear();
    sessionStorage.clear();
    mountHeatmapDom();
    vi.useFakeTimers();
  });
  afterEach(() => {
    vi.useRealTimers();
    vi.resetModules();
  });

  it('future cells (within current week, past today) get .is-future + aria-disabled + dataset.future', async () => {
    // Today = 2026-04-22 Wed → Thu/Fri/Sat/Sun of current week are future
    vi.setSystemTime(new Date('2026-04-22T03:00:00Z'));
    const mod = await import('../../src/ui/handlers/stats');
    mod.hydrateStats();
    const futureKeys = ['2026-04-23', '2026-04-24', '2026-04-25', '2026-04-26'];
    for (const key of futureKeys) {
      const cell = document.querySelector<HTMLButtonElement>(`.heatmap-cell[data-date="${key}"]`);
      expect(cell, `cell for ${key} exists`).not.toBeNull();
      expect(cell!.classList.contains('is-future')).toBe(true);
      expect(cell!.getAttribute('aria-disabled')).toBe('true');
      expect(cell!.dataset['future']).toBe('true');
      expect(cell!.getAttribute('aria-label')).toBe(`${key}, 미래 날짜`);
    }
  });

  it('past and today cells are not .is-future and not aria-disabled', async () => {
    vi.setSystemTime(new Date('2026-04-22T03:00:00Z'));
    const mod = await import('../../src/ui/handlers/stats');
    mod.hydrateStats();
    const past = document.querySelector<HTMLButtonElement>('.heatmap-cell[data-date="2026-04-20"]')!;
    const today = document.querySelector<HTMLButtonElement>('.heatmap-cell.is-today')!;
    expect(past.classList.contains('is-future')).toBe(false);
    expect(past.getAttribute('aria-disabled')).toBeNull();
    expect(past.dataset['future']).toBeUndefined();
    expect(today.classList.contains('is-future')).toBe(false);
    expect(today.getAttribute('aria-disabled')).toBeNull();
  });

  it('when today is Sunday, no future cells in window', async () => {
    vi.setSystemTime(new Date('2026-04-26T03:00:00Z')); // Sun
    const mod = await import('../../src/ui/handlers/stats');
    mod.hydrateStats();
    expect(document.querySelectorAll('.heatmap-cell.is-future').length).toBe(0);
  });

  it('mouseenter on future cell updates info to "아직 오지 않은 날짜"', async () => {
    vi.setSystemTime(new Date('2026-04-22T03:00:00Z'));
    const mod = await import('../../src/ui/handlers/stats');
    mod.hydrateStats();
    const future = document.querySelector<HTMLButtonElement>('.heatmap-cell[data-date="2026-04-23"]')!;
    future.dispatchEvent(new MouseEvent('mouseenter', { bubbles: true }));
    const info = document.getElementById('heatmapInfo');
    expect(info?.textContent).toBe('4월 23일 (목) · 아직 오지 않은 날짜');
  });

  it('click on future cell does not open the day-detail modal', async () => {
    vi.setSystemTime(new Date('2026-04-22T03:00:00Z'));
    const mod = await import('../../src/ui/handlers/stats');
    mod.hydrateStats();
    const future = document.querySelector<HTMLButtonElement>('.heatmap-cell[data-date="2026-04-23"]')!;
    future.dispatchEvent(new MouseEvent('click', { bubbles: true }));
    expect(document.querySelector('.dg-modal')).toBeNull();
  });

  it('ArrowDown from today on last column skips over future cells (stays on today)', async () => {
    // Today Wed (2026-04-22) is in last column row 3 (Mon=0, Tue=1, Wed=2).
    // Rows 3~6 (Thu,Fri,Sat,Sun) in last column are all future cells.
    // ArrowDown iterates +1 and must skip all future → no valid target → stay.
    vi.setSystemTime(new Date('2026-04-22T03:00:00Z'));
    const mod = await import('../../src/ui/handlers/stats');
    mod.hydrateStats();
    const today = document.querySelector<HTMLButtonElement>('.heatmap-cell.is-today')!;
    today.setAttribute('tabindex', '0');
    today.focus();
    today.dispatchEvent(new KeyboardEvent('keydown', { key: 'ArrowDown', bubbles: true }));
    expect(document.activeElement).toBe(today);
  });
});
```

- [ ] **Step 2: 테스트 실행해서 모두 fail 확인**

Run: `npm test -- tests/unit/heatmap.spec.ts`
Expected: 6건 모두 FAIL (is-future class/aria-disabled/dataset 등 아직 세팅 안 됨).

- [ ] **Step 3: `hydrateHeatmap` 루프에 future-guard 로직 추가**

`src/ui/handlers/stats.ts` 내 `hydrateHeatmap` 함수의 for 루프(현재 line 174~196) 교체:

```ts
  for (let i = 0; i < DAYS; i++) {
    const d = new Date(firstDay);
    d.setDate(firstDay.getDate() + i);
    const key = getDateStr(d);
    const n = counts.get(key) ?? 0;
    const level = Math.min(3, n);
    const isFuture = key > todayKey; // YYYY-MM-DD ISO → lexicographic == chronological
    const cell = document.createElement('button');
    cell.type = 'button';
    cell.className = `heatmap-cell level-${level}${key === todayKey ? ' is-today' : ''}${isFuture ? ' is-future' : ''}`;
    cell.dataset['date'] = key;
    if (isFuture) {
      cell.dataset['future'] = 'true';
      cell.setAttribute('aria-disabled', 'true');
      cell.setAttribute('aria-label', `${key}, 미래 날짜`);
    } else {
      const ariaLabel = n > 0 ? `${key}, ${n}개 달성` : `${key}, 기록 없음`;
      cell.setAttribute('aria-label', ariaLabel);
    }
    cell.addEventListener('click', () => {
      if (cell.dataset['future'] === 'true') return;
      openDayDetail(cell);
    });
    cell.addEventListener('mouseenter', () => {
      if (!info) return;
      if (cell.dataset['future'] === 'true') {
        info.textContent = `${formatCellLabel(key)} · 아직 오지 않은 날짜`;
      } else {
        info.textContent = n > 0
          ? `${formatCellLabel(key)} · ${n}개 달성`
          : `${formatCellLabel(key)} · 기록 없음`;
      }
    });
    cell.addEventListener('mouseleave', () => {
      if (info) setDefaultInfo(info, total, streak);
    });
    grid.append(cell);
  }
```

- [ ] **Step 4: `moveFocus`에 미래 셀 skip 로직 추가**

`src/ui/handlers/stats.ts` 내 `moveFocus`(현재 line 202~210) 교체:

```ts
  const moveFocus = (from: HTMLButtonElement, delta: number): void => {
    const startIdx = dataCells.indexOf(from);
    let target = startIdx + delta;
    const step = delta > 0 ? 1 : -1;
    // Skip consecutive future cells in the direction of travel.
    while (
      target >= 0 &&
      target < dataCells.length &&
      dataCells[target]!.dataset['future'] === 'true'
    ) {
      target += step;
    }
    if (target < 0 || target >= dataCells.length) return;
    from.setAttribute('tabindex', '-1');
    const next = dataCells[target]!;
    next.setAttribute('tabindex', '0');
    next.focus();
  };
```

- [ ] **Step 5: `src/styles/components/heatmap.css` 파일 끝에 `.is-future` 규칙 추가**

`Reduce motion` 블록(현재 line 143~147) **위에** 다음 추가:

```css
/* Future cells (v3.3.4.3) — rendered but non-interactive */
.heatmap-cell.is-future {
  opacity: 0.5;
  cursor: not-allowed;
}
.heatmap-cell.is-future:hover,
.heatmap-cell.is-future:focus-visible {
  transform: none;
}
```

- [ ] **Step 6: 테스트 전체 실행 확인 (unit + 기존 회귀)**

Run: `npm test -- tests/unit/heatmap.spec.ts`
Expected: 모든 테스트 (기존 + 신규 6건) PASS.

Run: `npm test`
Expected: 전체 vitest PASS (기존 276 + 신규 6 = 282건 근방).

- [ ] **Step 7: 커밋**

```bash
git add src/ui/handlers/stats.ts src/styles/components/heatmap.css tests/unit/heatmap.spec.ts
git commit -m "fix(stats): guard future cells in heatmap from click / misleading aria

Within-current-week days that are past today (e.g., Thu/Fri/Sat/Sun when
today is Wed) were rendered as level-0 cells whose click opened the
'기록 없음' modal. Now such cells get .is-future + aria-disabled +
dataset.future. Click is no-op, mouseenter shows '아직 오지 않은 날짜',
keyboard nav skips over them."
```

---

## Task 3: A4 — briefing-scroll wide-viewport max-width cap

**Files:**
- Modify: `src/styles/components/cards.css` (add max-width rules)
- Modify: `tests/smoke/cardnews.spec.ts` (+1 smoke test)
- Verify (read-only): `src/styles/pages/home.css:130-150` — grid item context 확인 완료

- [ ] **Step 1: 실패 smoke 테스트를 `tests/smoke/cardnews.spec.ts` 끝에 추가**

```ts
test('v3.3.4.3: wide viewport 1920 caps briefing-scroll width and centers it', async ({ page }) => {
  await page.setViewportSize({ width: 1920, height: 1080 });
  await seedBriefings(page, mkCards(5, true));
  await page.goto('/');

  const scroll = page.locator('#briefingScroll');
  const bbox = await scroll.boundingBox();
  expect(bbox).not.toBeNull();
  // Cap 1200px (content-box via box-sizing: border-box global rule)
  expect(bbox!.width).toBeLessThanOrEqual(1200);
  // Expect horizontal centering within the viewport
  const leftGutter = bbox!.x;
  const rightGutter = 1920 - (bbox!.x + bbox!.width);
  expect(Math.abs(leftGutter - rightGutter)).toBeLessThan(2);
});
```

- [ ] **Step 2: smoke 실행해서 fail 확인**

Run: `npx playwright test tests/smoke/cardnews.spec.ts -g "wide viewport 1920"`
Expected: FAIL — width ≈ 1920-left_padding(80)-right_padding(20) ≈ 1820px (cap 없음 상태).

- [ ] **Step 3: 시도 (a) — `margin-inline: auto + max-width`**

`src/styles/components/cards.css:29~34` 교체:

```css
.briefing-scroll {
  display: grid;
  grid-template-columns: 1fr;
  gap: 16px;
  padding: 0 20px 16px;
  max-width: 1200px;
  margin-inline: auto;
}
```

- [ ] **Step 4: smoke 재실행 + 실측**

Run: `npx playwright test tests/smoke/cardnews.spec.ts -g "wide viewport 1920"`
Expected: PASS — width ≤ 1200px, centered.

실측 (필수):
```bash
npm run dev
# 별도 터미널에서
open -na "Google Chrome" --args --new-window --user-data-dir=/tmp/dg-1920 "http://localhost:5173/"
# Chrome DevTools → Device Toolbar → Responsive → 1920×1080 설정
```
시각 확인 체크리스트:
- [ ] briefing-scroll이 가운데 정렬되어 보이는가?
- [ ] 3-col grid 유지되는가?
- [ ] 5th card(span 2) 높이가 row 상단과 정렬되는가?
- [ ] Track 수축(118px 증상 등) 없는가?

**(a) 실패 시 시도 (b):**
위 Step 3 대체 — 아래 추가 스타일로 grid-item context 보정:
```css
.briefing-scroll {
  display: grid;
  grid-template-columns: 1fr;
  gap: 16px;
  padding: 0 20px 16px;
  max-width: 1200px;
  margin-inline: auto;
}
@media (min-width: 768px) {
  #homeTab > .briefing-scroll {
    justify-self: center;
    width: 100%;
  }
}
```

**(a)(b) 모두 실패 시 시도 (c):** — Hayden에 보고 후 래퍼 마크업 검토 (별도 세션).

- [ ] **Step 5: scroll-indicator도 가운데 정렬되는지 시각 확인**

`#scrollIndicator`는 `.briefing-scroll`의 sibling (grid item). 이미 `home.css:145`에서 `grid-column: 1/-1` 지정. cap은 `.briefing-scroll` 자체에만 적용되므로 `#scrollIndicator`는 영향 없음. 시각적으로 정렬이 어색하면 동일 cap 추가 검토:
```css
#scrollIndicator {
  max-width: 1200px;
  margin-inline: auto;
}
```
(별도 commit — 이번 cycle에서 필요 판단 시 추가.)

- [ ] **Step 6: 전체 smoke 회귀 확인**

Run: `npm run test:smoke -- cardnews`
Expected: cardnews 전체 PASS (9건 → 10건 근방).

- [ ] **Step 7: 커밋**

```bash
git add src/styles/components/cards.css tests/smoke/cardnews.spec.ts
git commit -m "feat(cards): cap briefing-scroll width at 1200px on wide viewports

Above ~1440px the 3-col grid stretched each card to >500px width,
breaking the cardnews readability ratio. Apply max-width:1200px +
margin-inline:auto so the grid stays visually tight and centered."
```

---

## Task 4: A3 — 5th-card aspect parametric smoke

**Files:**
- Modify: `tests/smoke/cardnews.spec.ts` (replace existing v3.3.4.3 test with parametric)

- [ ] **Step 1: 기존 단일 테스트를 parametric 블록으로 교체**

`tests/smoke/cardnews.spec.ts` 내 line 229~244 (`test('v3.3.4.3: 5th card height matches...')`) 전체를 아래로 교체:

```ts
// v3.3.4.3: Verify the 41/25 override holds across the full desktop range.
// Entering @media (min-width: 1024px) the 5th card spans 2 columns; without
// the aspect override it would compute ~2× the height of 1-col siblings.
// A4 max-width cap at 1200 pins the grid track to ≈(1200-40-32)/3 ≈ 376px
// on 1920 viewports, keeping the 41/25 approximation well within 2px.
for (const width of [1024, 1280, 1440, 1920]) {
  test(`v3.3.4.3: 5th card height matches row siblings at ${width}px`, async ({ page }) => {
    await page.setViewportSize({ width, height: 900 });
    await seedBriefings(page, mkCards(5, true));
    await page.goto('/');

    const fourth = await page.locator('.briefing-card').nth(3).boundingBox();
    const fifth = await page.locator('.briefing-card').nth(4).boundingBox();
    expect(fourth).not.toBeNull();
    expect(fifth).not.toBeNull();
    expect(Math.abs(fourth!.height - fifth!.height)).toBeLessThan(2);
  });
}
```

- [ ] **Step 2: smoke 실행해서 통과 확인**

Run: `npm run test:smoke -- cardnews`
Expected: 4 viewports 모두 PASS. 기존 2.44KB 단일 테스트 대신 4건으로 늘어남.

오차 ≥2px 발생 시 (예상 낮음): cap 값 조정(1200→1280) 고려 + Hayden 협의. A3 확장이 A4에 의존하므로 두 Task 커밋이 인접하도록 순서 유지.

- [ ] **Step 3: 커밋**

```bash
git add tests/smoke/cardnews.spec.ts
git commit -m "test(smoke): parametric aspect check for 5th card across 1024-1920

Replace the single 1280px test with a loop over [1024, 1280, 1440, 1920]
to catch any aspect drift at the edges of the desktop range."
```

---

## Task 5: 최종 검증 + 독립 리뷰 + 배포 준비

**Files:**
- Modify: `.claude/completions/2026-04-24-v3.3.4.3-p2-cleanup.md` (새로 생성)
- Prepare for: memory update, main merge, firebase deploy

- [ ] **Step 1: 전체 검증 실행**

```bash
npm run lint
npm test
npm run test:smoke
npm run build
```
Expected:
- Lint: 0 error 0 warn
- Vitest: 282 근방 all pass
- Playwright: 73 근방 all pass (기존 68 + 신규 5~6)
- Build: tsc 0 error, vite bundle 성공

- [ ] **Step 2: 독립 코드 리뷰 디스패치**

Dispatch `superpowers:code-reviewer` agent with standard review:
- 범위: `main..feat/v3.3.4.3-p2-cleanup` 전체 커밋 (4~5개)
- 기준: spec 준수 + 명백한 버그 + WCAG/접근성 회귀 + CSS 회귀

- [ ] **Step 3: 리뷰 결과 triage**

- **P0/P1** → 이번 사이클에 in-cycle commit으로 반영
- **P2** → `.claude/completions/2026-04-24-v3.3.4.3-p2-cleanup.md`에 v3.5 백로그로 기록

- [ ] **Step 4: 완료 문서 생성**

`.claude/templates/completion-template.md`를 기반으로 `.claude/completions/2026-04-24-v3.3.4.3-p2-cleanup.md` 작성:
- Delivered (A1~A4)
- Verification 결과 (vitest/playwright/lint/build 수치)
- 독립 리뷰 결과 (P0/P1/P2 분포)
- v3.5 백로그 (리뷰 P2 + 기존 v3.3.4 closing polish P2 합산)
- Lessons learned

- [ ] **Step 5: Hayden 승인 요청 및 배포**

사용자에게 다음 보고:
1. 변경 파일 diff 요약
2. 테스트/빌드 수치
3. 1920px 실측 스크린샷 (Task 3 Step 4에서 확보)
4. 독립 리뷰 결과

승인 받으면:
```bash
git checkout main
git merge --no-ff feat/v3.3.4.3-p2-cleanup
npm run build
firebase deploy --only hosting
```

- [ ] **Step 6: 메모리 업데이트**

- 새 파일: `/Users/hayden/.claude/projects/-Users-hayden-Hayden-AX-Project-my-ai-assistance/memory/project_daily_growth_v3343.md`
- 인덱스: `MEMORY.md`에 엔트리 추가
- 완료 후 `project_daily_growth_v3342.md` open-items 섹션의 P2 항목들에 "resolved in v3.3.4.3 (2026-04-24)" 주석 추가.

---

## Self-Review Checklist

- [x] **Spec coverage**: A1 (Task 1), A2 (Task 2), A3 (Task 4), A4 (Task 3), 검증/리뷰/배포 (Task 5) — 전부 커버
- [x] **Placeholder scan**: "TBD"/"TODO"/"적절한" 없음. 모든 코드 블록 실제 코드.
- [x] **Type consistency**:
  - `dataset['future']` 표기 통일 (TS noUncheckedIndexedAccess 고려)
  - `getDateStr(d)` 사용 일관 (persistence + stats 공용)
  - `formatCellLabel(key)` 재사용 (기존 helper)
- [x] **Testing coverage**: unit 6 + smoke 5 = 11 additions

## Risks & Mitigations (Plan-level)

1. **Grid-item/margin:auto 충돌 재현** (Task 3): 3단계 구현 시도(a→b→c) 명시, 실측 Step 포함.
2. **A3 4viewport 중 1920px 오차**: Task 순서를 A4 → A3으로 배치 — cap 이후 안정화 상태에서 검증.
3. **Native disabled 대신 aria-disabled 선택** (Task 2): dataset 가드가 click 이벤트에 먼저 bind되는지 확인 — 테스트 Step 1에서 "click does not open modal" case로 검증.
4. **Keyboard nav skip 끝까지 못 가는 경우** (Task 2): `while` 루프가 `target < 0 || target >= dataCells.length` 범위 초과 후 early return — 테스트 "ArrowDown stays on today" case로 검증.

## Commits 예상 (Main에 merge 기준)

1. `fix(stats): rename heatmap summary label '최근 28일' → '이번 주 포함 4주'`
2. `fix(stats): guard future cells in heatmap from click / misleading aria`
3. `feat(cards): cap briefing-scroll width at 1200px on wide viewports`
4. `test(smoke): parametric aspect check for 5th card across 1024-1920`
5. *(선택)* 리뷰 P0/P1 반영 commit
6. *(선택)* `docs: add v3.3.4.3 completion doc`

Merge commit (no-ff) 포함 main에 5~7 commits 도입 예상.
