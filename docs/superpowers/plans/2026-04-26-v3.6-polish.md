# v3.6 Polish — P2 정리 + i18n Lite 정책 graduation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v3.5 final review에서 deferred된 P2 7건 + 신규 vitest flake 1건을 단일 사이클에 정리한다. 핵심은 (1) `isHttpsUrl` 강화 + 정책-vs-프로토콜 분리 컨벤션 명시, (2) `interests.ts`의 user state 중복 제거, (3) i18n Lite 정책의 ≥2 빈도 규칙 폐기와 캐쥬얼 메시지 단일 진입점화, (4) `showPartialTranslateFail` dedup helper 통합, (5) flake 안정화.

**Architecture:** 기존 Vite + TypeScript SPA 구조 유지. **새 모듈 0개 / 새 dep edge 0개**. `state/user.ts`/`utils/url.ts`/`ui/translateToast.ts`/`ui/messages.ts`의 SSOT 책임을 두껍게 한다. 단일 브랜치 `feat/v3.6-polish`에 Setup 1 + impl 6 + review-fix 0~2 = 7~9 commits. 각 task = 1 commit.

**Tech Stack:** Vite 5 / TypeScript strict / Vitest + Playwright / Firebase Hosting

**Spec:** `docs/superpowers/specs/2026-04-26-v3.6-polish-design.md`

---

## File Structure

### 신규 파일

| Path | 책임 |
|---|---|
| `tests/unit/translateToast.spec.ts` | T3 dedup hit/miss 단언 (vi.mock toast + module-level state isolation) |

### 수정 파일

| Path | 변경 | Task |
|---|---|---|
| `src/utils/url.ts` | `HTTPS_PREFIX` regex 상수 + prefix 검사 + non-empty hostname 검사 + JSDoc 3가지 명시 | T2 |
| `tests/unit/url.spec.ts` | 신규 reject 케이스 6건 (bare-path / backslash / empty-host / empty-prefix / relative / HTTPS:foo) | T2 |
| `src/ui/handlers/home.ts` | `swapTitleInDOM` 직전 `@internal` JSDoc + onDrain caller가 `showPartialTranslateFail` 호출 + `MSG.ANSWER_SAVED` 사용 + import 추가 | T3 / T4 / T5 |
| `tests/unit/settings-interests-section.spec.ts` | `setTimeout` 2곳 → `vi.waitFor` polling | T6 |
| `src/ui/modals/interests.ts` | `USER_STORAGE`/`LegacyUser`/`loadUser`/`saveUser` 삭제 + `state/user.ts` import + `MSG.INTERESTS_UPDATED` 사용 + `MSG` import 추가 | T1 / T4 |
| `src/ui/messages.ts` | `INTERESTS_UPDATED` + `ANSWER_SAVED` 추가 + JSDoc 정책 갱신 (≥2 폐기) | T4 |
| `tests/unit/messages.spec.ts` | `'exposes expected keys'` 리스트에 2 키 추가 | T4 |
| `src/ui/translateToast.ts` | `showPartialTranslateFail(failedCount)` helper 추가 (`lastShownError` 재사용) | T3 |

---

## Setup

- [ ] **Setup-1: 브랜치 확인 (이미 생성된 상태)**

```bash
git status
git rev-parse --abbrev-ref HEAD
```

Expected: `feat/v3.6-polish` 브랜치, working tree clean (이미 spec commit `cb6c493` + fix `ec191fb` 들어있음).

- [ ] **Setup-2: baseline 검증**

```bash
npm run lint
npx tsc --noEmit
npm test -- --run
npm run test:smoke
npm run build
```

Expected:
- lint / tsc: 0 error 0 warn
- vitest: **343 pass**
- Playwright: **79 pass**
- build: CSS ~33.56 kB / JS ~63.91 kB (gzip)

baseline이 위 수치와 다르면 v3.5 머지 직후 상태가 아니므로 정지하고 사용자에게 알림.

- [ ] **Setup-3: plan 커밋**

```bash
git add docs/superpowers/plans/2026-04-26-v3.6-polish.md
git commit -m "docs(v3.6): add implementation plan"
```

---

## Task 1: T2 — `isHttpsUrl` 강화 (prefix + non-empty hostname)

**Files:**
- Modify: `src/utils/url.ts`
- Modify: `tests/unit/url.spec.ts`

- [ ] **Step 1: 실패 테스트 6건 추가 (`tests/unit/url.spec.ts` 끝에 신규 describe 블록)**

기존 파일은 [tests/unit/url.spec.ts](tests/unit/url.spec.ts)이며 line 4 `describe('isHttpsUrl', ...)`가 이미 있다. 같은 describe의 마지막 it 블록 뒤에 다음 케이스를 추가한다:

```typescript
  it('rejects bare-path scheme (defense-in-depth, v3.6 P2-2)', () => {
    expect(isHttpsUrl('https:foo')).toBe(false);
    expect(isHttpsUrl('https:foo/bar')).toBe(false);
    expect(isHttpsUrl('https:./relative')).toBe(false);
  });

  it('rejects backslash variants (v3.6 P2-2)', () => {
    expect(isHttpsUrl('https:\\\\evil.com')).toBe(false);
  });

  it('rejects empty hostname (v3.6 P2-2)', () => {
    expect(isHttpsUrl('https://')).toBe(false);
    expect(isHttpsUrl('https:///path')).toBe(false);
  });

  it('rejects case-insensitive bare-path scheme (v3.6 P2-2 회귀)', () => {
    expect(isHttpsUrl('HTTPS:foo')).toBe(false);
  });
```

**주의:** `'https:\\\\evil.com'`의 4개 백슬래시 = 실제 문자열 `https:\\evil.com` (2 백슬래시) — TS 문자열 리터럴 이스케이프.

- [ ] **Step 2: 테스트 실패 확인**

```bash
npm test -- --run tests/unit/url.spec.ts
```

Expected: 4건 fail
- `'https:foo'` → 현재 true 반환 (`new URL('https:foo').protocol === 'https:'`)
- `'https:\\\\evil.com'` → 현재 true (브라우저 quirk)
- `'https://'` → 현재 true (hostname 빈 문자열인데 protocol만 검사)
- `'HTTPS:foo'` → 현재 true (case-insensitive scheme)

- [ ] **Step 3: 구현 (`src/utils/url.ts` 전체 교체)**

```typescript
/**
 * Validates that a value is an https:// URL parseable by `new URL()`.
 *
 * - Rejects all non-string types (null/undefined/number/object).
 * - Rejects non-https schemes (http, javascript, data, etc.).
 * - Rejects bare-path scheme variants (`https:foo`, `https:\\evil.com`,
 *   `HTTPS:foo`). Strict literal `https://` prefix required (case-insensitive).
 * - Rejects URLs with empty hostname (`https://`, `https:///path`).
 * - Strings with leading/trailing whitespace are rejected; callers must trim
 *   first. This is intentional and not an error signal — caller가 trim 책임.
 * - case-insensitive prefix 보존 의도 — `HTTPS://example.com` 입력은 통과
 *   (URL parser가 protocol을 lowercase로 정규화).
 *
 * 정책 분리: host policy(IP literal/localhost reject 등)는 본 함수가 아닌
 * 별도 `isAllowedImageHost` 류로 분기. 본 함수는 프로토콜 검사 only.
 */
const HTTPS_PREFIX = /^https:\/\//i;

export function isHttpsUrl(value: unknown): value is string {
  if (typeof value !== 'string') return false;
  if (value !== value.trim()) return false;
  if (!HTTPS_PREFIX.test(value)) return false;
  try {
    const u = new URL(value);
    return u.protocol === 'https:' && u.hostname.length > 0;
  } catch {
    return false;
  }
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
npm test -- --run tests/unit/url.spec.ts
```

Expected: 모든 url.spec.ts 케이스 pass (기존 + 신규 6건). 회귀로 `'https://example.com'`/`'HTTPS://example.com'`/`'https://cdn.example.com/img.jpg'` 등은 그대로 true.

- [ ] **Step 5: 전체 vitest + lint + tsc 확인**

```bash
npm test -- --run
npm run lint
npx tsc --noEmit
```

Expected: 343 + 6 = **349 pass**, 0 lint error, 0 tsc error.

- [ ] **Step 6: 커밋**

```bash
git add src/utils/url.ts tests/unit/url.spec.ts
git commit -m "feat(v3.6 T2): strengthen isHttpsUrl — prefix + non-empty hostname

Adds defense-in-depth against bare-path scheme variants:
- 'https:foo' / 'https:./relative' (no // — bare-path)
- 'https:\\\\evil.com' (backslash variant)
- 'https://' / 'https:///path' (empty hostname)
- 'HTTPS:foo' (case-insensitive prefix regression)

Policy stays separated: host policy (IP literal / localhost reject)
belongs in a future isAllowedImageHost function, not in isHttpsUrl.
Caller's trim() responsibility documented.

Closes v3.5 P2-2, P2-7."
```

---

## Task 2: T5 — `swapTitleInDOM` `@internal` JSDoc 마킹

**Files:**
- Modify: `src/ui/handlers/home.ts:121`

- [ ] **Step 1: 동작 변경 없는 변경이라 테스트 변경 없음 (`swap-title-scoping.spec.ts` 2건 그대로 pass 검증)**

```bash
npm test -- --run tests/unit/swap-title-scoping.spec.ts
```

Expected: 2 pass (Setup-2 baseline 그대로).

- [ ] **Step 2: JSDoc 추가 (line 121 직전)**

기존 (line 121 부근):
```typescript
);

export function swapTitleInDOM(id: string, titleKo: string, root: ParentNode = document): void {
  const titleEl = root.querySelector(`[data-briefing-id="${id}"] .card-title`);
  if (titleEl) titleEl.textContent = titleKo;
}
```

변경 후:
```typescript
);

/**
 * @internal — 자기 모듈 내부 helper. 외부 caller(archive 등) 통합은 v3.7+ 실수요 발생 시.
 * Test가 import하기 위해 export 유지하되, 일반 사용은 home.ts 내부에서만.
 */
export function swapTitleInDOM(id: string, titleKo: string, root: ParentNode = document): void {
  const titleEl = root.querySelector(`[data-briefing-id="${id}"] .card-title`);
  if (titleEl) titleEl.textContent = titleKo;
}
```

- [ ] **Step 3: 테스트 통과 확인 + lint + tsc**

```bash
npm test -- --run tests/unit/swap-title-scoping.spec.ts
npm run lint
npx tsc --noEmit
```

Expected: 2 pass / 0 lint / 0 tsc.

- [ ] **Step 4: 커밋**

```bash
git add src/ui/handlers/home.ts
git commit -m "chore(v3.6 T5): mark swapTitleInDOM as @internal

Export retained for test import; archive integration deferred to v3.7+
when actual demand surfaces. JSDoc signals 'home.ts internal helper, do
not call from external modules' to prevent drift.

Closes v3.5 P2-6."
```

---

## Task 3: T6 — settings-interests-section.spec.ts flake fix

**Files:**
- Modify: `tests/unit/settings-interests-section.spec.ts:46`
- Modify: `tests/unit/settings-interests-section.spec.ts:63`

- [ ] **Step 1: 현 baseline에서 flake 재현 확인**

```bash
for i in 1 2 3 4 5; do npm test -- --run tests/unit/settings-interests-section.spec.ts; done
```

Expected: 5회 중 1~2회 fail on `'renders edit button that opens interests modal on click'` — baseline flake 재현. 단독 실행은 거의 항상 pass이지만 suite-flake로 1/3 빈도. 이미 v3.5 메모리에 명시.

(만약 5회 모두 pass면 race가 잠시 잠잠한 상태일 뿐 — Step 2 이후 안정성 향상이 본 task 가치라 그대로 진행.)

- [ ] **Step 2: line 46 교체**

기존 (line 37-48):
```typescript
  it('renders edit button that opens interests modal on click', async () => {
    const { renderSettings } = await import('../../src/ui/tabs/settings');
    const container = document.createElement('div');
    document.body.append(container);
    renderSettings(container);
    const btn = container.querySelector<HTMLButtonElement>('#editInterestsBtn');
    expect(btn).not.toBeNull();
    btn?.click();
    // Wait for async dynamic import resolution
    await new Promise((r) => setTimeout(r, 50));
    expect(document.querySelector('.dg-modal')).not.toBeNull();
  });
```

변경 후:
```typescript
  it('renders edit button that opens interests modal on click', async () => {
    const { renderSettings } = await import('../../src/ui/tabs/settings');
    const container = document.createElement('div');
    document.body.append(container);
    renderSettings(container);
    const btn = container.querySelector<HTMLButtonElement>('#editInterestsBtn');
    expect(btn).not.toBeNull();
    btn?.click();
    // Poll for dynamic import resolution + DOM mount.
    await vi.waitFor(() => {
      expect(document.querySelector('.dg-modal')).not.toBeNull();
    });
  });
```

`vi`는 이미 line 1에서 import됨 (`import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';`).

- [ ] **Step 3: line 63 교체 (같은 파일 일관성)**

기존 (line 50-66, 're-renders chips when dg:interests:changed fires'):
```typescript
    document.dispatchEvent(new CustomEvent('dg:interests:changed'));

    // Allow handler to run
    await new Promise((r) => setTimeout(r, 20));
    const chips = container.querySelectorAll('.interest-chip');
    expect(chips.length).toBe(3);
```

변경 후:
```typescript
    document.dispatchEvent(new CustomEvent('dg:interests:changed'));

    // Poll until chip count reflects updated interests.
    await vi.waitFor(() => {
      expect(container.querySelectorAll('.interest-chip').length).toBe(3);
    });
```

- [ ] **Step 4: 안정화 검증 — suite 5회 연속 pass**

```bash
for i in 1 2 3 4 5; do npm test -- --run tests/unit/settings-interests-section.spec.ts || echo "FAILED at iter $i"; done
```

Expected: 0 fail across 5 iterations (현재 baseline 1/3 → 0/5).

- [ ] **Step 5: 전체 vitest + lint + tsc 확인**

```bash
npm test -- --run
npm run lint
npx tsc --noEmit
```

Expected: 349 pass (T2 적용 후 합계 유지 — T6은 +0), 0 lint, 0 tsc.

- [ ] **Step 6: 커밋**

```bash
git add tests/unit/settings-interests-section.spec.ts
git commit -m "test(v3.6 T6): stabilize settings-interests-section flake with vi.waitFor

Replaces fixed setTimeout(50) and setTimeout(20) with vi.waitFor polling
for dynamic-import + DOM mount synchronization. Same convention used in
v3.5 F4 (Playwright expect.poll, slack waitForResponse).

Closes new vitest suite-flake (1/3 frequency) deferred from v3.5."
```

---

## Task 4: T1 — `interests.ts` dedup → `state/user.ts` import

**Files:**
- Modify: `src/ui/modals/interests.ts:15-50, 95`

- [ ] **Step 1: 회귀 baseline 측정 (T1 적용 전 settings-interests-section 5회 pass — Task 3 이후 안정)**

```bash
for i in 1 2 3 4 5; do npm test -- --run tests/unit/settings-interests-section.spec.ts; done
```

Expected: 5회 모두 pass (Task 3 안정화 결과). 이게 T1 회귀 검증의 baseline.

- [ ] **Step 2: import 교체 (line 18 부근)**

기존 (line 15-18):
```typescript
import { openModal, closeModal } from './shared';
import { INTERESTS } from '../../utils/categories';
import { escapeHtml } from '../../utils/escapeHtml';
import { showToast } from '../../utils/toast';
```

변경 후 (한 줄 추가):
```typescript
import { openModal, closeModal } from './shared';
import { INTERESTS } from '../../utils/categories';
import { escapeHtml } from '../../utils/escapeHtml';
import { showToast } from '../../utils/toast';
import { getCachedUser, saveUser } from '../../state/user';
```

- [ ] **Step 3: 중복 코드 삭제 (line 20-47, USER_STORAGE/LegacyUser/loadUser/saveUser 모두)**

기존 (line 20-47, 28 lines):
```typescript
const USER_STORAGE = 'user';

interface LegacyUser {
  name: string;
  interests: string[];
  onboardedAt: string;
  streak: number;
  lastActiveDate: string;
  xp: number;
  level: number;
}

function loadUser(): LegacyUser | null {
  try {
    const raw = localStorage.getItem(USER_STORAGE);
    return raw ? (JSON.parse(raw) as LegacyUser) : null;
  } catch {
    return null;
  }
}

function saveUser(u: LegacyUser): void {
  try {
    localStorage.setItem(USER_STORAGE, JSON.stringify(u));
  } catch {
    /* ignore */
  }
}
```

→ **모두 삭제** (state/user.ts가 동일 키 `'user'` + 동일 type 제공).

- [ ] **Step 4: caller 호출 이름 변경 (line 50, 이전 line 50이 삭제 후 line 22 부근으로 이동)**

기존:
```typescript
export function openInterestsModal(): void {
  const user = loadUser();
```

변경 후:
```typescript
export function openInterestsModal(): void {
  const user = getCachedUser();
```

(line 95의 `saveUser({ ...user, interests: newInterests });`는 이름 동일 — import만 바뀌었으므로 그대로 둠.)

- [ ] **Step 5: 회귀 검증 — settings-interests-section 5회 + 단독 실행**

```bash
for i in 1 2 3 4 5; do npm test -- --run tests/unit/settings-interests-section.spec.ts; done
npm test -- --run
```

Expected:
- settings-interests-section: 5회 모두 pass (T1 dedup이 동작 변경 없음 검증)
- 전체 vitest: 349 pass (T1은 +0)

만약 fail 발생 시: state/user.ts와 interests.ts가 다른 localStorage 키를 쓴다는 뜻. 즉시 정지하고 사용자에게 알림.

- [ ] **Step 6: lint + tsc**

```bash
npm run lint
npx tsc --noEmit
```

Expected: 0/0. 만약 "unused import" 에러 발생 시 caller 호출 이름 변경(Step 4)을 다시 확인.

- [ ] **Step 7: 수동 smoke (사용자 단계 — 선택)**

```bash
npm run dev
```

브라우저에서:
1. 관심분야 수정 모달 열기 → 체크박스 변경 → 저장
2. 토스트 노출 확인
3. 모달 재진입 → 변경 사항 보존 확인

(사용자 진행 가능. 자동화 가능 시 Playwright 추가는 본 task scope 외.)

- [ ] **Step 8: 커밋**

```bash
git add src/ui/modals/interests.ts
git commit -m "refactor(v3.6 T1): dedup interests.ts user state into state/user.ts

Removes local LegacyUser interface + loadUser/saveUser duplicating
state/user.ts. Both modules used the same 'user' localStorage key, so
behavior is preserved.

Behavior change: saveUser errors (e.g. QuotaExceededError) now propagate
to click handler (previously silent-caught). Real-world frequency is
near-zero for ~200B JSON; v3.7 saveUser error UX cycle will give all
callers consistent handling.

Closes v3.5 P2-1."
```

---

## Task 5: T4 — MSG entries + i18n Lite 정책 갱신

**Files:**
- Modify: `src/ui/messages.ts`
- Modify: `tests/unit/messages.spec.ts`
- Modify: `src/ui/modals/interests.ts:95-96`
- Modify: `src/ui/handlers/home.ts:707`

- [ ] **Step 1: messages.spec.ts에 키 추가 (실패 테스트)**

기존 (line 25-30, `'exposes expected keys'`):
```typescript
  it('exposes expected keys', () => {
    expect(MSG.TRY_AGAIN).toBeTruthy();
    expect(MSG.SAVE_SUCCESS).toBeTruthy();
    expect(MSG.AI_RESPONSE_FAIL).toBeTruthy();
    expect(MSG.DEMO_API_KEY_PROMPT).toBeTruthy();
  });
```

변경 후:
```typescript
  it('exposes expected keys', () => {
    expect(MSG.TRY_AGAIN).toBeTruthy();
    expect(MSG.SAVE_SUCCESS).toBeTruthy();
    expect(MSG.AI_RESPONSE_FAIL).toBeTruthy();
    expect(MSG.DEMO_API_KEY_PROMPT).toBeTruthy();
    expect(MSG.INTERESTS_UPDATED).toBeTruthy();
    expect(MSG.ANSWER_SAVED).toBeTruthy();
  });
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
npm test -- --run tests/unit/messages.spec.ts
```

Expected: tsc 에러 (MSG.INTERESTS_UPDATED / ANSWER_SAVED 미정의) 또는 runtime fail (undefined).

- [ ] **Step 3: messages.ts 갱신 — JSDoc 정책 + MSG 추가**

기존 (`src/ui/messages.ts` 전체):
```typescript
/**
 * i18n Lite — 최고빈도 한국어 UX 메시지의 단일 진입점.
 *
 * 어조 규칙 (위반 시 lint 또는 PR review에서 catch):
 * - 캐주얼 `~했어요` 톤 (정중체 `~했습니다` 금지, 명사형 `~됨` 금지)
 * - 마침표는 모든 메시지(toast 포함) 끝에 사용
 * - 이모지는 success(✅) / info(ℹ️) / error(❌) 3종만, 그 외 emoji 금지
 *
 * 본 모듈은 Lite — 305개 분산 한국어 문자열 중 빈도 ≥2의 핵심 5개만 통합.
 * Full i18n (`t('key')` 시스템 + 다국어 카탈로그)은 v3.6 별도 사이클.
 */
export const MSG = {
  TRY_AGAIN: '잠시 후 다시 시도해 주세요.',
  SAVE_SUCCESS: '저장되었어요.',
  AI_RESPONSE_FAIL: '지금은 AI 응답을 받지 못했어요. 잠시 후 다시 시도해 주세요.',
  DEMO_API_KEY_PROMPT: 'AI 응답을 받으려면 API 키를 등록해 주세요.',
  partialTranslateFail: (count: number): string =>
    `일부 카드 번역에 실패했어요 (${count}건). 잠시 후 다시 시도해 주세요.`,
} as const;
```

변경 후:
```typescript
/**
 * i18n Lite — 사용자 가시 toast/dialog 텍스트의 단일 진입점.
 *
 * 어조 규칙 (위반 시 lint 또는 PR review에서 catch):
 * - 캐주얼 `~했어요` 톤 (정중체 `~했습니다` 금지, 명사형 `~됨` 금지)
 * - 마침표는 모든 메시지(toast 포함) 끝에 사용
 * - 이모지는 success(✅) / info(ℹ️) / error(❌) 3종만, 그 외 emoji 금지
 *
 * v3.6에서 빈도 ≥2 규칙을 폐기 — 사용자 가시 메시지는 빈도 무관 모두 통합.
 * Full i18n (`t('key')` 시스템 + 다국어 카탈로그)은 v3.7+ 별 사이클로,
 * 본 모듈은 그때까지 staging 역할.
 */
export const MSG = {
  TRY_AGAIN: '잠시 후 다시 시도해 주세요.',
  SAVE_SUCCESS: '저장되었어요.',
  AI_RESPONSE_FAIL: '지금은 AI 응답을 받지 못했어요. 잠시 후 다시 시도해 주세요.',
  DEMO_API_KEY_PROMPT: 'AI 응답을 받으려면 API 키를 등록해 주세요.',
  INTERESTS_UPDATED: '관심 분야가 업데이트되었어요.',
  ANSWER_SAVED: '답변이 저장되었어요.',
  partialTranslateFail: (count: number): string =>
    `일부 카드 번역에 실패했어요 (${count}건). 잠시 후 다시 시도해 주세요.`,
} as const;
```

- [ ] **Step 4: 테스트 통과 확인 — generic 테스트(period/tone)도 자동 커버**

```bash
npm test -- --run tests/unit/messages.spec.ts
```

Expected: 4 pass — period 검증 (모든 string은 `.`로 끝남) / tone 검증 / partialTranslateFail / expected keys (이제 6 키).

- [ ] **Step 5: caller 1 — interests.ts에 MSG import + 토스트 교체**

기존 (line 14-19, import 블록):
```typescript
import { openModal, closeModal } from './shared';
import { INTERESTS } from '../../utils/categories';
import { escapeHtml } from '../../utils/escapeHtml';
import { showToast } from '../../utils/toast';
import { getCachedUser, saveUser } from '../../state/user';
```

변경 후 (한 줄 추가):
```typescript
import { openModal, closeModal } from './shared';
import { INTERESTS } from '../../utils/categories';
import { escapeHtml } from '../../utils/escapeHtml';
import { showToast } from '../../utils/toast';
import { getCachedUser, saveUser } from '../../state/user';
import { MSG } from '../messages';
```

기존 (Task 1 이후 line 67 부근, save click handler):
```typescript
    saveUser({ ...user, interests: newInterests });
    closeModal();
    showToast('관심 분야가 업데이트되었어요');
    document.dispatchEvent(new CustomEvent('dg:interests:changed'));
```

변경 후:
```typescript
    saveUser({ ...user, interests: newInterests });
    closeModal();
    showToast(MSG.INTERESTS_UPDATED);
    document.dispatchEvent(new CustomEvent('dg:interests:changed'));
```

(주의: 새 메시지는 마침표 추가됨 — `'관심 분야가 업데이트되었어요.'` 어조 규칙 일관)

- [ ] **Step 6: caller 2 — home.ts:707 토스트 교체**

home.ts는 이미 line 32 부근에 `import { MSG } from '../messages';` 있음 (`grep -n 'from.*messages' src/ui/handlers/home.ts`로 확인). 추가 import 불필요.

기존 (line 707):
```typescript
  showToast('답변이 저장되었어요.');
```

변경 후:
```typescript
  showToast(MSG.ANSWER_SAVED);
```

- [ ] **Step 7: 전체 vitest + lint + tsc**

```bash
npm test -- --run
npm run lint
npx tsc --noEmit
```

Expected: 349 pass (T4는 +0 신규 케이스, generic 테스트가 자동 커버), 0 lint, 0 tsc.

- [ ] **Step 8: 커밋**

```bash
git add src/ui/messages.ts tests/unit/messages.spec.ts src/ui/modals/interests.ts src/ui/handlers/home.ts
git commit -m "feat(v3.6 T4): retire i18n Lite ≥2-frequency rule + add MSG.INTERESTS_UPDATED/ANSWER_SAVED

Two single-callsite messages were inline-stranded in v3.5 because they
fell below the ≥2 frequency threshold. The threshold proved hard to
enforce in PR review (settings.ts:178 escaped v3.5 entirely). Retire
the heuristic — all user-facing toast/dialog text now lives in MSG.

JSDoc reframed: MSG is staging for v3.7+ Full i18n, not a frequency-
gated subset. INTERESTS_UPDATED gains a period for tone consistency
(prior inline string had none).

Closes v3.5 P2-4."
```

---

## Task 6: T3 — `showPartialTranslateFail` dedup helper

**Files:**
- Create: `tests/unit/translateToast.spec.ts`
- Modify: `src/ui/translateToast.ts`
- Modify: `src/ui/handlers/home.ts:30, 115-117`

- [ ] **Step 1: 신규 테스트 파일 작성 (`tests/unit/translateToast.spec.ts`)**

```typescript
import { describe, it, expect, beforeEach, vi } from 'vitest';

vi.mock('../../src/utils/toast', () => ({
  showToast: vi.fn(),
}));

import { showToast } from '../../src/utils/toast';
import {
  showPartialTranslateFail,
  __resetToastDedupForTest,
} from '../../src/ui/translateToast';

describe('showPartialTranslateFail dedup', () => {
  beforeEach(() => {
    vi.clearAllMocks();
    __resetToastDedupForTest();
  });

  it('shows toast once when called twice with same failedCount (dedup hit)', () => {
    showPartialTranslateFail(3);
    showPartialTranslateFail(3);
    expect(showToast).toHaveBeenCalledTimes(1);
    expect(showToast).toHaveBeenCalledWith(
      expect.stringContaining('(3건)'),
    );
  });

  it('shows toast twice when called with different failedCount (dedup miss)', () => {
    showPartialTranslateFail(3);
    showPartialTranslateFail(2);
    expect(showToast).toHaveBeenCalledTimes(2);
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
npm test -- --run tests/unit/translateToast.spec.ts
```

Expected: tsc 에러 (`showPartialTranslateFail` 미export).

- [ ] **Step 3: 구현 — `src/ui/translateToast.ts`에 helper 추가**

기존 파일 끝 (line 50, `__resetToastDedupForTest` 직전 또는 직후) 위치에 추가. helper는 `lastShownError`/`showToast`/`MSG`를 모두 모듈 내에서 이미 사용 가능하므로 import 추가 불필요.

기존 (line 39-50):
```typescript
export function showTranslateError(err: unknown): void {
  // ... 기존 구현 ...
}

// Production reset — called from settings.ts when the user saves a new API
// key, so a stale error message (e.g. 401) doesn't suppress a future toast
// for an unrelated failure.
export function resetToastDedup(): void {
  lastShownError = null;
}

// Test-only escape hatch (no production caller).
export function __resetToastDedupForTest(): void {
  lastShownError = null;
}
```

변경 후 — `showTranslateError` 직후, `resetToastDedup` 직전에 helper 삽입:
```typescript
export function showTranslateError(err: unknown): void {
  // ... 기존 구현 그대로 ...
}

/**
 * Partial-failure toast for the translate queue's onDrain. Same dedup
 * mechanism as showCapToast / showTranslateError — identical messages
 * within session collapse to a single toast.
 */
export function showPartialTranslateFail(failedCount: number): void {
  const msg = MSG.partialTranslateFail(failedCount);
  if (lastShownError === msg) return;
  lastShownError = msg;
  showToast(msg);
}

// Production reset — ...
export function resetToastDedup(): void {
  lastShownError = null;
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
npm test -- --run tests/unit/translateToast.spec.ts
```

Expected: 2 pass.

- [ ] **Step 5: home.ts caller 교체**

기존 (line 30):
```typescript
import { showCapToast, showTranslateError } from '../translateToast';
```

변경 후:
```typescript
import { showCapToast, showTranslateError, showPartialTranslateFail } from '../translateToast';
```

기존 (line 115-117):
```typescript
    onDrain: ({ failedCount }) => {
      showToast(MSG.partialTranslateFail(failedCount));
    },
```

변경 후:
```typescript
    onDrain: ({ failedCount }) => {
      showPartialTranslateFail(failedCount);
    },
```

(`TranslateQueue.run()`이 onDrain 호출을 try/catch로 감쌈 — `translateQueue.ts:59-60`. caller side wrapping 불필요.)

- [ ] **Step 6: 전체 vitest + lint + tsc**

```bash
npm test -- --run
npm run lint
npx tsc --noEmit
```

Expected: 349 + 2 = **351 pass**, 0 lint, 0 tsc.

만약 lint가 home.ts에서 `MSG.partialTranslateFail` 미사용을 보고할 수 있음 — `MSG`는 `MSG.DEMO_API_KEY_PROMPT`(line 712)나 T4에서 추가된 `MSG.ANSWER_SAVED`(line 707)에서 여전히 사용되므로 import는 유지. 의심 시 grep으로 확인:

```bash
grep -n 'MSG\.' src/ui/handlers/home.ts
```

- [ ] **Step 7: 커밋**

```bash
git add tests/unit/translateToast.spec.ts src/ui/translateToast.ts src/ui/handlers/home.ts
git commit -m "feat(v3.6 T3): add showPartialTranslateFail dedup helper

Routes home.ts onDrain through translateToast.ts so identical
'(N건) 실패' messages collapse to a single toast within session,
matching the pattern of showCapToast / showTranslateError.

Different failedCount values still surface separately — each batch's
result is reported. TranslateQueue already wraps onDrain in try/catch
(translateQueue.ts:59-60, v3.5 I1), so caller-side wrapping is omitted.

Closes v3.5 P2-3 + P2-5."
```

---

## Cycle Verification (배포 전 final gate)

- [ ] **Verify-1: 전체 테스트 + 빌드**

```bash
npm run lint
npx tsc --noEmit
npm test -- --run
npm run test:smoke
npm run build
```

Expected:
- lint / tsc: **0 / 0**
- vitest: **351 pass** (343 + 6 url + 2 translateToast)
- Playwright: **79 pass** (회귀 검증 — UI 무변)
- build: JS 63.91 kB ± 2 kB (예상 net ~-0.05 kB)

- [ ] **Verify-2: bundle size delta 측정**

```bash
ls -la dist/assets/*.js | awk '{print $5, $9}'
```

기준: v3.5 baseline JS gzip 22.07 kB → v3.6 ±2 kB. 초과 시 spec §A.2 / R7에 따라 T3 helper inline화 검토.

- [ ] **Verify-3: 수동 smoke (사용자)**

```bash
npm run dev
```

브라우저에서:
1. 관심분야 수정 모달 → 저장 → 토스트 `'관심 분야가 업데이트되었어요.'` 노출 + 재진입 시 데이터 보존 (T1 + T4)
2. home tab 답변 입력 → 토스트 `'답변이 저장되었어요.'` 노출 (T4)
3. 외부 https RSS 카드 이미지 정상 로드 (T2 강화 회귀 없음)
4. settings tab → 모달 → 닫기 → 재진입 5회 (T6 안정화 + T1 dedup 회귀 없음)
5. (옵션) 번역 큐 partial fail — vitest로 커버되니 어려우면 skip

- [ ] **Verify-4: 독립 code-reviewer agent (CLAUDE.md 표준 리뷰)**

`superpowers:code-reviewer` 1회. 6 commits + setup이라 4~10 commits 범주 (표준 리뷰 적용). P0/P1/P2 분류.

- [ ] **Verify-5: P0=0 + P1 in-cycle 반영 후 사용자 머지 승인 대기**

- P0 발생 시: 즉시 정지 + 사용자 알림
- P1 발생 시: 별 commit으로 in-cycle 반영 (review-fix commit 0~2건)
- P2 발생 시: v3.7 backlog로 메모리에 기록

- [ ] **Verify-6: 머지 + 배포 (사용자 승인 후)**

```bash
git checkout main
git merge --no-ff feat/v3.6-polish -m "Merge feat/v3.6-polish (v3.6 Polish)"
npm run build
firebase deploy --only hosting
```

- [ ] **Verify-7: 메모리 업데이트 (out-of-cycle, 배포 후)**

신규 메모리 파일: `~/.claude/projects/.../memory/project_daily_growth_v36_polish.md`
+ `MEMORY.md` 인덱스에 신규 라인 추가
+ v3.5 메모리에 "v3.6에서 P2 7건 + flake 모두 소진" 마킹

---

## Plan Self-Review

자체 점검:

**1. Spec coverage:** §5 T1~T6 모두 본 plan의 Task 1~6에 1:1 매핑. §A.1 vitest delta breakdown(+8)이 plan 합계(+6 url + 2 translateToast = +8)와 일치. §6 execution order(T2/T5/T6/T1/T4/T3)가 plan Task 순서와 일치.

**2. Placeholder scan:** "TBD"/"TODO"/"등 적절히" 패턴 grep 결과 0건. 모든 step에 실 코드 또는 실 명령어 포함.

**3. Type consistency:** `getCachedUser` (T1, T4) / `saveUser` (T1, T4) / `MSG` (T4, T6) / `showPartialTranslateFail` (T3, T6) 시그니처 모두 plan 내 일관. `LegacyUser` 삭제 후 잔존 참조 없음 (interests.ts 삭제 + state/user.ts에 LegacyUser export 잔존하므로 다른 caller가 import 시 동작).

**4. 동작 변경 명시 누락:** T1 saveUser 에러 propagate를 commit message + spec R1에 명시 ✓.

**5. Edge case:** T2 vitest의 백슬래시 escape (`'https:\\\\evil.com'` → 실제 2 백슬래시) plan에 명시 ✓.
