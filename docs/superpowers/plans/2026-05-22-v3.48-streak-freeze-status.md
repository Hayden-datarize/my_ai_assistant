# v3.48 Streak Freeze 현황 패널 + 활동 내역 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Streak Freeze의 가시성(보유량 · 다음 충전 · 충전/사용 활동 내역)을 settings에 추가한다. schema v9→v10에 `freezeHistory` 배열을 추가하고 settings에 "❄️ Streak Freeze" 섹션을 둔다.

**Architecture:** xpHistory(v8) 선례를 1:1로 따라 schema bump(v9→v10) + recordDailyAnswer write + read helper + settings read-render. freezeEngine 시그니처는 무변경(regen 전후 count 차이로 earned 산출). 신규 event 0.

**Tech Stack:** Vite + TypeScript (vanilla), Vitest (jsdom), Playwright.

---

## File Structure

| 파일 | 책임 | 변경 |
|---|---|---|
| `src/state/user.ts` | User 타입 + hydrate/persist/validation + recordDailyAnswer + read helper | FreezeHistoryEntry 타입 + freezeHistory 필드 + sv 10 + 4 spot + write + getFreezeHistory |
| `src/state/migration.ts` | 버전별 migration chain | migrateUserToV10 |
| `src/state/freezeEngine.ts` | freeze regen/consume + 상수 | getNextFreezeEtaDays (FREEZE_CAP/SEVEN_DAYS_MS 재사용) |
| `src/ui/tabs/settings.ts` | settings 렌더 + wire | "❄️ Streak Freeze" 섹션 + wireFreezeSection |
| `tests/state/migration-v10.spec.ts` (신규) | v10 migration | chain superset + idempotent + 손상 |
| `tests/state/user-validation-v10.spec.ts` (신규) | validation guard | freezeHistory entry valid/invalid |
| `tests/state/freeze-history-write.spec.ts` (신규) | write + read | recordDailyAnswer earned/consumed push + getFreezeHistory + getNextFreezeEtaDays |
| `tests/smoke/settings-freeze.spec.ts` (신규) | settings UI | "❄️ Streak Freeze" 섹션 노출 |

---

### Task 1: Schema v9→v10 — freezeHistory 타입 + migration + validation

**Files:**
- Modify: `src/state/user.ts` (타입 line 39~42 근처, User line 155/157, hydrate 190/199/223, validation 258/274/285/293/311 범위 + 신규 guard)
- Modify: `src/state/migration.ts` (migrateUserToV10 추가)
- Test: `tests/state/migration-v10.spec.ts` (신규), `tests/state/user-validation-v10.spec.ts` (신규)

- [ ] **Step 1: Write failing migration tests**

`tests/state/migration-v10.spec.ts` (신규):

```ts
import { describe, it, expect } from 'vitest';
import { migrateUserToV10 } from '../../src/state/migration';

describe('migrateUserToV10 (v3.48)', () => {
  it('v9 user → v10 + freezeHistory []', () => {
    const v9 = { schemaVersion: 9, streakFreeze: { count: 2, lastEarnedAt: '2026-05-20' } };
    const out = migrateUserToV10(v9) as unknown as Record<string, unknown>;
    expect(out.schemaVersion).toBe(10);
    expect(out.freezeHistory).toEqual([]);
  });

  it('idempotent — v10 user 그대로 (freezeHistory 보존)', () => {
    const v10 = { schemaVersion: 10, freezeHistory: [{ date: '2026-05-20', kind: 'earned', amount: 1 }] };
    const out = migrateUserToV10(v10) as unknown as Record<string, unknown>;
    expect(out.schemaVersion).toBe(10);
    expect(out.freezeHistory).toEqual([{ date: '2026-05-20', kind: 'earned', amount: 1 }]);
  });

  it('손상 freezeHistory(non-array) → []', () => {
    const v9 = { schemaVersion: 9, freezeHistory: 'corrupt' };
    const out = migrateUserToV10(v9) as unknown as Record<string, unknown>;
    expect(out.freezeHistory).toEqual([]);
  });

  it('chain superset — v5 user도 v10 도달 + freezeHistory []', () => {
    const v5 = { schemaVersion: 5, streakFreeze: { count: 1, lastEarnedAt: '2026-05-01' } };
    const out = migrateUserToV10(v5) as unknown as Record<string, unknown>;
    expect(out.schemaVersion).toBe(10);
    expect(Array.isArray(out.freezeHistory)).toBe(true);
  });
});
```

- [ ] **Step 2: Run → FAIL**

Run: `npx vitest run tests/state/migration-v10.spec.ts`
Expected: FAIL (`migrateUserToV10` not exported).

- [ ] **Step 3: Add migrateUserToV10 to migration.ts**

`src/state/migration.ts` — `migrateUserToV9` 함수 정의 **다음**에 추가:

```ts
/**
 * v3.48: v9→v10 lazy migration — freezeHistory 배열 도입 (xpHistory v8 선례).
 * chain superset — v10 user는 same reference (downgrade barrier).
 */
export function migrateUserToV10(u: unknown): User {
  const r = u as Record<string, unknown> & { schemaVersion?: number; freezeHistory?: unknown };
  if (r.schemaVersion === 10) return r as unknown as User; // idempotent / downgrade barrier
  const v9 = migrateUserToV9(u);
  const v9r = v9 as unknown as Record<string, unknown>;
  const freezeHistory = Array.isArray(v9r.freezeHistory) ? v9r.freezeHistory : [];
  return { ...v9, schemaVersion: 10 as const, freezeHistory } as unknown as User;
}
```

- [ ] **Step 4: Run migration tests → PASS**

Run: `npx vitest run tests/state/migration-v10.spec.ts`
Expected: PASS (4/4).

- [ ] **Step 5: Add FreezeHistoryEntry type + User field + 4 user.ts spots**

`src/state/user.ts`:

**(a) 타입** — `XpHistoryEntry` interface(line 39~42) **뒤**에:

```ts
/** v3.48: Streak Freeze 활동 내역 entry — 충전/사용 통합 타임라인 (KST date anchor). */
interface FreezeHistoryEntry {
  date: string;                 // KST 'YYYY-MM-DD' (getKstDateStr)
  kind: 'earned' | 'consumed';  // 충전 / 사용
  amount: number;               // >0 정수
}
```

**(b) User 타입** — line 155 `schemaVersion: 9;` → `schemaVersion: 10;` + line 157 `xpHistory?` **뒤**에:

```ts
  // v3.48 NEW: optional (lower-version fixture 호환 + migrate default [] 보장 + v10 validation array required).
  freezeHistory?: FreezeHistoryEntry[];
```

**(c) hydrate** (getCachedUser):
- line 190 `(sv0 >= 2 && sv0 <= 9)` → `(sv0 >= 2 && sv0 <= 10)`
- line 199 다음 줄에 추가: `user = migrateUserToV10(user);  // v3.48: v9→v10 lazy migration`
- migration.ts import에 `migrateUserToV10` 추가 (기존 `migrateUserToV9` import 옆).

**(d) persist** — line 223 `parsed?.schemaVersion !== 9` → `!== 10`.

- [ ] **Step 6: Write failing validation tests**

`tests/state/user-validation-v10.spec.ts` (신규):

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { getCachedUser } from '../../src/state/user';

// isValidUserShape는 비-export — getCachedUser(localStorage)로 간접 검증 (corrupt → null).
function seed(u: unknown) { localStorage.setItem('user', JSON.stringify(u)); }

const baseV10 = {
  schemaVersion: 10,
  name: 'T', interests: ['leadership'], streak: 1, lastActiveDate: '2026-05-20',
  xp: 0, earnedBadges: {}, gamificationMigrated: true, xpHistory: [],
  plantStateByInterest: {}, streakFreeze: { count: 2, lastEarnedAt: '2026-05-20' },
  insights: [], answers: [], gardenBackfilled: true,
  missions: { active: [], cumulative: { dailyCount: 0, weeklyCount: 0, monthlyCount: 0 }, lastDailySeed: '', currentWeekIso: '', currentMonthIso: '' },
  freezeHistory: [],
};

describe('v10 freezeHistory validation (v3.48)', () => {
  beforeEach(() => localStorage.clear());

  it('valid v10 freezeHistory entry → user 반환', () => {
    seed({ ...baseV10, freezeHistory: [{ date: '2026-05-20', kind: 'earned', amount: 1 }] });
    expect(getCachedUser()).not.toBeNull();
  });

  it('amount NaN → corrupt(null)', () => {
    seed({ ...baseV10, freezeHistory: [{ date: '2026-05-20', kind: 'earned', amount: NaN }] });
    expect(getCachedUser()).toBeNull();
  });

  it('kind 오타 → corrupt(null)', () => {
    seed({ ...baseV10, freezeHistory: [{ date: '2026-05-20', kind: 'bogus', amount: 1 }] });
    expect(getCachedUser()).toBeNull();
  });

  it('amount<=0 → corrupt(null)', () => {
    seed({ ...baseV10, freezeHistory: [{ date: '2026-05-20', kind: 'consumed', amount: 0 }] });
    expect(getCachedUser()).toBeNull();
  });

  it('freezeHistory non-array → corrupt(null)', () => {
    seed({ ...baseV10, freezeHistory: 'x' });
    expect(getCachedUser()).toBeNull();
  });
});
```

> 주의: `getCachedUser`는 corrupt 시 `notifyCorruption()`(toast) 호출 → jsdom에서 안전(DOM 없으면 no-op). baseV10 shape은 isValidUserShape 전 필드 충족해야 하므로 위 fixture 유지.

- [ ] **Step 7: Run → FAIL (validation 미반영)**

Run: `npx vitest run tests/state/user-validation-v10.spec.ts`
Expected: 일부 FAIL (현재 v10 분기 없음 → sv 범위 밖이라 통과/실패 혼재).

- [ ] **Step 8: Update validation ranges + add freezeHistory guard**

`src/state/user.ts` isValidUserShape:
- line 258 `sv >= 4 && sv <= 9` → `<= 10`
- line 274 `sv >= 5 && sv <= 9` → `<= 10`
- line 285 `sv >= 6 && sv <= 9` → `<= 10`
- line 293 `sv >= 7 && sv <= 9` → `<= 10`
- line 311 `sv >= 8 && sv <= 9` → `<= 10`
- xpHistory guard 블록(line 319 `}`) **뒤**에 신규:

```ts
  // v3.48: v10 신규 freezeHistory 배열 guard (silent NaN 차단, v3.12 lesson).
  if (sv >= 10 && sv <= 10) {
    if (!Array.isArray(r.freezeHistory)) return false;
    for (const h of r.freezeHistory as unknown[]) {
      if (!h || typeof h !== 'object') return false;
      const he = h as Record<string, unknown>;
      if (typeof he.date !== 'string' || he.date.length === 0) return false;
      if (he.kind !== 'earned' && he.kind !== 'consumed') return false;
      if (typeof he.amount !== 'number' || !Number.isFinite(he.amount) || he.amount <= 0) return false;
    }
  }
```

- [ ] **Step 9: Run both spec → PASS + tsc/lint**

Run: `npx vitest run tests/state/migration-v10.spec.ts tests/state/user-validation-v10.spec.ts && npx tsc --noEmit && npm run lint`
Expected: PASS, tsc 0, lint 0.

- [ ] **Step 10: Commit**

```bash
git add src/state/user.ts src/state/migration.ts tests/state/migration-v10.spec.ts tests/state/user-validation-v10.spec.ts
git commit -m "feat(v3.48 T1): schema v9→v10 freezeHistory (xpHistory 선례 chain superset + validation)"
```

---

### Task 2: freezeHistory write (recordDailyAnswer) + getFreezeHistory

**Files:**
- Modify: `src/state/user.ts` (recordDailyAnswer line 360~392, read helper getXpHistory 근처)
- Test: `tests/state/freeze-history-write.spec.ts` (신규)

- [ ] **Step 1: Write failing write/read tests**

`tests/state/freeze-history-write.spec.ts` (신규):

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { saveUser, recordDailyAnswer, getFreezeHistory } from '../../src/state/user';
import { mkUser } from './userFixture';

describe('freezeHistory write + read (v3.48)', () => {
  beforeEach(() => localStorage.clear());

  it('regen 발생(7일 경과, lastActiveDate 과거) → earned entry push', () => {
    // lastEarnedAt 8일 전 → regenerateFreeze가 +1. count 1 → 2.
    const eightDaysAgo = new Date(Date.now() - 8 * 86400_000).toISOString().slice(0, 10);
    const yesterday = new Date(Date.now() - 86400_000).toISOString().slice(0, 10);
    saveUser(mkUser({
      streak: 3, lastActiveDate: yesterday,
      streakFreeze: { count: 1, lastEarnedAt: eightDaysAgo },
    }));
    recordDailyAnswer(10);
    const hist = getFreezeHistory();
    expect(hist.some((h) => h.kind === 'earned' && h.amount >= 1)).toBe(true);
  });

  it('consume 발생(gap 1, freeze 보유) → consumed entry push', () => {
    // lastActiveDate 2일 전 → gap 1 → consumeFreezeForGap(1) consumed 1, preserved.
    const twoDaysAgo = new Date(Date.now() - 2 * 86400_000).toISOString().slice(0, 10);
    const today = new Date().toISOString().slice(0, 10);
    saveUser(mkUser({
      streak: 3, lastActiveDate: twoDaysAgo,
      streakFreeze: { count: 2, lastEarnedAt: today },  // cap → regen 0
    }));
    recordDailyAnswer(10);
    const hist = getFreezeHistory();
    expect(hist.some((h) => h.kind === 'consumed' && h.amount === 1)).toBe(true);
  });

  it('regen 0 + consume 0 (오늘 첫 답변, gap 0) → push 없음', () => {
    const today = new Date().toISOString().slice(0, 10);
    const yesterday = new Date(Date.now() - 86400_000).toISOString().slice(0, 10);
    saveUser(mkUser({
      streak: 1, lastActiveDate: yesterday,
      streakFreeze: { count: 2, lastEarnedAt: today },  // cap → regen 0; gap 0 → consume 0
    }));
    recordDailyAnswer(10);
    expect(getFreezeHistory()).toEqual([]);
  });

  it('getFreezeHistory — 최근 desc + limit', () => {
    const today = new Date().toISOString().slice(0, 10);
    saveUser(mkUser({
      streakFreeze: { count: 0, lastEarnedAt: today },
      freezeHistory: [
        { date: '2026-05-01', kind: 'earned', amount: 1 },
        { date: '2026-05-08', kind: 'consumed', amount: 1 },
        { date: '2026-05-15', kind: 'earned', amount: 2 },
      ],
    }));
    const hist = getFreezeHistory(2);
    expect(hist).toHaveLength(2);
    expect(hist[0]!.date).toBe('2026-05-15');  // 최신 first
    expect(hist[1]!.date).toBe('2026-05-08');
  });
});
```

> `mkUser` fixture가 `freezeHistory`/`streakFreeze` override를 지원하는지 확인 — 미지원 시 fixture에 spread 허용 추가(아래 Step 3 참고).

- [ ] **Step 2: Run → FAIL**

Run: `npx vitest run tests/state/freeze-history-write.spec.ts`
Expected: FAIL (`getFreezeHistory` not exported; write 미반영).

- [ ] **Step 3: (필요 시) mkUser fixture에 freezeHistory/streakFreeze override 확인**

`tests/state/userFixture.ts` 확인 — `mkUser`가 partial override를 spread하면 OK. 만약 schemaVersion이 9 하드코딩이면 10으로 갱신 + freezeHistory default `[]` 추가. (실측 후 필요한 만큼만 수정.)

- [ ] **Step 4: Implement write in recordDailyAnswer**

`src/state/user.ts` recordDailyAnswer:

**(a)** line 362 `let freezePreserved = true;` **뒤**에:

```ts
  let freezeEarned = 0;  // v3.48: regen grant 캡처 (history earned entry용)
```

**(b)** line 367 `regenerateFreeze(u, now);`를 before/after 캡처로 교체:

```ts
    const freezeBefore = u.streakFreeze.count;
    regenerateFreeze(u, now);
    freezeEarned = u.streakFreeze.count - freezeBefore;  // v3.48
```

**(c)** line 392 `u.xpHistory.push(...)` **뒤**에:

```ts
  // v3.48: freeze 활동 내역 — 충전/사용 통합 (같은 atomic write에 포함).
  if (!Array.isArray(u.freezeHistory)) u.freezeHistory = [];
  if (freezeEarned > 0) u.freezeHistory.push({ date: today, kind: 'earned', amount: freezeEarned });
  if (freezeConsumed > 0) u.freezeHistory.push({ date: today, kind: 'consumed', amount: freezeConsumed });
```

> `freezeConsumed`는 기존 line 379에서 이미 산출됨. earned/consumed 모두 `u.lastActiveDate !== today` 블록에서만 0 초과 가능 → 같은 날 재답변 시 push 없음(정상).

- [ ] **Step 5: Implement getFreezeHistory**

`src/state/user.ts` — `getXpHistory` 함수 **근처**(read helper 영역)에:

```ts
/** v3.48: Streak Freeze 활동 내역 view — 최근 entry desc, limit slice. */
export interface FreezeHistoryView {
  date: string;
  kind: 'earned' | 'consumed';
  amount: number;
}

export function getFreezeHistory(limit = 10): FreezeHistoryView[] {
  const user = getCachedUser();
  const raw = user?.freezeHistory ?? [];
  if (raw.length === 0) return [];
  return raw.slice(-limit).reverse().map((h) => ({ date: h.date, kind: h.kind, amount: h.amount }));
}
```

- [ ] **Step 6: Run → PASS + N=5 + tsc/lint**

Run: `npx vitest run tests/state/freeze-history-write.spec.ts && for i in 1 2 3 4 5; do npx vitest run tests/state/freeze-history-write.spec.ts 2>&1 | grep -E "Tests +[0-9]"; done && npx tsc --noEmit && npm run lint`
Expected: PASS, 5/5, tsc 0, lint 0.

- [ ] **Step 7: Commit**

```bash
git add src/state/user.ts tests/state/freeze-history-write.spec.ts tests/state/userFixture.ts
git commit -m "feat(v3.48 T2): freezeHistory write (recordDailyAnswer earned/consumed) + getFreezeHistory"
```

---

### Task 3: getNextFreezeEtaDays (다음 충전 계산)

**Files:**
- Modify: `src/state/freezeEngine.ts` (FREEZE_CAP/SEVEN_DAYS_MS 재사용)
- Test: `tests/state/freezeEngine.spec.ts` (기존 확장)

- [ ] **Step 1: Write failing tests**

`tests/state/freezeEngine.spec.ts`에 append:

```ts
import { getNextFreezeEtaDays } from '../../src/state/freezeEngine';

describe('getNextFreezeEtaDays (v3.48)', () => {
  const at = (iso: string) => new Date(iso);

  it('count=2 (cap) → null (가득 참)', () => {
    const u = { streakFreeze: { count: 2, lastEarnedAt: '2026-05-20' } };
    expect(getNextFreezeEtaDays(u, at('2026-05-22T00:00:00+09:00'))).toBeNull();
  });

  it('count<2, 7일 중 2일 경과 → 약 5일', () => {
    const u = { streakFreeze: { count: 1, lastEarnedAt: '2026-05-20' } };
    expect(getNextFreezeEtaDays(u, at('2026-05-22T00:00:00+09:00'))).toBe(5);
  });

  it('count<2, 이미 7일 경과(remaining<0) → 0', () => {
    const u = { streakFreeze: { count: 0, lastEarnedAt: '2026-05-01' } };
    expect(getNextFreezeEtaDays(u, at('2026-05-22T00:00:00+09:00'))).toBe(0);
  });

  it('lastEarnedAt 손상 → 0', () => {
    const u = { streakFreeze: { count: 0, lastEarnedAt: 'not-a-date' } };
    expect(getNextFreezeEtaDays(u, at('2026-05-22T00:00:00+09:00'))).toBe(0);
  });
});
```

- [ ] **Step 2: Run → FAIL**

Run: `npx vitest run tests/state/freezeEngine.spec.ts`
Expected: FAIL (`getNextFreezeEtaDays` not exported).

- [ ] **Step 3: Implement in freezeEngine.ts**

`src/state/freezeEngine.ts` 끝에 추가 (기존 `FREEZE_CAP`, `SEVEN_DAYS_MS` 상수 재사용):

```ts
/**
 * v3.48: 다음 ❄️ 충전까지 남은 일수 (예상 — regen은 답변 시점 실행).
 * count >= cap이면 null (가득 참). lastEarnedAt 손상 또는 이미 경과 시 0.
 */
export function getNextFreezeEtaDays(
  u: { streakFreeze: { count: number; lastEarnedAt: string } },
  now: Date = new Date(),
): number | null {
  if (u.streakFreeze.count >= FREEZE_CAP) return null;
  const lastMs = Date.parse(`${u.streakFreeze.lastEarnedAt}T00:00:00+09:00`);
  if (!Number.isFinite(lastMs)) return 0;
  const remainingMs = lastMs + SEVEN_DAYS_MS - now.getTime();
  return Math.max(0, Math.ceil(remainingMs / 86400_000));
}
```

- [ ] **Step 4: Run → PASS + tsc/lint**

Run: `npx vitest run tests/state/freezeEngine.spec.ts && npx tsc --noEmit && npm run lint`
Expected: PASS, tsc 0, lint 0.

- [ ] **Step 5: Commit**

```bash
git add src/state/freezeEngine.ts tests/state/freezeEngine.spec.ts
git commit -m "feat(v3.48 T3): getNextFreezeEtaDays (cap→null, 손상/경과→0)"
```

---

### Task 4: Settings "❄️ Streak Freeze" 섹션 + wireFreezeSection

**Files:**
- Modify: `src/ui/tabs/settings.ts` (renderSettings innerHTML + wireFreezeSection)
- Modify: `src/styles/components/` 또는 settings CSS (최소 rule)
- Test: `tests/ui/settings-freeze.spec.ts` (신규, jsdom render)

- [ ] **Step 1: Write failing render test**

`tests/ui/settings-freeze.spec.ts` (신규):

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { renderSettings } from '../../src/ui/tabs/settings';
import { saveUser } from '../../src/state/user';
import { mkUser } from '../state/userFixture';

describe('settings freeze section (v3.48)', () => {
  beforeEach(() => { document.body.replaceChildren(); localStorage.clear(); });

  it('보유량 + 다음 충전 표시 (count 1, cap 미만)', () => {
    const eightDaysAgo = new Date(Date.now() - 8 * 86400_000).toISOString().slice(0, 10);
    saveUser(mkUser({ streakFreeze: { count: 1, lastEarnedAt: eightDaysAgo } }));
    const c = document.createElement('div');
    renderSettings(c);
    const status = c.querySelector('#freezeStatus')!;
    expect(status.textContent).toContain('❄️');
    expect(status.textContent).toContain('1');
  });

  it('count=2 (cap) → "가득 참"', () => {
    const today = new Date().toISOString().slice(0, 10);
    saveUser(mkUser({ streakFreeze: { count: 2, lastEarnedAt: today } }));
    const c = document.createElement('div');
    renderSettings(c);
    expect(c.querySelector('#freezeStatus')!.textContent).toContain('가득');
  });

  it('활동 내역 — entry 표시 / 빈 내역 안내', () => {
    const today = new Date().toISOString().slice(0, 10);
    saveUser(mkUser({
      streakFreeze: { count: 0, lastEarnedAt: today },
      freezeHistory: [{ date: '2026-05-15', kind: 'earned', amount: 1 }],
    }));
    const c = document.createElement('div');
    renderSettings(c);
    const hist = c.querySelector('#freezeHistory')!;
    expect(hist.textContent).toContain('충전');
  });
});
```

- [ ] **Step 2: Run → FAIL**

Run: `npx vitest run tests/ui/settings-freeze.spec.ts`
Expected: FAIL (`#freezeStatus` null).

- [ ] **Step 3: Add section to renderSettings innerHTML**

`src/ui/tabs/settings.ts` renderSettings의 innerHTML에서 "데이터 삭제" `<section>` **앞**에 추가 (문자열 내 삽입):

```html
<section class="settings-group"><div class="settings-group-title">❄️ Streak Freeze</div><div id="freezeStatus" class="settings-freeze-status" style="font-size:0.95rem;"></div><div id="freezeHistory" class="settings-freeze-history" style="margin-top:8px;font-size:0.85rem;color:var(--text-secondary);"></div><details style="margin-top:12px;"><summary style="cursor:pointer;font-size:0.9rem;">Streak Freeze란?</summary><p style="font-size:0.85rem;color:var(--text-secondary);margin-top:8px;line-height:1.6;">결석한 날 연속 기록을 대신 지켜주는 보호막이에요. 7일마다 1개씩 자동 충전되고(최대 2개), 빠진 날 자동으로 사용돼요.</p></details></section>
```

- [ ] **Step 4: Add wireFreezeSection + call**

import 추가 (settings.ts 상단):

```ts
import { loadUserData, getFreezeHistory } from '../../state/user';
import { getNextFreezeEtaDays } from '../../state/freezeEngine';
import { escapeHtml } from '../../utils/escapeHtml';
```

> `getSaveErrorMessage`는 이미 `'../../state/user'`에서 import 중 → `loadUserData, getFreezeHistory`를 같은 import 문에 합칠 것.

renderSettings의 `wireTranslateSection(container);`(line 43) **뒤**에 `wireFreezeSection(container);` 추가.

함수 정의 (wireTranslateSection 근처):

```ts
function wireFreezeSection(container: HTMLElement): void {
  const statusEl = container.querySelector<HTMLDivElement>('#freezeStatus');
  const histEl = container.querySelector<HTMLDivElement>('#freezeHistory');
  if (!statusEl || !histEl) return;
  const u = loadUserData();
  if (!u) { statusEl.textContent = '보유 정보를 불러올 수 없어요.'; return; }

  const count = u.streakFreeze.count;
  const eta = getNextFreezeEtaDays(u);
  const etaText = eta === null
    ? '가득 참 (2/2)'
    : eta === 0
      ? '곧 충전돼요 (답변 시)'
      : `다음 ❄️까지 약 ${eta}일 (답변 시 충전)`;
  statusEl.textContent = `보유: ❄️ ${count} / 2 · ${etaText}`;

  const hist = getFreezeHistory(10);
  if (hist.length === 0) {
    histEl.textContent = '아직 내역이 없어요.';
    return;
  }
  const rows = hist.map((h) => {
    const md = `${Number(h.date.slice(5, 7))}/${Number(h.date.slice(8, 10))}`;
    const label = h.kind === 'earned'
      ? `❄️ +${h.amount} 충전`
      : `❄️ -${h.amount} 사용 (연속 유지)`;
    return `<div>${escapeHtml(md)} · ${escapeHtml(label)}</div>`;
  }).join('');
  // eslint-disable-next-line no-restricted-syntax -- md/label은 숫자+정적 문자열, escapeHtml 적용
  histEl.innerHTML = rows;
}
```

- [ ] **Step 5: Run → PASS + tsc/lint**

Run: `npx vitest run tests/ui/settings-freeze.spec.ts && npx tsc --noEmit && npm run lint`
Expected: PASS, tsc 0, lint 0.

- [ ] **Step 6: build (CSS 유효성)**

Run: `npm run build`
Expected: 빌드 성공.

- [ ] **Step 7: Commit**

```bash
git add src/ui/tabs/settings.ts tests/ui/settings-freeze.spec.ts
git commit -m "feat(v3.48 T4): settings ❄️ Streak Freeze 섹션 (보유/다음충전/활동내역 read-render)"
```

---

### Task 5: Smoke — settings 섹션 노출

**Files:**
- Test: `tests/smoke/settings-freeze.spec.ts` (신규)

- [ ] **Step 1: Write smoke test**

`tests/smoke/settings-freeze.spec.ts` (신규):

```ts
import { test, expect } from '@playwright/test';
import { primeOnboardedUser } from '../helpers/seed';

test('v3.48: settings ❄️ Streak Freeze 섹션 노출 + 보유량', async ({ page }) => {
  await primeOnboardedUser(page, { interests: ['leadership'], schemaVersion: 4 });
  await page.addInitScript(() => {
    const raw = localStorage.getItem('user');
    if (!raw) return;
    const u = JSON.parse(raw);
    u.freezeHistory = [{ date: '2026-05-15', kind: 'earned', amount: 1 }];
    localStorage.setItem('user', JSON.stringify(u));
    sessionStorage.setItem('dg.briefings.auto-refresh-tried', '1');
  });
  await page.goto('/');
  await page.locator('#bottomNav button[data-tab-id="settings"]').click();

  const status = page.locator('#freezeStatus');
  await expect(status).toBeVisible();
  await expect(status).toContainText('❄️');
  await expect(page.locator('#freezeHistory')).toContainText('충전');
});
```

> `primeOnboardedUser(schemaVersion: 4)` seed는 production hydrate에서 v10으로 lazy migrate됨 (freezeHistory `[]` 보강) → addInitScript에서 freezeHistory override가 migrate 후 유지되는지 확인. migrate가 이미-array면 보존하므로 OK (migrateUserToV10: `Array.isArray ? 보존 : []`).

- [ ] **Step 2: Run smoke + N=3 cold-fork**

Run: `npm run test:smoke -- settings-freeze && for i in 1 2 3; do rm -rf node_modules/.vite-temp; npm run test:smoke -- settings-freeze 2>&1 | grep -E "passed|failed"; done`
Expected: PASS, 3/3 0 flake.

- [ ] **Step 3: Commit**

```bash
git add tests/smoke/settings-freeze.spec.ts
git commit -m "test(v3.48 T5): smoke — settings ❄️ Streak Freeze 섹션 노출"
```

---

### Task 6: 사이클-끝 검증 + bundle + retro

**Files:** retro/memory only

- [ ] **Step 1: 3종 일괄 + 양쪽 TZ**

Run: `npm run lint && npm test && TZ=America/New_York npm test && npm run test:smoke`
Expected: lint 0 / vitest 양쪽 PASS / smoke PASS.

- [ ] **Step 2: Bundle dual-record**

```bash
npm run build
echo "index:    $(gzip -c dist/assets/index-*.js | wc -c) B"
echo "archive:  $(gzip -c dist/assets/archive-*.js | wc -c) B"
echo "settings: $(gzip -c dist/assets/settings-*.js | wc -c) B"
echo "persistence: $(gzip -c dist/assets/persistence-*.js | wc -c) B"
```
Expected: index Δ noise (settings/state는 lazy/persistence chunk). retro 기록.

- [ ] **Step 3: retro + memory**

design.md §retro 추가 (test Δ, bundle, Codex 2-pass, lessons, carry) + `memory/project_daily_growth_v348.md` + MEMORY.md 한 줄.

---

## Self-Review (writing-plans)

**1. Spec coverage:**
- §3 schema v9→v10 (타입/migration/4 spot/validation) → Task 1 ✓
- §4 write (recordDailyAnswer earned/consumed) → Task 2 ✓
- §5 getFreezeHistory + getNextFreezeEtaDays → Task 2(read) + Task 3(eta) ✓
- §6 settings UI → Task 4 ✓
- §7 검증(단위/smoke/3종/TZ/bundle) → Task 1~3(단위) + Task 4(render) + Task 5(smoke) + Task 6 ✓
- §8 Codex 2-pass → plan 외부(controller dispatch)

**2. Placeholder scan:** 모든 step에 실제 코드/명령. Task 2 Step 3 / Task 5 Step 1의 "확인" 노트는 fixture/migrate 호환 실측 지시(placeholder 아님).

**3. Type consistency:** `FreezeHistoryEntry {date,kind,amount}` (Task1) = write push (Task2) = validation guard (Task1) = `FreezeHistoryView` (Task2) = settings render (Task4). `getNextFreezeEtaDays(u, now)` 시그니처 Task3=Task4 호출 일치. `getFreezeHistory(limit)` Task2=Task4 일치. schemaVersion 10 전 spot 일관.

**알려진 검증 포인트 (Codex 사전 review 집중 요청):**
- migrate chain superset: v2~v9 모든 prior가 v10 도달하는지 (Task1 Step1은 v5/v9만 대표 — Codex가 전 경로 확인).
- mkUser fixture schemaVersion (9 → 10) 갱신 시 다른 spec 영향 (Task2 Step3 실측).
- primeOnboardedUser smoke seed가 v10 migrate 후 freezeHistory override 유지 (Task5).

---

**End of plan.**
